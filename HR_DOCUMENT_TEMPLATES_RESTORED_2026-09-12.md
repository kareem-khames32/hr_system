# استعادة قوالب المستندات العامة — 12 سبتمبر 2026

نُفذ طلب المستخدم بإرجاع قوالب الموارد البشرية العامة مستقلة عن خطابات الطلبات. هذا التقرير يحدّث حالة المستندات والتشغيل الواردة في تقرير المتابعة السابق؛ ما ورد هناك عن انتظار الإذن والتوقف كان وصفًا لمرحلة سابقة.

## الاستخدام

1. من **الإعدادات ← قوالب المستندات** (`/settings/document-templates`) اختر قالبًا جديدًا. تتوفر بدايات: عقد عمل، إقرار، شهادة، نموذج عام. هذه بدايات قابلة للتعديل، وتُحفظ في قاعدة البيانات عند حفظ المستخدم؛ ليست مستندات شركة منشورة تلقائيًا.
2. اكتب أو الصق النص وأضف متغيرات الشركة والموظف والعقد، وحقولًا إضافية عند الحاجة. عقد العمل يتطلب بنود الشركة التي يكتبها المستخدم ولا يختلق شروطًا قانونية. تحفظ المسودة وتُنشر نسخة مستقلة عندما تصبح جاهزة.
3. من **إدارة الموظفين ← إنشاء مستند** (`/employees/documents/create`) اختر القالب المنشور والموظف عند الحاجة وأكمل الحقول. المعاينة لا تحفظ إصدارًا. زر الإصدار يحفظ PDF نهائيًا وسجله؛ مستند الموظف يرتبط تلقائيًا بأرشيفه. يوجد نفس المدخل من تبويب مستندات الموظف.
4. يمكن عرض وتنزيل وطباعة PDF المحفوظ. تغيير القالب أو بيانات الموظف لاحقًا لا يبدل النسخة السابقة. تعطيل القالب يمنع الإصدارات الجديدة ويحفظ السجل القديم.

**قوالب الخطابات** موجودة مستقلة في `/settings/letter-templates`؛ مسار الطلبات والموافقات وربط القوالب المنشورة باقٍ. المرفقات الحالية ومسار رفع النسخة الموقعة ورقيًا باقيان.

المحرر نصي بمتغيرات؛ لا يستورد تنسيق Word/PDF الأصلي ولا يوفّر توقيعًا إلكترونيًا. بيانات الشركة تُقرأ من إعداداتها الحالية؛ البيان غير المسجل المطلوب في النص يظهر كخطأ واضح ولا يُختلق. قراءة متغيرات الراتب المسجل تستخدم الصلاحية الحالية فقط؛ لا يوجد تعديل لمحرك الرواتب.

## التحقق

- **188/188** في [الاختبارات المجمعة](output/hr-documents-full-tests.txt)، دون تخطي. تشمل 9 اختبارات تكامل جديدة للمستندات، و4 لاختبارات المحتوى و3 لترحيل قاعدة البيانات.
- **4/4** اختبارات إضافية للمحرر بعد اكتمال الواجهة، في [سجل اختبارات الواجهة](output/hr-documents-ui-tests.txt). لم تكن هذه الأربعة ضمن تعداد188 السابق.
- API وواجهة الإنتاج نجحا؛ **86 صفحة**، مع TypeScript: [API](output/hr-documents-api-build.txt)، [الواجهة](output/hr-documents-frontend-build.txt).
- تغطي الاختبارات تعارض النسخ، منع تسرب المسودات، اشتراط الحقول المستخدمة، إصدار مستند عام دون موظف، نطاق الفرع الحالي، صلاحية المالية وملكية الموظف عبر مساري تنزيل PDF، إعادة المحاولة المتزامنة دون تكرار، وثبات بايتات النسخة المحفوظة.
- اختبار فشل SQL بعد كتابة PDF أثبت التراجع عن السجلات وحذف الملف الجزئي ثم نجاح نفس مفتاح إعادة المحاولة. هذه حماية للفشل الذي يلتقطه التطبيق؛ لا يدّعي الاختبار معالجة انقطاع كهرباء العملية بالكامل.
- اختبارات SQL/PDF تستخدم قواعد مؤقتة مع بيانات افتراضية. فُحص PDF العربي بصريًا، مع اتجاه صحيح لكود العقد والتاريخ.
- المتصفح المحلي: تسجيل الدخول، فتح القوالب، حفظ مسودة عامة، نشرها، إدخال الحقل، المعاينة، الإصدار، ظهور السجل، ثم فتح PDF المحفوظ نجحت. زر الطباعة عاد دون خطأ ظاهر؛ لم تُتحقق ورقة مطبوعة أو نافذة طابعة النظام من أدوات المتصفح المتاحة. [صورة PDF المحفوظ](output/hr-document-saved-preview.png).
- فُتحت صفحة الخطابات المستقلة وظهرت قوالبها الستة المنشورة. أُعيد فتح محرر المستندات على «قالب جديد» وظهرت البدايات الأربع، مع تأكيد تعطيل قالب الفحص. [صورة صفحة القوالب](output/hr-document-templates-restored.png). فحص التشغيل الأخير أعاد الواجهة HTTP200 وAPI `status: ok, db: up`.

## قاعدة المراجعة والتشغيل

طُبق الترحيل الإضافي **007** على `hr_review_pre_payroll_20260911162404_52ee65ac` بعد وقف API وأخذ نسخة checkpoint احتياطية والتحقق منها. أضاف ثلاثة جداول وأربعة فهارس؛ الترحيلات001–006 تخطاها السجل. فحص TypeORM مطابق، وعدد صفوف الجداول الموجودة لم يتغير أثناء الترحيل. [نتيجة التطبيق](docs/prepayrollmigration/migration-result.json).

أُعيد تشغيل API على `http://localhost:4001/api`، والواجهة الحالية على `http://localhost:3000`. قاعدة المصدر لم تُرحّل، وبيانات الموظفين لم تُعدّل. اختبار المتصفح أضاف قالبًا وإصدارًا عامًا واحدًا باسم **«نموذج مراجعة فنية — غير معتمد»** دون موظف في نسخة المراجعة فقط، ثم عُطّل القالب بعد الفحص. أُبقي الإصدار المحفوظ كدليل مراجعة.

تظل تفاصيل الشروط وقالب المصنع الفعلي من المستخدم. هذا التسليم يعيد مسار إنشاء المستندات ويحفظه؛ لا يعلن فحصًا بصريًا لكل وظائف النظام أو بدء مرحلة الرواتب.
