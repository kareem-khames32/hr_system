# تقرير الفروقات للمبرمج — نظامنا مقابل logic-leap HR
**التاريخ:** 2026-07-14 · **نطاق المقارنة:** كل الموديولات حتى ما قبل الرواتب (مع ملحق رواتب للمعلومية)
**منهجية التدقيق:** فحص حيّ على الإنتاج (`https://hr-backend.logic-leap.ai/api`, جلسة أدمِن مُصادَقة) + قراءة عميقة لكود Laravel المصدري المستنسَخ (5 عملاء تدقيق متوازيين على `app/Modules/*`: Migrations + Services + Actions + Routes).

> **الخلاصة التنفيذية:** نظامهم backend حقيقي وناضج — أقوى موديول عندهم هو **الحضور** ثم **الرواتب** (مبنية بالكامل، مش هيكل). لكن فيه فجوات جوهرية بيغطّيها نظامنا: **استحقاق الإجازة الزمني** (عندهم أعمدة ميتة/واجهة بس)، **الموافقات المتوازية + بانِي حقول الطلب الديناميكي** (عندهم تسلسلي + توجيه بس)، **تنسيق الإخلاء→التصفية المترابط** (عندهم أختام يدوية)، **نقل العهدة + المسؤولية المالية للعهدة الناقصة** (غير موجودة عندهم)، و**محرّك معادلات الرواتب القابل للتحرير** (عندهم منطق مثبّت بمعاملات).

---

## الجزء (أ) — تدقيق كامل لنظامهم (backend حقيقي / شكلي / غائب)

### 1) الحضور Attendance — أقوى موديولاتهم (مبني بالكامل)
**الجداول الحقيقية:** `shifts` (start_time/end_time، break_duration، **grace_period_minutes**، overtime_threshold، **is_overnight**) · `schedule_assignments` (**scope_type**: company/branch/department/team/employee + effective_from/to) + `schedule_assignment_entries` (ربط **shift لكل يوم أسبوع** day_of_week 0..6 + is_off_day) · `schedule_exceptions` (**effect**: off_to_work/work_to_off، **match_type**: specific_date/date_range/**recurring** بـ position: first..fourth/last/every) · `weekly_schedule_entries` (شبكة قديمة لكل موظف) · `work_days` (أسبوع افتراضي 7 صفوف للتينانت) · `attendance_records` (check_in/out، scheduled_start/end snapshot، **late_minutes**، **late_deduction_tier**: none/quarter_day/half_day/full_day، early_departure_minutes، status: present/absent/on_leave/holiday/off_day) · `overtime_records` · `exit_permits` + `permit_types` · `biometric_devices` (ZKTeco فعلي).

**المنطق المحسوب (كله حقيقي — `AttendanceCalculationService`):**
- **حلّ الجدول 4 طبقات** (الأخصّ يفوز): استثناء يومي ← تعيين نطاقي مؤرّخ لليوم ← شبكة قديمة ← أسبوع التينانت الافتراضي. يعمل داخل الكرون/الاستيراد.
- **التأخير مُدرَّج**: `late = scheduled_start.diff(check_in)`؛ ≤ grace → لا شيء، ≤30 → ربع يوم، ≤60 → نصف يوم، >60 → يوم كامل. الـgrace من الوردية.
- **الإذن يقلّل التأخير — نعم، اتجاهي**: إذن معتمد من نوع `late_arrival` بيزحلق نقطة بداية التأخير لوقت العودة (استئذان صباحي داخل النافذة = 0 تأخير). `exit_return` لا يؤثر على التأخير.
- **الانصراف المبكر** + **دقائق العمل** (خصم البريك مرة) + **دعم الوردية العابرة للمنتصف** (`is_overnight`).
- **الأوفرتايم التلقائي** من البصمة (بوابة `overtime_auto_from_biometric` + حد أدنى دقائق) → يفتح دورة موافقة، idempotent.
- **ترسيب الغياب التلقائي** (`MarkEmployeeAbsencesAction` كرون يومي): يكتب `absent` لأيام العمل المجدولة بلا بصمة وبلا إجازة معتمدة، و`holiday` لو اليوم عطلة.
- **خصم الإذن رصيدي** (`ExitPermitService`): سقف `max_duration`، بدل شهري بالدقائق/بالعدد، `deduction_rate`، مع **إسقاط الخصم `waive-deduction`**.
- **كل مسارات الكتابة** (self-service، إدخال يدوي، بصمة) تمرّ على نفس محرّك الحساب → أرقام متسقة للرواتب. تقارير يومية/شهرية بتجميع SQL حقيقي.

**غير موجود عندهم:** ورديات مرنة/عائمة (نوافذ ثابتة فقط) · تقسيم grace اتجاهي (grace واحد) · تبديل رمضان/موسمي تلقائي (مجرد comment في migration).

### 2) الإجازات Leave — رصيد ثابت (مفيش محرّك استحقاق)
**الجداول:** `leave_types` (annual_days، is_paid، **deducts_from_balance**، **salary_effect** paid/unpaid/partial، max_consecutive، carry_over_days، applicable_gender) · `leave_balances` (entitled/used/adjusted/carried، فريد لكل employee×type×year؛ **الرصيد المتبقي accessor محسوب مش عمود**) · `leave_requests` (pending/approved/rejected/cancelled + lot_consumption JSON) · `holidays` · `leave_opening_balances` (**دفتر FIFO** للأرصدة الافتتاحية/الترحيل اليدوي).

**⛔ الاستحقاق — الإجابة الحاسمة: ثابت/مقدَّم بالكامل، مفيش محرّك.** الرصيد يُمنح مرة واحدة عند إنشاء الموظف (`LeaveBalanceService::initializeForEmployee`)، مع **تقسيم نسبي للتعيين في نفس السنة فقط** (`(annual/12)×الأشهر المتبقية`). أعمدة `accrual_method` (monthly/yearly/daily) و`accrual_start` **ميتة تمامًا**: تُكتب وتُتحقّق وتُرجَّع للواجهة، لكن **صفر كود بيقراها** — مفيش job/command/scheduler للاستحقاق (الـscheduler فيه فقط biometric:sync + attendance:mark-absent + backups).
- **الخصم/الاسترجاع حقيقي وقوي**: يُخصَم عند الاعتماد فقط (قفل تشاؤمي + حارس idempotency)؛ يُسترجَع عند إلغاء/رفض المعتمد.
- **الترحيل السنوي (carry-over) غير مُنفَّذ آليًا**: مفيش كود دوران آخر السنة؛ `carry_over_policy`/`carry_over_max_days` **ميتة**؛ الترحيل يدوي عبر endpoint واحد بلا سقف.
- **نصف يوم غير مدعوم** (أيام كاملة فقط). المرفقات + منع التداخل + تحذير التعارض (غير حاجب) موجودة.
- **الإجازة لا تكتب حضورًا ولا تعلّم الأيام**: منفصلة عن الحضور؛ الجسر الوحيد أن الرواتب تقرأ الإجازة غير المدفوعة المعتمدة (وتحسبها **أيام تقويمية** لا أيام عمل — تعارض داخلي عندهم).

### 3) الاعتماد Approval — محرّك متعدد الخطوات قابل للتكوين (تسلسلي فقط)
**الجداول:** `request_types` (key فريد، is_system) · `approval_routes` (request_type_id + **branch_id** لكل فرع) · `approval_steps` (order، target_type: user/role/**direct_manager/department_manager/team_manager/hr_manager/branch_manager/job_title** + **fallback_user_id** تصعيد) · `approval_requests` (subject polymorphic، **payload JSON**، status، current_step_order) · `approval_actions` (سجل تدقيق) · `request_type_visibilities` (**من يحقّ له التقديم** بالنطاق).
- **التوجيه لكل نوع + لكل فرع**؛ المدير يُحلّ ديناميكيًا من موقع الموظف وقت القرار؛ تصعيد fallback؛ رؤية بالنطاق؛ حدث `ApprovalFinalized` واحد يشترك فيه **12 موديول**.
- **الأدمِن يقدر ينشئ نوع طلب جديد** (`POST approvals/request-types`) — **لكن مفيش بانِي حقول/نموذج ديناميكي**: بيانات الطلب في `payload` JSON حرّ بلا schema ولا تحقّق، وكل موديول بيقرأ مفاتيحه بالكود. فالنوع الجديد = **توجيه فقط**، مش نموذج إدخال.
- **13 نوع نظامي**: leave, overtime, exit_permit, recruitment, advance, loan, resignation, salary_increase, asset_assignment, asset_return, settlement, promotion, termination. (كلها is_system؛ لا يوجد نوع مخصّص في الإنتاج.)
- **قيود جوهرية**: **تسلسلي فقط** (لا متوازٍ/نصاب) · **الرفض نهائي** (يُنهي الطلب، لا إعادة للتعديل ولا send-back) · **لا صلاحيات تعديل لكل خطوة** · لا تفويض/SLA/اعتماد تلقائي · التوجيه **بالفرع فقط** (لا توجيه بالقسم) · seeders مكرّرة متعارضة.

### 4) دورة حياة الموظف Employee — مسنودة بالكود (لكن الإخلاء غير مُنسَّق)
**الجداول:** `employees` (هوية كاملة + **emergency_contact** + بنك/iban + probation_end/months + **status**: active/probation/archived/suspended + archived_at/archive_reason + **last_working_day** + **termination_reason**) · `employee_contracts` (fixed/open_ended، notice_period_days، renew/terminate) · `employee_settlements` + `settlement_lines` · `profile_update_requests` (تعديل بيانات بموافقة HR) · جداول مؤهلات (education/certifications/skills/languages/experiences).
- **التعيين حقيقي** (عقد + بذر رصيد إجازة). **إيقاف/تفعيل** عكسي حقيقي. **الأرشفة (الخروج)** ببوابة **إخلاء عهدة صارمة** (`ClearanceService::assertCleared` → 409 لو فيه عهدة غير مُعادة/مُسقطة). **الاستعادة** حقيقية.
- **⛔ الاستقالة**: طلب حقيقي يمشي في المحرّك؛ عند الاعتماد `ApplyResignationOnApprovalDecision` **يختم بس** `last_working_day` + `termination_reason`. **دي كل الأثر التلقائي** — مفيش توليد تصفية تلقائي، مفيش أرشفة تلقائية، مفيش فرض فترة إنذار، مفيش checklist تسليم. (docblock نفسه: «التصفية→الإخلاء→الأرشفة تبقى تدفّق HR يدوي من ملف الموظف».)
- **التصفية (EOS) حقيقية**: `SettlementService` يولّد بنودًا — **مكافأة نهاية خدمة سعودية** (½ شهر/سنة أول 5 سنوات، شهر كامل بعدها، نسبي) + **قيمة رصيد الإجازة نقدًا** + **القروض القائمة كخصم** + بنود يدوية؛ تمرّ في الموافقة؛ **تتوقف عند approved** (لا خطوة صرف/GL داخل الموديول).
- **البروبيشن خامل** (قيمة status بلا انتقال تلقائي). **انتهاء العقد تقرير فقط** (لا job).

### 5) العهدة Asset — تتبّع خفيف (لا نقل، لا دفتر، لا مسؤولية مالية)
`asset_types` (كتالوج أنواع + requires_approval) · `employee_assets` (status: **assigned/returned فقط**، identifier نص حرّ) · `asset_clearance_waivers`.
- **التسليم/الإرجاع حقيقي لكن سطحي** (تقليب status على **صف قابل للتعديل** — مفيش حدث دفتر منفصل)؛ يمرّ اختياريًا في الموافقة؛ **عكس القرار عند الرفض** حقيقي؛ **بوابة الإخلاء** حقيقية.
- **⛔ غير موجود إطلاقًا**: **نقل العهدة بين موظفين** · **دفتر حركة غير قابل للتعديل** · **تتبّع الحالة** (سليم/تالف) · **الفقد/التلف + المسؤولية المالية/استرداد التكلفة** · مخزون أصول مُقوَّم (أنواع + أرقام نصية فقط).

### 6) الوثائق Document — تخزين حقيقي (بلا نسخ/تذكير/PDF)
`documents` (employee_id، type، file، **expiry_date**، metadata) · `document_templates` (body، توليد بإحلال `{{placeholder}}`).
- **التخزين حقيقي** (disk فعلي، download، scoped للموظف). **⛔ ضعف**: لا **تعدُّد نسخ** · مرفق **للموظف فقط** (لا شركة/كيان) · **تتبّع الانتهاء pull فقط** (`documents/expiring?days=N`) بلا **تذكير/إشعار مجدول** · التوليد **ملف .txt** (لا PDF/DOCX).

### 7) التقويم Calendar — مُجمِّع للقراءة + iCal (التكرار stub)
`calendar_events` (polymorphic، recurrence_rule). **حقيقي**: `CalendarService::getEvents` يدمج 6 مصادر (أحداث/إجازات معتمدة/مقابلات/تدريب/دورات تقييم/انتهاء عقود) بنطاق حسب الدور؛ **تصدير iCal RFC-5545** حقيقي. **stub**: توسيع الأحداث المتكرّرة غير مُنفَّذ. **تعارض**: مخزنان للعطلات (calendar_events مقابل Holiday في موديول الإجازات — الحضور يحترم الثاني فقط).

### 8) الإعدادات Setting — طبقة CRUD واسعة حقيقية (+ عنصران شكليان)
حقيقي: branches · departments (**هرمي** parent_id + ربط cost_center) · teams · job_titles · grades (شرائح رواتب min/max) · cost_centers · **lookup_options هرمية** · company_info + **إعدادات إقليمية** (currency/timezone/calendar/week_start/weekend_days) · **work_days** (الأسبوع الافتراضي — **مقروء فعليًا من الحضور**).
**⛔ شكلي/stub**: `policies` (rules JSON **لا يُقيَّم بأي محرّك**) · `work_schedules` (جداول مسمّاة + `employees.work_schedule_id` تُخزَّن لكن **الحضور لا يقرأها** — الجدولة الفعلية عبر schedule_assignments؛ «رمضان» مجرد مثال في migration).

### 9) الرواتب Payroll (ملحق — مبنية بالكامل، للمعلومية)
> المقارنة تقف قبل الرواتب، لكن دي معلومة مهمة لخارطة طريقك: **رواتبهم مبنية بالكامل production-grade** (مش هيكل).
- **دورة**: draft → calculate → review → approve → pay. `PayrollCalculationService` يقرأ الحضور/الإجازة/الأوفرتايم/الأذونات فعليًا ويحسب كل شيء (شرائح خصم التأخير، الانصراف المبكر، خصم الغياب، دقائق الإذن، أوفرتايم بمضاعف، تناسب التعيين، بدلات، مكافآت).
- **GOSI حقيقي** (سعودي: نسبة موظف+شركة؛ غير سعودي: خطر فقط) قابل للتكوين.
- **قروض وسُلف منفصلة** (`kind` advance/loan على جدول واحد): سلفة = قسط واحد بسقف %؛ قرض = 2..60 قسط بسقف مضاعف الأساس، جدول أقساط، تفعيل عند الاعتماد، **ترحيل جزئي للقسط**.
- **تعويضات مُوجَّهة بالنطاق** `scoped_compensations` (company/branch/department/team/employee، earning/deduction) + `bonuses` + `payroll_entry_lines` (تفصيل مبوّب) + **تصدير WPS/GOSI/كاش** + لقطة كاش/بنك.
- **الفجوة الوحيدة بنيويًا**: **لا محرّك معادلات/تعبيرات قابل للتحرير** — `payroll_formulas` مخزن key-value لمعاملات رقمية، والمنطق **مثبّت بالكود**.

---

## الجزء (ب) — زياداتك أنت اللي **مش موجودة (أو أضعف) عندهم** — قائمة بناء للمبرمج
> كل بند مسنود بأدلة من كودنا (`api/src` + `src/app`). دي «الفروقات لصالحنا» اللي طلبت أوثّقها للمبرمج.

### ب-1) بانِي الطلبات الديناميكي بحقول مخصّصة + جمهور — عندنا / توجيه فقط عندهم
- **عندنا:** `RequestType` بيخزّن **schema حقول كامل** JSON: `customFields=[{key,label,type: text/number/date/select/file, required, options}]` + جمهور `visibleTo{mode:all/departments/roles/employees, ids}` + `requiredAttachments` + `destinationHandler` + سلسلة اعتماد لكل نوع. الأدمِن ينشئ نوعًا من الصفر بحقوله ونموذجه وجمهوره ويتحقّق من الإدخال وقت التقديم (`api/src/requests/entities/request-type.entity.ts:30-53`, `api/src/requests/requests.service.ts:82-115,172-213`, واجهة `src/app/settings/request-types/page.tsx`).
- **عندهم:** يقدر ينشئ نوعًا، لكن **بلا بانِي حقول** — البيانات في `payload` JSON حرّ بلا schema ولا تحقّق، وكل موديول يقرأ مفاتيحه بالكود (`request_types` بلا جداول حقول).
- **الفرق للمبرمج:** ابنِ **مُصمِّم حقول ديناميكي** (field schema + validation) + **جمهور بالنطاق** فوق نظام الأنواع عندهم.

### ب-2) موافقات **متوازية** + خطوات **شرطية بالعتبة** + إعادة للتعديل — عندنا / تسلسلي ورفض نهائي عندهم
- **عندنا:** خطوات بنفس `stepOrder` = **مجموعة متوازية، الكل لازم يوافق وأي رفض يرفض** (`requests.service.ts:635-712`). **خطوة شرطية**: تنشط فقط لو `payload[field] <op> value` (مثلًا مبلغ ≥ 5000 → المالية) (`requests.service.ts:612-630`). **إعادة للتعديل** `RETURN → RETURNED_FOR_INFO → resubmit` (`requests.service.ts:780-800`). **SLA + تصعيد** لكل خطوة + **تجاوز فرعي** (سلسلة بنفس الكود وبفرع تغلب العامّة). استهداف المعتمد: مدير مباشر/قسم/فرع/فريق مستقبِل + أدوار وظيفية + موظف بعينه.
- **عندهم:** **تسلسلي فقط**، **الرفض نهائي** (يُنهي الطلب — لا send-back)، لا خطوات شرطية بالعتبة، تصعيد fallback فقط، التوجيه **بالفرع فقط**.
- **الفرق للمبرمج:** أضِف **مجموعات موافقة متوازية (نصاب)**، **خطوات مشروطة بقيمة الحقل**، و**إرجاع-للتعديل** بدل الرفض النهائي.

### ب-3) تنسيق الإخلاء→التصفية **المترابط تلقائيًا** — عندنا / أختام يدوية عندهم
- **عندنا:** اعتماد الاستقالة → `status='notice_period'` + **يفتح `OffboardingCase` تلقائيًا (IN_CLEARANCE) بـ5 بنود إخلاء** (المدير/العهدة/IT/المالية/HR)؛ **بند العهدة محجوب طالما ماسك عهدة**؛ آخر بند يكتمل → **يبني التصفية تلقائيًا** ويحوّل لـIN_SETTLEMENT → اعتماد → SETTLED → **finalize (إنهاء+أرشفة+تعطيل الدخول)** فورًا أو بكرون يومي، مع **سحب الاستقالة** خلال الإنذار (`api/src/requests/destinations.service.ts:410-486`, `api/src/offboarding/offboarding.service.ts:128-398,457-565`).
- **عندهم:** الاستقالة عند الاعتماد **تختم بس** `last_working_day` + `termination_reason`؛ التصفية والإخلاء والأرشفة **خطوات HR يدوية منفصلة غير مترابطة** (docblock صريح عندهم).
- **الفرق للمبرمج:** اربط الاستقالة → **فتح حالة إخلاء تلقائي ببنود** → **بناء تصفية تلقائي** → **finalize مؤتمت** (بوابة عهدة + كرون).

### ب-4) دفتر مديونيات الموظف **→ خصم راتب** + مسؤولية العهدة المالية — عندنا / غائبة عندهم
- **عندنا:** عهدة مفقودة → تُعلَّم `LOST` + **يُقيَّد `EmployeeObligation` DEBIT** `custody_shortfall` PENDING بقيمة الأصل؛ **الرواتب تستهلك المديونيات PENDING** (DEBIT→خصومات أخرى، CREDIT→إضافات أخرى) وتُعلّمها `APPLIED` عند الصرف فقط (استهلاك مرة واحدة) مع **منع الازدواج** مقابل التصفية (`api/src/requests/entities/financial.entities.ts:56-109`, `destinations.service.ts:674-718`, `api/src/payroll/payroll.service.ts:328-343,442-448`).
- **عندهم:** العهدة **بوابة إخلاء فقط** (لازم تُعاد قبل التصفية) — **لا مسؤولية مالية، لا فقد/تلف، لا دفتر مديونيات، لا خصم راتب** من عهدة ناقصة.
- **الفرق للمبرمج:** أضِف **دفتر مديونيات عام (DEBIT/CREDIT، PENDING→APPLIED)** يستهلكه المسير، واربط **قيمة العهدة المفقودة** به.

### ب-5) محرّك استحقاق الإجازة **الشهري** + الترحيل بسقف/انتهاء — عندنا / رصيد ثابت عندهم
- **عندنا:** الاستحقاق **شهري محسوب**: `min(entitled, months × entitled/12)` من `max(بداية السنة, الانضمام+البروبيشن)` (`api/src/requests/leave-balances.service.ts:47-75`)؛ طبقتا رصيد (المُرحَّل يُستهلك أولًا)؛ **ترحيل سنوي** بسقف `carryover_max_days` وانتهاء `carryover_expiry_months`.
- **عندهم:** **مِنحة مقدَّمة ثابتة**؛ أعمدة `accrual_method`/`accrual_start` **ميتة (صفر كود يقراها)**؛ الترحيل يدوي بلا سقف.
- **الفرق للمبرمج:** فعِّل **محرّك استحقاق زمني حقيقي** يقرأ `accrual_method`، و**دوران سنوي آلي** بسقف/انتهاء.

### ب-6) الحضور: **شرائح تأخير قابلة للتحرير** + **نوافذ فتح/قفل أوفرتايم بالتاريخ** — عندنا / مثبّتة عندهم
- **عندنا:** `LatenessTier(fromMinutes,toMinutes,mode:FRACTION/MINUTES,value)` **يحرّرها الأدمِن** من شاشة «معادلات الرواتب» (`api/src/payroll/payroll-rules.entities.ts`, `payroll.service.ts:65-81`)؛ و**`OvertimePeriod(fromDate,toDate,OPEN/CLOSED,branchId)`** لفتح/قفل الأوفرتايم بنوافذ تاريخية (CLOSED تغلب) مع حماية الأوفرتايم المُعتمد مسبقًا (`attendance.entities.ts:232-254`, `attendance.service.ts:342-353`).
- **عندهم:** شرائح خصم التأخير **مثبّتة بالكود** (≤30 ربع، ≤60 نصف، >60 يوم) غير قابلة للتحرير؛ الأوفرتايم **بحدّ ثابت + موافقة** بلا **نوافذ فتح/قفل بالتاريخ**.
- **ملاحظة إنصاف:** التغطية الاتجاهية للإذن (صباحي يعفي التأخير) وترسيب الغياب التلقائي **موجودان عند الطرفين**.
- **الفرق للمبرمج:** حوّل شرائح التأخير لكيانات **قابلة للتحرير**، وأضِف **فترات أوفرتايم بالتاريخ**.

### ب-7) **نقل العهدة المباشر** بين الموظفين — عندنا / غير موجود عندهم
- **عندنا:** `transferCustody()` يقفل تخصيص الحائز كـ`TRANSFERRED` ويفتح **`PENDING_ACK` للمستلم** (إقرار المستلم + تأكيد مديره)، مع حالة `TRANSFERRED` مخصّصة ومسار طلب `custody_transfer` (`requests.service.ts:844-896`, `destinations.service.ts:595-634`).
- **عندهم:** **لا نقل إطلاقًا** — إعادة يدوية + تخصيص جديد فقط؛ ولا دفتر حركة غير قابل للتعديل.
- **الفرق للمبرمج:** أضِف **نقل عهدة مباشر** بحالة انتقالية ودورة إقرار/تأكيد.

### ب-8) (ملحق رواتب) محرّك **معادلات قابل للتحرير** — قيد البناء عندنا / منطق مثبّت عندهم
- **عندنا (قيد البناء):** مُقيّم معادلات آمن (بلا eval) + كتالوج متغيرات + شرائح — يخلّي بنود الاستحقاق/الخصم **يعرّفها المستخدم كصيغة** (`api/src/payroll/safe-formula.ts`, `payroll-variables.ts`).
- **عندهم:** المنطق **مثبّت بالكود** ومُعامَل بجدول `payroll_formulas` key-value (لا تعبيرات يحرّرها المستخدم).
- **ملاحظة:** بخلاف ده، **رواتبهم أنضج حاليًا** (انظر الجزء ج).

---

## الجزء (ج) — الورديات والجدول الأسبوعي **بالتفاصيل** (المقارنة اللي طلبتها تحديدًا)

### الورديات Shifts
| العنصر | نظامنا | logic-leap |
|---|---|---|
| نموذج الوردية | بداية/نهاية + **grace لكل وردية** + عتبة أوفرتايم | بداية/نهاية + **grace لكل وردية** + break + overtime_threshold |
| وردية عابرة للمنتصف | مدعومة | مدعومة (`is_overnight`) |
| **نمط مرن/عائم** | **مدعوم (fixed/flexible)** | **غير مدعوم — نوافذ ثابتة فقط** |
| grace اتجاهي (صبح/مساء) | grace واحد | grace واحد |
| مثال حيّ عندهم | — | «دوام الرياض 09:00→18:00 grace15 break60 OTthr60» · «الصباحية 08:00→17:00 grace15 break60 OTthr30» |

### الجدول الأسبوعي Weekly Schedule
- **عندهم (قوي ومُحكم):** جدول **بالنطاق** `schedule_assignments` (company/branch/department/team/**employee**) + `schedule_assignment_entries` **وردية لكل يوم أسبوع** + `is_off_day`، مع **صلاحية مؤرّخة** `effective_from/to`. الحلّ **4 طبقات بأولوية** (استثناء يومي ← تعيين نطاقي الأخصّ والأحدث بداية ← شبكة قديمة لكل موظف ← أسبوع التينانت الافتراضي). مثال حيّ: قسم#3 له جدول أسبوع «05→09 يوليو: دوام الرياض» ثم أسبوع «12→16 يوليو: الصباحية»، وموظف#4 مفتوح المدى بالسبت **دوام**. الأسبوع الافتراضي للتينانت: أحد–خميس 08→17، جمعة/سبت راحة.
- **عندنا:** **جدول مؤرّخ** + **ورديات لكل موظف** تشتقّ الويك-إند والساعات + **استثناءات أيام** (مثل «آخر سبت = دوام») + **نوافذ أوفرتايم بالتاريخ**. النمط المرن مدعوم.
- **من الأقوى في كل جزء:** *بنية دقّة الجدولة النطاقية المؤرّخة* أوضح وأكثر تشكيلًا عندهم (5 مستويات نطاق بأولوية صريحة)؛ *نوافذ الأوفرتايم بالتاريخ* و*النمط المرن* و*شرائح التأخير القابلة للتحرير* لصالحنا. الاستثناءات المتكرّرة («آخر سبت») **موجودة عند الطرفين**.
- **توصية للمبرمج:** لو عايز تطابق أفضل مستوى، تبنّى **نموذج التعيين النطاقي المؤرّخ متعدد المستويات مع أولوية حلّ صريحة** زيّهم، مع الحفاظ على نوافذ الأوفرتايم والنمط المرن والشرائح القابلة للتحرير عندنا.

---

## الجزء (د) — **مميزاتهم اللي مش عندنا (أو ناقصة)** — فجواتنا بأمانة للمبرمج
> نظامهم **أوسع** في الموديولات المكتملة؛ نظامنا **أعمق** في نواة العمليات. دي فجواتنا الحقيقية:

| # | القدرة | حالتنا | حالتهم | ملاحظة للمبرمج |
|---|---|---|---|---|
| د-1 | **التوظيف ATS** | **جزئي** (مرحلة مرشّح كنص فقط، بلا كيانات إعلانات/مقابلات/عروض) | **كامل** (إعلانات→مقابلات→عروض→تحويل لموظف) | ابنِ كيانات job_postings/interviews/job_offers + تحويل مرشّح→موظف |
| د-2 | **تقييم الأداء** | **غائب** (شاشات mock) | **كامل** (دورات + قوالب كفاءات + أهداف + **تقييم بأوزان**) | موديول جديد كامل |
| د-3 | **التدريب/LMS** | **غائب** (mock) | **كامل** (دورات→تسجيل→**شهادات تلقائية** + تحليلات فجوة المهارات) | موديول جديد كامل |
| د-4 | **مراسلات داخلية** | **غائب** | **موجود** (محادثات/رسائل، in-app بالـpolling) | موديول محادثات |
| د-5 | **بانِي تقارير مخصّص + تصدير** | **جزئي** (لوحات وتقارير جاهزة حقيقية؛ البانِي المخصّص mock بلا تصدير) | **كامل** (بانِي آمن + CSV/Excel/**PDF**) | فعِّل حفظ/تشغيل التقرير المخصّص + مصدّرات |
| د-6 | **تقوية الدخول** | **جزئي** (bcrypt + **إبطال توكِن فوري** بـtokenVersion؛ بلا 2FA/LDAP/قفل حساب/سياسة كلمة سر) | **2FA (TOTP) + LDAP/AD + قفل بعد 5 محاولات** (سياسة كلمة السر ضعيفة عندهم أيضًا) | أضِف 2FA + قفل الحساب (LDAP اختياري) |
| د-7 | **تعدُّد المستأجرين** | **غائب** (شركة واحدة، عزل بالفرع) | **كامل** (SaaS بعمود tenant_id + حارس تسرّب) | قرار معماري — لو هتبيعه SaaS |
| د-8 | **GOSI/التأمينات** | **غائب** (placeholder) | **حقيقي** (سعودي/غير سعودي قابل للتكوين) | مطلوب لسوق السعودية |
| د-9 | **تصدير WPS/ملف بنك** | **غائب** (تقرير كاش/تحويل فقط) | **موجود** (WPS/GOSI/كاش) | أضِف مصدّر WPS لحماية الأجور |
| د-10 | **الوثائق: توليد PDF** | **جزئي** (تتبّع انتهاء موجود؛ توليد PDF مؤجَّل) | **جزئي** (قوالب تُولّد **.txt** لا PDF — الطرفان ناقصان PDF) | أضِف توليد PDF فعلي |
| د-11 | **كيان العقود + تجديد** | **جزئي** (حقول على الموظف بلا تاريخ تجديد) | **كامل** (كيان عقد + تجديد/إنهاء + عقد نشط واحد) | كيان عقود مستقل بتاريخ |
| د-12 | **مؤهلات الموظف** | **غائب** | **موجود** (تعليم/شهادات/مهارات/لغات/خبرات) | 5 جداول مؤهلات |
| د-13 | **تقويم iCal export** | **جزئي** (تجميع بلا ics) | **كامل** (تصدير RFC-5545) | أضِف مصدّر .ics |
| د-14 | **إشعارات بريد/SMS** | **جزئي** (in-app مُشتقّة فقط) | **مبني** (بريد/Twilio — لكن غير مربوط غالبًا عندهم) | أضِف قناة بريد/SMS مربوطة بالأحداث |

**تعادل تقريبي (الطرفان أقوياء):** إعدادات المؤسسة الهرمية · بوابة الحضور/البصمة ZKTeco · دورة موافقات متعددة الخطوات · تصفية نهاية الخدمة · تتبّع انتهاء الوثائق.

---

## الجزء (هـ) — مصفوفة القرار السريعة
| المحور | الأعمق/الأقوى | لماذا |
|---|---|---|
| مرونة الطلبات والموافقات | **نحن** | حقول ديناميكية + متوازي + شرطي + إرجاع-للتعديل مقابل توجيه تسلسلي |
| تنسيق الإخلاء→التصفية | **نحن** | مؤتمت مترابط مقابل أختام يدوية |
| العهدة (نقل + مسؤولية مالية + دفتر) | **نحن** | نقل مباشر + مديونية→راتب مقابل بوابة إخلاء فقط |
| استحقاق الإجازة الزمني | **نحن** | محرّك شهري + ترحيل مقابل أعمدة ميتة |
| شرائح التأخير القابلة للتحرير + نوافذ أوفرتايم | **نحن** | كيانات يحرّرها المستخدم مقابل ثوابت كود |
| دقّة الجدولة النطاقية المؤرّخة | **هم** | 5 مستويات نطاق بأولوية حلّ صريحة |
| نضج الرواتب (GOSI + WPS + قروض/سُلف + تعويضات نطاقية) | **هم حاليًا** | مبني بالكامل مقابل قيد البناء عندنا (لكن معادلاتنا قابلة للتحرير) |
| اتساع الموديولات (توظيف/أداء/تدريب/مراسلات/تقارير مخصّصة) | **هم** | مكتملة مقابل غائبة/mock عندنا |
| تقوية الدخول (2FA/LDAP/قفل) + تعدُّد المستأجرين | **هم** | موجودة مقابل غائبة عندنا |

---

## الجزء (و) — الخلاصة والتوصية
1. **قصّتك للسوق:** نظامك **أعمق في نواة العمليات اليومية** (طلبات/موافقات/حضور/عهدة/إخلاء/استحقاق) — دي اللي بتفرّق في التشغيل الحقيقي. نظامهم **أوسع في الموديولات الجانبية** (توظيف/أداء/تدريب/تقارير/SaaS).
2. **لو الهدف تكافؤ تنافسي**، رتّب البناء: **(GOSI + WPS)** [ضروري للسعودية] ← **تقوية الدخول (2FA + قفل)** ← **بانِي التقارير المخصّص + تصدير** ← **كيان عقود + مؤهلات** ← ثم الموديولات الأوسع (أداء/تدريب/توظيف الكامل/مراسلات) حسب طلب عملائك.
3. **حافظ على تفوّقك** في: بانِي الطلبات، الموافقات المتوازية/الشرطية، تنسيق الإخلاء، دفتر المديونيات، الاستحقاق الشهري، ومحرّك المعادلات القابل للتحرير — دي **مش موجودة عندهم** وهي ميزتك الحقيقية.

---

### ملاحظة تنفيذ التدقيق (شفافية)
- **الفحص الحيّ** تمّ للقراءة فقط على `hr-backend.logic-leap.ai/api` بجلسة الأدمِن؛ **لم تُنشأ أي بيانات على إنتاجهم** (بدأت موظف تجريبي في المعالج ثم أغلقته دون حفظ = لا سجل في الباك — تحقّقت: لا يوجد أي موظف باسم/بريد الاختبار). **لا يوجد ما يُنظَّف.**
- الأرقام الحيّة وقت التدقيق: 7 موظفين · 2 ورديات · 3 تعيينات جدول · 8 أنواع إجازة · 13 نوع طلب · 5 طلبات اعتماد · 2 فرع · 12 وثيقة.
- **حدود:** لم أختبر تجربة **يوزر الموظف** بتسجيل دخول منفصل (قاعدة أمان تمنعني من كتابة كلمات المرور في النماذج)؛ عوّضتها بتدقيق مسارات الخدمة الذاتية `/me/*` من الكود المصدري مباشرة.




---

# الجزء (ز) — الفرق المفصّل option-by-option (الطلبات + الإعدادات)
> جرد حصري لكل خيار في النظامين عبر workflow من 6 عملاء (قراءة كود المصدر للطرفين). ⚑ = موجود عندهم لكنه شكلي/غير مُستهلَك بالكود.

## الإعدادات — فرق مفصّل (option-by-option)

> مصدر المقارنة: جردٌ حصري لكلا النظامين. «عندنا» = نظام HR (NestJS)، «عندهم» = منافس Laravel (logic-leap). «شكلي عندهم» = موجود في الـDB/الواجهة لكنه غير مُستهلَك بأي محرك.

### 1) الجدول التفصيلي (صفّ لكل خيار)

| المجال | الإعداد/الخيار | عندنا | عندهم | لصالح مين |
|---|---|---|---|---|
| الفروع | كود الفرع (`code` فريد) | ✅ | ❌ | **لصالحنا** |
| الفروع | البريد الإلكتروني للفرع | ✅ | ❌ | **لصالحنا** |
| الفروع | مركز تكلفة على الفرع نفسه | ✅ `costCenter` | ❌ (على القسم فقط) | **لصالحنا** |
| الفروع | علم المقر الرئيسي `isHeadquarters` | ✅ | ❌ | **لصالحنا** |
| الفروع | عطلة أسبوعية خاصة بالفرع | ✅ على الكيان والمحرك — **غير مُتاح في الواجهة** (خارج DTO) | ❌ | لصالحنا (جزئي) |
| الفروع | مدير الفرع | ✅ (موظف) | ✅ (مستخدم) | متعادل |
| الفروع | اسم/عنوان/مدينة/هاتف | ✅ | ✅ | متعادل |
| الأقسام | كود القسم | ✅ | ❌ | **لصالحنا** |
| الأقسام | تشجير هرمي `parentId` | ✅ | ✅ (عمق غير محدود) | متعادل |
| الأقسام | ربط مركز تكلفة بالقسم | ❌ | ✅ `cost_center_id` | لصالحهم |
| الأقسام | إلزامية مدير القسم | اختياري | إلزامي | متعادل (فرق تصميم) |
| الفرق | كود الفريق | ✅ | ❌ | **لصالحنا** |
| الفرق | اسم إنجليزي `name_en` | ❌ | ✅ | لصالحهم |
| المسميات الوظيفية | وصف `description` | ❌ | ✅ | لصالحهم |
| الدرجات | مستوى رقمي `level` | ❌ | ✅ | لصالحهم |
| الدرجات | حد أدنى/أقصى للراتب (استرشادي غير مُلزِم) | ✅ | ✅ | متعادل |
| مراكز التكلفة | نطاق فرع للمركز | ❌ | ✅ `branch_id` (null=عام) | لصالحهم |
| القوائم المنسدلة | إدارة قوائم (جنسية/دولة/عملة/مدينة/منطقة — هرمية) | ❌ | ✅ `LookupOption` (5 أنواع) | لصالحهم |
| الورديات | اسم إنجليزي للوردية | ❌ | ✅ | لصالحهم |
| الورديات | مدة الراحة `break_duration_minutes` | ❌ | ✅ | لصالحهم |
| الورديات | علم وردية ليلية `is_overnight` | ❌ | ✅ | لصالحهم |
| الورديات | نمط مرن `flexible` + ساعات مطلوبة | ✅ | ❌ (أوقات ثابتة فقط) | **لصالحنا** |
| الورديات | نوافذ تصنيف البصمة `checkin/checkout From/To` | ✅ | ❌ | **لصالحنا** |
| الورديات | تجاوز سماح/عتبة أوفرتايم لكل وردية | ✅ | ✅ | متعادل |
| **شرائح خصم التأخير** | شرائح قابلة للتعريف (نطاق دقائق + نمط `FRACTION/MINUTES` + قيمة) بواجهة إدارية | ✅ `LatenessTier` (CRUD) | ❌ **ثابتة بالكود** (grace/≤30/≤60/>60) | **لصالحنا (كبير)** |
| **معامل عقوبة الغياب** | قابل للتعريف (1 / 1.5 / 2) | ✅ `attendance.absence_penalty_days` | ❌ **ثابت بالكود** | **لصالحنا** |
| السماح العام `grace` | قيمة عامة + تجاوز بالوردية | ✅ | ✅ (لكل يوم + بالوردية) | متعادل |
| كشف الأوفرتايم | عتبة (ساعات/دقائق) عامة + بالوردية | ✅ | ✅ | متعادل |
| مضاعف الأوفرتايم | أيام العمل | ✅ 1.5 | ✅ 1.5 | متعادل |
| مضاعف الأوفرتايم | نهاية الأسبوع | ✅ منفصل | جمعة فقط | لصالحنا (جزئي) |
| مضاعف الأوفرتايم | العطلة الرسمية (مُعامل منفصل) | ✅ `multiplier_holiday`=2 | ❌ | **لصالحنا** |
| الأوفرتايم | تشغيل/إيقاف عام + نوافذ تواريخ فتح/إغلاق مُوجّهة بالفرع | ✅ `overtime.enabled` + `OvertimePeriod` | ❌ | **لصالحنا** |
| الأوفرتايم | اشتراط تأكيد المدير للـOT من البصمة | ✅ | ✅ | متعادل |
| أيام العمل الأسبوعية | تعريف اليوم/ساعاته/سماحه **لكل يوم** | جزئي (CSV عطلة + ساعات افتراضية) | ✅ 7 صفوف بساعات وسماح لكل يوم | لصالحهم (جزئي) |
| جداول العمل المسماة | **مُستهلَكة فعليًا بالمحرك** | ✅ `WorkSchedule` مُسند للموظف ويُقرأ | ⚑ **شكلي** — لا يقرأها المحرك (يعتمد `schedule_assignments`)؛ `rules` JSON لا يُقيَّم أبدًا | **لصالحنا** |
| الجدولة بالنطاق | إسناد ورديات بنطاق شركة/فرع/قسم/فريق بمدى تواريخ | ❌ (لدينا أسبوعي لكل موظف + override يومي) | ✅ `ScheduleAssignment` (الأكثر تخصيصًا يفوز) | لصالحهم |
| استثناءات الجدول | قلب يوم إجازة↔عمل | ✅ `ScheduleExceptionRule` (فرع + تكرار أسبوعي/ترتيب `1ST..LAST`) | ✅ `ScheduleException` (نطاق موظف/قسم/فريق + تاريخ محدد/مدى + أولوية + إسناد وردية عند القلب) | لصالحهم (أغنى) |
| أنواع الإذن | اسم إنجليزي | ❌ | ✅ | لصالحهم |
| أنواع الإذن | توقيت صباحي/مسائي/كلاهما | ✅ (`both` مدعوم) | صباحي/مسائي فقط | لصالحنا (جزئي) |
| أنواع الإذن | نوع `kind` (تأخير صباحي يُزيح خط بداية التأخير) | ❌ | ✅ | لصالحهم |
| أنواع الإذن | حد مدة + عدد شهري + دقائق مجانية شهرية | ✅ | ✅ | متعادل |
| أنواع الإذن | نسبة الخصم | ✅ `deductionPct` 0–100 | ✅ `deduction_rate` | متعادل |
| أنواع الإذن | خصم بعد عتبة `after_threshold` + دقائق العتبة | ❌ | ✅ | لصالحهم |
| أنواع الإذن | ازدواج مفتاحَي خصم (`financial_effect` + `deduction_type`) | — | ✅ لكن **متكرر/متداخل** | (ملاحظة تكرار عندهم) |
| العطلات الرسمية | مدى ممتد `endDate` + حقل دولة | ✅ | ❌ (تاريخ مفرد) | **لصالحنا** |
| العطلات الرسمية | تكرار سنوي `is_recurring` | ❌ (تُضاف كل سنة) | ✅ | لصالحهم |
| أجهزة البصمة | نمط اتصال `direct/push` صريح + `device_type` | ضمني | ✅ صريح | لصالحهم (جزئي) |
| أجهزة البصمة | فاصل مزامنة تلقائي عام | ✅ `sync_interval_minutes` | ❌ | **لصالحنا** |
| أجهزة البصمة | سر/مفتاح الجهاز | ✅ `device_key` عام + `authKey` | ✅ `comm_key` + `api_token` لكل جهاز | متعادل |
| أنواع الإجازات | كود/مدفوعة/مرفق مطلوب | ✅ | ✅ | متعادل |
| أنواع الإجازات | اسم إنجليزي | ❌ | ✅ | لصالحهم |
| أنواع الإجازات | رصيد سنوي **لكل نوع** `annual_days` | ❌ (رصيد سنوي عام) | ✅ لكل نوع | لصالحهم |
| أنواع الإجازات | ترحيل **لكل نوع** `carry_over_days` | ❌ (عام) | ✅ لكل نوع | لصالحهم |
| أنواع الإجازات | تقييد بالجنس `applicable_gender` | ❌ | ✅ | لصالحهم |
| أنواع الإجازات | مرة واحدة بالخدمة `oncePerService` (حج) | ✅ | ❌ | **لصالحنا** |
| أنواع الإجازات | مصدر الرصيد `balanceSource` (annual/sick/none) | ✅ | عبر أنواع منفصلة | متعادل |
| أنواع الإجازات | أثر الراتب `partial` | ❌ (`isPaid` ثنائي) | ✅ لكن **بلا نسبة/مبلغ** (قيمة enum غير مُعامَلة) | لصالحهم (شكلي جزئي) |
| إعداد الاستحقاق | نمط الاستحقاق (شهري/سنوي/يومي) | ✅ `accrual_mode` | ❌ | **لصالحنا** |
| إعداد الاستحقاق | شهور فترة التجربة قبل الاستحقاق | ✅ `probation_months` | ❌ | **لصالحنا** |
| الترحيل | حد ترحيل + شهور صلاحية | ✅ عام | لكل نوع + انتهاء لكل دفعة | متعادل (نماذج مختلفة) |
| الأرصدة الافتتاحية | دفتر دفعات FIFO متعدد (مصدر/كمية/انتهاء لكل دفعة) | طبقة افتتاحية واحدة | ✅ متعدد الدفعات | لصالحهم |
| دورة الرواتب | يوم بدء الدورة | ✅ 23 | ✅ 23 | متعادل |
| دورة الرواتب | يوم نهاية الدورة **صريح** | مشتق | ✅ `cycle_end_day` | لصالحهم |
| دورة الرواتب | نوع الدورة `calendar/custom` | ❌ | ✅ | لصالحهم |
| دورة الرواتب | يوم الصرف `payment_day` | ❌ | ✅ | لصالحهم |
| المقسومات | أيام الشهر / ساعات اليوم | ✅ 30 / 8 | ✅ 30 / 8 | متعادل |
| قسيمة الراتب | لغة القسيمة (ar/en/bilingual) | ❌ | ✅ | لصالحهم |
| اعتماد الرواتب | دور المعتمد | عبر سلاسل الاعتماد الكاملة | ✅ مفتاح دور واحد | متعادل (لدينا أغنى) |
| نهاية الخدمة | شهور المكافأة لكل سنة `eos.months_per_year` | ✅ | ❌ (لديهم GOSI بدلها) | **لصالحنا** |
| التأمينات **GOSI** | نِسب موظف/شركة + سقوف تأمينية + أساس الحساب + تأمين أخطار | ❌ | ✅ `GosiSetting` (singleton) | **لصالحهم (كبير)** |
| أنواع البدلات | كتالوج (ثابت/نسبة، إيراد/خصم، خاضع ضريبة/تأمين) | ❌ (متغيرات ثابتة BASIC/HOUSING/…) | ✅ `AllowanceType` | لصالحهم |
| أنواع الخصومات | كتالوج خصومات | ❌ | ✅ لكن **بلا جدول ربط** — لا يطبّقه الحساب | لصالحهم (شكلي) |
| تعويضات بالنطاق | مكافأة/خصم دائم بنطاق تنظيمي | ❌ (لمرة عبر البند/الوجهة) | ✅ `ScopedCompensation` | لصالحهم |
| سياسة السلف/القروض | سقف مضاعف الرصيد + حدود نسبة/مبلغ سلفة وقرض | ❌ | ✅ (5 مفاتيح) | لصالحهم |
| محرك المعادلات | مُقيّم آمن + كتالوج 18 متغيرًا + مُتحقّق | ✅ لكن **غير موصول بشاشة سياسة بعد** (سقالة) | مفاتيح config بسيطة فقط (نصوص غير مُصنَّفة) | لصالحنا (محتمل) |
| معادلة خصم التأخير | نمط `FRACTION` مقابل `MINUTES` قابل للتعريف | ✅ | ثابت بالكود | **لصالحنا** |
| نطاق مسير الرواتب | تعريف المسير بنطاق (شركة/فرع/قسم/فريق/CC/مخصّص) | ✅ `PayrollRun.scopeType` | غير مُعدَّد كإعداد | لصالحنا (جزئي) |
| أنواع الطلبات | إنشاء نوع مخصص | ✅ | ✅ | متعادل |
| **أنواع الطلبات** | **بانّي حقول ديناميكي مُصنَّف** (نص/رقم/تاريخ/اختيار/ملف + مطلوب + خيارات) | ✅ `customFields` | ❌ (`payload` غير مُصنَّف) | **لصالحنا (كبير)** |
| أنواع الطلبات | تصنيف الفئة (9 فئات) | ✅ | ❌ | **لصالحنا** |
| **أنواع الطلبات** | **معالج وجهة تلقائي** (16 معالجًا: رصيد/رواتب/عهدة/PDF…) | ✅ `destinationHandler` | ❌ | **لصالحنا (كبير)** |
| أنواع الطلبات | أعلام (يؤثر بالرصيد/مسار أمني/سرّي/يولّد PDF/المرحلة) | ✅ | ❌ | **لصالحنا** |
| أنواع الطلبات | جمهور الرؤية `visibleTo` | ✅ (يشمل **أدوار** + أقسام + موظفين) | ✅ نطاقات فقط (**بلا أدوار**) | لصالحنا (جزئي) |
| مسارات الاعتماد | نطاق فرع للمسار | ✅ | ✅ | متعادل |
| سلاسل الاعتماد | اعتماد تلقائي `autoApprove` (سلسلة فارغة تُنفَّذ فورًا) | ✅ | ❌ | **لصالحنا** |
| خطوات الاعتماد | معتمد بالدور الهرمي (مباشر/قسم/فرع) | ✅ | ✅ | متعادل |
| خطوات الاعتماد | استهداف **أي دور مخصّص** | ❌ (enum ثابت 11) | ✅ (Spatie، يشمل مخصص) | لصالحهم |
| خطوات الاعتماد | استهداف **مسمى وظيفي** `job_title` | ❌ | ✅ | لصالحهم |
| **خطوات الاعتماد** | **معتمدون بالتوازي** `isParallel` | ✅ | ❌ (تسلسلي فقط) | **لصالحنا** |
| **خطوات الاعتماد** | **خطوة شرطية بعتبة** (حقل/عامل/قيمة) | ✅ | ❌ | **لصالحنا (كبير)** |
| خطوات الاعتماد | SLA أيام لكل خطوة + `canDelegate` | ✅ | ❌ | **لصالحنا** |
| خطوات الاعتماد | التصعيد | ✅ `escalateTo` (دور، عند تجاوز SLA) | ✅ `fallback_user` (مستخدم، عند تعذّر الحل) | متعادل |
| خطوات الاعتماد | معتمد موظف محدد | ✅ | ✅ | متعادل |
| كتالوج الصلاحيات | التفصيل | 39 صلاحية | ~130 (يشمل مستوى الحقل: راتب/PII + لكل منطقة إعداد) | لصالحهم |
| الأدوار | أدوار مخصصة + محرر صلاحيات | ✅ | ✅ | متعادل |
| **تجاوزات لكل مستخدم** | GRANT/REVOKE فوق الدور | ✅ `UserPermissionOverride` | ❌ | **لصالحنا** |
| نطاق فروع المستخدم | تعدد الفروع لكل مستخدم | ❌ (فرع واحد) | ✅ `branch_user` | لصالحهم |
| صلاحيات الإعدادات | تفصيل لكل منطقة إعداد | ❌ (مظلة `settings.manage` + قليل) | ✅ per-area | لصالحهم |
| ملف الشركة | اسم/شعار/سجل تجاري/رقم ضريبي/تأمين — **مُستهلَك بالمستندات** | ❌ (لوحة شكلية بلا باك) | ✅ `CompanyInfo` | لصالحهم |
| إعدادات إقليمية | العملة | ✅ **فعلية تقود كل عرض للأموال** | ✅ مخزّنة | لصالحنا (فعلية) |
| إعدادات إقليمية | منطقة زمنية/تقويم/تنسيق تاريخ/بداية الأسبوع/عطلة | لوحة شكلية | ⚑ مخزّنة لكن **لا يستهلكها محرك** (زخرفية) | متعادل (شكلي عند الطرفين) |
| الإشعارات | مفاتيح لكل حدث (`in_app/email`) + اختبار SMS | لوحة شكلية | ✅ (كاش، بلا جدول) | لصالحهم |
| المصادقة/النظام | LDAP/AD + مصادقة ثنائية + قفل حساب + `backup:manage` | ❌ | ✅ | لصالحهم |
| قوالب المستندات | قوالب قابلة للتحرير (نص مُرمَّز يُصيَّر) | ثابتة/شكلية (التحرير «مرحلة لاحقة») | ✅ `DocumentTemplate` يُصيَّر عبر `TemplateRenderer` | لصالحهم |
| السياسات (`rules` JSON عامة) | شاشة سياسات عامة | ❌ (لدينا إعدادات محددة بديلة) | ✅ لكن ⚑ **ميتة تمامًا** (لا تُقرأ، حلّ محلها `schedule_exceptions`) | لصالحهم (شكلي) |
| أنواع الأصول | كتالوج `AssetType` | ✅ | غير مُعدَّد كإعداد | لصالحنا (جزئي) |

---

### إعدادات عندنا مش عندهم
(كل مقبض تحكّم موجود لدينا ويغيب لديهم — محور اهتمامك)

1. **شرائح خصم التأخير القابلة للتعريف** (`LatenessTier`): نطاقات دقائق + نمط `FRACTION` (كسر يوم) مقابل `MINUTES` (دقائق فعلية × سعر الدقيقة) + قيمة + ترتيب، بواجهة إدارية. **عندهم ثابتة في الكود بلا أي شاشة**.
2. **معامل عقوبة الغياب** القابل للتعريف (1 / 1.5 / 2) — `attendance.absence_penalty_days`. **عندهم ثابت بالكود**.
3. **مُعامل أوفرتايم منفصل للعطلة الرسمية** (`overtime.multiplier_holiday`) بجانب أيام العمل ونهاية الأسبوع — لديهم مضاعف جمعة فقط.
4. **تشغيل/إيقاف الأوفرتايم عامًا + نوافذ تواريخ فتح/إغلاق** مُوجّهة بالفرع (`overtime.enabled` + `OvertimePeriod`، والإغلاق يفوز عند التداخل).
5. **نوافذ تصنيف البصمة داخل الوردية** (`checkinFrom/To`, `checkoutFrom/To`).
6. **الوردية المرنة** (`shiftMode=flexible` + `requiredHours`).
7. **مدى العطلة الممتد** (`endDate`) + حقل الدولة على العطلة.
8. **نمط الاستحقاق** (شهري/سنوي/يومي) + **شهور فترة التجربة قبل الاستحقاق** + **شهور صلاحية الترحيل** كإعداد عام.
9. **إجازة مرة واحدة بالخدمة** (`oncePerService`، كالحج).
10. **بانّي الحقول الديناميكي المُصنَّف** لأنواع الطلبات (نص/رقم/تاريخ/اختيار/ملف + مطلوب + خيارات) — لديهم `payload` غير مُصنَّف.
11. **تصنيف فئة الطلب** (9 فئات).
12. **معالجات الوجهة التلقائية** (16 معالجًا: رصيد إجازات/رواتب/أوفرتايم/تصحيح حضور/قروض/عهدة/توليد PDF…).
13. **أعلام نوع الطلب**: `affectsBalance` / `isSecurityRoute` / `isConfidential` / `autoGeneratesPdf` / `phase`.
14. **اعتماد تلقائي للسلسلة** (`autoApprove`).
15. **معتمدون بالتوازي** في نفس المستوى (`isParallel`).
16. **خطوات اعتماد شرطية بعتبة** (`thresholdField` / `thresholdOp` / `thresholdValue`).
17. **SLA أيام لكل خطوة** + **تفويض** (`canDelegate`).
18. **تجاوزات صلاحيات لكل مستخدم** (GRANT/REVOKE فوق الدور، مع إبطال التوكن).
19. **رؤية نوع الطلب بالأدوار** (بجانب الأقسام/الموظفين).
20. **شهور مكافأة نهاية الخدمة** لكل سنة (`eos.months_per_year`).
21. **فاصل المزامنة التلقائية العام** للأجهزة (`sync_interval_minutes`).
22. أكواد **الفرع/القسم/الفريق** + **بريد الفرع** + **علم المقر الرئيسي** + مركز تكلفة على الفرع.
23. **توقيت الإذن `both`** (صباحي+مسائي) — لديهم صباحي/مسائي فقط.
24. **كتالوج أنواع الأصول** كشاشة إعداد.
25. **تعريف نطاق مسير الرواتب** (شركة/فرع/قسم/فريق/مركز تكلفة/مخصّص) — `PayrollRun.scopeType`.
26. **عطلة أسبوعية خاصة بالفرع** على مستوى المحرك (ملاحظة: مقروءة بالمحرك لكن غير مُتاحة في الواجهة حاليًا لخروجها من الـDTO).

---

### إعدادات عندهم مش عندنا
(فجوات حقيقية — بصدق)

1. **إعدادات GOSI/التأمينات** (نِسب موظف/شركة، سقف/حد أدنى للراتب التأميني، أساس الحساب basic/basic+housing، تأمين الأخطار) — **غياب كامل عندنا** (مهم للسوق السعودي).
2. **كتالوج أنواع البدلات** (`AllowanceType`): ثابت/نسبة، إيراد/خصم، خاضع للضريبة/التأمين — لدينا متغيرات رواتب ثابتة فقط.
3. **سياسة السلف والقروض**: سقف مضاعف الرصيد + حدود نسبة/مبلغ للسلفة والقرض (5 مفاتيح).
4. **تفاصيل دورة الرواتب**: يوم نهاية صريح + نوع الدورة (calendar/custom) + يوم الصرف.
5. **لغة قسيمة الراتب** (عربي/إنجليزي/ثنائي).
6. **التعويضات بالنطاق التنظيمي** (`ScopedCompensation`): مكافأة/خصم دائم على شركة/فرع/قسم/فريق/موظف.
7. **الجدولة المُوجّهة بالنطاق** (`ScheduleAssignment`): إسناد ورديات لشركة/فرع/قسم/فريق بمدى تواريخ فعّال — لدينا أسبوعي لكل موظف + override يومي فقط.
8. **استثناءات جدول أغنى**: نطاق قسم/فريق/موظف + تاريخ محدد/مدى تواريخ + أولوية + إسناد وردية عند قلب اليوم.
9. **إجازات بمعطيات لكل نوع**: رصيد سنوي `annual_days` + ترحيل `carry_over_days` + تقييد بالجنس — لدينا رصيد عام.
10. **دفتر أرصدة افتتاحية متعدد الدفعات (FIFO)** بمصدر وكمية وتاريخ انتهاء لكل دفعة.
11. **نوع الإذن `late_arrival`** (يُزيح خط بداية التأخير) + **خصم بعد عتبة** `after_threshold` بدقائق عتبة.
12. **خصائص وردية**: مدة الراحة + علم الوردية الليلية.
13. **تكرار العطلة السنوي** (`is_recurring`).
14. **قوائم منسدلة مُدارة** (جنسية/دولة/عملة/مدينة/منطقة — هرمية).
15. **ملف الشركة الكامل** (سجل تجاري/رقم ضريبي/تأمين/شعار) **مُستهلَك فعليًا** في المستندات والتقارير — لدينا لوحة شكلية.
16. **قوالب مستندات قابلة للتحرير** (نص مُرمَّز يُصيَّر عبر `TemplateRenderer`) — لدينا قوالب ثابتة و«مرحلة لاحقة».
17. **إعدادات إشعارات لكل حدث** (in_app/email) + اختبار SMS.
18. **LDAP/AD + مصادقة ثنائية + قفل الحساب + صلاحية النسخ الاحتياطي**.
19. **كتالوج صلاحيات أدقّ (~130)** يشمل مستوى الحقل (راتب/PII) وصلاحيات لكل منطقة إعداد.
20. **تعدد الفروع لكل مستخدم** (`branch_user`).
21. **استهداف خطوة الاعتماد لأي دور مخصّص أو لمسمى وظيفي**.
22. حقول تفصيلية: `name_en` على الوردية/الفريق/نوع الإذن/نوع الإجازة، وصف المسمى، مستوى الدرجة، مركز تكلفة القسم، نطاق فرع لمركز التكلفة.

---

### ملاحظة: خيارات «شكلية عندهم» (موجودة في الـUI/DB لكنها غير مُستهلَكة)
هذه تبدو ميزات لصالحهم في المقارنة السطحية لكنها **لا تعمل فعليًا** — فلا تُحسَب فجوات حقيقية علينا:

| الخيار عندهم | الحالة | التفصيل |
|---|---|---|
| `policies` (+ `rules` JSON) | **ميت تمامًا** | CRUD كامل، لكن لا يقرأه أي محرك؛ حلّ محله `schedule_exceptions`. تعليق داخل الكود نفسه يقر بأنه غير مربوط. |
| `work_schedules` (+ `rules`/`work_hours` JSON) | **شكلي مقابل المحرك** | يُخزَّن ويُوسَم للموظف فقط؛ المحرك يقرأ `schedule_assignments` لا هذا الجدول، و`rules` لا تُقيَّم. (بينما `WorkSchedule` عندنا **مُستهلَك فعلاً**.) |
| الكتلة الإقليمية في `company_infos` (`timezone/calendar/week_start/weekend_days/date_format`) | **زخرفية** | لا يقرأها أي محرك حضور/إجازات/رواتب؛ العطلة/الساعات الحقيقية تأتي من `work_days`/`schedule_assignments`. |
| `deduction_types` | **كتالوج تسميات** | لا يوجد جدول ربط `employee_deductions`؛ لا شيء في حساب الرواتب يشير إليه. |
| `permit_types.financial_effect` | **متكرر** | يتعايش مع `deduction_type` الأحدث — مفتاحا خصم متداخلان على نفس الصف. |
| `leave_types.salary_effect = partial` | **غير مُعامَل** | القيمة مقبولة بالتحقّق لكن لا حقل نسبة/مبلغ يُفعّل «الجزئي». |
| `weekly_schedule_entries` | **قديم** | يُقرأ كاحتياطي طبقة-2 فقط؛ الكتابات الجديدة تذهب إلى `schedule_assignments`. |

**الخلاصة:** أكبر تفوّق لنا في **محرّك القواعد القابل للتعريف** — شرائح خصم التأخير، معامل الغياب، الأوفرتايم متعدد الطبقات + نوافذ التواريخ، وبانّي الطلبات (حقول ديناميكية + 16 معالج وجهة) + خطوات اعتماد متوازية/شرطية/بـSLA + تجاوزات صلاحيات لكل مستخدم — كلها ثابتة بالكود أو غائبة لديهم. أكبر فجواتنا الحقيقية: **GOSI، كتالوج البدلات، سياسة القروض، ملف الشركة/قوالب المستندات المُصيَّرة، والجدولة المُوجّهة بالنطاق**.

## الطلبات والموافقات — فرق مفصّل (option-by-option)

### 1) جدول المقارنة التفصيلي (خيار بخيار)

| المحور | القدرة/الخيار | عندنا | عندهم | لصالح مين |
|---|---|---|---|---|
| **بناء نوع الطلب** | إنشاء نوع طلب مخصّص جديد | ✅ `POST` builder كامل | ✅ `POST /approvals/request-types` | تعادل |
| بناء نوع الطلب | كود/مفتاح فريد بريجيكس | ✅ `code` `^[A-Z][A-Z0-9_]{2,40}$` + توليد تلقائي `CUSTOM_n` | ✅ `key` `^[a-z0-9_\-]+$` فريد/تينانت | تعادل |
| بناء نوع الطلب | اسم عربي | ✅ `nameAr` (Min 3) | ✅ `name_ar` | تعادل |
| بناء نوع الطلب | اسم إنجليزي (ثنائي اللغة) | ❌ غير موجود | ✅ `name_en` | **عندهم** |
| بناء نوع الطلب | تصنيف النوع (categories) | ✅ **9 تصنيفات** ثابتة، ثابت بعد الإنشاء | ❌ لا يوجد تصنيف إطلاقًا | **عندنا** |
| بناء نوع الطلب | مرحلة `phase` (P1/P2/P3) | ✅ (داخلي) | ❌ | **عندنا** |
| **الحقول الديناميكية** | مُصمّم حقول للنموذج (form builder) | ✅ **5 أنواع** `text/number/date/select/file` لكل نوع | ❌ **معدوم** — الحمولة `payload` JSON غير مُنمَّط | **عندنا (فارق جوهري)** |
| الحقول الديناميكية | خصائص الحقل: key/label/required/options | ✅ مع ريجيكس مفاتيح ومنع تكرار | ❌ لا ميتاداتا حقول أصلًا | **عندنا** |
| الحقول الديناميكية | تحقّق وقت الإرسال (select ضمن الخيارات، number يُحلَّل، required يُحجب) | ✅ `requests.service.ts:176-206` | ❌ `payload=['nullable','array']` بلا أي قاعدة داخلية | **عندنا** |
| **تعديل النوع** | نقطة تعديل/تحديث النوع (PUT) | ✅ update DTO (اسم/تفعيل/سلسلة/حقول/جمهور/وجهة/مرفقات) | ❌ **لا يوجد** endpoint تحديث إطلاقًا | **عندنا** |
| تعديل النوع | تفعيل/تعطيل (toggle) | ✅ `isActive` (لا حذف صلب) | ❌ لا مسار toggle (فقط create/delete) | **عندنا** |
| تعديل النوع | حماية أنواع النظام من الحذف | ✅ code/category ثابتان بعد الإنشاء | ✅ `is_system` يمنع الحذف | تعادل |
| **الجمهور/الرؤية** | نطاق من يرى/يُرسِل النوع | ✅ **4 أوضاع** `all/departments/roles/employees` | ✅ **5 نطاقات** `company/branch/department/team/employee` | تعادل (تركيبة مختلفة) |
| الجمهور/الرؤية | رؤية على مستوى **الدور** (role) | ✅ يطابق `user.role` | ❌ لا يوجد نطاق دور | **عندنا** |
| الجمهور/الرؤية | رؤية على مستوى **الفريق/الشركة** | ⚠️ لا فريق (عندنا all/dept/role/emp) | ✅ team + company | **عندهم** |
| الجمهور/الرؤية | افتراضي = مرئي للكل عند غياب قيود | ✅ (`all`) | ✅ (لا صفوف = الكل) | تعادل |
| الجمهور/الرؤية | تجاوز للمشرف (super_admin/‎*‎/manage) | ✅ يرى الكل | ⚠️ عبر الصلاحيات فقط | تعادل |
| **المرفقات** | مرفقات مطلوبة وصفية على النوع | ✅ `requiredAttachments` نص حر | ❌ | **عندنا** |
| المرفقات | فرض مرفق فعليًا عند الإرسال | ✅ لأنواع الإجازات (`requiredAttachment` مفروض) | ❌ لا فرض | **عندنا** |
| **الوجهة/الأثر عند الاعتماد** | محرك وجهات يكتب سجلًا دائمًا | ✅ **القاعدة الذهبية**: كل اعتماد يكتب `destinationRef`؛ 17 whitelist + ~25 handler منفّذ | ⚠️ 13 listener «best-effort» قد تفشل بصمت (try/catch لا يحجب القرار) | **عندنا** |
| الوجهة/الأثر | fallback لسجل دائم عند غياب handler | ✅ `REQ-YYYY-NNNNNN` مضمون | ❌ listener غائب = no-op بلا سجل | **عندنا** |
| الوجهة/الأثر | دفتر التزامات مالي (DEBIT/CREDIT، يُستهلك مرة) | ✅ `EmployeeObligation` (bonus/allowance/expense/adjustment/loss) | ❌ لا دفتر التزامات موحّد | **عندنا** |
| الوجهة/الأثر | فتح حالة إخلاء طرف تلقائية (5 أطراف) | ✅ `employee_status` RESIGNATION→OffboardingCase | ❌ (استقالة = ختم `last_working_day` فقط) | **عندنا** |
| الوجهة/الأثر | تنفيذ مؤجَّل (تحويل مستقبلي/عهدة بانتظار إقرار) عبر `IN_EXECUTION` | ✅ scheduler + manager-confirm | ❌ لا حالة تنفيذ مؤجَّل | **عندنا** |
| الوجهة/الأثر | تتبّع مصدر السجل (`sourceRequestId`/Ref) | ✅ في الالتزامات والسجلات | ⚠️ عبر subject_type/subject_id فقط | **عندنا** |
| **التوازي في الاعتماد** | خطوات متوازية / كوورم / الكل-يعتمد | ✅ `isParallel` (نفس stepOrder = مجموعة، **الكل يعتمد**، أي رفض يرفض) | ❌ **تسلسلي فقط**؛ داخل الخطوة **أي فرد** يكفي (لا كوورم) | **عندنا (فارق جوهري)** |
| التوازي | تسلسل الخطوات | ✅ | ✅ حسب `order` | تعادل |
| **خطوات شرطية** | خطوة مشروطة بعتبة (threshold على الحمولة) | ✅ `thresholdField/Op(>= > < <=)/Value` (مثال: مالية عند مبلغ≥5000، تنفيذي عند زيادة≥10%) | ❌ **معدوم** كليًا | **عندنا (فارق جوهري)** |
| خطوات شرطية | تخطّي/تفرّع منطقي | ✅ الخطوة تُتخطّى إن لم تتحقق العتبة (NaN⇒skip) | ❌ كل خطوة مُهيّأة تعمل دائمًا | **عندنا** |
| **اختيار السلسلة/المسار** | تجاوز لكل فرع (per-branch) | ✅ نسخة سلسلة بنفس code + `branchId` تسبق العامة | ✅ `branch_id` على المسار (فرعي يسبق العام) | تعادل |
| اختيار السلسلة | اختيار مسار بشرط/مبلغ/قسم | ✅ (عبر الخطوات الشرطية داخل السلسلة) | ❌ **الاختيار بالفرع فقط**؛ dept/team/job كأهداف لا كمحدِّدات | **عندنا** |
| **إرجاع/إعادة إرسال** | RETURN للطلب لطلب معلومات | ✅ `RETURNED_FOR_INFO` → `resubmit()` يدمج الحمولة **ويعيد حل السلسلة** | ❌ **معدوم** — القرار ثنائي approved/rejected فقط | **عندنا (فارق جوهري)** |
| إرجاع/إعادة | إعادة فتح/طلب تعديل | ✅ | ❌ لا re-open | **عندنا** |
| **SLA/التصعيد** | مهلة زمنية للخطوة (`slaDays`, `dueAt`) | ✅ | ❌ لا مواعيد استحقاق | **عندنا** |
| SLA/التصعيد | تصعيد آلي عند تجاوز المهلة | ✅ `escalateTo` + محرك `runEscalations` (يبدّل الدور، يعيد نافذة SLA، يسجّل ESCALATED) | ❌ لا تصعيد زمني | **عندنا** |
| SLA/التصعيد | معتمد بديل عند تعذّر الحل (fallback approver) | ⚠️ عبر cascade مدير + `super_admin` يفي بأي خطوة | ✅ `fallback_user_id` صريح لكل خطوة (عند عدم حل الهدف) | **عندهم (آلية صريحة)** |
| SLA/التصعيد | تفويض (delegation) | ⚠️ `canDelegate` معروض لكن غير مربوط بفعل | ❌ لا تفويض/تغطية إجازة | تعادل (كلاهما ناقص) |
| SLA/التصعيد | تذكيرات مجدولة | ❌ | ❌ | تعادل |
| **أنواع المعتمِد** | أدوار المعتمِد المتاحة | ✅ **11 دورًا** (5 هيكلي + 6 وظيفي) | ✅ **8 أنواع استهداف** | **عندنا (أوسع)** |
| أنواع المعتمِد | مدير مباشر/قسم/فرع | ✅ (مع cascade متدرّج للمدير المباشر) | ✅ | تعادل |
| أنواع المعتمِد | موظف محدّد / دور / | ✅ `specific_employee` + أدوار | ✅ `user`/`role` | تعادل |
| أنواع المعتمِد | استهداف بـ **المسمّى الوظيفي** (job_title) | ❌ غير موجود | ✅ `job_title` (كل حاملي المسمّى) | **عندهم** |
| أنواع المعتمِد | مدير الفريق المستقبِل (تحويلات) | ✅ `receiving_team_manager` (payload.toTeamId) | ⚠️ `team_manager` (فريق الطالب فقط) | **عندنا** |
| أنواع المعتمِد | أدوار وظيفية بالصلاحية (hr/finance/it/custody/payroll/executive) | ✅ عبر perm `approve.*` | ⚠️ عبر أدوار sanctum عامة + `hr_manager` | **عندنا** |
| أنواع المعتمِد | `super_admin` يفي بأي خطوة (كسر أي جمود) | ✅ | ⚠️ فقط كـ fallback مبذور | **عندنا** |
| **سلوك السلسلة الفارغة** | لا سلسلة إطلاقًا | ✅ `BadRequestException` — إيقاف صلب (لا تنفيذ بلا اعتماد) | ⚠️ `openIfConfigured` = no-op صامت | **عندنا (أوضح)** |
| سلوك السلسلة الفارغة | سلسلة فارغة + `autoApprove=false` | ✅ إيقاف صلب حتى إضافة معتمدين | ⚠️ لا يفتح طلب أصلًا | تعادل |
| سلوك السلسلة الفارغة | اعتماد تلقائي فوري (`autoApprove=true`) | ✅ تنفيذ فوري بخيار المالك صراحةً (toggle محروس بصفر خطوات) | ❌ لا مفهوم auto-approve | **عندنا** |
| سلوك السلسلة الفارغة | حالة «لا معتمِد قابل للحل» | ✅ لا تحدث (cascade + super_admin) | ⚠️ `blocked` | تعادل |
| **الرفض** | الرفض نهائي | ✅ (أي رفض في مجموعة متوازية يرفض الكل) | ✅ رفض واحد ينهي | تعادل |
| **إلغاء/سحب** | إلغاء الطلب | ✅ `CANCELLED` قابلة للوصول من DRAFT/SUBMITTED/UNDER_REVIEW | ❌ `cancelled` معرّفة لكن **لا endpoint يصلها** | **عندنا** |
| **سجل التدقيق** | أثر تدقيق غير قابل للتعديل | ✅ `request_approvals` قبل كل تغيير حالة + ESCALATED/RETURN | ✅ `approval_actions` (step/approver/decision/comment/acted_at) | **عندنا (أغنى)** |
| **الإشعارات** | إشعار المعتمِدين/الطالب | ⚠️ غير موثّق صراحةً | ✅ push للمعتمدين عند الفتح/التقدّم + للطالب عند الحسم | **عندهم** |
| **التحقق المسبق** | فحص كفاية الرصيد قبل الإرسال | ✅ `affectsBalance` + maxDays + oncePerService + مرفق مفروض | ⚠️ سقوف على السلفة/القرض فقط (`enforceCap`) | **عندنا** |
| **مسار سرّي** | إخفاء المدير المباشر للطلبات السرّية | ✅ `isConfidential` يتخطّى `direct_manager_of_requester` (تظلّمات/بلاغات) | ❌ لا مفهوم سرّية | **عندنا** |
| **حجم الكتالوج** | أنواع مبذورة جاهزة | ✅ **55 نوعًا** / 9 تصنيفات + ~25 handler + 11 نوع إجازة | ⚠️ **13 نوع نظام** فقط | **عندنا** |
| **تحرير الخطوات** | تعديل/حذف/إعادة ترتيب الخطوات | ✅ reorder + replaceChainSteps (الطلبات الجارية غير متأثرة — snapshot) | ❌ **append-only**؛ حذف المسار كاملًا فقط، لا reorder | **عندنا** |

---

### قدرات في الطلبات عندنا مش عندهم

قائمة حصرية بكل ما نملكه ويفتقده المنافس (محور الاهتمام الرئيسي):

1. **مُصمّم حقول ديناميكي كامل (5 أنواع)** — text/number/date/select/file مع required/options/ريجيكس مفاتيح وتحقّق وقت الإرسال. عندهم النوع = `key+name+flags` والحمولة JSON غير مُنمَّطة بلا أي قاعدة تحقّق داخلية.
2. **تصنيف الأنواع إلى 9 فئات** (leaves/time_attendance/financial/…) — عندهم صفر تصنيف.
3. **نقطة تعديل النوع (update)** وزر **تفعيل/تعطيل** — عندهم لا endpoint تحديث ولا toggle إطلاقًا (create/delete فقط).
4. **خطوات موافقة متوازية + دلالة «الكل يعتمد / أي رفض يرفض»** — عندهم تسلسلي بحت وأي فرد داخل الخطوة يكفي (لا كوورم ولا all-must-approve).
5. **خطوات مشروطة بعتبة** (`thresholdField/Op/Value`: مالية عند مبلغ≥5000، تنفيذي عند زيادة≥10%، مع تخطّي عند NaN) — معدومة تمامًا عندهم.
6. **إرجاع لطلب معلومات (RETURN) + إعادة إرسال تعيد حلّ السلسلة من جديد** — عندهم قرار ثنائي approved/rejected بلا send-back/re-open.
7. **SLA لكل خطوة + محرك تصعيد آلي زمني** (`slaDays`/`dueAt`/`escalateTo`/`runEscalations` يبدّل الدور ويعيد النافذة) — عندهم لا مواعيد ولا تصعيد زمني (fallback عندهم فقط عند تعذّر حل الهدف، لا على المهلة).
8. **اعتماد تلقائي بخيار صريح (`autoApprove` محروس بصفر خطوات)** + سلوك سلسلة فارغة مُحكم بثلاث نتائج واضحة (إيقاف صلب / انتظار معتمدين / تنفيذ فوري).
9. **القاعدة الذهبية: كل اعتماد يكتب سجلًا دائمًا** عبر ~25 handler منفّذ + fallback مضمون `REQ-`. عندهم listeners best-effort قد تفشل بصمت وقد لا تترك أثرًا.
10. **دفتر التزامات مالي موحّد** (`EmployeeObligation` DEBIT/CREDIT، يُستهلك مرة، effectiveDate-gated) — لا نظير عندهم.
11. **حالة إخلاء طرف تلقائية بخمسة أطراف** عند الاستقالة + أرشفة/تعطيل مستخدم عند التقاعد — عندهم ختم تاريخ فقط.
12. **تنفيذ مؤجَّل عبر `IN_EXECUTION`** (تحويلات مستقبلية التاريخ، عهدة بانتظار الإقرار، منفّذة عبر scheduler/manager-confirm) — عندهم لا حالة تنفيذ مؤجّل.
13. **مسار سرّي** (`isConfidential`) يتخطّى المدير المباشر تلقائيًا للتظلّمات/البلاغات — لا مفهوم سرّية عندهم.
14. **`super_admin` يفي بأي خطوة (كسر أي جمود)** كسلوك أصيل، لا كمجرد fallback مبذور.
15. **إلغاء/سحب الطلب فعليًا** (CANCELLED قابلة للوصول) — عندهم الحالة معرّفة لكن لا endpoint يصلها.
16. **تعديل/إعادة ترتيب/استبدال خطوات السلسلة** مع عزل الطلبات الجارية (snapshot على الطلب) — عندهم append-only بلا reorder/حذف خطوة.
17. **رؤية على مستوى الدور** (roles audience) — عندهم لا نطاق دور.
18. **فحوص إجازات مسبقة غنية** (كفاية الرصيد، maxDays، oncePerService مع فحص in-flight، مرفق مفروض) — عندهم سقوف السلفة/القرض فقط.
19. **مدير الفريق المستقبِل** (`receiving_team_manager` من payload.toTeamId) كدور هيكلي للتحويلات — عندهم team_manager = فريق الطالب فقط.
20. **كتالوج ضخم**: 55 نوعًا مبذورًا لكلٍّ سلسلته الخاصة + 11 نوع إجازة قابل للتهيئة، مقابل 13 نوع نظام عندهم.

---

### عندهم مش عندنا (بأمانة)

1. **اسم إنجليزي (`name_en`) — أنواع ثنائية اللغة**: عندهم حقل اسم إنجليزي على كل نوع؛ عندنا `nameAr` فقط بلا حقل إنجليزي مقابل.
2. **استهداف بالمسمّى الوظيفي (`job_title`)**: خطوة تُوجَّه لكل حاملي مسمّى وظيفي معيّن — لا يوجد لدينا نظير مباشر (لدينا أدوار وموظف محدّد ومديرون هيكليون فقط).
3. **رؤية على مستوى الفريق والشركة**: نطاقات `team` و`company` صريحة ضمن رؤية النوع — عندنا all/departments/roles/employees بلا نطاق فريق مستقل.
4. **معتمِد بديل صريح لكل خطوة (`fallback_user_id`)**: آلية مُهيّأة صراحةً عند تعذّر حل الهدف (مدير غير مُعيَّن/دور فارغ)، مع بذر super-admin افتراضيًا — عندنا نعتمد على cascade المدير و«super_admin يفي بأي خطوة» بدل حقل fallback قابل للتهيئة لكل خطوة.
5. **إشعارات حسم موثّقة صراحةً**: listeners تُشعِر المعتمدين عند الفتح/التقدّم والطالب عند الحسم (`NotifyRequesterOnApprovalDecision`) — طبقة الإشعارات عندنا غير موثّقة صراحةً في محرك الطلبات.

---

### مقارنة جنبًا إلى جنب — كتالوج الأنواع المبنيّة (وجهاتنا مقابل أنواع النظام الـ13 عندهم)

| مجال الأثر | **عندنا** (handler / نوع — الأثر الدائم) | **عندهم** (نوع نظام — أثر listener) |
|---|---|---|
| إجازات | `LEAVE` → `leave_calendar_balance`: إدراج Leave + **خصم رصيد طبقي** + حارس تداخل → `LV-`؛ `leave_calendar_payroll` بلا خصم؛ `leave_balance_restore` إلغاء + **استرداد رصيد** → `LVX-` (+11 نوع إجازة مُهيّأ) | `leave` → `ApproveLeaveAction` خصم رصيد / `RejectLeaveAction` |
| عمل إضافي | `overtime_entries` (PRE_REQUESTED، rate≈1.5) → `OT-`؛ `overtime_auto` اعتماد بصمة مكتشفة بالساعات الفعلية | `overtime` → `ApproveOvertimeAction` (يصل للـpayroll) |
| استئذان/حضور | `attendance_corrections` إدراج تصحيح IN/OUT يُطبَّق على حساب اليوم → `AC-` (+`PERMISSION`/`attendance_log`) | `exit_permit` → `ApproveExitPermitAction` |
| توظيف/عروض | — (لا نظير مباشر في الوجهات المنفّذة) | `recruitment` → تطبيق قرار عرض العمل (draft) |
| سلفة/قرض | `loans_installments` إدراج Loan + توليد جدول أقساط شهرية (الأخير يمتص التقريب) → `LN-` | `advance` و`loan` → `CreateLoanAction::activate` |
| استقالة/إنهاء | `employee_status`: RESIGNATION→`notice_period` **+ فتح إخلاء طرف بـ5 أطراف**؛ RETIREMENT→`archived` + تعطيل مستخدم → `OFB-`/`ST-` | `resignation` و`termination` → ختم `last_working_day` + `termination_reason` |
| زيادة راتب | `salary_update_history`: استبدال basicSalary + سجل تدقيق → `SAL-` (سلسلة بعتبة زيادة≥10%) | `salary_increase` → إضافة `amount` للراتب + مزامنة العقد (يتخطّى المستقبلي) |
| عهدة/أصول | `custody_assignments_ack` (PENDING_ACK، ينتظر إقرار) → `CU-`؛ `custody_assignments` إرجاع → `CUR-`؛ `custody_transfer` نقل → `CUT-`؛ `custody_finance` فقد/تلف → **التزام DEBIT** بقيمة الأصل | `asset_assignment` / `asset_return` → تأكيد/تراجع حالة الأصل |
| تسوية مستحقات | (ضمن مسار الاستقالة/الإخلاء) | `settlement` → اعتماد التسوية (جاهزة للصرف) |
| ترقية/تغيير مسمّى | `employee_update_promotions`: إدراج Promotion + استبدال jobTitle + سجل → `PR-` | `promotion` → تطبيق `new_job_title_id/new_grade_id/new_basic_salary` |
| نقل بين الفرق | `transfers_effective_date`: إدراج Transfer (SCHEDULED)؛ تنفيذ فوري إن ≤اليوم وإلا تأجيل للـscheduler | — (نوع `transfer` موجود فقط في seeder يتيم بلا listener) |
| بيانات شخصية | `employee_record` (حقول محدودة phone/email/bankName)؛ `payroll_bank_secure` **مسار أمني** لتغيير IBAN → `BNK-` | — |
| خطابات | `letter_pdf_generator`: إدراج LetterRequest (GENERATED) + مسار PDF محجوز → `LTR-` (6 أنواع خطاب autoGeneratesPdf) | — |
| مالية إضافية (دفتر التزامات) | `payroll_bonus`/`payroll_allowance`/`expense_register`/`payroll_adjustment` → التزام **CREDIT** يُستهلك مرة بمسير الرواتب | — |
| علاقات موظفين | `GRIEVANCE`/`WHISTLEBLOWING`/`PENALTY_OBJECTION` (سرّية، تتخطّى المدير المباشر) + اقتراحات/اجتماعات | — |
| **الإجمالي** | **55 نوعًا / 9 تصنيفات، ~25 handler منفّذ، 11 نوع إجازة، القاعدة الذهبية (سجل دائم دائمًا)** | **13 نوع نظام، listeners best-effort (قد تفشل بصمت)، بلا سجل احتياطي مضمون** |

> ملاحظة أمانة: عندهم seeder يتيم ثانٍ (`ApprovalRequestTypeSeeder`) بمفاتيح مختلفة (`permit`/`transfer`/`salary_adjustment`) **بلا listeners = مفاتيح ميتة** وغير مربوط بـ`DatabaseSeeder` — النظام الحيّ يستخدم الـ13 فقط.


---

# الجزء (ح) — الشرح التفصيلي لكل دورة (طلب-بطلب + الدورات الكاملة)
> هذا الجزء يشرح **الدورة الكاملة لكل طلب** (البيانات الداخلية + دورة الحالات + كيف يؤثر على النظام)، ومرونة الأوفرتايم، ودورة الاستقالة كاملة، والنقل بين الفرق، ومرونة الورديات (وردية لكل يوم/أسبوعي/آخر سبت)، ودورة العهدة كاملة، وجرد البيانات الديمو الفعلية عندهم — سردًا لا جداول.

## كتالوج الطلبات — بطاقة تفصيلية لكل طلب (الدورة + البيانات + التأثير)

> **قبل ما تبدأ تقرأ البطاقات — إطار عام لازم يكون في دماغك:**
>
> **عندنا (نظامنا):** المحرّك عنده **9 حالات** (`state-machine.ts`): `DRAFT → SUBMITTED (لحظية مش بتتسجّل أبدًا) → UNDER_REVIEW → APPROVED → IN_EXECUTION → COMPLETED`، وجانبيًّا `RETURNED_FOR_INFO` و`REJECTED` و`CANCELLED`. لحظة ما الطلب يتعتمد نهائيًّا، بيتنادى **معالج (handler)** من `destinations.service.ts` بيكتب **سجل حقيقي في جدول مخصّص** ويرجّع **مرجع (ref)** على شكل `PREFIX-سنة-رقم6خانات` (مثال `LV-2026-000014`). القاعدة الذهبية عندنا: **كل اعتماد بيكتب سجلًا — حتى لو المعالج مش متعمِل (stub)، بيتكتب سجل عام `REQ-سنة-رقمالطلب` والطلب يُقفل `COMPLETED`**. مفيش اعتماد بيروح في الهوا.
>
> **عندهم (المنافس):** محرّك بحالات **5 على الورق بس 4 اللي بتوصلها فعلًا**: `pending → approved` أو `rejected` أو `blocked`؛ وحالة `cancelled` **ميتة** (موجودة في الـenum بس مفيش سطر كود بيوصّلها ومفيش endpoint إلغاء أصلًا). الاعتماد **تسلسلي بموافِق واحد لكل خطوة** — أول رفض في أي خطوة **بيقتل الدورة كلها فورًا** (مفيش نصاب/quorum ولا خطوات متوازية). لمّا الطلب يوصل `approved` أو `rejected` بيطلق حدث `ApprovalFinalized` (`ApprovalFinalized.php`)، و**12 listener** مسجّلين في `AppServiceProvider.php` بيسمعوا الحدث و**كل واحد منهم بيعمل تأثيره بأسلوب best-effort داخل `try/catch`** — يعني لو التأثير فشل، القرار نفسه ما بيتكسرش، بس التأثير ممكن **ما يحصلش وحدًا يعرف**. وأخطر حاجة عندهم: حالة **`blocked`** — لو الخطوة الحالية ما لقتش موافِق (ولا حتى `fallback`)، الطلب بيتعلّق `blocked` **من غير ما يطلق أي حدث**، والسجل الأصلي (الإجازة/السلفة/إلخ) بيفضل `pending` للأبد بلا أثر ولا إشعار.

---

### 1) بطاقة عائلة الإجازات (Leave)

**الاسم + التصنيف.** عندنا: `LEAVE` («طلب إجازة») + `LEAVE_MODIFY_CANCEL` («إلغاء/تعديل إجازة») تحت تصنيف `leaves`. مهم: السييد بيعطّل (`isActive=false`) الـ11 نوع إجازة المستقلّة (سنوية/مرضية/عارضة… إلخ) ويسيب **نوع موحّد واحد `LEAVE`** بيختار الموظف نوع الإجازة جوّاه من `leaveType`. عندهم: نوع واحد `leave` (subject-bound على `leave_request`).

**البيانات الداخلية (الحقول الفعلية).**
- **عندنا (`requiredFields` لـ`LEAVE`):** `leaveType`, `fromDate`, `toDate`, `days`. الموظف كمان ممكن يدخل نصف يوم عن طريق `payload.period = MORNING|EVENING` (لازم يوم واحد بس)، و`payload.attachmentUrl` لو نوع الإجازة بيطلب مرفق (مثال المرضية بتطلب «تقرير طبي»). حقل `days` بيتكتب بالمحرّك نفسه: **بيحسب أيام العمل الفعلية** عبر `attendance.workingDaysForEmployee` (بيتخطّى الويكندات والعطلات ومش بيخصمها)، ويرفض المدى اللي كله عطلات، ويحطّ `0.5` لنصف اليوم.
- **عندهم (payload):** `leave_type_id`, `start_date`, `end_date`, `total_days` — بس دي **معلوماتية**؛ الـlistener بيعيد قراءة صف الإجازة نفسه. حساب أيام العمل بيتم عبر `LeaveDayCalculator::countWorkingDays` وقت التطبيق.

**الدورة (خطوة بخطوة).**
- **عندنا** (`requests.service.ts::submit`): قبل ما السلسلة تتحل، فيه حُرّاس (guards) على فئة `leaves`: لازم `leaveType` نشط، نصف اليوم يوم واحد بس، يمنع **التداخل** مع إجازة `APPROVED` أو طلب إجازة قيد التنفيذ، يفرض `LeaveType.maxDays` و`oncePerService` (للحج مثلًا) و`requiredAttachment`، ولو `affectsBalance` بيتأكد من كفاية الرصيد عبر `leaveBalances.assertSufficient`. بعدها `DRAFT → UNDER_REVIEW` (سلسلة القصد `CHAIN_MANAGER_HR`: المدير المباشر ثم HR)، ولمّا كل الخطوات تعتمد → `APPROVED` ثم `executeDestination()` → **`COMPLETED`**.
- **عندهم** (`LeaveController::store` → `LeaveService::create` بيعمل صف `pending` → `ApprovalEngine::open`): الطلب `pending` على `current_step_order` = أول خطوة، بيتنقّل تسلسليًّا بالخطوات لحد `approved`. أي رفض في أي خطوة → `rejected` فورًا وخلاص. لو الخطوة ما لقتش موافِق → `blocked` والإجازة تفضل `pending`.

**بيأثّر إزاي.**
- **عندنا:** المعالج `leave_calendar_balance` (= `leaveHandler(true)`) بيدخل صف في جدول `leaves` بحالة `APPROVED` (فترة FULL/MORNING/EVENING، و`isUnpaid` من `LeaveType.isPaid`)، فيه **حارس تداخل تاني** وقت الكتابة، ولو `balanceSource ≠ none` بينادي `leaveBalances.deduct` (بيستهلك طبقة الرصيد الافتتاحي `openingDays` الأول ثم `taken`). المرجع `LV`. وبعد الـcommit بيعيد حساب أيام الحضور عبر المدى (سقف 62 يوم). أنواع تانية من المعالج: `leave_calendar_payroll` بيكتب صف الإجازة **بدون خصم رصيد** (الرواتب بتتعامل معاها يوم بيوم عبر `isUnpaid`)، و`leave_calendar_once` للحج (بلا خصم).
- **عندهم:** الـlistener `SyncLeaveOnApprovalDecision` (حارسه `leave.status==pending`): عند الموافقة → `ApproveLeaveAction` يحطّ `status=approved, approved_by, approved_at` و**يخصم الرصيد** إلا لو `leaveType.deducts_from_balance==false`. الخصم **FIFO**: يستهلك دفعات الرصيد الافتتاحي الأول (`OpeningLeaveBalanceService::consumeFifo`) ثم من الاستحقاق السنوي (`LeaveBalanceService::deduct`)، ويسجّل خريطة `lot_consumption` لكل سنة عشان الاسترجاع الدقيق لاحقًا. عند الرفض → `RejectLeaveAction` (والإجازة كانت لسه `pending` فمفيش رصيد يترجّع).

**عندنا مقابل عندهم — في العائلة دي بالذات.** الاتنين عندهم دفتر رصيد بطبقتين (افتتاحي + استحقاق) وخصم FIFO؛ التطابق هنا قوي. الفرق الجوهري: **إلغاء/تعديل الإجازة**. عندنا نوع مستقل `LEAVE_MODIFY_CANCEL` (حقل `leaveId`، معالج `leave_balance_restore`، سلسلة `CHAIN_MANAGER`) بيحوّل الإجازة الأصلية لـ`CANCELLED` و**يرجّع الرصيد** (طبقة الاستحقاق الأول ثم الافتتاحي، مرجع `LVX`) ويعيد حساب المدى الأصلي. **عندهم مفيش مسار إلغاء إجازة أصلًا** (لا نوع ولا endpoint) — لو اتعمدت إجازة، تعديلها/إلغاؤها يدوي بره الدورة. كمان عندنا فرق **نصف اليوم (MORNING/EVENING)** كمفهوم أصيل في المحرّك؛ عندهم مفيش تخصيص نصف يوم في الـpayload المذكور.

---

### 2) بطاقة العمل الإضافي (Overtime)

**الاسم + التصنيف.** عندنا نوعين تحت `time_attendance`: `OVERTIME` («عمل إضافي» — مطلوب مسبقًا PRE_REQUESTED) و`OVERTIME_AUTO` («عمل إضافي مكتشف بالبصمة» — بيتغذّى بكرون). عندهم نوع واحد `overtime` (subject-bound على `overtime_record`).

**البيانات الداخلية.**
- **عندنا:** `OVERTIME` بياخد `date`, `hours`. `OVERTIME_AUTO` مفيهوش حقول يدوية — بيتخلق آليًّا.
- **عندهم:** `overtime_minutes`, `date`.

**الدورة.**
- **عندنا:** `OVERTIME` عادي: `DRAFT → UNDER_REVIEW` (سلسلة `CHAIN_MANAGER`) → `APPROVED` → `COMPLETED`. أمّا `OVERTIME_AUTO` فدورته مميّزة: كرون `@Cron('45 */3 * * * *') reconcileAutoOvertime()` بياخد صفوف `overtime_entries` اللي `source=BIOMETRIC_DETECTED, status=DETECTED, requestId=NULL` ويسوّقها في طلبات `OVERTIME_AUTO` خلال سلسلتها؛ **لو السلسلة مش متظبطة، الكرون بيرجّع الصف لـ`DETECTED` ويمسح الطلب** (مفيش تعليق صامت). حالات الأوفرتايم الداخلية: `DETECTED → SUBMITTED → APPROVED → PAID` (أو `REJECTED`).
- **عندهم:** بيتفتح الطلب جوّه `AttendanceCalculationService` (~L327) وقت حساب الأوفرتايم، الموظف هو الطالب، والدورة تسلسلية عادية لحد `approved`.

**بيأثّر إزاي.**
- **عندنا:** معالج `overtime_entries` بيدخل صف في `overtime_entries` (`source=PRE_REQUESTED`, `hoursRequested`, معدّل من الكونفيج `overtime.multiplier_weekday≈1.5`, حالة `APPROVED`)، والمستحَق `payable=min(approved, actual)` بيتحسب وقت مزامنة البصمة. معالج `overtime_auto` بيلاقي الصف الموجود بالـ`requestId` ويحطّ `payableHours=hoursActual`. المرجع `OT`. الصف ده **بتستهلكه الرواتب**.
- **عندهم:** `SyncOvertimeOnApprovalDecision` → عند الموافقة `ApproveOvertimeAction` يحطّ `status=approved`؛ ودي الحالة اللي الرواتب بتقراها بعد كده. (المشكلة اللي اتحلّت عندهم كانت إن موافقة المحرّك ما كانتش بتوصل للرواتب أصلًا.)

**عندنا مقابل عندهم.** الاتنين بيغذّوا الرواتب بحالة أوفرتايم معتمدة. الفروق: عندنا **مسار اكتشاف بيومتري آلي كامل** (`OVERTIME_AUTO` + كرون تسوية + تنظيف تلقائي لو مفيش سلسلة)، وعندنا **معاملات متعددة** في الكونفيج (weekday 1.5 / weekend 1.5 / holiday 2)، ومنطق `payable=min(معتمد, فعلي)`. عندهم أبسط: حالة نصّية بتتقلب على `overtime_record` والباقي بيحصل في الرواتب — مفيش مسار كرون-اكتشاف مستقل مذكور، والمعدّل مش مُدار كسياسة متعددة الطبقات.

---

### 3) بطاقة الإذن/الاستئذان (Permission / Exit Permit)

**الاسم + التصنيف.** عندنا `PERMISSION` («استئذان») تحت `time_attendance`. عندهم `exit_permit` (subject-bound على `exit_permit`).

**البيانات الداخلية.**
- **عندنا:** `date`, `from`, `to`. وفيه حارس خاص عند الـsubmit: `payload.permissionType` لازم يطابق `PermissionType` نشط، والمدة (`from`/`to`) ≤ `maxDurationMinutes`.
- **عندهم:** `employee_id`.

**الدورة.**
- **عندنا:** `DRAFT → UNDER_REVIEW` (سلسلة `CHAIN_MANAGER`) → `APPROVED` → `COMPLETED`.
- **عندهم:** بيتفتح من `ExitPermitController::store` (وكمان `SelfServiceAttendanceController`) بعد `ExitPermitService::create`، ودورة تسلسلية لحد `approved`.

**بيأثّر إزاي.** نقطة مهمة هنا:
- **عندنا `PERMISSION` معالجه `attendance_log` مش متعمِل (stub)** — يعني ما بيكتبش جدول مخصّص، وبيقع على السجل العام `REQ-سنة-رقمالطلب` والطلب يُقفل `COMPLETED`. **بس** الاستثناء المهم: بعد الاعتماد، `executeDestination()` **بيعيد حساب يوم الحضور** بتاريخ `payload.date` — يعني أثره موجود لكنه **عايش في الـpayload بس** (مفيش صف «إذن» مستقل).
- **عندهم:** `SyncExitPermitOnApprovalDecision` → عند الموافقة `status=approved` على `exit_permits`، ونوافذ الإذن دي **بيقراها الحضور لاحقًا** عشان يعذر التأخير/الخروج. يعني عندهم **سجل إذن حقيقي بحالة** بيغيّر سلوك الحضور فعليًّا.

**عندنا مقابل عندهم.** هنا **عندهم أقوى في العائلة دي تحديدًا**: عندهم كيان `exit_permit` مستقل بحالة معتمدة بيستهلكه الحضور صراحةً لإعذار التأخير. عندنا الإذن **مالوش جدول مخصّص** (معالج stub) وأثره مقتصر على إعادة حساب يوم الحضور من الـpayload — أضعف كسجل قائم بذاته. لازم يتقال صراحة: **الإذن عندنا شبه-عام (generic-ish)، وعندهم كيان أول-درجة.**

---

### 4) بطاقة تصحيح البصمة (Punch Correction)

**الاسم + التصنيف.** عندنا `PUNCH_CORRECTION` («تصحيح/طلب بصمة») تحت `time_attendance`. **عندهم مفيش نوع مقابل** — مفيش `punch_correction` ضمن الـ13 نوع.

**البيانات الداخلية (عندنا).** `date`, `punchType` (IN/OUT), `time`, `reason`.

**الدورة (عندنا).** `DRAFT → UNDER_REVIEW` (سلسلة `CHAIN_MANAGER`) → `APPROVED` → `COMPLETED`.

**بيأثّر إزاي (عندنا).** معالج `attendance_corrections` بيدخل صف في جدول `attendance_corrections` بـ`correctedPunch` JSON على شكل `{in/out}` مبنيّ من `punchType` و`time`. المرجع `AC`. وبعد الاعتماد `executeDestination()` بيعيد حساب يوم الحضور بتاريخ `payload.date`.

**عندنا مقابل عندهم.** **الطلب ده غير موجود خالص عندهم.** تصحيح البصمة/طلب بصمة يدوية عملية HR/حضور بره دورة الاعتماد عندهم. دي ميزة صافية لنا.

---

### 5) بطاقة السلفة (Advance)

**الاسم + التصنيف.** عندنا `LOAN` هو النوع المالي الموحّد («سلفة») تحت `financial` — مفيش تفرقة نوع بين «سلفة» و«قرض» عندنا، الفرق بيطلع من الحقول والسلسلة. عندهم نوع مستقل `advance` (subject-bound على `loan` بـ`kind='advance'`).

**البيانات الداخلية.**
- **عندنا (`LOAN`):** `amount`, `months`.
- **عندهم (`advance`):** `kind`, `total_amount`؛ بيتخلق عبر `SelfServicePayrollController::requestAdvance` بـ`installment_count=1` وسقف `max_advance_percentage` (افتراضي 50%).

**الدورة.**
- **عندنا:** `DRAFT → UNDER_REVIEW` → `APPROVED` → `COMPLETED`. سلسلة القصد `CHAIN_MANAGER_FINANCE_T`: المدير المباشر ثم **finance بس لو المبلغ ≥ 5000** (عتبة `thresholdMet` على `amount`).
- **عندهم:** صف `loan` بحالة `pending, kind=advance` ثم دورة تسلسلية لحد `approved`.

**بيأثّر إزاي.**
- **عندنا:** معالج `loans_installments` بيدخل صف `loans` (حالة `APPROVED`) + عدد `months` من `loan_installments` (أقساط شهرية متساوية، أول كل شهر جاي، آخر قسط بيمتصّ التقريب). المرجع `LN`. الأقساط دي **بتستهلكها الرواتب** كخصم.
- **عندهم:** `SyncLoanOnApprovalDecision` → عند الموافقة `CreateLoanAction::activate` (`status=active`, `remaining_balance=total_amount`, يعيد توليد جدول القسط الواحد، idempotent)؛ عند الرفض `status=rejected`.

**عندنا مقابل عندهم.** الاتنين بيولّدوا جدول أقساط بتخصمه الرواتب، وفيهم حماية ضد الخصم المزدوج (عندهم guard على `status==pending`، عندنا الحالة بتتقفل مرة). الفرق: عندهم **السلفة نوع منفصل بسقف نسبة مخصوص (50%)** وقسط واحد ثابت؛ عندنا **موحّد مع القرض** والتفرقة بتحصل بالعتبة على السلسلة (finance عند ≥5000) وبعدد الشهور.

---

### 6) بطاقة القرض (Loan)

**الاسم + التصنيف.** عندنا نفس النوع `LOAN` (مافيش نوع «قرض» مستقل — هو والسلفة كيان واحد). عندهم نوع مستقل `loan` (subject-bound على `loan` بـ`kind='loan'`).

**البيانات الداخلية.**
- **عندنا:** `amount`, `months` (نفس `LOAN`).
- **عندهم:** `kind`, `total_amount`؛ بيتخلق عبر `SelfServicePayrollController::requestLoan` بـ`installment_count` بين 2 و60 وسقف `max_loan_percentage` (افتراضي 300%).

**الدورة.** نفس دورة السلفة عند الطرفين (تسلسلي لحد الموافقة).

**بيأثّر إزاي.**
- **عندنا:** نفس معالج `loans_installments` — بيفرق بس في عدد الشهور. المرجع `LN`. وفيه نوع تالت مرتبط: `EARLY_LOAN_SETTLEMENT` («سداد سلفة مبكر»، حقل `loanId`، سلسلة `CHAIN_FINANCE`) بيمرّ على نفس معالج `loans_installments`.
- **عندهم:** نفس `SyncLoanOnApprovalDecision` بالظبط — السلفة والقرض **بيتشاركوا `subject_type=loan` ونفس الـlistener**، الفرق بس في عمود `kind` والسقوف. الموافقة → تفعيل + توليد N قسط شهري؛ الرفض → `rejected`.

**عندنا مقابل عندهم.** تطابق وظيفي عالي (تفعيل + جدول أقساط بيغذّي خصم الرواتب). فرق التصميم: **عندهم اتنين نوع (`advance`/`loan`) بسقفين مختلفين وlistener واحد**؛ عندنا **نوع واحد `LOAN`** + نوع مستقل للسداد المبكر (`EARLY_LOAN_SETTLEMENT`) اللي **مفيش مقابل ليه عندهم**.

---

### 7) بطاقة العهدة — تخصيص (Custody / Asset Assignment)

**الاسم + التصنيف.** عندنا `CUSTODY_REQUEST` («طلب عهدة») تحت `custody_assets`. عندهم `asset_assignment` (subject-bound على `employee_asset`).

**البيانات الداخلية.**
- **عندنا:** `payload.assetIds` (مصفوفة أصول) — بتتحقّق وقت الـsubmit.
- **عندهم:** `employee_id`, `asset_type_id`.

**الدورة — ودي أعقد عائلة عندنا.**
- **عندنا:** عند الـsubmit فيه حارس صارم: كل أصل لازم يكون `AVAILABLE` من غير `currentHolderId` ولا تخصيص مفتوح. `DRAFT → UNDER_REVIEW` (سلسلة `CHAIN_MANAGER_CUSTODY`: المدير ثم custody_officer) → `APPROVED` → معالج `custody_assignments_ack` بيعمل لكل أصل صف `custody_assignments` بحالة `PENDING_ACK` والطلب بيروح **`IN_EXECUTION` (مش `COMPLETED`)** لأنه لسه مستنّي. بعدها دورة يدوية من جزئين: `acknowledgeCustody` (الموظف يقرّ باستلام عهدته) → `PENDING_MANAGER_CONFIRM`؛ ثم `managerConfirmCustody` (المدير المباشر أو صلاحية `custody.assign`) → `ACTIVE` + الأصل يبقى `ASSIGNED` وينحطّ له `holder` (مع حارس trample يرفض لو الأصل متمسوك بالفعل). لمّا **كل** تخصيصات الطلب تتسوّى → الطلب `COMPLETED`. حالات العهدة (`CustodyStatus`): `PENDING_ACK, PENDING_MANAGER_CONFIRM, ACTIVE, RETURN_REQUESTED, RETURNED, TRANSFERRED, LOST, DAMAGED`.
- **عندهم:** الأصل بيتخلق `status=assigned` مبدئيًّا (تجريبي)، والطلب بيتفتح **بس لو `AssetType.requires_approval`**. الدورة تسلسلية لحد قرار واحد.

**بيأثّر إزاي.**
- **عندنا:** معالج `custody_assignments_ack` → صف `custody_assignments = PENDING_ACK`، مرجع `CU`، **completed=false**. الحالة النهائية بتتحقّق بالإقرار + تأكيد المدير.
- **عندهم:** `ApplyAssetDecisionOnApproval` → **الموافقة = NO-OP** (بتأكّد حالة `assigned` التجريبية)، و**الرفض** (لو `asset.status==assigned`) = تراجع: `status=returned, returned_at=now()`.

**عندنا مقابل عندهم — أوضح فرق فلسفي.** عندهم **الأصل بيتخصّص فورًا (تجريبيًّا) والاعتماد وظيفته الرفض بس** — الموافقة ما بتعملش حاجة. عندنا **العكس تمامًا**: الأصل ما بيتملّكش فعليًّا إلا بعد سلسلة **إقرار الموظف + تأكيد مديره** (two-phase handshake)، والاعتماد بيفتح المرحلة دي مش بينهيها. نظامنا هنا **أغنى بمراحل كاملة** (`PENDING_ACK → PENDING_MANAGER_CONFIRM → ACTIVE`) مفيش منها أثر عندهم.

---

### 8) بطاقة العهدة — إرجاع (Custody Return / Asset Return)

**الاسم + التصنيف.** عندنا `CUSTODY_RETURN` («إرجاع عهدة»). عندهم `asset_return` (subject-bound على `employee_asset`).

**البيانات الداخلية.** عندنا `assignmentId`. عندهم `employee_id`, `asset_type_id`.

**الدورة.** عندنا `DRAFT → UNDER_REVIEW` (سلسلة `CHAIN_CUSTODY`: custody_officer) → `APPROVED` → `COMPLETED`. عندهم: الأصل بيتحطّ `returned` مبدئيًّا (تجريبي) ثم دورة تسلسلية.

**بيأثّر إزاي.**
- **عندنا:** معالج `custody_assignments` بيحطّ التخصيص `RETURNED` (`returnedAt`, `condition`)، والأصل يرجع `AVAILABLE` و`holder=null`. المرجع `CUR`، completed=true.
- **عندهم:** نفس `ApplyAssetDecisionOnApproval`: **الموافقة NO-OP**؛ **الرفض** (لو `asset.status==returned`) = **إعادة العهدة**: `status=assigned, returned_at=null`.

**عندنا مقابل عندهم.** نفس النمط: عندهم الإرجاع بيحصل تجريبيًّا والاعتماد بيتراجع عنه لو اترفض؛ عندنا الاعتماد نفسه هو اللي بيثبّت الإرجاع ويحرّر الأصل. عندنا سجل إرجاع بحالة وشرط (`condition`)، عندهم قلب حالة على نفس `employee_asset`.

---

### 9) بطاقة العهدة — نقل (Custody Transfer)

**الاسم + التصنيف.** عندنا `CUSTODY_TRANSFER` («نقل عهدة»). **عندهم مفيش مقابل مباشر** — مفيش نوع «نقل عهدة» ضمن الـ13.

**البيانات الداخلية (عندنا).** `assignmentId`, `toEmployeeId`.

**الدورة (عندنا).** `DRAFT → UNDER_REVIEW` (سلسلة `CHAIN_CUSTODY`) → `APPROVED` → معالج `custody_transfer` → الطلب `IN_EXECUTION` (مستنّي)، لأنه بيقفل التخصيص المصدر `TRANSFERRED` ويفتح `PENDING_ACK` جديد للمستلِم؛ بعدها المستلِم يقرّ ثم مديره يأكّد → `ACTIVE`.

**بيأثّر إزاي (عندنا).** `custody_transfer`: التخصيص المصدر → `TRANSFERRED`، تخصيص جديد `PENDING_ACK` لـ`toEmployeeId`، الأصل `ASSIGNED` و`holder=null` (في الطريق/in-transit). المرجع `CUT`، **completed=false**.

**عندنا مقابل عندهم.** **نقل العهدة من موظف لموظف مباشرة غير موجود عندهم كنوع طلب.** عشان تنقل عندهم لازم إرجاع ثم تخصيص جديد (عمليتين). عندنا عملية واحدة بـhandshake كامل للمستلِم. ميزة صافية لنا.

---

### 10) بطاقة العهدة — فقد/تلف (Custody Loss/Damage)

**الاسم + التصنيف.** عندنا `CUSTODY_LOSS_REPORT` («بلاغ فقد/تلف»). **عندهم مفيش مقابل.**

**البيانات الداخلية (عندنا).** `assignmentId`, `description`.

**الدورة (عندنا).** `DRAFT → UNDER_REVIEW` (سلسلة `CHAIN_MANAGER_CUSTODY`) → `APPROVED` → `COMPLETED`.

**بيأثّر إزاي (عندنا) — ده أهم جزء.** معالج `custody_finance`: لو التخصيص مفتوح → يبقى `LOST` والأصل `RETIRED` و`holder=null`؛ ولو `asset.value > 0` → **بيكتب التزام مالي DEBIT** في جدول `employee_obligations` بفئة `custody_shortfall`، حالة `PENDING`، بقيمة الأصل، ومصدره `asset:<id>`. يعني الفقد **بيتحوّل لمديونية على الموظف بتستهلكها الرواتب**. المرجع `REQ`.

**عندنا مقابل عندهم.** **بلاغ الفقد وتحويله لمديونية غير موجود عندهم إطلاقًا.** دي حلقة مالية-عهدية كاملة (عهدة ضايعة → خصم من الراتب) حصرية لنا.

---

### 11) بطاقة الاستقالة (Resignation)

**الاسم + التصنيف.** عندنا `RESIGNATION` («استقالة») تحت `employment_status`. عندهم `resignation` — **subjectless generic** (بيتقدّم عبر `POST /approvals/requests` من غير subject، والـlistener بيتعرّف عليه من `key=resignation`).

**البيانات الداخلية.** عندنا `lastWorkingDate`, `reason`. عندهم `effective_date`, `reason`.

**الدورة.**
- **عندنا:** `DRAFT → UNDER_REVIEW` (سلسلة `CHAIN_MANAGER_HR_EXEC`: مدير → HR → تنفيذي) → `APPROVED` → `COMPLETED`.
- **عندهم:** generic pending → تسلسلي → approved.

**بيأثّر إزاي — الفرق الأخطر في الكتالوج كله.**
- **عندنا:** معالج `employee_status` بيحطّ `emp.status=notice_period`، يكتب `employee_status_history`، و**بيفتح حالة إنهاء خدمة كاملة**: صف في `offboarding_cases` (حالة `IN_CLEARANCE`, `resignationRequestId`, `lastWorkingDay`) + **5 بنود إخلاء طرف (`clearance_items`)** بأطراف: manager / custody / it / finance / hr (وبند العهدة بيسجّل عدد العُهد المفتوحة). المرجع `OFB`. ودورة الأوفبوردنج ليها حالاتها: `IN_CLEARANCE → IN_SETTLEMENT → SETTLED → CLOSED` (أو `CANCELLED` لو الموظف سحب استقالته في فترة الإشعار)، مع `settlement_lines` (CREDIT/DEBIT).
- **عندهم:** `ApplyResignationOnApprovalDecision` (حارس: approved + subjectless + key=resignation + الموظف مالوش `last_working_day` قبل كده) — **بس بيختم عمودين على `employees`**: `last_working_day = effective_date` و`termination_reason = "استقالة — {reason}"` مقصوص لـ240 حرف. مفيش إخلاء طرف، مفيش تسوية، مفيش أرشفة تلقائية.

**عندنا مقابل عندهم.** الاتنين بيسجّلوا استقالة، لكن **عندهم "SHALLOW stamp" (عمودين وخلاص)** والأوفبوردنج كله يدوي عند HR. عندنا **دورة أوفبوردنج مؤسسية كاملة** (إخلاء طرف 5 أطراف + تسوية + أرشفة). فرق ضخم لصالحنا.

---

### 12) بطاقة إنهاء الخدمة (Termination)

**الاسم + التصنيف.** عندهم `termination` — subjectless-or-subject (target = الموظف في subject لو `subject_type=='employee'`، وإلا ملف الطالب نفسه). **عندنا مفيش نوع اسمه `TERMINATION` بالظبط** — إنهاء الخدمة من الإدارة بيتم عبر تغيير حالة الموظف (`employee_status`) والمسارات المرتبطة (استقالة/تقاعد)، لكن نوع «إنهاء خدمة» مستقل بيبتدي من الطالب مش موجود بنفس الاسم؛ الأقرب عندنا `RETIREMENT` («تقاعد»، سلسلة `CHAIN_HR`، معالج `employee_status`) اللي بيعمل أرشفة فعلية.

**البيانات الداخلية.** عندهم `effective_date`, `reason`. (`RETIREMENT` عندنا بلا حقول مطلوبة.)

**الدورة.** عندهم generic → تسلسلي → approved. عندنا `RETIREMENT`: `UNDER_REVIEW` (HR) → `APPROVED` → `COMPLETED`.

**بيأثّر إزاي.**
- **عندهم:** `ApplyTerminationOnApprovalDecision` (نفس نمط الاستقالة) — **يختم `last_working_day` + `termination_reason = "إنهاء خدمة — {reason}"`** (240 حرف). أثر ضحل، عمودين، والأوفبوردنج يدوي.
- **عندنا (`RETIREMENT`):** معالج `employee_status` بيحطّ `emp.status=archived, isActive=false, archivedAt, archiveReason=تقاعد`، **و بيعطّل حسابات الـ`users` المرتبطة**، ويكتب `employee_status_history`. المرجع `ST`.

**عندنا مقابل عندهم.** عندهم إنهاء الخدمة **ختم عمودين** بلا أرشفة ولا تعطيل حسابات. عندنا التقاعد **بيأرشف الموظف فعليًّا ويقفل وصوله**. لو المطلوب «إنهاء خدمة من الطالب» بالمعنى الحرفي عندهم، فهو نوع موجود عندهم ومش موجود عندنا بالاسم ده — بس أثره عندهم أضعف بكتير من أرشفتنا.

---

### 13) بطاقة الترقية (Promotion)

**الاسم + التصنيف.** عندنا `PROMOTION` («ترقية») تحت `employment_status`. عندهم `promotion` — subjectless-or-subject.

**البيانات الداخلية.** عندنا `toTitle`. عندهم `new_job_title_id`, `new_grade_id`, `new_basic_salary`.

**الدورة.** عندنا `UNDER_REVIEW` (سلسلة `CHAIN_MANAGER_HR_EXEC`: مدير → HR → تنفيذي) → `APPROVED` → `COMPLETED`. عندهم generic تسلسلي.

**بيأثّر إزاي.**
- **عندنا:** معالج `employee_update_promotions` بيدخل صف `promotions`، يحدّث `employees.jobTitle = toTitle`، ويكتب `employee_status_history` (title: قديم→جديد). المرجع `PR`.
- **عندهم:** `ApplyPromotionOnApprovalDecision` (حارس approved) بيطبّق **الحقول الموجودة بس**: `employees.job_title_id / grade_id / basic_salary`، ولو `basic_salary` اتغيّر بيعكسه على `activeContract().basic_salary`. payload فاضي → no-op.

**عندنا مقابل عندهم.** عندهم الترقية **أشمل في الحقول** (مسمى + درجة `grade` + راتب أساسي في عملية واحدة + مزامنة العقد). عندنا الترقية بتغيّر **المسمى بس** (`toTitle`) وبتسجّلها في جدول `promotions` منفصل بتاريخ. لو محتاج الترقية تغيّر الدرجة والراتب سوا، **عندهم بيعمل كده وعندنا لأ في نفس الطلب** (تغيير الراتب عندنا طلب منفصل `SALARY_INCREASE`).

---

### 14) بطاقة تغيير الراتب (Salary Change)

**الاسم + التصنيف.** عندنا `SALARY_INCREASE` («زيادة راتب») تحت `financial`. عندهم `salary_increase` — subjectless generic (الهدف ملف الطالب نفسه).

**البيانات الداخلية.** عندنا `newSalary`, `increase_pct`. عندهم `amount` (زيادة شهرية موجبة SAR), `effective_date`.

**الدورة.** عندنا `UNDER_REVIEW` (سلسلة `CHAIN_MANAGER_HR_EXEC_PCT`: مدير → HR ثم **تنفيذي بس لو `increase_pct ≥ 10`**) → `APPROVED` → `COMPLETED`. عندهم generic تسلسلي.

**بيأثّر إزاي.**
- **عندنا:** معالج `salary_update_history` بيحدّث `employees.basicSalary = newSalary` مباشرة، ويكتب `employee_status_history` (salary: قديم→جديد). المرجع `SAL`.
- **عندهم:** `ApplySalaryIncreaseOnApprovalDecision` (حارس approved + amount>0): لو `effective_date` في المستقبل → **NO-OP + لوج "apply manually"** (مفيش scheduler)؛ غير كده `employees.basic_salary += amount` (تقريب لخانتين) **ويعكسه على `activeContract().basic_salary`**. amount≤0 → يتخطّى.

**عندنا مقابل عندهم.** فرقان مهمّان: (1) **النموذج**: عندنا الموظف بيدخل **الراتب الجديد المطلق (`newSalary`)** والنظام بيحطّه؛ عندهم بيدخل **دلتا (`amount`)** بتتضاف. (2) **التاريخ المستقبلي**: عندهم الزيادة المؤجّلة **بتتأجّل يدويًّا (تُهمَل بصمت مع لوج)** لأن مفيش scheduler؛ عندنا التطبيق فوري عند الاعتماد. (3) عندهم **بيزامن العقد `activeContract`**؛ عندنا بنحدّث `employees.basicSalary` ونسجّل في `employee_status_history` بس (مش مذكور مزامنة عقد). العتبة عندنا على السلسلة (تنفيذي عند ≥10%) — منطق حوكمة مش موجود بنفس الشكل عندهم.

---

### 15) بطاقة النقل بين الفرق (Team Transfer)

**الاسم + التصنيف.** عندنا `TEAM_TRANSFER` («نقل بين الفرق») تحت `employment_status`. **عندهم مفيش نوع نقل بين فرق ضمن الـ13** (فيه seeder ثاني غير مستخدم فيه `transfer` بس مش موصول ولا بيستهلكه أي module).

**البيانات الداخلية (عندنا).** `toTeamId`, `effectiveDate`.

**الدورة (عندنا).** حارس مهم عند الـsubmit: **يُمنع النقل لو عند الطالب أي عهدة مفتوحة** (`PENDING_ACK|PENDING_MANAGER_CONFIRM|ACTIVE|RETURN_REQUESTED`). ثم `UNDER_REVIEW` (سلسلة `CHAIN_TRANSFER`: المدير الحالي → **مدير الفريق المستقبِل** (`receiving_team_manager` بيتحلّ من `payload.toTeamId`) → HR) → `APPROVED` → معالج `transfers_effective_date`.

**بيأثّر إزاي (عندنا).** المعالج بيدخل صف `transfers` بحالة `SCHEDULED`؛ لو `effectiveDate ≤ اليوم` بينفّذ فورًا (`emp.teamId` يتحدّث + `EXECUTED` + status_history team: من→إلى، completed=true)، وإلا الطلب يفضل **`IN_EXECUTION`** لحد ما الكرون اليومي `runScheduledTransfers()` ينفّذه في تاريخه. المرجع `TR`.

**عندنا مقابل عندهم.** **النقل بين الفرق كطلب معتمَد غير موجود عندهم.** وعندنا مش بس موجود، ده كمان **مجدوَل بتاريخ نفاذ** (immediate أو مؤجّل عبر كرون)، وفيه **موافِق ديناميكي = مدير الفريق المستقبِل** (بيتحلّ من الـpayload)، وحارس عهدة مفتوحة. ميزة صافية ومركّبة لنا.

---

### 16) بطاقة الخطابات (Letters)

**الاسم + التصنيف.** عندنا **6 أنواع** تحت `letters` كلها `autoGeneratesPdf=true` ومعالجها `letter_pdf_generator`: `LETTER_SALARY` (تعريف راتب)، `LETTER_EMPLOYMENT` (خطاب توظيف)، `LETTER_EXPERIENCE` (شهادة خبرة)، `LETTER_NOC` (عدم ممانعة)، `LETTER_EMBASSY` (سفارة/تأشيرة)، `LETTER_BANK_LOAN` (قرض بنكي). **عندهم مفيش أي عائلة خطابات.**

**البيانات الداخلية (عندنا).** أغلبها بلا حقول مطلوبة (النوع نفسه بيحدّد نوع الخطاب).

**الدورة (عندنا).** `UNDER_REVIEW` (أغلبها `CHAIN_HR`، والـNOC `CHAIN_MANAGER_HR`) → `APPROVED` → `COMPLETED`.

**بيأثّر إزاي (عندنا).** معالج `letter_pdf_generator` بيدخل صف `letter_requests` (`letterType` = الكود ناقص `LETTER_`، حالة `GENERATED`، و`generatedPdfRef = letters/<code>/<reqId>.pdf` كحجز لملف الـPDF الفعلي لاحقًا). المرجع `LTR`.

**عندنا مقابل عندهم.** **عائلة كاملة (6 أنواع + توليد PDF) موجودة عندنا ومنعدمة عندهم.** خطابات التعريف والخبرة والسفارة كلها خارج نطاق نظامهم.

---

### 17) بطاقة المكافأة/الخصم/المصروف/التسوية-المالية (Obligations: Bonus / Deduction / Expense / Adjustment / Per-diem)

**الاسم + التصنيف.** عندنا مجموعة `financial` بتغذّي **دفتر المديونيات `employee_obligations`**:
`BONUS` («مكافأة/حافز»، معالج `payroll_bonus`)، `PER_DIEM` («بدل سفر/انتداب»، `payroll_allowance`)، `EXPENSE_CLAIM` («صرف مصروفات»، `expense_register`)، `DEDUCTION_OBJECTION` («اعتراض على خصم»، `payroll_adjustment`). عندهم **مفيش أنواع مكافآت/بدلات/مصروفات كدفتر التزامات** — أقرب حاجة الرواتب بتتغذّى من القروض والأوفرتايم بس.

**البيانات الداخلية (عندنا).** `BONUS`: `amount`, `reason`؛ `PER_DIEM`: `amount`؛ `EXPENSE_CLAIM`: `amount`, `description`؛ `DEDUCTION_OBJECTION`: `reason`. وكمان `payload.type` اختياري بيقلب الاتجاه لـ`DEBIT` بدل الافتراضي `CREDIT`.

**الدورة (عندنا).** `BONUS`/`PER_DIEM`/`EXPENSE_CLAIM`: `UNDER_REVIEW` (سلاسل `CHAIN_MANAGER_HR` أو `CHAIN_MANAGER_FINANCE`) → `APPROVED` → `COMPLETED`. `DEDUCTION_OBJECTION`: `CHAIN_HR_FINANCE`.

**بيأثّر إزاي (عندنا).** المعالجات دي كلها `obligationHandler`: لو فيه `amount>0` بتدخل صف في `employee_obligations` (النوع الافتراضي `CREDIT`، فئة `bonus/allowance/expense/adjustment`، حالة `PENDING`، `sourceRequestId`, `effectiveDate`). المرجع `REQ`. الالتزامات دي **بتستهلكها الرواتب مرة واحدة** ودورتها `PENDING → APPLIED` (بحقول `appliedPayrollRunId`, `appliedAt`).

**عندنا مقابل عندهم.** **دفتر المديونيات (bonus/allowance/expense/adjustment + custody_shortfall) بالكامل حصري لنا.** عندهم مفيش قناة اعتماد بتضيف مكافأة أو بدل أو مصروف للراتب — دي حلقة مالية-رواتب كاملة موجودة بس عندنا.

---

### 18) بطاقة التظلّم/البلاغ السرّي/الاعتراضات السرّية (Grievance / Whistleblowing / Confidential objections)

**الاسم + التصنيف.** عندنا تحت `employee_relations`: `GRIEVANCE` («تظلّم/شكوى»)، `WHISTLEBLOWING` («بلاغ عن مخالفة»)، `PENALTY_OBJECTION` («اعتراض على جزاء») — الثلاثة `isConfidential=true`؛ وكمان `APPRAISAL_OBJECTION`, `SUGGESTION`, `HR_MEETING`. **عندهم مفيش أي عائلة علاقات موظفين/تظلّمات.**

**البيانات الداخلية (عندنا).** `GRIEVANCE`/`WHISTLEBLOWING`: `description`؛ `PENALTY_OBJECTION`: `reason`.

**الدورة (عندنا) — وميزة السرّية مهمة.** `UNDER_REVIEW` (`CHAIN_HR` أو `CHAIN_HR_EXEC` للاعتراض على الجزاء). بسبب `isConfidential`، وقت حلّ السلسلة (`resolveChain`) **بيتم إسقاط أي خطوة دورها `direct_manager_of_requester`** — يعني الشكوى ما بتمرّش على المدير المباشر اللي غالبًا هو موضوع الشكوى. → `APPROVED` → `COMPLETED`.

**بيأثّر إزاي (عندنا).** معالجات العائلة دي كلها **stub غير متعمِلة** (`er_case`, `er_case_anonymous`, `appraisal_register`, `suggestions_register`, `meetings_register`) → بتقع على السجل العام `REQ-سنة-رقمالطلب` والطلب يُقفل `COMPLETED`. يعني **الطلب نفسه هو السجل** بلا جدول قضايا مخصّص لسه.

**عندنا مقابل عندهم.** **العائلة دي منعدمة عندهم تمامًا.** وحتى مع إن معالجاتنا لسه stub، عندنا **مفهوم السرّية (`isConfidential`) اللي بيتخطّى المدير المباشر في التوجيه** — دي حوكمة تظلّمات حقيقية مبنية في المحرّك، مفيش أي أثر ليها عندهم.

---

### 19) بطاقة التسوية النهائية (Settlement)

**الاسم + التصنيف.** عندهم نوع مستقل `settlement` (subject-bound على `employee_settlement`). عندنا **مفيش نوع طلب `settlement` مستقل بيتقدّمه الموظف** — التسوية عندنا **جزء داخلي من دورة الأوفبوردنج** (بعد الاستقالة): `offboarding_cases` بتتنقّل `IN_SETTLEMENT` وبتتولّد `settlement_lines` (CREDIT/DEBIT، بعضها `isAuto`). يعني التسوية عندنا **مرحلة مؤسسية جوّه الأوفبوردنج مش طلب اعتماد قائم بذاته**.

**البيانات الداخلية.** عندهم `employee_id`, `net_amount` (معلوماتي؛ الـlistener بيعيد قراءة الصف).

**الدورة.**
- **عندهم:** `SettlementService` (~L127) بيفتح الطلب لمّا مسودة تسوية تتقدّم (`status → pending`) → تسلسلي → `approved`.
- **عندنا:** جزء من متتالية الأوفبوردنج `IN_CLEARANCE → IN_SETTLEMENT → SETTLED → CLOSED`، مش عبر `POST` طلب مستقل.

**بيأثّر إزاي.**
- **عندهم:** `SyncSettlementOnApprovalDecision` (حارس `status==pending`): الموافقة → `status=approved` (جاهزة للصرف — والصرف نفسه خطوة منفصلة)؛ الرفض → `rejected`.
- **عندنا:** التسوية بتتسجّل كـ`settlement_lines` جوّه `offboarding_cases` المرتبطة بالاستقالة (مرجع `OFB`)، وبتتقفل بحالة `SETTLED/CLOSED`.

**عندنا مقابل عندهم.** الفلسفة مختلفة: عندهم **التسوية طلب اعتماد مستقل بحالة على `employee_settlement`** (بوابة قبل الصرف). عندنا **التسوية مرحلة مدمجة في دورة الأوفبوردنج الأكبر** (مرتبطة بالاستقالة، ببنود CREDIT/DEBIT وبنود آلية). عندهم أوضح كـ«طلب تسوية»؛ عندنا أعمق كـ«دورة إنهاء خدمة متكاملة» بس من غير طلب تسوية منفصل بيقدّمه المستخدم.

---

### جدول موجز ختامي — إجمالي الأنواع ودلالة القاعدة الذهبية

| المحور | نظامنا (HR Requests Engine) | المنافس (Approval System) |
|---|---|---|
| **إجمالي الأنواع** | **63 نوع مصنّفة في 9 تصنيفات** (leaves, time_attendance, financial, employment_status, personal_data, letters, custody_assets, training, employee_relations)؛ منها ~50 نشط بعد تعطيل الـ11 نوع إجازة المستقلّة | **13 نوع نظامي** (leave, overtime, exit_permit, recruitment, advance, loan, resignation, salary_increase, asset_assignment, asset_return, settlement, promotion, termination) |
| **الحالات** | 9 حالات مع مسارات تنفيذ (`IN_EXECUTION`) وإلغاء (`CANCELLED`) وإرجاع للاستيفاء (`RETURNED_FOR_INFO`) فعّالة | 5 بالورق / **4 فعّالة** (`pending/approved/rejected/blocked`)؛ **`cancelled` حالة ميتة، مفيش endpoint إلغاء** |
| **بنية الاعتماد** | خطوات متوازية + نصاب (كل خطوة parallel لازم يعتمدوا كلهم)، عتبات (`thresholdMet`)، تصعيد بالـSLA (`runEscalations`)، تفويض، تخطّي المدير للسرّي | **تسلسلي بموافِق واحد لكل خطوة**، **أول رفض بيقتل الدورة**، مفيش نصاب/توازي/تفويض |
| **القاعدة الذهبية للتأثير** | **كل اعتماد بيكتب سجلًا دائمًا** عبر معالج + مرجع `PREFIX-سنة-رقم6`؛ حتى الـstub بيكتب `REQ` ويقفل `COMPLETED` — **مفيش اعتماد بيضيع** | **listeners بأسلوب best-effort داخل `try/catch`**؛ التأثير ممكن يفشل بصمت والقرار يفضل قائم؛ و**`blocked` بيعلّق السجل `pending` للأبد بلا حدث ولا إشعار** |
| **قوة التأثير** | 25 معالج متعمِل بيكتب جداول حقيقية (leaves/loans/transfers/promotions/obligations/offboarding…) + دفتر مديونيات كامل + أوفبوردنج 5-أطراف + مزامنة رواتب | تدرّج: تأثير كامل (leave/loan/salary/promotion/overtime)، **ختم سطحي عمودين** (resignation/termination)، **رفض-فقط والموافقة no-op** (recruitment/asset_assignment/asset_return) |
| **حصري لنا (مفيش عندهم)** | تصحيح البصمة، نقل العهدة، بلاغ فقد العهدة، النقل بين الفرق المجدوَل، **6 خطابات + PDF**، دفتر المكافآت/البدلات/المصروفات، التظلّم/البلاغ السرّي بتخطّي المدير، الأوفبوردنج المتكامل، الأوفرتايم البيومتري الآلي | — |
| **حصري لهم (أضعف أو مختلف عندنا)** | التسوية عندهم **طلب مستقل** (عندنا مرحلة جوّه الأوفبوردنج)؛ الإذن `exit_permit` عندهم **كيان أول-درجة بيعذر الحضور** (عندنا معالج stub)؛ الترقية عندهم بتغيّر مسمى+درجة+راتب معًا (عندنا مسمى بس)؛ `termination` كنوع بيبدأ من الطالب | multi-tenant صريح؛ نموذج زيادة الراتب بالدلتا مع مزامنة العقد |

**الخلاصة للقارئ (صاحب النظام + المبرمج):** الفرق الجوهري مش في العدّ (63 مقابل 13) بل في **الفلسفة**. نظامنا بيعامل كل طلب كـ**معاملة بتكتب سجلًا مؤكّدًا في جدول مخصّص بمرجع متتبَّع**، مع محرّك حوكمة غني (توازي/عتبات/تصعيد/سرّية/جدولة) وحلقات مالية-تشغيلية مقفولة (عهدة→مديونية، استقالة→أوفبوردنج، أوفرتايم بيومتري→رواتب). المنافس بيعامل الاعتماد كـ**إشارة (signal)** بيسمعها listener بيحاول يطبّق أثرها best-effort — أنظف وأبسط معماريًّا وmulti-tenant، لكنه **معرّض للفشل الصامت (`blocked`) وللتأثيرات السطحية (ختم عمودين)**، وناقص عائلات كاملة (خطابات، عهدة متقدّمة، مديونيات، تظلّمات، تصحيح بصمة، نقل فرق).

## الأوفرتايم (العمل الإضافي) — المرونة والدورة الكاملة

الأوفرتايم هو أكتر مكان بتتجلّى فيه الفلسفة المختلفة بين النظامين. عندنا الأوفرتايم مبني على **ثلاث طرق مستقلة بتتلاقى في نفس السجل** (`OvertimeEntry`)، وكل طريقة ليها مصدر (`source`) وحالة (`status`) ومضاعِف (`rate`) خاص بيها؛ والمضاعِف بيتغيّر حسب نوع اليوم (عادي/ويك إند/عطلة)، وفيه طبقة تحكّم زمنية بالتواريخ (نوافذ فتح/قفل) مش موجودة عند المنافس أصلاً. المنافس عنده **طريقة واحدة عملياً**: اكتشاف من البصمة ثم موافقة/رفض، بمضاعِف واحد ثابت، من غير أي بُعد زمني ولا تمييز نوع اليوم.

قبل ما ندخل في الطرق، دي الحقول الفعلية اللي بيشتغل عليها كل طرف عشان الكلام يبقى ملموس.

### الكيانات والحقول الفعلية

**عندنا** — الكيان الأساسي `OvertimeEntry` في `api/src/requests/entities/attendance.entities.ts`، وأعمدته:
- `source`: نوع `'BIOMETRIC_DETECTED' | 'PRE_REQUESTED'` — بيقول القيد اتولد إزاي (بصمة تلقائية ولا طلب مسبق).
- `status`: نوع `'DETECTED' | 'SUBMITTED' | 'APPROVED' | 'PAID' | 'REJECTED'` — دورة الحياة الكاملة لحد الصرف.
- `hoursRequested`: الساعات اللي طلبها الموظف مقدّماً (للـ PRE_REQUESTED).
- `hoursActual`: الساعات الفعلية المحسوبة من البصمة (بتتملأ عند مزامنة ZKTeco).
- `payableHours`: الساعات المستحقة فعلاً للدفع = `min(المعتمد، الفعلي)` — دي اللي الرواتب بتقراها.
- `rate`: مضاعِف الأجر لهذا القيد بالذات (decimal، افتراضي 1.5) — **مخزّن على مستوى كل قيد**، فكل يوم بيحمل مضاعِفه الخاص.
- `requestId` و`payrollRunId`: ربط بسلسلة الاعتماد وبمسير الرواتب عند الدفع.

بالإضافة لكيان `OvertimePeriod` في `api/src/attendance/attendance.entities.ts` (نوافذ الفتح/القفل، هنشرحه في الطريقة التانية)، وحقل `overtimeThresholdHours` على الوردية في `api/src/assets/assets.entities.ts` (عتبة بدء الأوفرتايم **بالساعات** بعد نهاية الوردية، NULL = القيمة العامة). ومفاتيح الـ config اللي بتحكم كل ده:
- `overtime.enabled` (افتراضي `true`) — المفتاح العام لتشغيل/إيقاف الاكتشاف.
- `overtime.detection_threshold_hours` (افتراضي `0.5`) — الحد الأدنى للساعات عشان يتسجّل قيد.
- `overtime.biometric_requires_confirmation` (افتراضي `true`) — يتطلب تأكيد مدير قبل الاعتماد ولا اعتماد فوري.
- `overtime.multiplier_weekday` / `overtime.multiplier_weekend` / `overtime.multiplier_holiday` (افتراضي `1.5` لكل واحد) — **ثلاث مضاعِفات منفصلة** حسب نوع اليوم.

**عندهم** — الكيان `OvertimeRecord` في `app/Modules/Attendance/Models/OvertimeRecord.php` (وأعمدته من مايجريشن `2026_04_15_000001_create_attendance_tables.php`):
- `attendance_record_id`: **مفتاح خارجي صلب (cascadeOnDelete)** لسجل يوم حضور واحد — يعني كل قيد أوفرتايم مربوط 1:1 بيوم حضور له وردية.
- `overtime_minutes`: عدد دقائق الأوفرتايم (integer).
- `status`: enum `['pending', 'approved', 'rejected']` بس — تلات حالات مفيش فيها `PAID`.
- `approved_by` / `approved_at` / `rejection_reason` / `notes`.
- **مفيش عمود `rate`، مفيش `source`، مفيش `hoursRequested`، مفيش `payableHours`**. القيد بيحمل الدقائق الخام بس، والمضاعِف بيتحدّد وقت حساب الراتب من مفتاح واحد عام.

ومفاتيحهم في `app/Modules/Payroll/Models/PayrollFormula.php`: `overtime_auto_from_biometric` (افتراضي `'1'`)، `overtime_min_minutes` (افتراضي `'30'`)، `overtime_multiplier` (`1.5`)، وحقل الوردية `overtime_threshold_minutes` (integer، افتراضي `30`) — عتبة **بالدقائق**. وفيه مفتاح `friday_overtime_multiplier` بقيمة `2.0` — بس نرجع له في آخر القسم لأن ليه مفاجأة.

---

### الطريقة الأولى: الاكتشاف التلقائي من البصمة (الطريقة المشتركة)

دي الطريقة الوحيدة اللي الطرفين بيشتركوا فيها من حيث المبدأ، لكن التفاصيل مختلفة.

**عندنا** — الدالة `detectOvertime` في `api/src/attendance/attendance.service.ts`، وبتتنادى من `computeDay` بشرط `checkIn && checkOut && !isFullLeaveDay` (يعني لازم دخول وخروج، وبصمة خروج وحيدة ماتنتجش أوفرتايم وهمي). الخطوات بالترتيب اللي في الكود:

1. **حساب الدقائق الزيادة حسب نوع اليوم**: لو اليوم عادي (`kind = 'WORKING'`) → `extraMinutes = checkOut − shift.end` (بس اللي بعد نهاية الوردية). أما لو ويك إند أو عطلة (`WEEKEND`/`HOLIDAY`) → `extraMinutes = checkOut − checkIn` — **اليوم كله أوفرتايم** لأنه مفيش وردية مرجعية أصلاً. دي نقطة مهمة: عندنا الشغل يوم الأجازة بيتحسب بالكامل.
2. **تحديد المضاعِف**: `rate = overtimeMultiplier(kind)` اللي بيرجّع `multiplier_holiday` أو `multiplier_weekend` أو `multiplier_weekday` حسب نوع اليوم.
3. **أولوية الطلب المسبق**: لو فيه قيد `PRE_REQUESTED` لنفس الموظف/اليوم → بيحدّث `hoursActual` و`rate`، ولو حالته `APPROVED` بيحسب `payableHours = min(hoursRequested, actualHours)` ويرجع. (الموافقة الصريحة بتغلب على أي قفل — هنوضّحها في الطريقة التالتة.)
4. **بوابة الفتح/القفل**: `otOpen = isOvertimeOpen(date, branchId)`. لو مقفول → **بيمسح** أي قيد `BIOMETRIC_DETECTED` حالته `DETECTED` (قفل بأثر رجعي)، ويرجع. (القيد المسبق المعتمد ماينمسّش.)
5. **العتبة**: `threshold = shift.overtimeThresholdHours ?? overtime.detection_threshold_hours` (0.5 افتراضي). لو `actualHours < threshold` → يرجع بدون تسجيل.
6. **التسجيل**: لو فيه قيد `BIOMETRIC_DETECTED` قديم بيحدّثه؛ وإلا بينشئ `OvertimeEntry` جديد بـ`source = 'BIOMETRIC_DETECTED'`. الحالة تعتمد على `overtime.biometric_requires_confirmation`: لو `true` → الحالة `DETECTED` و`payableHours` فاضية (مستني تأكيد مدير)؛ لو `false` → الحالة `APPROVED` فوراً و`payableHours = actualHours`.

**دورة الحالات عندنا**: `DETECTED` → (تأكيد المدير عبر `confirmOvertime`) → `APPROVED` (وبيتحط `payableHours = hoursActual`) أو `REJECTED` (وبيتحط `payableHours = 0`). التأكيد اليدوي بيمرّ على `pendingOvertime` اللي بيرجّع القيود `DETECTED` اللي `requestId IS NULL` وبنطاق الفرع، وصلاحية `overtime.confirm`. القيد اللي اتوجّه لسلسلة اعتماد (`requestId` مش فاضي) بيتعامل معاه في الطريقة التالتة.

**عندهم** — الدالة `syncAutoOvertime` في `app/Modules/Attendance/Services/AttendanceCalculationService.php`. الخطوات:

1. **الشروط الحاكمة**: لازم `overtime_auto_from_biometric = '1'` **و** `check_out` موجود **و** `shift_id` موجود **و** `scheduled_end` موجود. يعني لو الموظف اشتغل يوم أجازته اللي مالوش فيه وردية (مفيش `scheduled_end`) → **صفر أوفرتايم**. دي فجوة مقابل نظامنا اللي بيحسب اليوم كله في الأجازة.
2. **حساب الدقائق**: `overtimeMinutes = checkOut > scheduledEndAt ? diff : 0` (مع مراعاة `is_overnight` بإضافة يوم).
3. **بوابتين للتأهيل**: `qualifies = checkOut > (scheduledEnd + overtime_threshold_minutes)` **و** `overtimeMinutes >= overtime_min_minutes`. عتبتين بالدقائق: واحدة من الوردية وواحدة أرضية عامة (30 دقيقة).
4. **الحفظ (idempotent)**: لو مأهّلش وفيه قيد `pending` قديم → بيمسحه **وبيلغي طلب الاعتماد المفتوح** المرتبط بيه (`ApprovalRequest` بحالة `pending` → `cancelled`) عشان ماينفعش يفضل معلّق في طابور المعتمِدين على سجل اتحذف. لو مأهّل وفيه قيد `pending` والدقائق اتغيّرت → بيحدّثه. لو مفيش → بينشئ `OvertimeRecord` بحالة `pending`، وبعدين `engine->openIfConfigured('overtime', 'overtime_record', ...)` بيفتح دورة اعتماد لو الـ tenant ضابط سلسلة أوفرتايم.

**دورة الحالات عندهم**: `pending` → `approved` أو `rejected`. التأكيد بيتم بطريقتين: (أ) مباشرة عبر `OvertimeController::approve/reject` (راوت `attendance/overtime/{id}/approve` بصلاحية `overtime:approve`)، اللي بينادي `ApproveOvertimeAction` (يحط `status='approved'`, `approved_by`, `approved_at`) أو `RejectOvertimeAction`؛ (ب) عبر `SyncOvertimeOnApprovalDecision` listener اللي بيسمع `ApprovalFinalized` من محرك الاعتماد ويعكس القرار على السجل — لكنه **pending-guarded** يعني ماينفعش يكتب فوق قيد اتحسم قبل كده.

الفرق في الطريقة دي نفسها: عندنا الاكتشاف بيميّز نوع اليوم ويخزّن مضاعِف مستقل لكل قيد ويحسب اليوم كله في الأجازة؛ عندهم الاكتشاف بيتطلب وردية مجدولة، بيسجّل دقائق خام بس، والمضاعِف بيتأجّل لوقت الراتب بقيمة واحدة عامة.

---

### الطريقة الثانية: نافذة الفتح/القفل بالتاريخ — **عندنا فقط**

دي طبقة تحكّم زمنية مالهاش أي مقابل عند المنافس. الكيان `OvertimePeriod` في `api/src/attendance/attendance.entities.ts` بحقول: `name`، `fromDate`، `toDate`، `effect` (نوع `'OPEN' | 'CLOSED'`)، `branchId` (NULL = كل الفروع، قيمة = فرع بعينه)، `isActive`.

القرار بيتاخد في `isOvertimeOpen(date, branchId)` (نفس الملف، `attendance.service.ts`):
1. بيجيب الفترات النشطة اللي **بتغطّي اليوم** (`fromDate <= date <= toDate`) و**بتطابق الفرع** (`branchId == null` أو `== branchId` — الفرع المحدّد بيتقدّم على العام).
2. **`CLOSED` بيحسم**: لو أي فترة مغطّية أثرها `CLOSED` → مقفول (return false) حتى لو فيه فترة `OPEN` متداخلة.
3. لو فيه `OPEN` → مفتوح.
4. لو مفيش فترات مغطّية → بيرجع للمفتاح العام `overtime.enabled`.

المعنى العملي: تقدر تعمل «افتح الأوفرتايم في رمضان لفرع القاهرة» بفترة `OPEN` مؤرّخة موجّهة بالفرع، أو «اقفل أول أسبوعين من الشهر على مستوى الشركة» بفترة `CLOSED`. وإدارة الفترات كاملة عبر `createOvertimePeriod` / `updateOvertimePeriod` / `deleteOvertimePeriod` مع تحقّق من صيغة التواريخ ومن أن `fromDate <= toDate` ومن أن `effect` واحدة من `OPEN|CLOSED`.

**التأثير على السجلات**: البوابة دي بتشتغل في `detectOvertime` (خطوة 4 فوق). لما الفترة تبقى `CLOSED`، النظام **بيمسح فعلياً** أي قيد `BIOMETRIC_DETECTED` غير معتمد لنفس اليوم — يعني قفل بأثر رجعي بينضّف القيود المكتشفة اللي لسه ماحدش أكّدها. المهم: القيد `PRE_REQUESTED` المعتمد **ماينمسّش** (الموافقة الصريحة أقوى من القفل).

**عندهم**: لا يوجد أي مفهوم للنوافذ الزمنية. الأوفرتايم بيتحكم فيه مفتاح واحد `overtime_auto_from_biometric` (شغّال/مقفول للـ tenant كله)، من غير أي بُعد تاريخي ولا استهداف بالفرع. لو عايز تقفل أسبوع معيّن مالكش غير إنك تطفّي الاكتشاف كله للمنشأة بالكامل.

---

### الطريقة الثالثة: طلب الموظف/المدير للأوفرتايم مقدّماً (PRE_REQUESTED)

دي بتخلّي الأوفرتايم «مخطّط له» بدل ما يكون اكتشاف بعدي بس. المعالج `overtimeHandler` (مفتاح الوجهة `overtime_entries`) في `api/src/requests/destinations.service.ts`:
- بيتنفّذ بعد ما طلب الأوفرتايم يعدّي في سلسلة الاعتماد، وبينشئ `OvertimeEntry` بـ`source = 'PRE_REQUESTED'`، `hoursRequested = payload.hours`، `rate = payload.rate ?? overtime.multiplier_weekday ?? 1.5`، والحالة `APPROVED` مباشرة (لأنه خلص اعتماده كطلب).
- بعد كده لما البصمة تتزامن، `detectOvertime` بيلاقي القيد المسبق ده ويحسب `payableHours = min(hoursRequested, actualHours)` — يعني الموظف طلب 3 ساعات وبصم ساعتين ونص → بيتدفع ساعتين ونص بس؛ طلب 3 وبصم 4 → بيتدفع 3 (السقف المعتمد). ده بيمنع الدفع الزائد وبيحترم اللي المدير وافق عليه فعلاً.

وفيه جسر تالت مهم بيربط الاكتشاف بالاعتماد: `reconcileAutoOvertime` (كرون `@Cron('45 */3 * * * *')`) في `api/src/requests/requests.service.ts`. ده بياخد القيود `BIOMETRIC_DETECTED` اللي حالتها `DETECTED` (اللي مالهاش سلسلة لسه) ويوجّهها لنوع الطلب `OVERTIME_AUTO` لو الـ tenant ضابط سلسلة اعتماد ليه: بينشئ request، بيربطه بالقيد (`entry.requestId`) ويحوّل حالته لـ`SUBMITTED`، بيحلّ السلسلة؛ لو `autoApprove` → الطلب `APPROVED` وبينفّذ الوجهة، وإلا `UNDER_REVIEW`. ولو مفيش سلسلة مضبوطة → القيد يفضل `DETECTED` للتأكيد اليدوي. والمعالج `overtimeAutoHandler` (مفتاح `overtime_auto`) بيقفل الدايرة: يلاقي القيد بالـ`requestId`، يحطّه `APPROVED` ويحسب `payableHours = hoursActual`.

يعني عندنا فعلياً مسارين للتأكيد: تأكيد مدير يدوي مباشر (`confirmOvertime`)، أو توجيه لسلسلة اعتماد متعددة الخطوات مع SLA وتصعيد (عبر محرك الطلبات نفسه).

**عندهم**: **مفيش endpoint لطلب أوفرتايم أصلاً**. راوتات `OvertimeController` هي `index` (قراءة) و`approve` و`reject` بس — مفيش `store`/`create`. المفتاح `overtime_auto_from_biometric` قيمته `'0'` وصفه «employee-requested only»، لكن مافيش أي مسار في الكود يخلّي الموظف يطلب. فعملياً لو طفّيت الاكتشاف التلقائي، الأوفرتايم **بيبطّل يتسجّل خالص** — الوضع «بطلب الموظف» وضع ميّت مالوش تنفيذ. فمافيش عندهم لا طلب مسبق، ولا `min(المطلوب، الفعلي)`، ولا أولوية للموافقة على القفل (لأنه مفيش قفل أصلاً).

---

### المضاعِف: إزاي بيتحوّل لفلوس

**عندنا** — كل قيد بيحمل `rate` بتاعه، والرواتب في `api/src/payroll/payroll.service.ts` بتقرا القيود `status = 'APPROVED'` في الفترة وتحسب:
- `otHours = Σ payableHours`
- `otAmount = Σ (payableHours × rate × hourRate)`

فكل يوم بيتدفع بمضاعِفه الفعلي حسب نوعه (عادي 1.5، ويك إند، عطلة — كل واحد بقيمته المخزّنة على القيد). التمييز بين أنواع الأيام محفوظ لحظة الاكتشاف ومربوط بالقيد نفسه، فمابيضيعش وقت الحساب.

**عندهم** — `PayrollCalculationService::calculateOvertime`: بياخد `OvertimeRecord` بحالة `approved` في الفترة، `totalMinutes = Σ overtime_minutes`، `totalHours = totalMinutes / 60`، و`pay = totalHours × hourlyRate × overtime_multiplier`. المضاعِف **قيمة واحدة ثابتة** (1.5) بتتطبّق على كل الدقائق سواء كانت يوم عادي أو جمعة أو عيد.

وهنا المفاجأة اللي وعدت بيها: المفتاح `friday_overtime_multiplier = 2.0` **معرّف في `PayrollFormula.php` لكنه غير مُستخدَم في أي سطر في المشروع كله** — بحثت عنه في كل ملفات الـ PHP فماظهرش إلا في سطر تعريفه. يعني حتى «مضاعِف الجمعة» اللي ممكن حد يفتكره ميزة، هو فعلياً **حقل ميّت (dead config)**؛ الأوفرتايم يوم الجمعة أو العيد بيتدفع بنفس الـ1.5 زيّه زي أي يوم عادي. فالوصف الأدق إنهم مضاعِف واحد مسطّح، من غير أي تمييز نوع يوم شغّال إطلاقاً، لا عطلة ولا جمعة.

---

### الخلاصة الجوهرية للفرق

نظامنا بيدّي صاحب المنشأة **ثلاث ذراعات تحكّم متعامدة**: مصدر القيد (بصمة تلقائية / طلب مسبق)، وبُعد زمني (نوافذ فتح/قفل مؤرّخة وموجّهة بالفرع مع أولوية `CLOSED`)، ومضاعِف مميّز حسب نوع اليوم مخزّن على كل قيد على حدة — كل ده بيمرّ في دورة حالات كاملة (`DETECTED → SUBMITTED → APPROVED → PAID`) عبر محرك اعتماد فيه خطوات وSLA وتصعيد، وبيوصل للرواتب بـ`payableHours × rate` محسوب صح لكل يوم، مع منطق `min(المطلوب، الفعلي)` اللي بيحمي من الدفع الزائد.

المنافس عنده **ذراع واحدة**: اكتشاف من البصمة (بشرط وجود وردية مجدولة) ثم `pending → approved/rejected`، بدقائق خام، وبمضاعِف واحد مسطّح 1.5 بيتحدّد لحظة الراتب — من غير نوافذ زمنية، من غير طلب مسبق قابل للتنفيذ، من غير حساب لليوم كامل في الأجازة، ومن غير أي تمييز نوع يوم شغّال (مضاعِف الجمعة عندهم حقل معرّف بس ميّت). باختصار: هما عندهم «اكتشِف واعتمد»؛ إحنا عندنا «اكتشِف، أو خطّط مقدّماً، أو افتح/اقفل زمنياً بالفرع — وادفع بالمضاعِف الصح لكل يوم».

## دورة الاستقالة وإنهاء الخدمة الكاملة

دي أعمق نقطة فرق بين النظامين، وهنمشي فيها خطوة بخطوة من ساعة ما الموظف يضغط «قدّم استقالة» لحد ما ملفه يتقفل ويتعطّل دخوله. الخلاصة اللي هتتأكد منها في السطور اللي جاية: **عندنا خط أنابيب (pipeline) مؤتمت ومترابط، الاستقالة المعتمدة بتشغّل سلسلة كاملة لوحدها؛ عند المنافس مجرد «ختم تاريخ» على كارت الموظف، وبعدها HR لازم يشتغل يدوي في ٣ محطات منفصلة مفيش حاجة رابطاها ببعض**.

---

### أولًا: عندنا — خط الأنابيب المؤتمت (Automated Pipeline)

#### 1) تقديم الاستقالة والموافقة عليها
الموظف بيقدّم طلب من كتالوج الطلبات كودُه `RESIGNATION` (الـ `destinationHandler` بتاعه = `employee_status`)، والـ payload فيه `lastWorkingDate` و`reason`. الطلب بيمشي في سلسلة الاعتماد العادية زي أي طلب. لحد هنا مفيش فرق جوهري — الفرق بيبدأ **لحظة الاعتماد**.

#### 2) ماذا يحدث تلقائيًا لحظة الاعتماد — `employeeStatusHandler`
الملف: `api/src/requests/destinations.service.ts`. أول ما الطلب يتعتمد، الـ handler ده بيشتغل جوّه نفس الـ transaction بتاعة تنفيذ الطلب:

- بيقرأ الموظف، وبيحسب `newStatus`. لأن `type.code === 'RESIGNATION'` بيحطّ حالة الموظف على **`notice_period`** (مش archived على طول — دي نقطة مفصلية: الموظف بيدخل «فترة إشعار» رسمية مش بيتشال فورًا). لاحظ إن `RETIREMENT` بالمقابل بيروح `archived` على طول ويعطّل الدخول، فالتفرقة بين المسارين محسوبة.
- بيكتب سجل دائم في **`EmployeeStatusHistory`**: `oldStatus` (الحالة القديمة)، `newStatus = 'notice_period'`، و`reason` بيتضمّن آخر يوم عمل والسبب (`استقالة — آخر يوم عمل ...`)، مع `requestId` عشان الأثر يفضل مربوط بالطلب الأصلي.
- **وهنا القلب**: لأن الكود `RESIGNATION`، بيفتح تلقائيًا **`OffboardingCase`** (كائن حالة إنهاء خدمة مستقل — مش مجرد حقل على الموظف):
  - أول بيتأكد إنه مفيش حالة مفتوحة قبل كده (`status IN ['IN_CLEARANCE','IN_SETTLEMENT','SETTLED']`) عشان ميكرّرش الملف.
  - بيسجّل `employeeId`، و`resignationRequestId` (يربط الحالة بطلب الاستقالة)، و`lastWorkingDay` (من `payload.lastWorkingDate`)، والحالة الابتدائية **`IN_CLEARANCE`**.
  - بيعدّ العُهد المفتوحة (`CustodyAssignment` بحالات `PENDING_ACK`, `PENDING_MANAGER_CONFIRM`, `ACTIVE`, `RETURN_REQUESTED`) عشان يكتب العدد في نص بند العهدة.
  - وبيولّد **بنود الإخلاء الخمسة** (`ClearanceItem`) دفعة واحدة، كل بند بجهته (`party`) وحالته الابتدائية `PENDING`.

#### بنود الإخلاء الخمسة عندنا (الجهات الخمس)
1. **`manager`** — «تسليم المهام ونقل المعرفة».
2. **`custody`** — «إرجاع العُهد والأصول» (والنص بيتحقن فيه عدد العُهد المفتوحة، أو «لا عهد مفتوحة»).
3. **`it`** — «إلغاء الصلاحيات والأجهزة».
4. **`finance`** — «تسوية السلف وحساب المستحقات».
5. **`hr`** — «تسليم الوثائق وشهادة الخبرة».

الـ handler بيرجّع مرجع دائم `OFB-<سنة>-<id>` مع note «فُتحت حالة إخلاء الطرف». يعني **باعتماد واحد اتفتحت حالة كاملة ومعاها ٥ بنود جاهزة للتنفيذ** — من غير أي تدخل بشري إضافي.

#### 3) إخلاء الطرف — `completeItem`
الملف: `api/src/offboarding/offboarding.service.ts`. كل جهة بتقفل بندها عبر `completeItem`، والدالة بتفرض:

- **تفويض دقيق**: بند `manager` يعتمده **المدير المباشر هيكليًا** (بيتحقق عبر `resolver.directManagerOf`)؛ باقي الجهات بصلاحياتها من خريطة `PARTY_PERMS` (مثلًا `it` تحتاج `approve.it`، `finance` تحتاج `approve.finance`، `custody` تحتاج `custody.assign`/`approve.custody`)؛ وأي حد معاه `offboarding.manage` (HR/Admin) يقدر يقفل أي بند.
- **الحارس الحاسم على العهدة**: بند `custody` **بيرفض يتقفل** طول ما الموظف ماسك أي عهدة مفتوحة (بيعدّها بنفس الحالات الأربعة ويرمي `BadRequestException`: «الموظف لا يزال ماسكًا N عهدة — أرجعها أولًا أو وثّق قيمتها كخصم وحوّلها LOST»). يعني مستحيل ملف يخلص وحد ماسك لابتوب.
- عند القفل: البند بياخد `status = 'DONE'`، مع `doneBy`, `doneAt`, و`note`، وممكن `amount` (خصم موثّق يدوي بيغذّي التصفية بعدين).
- **الانتقال الآلي**: بعد كل قفل بيعدّ البنود المتبقية (`PENDING`/`BLOCKED`). أول ما توصل صفر → بيستدعي `buildSettlement` **تلقائيًا** وبيحوّل الحالة إلى **`IN_SETTLEMENT`**. مفيش زرار «ابدأ التصفية» — اكتمال الإخلاء نفسه هو الزناد.

#### 4) بناء التصفية آليًا — `buildSettlement` (المعادلة بالتفصيل)
الحسبة كلها من مالية الموظف الفعلية:

- **الأساس**: `gross = basicSalary + housingAllowance + transportAllowance + otherAllowance`، و`dayRate = gross / payroll.monthly_days` (افتراضي 30). لاحظ إننا بنحسب على **الإجمالي بكل البدلات**، مش الأساسي بس.
- **(+) مكافأة نهاية الخدمة**: `eos = gross × eos.months_per_year × years`، حيث `years = (آخر يوم عمل − تاريخ الالتحاق)/365.25 يوم`، و`months_per_year` قابلة للإعداد (افتراضي 0.5 شهر/سنة). صيغة **خطية قابلة للتهيئة على الإجمالي**. تتكتب سطر `CREDIT` بـ `isAuto = true`.
- **(+) بدل رصيد الإجازات**: بياخد `LeaveBalance` نوع `annual` للسنة، بيحسب المتبقّي من الافتتاحي الساري (`openingDays − openingTaken` بشرط `openingExpiry`) زائد الاستحقاق المتراكم **مقسوم شهريًا لتاريخه** (`accrual_mode = monthly` مع مراعاة `probation_months`) ناقص المأخوذ. الرصيد × `dayRate` → سطر `CREDIT`. يعني المستقيل بياخد بدل على اللي استحقّه فعلًا لغاية آخر يوم، مش السنة كاملة.
- **(+) أوفرتايم معتمد لم يُصرف**: بيجمع `OvertimeEntry` بحالة `APPROVED`، وبيحسب `payableHours × rate × hourRate` (حيث `hourRate = dayRate / payroll.daily_hours`، افتراضي 8) — كل قيد بمُضاعِفه المخزّن (عادي/عطلة). سطر `CREDIT`.
- **(−) رصيد السلف المتبقّي**: بيجمع أقساط `LoanInstallment` غير المدفوعة (`paid = false`) لكل قروض الموظف → سطر `DEBIT`.
- **(−) قيمة العهدة المفقودة/التالفة** مع **منع الازدواج**: بيجيب العُهد `LOST`/`DAMAGED`، بس **بيتخطّى** أي أصل اتقيّد قيمته أصلًا كمديونية في دفتر المديونيات (`EmployeeObligation` category `custody_shortfall`, `sourceRef = asset:<id>`) عشان ميتخصمش مرتين (مرة في المسير ومرة في التصفية). الباقي فقط × قيمته → سطر `DEBIT`.
- **(−) خصومات موثّقة يدويًا في بنود الإخلاء**: أي `ClearanceItem` عليه `amount > 0` بيتحوّل لسطر `DEBIT` باسم «خصم من إخلاء الطرف (…)».

كل السطور دي `isAuto = true` وقابلة للتعديل/الحذف/إعادة التوليد **قبل الاعتماد بس** (عبر `recalcLines`, `addLine`, `updateLine`, `deleteLine`، وكلها محميّة بـ `editableCase` اللي بيتأكد إن الحالة `IN_SETTLEMENT`). HR يقدر يضيف بنود يدوية (`isAuto = false`) والصافي بيتحسب `credits − debits`.

#### 5) اعتماد التصفية — `approveSettlement`
لما HR يعتمد: `settlementNet` بياخد الصافي المحسوب، و`settlementApprovedBy`/`settlementApprovedAt` بيتختموا، وبيتولّد مرجعين دائمين: **`settlementDocRef = SET-<سنة>-<id>`** (مستند التصفية) و**`clearanceCertRef = CLR-<سنة>-<id>`** (شهادة إخلاء الطرف)، والحالة تبقى **`SETTLED`** (مقفولة — مفيش تعديل بعد كده). ولو **آخر يوم عمل عدّى فعلًا** بينده `finalize` فورًا.

#### 6) الإنهاء والأرشفة الفعلية — `finalize`
- حالة الموظف → **`terminated`**، `isActive = false`، `archivedAt`، `archiveReason = «انتهاء خدمة — آخر يوم عمل …»`.
- **تعطيل الدخول**: `users.update({ employeeId }, { isActive: false })` — المنتهي مش هيقدر يعمل login ولا يقدّم طلبات.
- سجل `EmployeeStatusHistory` بالانتقال + مرجع التصفية والصافي، وحالة الـ case → **`CLOSED`**.

#### 7) الكرون والتراجع
- **`finalizeDue`** — كرون `@Cron('30 0 * * *')` (يوميًا 12:30 صباحًا): بيمرّ على الحالات `SETTLED` اللي حلّ فيها آخر يوم عمل وينهيها آليًا. يعني حتى لو الاعتماد اتم قبل آخر يوم عمل، النظام نفسه بيقفل الملف في معاده الصحيح **من غير ما حد يفتكر**.
- **`withdraw`** — الموظف نفسه أو HR يقدر يتراجع عن الاستقالة **طول ما الحالة قبل `CLOSED`** (`IN_CLEARANCE`/`IN_SETTLEMENT`/`SETTLED`): الموظف يرجع `active` و`isActive = true`، والـ case → **`CANCELLED`**، مع سجل تاريخي. فيه كمان `myActiveCase` عشان زرار «التراجع» يظهر للموظف في البورتال.

**فالخلاصة عندنا: اعتماد واحد → حالة + ٥ بنود → إخلاء → تصفية محسوبة من ٦ مصادر مالية حقيقية → اعتماد → إنهاء وأرشفة وتعطيل دخول، إما فورًا أو بالكرون. كل محطة بتزنّق اللي بعدها آليًا، وكل خطوة بتكتب سجل دائم.**

---

### ثانيًا: المنافس — ختم تاريخ + خطوات HR يدوية منفصلة

#### 1) الموافقة على الاستقالة — `ApplyResignationOnApprovalDecision.php`
ده listener على حدث `ApprovalFinalized`. لما طلب `resignation` يتعتمد (والشرط `status === 'approved'` وبلا `subject_type`)، بيلاقي الموظف بـ `user_id`، ولو الموظف موجود ومالوش `last_working_day` قبل كده، بيعمل **حاجة واحدة بس**:

```
$employee->update(array_filter([
    'last_working_day'   => $payload['effective_date'] ?? null,
    'termination_reason' => Str::limit('استقالة — ...', 240),
]));
```

يعني **بيختم حقلين على كارت الموظف وخلاص**: `last_working_day` و`termination_reason`. مفيش `notice_period`، مفيش case، مفيش بنود إخلاء، مفيش أي حاجة تتشغّل بعدها. والكومينت في الملف نفسه بيعترف صراحةً إن «الـ offboarding الحقيقي (settlement → clearance → archive) بيفضل **flow يدوي متعمّد** من بروفايل الموظف». ولاحظ إنه لو الطلب مافيهوش `effective_date` بيسيب `last_working_day` فاضي — فحتى التاريخ نفسه ممكن ميتحطّش.

نفس المنطق بالظبط في `ApplyTerminationOnApprovalDecision.php` لطلبات الإنهاء: ختم `last_working_day` + `termination_reason` وبس.

#### 2) التصفية — خطوة يدوية منفصلة تمامًا: `SettlementService.php`
مفيش أي حاجة بتربط الاستقالة بالتصفية. HR لازم **بإيده** يفتح شاشة الموظف ويضغط «Generate» (عبر `SettlementController@generate`)، اللي بيستدعي `SettlementService::generate`:

- بيعمل `EmployeeSettlement` بحالة **`draft`** (مش مربوط بأي كائن استقالة — مجرد سجل تصفية على الموظف).
- بيولّد سطور آلية (`source = 'auto'`):
  - **(+) مكافأة نهاية الخدمة** `endOfServiceGratuity`: صيغة **السعودية المتدرّجة** على **الأساسي فقط**: `½ شهر × basic × سنة` لأول ٥ سنين، و**شهر كامل** لكل سنة بعد كده (`firstFive = basic × 0.5 × 5` + `beyond = basic × 1.0 × (years−5)`). السنين = `hire_date.floatDiffInYears(lastDay)`.
  - **(+) بدل رصيد الإجازات**: `remainingDays × dailyRate`، حيث `dailyRate = basic / daily_salary_divisor` (افتراضي 30). لاحظ الأساسي بس، ومجموع `remaining_days` من `LeaveBalance` للسنة الحالية بدون تقسيم شهري متراكم.
  - **(−) السلف**: `Loan` بحالة `active`، مجموع `remaining_balance` → سطر `deduction`.
- `recompute` بيحسب `total_earnings`, `total_deductions`, `net_amount`.
- HR يقدر يضيف/يشيل سطور يدوية (`addLine`/`removeLine`)، بس **على الـ draft بس** (`assertDraft` بيرمي 422 غير كده).
- **`submit`**: بيحوّل الحالة إلى **`pending`** وبيبعتها للـ approval engine (`openIfConfigured('settlement','employee_settlement', id, ...)`).

**اللي ناقص في تصفيتهم مقارنة بينا**: مفيش سطر **أوفرتايم غير مصروف**، مفيش سطر **عهدة مفقودة/تالفة**، ومفيش **منع ازدواج** مع دفتر المديونيات، ومفيش تحويل تلقائي لمبالغ بنود الإخلاء لخصومات. تصفيتهم ٣ سطور آلية؛ تصفيتنا ٦ مصادر. وكمان مكافأتهم على الأساسي؛ بتاعتنا على الإجمالي وقابلة للتهيئة.

#### 3) اعتماد التصفية بيقف عند `approved` — `SyncSettlementOnApprovalDecision.php`
لما موافقة التصفية تخلص: لو `approved` بيحط الحالة `approved` (+ `approved_by`, `approved_at`) — والكومينت بيقول «ready for payout»؛ لو `rejected` بيحط `rejected`. **وبيقف هنا**. مفيش أي إنهاء خدمة ولا أرشفة تلقائية بتتشغّل من اعتماد التصفية. الموظف لسه `active` في النظام حتى بعد ما تصفيته تتعتمد.

#### 4) الأرشفة — خطوة يدوية ثالثة منفصلة: `ArchiveEmployeeAction.php` + `ClearanceService.php`
عشان الموظف يتقفل فعلًا، HR لازم **بإيده** ينده أرشفة (`ArchiveEmployeeAction::execute`)، اللي بتعمل بالترتيب:

- **بوابة العهدة**: `ClearanceService::assertCleared($employeeId)` — لو فيه `EmployeeAsset` بحالة `assigned` ومش متنازَل عنه (waiver)، بترمي **409** «Employee has outstanding custody…». فيه دعم لـ blanket waiver (تنازل شامل) يفكّ البوابة.
- بعدها: `status = 'archived'`، `archived_at`، `archive_reason`، وتعطيل `user->is_active = false`.

يعني بوابة العهدة عندهم موجودة وقوية (زي بتاعتنا)، **بس** هي خطوة يدوية معزولة، مش جزء من سلسلة بتتشغّل لوحدها. ومفيش عندهم مقابل لحالة `notice_period`، ولا `finalizeDue` cron يقفل الملف في معاد آخر يوم عمل تلقائيًا، ولا زرار `withdraw` مبني على كائن حالة يرجّع الموظف نشط.

---

### الفرق الجوهري في جملة
عندنا **كائن `OffboardingCase` واحد بيربط كل السلسلة** ودورة حالاته (`IN_CLEARANCE → IN_SETTLEMENT → SETTLED → CLOSED`، أو `CANCELLED`) هي اللي بتسوق العملية: كل مرحلة بتفتح اللي بعدها آليًا، والإنهاء بيتم فورًا أو بالكرون، والدخول بيتعطّل، وكل خطوة سجل دائم. عند المنافس **مفيش كائن رابط أصلًا**: الاستقالة بتختم تاريخ، والتصفية سجل `EmployeeSettlement` منفصل بيقف عند `approved`، والأرشفة `Action` تالتة يدوية ببوابة عهدة معزولة — تلات محطات مفكوكة، ربطها يدوي على HR، ولو HR نسي أي واحدة، الموظف يفضل «مستقيل على الورق» لكن `active` في النظام ودخوله شغّال.

## مرونة الورديات والجدول الأسبوعي + النقل بين الفرق

الموضوع ده فيه محورين متشابكين: (١) إزاي كل نظام بيحدد «الموظف ده بيشتغل إمتى ووردية إيه في اليوم الفلاني»، و(٢) لما موظف يتنقل من فريق لفريق، هل النقل ده بينفّذ فعلاً ولا بيفضل ورقة في الهوا. هنمشي نقطة نقطة، وكل نقطة هنقولك الحقول الداخلية بأسمائها، ودورة الحالة خطوة بخطوة، وبتأثّر إزاي على أي سجل أو رصيد.

### ١) تعريف الوردية نفسها (الكيان الأساسي)

**عندنا** — الوردية بتتعرّف في كتالوج الورديات `Shift` اللي بيستهلكه `attendance.service.ts` عن طريق دالة `shiftConfig(name)` (بتجيب الوردية بالاسم، وبتشيل لاحقة زي «(يوم خاص)» وتجرّب المطابقة المجرّدة). الحقول اللي المحرّك بيقراها من الكائن ده (المتغيّر `sc`): `shiftMode` (بياخد قيمة `fixed` أو `flexible`)، `requiredHours` (ساعات مطلوبة للنمط المرن)، `graceMinutes` (فترة سماح خاصة بالوردية، ولو مش متحطّة بيرجع للمفتاح العام `attendance.grace_minutes`)، `overtimeThresholdHours` (عتبة كشف الأوفرتايم، ولو فاضية بيرجع للمفتاح العام `overtime.detection_threshold_hours`)، وأربع نوافذ للبصم: `checkinFrom`/`checkinTo` و`checkoutFrom`/`checkoutTo`. النوافذ دي بتشتغل في `computeDay` تحت شرط `hasWindows` (لازم النافذتين موجودين مع بعض)، وبتستخدم عشان تصنّف البصمات الخام: أنهي بصمة «دخول» وأنهي «خروج» بناءً على وقوعها جوه النافذة، مش مجرد «أول بصمة/آخر بصمة».

**المنافس** — الوردية هي موديل `Shift.php` (Attendance/Models). الحقول: `start_time`, `end_time`, `break_duration_minutes` (وقت راحة غير مدفوع بيتخصم مرة من إجمالي دقائق العمل)، `grace_period_minutes`, `overtime_threshold_minutes`, `is_overnight` (بوليان للوردية الليلية)، `is_active`. **مفيش عندهم** مفهوم `shiftMode`/`flexible`، **ولا** `requiredHours`، **ولا** نوافذ `checkin/checkout` — يعني البصمة عندهم بتتقارن بـ`scheduled_start`/`scheduled_end` على طول من غير تصنيف بنوافذ. في المقابل عندهم حاجة إحنا مبنعملهاش صراحةً في النمط الثابت وهي `break_duration_minutes` (الراحة تتخصم من العمل).

### ٢) وردية لكل يوم + الجدول الأسبوعي

هنا في فرق **بنيوي** مهم يتفهم كويس، لأنه بيغيّر طريقة الاستخدام.

**عندنا** الجدول الأسبوعي هو كيان `ScheduleEntry` (جدول `weekly_schedule_entries` في `attendance.entities.ts`)، ومفتاحه الفريد `@Unique(['weekStart', 'employeeId'])`. لاحظ: الصف الواحد فيه `shiftName`+`startTime`+`endTime` **واحدة للأسبوع كله**، و`weekStart` = تاريخ الأحد اللي بيقع فيه اليوم (بتتحسب بدالة `weekKeyOf`). يعني الكيان ده بطبيعته «أحمد الأسبوع ده على وردية ١٠ والأسبوع اللي بعده على وردية ١١» — تنويع **أسبوعي** مش **يومي**. عشان تعمل «الاتنين وردية صباحي والتلات وردية مسائي» في نفس الأسبوع، بتلجأ لكيان تاني: `ScheduleDayOverride` (جدول `schedule_day_overrides`، مفتاح `@Unique(['employeeId', 'date'])`) — تجاوز وردية **ليوم بعينه** بتاريخه، وده بيتقدّم على وردية الأسبوع. فعلياً «وردية لكل يوم» عندنا بتتحقق بمزيج: وردية الأسبوع كخط أساس + أوفررايد يومي لأي يوم مختلف. وفيه دالة `setDayOverridesBulk` بتعمل الأوفررايد اليومي لمجموعة موظفين بنطاق (شركة/فرع/قسم) دفعة واحدة — «يوم استثنائي بدوام مختلف» من غير ما تلمس وردية الأسبوع.

ترتيب الحسم عندنا لوردية اليوم موجود في دالة `shiftFor(employeeId, date)`، وبالأولوية دي بالظبط:
1. `schedule_day_overrides` لو فيه أوفررايد لليوم ده → بيرجع باسم «(يوم خاص)».
2. وإلا `weekly_schedule_entries` لأسبوع اليوم (`weekKeyOf(date)`).
3. وإلا ساعات جدول عمل الموظف `workSchedule` لو `emp.workScheduleId` موجود.
4. وإلا `DEFAULT_SHIFT` (صباحي ٠٨:٠٠–١٧:٠٠).

**المنافس** الجدول عنده أغنى وأكتر تفصيلاً في نقطة «الوردية لكل يوم». عندهم `schedule_assignments` (موديل `ScheduleAssignment.php`) بيحمل `scope_type` (company/branch/department/team/employee) + `scope_id` + `effective_from`/`effective_to`، وتحته `schedule_assignment_entries` (`ScheduleAssignmentEntry.php`) فيه **صف لكل `day_of_week`** (٠=الأحد..٦) وكل صف بيربط `shift_id` أو `is_off_day=true`. يعني التسنيد الواحد بيوصف الأسبوع كامل بوردية مختلفة لكل يوم جوه سجل واحد — من غير ما تحتاج «أوفررايد» منفصل زينا. وكمان عندهم `WeeklyScheduleEntry` لكل موظف/يوم كطبقة رجوع.

حل الوردية عندهم في `AttendanceCalculationService::resolveScheduleFor` بيمشي بالطبقات دي (بعد استثناءات الجدول اللي هنشرحها في نقطة ٣):
1. التسنيد المُنطبق بالنطاق: `ScheduleAssignmentService::resolveForEmployee` بينده `ScopeResolver` اللي بيرتّب المرشحين من الأخص للأعم: **employee > team > department (بسلسلة الأب حتى الجذر) > branch > company**. وجوه نفس النطاق: المؤرَّخ (اللي ليه `effective_from`) بيغلب المفتوح، والأحدث بداية بيكسب. بعدها `firstWhere('day_of_week', …)`؛ لو الصف `is_off_day` أو بلا وردية بينزل للطبقة اللي تحت.
2. `WeeklyScheduleEntry` لنفس اليوم.
3. `WorkDay` الافتراضي للـtenant لليوم ده.

خلاصة النقطة: **في تفصيل «وردية مختلفة لكل يوم من نفس السجل»، وفي عمق هرمية النطاق (٥ طبقات بأولوية)، المنافس أغنى منّنا**. إحنا بنفصل «يشتغل ولا لأ» عن «وردية إيه»، ونطاق الأوفررايد الجماعي عندنا (شركة/فرع/قسم) موجود لكن الجدولة الأساسية عندنا وردية واحدة للأسبوع + أوفررايد يومي.

**الأثر على السجلات:** أي حل وردية عندنا بيتغذّى منه `computeDay` اللي بيكتب صف `attendance_days` بالحقول: `status`, `lateMinutes`, `earlyLeaveMinutes`, `workMinutes`, `excusedMinutes`, `deductibleMinutes` — والحقول دي هي أساس خصومات المسير. عند المنافس نفس الفكرة: `AttendanceCalculationService` بيكتب في `attendance_records` الحقول `shift_id`, `scheduled_start`, `scheduled_end`, `late_minutes`, `late_deduction_tier`, `total_work_minutes`, `early_departure_minutes`.

### ٣) «آخر سبت في الشهر = دوام»

ده السيناريو اللي بيمتحن مرونة قلب العطلة إلى دوام (والعكس).

**عندنا** الكيان هو `ScheduleExceptionRule` (جدول `schedule_exception_rules`). حقوله: `weekday` (قيمة من `SUN..SAT`)، `occurrence` (`ALL`/`1ST`/`2ND`/`3RD`/`4TH`/`LAST`)، `effect` (`WORK` = يحوّل عطلة→دوام، أو `OFF` = دوام→عطلة)، و`branchId` (`null` = يسري على كل الفروع، أو رقم فرع بعينه بيتقدّم على العام). فـ«آخر سبت دوام» = صف `weekday='SAT'`, `occurrence='LAST'`, `effect='WORK'`. التقييم بيحصل في `evalNonWorking` جوه `attendance.service.ts`:
- الأول بيحسب اسم اليوم، وبيشوف هل هو ويك-إند (من `attendance.weekend_days` أو الفرع أو جدول الموظف).
- بعدين حارس مهم: **العطلة الرسمية `public_holidays` بترجع `true` على طول ومبتتبطّلش بأي قاعدة استثناء** — يعني لو آخر سبت صادف عيد رسمي، يفضل عطلة رغم قاعدة الـWORK.
- بعدين بيجيب القواعد المطابقة لنفس اليوم، بيفلترها بالتكرار عن طريق `occurrenceOf(date)` (بتحسب `index` من ١..٥ و`isLast`) و`occurrenceMatches`، وبيرتّبها بحيث **الفرعية (branchId محدّد) تتطبّق بعد العامة فتحسم**. كل قاعدة `WORK` بتشيل صفة الويك-إند، و`OFF` بتضيفها.

القاعدة دي عندنا **مبتحملش وردية** — هي بس بتقلب «يشتغل/ما يشتغلش». الوردية لليوم اللي بقى دوام بتتحدد بعدها من `shiftFor` العادي (أوفررايد→أسبوعي→جدول عمل→افتراضي). ونطاقها **بالفرع بس** (`branchId`)، مفيش قسم/فريق/موظف على مستوى القاعدة دي.

**الأثر:** `isNonWorkingDay`/`evalNonWorking` بيأثّروا على: (أ) `computeDay` — لو اليوم اتحسب دوام يبقى فيه تأخير/غياب ممكن، ولو عطلة يتسجّل `status='holiday'` والحضور فيه كله شغل بيوم عطلة؛ (ب) `materializeAbsences` — بيتخطّى أيام العطلة فمايكتبش غياب فيها؛ (ج) `workingDaysBetween` — اللي بيحسب أيام العمل الفعلية، وده اللي بيتخصم من رصيد الإجازة (الإجازة بتخصم أيام العمل بس)؛ (د) `dayKind` — تصنيف اليوم (WORKING/WEEKEND/HOLIDAY) اللي بيحدد مُضاعِف الأوفرتايم في `overtime_entries` (`rate`/`payableHours`).

**المنافس** الكيان هو `ScheduleException.php`. حقوله أغنى في المطابقة: `match_type` بياخد `specific_date` (يوم واحد) أو `date_range` (مدى) أو `recurring` (متكرر)، و`effect` (`off_to_work`/`work_to_off`)، و`day_of_week`، و`position` (`every`/`first`/`second`/`third`/`fourth`/`last`)، و`shift_id` (اختياري)، و`priority`، ونطاق `scope_type`+`scope_id`. فـ«آخر سبت دوام» = `match_type='recurring'`, `day_of_week=6`, `position='last'`, `effect='off_to_work'`. منطق «آخر سبت» عندهم في `matchesRecurring`: `last` معناها «مافيش نفس اليوم تاني بعد ٧ أيام في نفس الشهر» (`addDays(7)->month !== month`). الحسم في `ScheduleExceptionService::resolveForDate`: **الفائز = أعلى `priority`، وعند التعادل الأخص نطاقاً** (employee > team > department > branch > company) عن طريق ترتيب `candidatesFor`.

وفيه فرقين لصالح المنافس هنا: (أ) الاستثناء بيتقرا **أول حاجة (الخطوة ٠)** في `resolveScheduleFor` فبيغلب كل الطبقات — و`off_to_work` ممكن **يحدد وردية اليوم بنفسه** عن طريق `shift_id` (ولو مفيش، بياخد ساعات الـtenant الافتراضية `defaultWorkingSchedule`)، أما `work_to_off` بيرجّع `emptySchedule` (اليوم أوف). (ب) نطاق الاستثناء عندهم company→branch→department→team→employee، مش الفرع بس زينا. في المقابل، عندنا الحارس بتاع «العطلة الرسمية تكسب دايماً» صريح وواضح، وعندنا كمان `OvertimePeriod` لفتح/قفل الأوفرتايم بمدى تواريخ (رمضان مثلاً) وهي مسألة تانية منفصلة.

خلاصة النقطة دي: **في مرونة الاستثناء نفسه (تحديد وردية لليوم المقلوب + هرمية النطاق + أنواع المطابقة)، المنافس أوسع**؛ إحنا عندنا حماية أوضح للعطلة الرسمية وفصل نظيف بين «يشتغل ولا لأ» و«وردية إيه».

### ٤) الوردية الليلية (اللي بتعدّي منتصف الليل)

**عندنا** المعالجة الصريحة للوردية الليلية موجودة **في النمط المرن بس**: في `computeDay` تحت `sc?.shiftMode === 'flexible'` بنحسب المدة `dur = shiftEnd > shiftStart ? shiftEnd - shiftStart : shiftEnd + 1440 - shiftStart` — الجزء التاني بيلف حوالين منتصف الليل صح. لكن في النمط الثابت `fixed`، الانصراف المبكر والأوفرتايم بيتحسبوا بدقائق نفس اليوم (`shiftEnd - toMinutes(checkOut)`, و`toMinutes(checkOut) - toMinutes(shift.end)` في `detectOvertime`)، من غير علم صريح بإن النهاية بعد منتصف الليل. يعني **الوردية الليلية عندنا مضبوطة في المرن، وفي الثابت مفترضة نفس-اليوم**.

**المنافس** عنده `is_overnight` بوليان صريح على الوردية، وبيطبّقه في `computeDerivedFields` و`syncAutoOvertime`: `if is_overnight scheduledEndAt->addDay()` — فالانصراف المبكر والأوفرتايم بيتحسبوا صح للوردية اللي تنتهي بعد منتصف الليل حتى في الوضع الثابت. **هنا المنافس أنضف للوردية الليلية الثابتة.**

### ٥) النمط المرن (flexible)

**عندنا** ده تفوّق واضح لصالحنا. لما `sc.shiftMode === 'flexible'`: **مفيش تأخير بوقت البداية أصلاً** — المهم إكمال الساعات. المطلوب `requiredMin = requiredHours*60` (أو مدة الوردية لو مش محدّد). بنطرح الوقت المعذور جوه الوردية (إجازة جزئية/إذن بدون خصم) من المطلوب: `effectiveRequired = requiredMin - excusedInShift`، وبعدين `deficit = effectiveRequired - workMinutes`، والتأخير `lateMinutes = deficit > grace ? deficit : 0`. الحالة بتطلع `late`/`partial_leave`/`present`. ده بيتكتب في `attendance_days` (`lateMinutes`/`workMinutes`/`excusedMinutes`) وبيغذّي المسير.

**المنافس** **مفيش عنده نمط مرن خالص**. الحساب دايماً ثابت في `computeDerivedFields`: شرائح تأخير مقارنةً بـ`scheduled_start` (جوه السماح→`none`، ≤٣٠ دقيقة→`quarter_day`، ≤٦٠→`half_day`، >٦٠→`full_day`)، ومفيش مفهوم «ساعات مطلوبة تكمّلها امتى ما تحب». يبقى **النمط المرن نقطة لصالحنا**.

### ٦) مين يكسب عند التعارض — ملخص القرار

**عندنا** في مسارين مفصولين:
- **وردية اليوم** (`shiftFor`): أوفررايد يومي ← وردية الأسبوع ← ساعات جدول عمل الموظف ← الافتراضية.
- **هل اليوم عطلة** (`evalNonWorking`): العطلة الرسمية تكسب أولاً ولا تتبطّل ← ويك-إند (جدول الموظف `weekendDays` يغلب الفرع يغلب العام `FRI,SAT`؛ و`''` = دوام ٧ أيام صراحة) ← قواعد `ScheduleExceptionRule` (العامة ثم الفرعية اللي تحسم).

**المنافس** قرار واحد متدرّج في `resolveScheduleFor`: استثناء الجدول (خطوة ٠، يغلب الكل) ← تسنيد بالنطاق (ScopeResolver: موظف>فريق>قسم>فرع>شركة، والمؤرَّخ يغلب المفتوح، والأحدث بداية يكسب) ← جدول الموظف الأسبوعي ← `WorkDay` الافتراضي.

### ٧) النقل بين الفرق — الفرق الحاسم

**عندنا: منفّذ بالكامل بتاريخ سريان + جدولة.** نوع الطلب `TEAM_TRANSFER` (في `requests-seed.data.ts`) بسلسلة `CHAIN_TRANSFER` ومعالج `transfers_effective_date`، وحقوله المطلوبة `toTeamId` و`effectiveDate`. دورة الحالة خطوة بخطوة:

1. **السلسلة والمعتمد الخاص:** السلسلة فيها خطوة دور `receiving_team_manager` (السيد seed: مدير مباشر ← **مدير الفريق المستلم** SLA ٣ أيام يتصعّد لـHR ← HR). في `approver-resolver.service.ts` الدور ده بيقرا `payload.toTeamId` ويحلّ **مدير الفريق اللي هيستقبل الموظف** كمعتمد — يعني الفريق المستقبِل بيوافق على استقبال الموظف، مش بس مديره الحالي.
2. **عند الاعتماد النهائي** بيشتغل `transferHandler` في `destinations.service.ts`: بيقرا `effectiveDate`، وبيكتب صف دائم في جدول `transfers` (`Transfer` في `employment.entities.ts`) بالحقول: `requestId`, `employeeId`, `fromTeam = emp.teamId`, `toTeam = payload.toTeamId`, `effectiveDate`, `status='SCHEDULED'`.
3. **لو `effectiveDate <= today`** → بيستدعي `executeTransfer` فوراً: بيكتب `emp.teamId = toTeam` (تعديل فعلي على `Employee`)، ويعلّم `Transfer.status='EXECUTED'` + `executedAt`، ويكتب صف `EmployeeStatusHistory` (`team:from → team:to`, reason «نقل بين الفرق — تنفيذ بتاريخ السريان»). النتيجة `completed=true` والطلب يقفل.
4. **لو `effectiveDate` في المستقبل** → بيرجع `completed=false` والطلب يفضل `IN_EXECUTION`، والـscheduler اليومي `runScheduledTransfers` (في `requests.service.ts`) بيلف على كل `Transfer` بحالة `SCHEDULED`، وأي واحد `effectiveDate <= today` بينفّذه بـ`executeTransfer` جوه transaction، وبعدها يقفل الطلب `COMPLETED` + `completedAt`.

**الأثر النهائي عندنا:** بيتغيّر `employee.teamId`، ويتكتب صف `transfers` دائم قابل للتتبع، ويتكتب `EmployeeStatusHistory` (سجل الحالة الوظيفية)، والطلب يوصل `COMPLETED`. وبما إن teamId هو المرساة لأي منطق مبني على الفريق (سلاسل الاعتماد، النطاقات)، النقل بيسري فعلياً على النظام كله من تاريخ السريان.

**المنافس: غير منفّذ — نوع يتيم.** كلمة `'transfer' => 'نقل'` موجودة **بس** في `ApprovalRequestTypeSeeder.php` (سطر ٢٦) كنوع طلب ضمن قائمة الأنواع، ومذكورة في **تعليق** migration الاعتمادات، ومستخدمة في اختبار `Phase6/ApprovalWorkflowTest.php` (دالة `test_rejection_ends_workflow`) كمجرد **نوع عيّنة** لاختبار محرك الاعتماد العام (بيتأكد إن الرفض بينهي المسار). لما دوّرنا في `app` و`database` كلها على أي كود بيتفاعل مع النقل، النتيجة:
- **مفيش** موديل `Transfer`/`Movement`/`Reassign` خالص.
- **مفيش** listener بيتفاعل مع `subject_type='transfer'`. الموجود بس `SyncOvertimeOnApprovalDecision` (بيتفاعل مع `overtime_record`) و`SyncExitPermitOnApprovalDecision` (أذونات الخروج) — مفيش نظير للنقل.
- **مفيش** أي سطر بيكتب على `employee.team_id` أو `department_id` أو `branch_id` نتيجة اعتماد نقل، **ولا** تاريخ سريان، **ولا** جدولة، **ولا** سجل حركة.

يعني عملياً عند المنافس: لو موظف قدّم طلب «نقل» واتعمد، محرك الاعتماد يقفل الطلب `approved` وخلاص — **الموظف مايتحركش فعلياً من فريقه**، ولا بيورث جدول/نطاق الفريق الجديد، ولا فيه أثر على `attendance_records` أو المسير. النوع «لِيبِل بلا تنفيذ» (label بلا side-effect). وبما إن كل الجدولة عندهم متعلّقة بـ`team_id`/`department_id`/`branch_id` عبر `ScopeResolver`، فالنقل الوهمي ده معناه إن أي «نقل» مفروض يغيّر جدول الموظف أو نطاقه **لن يحصل**، لأن حقول النطاق دي ما اتغيّرتش أصلاً.

**الخلاصة على المحورين:** المنافس أغنى في تشريح الجدول (وردية لكل يوم من سجل واحد، هرمية نطاق من ٥ طبقات، استثناءات بأنواع مطابقة أكتر ووردية داخل الاستثناء، وعلم `is_overnight` صريح). بينما إحنا أقوى في **النمط المرن** (ساعات مطلوبة بدل شرائح تأخير)، وفي **حماية العطلة الرسمية** الصريحة، وفوق كل ده في **النقل بين الفرق**: عندنا منظومة كاملة (طلب → موافقة مدير الفريق المستقبِل → صف `transfers` مجدول → تنفيذ بتاريخ سريان فوري أو بالـscheduler → تعديل `teamId` + سجل حالة وظيفية → قفل الطلب)، وعندهم مجرد نوع طلب يتيم بلا أي تنفيذ.

## دورة العهدة (الأصول) الكاملة

الفكرة اللي بتفرّق النظامين هنا مش «إن الاتنين بيديوا الموظف لابتوب ويرجّعوه». الفرق إن نظامنا بيتعامل مع العهدة كـ**أصل له قيمة مالية ومسؤول قانوني وسجل حركة**، بينما المنافس بيتعامل معاها كـ**فلاج بحالتين** (`assigned`/`returned`) على صف في جدول. تعال نمشي على الدورة خطوة بخطوة عند الطرفين.

---

## ① كتالوج الأنواع والأصول — إيه اللي بنسلّمه أصلاً؟

**عندنا** فيه فصل بين حاجتين مختلفتين تماماً:
- **كتالوج الأنواع** `AssetType` (جدول `asset_types` في `assets.entities.ts`): مجرد قايمة تصنيفات للإعدادات — `id`, `name` (unique), `isActive`. ده «نوع» مجرّد.
- **الأصل الفعلي المرقّم** `Asset` (جدول `assets` في `custody.entities.ts`): ده الكيان الحقيقي اللي بيتسلّم. حقوله: `id`, `name`, `category`, `serialNumber`، وأهم حقلين: `value` (decimal 18,2 — **قيمة الأصل بالفلوس، هي دي اللي هتتحوّل لخصم لو ضاع**)، و`status: AssetStatus` بثلاث قيم `AVAILABLE | ASSIGNED | RETIRED`، و`currentHolderId` (مين ماسكه دلوقتي فعلاً). يعني عندنا **مخزون حقيقي**: كل قطعة ليها هوية وقيمة وحالة مخزنية وحائز.

**عند المنافس** فيه جدول واحد بس، وهو خلطة بين الاتنين: `asset_types` (migration `2026_06_25_000007_create_asset_tables.php`) حقوله `name_ar`, `name_en`, `code`, `is_active`، وبعدين migration تاني (`2026_06_30_000009`) ضاف `requires_approval`. الأصل «المُسلّم» عندهم هو `employee_assets` وحقوله: `employee_id`, `asset_type_id`, `identifier` (سيريال/تاج كـ string حر), `status` (default `assigned`), `assigned_at`, `returned_at`, `notes`. **مفيش عندهم مخزون مركزي للأصل نفسه** — مفيش صف اسمه «اللابتوب رقم 5» له حالة عامة (متاح/متقاعد) وحائز حالي؛ فيه بس «تخصيص» بيربط نوع بموظف. ومفيش **حقل `value` أصلاً** — الأصل عندهم بلا قيمة مالية في الداتا، وبالتالي مفيش أي أساس لأي مسؤولية مالية لاحقاً. دي أول وأخطر فجوة، وهنشوف نتيجتها في الآخر.

---

## ② التخصيص — إزاي الأصل بيروح للموظف؟

**عندنا الدورة ثنائية الإقرار (two-phase)** وده جوهري:

1. الموظف بيقدّم طلب `CUSTODY_REQUEST` مختار فيه أصول من المتاح. عند التقديم (`requests.service.ts` → `submit`) فيه حرّاس حاسمين: كل أصل لازم يكون `status === 'AVAILABLE'` و`currentHolderId` فاضي، **ومفيش إسناد مفتوح تاني** لنفس الأصل (بنعُدّ الإسنادات في `PENDING_ACK/PENDING_MANAGER_CONFIRM/ACTIVE/RETURN_REQUESTED`). لو أي شرط سقط → رفض مبكر.
2. أول ما الطلب يتعتمد بيشتغل `custodyAssignHandler` (`destinations.service.ts`): بيعمل صف `CustodyAssignment` لكل أصل بحالة **`PENDING_ACK`**، والطلب بيرجع `completed:false` فيبقى `IN_EXECUTION` (مش مكتمل لسه). لاحظ إن الأصل **بيفضل `AVAILABLE`** طول ما إحنا في `PENDING_ACK` — عشان كده فيه حارس تاني جوه الـ handler بيعيد فحص الإسناد المفتوح عشان طلبين اتقدّموا سوا على نفس الأصل ميعملوش إسنادين.
3. **إقرار المستلم**: الموظف بيأكّد الاستلام بـ `acknowledgeCustody` (`requests.service.ts`) → الحالة تتنقل لـ **`PENDING_MANAGER_CONFIRM`** ويتكتب `acknowledgedAt`. الحارس: لازم العهدة باسمه هو، ولازم تكون `PENDING_ACK`.
4. **تأكيد المدير المباشر**: `managerConfirmCustody` → الحالة تبقى **`ACTIVE`** (وده «الملزِم قانونياً») ويتكتب `managerConfirmAt`. الحرّاس: لازم تكون `PENDING_MANAGER_CONFIRM`، والفاعل لازم يكون المدير المباشر لصاحب العهدة أو صاحب صلاحية `custody.assign` أو `super_admin`. وفيه **حارس دهس (race guard)**: لو الأصل مُسنَد فعلاً لموظف تاني، أو له إسناد `ACTIVE` تاني، بيرفض. بعد التثبيت بيتحدّث الأصل نفسه في المخزون: `currentHolderId = employeeId` و`status = 'ASSIGNED'`. وبعدها لو كل أصول الطلب اتسلّمت فعلاً (مفيش إسنادات شقيقة معلّقة) الطلب يتقفل `COMPLETED`.

يعني عندنا **الأصل ميبقاش مسؤولية الموظف قانوناً غير لما هو نفسه يقرّ بالاستلام ومديره يعتمد** — مسار فيه توقيعين وسجل زمني كامل (`assignedAt`, `acknowledgedAt`, `managerConfirmAt`).

**عند المنافس التخصيص فعل واحد فوري** (`EmployeeAssetController::store`): بيعمل `EmployeeAsset` بـ `status = 'assigned'` و`assigned_at = now()` على طول — **من غير أي إقرار من الموظف ولا أي تأكيد من مدير**. اللي بيخصّص هو مستخدم عنده صلاحية على نطاق الموظف (`EmployeeScopeService::authorize`)، والموظف نفسه مالوش أي خطوة في الدورة. بعد ما بيتعمل التخصيص «مبدئياً» بيتنده `routeForApproval($asset, 'asset_assignment')` — **وده مشروط**: بيشتغل بس لو `AssetType.requires_approval == true` و فيه route متظبّط (`ApprovalEngine::openIfConfigured`)؛ غير كده **no-op** والتخصيص خلص وخلاص. يعني الموافقة عندهم — لو أصلاً اتفعّلت — بتيجي **بعد** ما الحالة اتغيّرت، مش قبلها.

وأخطر نقطة في تصميمهم: الـ Listener `ApplyAssetDecisionOnApproval`. الكود بيقول صراحةً إن **الموافقة (approved) = no-op** (بتأكّد الحالة المبدئية بس)، و**الرفض (rejected) هو اللي بيعمل حاجة**: تخصيص مرفوض بيرجّع الحالة لـ `returned`، وإرجاع مرفوض بيرجّعها `assigned`. فالموديل كله عبارة عن **زرار toggle بين قيمتين**، والموافقة مجرد تثبيت متأخّر. مفيش عندهم حالة «بانتظار الإقرار» ولا «بانتظار المدير».

---

## ③ الإرجاع — رجوع الأصل للمخزون

**عندنا** الموظف الأول بيعلّم «سلّمت» بـ `requestCustodyHandover` → الحالة **`RETURN_REQUESTED`** (لسه محسوبة عليه في كل الحرّاس). وبعدها طلب إرجاع معتمد بيشغّل `custodyReturnHandler`: الحالة تبقى **`RETURNED`**، يتكتب `returnedAt`، ويتكتب **`condition`** (الحالة عند الإرجاع — «سليمة» افتراضياً أو أي وصف). وأهم أثر: الأصل نفسه يرجع للمخزون `currentHolderId = null` و`status = 'AVAILABLE'` — يبقى جاهز لإسناد جديد. فحقل `condition` هنا بيسجّل **حالة الأصل وقت التسليم (سليم/تالف)** — معلومة مفيش ليها أي مكان عند المنافس.

**عند المنافس** (`EmployeeAssetController::return`): مجرد `update(['status' => 'returned', 'returned_at' => now()])` + `routeForApproval('asset_return')`. مفيش حقل `condition`، فمفيش تسجيل «رجع سليم ولا تالف». ومفيش «مخزون» يرجعله الأصل — الصف بيتقلب لـ `returned` وخلاص. الحالة عندهم بحرفيّتها **قيمتين لا ثالث لهما**: `assigned | returned` (مكتوبة كده في الـ migration نفسها كتعليق).

---

## ④ النقل المباشر بين موظفين — عندنا بس

دي الحاجة اللي **مش موجودة خالص عند المنافس**. عندهم عشان تنقل لابتوب من أحمد لسعيد لازم: ترجّعه من أحمد (`returned`) وتخصّصه من الأول لسعيد (`assigned`) — عمليتين منفصلتين مفيش أي رابط بينهم في الداتا، ومفيش أي أثر بيقول «ده نفس الأصل اتنقل».

**عندنا فيه نقل حقيقي ذرّي** (`transferCustody` في `requests.service.ts`، ونظيره في المسار الآلي `custodyTransferHandler` بـ `destinations.service.ts`):
- الحرّاس: صلاحية `custody.assign`، والعهدة لازم تكون `ACTIVE`، والمستلم موجود ونشط ومش نفس الحامل الحالي.
- **الخطوة 1**: عهدة الحامل الحالي تتقفل بحالة **`TRANSFERRED`** (حالة نهائية مستقلة معناها «اتنقلت لموظف تاني، عهدة جديدة اتفتحت للمستلم») + `returnedAt` + `condition = 'منقولة لموظف آخر'` أو نوت النقل.
- **الخطوة 2**: يتفتح `CustodyAssignment` جديد للمستلم بحالة **`PENDING_ACK`** و`assignedBy` = اللي عمل النقل — يعني المستلم بيدخل **نفس دورة التسليم من أولها**: يقرّ بالاستلام → مديره يعتمد → يبقى `ACTIVE` باسمه.
- **الخطوة 3**: الأصل يتحط في حالة «قيد النقل»: `currentHolderId = null` بس `status = 'ASSIGNED'` (مش `AVAILABLE`) — يعني هو مش في المخزون المتاح ومش محسوب على حد لحد ما المستلم يقبل ومديره يعتمد.

النتيجة: عندنا **سلسلة حيازة متصلة** — تقدر تتبع الأصل الواحد وهو ماشي من موظف لموظف عبر صفوف `TRANSFERRED → PENDING_ACK → ACTIVE`، وكل انتقال له توقيع ووقت. عندهم ده مستحيل يتتبّع لأنه أصلاً حركتين غير مرتبطتين.

---

## ⑤ الفقد/التلف والمسؤولية المالية → مديونية → خصم راتب — عندنا بس

هنا بيبان الفرق الجوهري اللي بيحوّل «نظام تتبّع أصول» لـ «نظام مسؤولية مالية».

**عند المنافس ملهاش وجود إطلاقاً.** مفيش حالة `lost` ولا `damaged` في `employee_assets` (الحالات قيمتين بس). مفيش حقل `value` على الأصل. فمفيش أي طريقة تقول «الموظف ضيّع اللابتوب اللي بـ 8000 جنيه» ولا أي أثر مالي يترتب على كده. أقصى حاجة يعملوها إنهم يسيبوه `assigned` للأبد (وقتها بوابة الإخلاء بس هي اللي هتمسكه — بس من غير أي فلوس تتحصّل).

**عندنا مسار كامل** (`custodyFinanceHandler` في `destinations.service.ts`، الـ handler المربوط بكود `custody_finance`): لما طلب بلاغ فقد/تلف يتعتمد:
1. لو الإسناد في أي حالة مفتوحة → حالته تبقى **`LOST`** (والـ enum فيه كمان `DAMAGED` كحالة نهائية مستقلة للتلف)، يتكتب `returnedAt` و`condition = الوصف`.
2. الأصل يتقاعد في المخزون: `status = 'RETIRED'` و`currentHolderId = null` — يخرج من الدورة خالص.
3. **وأهم خطوة**: لو `asset.value > 0` بيتفتح قيد في **دفتر المديونيات** — صف `EmployeeObligation` بـ: `employeeId` = الحائز، `type = 'DEBIT'` (خصم)، `category = 'custody_shortfall'`، `amount` = قيمة الأصل، `status = 'PENDING'`، و`sourceRef = 'asset:{assetId}'` (رابط رجعي للأصل عشان منع الازدواج بعدين).

**بعدها الربط بالرواتب** (`payroll.service.ts`): المسير الشهري بيلمّ كل `EmployeeObligation` بحالة `PENDING`، الـ `DEBIT` بيتخصم من صافي الراتب، وبعد الصرف القيد بيتقفل **`APPLIED`** مع `appliedPayrollRunId` و`appliedAt`، واتسجّل في `breakdown.obligationIds`. يعني القيد **بيتستهلك مرة واحدة بس ومايتكررش** الشهر اللي بعده. فالقيمة اللي ضاعت بتتحوّل فعلاً لفلوس بتتقطع من المرتب.

---

## ⑥ منع الازدواج مع التصفية — تفصيلة دقيقة عندنا

فيه سؤال محرج: لو الأصل اتقيّد كـ `custody_shortfall` في الدفتر (وهيتخصم في المسير الشهري)، وكمان الموظف بيسيب الخدمة والتصفية بتحسب العهد الضايعة — مش هيتخصم مرتين؟ نظامنا حالّها (`offboarding.service.ts` → `buildSettlement`): بنجمع كل `custody_shortfall` obligations ونطلّع منها `obligationAssetIds` من `sourceRef`، وبعدين لما نعُدّ العهد `LOST/DAMAGED` في التصفية **بنتخطّى أي أصل اتقيّد في الدفتر بالفعل** (`if (obligationAssetIds.has(c.assetId)) continue`) — عشان ميتخصمش مرة في المسير ومرة في التصفية. ده مستوى تدقيق مالي **مفيش له أي مقابل عند المنافس** لأنه أصلاً مفيش عندهم قيمة ولا خصم.

---

## ⑦ بوابة الإخلاء عند ترك الخدمة

هنا **الطرفين عندهم بوابة** — بس بمستويين مختلفين.

**عند المنافس** (`ClearanceService.php`, US7): `outstanding($employeeId)` بترجّع كل `employee_assets` بحالة `assigned` واللي مش متنازل عنها. `canSettle` = القايمة فاضية. و`assertCleared` بترمي **`abort(409)`** لو لسه فيه عهد. وفيه منفذ شرعي: `AssetClearanceWaiver` (حقوله `employee_id`, `employee_asset_id` — null معناها تنازل شامل، `waived_by_user_id`, `reason`) عشان مستخدم مخوّل يعدّي حالة مشروعة بتوثيق. كويس — دي **بوابة حقيقية**. بس هي بوابة **بلا تسوية مالية**: بتمنع الإغلاق أو بتتنازل، لكن **عمرها ما بتحوّل الأصل غير المُرجَع لفلوس**.

**عندنا** البوابة أعمق وجزء من دورة إخلاء طرف بخمس جهات. الاستقالة المعتمدة (`employeeStatusHandler`) بتفتح `OffboardingCase` بحالة `IN_CLEARANCE` وبتولّد `ClearanceItem` لجهة `custody` مكتوب جنبها عدد العهد المفتوحة. وبند العهدة **مايكملش (`DONE`)** طول ما الموظف ماسك أي عهدة مفتوحة — بنعُدّ `PENDING_ACK/PENDING_MANAGER_CONFIRM/ACTIVE/RETURN_REQUESTED`، ولو فيه واحدة بيرمي: «الموظف لا يزال ماسكاً N عهدة — أرجعها أولاً (أو وثّق قيمتها كخصم وحوّلها LOST)». وكمان نفس المنطق بيحمي **النقل بين الفرق** (`TEAM_TRANSFER` §2.9): ممنوع تنقل موظف وعليه عهدة مفتوحة. وبعد ما البنود تكمل، التصفية بتحسب قيمة العهد الضايعة (مع منع الازدواج بتاع نقطة ⑥) وتطلّعها **خصم فعلي** في مستحقات نهاية الخدمة.

---

## خلاصة الفرق

المنافس بنى **سجل تخصيص أصول بحالتين** (`assigned/returned`) + بوابة إخلاء بتنازل. ده محترم لكنه سقفه: مفيش مخزون مركزي للأصل، مفيش قيمة مالية، مفيش إقرار موظف ولا تأكيد مدير، مفيش نقل مباشر (ولا حتى إمكانية تتبّع إن أصل اتنقل)، مفيش تسجيل حالة (سليم/تالف)، ومفيش حالة فقد/تلف — وبالتالي **صفر مسؤولية مالية**؛ الموافقة عندهم مجرد toggle بيتأكّد بالموافقة وبيترجّع بالرفض.

نظامنا بيعامل العهدة كـ**أصل بقيمة وحائز وسجل حركة ومسؤولية**: دورة حالات من سبع قيم (`PENDING_ACK → PENDING_MANAGER_CONFIRM → ACTIVE → RETURN_REQUESTED → RETURNED/TRANSFERRED/LOST/DAMAGED`)، بتوقيعين (إقرار الموظف + تأكيد المدير) وطوابع زمنية لكل خطوة، نقل حقيقي متصل بين الموظفين، تسجيل حالة الأصل عند كل تسليم، ومسار كامل من الفقد → قيد `DEBIT` في دفتر المديونيات → خصم من المسير الشهري (يُستهلك مرة واحدة) → أو تسوية في مستحقات نهاية الخدمة، مع حارس منع ازدواج بين الاتنين — وبوابة إخلاء طرف تمنع الخروج (والنقل) قبل تصفية العهدة مالياً وفعلياً.

## البيانات الديمو الفعلية الموجودة عندهم (جرد حيّ من الإنتاج)
> فحص حيّ (قراءة فقط) لتينانت الديمو بتاعهم `hr-backend.logic-leap.ai` وقت التدقيق — عشان تعرف النظام «مليان» بإيه فعلًا مقابل شاشات فاضية.

**نظرة عامة:** تينانت ديمو **خفيف التعبئة** — الإعدادات مبذورة كويس، لكن الحركة الفعلية (طلبات، بصمات، إجازات) شبه فاضية، وكذا دورة **متوقفة/معطّلة**.

### الموظفون والتنظيم
- **7 موظفين**: 6 نشط + 1 مؤرشف. **وحتى بياناتهم ناقصة**: 4 بس عندهم راتب أساسي، 2 بس عندهم IBAN، 2 بس مسجّلين GOSI. (يعني 3 موظفين من غير راتب أصلًا.)
- **فرعان**: «فرع الرياض»، «تاتا». · **قسمان**: «تحليل بيانات»، «الموارد البشرية». · **فريق واحد**: «تطوير».
- **مسمّيان وظيفيان**: «مطور نظم»، «أخصائي موارد بشرية». · **درجة واحدة**: «الدرجة الأولى» (5000–15000). · **مركز تكلفة واحد**: «تحليل بيانات/an».

### الإعدادات المبذورة (شغّالة)
- **وردتان**: «دوام الرياض» (09:00→18:00)، «الوردية الصباحية» (08:00→17:00). · **3 تعيينات جدول** (بقسم/موظف بمدى تواريخ).
- **8 أنواع إجازة** (بذور افتراضية، **كلها بالإنجليزية وكلها «مدفوعة»**): Annual 21 · Sick 30 · Emergency 5 · Marriage 5 · Maternity 70 · Paternity 3 · Hajj 10 · Bereavement 5. ⚠️ ولا نوع غير مدفوع مُعرّف.
- **3 أنواع إذن**: استئذان شخصي · مهمة عمل رسمية · إذن تأخير صباحي.
- **نوعا عهدة**: «اجهزة لوحية» (تتطلب موافقة) · «كتيبات».
- **12 وثيقة**: other×3 · certificate×4 · passport×2 · national_id×2 · contract×1. · **4 عقود**.

### الحركة الفعلية (شبه فاضية — دليل أن الدورات لم تُختبَر)
- **5 طلبات اعتماد فقط، ولا واحد مكتمل**: `salary_increase` (معلّق، خطوة1) · `resignation` (معلّق) · `resignation` (معلّق) · `overtime` (معلّق) · **`resignation` (blocked — بلا مسار مُهيّأ!)**. أي أن عندهم **استقالة عالقة في حالة blocked** لأن سلسلة الاعتماد مش متظبطة — دليل حيّ على هشاشة «التوجيه بالفرع فقط».
- **سجل أوفرتايم واحد** مكتشف. · **صفر إجازات مقدَّمة** (رغم 8 أنواع). · **صفر أذونات خروج** (رغم 3 أنواع إذن). · **صفر عطلات رسمية** · **صفر استثناءات جدول** · **صفر جداول عمل مسمّاة**.
- **موديولات كاملة فاضية**: الأداء (0 دورات) · التوظيف (0 وظائف) · التدريب (0 دورات).

**الخلاصة:** النظام **مبني** لكن **غير مُشغَّل فعليًا كبيئة إنتاج** — بيانات عرض تكفي لإثبات أن الموديولات مربوطة وتحفظ، لكن الدورات التشغيلية (إجازات، أذونات، حضور يومي، إتمام موافقات) لم تُمارَس، وفيه طلب استقالة **عالق blocked**.
