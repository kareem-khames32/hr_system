# أوافق على التشغيل

**السبب:** أُغلق CR5-B01 في الاختبار الأصلي وفي حالات إضافية للنطاق والقرارات؛ لم أجد عيبًا حاجزًا جديدًا في فرق الجولة السادسة.

مراجعة مستقلة بتاريخ 26 سبتمبر 2026، للفرق `d352f40..508480b`. قرأت الفرق كاملًا: ملف منتج واحد، `requests.service.ts`، وثلاثة ملفات اختبارات. الحكم يحدّث حكم الجولة الخامسة بعد إغلاق حاجزها الأخير؛ لا يلغي الملاحظات غير الحاجزة وحدود الاختبار السابقة.

## 1. الحاجز السابق: CR5-B01 مغلق

**شغّلته:** أعدت `codex-review-round5-boundaries.integration.cjs` نفسه **دون تعديل**. إنشاء الطلب ونقل الموظف من A إلى C وتغيير المسمى تمّا عبر API. بعد النقل، أعاد ملف الموظف 404 للمعتمد في A، وحجبت التفاصيل التنظيم الحالي، واختفى `requesterJobTitle` من رد الصندوق. اختبارات الملف الستة نجحت، ومنها الحالة التي كانت تفشل في الجولة الخامسة. [نتيجة إعادة التشغيل](api/test/codex-review-round6-results-regression.cjs).

أضفت سيناريو مستقلًا بستة طلبات، بينها طلب سري وطلب بالنيابة وطلب مالي، ثم نقلت الموظف عبر API مع تغيير المسمى والقسم والفريق والمدير. تحققت من غياب **خاصية المسمى نفسها** خارج النطاق، ومن بقاء الاسم والكود والطلبات:

| حساب المعتمد | الطلبات الظاهرة في الصندوق | المسمى الحالي |
|---|---:|---|
| توكن قديم لفرع A | 6 | محجوب |
| نطاق A+B | 6 | محجوب |
| نطاق A+C | 6 | ظاهر بالقيمة الصحيحة |
| كل الفروع، دون دور مدير النظام | 6 | ظاهر بالقيمة الصحيحة |
| مدير النظام | 6 | ظاهر بالقيمة الصحيحة |
| فرع C فقط، أو نطاق فارغ، أو مشاهد بلا صلاحية اعتماد | 0 لكل حساب | لا رد يكشفه |

**لم يتعطل القرار:** المعتمد في A وافق على طلب ورفض آخر وأعاد ثالثًا؛ الحالات المحفوظة كانت `COMPLETED` و`REJECTED` و`RETURNED_FOR_INFO`، بسجل قرار واحد لكل طلب. وافق أيضًا على الاستحقاق المالي: **123.45 متوقعة و123.45 فعلية، والتزام واحد**. محاولتا إعادة الاعتماد أعادتا 400 ولم تكررا الأثر. بقي الطلبان غير المعالجين في الصندوق، وظهر سجل القرارات الأربعة دون التنظيم الأجنبي.

**السبب الذي قرأته:** [inbox عند السطر 1970](api/src/requests/requests.service.ts#L1970) يفحص فرع الموظف الحالي قبل إرجاع المسمى. نطاق اختيار الطلبات التاريخية بقي كما هو. الإصلاح يعالج مصدر التسريب في الخادم، وليس مجرد إخفاء عرض الواجهة.

الدليل: [الاختبارات الجديدة](api/test/codex-review-round6-requests.integration.cjs)، [نتائجها 3/3](api/test/codex-review-round6-results-requests.cjs).

## 2. غير الحاجز

لم أثبت عيب منتج جديدًا. ملاحظتا N04 الخاصة بالرقم القومي في القسيمة التاريخية وN05 الخاصة بالأداء تبقيان من الجولة السابقة؛ لم أعد فحصهما هنا.

ملاحظة تجهيز حساب الإعدادات السابقة أُغلقت في التشغيل: سجل `fulltest-settings` الداخلي أصبح **`checks=360 passed=360 failed=0`**. اختبرت نسخة مطابقة وظيفيًا توجه ملف النتائج إلى بادئة الجولة السادسة فقط. اختبار RC-UI-11 المعدل نجح ضمن مجموعة الوحدة كذلك.

## 3. الأداء

لم أقس الأداء، حسب التعليمات ومع وجود تشغيل تكامل موازٍ. أزمنة مشغّل الاختبارات ليست قياسات أداء للمنتج.

## 4. القراءات الأخرى وما خرج سليمًا

**شغّلته:** قارنت ردود `/requests/all` و`/requests/inbox` و`/requests/mine?includeOnBehalf=1` و`/requests/my-decisions` لثلاثة حسابات تاريخية: **12 قراءة** دون المسمى أو القسم أو الفريق أو المدير الحالي خارج النطاق. التفاصيل حجبت الحقول الخمسة؛ الطلب السري حجب البطاقة عن غير طرفه. صاحب الطلب نفسه رأى تنظيمه الحالي الصحيح، واحتفظ المنشئ بقراءة طلبه بالنيابة.

اختبرت أيضًا قائمة الموظفين التي تستخدمها لوحة الطلبات: الموظف المنقول غير موجود فيها لحساب A. تقرير الطلبات أعاد إجمالي **6** دون تنظيم الموظف، مطابقًا للطلبات الستة المزروعة.

**قرأته:** بحثت حقول التنظيم وقراءات الموظفين في `requests.service.ts`، وراجعت مستهلكي الصندوق ولوحة الطلبات والتقرير. لم أجد إثراءً آخر للتنظيم الحالي في قوائم هذه الخدمة غير الصندوق؛ بطاقة التفاصيل تستخدم الحارس السابق. لوحة الطلبات تستمد القسم من قائمة الموظفين المقيدة بالنطاق، وويدجت الموافقات يستخدم الصندوق نفسه ويعرض الكود عند غياب المسمى. هذه مراجعة كود وليست ادعاءً باختبار متصفح كامل.

| التشغيل المستقل | النتيجة |
|---|---:|
| اختبارات حدود الجولة الخامسة الأصلية | 6/6 |
| اختبارات النطاق والقرارات الجديدة | 3/3 |
| leave-contract، بما فيه تحقق الصندوق المضاف | 15/15 |
| fulltest-settings | 39/39 سيناريو؛ **360/360 تحقق داخلي** |
| مجموعة الوحدة الآمنة للمراجعة، 135 ملفًا | **1540/1540** |
| TypeScript في API، دون إخراج أو ملفات incremental | ناجح |

إجمالي التكامل المعاد تشغيله **63/63**. [نتائج التكامل](api/test/codex-review-round6-results-regression.cjs)، [سجل الإعدادات الداخلي](api/test/codex-review-round6-settings-findings.cjs)، [الوحدة](api/test/codex-review-round6-results-unit.cjs).

## 5. حدود الفحص والعزل

لم أعد جميع دورات النظام، أو مجموعة الوحدة 1777 كاملة، أو اختبار PDF الذي تعذر سابقًا، أو اختبار متصفح تفاعلي. قائمة ملفات الوحدة الـ25 المستبعدة من مشغّل المراجعة محفوظة في نتيجته. لا تغيير واجهة في هذا الفرق، ولم أعد فحص TypeScript للجذر. لا ترحيل جديد؛ ترحيل اختبار leave-contract نُفذ على قاعدته المؤقتة فقط، وفق توضيح المالك السابق. لا اختبار جديد على محرك SQL Server 2019 الفعلي.

أعدت قراءة الـbrief. شغّلت Nest وحراسه داخل عملية الاختبار دون مستمع HTTP، مع حجب البريد والمجال والمهام المجدولة. لم أشغّل أو أوقف خدمة، ولم أكتب أو أرحّل على `hr_system` أو `hr_review_pre_payroll*`، ولم ألمس قواعد الاختبارات الموازية.

أنشأت الاختبارات **أربع قواعد مؤقتة** وحذفتها. تحققت بعد ذلك باتصال مستقل إلى master من غياب الأسماء الأربعة عن `sys.databases`: **4/4 غير موجودة**. ملفات المنتج والترحيلات مطابقة لـ`508480b`، وأدلة الجولة الخامسة بقيت بنفس البصمات. الملفات الجديدة تحت `api/test/codex-review-round6-*` وهذا التقرير فقط؛ فحص الأسرار لم يجد قيم البيئة الحساسة أو JWT فيها. [دليل النطاق](api/test/codex-review-round6-scope-evidence.cjs)، [التنظيف والتحقق النهائي](api/test/codex-review-round6-final-audit-results.cjs).

لإعادة الاختبار الأصلي والتحقق الإضافي من جذر المشروع:

```powershell
$env:REVIEW_CONCURRENCY='1'
$env:REVIEW_RESULT_SUFFIX='recheck'
Remove-Item Env:REVIEW_PATTERN -ErrorAction SilentlyContinue
Remove-Item Env:REVIEW_PRELOAD -ErrorAction SilentlyContinue
Remove-Item Env:REVIEW_BASELINE -ErrorAction SilentlyContinue
node api/test/codex-review-round6-suite.cjs codex-review-round5-boundaries codex-review-round6-requests
```
