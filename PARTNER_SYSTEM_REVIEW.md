# مراجعة نظام الشريك — تقرير حالة
**التاريخ:** 2026-09-07 · **النطاق:** مراجعة قراءة فقط (لم يُعدَّل أي ملف) · **الفترة:** ما بُني منذ 2026-07-14

> راجعتُ آخر نسخة من المستودعين على GitHub بعد شهرين من عمل الفريق (47 commit في الباك · 35 في الفرونت)،
> وقارنتها بنظامنا. هذا التقرير يجيب: **هل الشغل ماشي كويس؟ وما الذي يحتاج تدخّلًا؟**

---

## الخلاصة في دقيقة

**الحكم العام: الفريق يعمل بانضباط هندسي عالٍ — أعلى من المتوسط — لكن تحت هذا الانضباط توجد ثغرات مالية وأمنية حقيقية تحتاج تدخّلًا فوريًا.**

### ✅ ما يستحق التقدير
- **ينفّذون من المواصفة التي سُلّمت لهم بأمانة**، بل ويستخدمون معرّفات المتطلبات نفسها في رسائل الـcommits (`PL-05/3`, `PL-08/1`, `PR-10`).
- **القاعدة الأصعب في المواصفة نُفِّذت بشكل صحيح تمامًا**: الحضور بعد نافذة الساعة المرنة يُقاس **من بداية الدوام الرسمية** لا من نهاية النافذة (09:00 بداية + نافذة تنتهي 10:00 + حضور 10:01 ⇒ **61 دقيقة**) — ومغطاة بثلاثة اختبارات متقاطعة.
- **بنية حوكمة نادرة**: أعلام سلوك محكومة · لقطات مختومة بـhash · محرك مقارنة (parity) · فصل صلاحيات في الاعتماد · هجرات آمنة بمسار رجوع.
- **381 ملف اختبار** وتوثيق داخل الكود أعلى من المعتاد.
- **سدّوا فجوات كانت لصالحنا**: نقل العهدة · الحقول الديناميكية للطلبات · الساعة المرنة · فصل نقص الساعات · موديول Offboarding كامل.

### 🔴 ما يحتاج تدخّلًا فوريًا (بترتيب الخطورة)

| # | الملاحظة | الأثر |
|---|---|---|
| 1 | **بيانات إنتاج حقيقية داخل المستودع**: 4.4 ميجابايت · **83,622 بصمة حضور** لـ**676 موظفًا** + عناوين IP عامة لأجهزة البصمة — وقابلة لإعادة تعريف الأشخاص | خرق خصوصية · تُرفع للخادم مع كل نشر |
| 2 | **بيانات اعتماد حقيقية في تاريخ Git** (حساب + كلمة مرور مقابل الـAPI الحيّ). الحجب تجميلي — القيمة باقية لكل من استنسخ المستودع، **ولا يوجد ما يشير إلى تدوير كلمة المرور** | اختراق محتمل |
| 3 | **شريحة خصم التأخير: قنبلة بنقرة واحدة.** حقل «نمط الخصم» مخزَّن ومعروض في الواجهة لكنه **مُتجاهَل في الحساب**؛ ضبط «دقائق = 30» يخصم **30 يوم راتب** عن يوم تأخير واحد. ولا حدّ أقصى على القيمة، ولا اختبار يغطيها | خصم كارثي من رواتب حقيقية |
| 4 | **«الغياب» في النظام ليس غيابًا**: يقرأ طلبات الإجازة بدون راتب فقط. الغياب الفعلي بلا إذن ⇒ **خصم صفر**. ومعامل عقوبة الغياب موصول بالكمية الخطأ (يضاعف خصم إجازة معتمدة قانونًا) | خسارة مالية + خصم ظالم |
| 5 | **الموظف التارك**: يأخذ **شهرًا كاملًا** أو **صفرًا** حسب توقيت تغيير حالته — و**التصفية النهائية لا تحسب راتب الشهر الأخير إطلاقًا** | دفع زائد أو حرمان موظف من مستحقه |
| 6 | **بوابة التكافؤ مقفلة تصميميًا**: تفعيل أي علم مالي يشترط تقرير تكافؤ، والمسار الوحيد لإنتاجه **مرفوض في الإنتاج**. النتيجة: منظومة الرواتب الجديدة **غير قابلة للتفعيل** إلا بإدخال صف يدوي في قاعدة البيانات (وهو ما يُفرغ البوابة من معناها) | الشغل المُنجز لا يمكن تشغيله |
| 7 | **المزامنة تدهس التصحيحات المعتمدة**: تصحيح بصمة مرّ بدورة اعتماد كاملة يُمحى في أول إعادة مزامنة تلمس اليوم نفسه | حوكمة تُلغيها كتابة صامتة |
| 8 | **الورديات الليلية مكسورة في مسار البصمة**: التجميع بالتاريخ التقويمي يقسّم الوردية 22:00→06:00 إلى يومين ⇒ يوم بلا انصراف ويوم بتأخير هائل | أرقام حضور خاطئة |
| 9 | **الاختبارات على SQLite والإنتاج MySQL** · **صفر قياس تغطية** · **12 اختبار E2E آخر حالة مسجّلة لها «فاشل» منذ 5 أغسطس وغير حاجزة في النشر** | ثقة زائفة في الاختبارات |
| 10 | **لا قفل على المهام المجدولة**: تشغيل على أكثر من عقدة ⇒ **استحقاق إجازات مضاعف** وترحيل غياب مضاعف | فساد بيانات صامت |

### 🟠 ملاحظات مهمة أخرى
- **بنود البدلات في القسيمة غير متناسبة** بينما الإجمالي متناسب ⇒ مجموع الأسطر لا يساوي الإجمالي (خرق لقاعدة كتبوها بأنفسهم).
- **التأمينات (GOSI)**: تستعلم بتاريخ اليوم لا بنهاية الفترة (إعادة احتساب شهر قديم تعطي رقمًا مختلفًا)، ومطابقة الجنسية نصية هشّة ⇒ «Saudi Arabia» تُعامل **غير سعودي** وحصة الموظف تصير صفرًا بصمت.
- **أقساط السلف** تُجمَّع بالشهر التقويمي داخل دورة 23→22 ⇒ أقساط تسقط من الدورة الحالية.
- **تحويل وردية إلى «مرنة» عبر التعديل بلا ساعات مطلوبة** يتخطّى حساب النقص كليًا بصمت.
- **سحب إنهاء الخدمة ذاتيًا بلا صلاحية ولا موافقة وفي أي مرحلة**.
- **بانِي حقول الطلب يعمل مرّة واحدة لكل مفتاح** ويعطب من الحفظة الثانية.
- **لا يوجد endpoint لطلب أوفرتايم من الموظف** ⇒ السيناريو المطلوب («موظف يطلب إضافيًا في فترة مغلقة فيُحتسب من البصمة») **غير ممثَّل في النظام**.

---

## كيف تقرأ هذا التقرير
الأقسام الخمسة التالية تفصيلية بأدلة (ملف:سطر). درجات الملاحظات: **🔴 حرج** (يمسّ مالًا أو أمنًا أو بيانات) · **🟠 مهم** (منطق ناقص أو حالة حدّية غير معالجة) · **🟡 تحسين**.


---

# القسم ١ — الرواتب

# تقرير مراجعة: موديول الرواتب — `hr/app/Modules/Payroll`
**النطاق الزمني:** ما بُني منذ 2026-07-14 · **الحكم العام:** الشغل ماشي كويس **من ناحية الهندسة والحوكمة**، لكن **تحت طبقة الحوكمة الفاخرة يوجد محرك حساب فيه ثغرات مالية حقيقية لم تُلمس** — بعضها قابل للتفجير من شاشة إعدادات عادية.

---

## 0) خلاصة تنفيذية

فريق العميل نفّذ منذ 2026-08-19 خمس مراحل مرقّمة (`Layer 0 · Phase 0.1 → 0.5`) بانضباط نادر: أعلام سلوك محكومة، لقطات (baselines) مختومة بـhash، محرك مقارنة (parity) بتصنيف مغلق للفروق، فصل صلاحيات في الاعتماد، ومهاجرات آمنة برجوع يحمي البيانات. جودة التوثيق داخل الكود (الـdocblocks) والاختبارات **أعلى من المتوسط الصناعي بوضوح**، والاختبارات الجديدة (~40 ملف اختبار للرواتب) تغطي المسارات التي شحنوها تغطية جادة.

لكن:
1. **الفرق كله انصبّ على «كيف نغيّر بأمان»، لا على «هل الحساب صحيح».** البنود الأقدم (البدلات، الغياب، السلف، التسويات اليدوية، التأمينات) لم تُراجَع ولا تُغطّى بأي parity، وفيها **6 عيوب مالية مؤكّدة** موصوفة أدناه.
2. **بوابة التكافؤ (parity gate) ثغرتها بنيوية:** التقرير الذي يفتح العلم يُنتَج والعلم مُطفأ — أي أنه لا يمكنه بنيويًا أن يكون قد اختبر السلوك الجديد، والبوابة لا تتحقق من ذلك.
3. **التناسب (proration) نصف مُنفّذ:** الملتحق فقط. **التارك يأخذ شهرًا كاملًا** (موثّق كتأجيل)، والأسوأ أن **التسوية النهائية لا تحسب راتب الشهر الأخير أصلًا** → إما دفع زائد أو صفر.

---

## 1) `PayrollCalculationService.php` — منطق الحساب

### ما نُفِّذ
دالة واحدة `calculateForEmployee()` (سطر 66) بترتيب واضح: تناسب → بدلات → إضافي (بمضاعف حسب نوع اليوم: عادي/عطلة أسبوعية/عيد) → مكافآت → تعويضات بنطاق → GOSI → تأخير → غياب → أذونات → انصراف مبكر/نقص ساعات → التزامات → طبقة معادلات → سلف. المستحقات والخصومات مفصولة، والصافي محمي من السالب بثلاث مصدّات متتالية (`clamp` للمعادلات سطر 149، `clamp` للالتزامات، `min()` للسلف سطر 158).

### هل التنفيذ صحيح ومكتمل؟ — **لا. ست مشاكل مؤكّدة**

🔴 **1. بنود البدلات في القسيمة غير متناسبة بينما الإجمالي متناسب.**
`PayrollCalculationService.php:95-96` يضرب `total_allowances` و`basic_salary` في النسبة، لكن `:892` يكتب سطر البدل بـ`$item['computed_amount']` **الخام بلا نسبة**. النتيجة: لموظف ملتحق منتصف الشهر، **مجموع أسطر القسيمة لا يساوي إجمالي القسيمة** — وهو خرق مباشر لقاعدة PL-08/2 (المطابقة بين الأسطر والإجماليات) التي ذكروها بأنفسهم في commit 1ed9d2c. لاحظ التناقض: خصومات الموظف الثابتة **تُضرب** في النسبة عند إنشاء أسطرها (`:324-325`)، فالسلوك غير متسق داخل نفس الدالة.

🔴 **2. «الغياب» في النظام ليس غيابًا.**
`calculateAbsenceDeductions()` (`:803-839`) يقرأ **طلبات إجازة بدون راتب معتمدة فقط**. لا يوجد أي قراءة لأيام `absent` من الحضور في الموديول كله (تحقّقت: `grep absent app/Modules/Payroll/` → لا شيء). أثر ذلك:
- موظف غائب بلا إذن ولا طلب إجازة → **خصم صفر**.
- والأخطر: المعامل `absence_penalty_days` (`:836`) — الموصوف في تعليقه حرفيًا بأنه لليوم *unexcused* — مضروب في **الإجازة غير المدفوعة المعتمدة**. ضبطه على 2 (كما تقتضي مواصفتكم: غياب بلا إذن 1.5-2 يوم) سيضاعف خصم إجازة مُعتمَدة قانونًا، ويظل الغياب الحقيقي بصفر. **المعامل موصول بالكمية الخطأ.**
- كما أن الأيام تُحسب تقويميًا (`diffInDays+1`) فتشمل الجمعة/السبت والعطل داخل فترة الإجازة.

🔴 **3. `mode` في شرائح التأخير مُخزَّن ومعروض في الواجهة ومُتجاهَل في الحساب.**
الجدول يحمل `mode` = `fraction|minutes` (`Migrations/2026_08_02_000005:28`)، والـRequest يقبله (`SetLatenessTiersRequest.php:21`)، والواجهة تعرض قائمة اختيار «نسبة من اليوم / **دقائق خصم**» (`hr-dashboard/src/features/settings/pages/attendance-rules/page.tsx:300-311`). لكن `LatenessTierService::tiersFor()` **لا ينقل `mode` أصلًا** (`:234-239`) و`fractionFor()` يعيد `value` كنسبة دائمًا (`:273-279`)، ثم `calculateLateDeductions` يحسب `dailySalary × value` (`:566`).
**أثر عملي:** مدير موارد بشرية يضبط شريحة `{mode: 'minutes', value: 30}` من شاشة الإعدادات → النظام يخصم **30 يوم راتب** عن يوم تأخير واحد. لا يوجد أي `max` على `value` كذلك (`min:0` فقط). هذه قنبلة قابلة للتفجير بنقرة، وبلا اختبار واحد يغطيها.

🟠 **4. التأمينات (GOSI) لا تعرف التناسب ولا الفترة.**
`:115` يستدعي `calculateForEmployee($employee, ...)` بالراتب **الكامل** بغض النظر عن التناسب — قابل للنقاش نظاميًا لكنه غير موثّق كقرار. الأخطر أن `GosiCalculationService::getHousingAllowance()` يستعلم بـ`now()` (`:127-130`) لا بـ`periodEnd` — بينما بقية المحرك يمرّر `periodEnd` (`:92`). إعادة احتساب مسير شهر قديم اليوم تعطي قاعدة تأمينية مختلفة عن الأصلية. كذلك `isSaudiNational()` (`:155-159`) قائمة نصية مغلقة `['saudi','sa','سعودي','سعودية']` — `"Saudi Arabia"` أو `"KSA"` أو مسافة زائدة ⇒ الموظف يُعامَل غير سعودي ⇒ **حصة الموظف صفر** بصمت.

🟠 **5. السلف تُجمَّع بشهر تقويمي داخل دورة مخصّصة.**
`calculateLoanDeductions()` (`:841-857`) يرشّح `due_date <= $periodStart->copy()->endOfMonth()`. في دورة 23→22 (المدعومة رسميًا في `PayrollCycleService`)، الأقساط المستحقة بين 1 و22 من شهر الفترة **تسقط من الدورة الحالية** وتُحصَّل في التالية. بقية المحرك كله يستخدم `periodEnd`. (وأيضًا `$dueMonth` في `:843` متغيّر ميت.)

🟡 **6. `withoutGlobalScope('tenant')` غير متسق.** المحرك يعلّق بإسهاب على أن الرواتب تعمل في سياق بلا مستأجر ولذلك يفكّ النطاق صراحةً في كل استعلام… عدا `BonusService::getPendingForEmployee` (`:42`) و`ObligationService` كله (`:49, 62, 138`) التي تعتمد على النطاق العام. إما التعليق خاطئ أو الاستعلامات خاطئة.

---

## 2) التناسب — commit `66d9d00` (2026-08-19)

### المعادلة بالضبط (`PayrollCalculationService.php:474-512`)
```
legacy (افتراضي):  hire ∈ [start, end]  →  ratio = (diffInDays(hire,end)+1) / divisor   [بلا سقف]
srs_pr10_full_coverage:
   hire == null أو hire ≤ start        →  ratio = 1.0   (بلا أي قسمة)
   غير ذلك                             →  ratio = min((diffInDays(hire,end)+1)/divisor, 1.0)
```
- **الأساس:** `daily_salary_divisor` (افتراضي **30 ثابت**)، لا طول الفترة. مؤكَّد باختبار `IncompleteCycleTest::test_the_divisor_is_not_the_cycle_length`.
- **يُطبَّق على:** الأساسي، البدلات (الإجمالي فقط)، الخصومات الثابتة. **لا يُطبَّق على:** GOSI، التأخير، الغياب، الإضافي، السلف، الالتزامات.

### التقييم
✅ **التشخيص ممتاز.** العيب الأصلي (`between()` شامل ⇒ من التحق يوم 1 يُقسَّم) كان يدفع **103.33% في شهر 31 يومًا و93.33% في فبراير** — عيب واحد باتجاهين، وقد أدركوا بحق أن `MIN(...,1)` وحده ما كان ليصلحه. الفصل بين PR-10/1 و PR-10/2 كفرعين منفصلين مبرّر تمامًا لأجل أثر التدقيق.
✅ التغطية ممتازة: `FullCycleTest` (7 حالات: قبل الدورة، اليوم الأول، 31 يومًا، فبراير 28، فبراير كبيسة 29، دورة 23→22، البدلات)، `FactorCeilingTest` (5)، `IncompleteCycleTest` (4)، `LegacyInvariantTest`، `FinalizedRunTest` (المسيّرات المعتمدة/المدفوعة لا يُعاد حسابها).

🔴 **الملاحظة الحاسمة — الموظف التارك.**
`effective_to` هو دائمًا نهاية الفترة؛ لا يُقرأ `last_working_day` ولا `termination_date` في أي مكان من الموديول (تحقّقت بالـgrep: الظهور الوحيد في `PayrollBaselineService.php:318` كـ *لقطة* لا كمُدخل حساب). النتيجة سيناريوهان كلاهما خاطئ:
- الموظف ما زال `active` وقت التشغيل ⇒ **شهر كامل** ولو ترك يوم 5.
- الحالة قُلبت إلى `terminated` قبل التشغيل ⇒ يُستبعَد كليًا من `CalculatePayrollAction:55` ⇒ **صفر عن أيامه المعمولة**… و**التسوية النهائية لا تحسب راتب الشهر الأخير إطلاقًا**: `SettlementService::build()` (`app/Modules/Employee/Services/SettlementService.php:69-85`) يبني مكافأة نهاية الخدمة + بدل رصيد الإجازات + السلف + العُهد **ولا سطر راتب**.
هم وثّقوا الفجوة صراحةً (`OBD-8`, Gate A Decision 1) ومنعوا حتى تسجيل علم لها (`BehaviorFlagDefaultsTest::test_no_termination_coverage_flag_is_registered`) — انضباط محترم، لكن **الفجوة مفتوحة في نظام إنتاج، والطرف الآخر منها (التسوية) لم يُغطَّ بأي شيء**.

🟡 حالات حدّية أخرى: `working_days` تُسجَّل = `divisor` (30) للتغطية الكاملة حتى في شهر 31 يومًا — سليم حسابيًا، مضلّل تقريريًا. و`calculateProRating` عام يقبل `hire > periodEnd` (نسبة سالبة) لكنه محمي عمليًا بمرشّح `CalculatePayrollAction:56-59`.

---

## 3) بوابة التكافؤ — `PayrollBehaviorFlag.php` + `PayrollParityService.php`

### كيف تعمل
كتالوج أعلام مغلق (`Support/PayrollBehaviorFlag.php:78-128`، 5 أعلام) — **غياب الصف = السلوك القديم**، فلا backfill ولا كتابة على أي مستأجر. المسار: `requestChange` → تقرير parity → معتمِد **مختلف** → `approveAndEnable` في معاملة واحدة من 12 خطوة (`PayrollBehaviorFlagService.php:158-312`).

### التقييم
✅ **الأعلام آمنة افتراضيًا بشكل مُثبت.** `BehaviorFlagDefaultsTest` يثبت: كل علم يعود لقيمته القديمة عند غياب الصف (T-1)، لا صف لأي مستأجر بعد المهاجرة (T-2)، مفتاح غير مسجّل = خطأ صريح لا افتراض صامت (T-3)، وكل قيمة قديمة ضمن القيم المسموحة.
✅ **أذكى ما في الملف:** قائمة تحقّق (allowlist) تمنع أي محرك من قراءة علم غير مصرّح له به صراحةً (`:85-131`)، مع اختبار يثبت أن الافتراضي «ممنوع» (`test_a_flag_absent_from_an_engine_allowlist_is_forbidden_in_it`). هذه سقّاطة (ratchet) حقيقية لا زخرفة.
✅ فصل الصلاحيات مُحكم: الطالب لا يعتمد ولو كان `super_admin`، والفحص يسبق فحص الصلاحية عمدًا لتفادي تجاوز `Gate::before` (`:180-188`). وفشل كتابة سجل التدقيق **يُرجِع المعاملة** (`:302-307`).
✅ المقارنة بندًا بندًا فعليًا: على مستوى إجماليات المسيّر ثم كل موظف ثم كل حقل ثم كل سطر (`PayrollParityService::diff` `:627-780`)، مع مطابقة الأسطر بمفتاح `category|reference_type|reference_id`، وحمل `evaluation_status` و`source` **خارج المفتاح** حتى لا تنكسر اللقطات التاريخية. و`reconcileRunTotals` (`:809`) يرفض تفسير إجمالي مسيّر ما لم يكن **كل** مساهم فيه مفسَّرًا و**تُجمع الفروق بالضبط** — أي أن أخطاء متقابلة لا تُلغي بعضها إلى تقرير نظيف (مُختبَر: `test_offsetting_unexplained_employee_differences_cannot_cancel_into_clean`).

### 🔴 الثغرة البنيوية في البوابة
`approveAndEnable` يتطلب تقرير parity بـ`unexplained_count === 0` (`PayrollParityReport::supportsEnablement():60-63`) — و**لا شيء غير ذلك**. لا يتحقق من:
- أن التقرير **مارس فعلًا** العلم المطلوب. الحقل `flag_set` يُكتب في التقرير (`PayrollParityService.php:1384`) لكن **لا يُقرأ في أي مكان** — تحقّقت: `grep -rn flag_set app/` يعطي كتابة وعرضًا فقط.
- أن التقرير حديث، أو أن `baseline_id` يخص فترة ذات صلة، أو أن `to_value` يظهر في `flag_set`.

وبما أن `captureReplay` يعيد التشغيل بالعلم **الحالي** للمستأجر (والـreplay ممنوع في الإنتاج `:101-112`)، فالتسلسل الوحيد الممكن هو: تجميد لقطة تحت legacy → parity يقارن legacy بـlegacy → **صفر فروق** → استخدام هذا التقرير النظيف لفتح **أي** علم. تعليقهم في `:265-275` يعترف بهذا ضمنًا: «التقرير الذي يُظهر التغيير يُنتَج **بعد** التفعيل». أي أن البوابة تتحقق من **إجراء** لا من **دليل**. كل الآلة المكلفة (التصنيف، الأقفال، الاعتماد المزدوج) تقف على شرط يمكن الوفاء به بتقرير فارغ المعنى.

🟠 **`ENGINE_VERSION` لم يُرفَع أبدًا.** ثابت في `layer0-phase0.1` (`PayrollBaselineService.php:40`) رغم أن المراحل 0.2→0.5 غيّرت سلوك الحساب، وتعليقه ينص حرفيًا على أنه «يُرفَع عمدًا من كل مرحلة تغيّر سلوك الحساب». `git log -S` يؤكد أنه لم يُلمس منذ 17afcda. النتيجة: لقطة قبل PR-10 وأخرى بعده **غير قابلتين للتمييز** بالحقل المخصص لذلك تحديدًا.

🟡 `PRORATA_INVARIANT_FIELDS` (`:82-89`) مشتقّة «بالفحص اليدوي» من المحرك، ولا يوجد اختبار يربطها بأعمدة `COMPARED_FIELDS` — إضافة عمود مالي جديد للمحرك مستقبلًا ستمر بلا حراسة.

---

## 4) قابلية التفسير — commit `1ed9d2c` (Phase 0.2)

### ما نُفِّذ
4 أعمدة nullable على `payroll_entry_lines` (مهاجرة `2026_08_19_000001`): `component_code`, `evaluation_status` (`zero|failed`), `evaluation_note`, `source_expression`. مكشوفة في `PayrollEntryLineResource:30-33` و**مُعرَّضة فعليًا في الواجهة** (`hr-dashboard/src/features/payroll/pages/payslip/_id/page.tsx:44-69`). المهاجرة عكسها يرفض إسقاط الأعمدة إذا كانت تحمل بيانات (`:96-111`) — انضباط ممتاز.

### هل يُحفظ تفصيل كل بند (مدخلات/معادلة/ناتج)؟ — **لا. جزئيًا فقط**
🟠 ما يُحفَظ فعليًا هو **حالة** البند لا **اشتقاقه**:
- `source_expression` يُملأ **حصريًا** لصف معادلة فشل بالقسمة على صفر (`FormulaOverlayService::row():180`). المعادلة الناجحة لا تحفظ نصها.
- **البنود الأصلية لا تحفظ شيئًا من مدخلاتها:** سطر التأخير لا يحفظ الدقائق ولا كسر الشريحة المطبَّق (النص `"Late deduction (half_day)"` في التسمية فقط)؛ سطر الغياب لا يحفظ `absence_penalty_days`؛ الإضافي لا يحفظ أي مضاعف استُخدم من الثلاثة؛ GOSI لا يحفظ النسبة ولا مصدر قاعدة الاحتساب. فإعادة اشتقاق أي رقم بعد تغيير الإعدادات **غير ممكنة من القسيمة**.
- الاستثناء المضيء الوحيد: سطر FR-032 «لم يُحتسب التأخير» (`PayrollCalculationService.php:1023-1040`) يحمل ملاحظة تشرح القرار، **ويُصدَر بلا قيد على `zero_detail_lines`** عمدًا — قرار صحيح تمامًا لأن كتمه يعيد بالضبط الصمت الذي وُجد المطلب لإزالته.

🟠 **الفشل ما زال صامتًا في أغلب أشكاله.** `zero_with_warning` يُظهر **القسمة على صفر فقط** (`isDivisionByZero():156-160`، تمييز بمطابقة نص الرسالة — هش، لكنهم غطّوه بـ`DivisionByZeroCanaryTest`). أي فشل آخر (متغير مجهول، خطأ نحوي، معادلة تشير إلى أخرى مُعطَّلة) **يُسقَط بصمت مع سطر Log** (`:105-121`). والأسوأ: **دورة اعتماد دائرية بين المعادلات** ترمي من `topologicalOrder` فتُلتقط في `evaluateFormulaOverlay:368` وتُرجِع `$empty` ⇒ **كل** بنود الطبقة تختفي لكل الموظفين في المسيّر، بسطر تحذير واحد فقط. هذا هو عين الاختفاء الصامت الذي وُجدت PL-05/3 لإزالته.

🟡 القسيمة **ليست PDF**: `downloadPayslip` (`SelfServicePayrollController.php:188`) يعيد `PayrollEntryResource` (JSON) لا مستندًا.

---

## 5) `Routes/api.php` — نقاط النهاية والصلاحيات

✅ 198 سطرًا، **كل مسار محروس بصلاحية**، بلا استثناء واحد. التقسيم منطقي ومقروء. نقاط قوة محددة:
- الاعتماد بصلاحية منفصلة عن الطلب: `payroll:rollout:approve` مقابل `payroll:flags:manage` (`:169-173`)، والتعليق يشرح أن البوابة الحقيقية في الخدمة لا في المسار — صحيح ومُثبت بالكود.
- صلاحيات الحوكمة أُبقيت **خارج** `formulas:*` عمدًا (لأن الأخيرة تمنح كتابة حرة على كل مفتاح معادلة) — تفكير أمني ناضج.
- الخدمة الذاتية بملكية مضمونة بالبناء (`:23-36`) والتصدير `POST` مع `LogAuditTrail` لأنه تسريب بيانات رواتب.

🟡 `payroll/bonuses` **القراءة** محروسة بـ`bonuses:create` (`:114, 116`) لا بصلاحية قراءة — من يقرأ يستطيع الإنشاء، ولا صلاحية «عرض فقط» للمكافآت. نفس النمط في `scoped-compensations` (`:120-123`).
🟡 `review` و`revert` و`destroy` كلها تحت `payroll:process` نفسها — لا فصل بين المُدخِل والمُراجِع على مستوى المسار (الفصل موجود فقط عند `approve`).

---

## 6) الفجوات مقابل منظومة رواتب ناضجة

| المطلوب | الحالة | الدليل |
|---|---|---|
| **كيان «سياسة» يُسنَد لمسيّر** | ❌ **غير موجود**. الإعدادات مفاتيح `key/value` عامة على المستأجر (`PayrollFormula::getForTenant`)، تُقرأ **حيّة وقت الحساب**. لا سياسة مُسمّاة، لا إصدارات، لا إسناد لمسيّر، ولا تجميد للإعدادات على المسيّر. تغيير مفتاح اليوم يغيّر إعادة احتساب مسيّر الشهر الماضي. اللقطة الوحيدة موجودة في `PayrollBaselineService::settingsSnapshot` وهي **لأغراض المقارنة فقط** لا كمدخل حساب. | `Models/PayrollFormula.php:17-25` |
| **شرائح قابلة للتعريف (كسر يوم / دقائق 1:1 / مضاعف)** | ⚠️ **معلن ومكسور**. `fraction` يعمل؛ `minutes` مخزَّن ومعروض ومتجاهَل ⇒ خصم كارثي (بند 🔴3). لا نمط «مضاعف» إطلاقًا. | `LatenessTierService.php:234-279` |
| **مسيّر بنطاق (فرع/قسم/فريق)** | ❌ **غير موجود**. `CalculatePayrollAction:53-60` يجلب **كل** موظفي المستأجر النشطين. لا حقل نطاق على `payroll_runs`، ولا لقطة أعضاء. النطاق موجود على *التعويضات* (`ScopedCompensation`) لا على المسيّر. | |
| **حارس ضد ازدواج الصرف** | ⚠️ **جزئي وفيه ثقب**. مُحكم للمكافآت (`disbursed` + إفراج عند إعادة الحساب، `CalculatePayrollAction:28-33`) وللالتزامات (`applied_payroll_run_id` + `releaseForRun`). ومنع مسيّر عادي مكرر لنفس الفترة (`PayrollRunService.php:141-151`). **لكن:** ① **الأقساط لا تُحجَز عند الحساب، بل تُعلَّم عند الدفع فقط** (`MarkPayrollPaidAction:35-72`) — مسيّران للفترة نفسها (عادي + تكميلي، والتكميلي **غير محكوم بأي منع**) يحسبان **نفس القسط** ثم يُدفعان ⇒ تحصيل مزدوج. ② **`ScopedCompensation` لا يُستهلك أبدًا**: الحالة `pending` تبقى للأبد والمحرك يعلّق صراحةً بأنه «يعاملها كقاعدة دائمة بلا تغيير الحالة» (`PayrollCalculationService.php:262-268, 277`)، بينما المهاجرة تُعرّف `pending\|disbursed\|cancelled` و**لا شيء يكتب `disbursed` في المستودع كله**. مكافأة قسم لمرة واحدة بـ1000 ريال ⇒ **تتكرر كل شهر إلى الأبد** حتى يحذفها أحد. لا حقل تاريخ سريان ولا علم «متكرر/مرة واحدة». | 🔴 |
| **سقوف السلف** | ✅ **موجودة** — لكن **مسار الخدمة الذاتية يتجاوز أهمها**. `CreateLoanAction::validateCap` (`:71-83`) يفرض سقف الرصيد القائم التراكمي (`loan_balance_cap_multiplier`, ×5). لكن `SelfServicePayrollController::createRequest` (`:88-97`) ينشئ `Loan::create` **مباشرة** ويفرض `enforceCap` على **المبلغ المطلوب فقط** (نسبة/مبلغ)، ولا `activate()` يعيد الفحص (`:50-69`) ⇒ طلبات متتالية كلٌّ منها تحت 50% تتراكم فوق سقف الـ×5. | 🟠 |
| **سلفة استثنائية بالتقسيط** | ⚠️ السلفة الذاتية مثبّتة على قسط واحد (`:59`)؛ التقسيط متاح عبر مسار الإدارة فقط. لا يوجد سقف قانوني على **نسبة الخصم الشهري من الأجر** (نظام العمل السعودي م.93: 10% للسلف / نصف الأجر إجمالًا) في أي مكان. | 🟠 |

---

## 7) الاختبارات — ما تغطيه فعلًا وما هو مكشوف

**المغطّى جيدًا (~40 ملف):**
`PayrollProration/` (8 ملفات: تغطية كاملة، شهور 28/29/30/31، دورة مخصّصة، سقف العامل، ثبات القديم، المسيّرات المنتهية، تصنيف parity بـ9 حالات) · `PayrollGovernance/` (12: الافتراضات، البوابة، فصل الصلاحيات، ذرّية المعاملة، عدم قابلية اللقطة للتغيير، رجوع المهاجرة، صلاحيات الوصول) · `PayrollShortfall/` (10: حياد المبلغ، عزل الحوكمة، مصدر السطر، رجوع المهاجرة) · `PayrollFlexibleLateness/` (7) · `PayrollExplainability/` (4).

**المكشوف — والأخطر أنه يتقاطع بالضبط مع العيوب أعلاه:**
- 🔴 **`AdjustPayrollEntryAction` لا يُختبر إلا بأربع حالات بدائية** (`Phase4/PayrollAdjustmentTest.php`) لا تلمس أي عمود أُضيف بعد أبريل. راجع بند التسويات في القسم التالي.
- 🔴 **صفر اختبارات لـ`mode` في شرائح التأخير**، ولا لأي قيمة `value` شاذة.
- 🔴 **صفر اختبارات للتحصيل المزدوج للأقساط** عبر مسيّرين، ولا لتكرار `ScopedCompensation` عبر الشهور.
- 🔴 **صفر اختبارات للتارك** في الرواتب أو التسوية (متعمَّد لكنه مكشوف).
- 🟠 **صفر اختبارات لمطابقة الأسطر بالإجماليات** (`Σ lines == total`) — وهي بالضبط الخاصية المكسورة في بند 🔴1، والتي عرّفوها بأنفسهم كقاعدة PL-08/2.
- 🟠 لا اختبار للتناسب × GOSI، ولا لدورة مخصّصة × سلف.

---

## ملحق: عيب لم يكن ضمن نقاط التركيز لكنه الأخطر ماليًا

🔴 **`AdjustPayrollEntryAction::recalculateEntry()` يمسح نصف بنود المحرك عند أي تسوية يدوية أو إعفاء.**
`app/Modules/Payroll/Actions/AdjustPayrollEntryAction.php:96-125` يعيد بناء الإجماليات من صيغة مجمّدة على أعمدة أبريل 2026:
```php
$customDeductions = round($manualDeductions, 2);                    // :109  ← يمسح الخصومات بالنطاق + الثابتة
$grossEarnings = basic + allowances + overtime_pay + bonuses;       // :111  ← يُسقط custom/obligation/formula_earnings
$totalDeductions = gosi + late + absence + permit + loan + manual;  // :115  ← يُسقط early_deductions + obligation + formula
```
كل الأعمدة المضافة بعد ذلك — `early_deductions` (يوليو 13)، `obligation_*` (أغسطس 2)، `formula_*`، `custom_earnings` — **غير مذكورة**. أي إضافة سطر يدوي أو إعفاء واحد على القسيمة ⇒ **إسقاط صامت لخصومات نقص الساعات والانصراف المبكر والالتزامات والمعادلات والخصومات الثابتة والخصومات بالنطاق** ⇒ ارتفاع الصافي بلا أثر. وبما أن أسطر تلك البنود **تبقى موجودة**، تصبح القسيمة متناقضة داخليًا. وأيضًا: لا يُعاد حساب `cash_paid`/`bank_paid` ⇒ لقطة التقسيم النقدي/البنكي (VF-04) تخالف الصافي بعد أي تسوية ⇒ تقارير WPS والنقدي خاطئة.

🟠 وفي نفس الملف، `waiveAutoDeduction('late')` يطابق بـ`reference_type = 'attendance_record'` (`:171`) — وهو المفتاح الذي تستخدمه **أيضًا** أسطر نقص الساعات (Phase 0.4) وأسطر FR-032. إعفاء التأخير **يحذف أسطر نقص الساعات** بينما يصفّر عمود `late_deductions` وحده ⇒ عمود `early_deductions` يبقى محمّلًا بمبلغ لا سطر له. العمود `source` (`SOURCE_LATENESS` مقابل `SOURCE_SHORTFALL`) الذي أُضيف في المرحلة 0.4 موجود ويحلّ المشكلة تمامًا — لكن الإعفاء لا يقرأه.

---

## الحكم النهائي

| الجانب | التقييم |
|---|---|
| هندسة إطار التسليم الآمن (0.1) | ممتاز — أفضل ما رأيته في هذا المستودع |
| تشخيص PR-10 وتنفيذه واختباره | ممتاز |
| انضباط المهاجرات والرجوع | ممتاز |
| **صلابة بوابة التكافؤ كدليل** | **ضعيفة** — إجراء بلا دليل (`flag_set` غير مقروء) |
| **اكتمال قابلية التفسير** | **جزئية** — حالة البند لا اشتقاقه |
| **صحة محرك الحساب الموروث** | **ضعيفة** — 6 عيوب مالية، أخطرها مفتوح من شاشة إعدادات |
| نضج المنظومة (سياسة/نطاق/ازدواج) | **ناقصة جوهريًا** |

**الخلاصة:** الفريق يبني **آلة ممتازة لتغيير الرواتب بأمان**، ويبنيها بجدية حقيقية. لكنه لم يفتح بعد صندوق **صحة الحساب نفسه**، والصندوق فيه ما هو أخطر من كل ما أصلحوه: `mode: 'minutes'` القابل للنقر، ومحو التسويات اليدوية لنصف بنود المحرك، والغياب الذي لا يُخصَم، والتعويضات بالنطاق التي لا تتوقف. أضف أن بوابتهم الحارسة — بحكم بنيتها — لا تستطيع رصد أيٍّ من ذلك، لأنها تقارن المحرك بنفسه.

---

# القسم ٢ — الحضور والورديات

# تقرير مراجعة: موديول الحضور (Laravel · `app/Modules/Attendance`)

**الخلاصة أولًا:** الشغل ماشي كويس على محورين ومتعثّر على محاور أخرى. جودة نواة الحساب (FX-05/FX-07) **ممتازة ومختبَرة بعمق**، لكنها محاطة بمسارات كتابة ضعيفة الحراسة (تعديل يدوي بلا validation، تصحيح بصمة يفقد الجدول، مزامنة بصمة تدهس التصحيحات) تُفرِّغ جزءًا من هذا الإتقان. الجهد الحقيقي واضح: 22 كوميت على الموديول منذ 2026-07-14، وتغطية اختبارية جادة (13 ملف اختبار على محوري FX-05/FX-07 وحدهما).

---

## ① الساعة المرنة — القواعد الأربع

**ما نُفِّذ:** `2026_08_03_000002_add_shift_flexibility` أضاف `shifts.mode/required_hours/grace_before/grace_after/checkin_window_*` ونسخها على `attendance_records`. `2026_08_23_000003` أضاف `applied_flex_window_minutes` (مدة النافذة بالدقائق، لا وقت نهايتها). التنفيذ في `Services/AttendanceCalculationService.php:491-542`.

**القاعدة (ج) — الفحص الحاسم:** ✅ **صحيح. الطرح من `shiftStart` وليس من `flexTo`.**

```
AttendanceCalculationService.php:507-508
  $officialStartAt = Carbon::parse($date . ' ' . $schedule['scheduled_start']);
  $windowEndAt     = $officialStartAt->copy()->addMinutes($appliedWindow);
:522  if ($checkIn->gt($windowEndAt)) {
:528      $lateMinutes = (int) $officialStartAt->diffInMinutes($measuredAt);
```

**المثال الرقمي من الكود:** بداية رسمية 09:00، نافذة 60 دقيقة ⇒ `windowEndAt = 10:00`. حضور 10:01 ⇒ العتبة تُقارَن بـ 10:00 (تجاوُز)، والقياس `10:01 − 09:00 = 61`. لو كان الطرح من `flexTo` لأنتج **1**، وهو ما يسمّيه الاختبار صراحةً فشلًا (`ThresholdSemanticsTest.php:28-45`).

تفصيلة ذكية ومُبرَّرة: العتبة تقرأ **الثواني غير المقتطعة** بينما القياس يستخدم `startOfMinute()` (`:520`)، فحضور 10:00:59 = 60 دقيقة وليس 0 ولا 1 — مُغطّى بثلاثة اختبارات متقاطعة (`ThresholdSemanticsTest.php:86,124`).

**بقية القواعد:** (أ) و(ب) و(د) صحيحة — الدخول داخل النافذة لا يدخل فرع `:522` أصلًا فيبقى `late_minutes = 0`. والخصم يُطبَّق حتى مع إكمال الساعات لأن `late_deduction_tier` يُحسم بمعزل عن `total_work_minutes`، و`PayrollCalculationService.php:572-578` يستعلم عن `late_deduction_tier != 'none'` فقط (مختبَر: `ThresholdSemanticsTest.php:163`).

### ملاحظات

🔴 **تصريح بالمرونة بلا `required_hours` عبر PATCH.** `StoreShiftRequest.php:28` يفرض `required_if:mode,flexible`، لكن `UpdateShiftRequest.php:29` **أسقط الشرط**. تحويل وردية إلى `flexible` بدون `required_hours` يجعل `AttendanceCalculationService.php:609` (`if ($requiredHours !== null)`) يتخطّى حساب النقص كليًا ⇒ القاعدة (ب) تنتج صفر خصم بصمت.

🔴 **التفاف على قاعدة FX-02 عند التعديل.** `ValidatesShiftWindow.php:62-66` تخرج فورًا إذا لم يكن `mode` حاضرًا في المُدخلات، و`UpdateShiftRequest.php:49-56` تدمج من قاعدة البيانات `start_time, end_time, is_overnight, grace_*` **فقط** — لا `mode` ولا `required_hours` ولا `checkin_window_end`. النتيجة: `PUT /attendance/shifts/{id}` بحقل `checkin_window_end` وحده يتجاوز شرطي «النافذة أقصر من المدة المطلوبة» و«لا تقل عن دقيقة». يمكن ضبط نافذة وصول 15 ساعة على وردية 8 ساعات. الاختبار `DeclinedAndValidationTest.php:230` يوثّق هذا السلوك كمقصود لسبب آخر (تعديل الاسم)، فلم يُلحَظ الثغرة.

🟠 **تصريح الإذن (`late_arrival` permit) لا يُطبَّق على الوردية المرنة.** `lateArrivalPermittedUntil()` يُستدعى في الفرع الثابت فقط (`:551`). موظف على وردية مرنة بإذن تأخير معتمد يُخصم منه رغم الإذن.

🟠 **النافذة المطبَّقة لا تُثبَّت في مساري إعادة الحساب الحقيقيين.** `pinProfile()` يثبّت `calculation_profile` فقط (`:780-785`). في `AttendanceService.php:110` (تعديل إداري) و`BiometricSyncService.php:478` (إعادة مزامنة يوم ماضٍ) يُعاد اشتقاق النافذة من الوردية **الحالية** ⇒ إعادة كتابة رجعية هي بالضبط ما تمنعه FX-05 AC5. المفارقة: `PayrollParityService.php:415-416` يعالج هذا عبر `$frozenWindows` — فالفريق يعرف المشكلة لكنه حلّها في هارنس التكافؤ فقط. اختبار AC5 الوحيد (`LegacyAndWindowTest.php:146`) يمرّ لأنه يستخدم `scheduleFromRecord()` حصرًا ولا يلمس هذين المسارين.

🟠 **الوردية المرنة الليلية غير قابلة للتعريف.** `ValidatesShiftWindow.php:79` تحسب `$windowEnd - $start` دون معالجة تجاوز منتصف الليل ⇒ وردية تبدأ 23:30 بنافذة حتى 00:15 تُرفَض. وفي وقت التنفيذ `deriveFlexWindowMinutes():758` يُرجع `null` لأي نافذة لا تتجاوز البداية رقميًا ⇒ الوردية تسقط في فرع FR-032 بلا تأخير أبدًا.

🟠 **يوم مرن بلا انصراف = لا خصم إطلاقًا.** `:648-656` يعيد `total_work_minutes = null` و`shortfall_minutes = null` عند غياب البصمة الثانية. موظف مرن يبصم دخولًا وينصرف بلا بصمة لا يُحتسب عليه أي نقص. لا آلية auto-close ولا علامة يوم ناقص.

🟡 **خصم مزدوج محتمل للدقائق نفسها.** يوم مرن تجاوز النافذة وأنقص ساعاته يُنتج سطر تأخير (كسر **يوم** كامل) + سطر نقص (بالساعة) معًا — مؤكَّد بـ `CombinedPhasesTest.php:58,87`. قرار سياسة لا خطأ برمجي، لكنه غير محدود سقفًا.

---

## ② فصل النقص عن التأخير

**ما نُفِّذ:** `2026_08_23_000001` أضاف ثلاثة أعمدة nullable: `shortfall_minutes`, `raw_lateness_minutes`, `calculation_profile`. المنطق في `AttendanceCalculationService.php:605-647`.

**هل الحقول منفصلة فعلًا؟** ✅ نعم على مستوى التخزين: تحت `srs_fx07` يذهب النقص إلى `shortfall_minutes` و`early_departure_minutes` يُصفَّر (`:621-622`)؛ وتحت legacy يستعير النقص العمود القديم و`shortfall_minutes` يبقى NULL (`:626`). قرار **عدم** تحويل العمودين إلى `integer` cast (`AttendanceRecord.php:66-70`) للحفاظ على تمييز NULL عن 0 قرار صحيح ومُبرَّر.

**هل المسير يستهلكهما بسياستين مختلفتين؟** ❌ **لا — يجمعهما في مقدار واحد.**

```
PayrollCalculationService.php:689-699
  ->get(['id','date','early_departure_minutes','shortfall_minutes']);
  $earlyMinutes += (int) $record->early_departure_minutes;
  $recordShortfall = $record->shortfall_minutes === null ? 0 : (int) $record->shortfall_minutes;
  ... $minutes = $earlyMinutes + $shortfallMinutes;
  $total = round(($minutes / 60) * $hourlyRate, 2);
```

🟠 الفصل **محاسبي وعرضي فقط**: سطران منفصلان في breakdown، لكن **معدّل خصم واحد** (`hourly`) وسويتش واحد (`early_departure_deduction`). لا يمكن اليوم أن يقول العميل «انصراف مبكر = ربع يوم، نقص ساعات = بالساعة». الحياد المبلغي (amount-neutrality) هو الهدف المُعلَن للمرحلة ونجح فيه — لكن السؤال الأصلي «سياستان مختلفتان» لم يُجَب بعد. `calculateEarlyDepartureDeductions` يُستدعى بـ `$mode` واحد.

🟡 `raw_lateness_minutes` مُعلَن write-only ولا يقرأه أي تقرير/تصدير/resource — عمود بلا مستهلك حاليًا.

---

## ③ شرائح خصم التأخير

**ما نُفِّذ:** `LatenessTier` (tenant, key, from_minutes, to_minutes, **mode**, value, sort_order, is_active) + `LatenessTierService`.

**قابلة للتعريف؟** ✅ جزئيًا — الشرائح والحدود والقيمة قابلة للتحرير، مع سقوط آمن للثوابت التاريخية (0.25/0.5/1.0) عند غياب تعريفات (`LatenessTierService.php:17-41`).

**الأنماط المدعومة؟** ❌ **نمط واحد فقط: كسر يوم.** لا مضاعف ولا خصم بالدقيقة.

🟠 **عمود `mode` ميت.** موجود في `LatenessTier::$fillable`، لكن `tiersFor():32-37` لا يُخرجه أصلًا، و`fractionFor():65-78` يُعيد `value` كنسبة من **الأجر اليومي** دون النظر إلى `mode`. أي شركة تعرّف `mode = 'minutes'` أو `'multiplier'` ستُخصم منها كسر يوم صامتًا. تعليق الاختبار نفسه يقرّ بالفجوة: التسعير بالمضاعف مؤجَّل («LT-03 MULTIPLIER pricing, which this phase deliberately does not implement» — `ThresholdSemanticsTest.php:22`).

🟡 استعلام غير مُذاكَر: `resolveTierKey()` يستدعي `tiersFor()` التي تضرب قاعدة البيانات في كل نداء، و`calculateLateDeductions` يناديها **داخل حلقة السجلات** (`PayrollCalculationService.php:583`) ⇒ N+1 على مسير كامل.

🟡 `LatenessTier::where('tenant_id', …)` يعتمد على أن الـ tenant مربوط. آمن حاليًا لأن `BiometricSyncService::processLogs():362-373` يربط الـ tenant للدفعة — لكنها اعتمادية ضمنية غير موثَّقة في الخدمة نفسها.

🟡 التسامح على الوردية المرنة يُمرَّر 0 (`:536`) وهذا **صحيح ومُبرَّر** (النافذة هي التسامح) — لكنه يعني أن شريحة `from_minutes: 1` تبدأ فورًا بعد النافذة: فرق 30 ثانية بين 10:00:00 و10:00:59 يقفز باليوم من «لا خصم» إلى «نصف يوم». مطابق للمواصفة، وقاسٍ عمليًا.

---

## ④ البنية الجديدة

| الجدول | الغرض | التقييم |
|---|---|---|
| `working_day_patterns(+_days,+_rules)` | نمط أيام العمل المتكرر، مُنطَّق بالنطاق ومؤرَّخ الفعالية، مع قواعد ترتيبية شهرية («آخر سبت عمل») | ✅ تصميم سليم؛ فهارس ملائمة |
| `attendance_configurations` | صف واحد للمستأجر: `consumption_mode` (legacy\|published) + `foundation_enabled` كمفتاح إيقاف | ✅ مفتاح إيقاف مزدوج جيد (`effectiveConsumptionMode()`) |
| `attendance_calendar_versions/_days` | تشغيلة توليد مؤرشفة بالإصدار مع تتبّع المصدر (عطلة/استثناء/قاعدة) | ✅ النسخ بالإصدار + `restrictOnDelete` على النمط قرار صحيح |
| `weekly_schedule_versions/_days` | التقويم + الوردية لكل يوم؛ منشور بلا مسار تحديث | ⚠️ ازدواج مصدر الحقيقة (أدناه) |
| `attendance_records.weekly_schedule_day_id` | تتبّع AC-005: أي يوم مولَّد خدم هذا السجل | ✅ nullable + nullOnDelete |

**هل حُلّت مشكلة «تعديل الوردية لا يسري على الأيام المحسوبة»؟** ⚠️ **جزئيًا فقط.**

🟠 **`ShiftService::update()` لا يُطلق إعادة التوليد إطلاقًا** (`ShiftService.php:36-51` — تسجيل تدقيق فقط). محفّزات `FutureRegenerationService` مربوطة بالاستثناءات والتخصيصات والأنماط والعطلات (`ScheduleExceptionController:34,55,70` · `ScheduleAssignmentService:98,111` · `WorkingDayPatternService:101` · `LeaveServiceProvider:40`) — **وليس بتعديل الوردية**، وهو أكثر تعديل يُجريه العميل.

🟠 **قارئان متناقضان على نفس الصف.** `ScheduleGenerationService.php:113-115` يخزّن لقطة مجمّدة `scheduled_start/scheduled_end` **إلى جانب** `shift_id`:
- `publishedScheduleFor():203-207` يقرأ `scheduleFromShift($day->shift)` ⇒ أوقات الوردية **الحيّة**.
- `dayInfoFor():291-292` يقرأ `$day->scheduled_start` ⇒ **اللقطة المجمّدة**.

بعد تعديل وردية، التقرير الشهري يعرض نافذة والمسير يحسب على نافذة أخرى، بلا أي إنذار. أحد المصدرين زائد ويجب حذفه.

🟡 `AttendanceConfiguration::forTenant()` يستخدم `firstOrCreate` — **كتابة داخل مسار قراءة** يُستدعى من `consumptionMode()` و`FutureRegenerationService:31`. مُذاكَر داخل النسخة الواحدة، لكنه سلوك مفاجئ.

---

## ⑤ تصحيح البصمة

**ما نُفِّذ:** `correction_scope` (check_in\|check_out\|both) في `2026_08_07_000001`، مع حجب تداخل ذكي: طلب معلّق لبصمة الدخول لم يعد يقفل تصحيح الانصراف لنفس اليوم (`PunchCorrectionService.php:90-100`). قفل الفترة المُقفَلة ماليًا يتطلب `payroll:process` (`:218`). Validation قوي ومتّسق (`Controller:215-254`): لا تواريخ مستقبلية، البصمة داخل نطاقها إلزامية وخارجه محظورة، والوقت المطلوب يجب أن يقع في نفس اليوم.

**الصلاحيات:** ✅ سليمة عمومًا — `attendance:read` للقائمة، `attendance:update` للتقديم/الاعتماد/الرفض، وطابور HR يمرّ الآن بـ `EmployeeScopeService::constraintFor` (`:119-121`) بعد أن كان يكشف المستأجر كله. مسارات `/me` تحلّ الموظف من المُنادي لا من الجسم.

### ملاحظات

🔴 **تصحيح ليوم بلا سجل حضور يُنتج يومًا بلا جدول ولا تأخير — للأبد.**
```
PunchCorrectionService.php:227-229
  $record = $this->recordFor(...) ?? AttendanceRecord::create([
      'tenant_id'=>…, 'employee_id'=>…, 'date'=>$date, 'status'=>'present',
  ]);                                  // ← لا shift_id ولا scheduled_start/end
:254  $this->calculator->scheduleFromRecord($record->fresh('shift'))
```
`scheduleFromRecord()` على سجل فارغ يعيد `emptySchedule(null, null)` ⇒ `scheduled_start` = null ⇒ فرعا التأخير (`:506`, `:545`) لا يُنفَّذان، و`syncAutoOvertime` يخرج فورًا لغياب `shift_id` (`:814`). هذه هي **الحالة الأشيع** («نسيت البصمة») وتنتج يوم حضور كامل بصفر تأخير وصفر إضافي. المسار الصحيح كان `resolveScheduleFor()` عند غياب سجل سابق.

🟠 **يوم مُعلَّم `absent` يبقى `absent` بعد اعتماد التصحيح.** `apply()` يحدّث `check_in/check_out/notes` (`:241-247`) و`computeDerivedFields` لا يُرجع `status` أبدًا. سجل كتبه `MarkEmployeeAbsencesAction` (`status='absent'`) يبقى غيابًا في كل التقارير والداشبورد رغم البصمة المعتمدة. (لا يؤثر على خصم المسير لأن الغياب يُخصم من الإجازات بلا أجر، لكنه تناقض تقاريري).

🟡 `recalc_result` لا يسجّل `shortfall_minutes` ولا `applied_flex_window_minutes` (`:262-266`) — سجل التصحيح لا يوثّق الكميّتين الجديدتين.

🟡 `notes` يُلحَق بلا سقف عند كل تصحيح (`:245-246`) — نمو غير محدود.

🟡 مسار `PATCH /me/punch-corrections/{id}/cancel` يعتمد على أن `withoutGlobalScope` غير مطبَّق (`Controller:60`) — سليم، لكن الملكية «تُفرَض بالنطاق» بلا فحص صريح كما يقول تعليق `cancelOwn():148`.

---

## ⑥ البصمة (TCP + الذاكرة)

**ما نُفِّذ (وهو عمل جيّد):** `ZKTecoClient::connect():100-119` صار TCP أولًا مع سقوط إلى UDP، و`readLargeData():533-547` يرمي استثناءً صريحًا عند نقل ناقص بدل تمرير بيانات مشوّهة — مع رسالة تشخيصية تطلب فتح TCP 4370. الأهم: `parseAttendance():202-240` لم يعد **يخمّن** تخطيط السجل من قابلية القسمة (السبب الجذري للفساد)، بل يقرأ ترويسة الحجم الداخلية ويتحقّق من اتساقها. هذا **إصلاح جذري صحيح**.

**إصلاح الذاكرة:** `decodeTime():310-331` صار يبني سلسلة `Y-m-d H:i:s` بدل كائن Carbon ويتحقّق بـ `checkdate()` + مقارنة نصية؛ و`writeGroupedLogs():389-400` يجمّع سلاسل ويحرّر `$logs`. القياسات في نص الكوميت حقيقية (172MB → 72MB على 49,328 بصمة).

**جذريًا أم التفافيًا؟** ⚠️ **التفافيًا.**

🟠 التمثيل تحسّن لكن **الاستهلاك ما زال O(تاريخ الجهاز كاملًا)**. `getAttendance()` يسحب السجل كله في مصفوفة واحدة، ثم تُطبَّق نافذة التاريخ **بعد** ذلك في PHP (`BiometricSyncService.php:243-250`). لا أمر بروتوكول بنطاق تاريخ، ولا streaming، ولا معالجة على دفعات. نص الكوميت نفسه يقرّ: «38 MB fits, but a site with several times this history would not» (`:214-218`). و`raiseMemoryLimitTo(256)` (`:283-303`) رفع سقف لا حلّ — و`@ini_set` مكتوم بلا فحص للقيمة المرتجعة ولا تسجيل عند فشله على استضافة مشتركة.

🔴 **المزامنة تدهس التصحيحات المعتمدة والإدخالات اليدوية.** `writeGroupedLogs():499-503` يستخدم `updateOrCreate` بلا أي حارس على `is_manual_entry`، ويفرض `'status' => 'present'` و`'is_manual_entry' => false`. تصحيح بصمة مرّ بدورة اعتماد كاملة يُمحى في أول إعادة مزامنة تلمس نفس اليوم (والنافذة الافتراضية `last_sync_at − 2 days` تلمسه فعليًا). حوكمة كاملة تُلغيها كتابة صامتة.

🔴 **الورديات الليلية مكسورة في مسار البصمة.** التجميع `$grouped[$uid][substr($ts,0,10)][]` (`:400`) يقسّم بالتاريخ التقويمي. وردية 22:00→06:00 تُنتج: يوم أول بـ `check_in=22:00` و`check_out=null` (⇒ `total_work_minutes = null`، لا نقص ولا انصراف مبكر)، ويوم ثانٍ بـ `check_in=06:00` يُقرأ كتأخير هائل. معالجة `is_overnight` موجودة في المحرك (`AttendanceCalculationService.php:630-631`) لكن التجميع يهدمها قبل الوصول إليها.

🟠 **إشارة نوع البصمة تُحسب ثم تُهمَل.** `ZKTecoClient::punchType():333-341` يفكّ 'in'/'out'/'auto' من الجهاز، و`writeGroupedLogs` يتجاهلها كليًا لصالح «أول/آخر بصمة». يوم بثلاث بصمات (دخول/خروج غداء/عودة) بلا بصمة انصراف نهائية يُسجَّل انصرافًا وقت العودة.

🟡 موظف واحد ببصمة واحدة فقط ⇒ `lastPunch = null` ⇒ `total_work_minutes = null` وصفر نقص (نفس ثقب البند ①).

---

## ⑦ الأوفرتايم

**نوافذ الفتح/الإغلاق:** ✅ موجودة — `OvertimePeriodService::isOpen()` بمنطق «المغلق يفوز» ونطاق فرع اختياري، وتُستشار في `AttendanceCalculationService.php:831-832`.

**اكتشاف من البصمة بدورة اعتماد:** ✅ موجود — `syncAutoOvertime():793-884` يلتقط تلقائيًا (بعتبة `overtime_threshold_minutes` + `overtime_min_minutes`)، ينشئ سجلًا `pending`، ويدخله في `ApprovalEngine::openIfConfigured('overtime', …)`. الفكرة idempotent: سجل معلّق يُحدَّث أو يُحذف مع إلغاء طلب الاعتماد المفتوح (`:838-846`)، وسجل محسوم لا يُمسّ.

**طلب الموظف لإضافي في فترة مغلقة يُحسب من البصمة؟** ❌ **غير مُنفَّذ إطلاقًا.**

🔴 **لا يوجد endpoint لطلب أوفرتايم من الموظف.** `OvertimeController` يحتوي `index/export/approve/reject` فقط، و`SelfServiceAttendanceController` لا يحتوي أي دالة أوفرتايم. الأوفرتايم مسار آلي أحادي الاتجاه. وبالتالي السيناريو المطلوب — «موظف يطلب إضافيًا في فترة مغلقة فيُحتسب من البصمة» — **لا يمكن تمثيله**: الفترة المغلقة تقتل الالتقاط الآلي (`:832`) ولا يوجد مسار بشري يتجاوزها.

🟠 **الفترة المغلقة تحذف سجلات معلّقة بلا رجعة.** `:834-850` يحذف السجل المعلّق عند عدم التأهّل. إن فُتحت النافذة لاحقًا، لا شيء يعيد السجل إلا إعادة مزامنة كاملة لتلك الأيام. وبالمقابل سجل **معتمَد** قبل الإغلاق لا يُمسّ ويُدفع — سياستان متناقضتان لنفس الحدث.

🟠 **لا أوفرتايم للورديات المرنة.** `:814` يشترط `$schedule['scheduled_end']`، والأوفرتايم يُقاس من نهاية الدوام. المنطق الصحيح للمرنة هو «ما يتجاوز `required_hours`» — غير مُنفَّذ. وردية مرنة بـ `end_time` معرَّف تنتج أوفرتايم بمرجع لا معنى له فيها.

🟡 عتبة الأوفرتايم تُقرأ من الوردية الحالية (`scheduleFromShift():922`) لا من لقطة السجل — تعديل العتبة يعيد تقييم أيام سابقة عند أي إعادة حساب.

---

## ⑧ خارج النقاط السبع — أخطر ما وجدت

🔴 **`AttendanceController::update()` يمرّر `$request->all()` بلا أي validation.**
```
AttendanceController.php:84   $record = $this->service->update($record, $request->all());
AttendanceService.php:87      if (array_intersect(array_keys($data), ['check_in','check_out','date','employee_id','shift_id'])) { … }
AttendanceService.php:120     $record->update($data);      // ← لا إعادة حساب، لا فلترة حقول
```
لا `FormRequest`، ولا قائمة حقول مسموحة. و`AttendanceRecord::$fillable` يشمل `late_minutes`, `late_deduction_tier`, `shortfall_minutes`, `raw_lateness_minutes`, `calculation_profile`, `applied_flex_window_minutes`, `status`. طلب يحمل `{"late_minutes":0,"late_deduction_tier":"none","shortfall_minutes":0}` **لا يلمس أيًّا من مفاتيح `array_intersect`** ⇒ يسقط إلى `:120` ويكتب مباشرة بلا إعادة حساب. أي حامل `attendance:update` يستطيع تصفير خصومات أي موظف في نطاقه، والالتفاف كليًا على دورة اعتماد تصحيح البصمة، بل والعبث بأعمدة الحوكمة (`calculation_profile`) التي بُني حولها إطار كامل من الأدلة والـ rollback المحمي. الثغرة الأمنية المجاورة (`AUD-050`) عولجت بعناية في نفس الدالة — وهذه لم تُلحَظ.

🟡 `ManualEntryRequest` لا يتحقّق أن `check_in`/`check_out` يقعان في `date` (بينما `PunchCorrectionController:247-254` يتحقّق) ⇒ إدخال يدوي بتاريخ 2026-04-06 وبصمة 2026-04-10 يُنتج آلاف دقائق التأخير.

---

## تقييم التغطية الاختبارية

**قوية بشكل لافت** على FX-05/FX-07: 13 ملف اختبار، بعضها مكتوب ليفشل تحديدًا على التصميمين الخاطئين (`ThresholdSemanticsTest.php:124`)، مع اختبار مسار كتابة حقيقي عبر `CheckInAction`/`CheckOutAction` بُني لأن نفس الثغرة وقعت في المرحلة 0.4 (`WritePathTest.php:13-31`). هذا مستوى نضج عالٍ.

**الفجوات المقابلة:**
- لا اختبار لإعادة المزامنة البصمية بعد تعديل نافذة (المسار الذي يكسر AC5 فعليًا).
- لا اختبار لـ `AttendanceService::update` مع نافذة معدَّلة.
- لا اختبار لتصحيح بصمة على يوم بلا سجل حضور.
- لا اختبار لوردية ليلية عبر مسار البصمة.
- لا اختبار يثبت أن المزامنة **لا** تدهس تصحيحًا معتمدًا.
- لا اختبار لـ `AttendanceController::update` بحمولة عشوائية.

النمط واضح: **المحور الذي كُتبت له مواصفة (PL/FX/FR) مُنفَّذ ومُختبَر بامتياز؛ وما وقع بين المواصفات — التفاعل بين المسارات — هو حيث تسكن كل العيوب الحرجة.**

---

# القسم ٣ — الاعتمادات والعهدة وإنهاء الخدمة

# تقرير مراجعة: الاعتمادات والأصول وإنهاء الخدمة
**النطاق:** `hr/app/Modules/{Approval,Asset,Offboarding,Employee}` · الفترة من 2026-07-14

**الخلاصة أولًا:** الشغل ماشي كويس **من ناحية العمق المعماري** — محرّك الاعتماد الآن محرّك حقيقي (سياسات متوازية، نصاب، تسلسل، تفويض، تصعيد، إرجاع للتعديل، تجاوز إداري مُوثّق)، ودورة إنهاء الخدمة والعهدة صارت كيانات أولى بدل حقول متناثرة، والتغطية الاختبارية كثيفة (17 ملف اختبار للاعتمادات وحدها). لكن **الاكتمال ليس بمستوى العمق**: هناك ثغرة حوكمة حرجة في سحب إنهاء الخدمة، وعطل وظيفي مؤكّد في بانِي الحقول الديناميكي، وعدة مسارات «طلب معلّق للأبد» بلا مخرج، وميزات مُهاجَر لها في قاعدة البيانات بلا أي واجهة تشغّلها.

---

## ① محرّك الاعتماد

### ما نُفِّذ
| البند | الحالة |
|---|---|
| بانِي حقول ديناميكي | ✅ `request_type_fields` + `RequestTypeController::setFields` (`RequestTypeController.php:106-139`) + تحقق فعلي `PayloadValidator.php:22-70` (يُسقط المفاتيح غير المعرّفة، يفرض required/numeric/date/in) |
| موافقات متوازية / نصاب | ✅ 6 سياسات `ApprovalStep.php:12-26`، التقييم في `ApprovalEngine.php:258-331` (all/majority/quorum/sequential + كشف «الأغلبية صارت مستحيلة» → رفض مبكر) |
| إرجاع للتعديل (send-back) | ✅ `returnToRequester` `ApprovalEngine.php:464-500` + `resubmit` :507-551 — الرفض ما زال نهائيًا (لا reopen)، وهذا سليم |
| `effect_applied_at` | ⚠️ موجود لكن **مستهلَك في مكان واحد فقط** |
| `payload_version` | ⚠️ يُكتب ويُعرض ولا يُقرأ في أي منطق |

### ملاحظات ومخاطر

**🔴 `setFields` يعطب من الحفظة الثانية — بانِي الحقول يعمل مرّة واحدة لكل مفتاح.**
`RequestTypeField extends BaseModel` و`BaseModel` يستخدم `SoftDeletes` (`app/Core/Models/BaseModel.php:12`). و`setFields` يفعل `$type->fields()->delete()` ثم يُعيد الإنشاء بنفس المفاتيح (`RequestTypeController.php:123-135`) — بينما المهاجرة تضع فهرسًا فريدًا صلبًا **بلا `deleted_at`**:
`2026_08_02_000002_add_dynamic_request_fields.php:33` → `$table->unique(['tenant_id','request_type_id','key'])`.
الصف المحذوف منطقيًا يبقى في الجدول بنفس القيم ⇒ إعادة إدراج نفس `key` = انتهاك قيد فريد ⇒ 500. أي تعديل ثانٍ على مخطط حقول أي نوع طلب يفشل.
**لماذا لم تكتشفه الاختبارات:** كل اختبار ينشئ نوعًا جديدًا ويستدعي `setFields` مرّة واحدة فقط (`DynamicRequestFieldsTest.php:45,60,73,86`، `ThresholdFieldGovernanceTest.php:44`، `ApprovalPolicyEngineTest.php:62`). لا اختبار واحد يحفظ المخطط مرّتين — وهي أول شيء يفعله المستخدم الحقيقي.

**🔴/🟠 الموافقات القديمة تبقى صالحة بعد تغيير الحمولة عند إعادة التقديم.**
`resubmit` يزيد `payload_version` (`ApprovalEngine.php:534`) لكنه **لا يُبطل `approval_actions` المسجّلة على نفس الخطوة**. في خطوة `all_approve`: يعتمد A، ثم يُرجع B الطلب، فيُعدّل مقدّم الطلب المبلغ من 1,000 إلى 100,000، ثم يعتمد B وحده ⇒ `evaluateStep` يرى اعتمادين ⇒ الخطوة تكتمل. **A اعتمد نسخة أخرى تمامًا.** وهذا بالضبط جواب سؤال «ما دور `payload_version`؟»: لا شيء تشغيليًا — لا `decide` ولا `evaluateStep` ولا أي بوابة تقرأه (تأكيد: كل الاستخدامات في `ApprovalRequest.php:38,56` و`ApprovalRequestResource.php:27` و`ApprovalEngine.php:519-541` — كتابة وعرض فقط). هو علامة تدقيق لا آلية حماية.

**🟠 `effect_applied_at` لا يمنع التطبيق المزدوج إلا للزيادة على الراتب.**
المستهلك الوحيد في الكود كله هو `ApplySalaryIncreaseOnApprovalDecision.php:43,75`. بقية المستمعين — 15 مستمعًا لـ`ApprovalFinalized` — لا يفحصونه ولا يختمونه، ومنهم `ApplyGenericEffectOnApprovalDecision.php:25-63` الذي يعتمد بدلًا منه على حارس هشّ (`$subject->status === 'pending'`). فالعمود يحمي أثرًا واحدًا لا «الآثار التراكمية» كما يوحي توثيق المهاجرة.

**🟠 `decide()` بلا معاملة ولا قفل.** `ApprovalEngine.php:172-251` يقرأ فحص «سبق أن اتخذت إجراءً» ثم يكتب ثم يقيّم — بلا `DB::transaction` ولا `lockForUpdate`. معتمِدان متزامنان على خطوة `majority` قد يستوفيان النصاب معًا فيُستدعى `advanceOrFinalize` مرّتين ⇒ `ApprovalFinalized` مرّتين ⇒ أثر تراكمي مزدوج لكل مستمع بلا حارس.

**🟠 طلبات تُحبس للأبد بلا مخرج غير التجاوز الإداري:**
- `replaceSteps` يحذف كل الخطوات ويعيد إنشاءها (`ApprovalRouteController.php:56-59`) بلا أي معالجة للطلبات الجارية. طلب `pending` على `current_step_order = 2` وصار المسار من خطوة واحدة ⇒ `decide` يرمي «لا توجد خطوة نشطة» أبدًا.
- `ApprovalRouteController::destroy` (`:88-92`) يحذف المسار بلا فحص طلبات مفتوحة ⇒ `$request->route` = null ⇒ «لا يوجد مسار اعتماد».
- حالة `blocked` (لا معتمِد قابل للحل) لا يوجد لها مسار «إعادة توجيه» أو «إسناد معتمِد» — فقط تجاوز أو إلغاء.

**🟠 «الأثر المُعلَن بالبيانات» (`add_effect_to_request_types`) ميزة ميتة عمليًا.**
`RequestTypeController::update` يتحقّق من قائمة ثابتة لا تتضمن `effect` (`:57-68`)، و`store` لا يضبطه، ولا يوجد seeder ولا مهاجرة تكتبه (بحث شامل: الكاتب الوحيد هو `GenericEffectTest.php:73,86,111` مباشرة على الموديل). النتيجة: الهدف المُعلن «نوع طلب جديد يحتاج إعدادًا لا كودًا» غير قابل للتحقيق عبر أي واجهة — لا API ولا شاشة.

**🟡 دلالات غير محدّدة في التفويض المتضارب.** `evaluateStep` يُرجِع قرار المفوَّض إليه إلى المفوِّض (`ApprovalEngine.php:277-284`)، لكن `decide` يمنع التكرار بمعرّف المستخدم فقط (`:202-209`). فلو اعتمد المفوِّض ورفض المفوَّض إليه، ينهار «المقعد» الواحد إلى اعتماد + رفض معًا ⇒ `$rejections > 0` ⇒ رفض. سلوك غير موثّق ولا مُختبَر.

**🟡 `steps.*.order` بلا `distinct`** (`ReplaceApprovalStepsRequest.php:27`) — ترتيبان متطابقان يُسقطان خطوة صامتًا عبر `firstWhere('order', …)`.

**🟡 المستفيد لا يستطيع إعادة التقديم.** `resubmit` يشترط `requester_user_id` حصرًا (`ApprovalEngine.php:512-514`)؛ طلب قُدِّم «بالنيابة» ثم أُعيد للتعديل لا يستطيع الموظف المستفيد استكماله — فقط الإداري الذي قدّمه.

---

## ② التجاوز الإداري (admin override)

### ما نُفِّذ — التنفيذ سليم في جوهره
- صلاحية مستقلة `approvals:override` **غير مُسنَدة لأي دور افتراضيًا** (`PermissionRegistry.php:99-107`) — قرار جيد؛ `super_admin` يصلها عبر `Gate::before` فقط.
- سبب إلزامي في طبقتين (`ApprovalRequestController.php:276` + `ApprovalEngine.php:401-403`).
- ختم ثلاثي على الطلب + `is_override` على سطر الإجراء + سجل `audit_logs` مستقل (`ApprovalEngine.php:422-445`).
- **الأهم وقد أصابوا فيه:** التجاوز بالاعتماد يمرّ من `finalizeApproved` نفسه (`:449`)، فالآثار البعدية تُطبَّق كاملة — لا «إغلاق شكلي». و15 اختبارًا في `AdminOverrideTest.php` تغطي ذلك.

### المخاطر الرقابية
**🟠 لا حوكمة نطاق (branch scope) على التجاوز إطلاقًا.**
`ApprovalRequestController::override` (`:269-295`) لا يستدعي `EmployeeScopeService` — بينما `show` نفسه يفعل (`:119-124`) و`store` بالنيابة يفعل (`:153`). فحاملُ `approvals:override` مقيّد الفرع يستطيع إغلاق طلبات موظفين خارج فرعه كليًا. التبرير في التوثيق («ليس شرطًا أن يكون معتمِدًا في السلسلة») يبرّر تخطّي *السلسلة*، لا تخطّي *النطاق التنظيمي*.

**🟠 التجاوز يخترق كل بوابات النزاهة الأخرى بلا استثناء موثّق.** لا يوجد استثناء لأنواع الطلبات المالية أو `is_security_route` أو `affects_balance` — تجاوز واحد يصرف قرضًا أو يعتمد مخالصة نهائية. الأثر الرقابي الوحيد لاحق (سجل)، لا مانع مسبق (مثل «تجاوز مالي يتطلب معتمِدَين»).

**🟡 لا إشعار موجّه للمعتمِدين الذين سُحبت الخطوة من تحتهم** — عكس `cancel` الذي يُشعرهم صراحة (`ApprovalEngine.php:761-765`).

---

## ③ الأصول والعهدة

### ما نُفِّذ
| البند | الحالة |
|---|---|
| نقل عهدة مباشر بين موظفين | ✅ دورة كاملة: `initiate → approval → awaiting_ack → acknowledge` مع مهلة 7 أيام وانتهاء صلاحية مجدول (`AssetTransferService.php:38-199`, `routes/console.php:42`) |
| دفتر حركة | ✅ `asset_movements` أحادي الاتجاه (`AssetMovement.php`, `AssetLifecycleService.php:13-31`) |
| تتبّع حالة (سليم/تالف) | ⚠️ `condition` حقل نصّي حرّ بلا قاموس، والحالات `lost/damaged` على `status` |
| مسؤولية مالية عند الفقد → الرواتب | ✅ **الأنظف في النطاق كله**: `AssetChargeService.php:35-81` — التزام واحد لا يتكرّر (`existsForSource`)، ووجهته تُحسم بالسياق: راتب للموظف على رأس العمل، مخالصة للمُنهي خدمته (`:84-91`) |

**التقييم:** الملكية لا تنتقل إلا بعد الاعتماد **و** إقرار المستلم — هذا صحيح ومُختبَر (`AssetTransferWorkflowTest.php`، 9 اختبارات).

### الملاحظات
**🟠 التسليم والإرجاع يُطبَّقان قبل الاعتماد — والبوابة الصلبة تُفتح مؤقتًا.**
`EmployeeAssetController::return` يضبط `status = 'returned'` فورًا ثم يفتح دورة الاعتماد بعدها (`:69-76`). و`ClearanceService::outstanding` لا يحسب إلا `status = 'assigned'` (`ClearanceService.php:29`). فبين لحظة «إرجاع غير معتمد» ولحظة رفضه، **بوابة العهدة الصلبة تمرّ** — ويمكن إتمام المخالصة والأرشفة. الرفض لاحقًا يعيد الأصل إلى `assigned` (`ApplyAssetDecisionOnApproval.php:45-46`) على موظف صار مؤرشفًا.

**🟠 التراجع لا يُسجَّل في «الدفتر الذي لا يُعدَّل».**
`ApplyAssetDecisionOnApproval` يعكس الحالة بلا `AssetMovement` مقابل (`:43-47`). فدفتر الحركة يقول «إرجاع» بينما الحالة `assigned` — أي أن سجل التدقيق يفارق الواقع.

**🟠 سلسلة الحيازة تنقطع عند كل نقل.**
`acknowledge` ينشئ صف عهدة **جديدًا** للمستلم ويسجّل الحركة على الجديد فقط (`AssetTransferService.php:131-149`). الصف القديم لا يحمل حركة إغلاق، و`asset_transfers` لا يحمل مؤشرًا للصف الجديد (يحمل `employee_asset_id` القديم فقط). فلا يمكن إعادة بناء سلسلة «هذا الجهاز مرّ على من» من قاعدة البيانات إلا بالتخمين عبر `identifier`.

**🟠 نقل عالق في `pending` يقفل الأصل إلى الأبد.**
`expireTimeouts` لا يتناول إلا `awaiting_ack` (`:185`)، و`ACTIVE_STATUSES` تشمل `pending` (`AssetTransfer.php:98-102`). فإن كان مسار اعتماد `asset_transfer` معطّلًا أو صار `blocked`، يبقى النقل `pending` أبدًا ولا يُقبل أي نقل آخر لنفس الأصل — بلا مسار إلغاء (لا endpoint إلغاء للمُبادِر أصلًا).

**🟡 `employee_asset` غير مسجَّل في `ApprovalSubjectRegistry.php:17-27`** — إلغاء طلب تسليم/إرجاع يترك الأصل في حالته المؤقتة بلا تعويض.
**🟡 `AssetTransfer::STATUS_APPROVED` (`:91`) معرّفة ولا تُكتب أبدًا** — حالة ميتة في آلة الحالات.
**🟡 انحراف عقد بين الواجهة والباك:** `AssetStatus = 'assigned' | 'returned'` في `hr-dashboard/src/services/assets.service.ts:10` بينما الباك يُصدِر `transferred|lost|damaged`؛ و`STATUS_LABELS` في `EmployeeAssetsTab.tsx:23-26` تعرف تسميتين فقط، والاحتياطي `?? asset.status` (`:179`) يطبع نصًا إنجليزيًا خامًا داخل واجهة عربية. وقائمة `AssetTransferStatus` (`:16-24`) تُدرج `acknowledged`/`cancelled` غير الموجودتين وتُغفل `completed`/`expired`.

---

## ④ موديول Offboarding

### الدورة كما نُفِّذت — **آلية إلى حدٍّ بعيد وهذا إنجاز حقيقي**
```
اعتماد استقالة/إنهاء خدمة
  → openCase تلقائيًا (ApplyResignationOnApprovalDecision.php:58 / ApplyTerminationOnApprovalDecision.php:121)
  → بذر 5 بنود إخلاء طرف + الموظف → notice_period  (OffboardingService.php:56-85)
  → completeItem × 5 (بوابة عهدة صلبة على بند custody :101-103)
  → transitionToSettlement تلقائيًا: بناء المخالصة + طيّ مبالغ البنود كخصومات + تقديمها للاعتماد (:210-238)
  → اعتماد المخالصة → markSettled (SettleOffboardingCaseOnSettlementApproval.php:39)
  → مسح يومي 00:15 يقيس الجاهزية (5 موانع) → finalize (أرشفة + تعطيل الدخول) أو pending_finalization
```
بوابة العهدة موجودة وحقيقية (`assertCleared` تُستدعى في البند وفي `ArchiveEmployeeAction.php:17`). التصفية مكتملة معقولًا (مكافأة + رصيد إجازات + قروض + خصومات البنود).

### الملاحظات

**🔴 سحب إنهاء الخدمة ذاتيًا بلا أي صلاحية ولا موافقة، وفي أي مرحلة.**
`app/Modules/Offboarding/Routes/api.php:13` — `POST me/offboarding/withdraw` **بلا أي middleware صلاحيات**، و`SelfServiceOffboardingController::withdraw` (`:97-107`) لا يفحص شيئًا، و`OffboardingService::withdraw` (`:331-357`) يقبل أي حالة ضمن `OPEN_STATUSES` — أي بما فيها `settled` و`pending_finalization`.
النتيجة العملية:
1. موظف **مفصول** (لا مستقيل) — لأن `openCase` تُستدعى من مستمع الـ`termination` أيضًا — يستطيع بنداء API واحد أن يُعيد نفسه `active`، ويُعيد تفعيل حساب دخوله (`$employee->user->update(['is_active' => true])` `:348`)، ويمسح `last_working_day` و`termination_reason`.
2. حتى بعد اعتماد المخالصة (وربما صرفها) — لا شيء يعكس المخالصة ولا يمنع السحب.
3. الحالة السابقة لا تُستعاد بل تُفرض `'active'` قسرًا (`:342`) — موظف كان `probation` يعود `active`.
لا اختبار يغطي السحب المتأخر: `OffboardingLifecycleTest.php:147` يختبر السحب من `in_clearance` عبر الخدمة مباشرة فقط.

**🟠 `reopenItem` بعد بناء المخالصة يخلق مخالصة يتيمة قابلة للصرف.**
`OffboardingService.php:160-165` يفكّ `settlement_id` ويعيد الحالة لـ`in_clearance` — لكنه **لا يلغي المخالصة القديمة ولا يسحب طلب اعتمادها**. وهي بحالة `pending` لا `draft`، فـ`SettlementService::generate` (`:42-45`) لن يعيد استخدامها بل يُنشئ ثانية. النتيجة: مخالصتان لنفس الموظف، الأولى ما زالت في دورة اعتماد حيّة؛ وإن اعتُمدت فـ`SettleOffboardingCaseOnSettlementApproval.php:31` لن يجد لها حالة (`settlement_id` صار null) فتُصرف بصمت خارج الدورة.

**🟠 مسح «التعافي» المجدول يُنتج حالات لا يمكن اعتمادها.**
`catchUpStalledClearances` (`:181-207`) يُستدعى من الأمر المجدول (`OffboardingReadinessCommand.php:44`) بلا مستخدم مصادَق. و`SettlementService::submit` يفتح دورة الاعتماد فقط `if ($requester = auth()->user())` (`SettlementService.php:130-136`). فالمخالصة تُضبط `pending` بلا أي `approval_request` ⇒ الجاهزية تشترط `settlement->status === 'approved'` (`OffboardingReadinessService.php:39`) ⇒ الحالة تعلق في `in_settlement` للأبد. الإصلاح يُنتج عطلًا مختلفًا.

**🟠 مانع «طلبات الاعتماد المعلّقة» يفوته نصف الطلبات.**
`OffboardingReadinessService.php:50-52` يبحث بـ`requester_user_id` فقط وبحالة `pending` فقط. فطلب قُدِّم **بالنيابة** عن الموظف المغادر (وهي ميزة المشروع نفسه) لا يمنع الأرشفة، وكذلك طلب عالق في `blocked` أو `returned`. الموظف يُؤرشَف وطلباته تبقى معلّقة في صناديق المعتمِدين.

**🟠 «الطرف المسؤول» بيانات لا صلاحيات.**
كل عمليات البنود (`complete/assign/reopen`) محكومة بـ`offboarding:manage` وحدها (`Routes/api.php:42-47`). حقل `assigned_user_id` لا يمنح صاحبه حق إنجاز بنده، ولا يوجد endpoint لوضع بند في `blocked` رغم وجود الحالة (`ClearanceItem.php:214`) ولا لتسجيل `hr_override` رغم وجود نوع الحدث (`OffboardingEvent` — أنواع `item_delegated` و`hr_override` معرّفة ولا تُكتب أبدًا). فعمليًا: قسم واحد (HR) ينقر نيابة عن الخمسة.

**🟠 مسار أرشفة موازٍ يتخطّى الدورة كلها.** `POST employees/{id}/archive` بـ`employees:update` فقط — يفرض بوابة العهدة لكنه لا يفحص وجود حالة إنهاء خدمة مفتوحة ولا اعتماد المخالصة. الموظف يُؤرشَف والحالة تبقى `in_clearance` إلى الأبد. وكذلك `employees/{id}/restore` يعيده بلا أي حدث على الجدول الزمني.

**🟡 مستمع الاستقالة يصمت تمامًا إذا كان `last_working_day` مضبوطًا مسبقًا** (`ApplyResignationOnApprovalDecision.php:39`) — استقالة معتمَدة بلا حالة إنهاء خدمة وبلا سطر سجل واحد.

---

## ⑤ دورة بيانات الموظف

### ما كانت المشكلة وكيف عولجت — **هذا البند مُعالَج جيدًا**
1. **«الموظف يختفي بعد الإضافة»** (`a34ba10`): القائمة كانت تُرجع `status=active` حصرًا؛ صارت تستثني `archived` فقط.
2. **«الرقم الوظيفي يُداس»**: `EmployeeService::update` يُسقط `employee_number` قسرًا (`EmployeeService.php:245`).
3. **«البيانات لا تُحفظ بعد الحفظ»** — والسبب الجذري كان في الواجهة لا الباك (`hr-dashboard/8f5fceb`): `splitNamesFromApi` كان يثبّت اسم العائلة عند الكلمة الرابعة، فأي اسم عربي من 2–3 كلمات يترك الحقل الإلزامي فارغًا ⇒ **المعالج لا يتجاوز الخطوة 1 أصلًا** ⇒ الخطوتان المالية والمؤهلات لا تُرسَلان قط. عولج بالتثبيت على الكلمة الأخيرة.
4. **الحقول الحسّاسة كانت تُسقَط بصمت مع رد 200** — صار `guardSensitiveFields` (`EmployeeController.php:95-136`) يرفض بـ403 عند تغيير فعلي، مع مقارنة قانونية (`canonicalize` `:155-181`) تتسامح مع إعادة إرسال نفس القيمة. والبوابة صارت تُطبَّق على **الإنشاء** أيضًا (`:48`) — كانت مفتوحة تمامًا عند التوظيف.
5. **العقد النشط لم يكن يُحدَّث بعد زيادة الراتب** — عولج بـ`syncActiveContractCompensation` (`EmployeeService.php:307-320`) مع ترك العقود المنتهية كسجل تاريخي.
6. **المؤهلات:** المسارات الخمسة (`education|certifications|skills|languages|experiences`) مجموعة كاملة في المعالج ومعروضة في البروفايل؛ تفريغ حقل اختياري صار يُرسَل `null` صريحًا فيُحفظ.

### حقول تُجمع ولا تُعرض / العكس
- **تُجمع ولا تُعرض في `EmployeeResource`:** `contract_type`, `contract_number`, `contract_start_date`, `contract_end_date`, `contract_duration_months`, `contract_notice_days` — وهذا **مقبول**: تُحوَّل إلى `employee_contracts` وتُعرض عبر `active_contract` (`ContractResource.php:22-27`). خارج ذلك لا يوجد حقل مجموع ومفقود.
- **🟠 عدم تماثل حقيقي بين الإضافة والتعديل:** الحقول الستة أعلاه موجودة في `StoreEmployeeRequest` وغائبة كليًا عن `UpdateEmployeeRequest` (تحقّقت بالمقارنة). فبيانات العقد تُدخَل مرّة واحدة عند التوظيف ولا يمكن تصحيحها من شاشة تعديل الموظف — يلزم مسار `contracts/*` منفصل. رقم عقد أُدخل خطأً لا يُصحَّح من حيث أُدخل.
- **🟡 `status` غير قابل للتعديل** عبر `UpdateEmployeeRequest` (قرار سليم — يمرّ عبر suspend/archive)، لكنه يُعرض في المورد، ما يوهم الواجهة بأنه حقل قابل للتحرير.

### ملاحظات إضافية
**🟡 توثيق `guardSensitiveFields` يخالف الكود.** التعليق يقول «حمولة تُعيد القيمة المخزَّنة (أو تتركها فارغة) ما زالت مقبولة» (`EmployeeController.php:~100`)، لكن `changesStoredValue` (`:138-146`) يقارن `null` الوارد بالقيمة المخزَّنة ويعتبرها **تغييرًا** ⇒ 403. الواجهة تتفادى ذلك بحذف الحقول الفارغة قبل الإرسال (`addEmployeeApiSchema.ts:366,372`) — لكن أي عميل API يعيد `national_id: null` يُمنع من حفظ أي تعديل. الوعد بالتوافق الخلفي غير محقّق خارج واجهتهم.

**🟡 `canonicalize` يحوّل كل نص رقمي إلى `float`** (`:174-176`). `national_id`/`gosi_number` نصوص رقمية؛ عند تجاوز 15 خانة تفقد الدقة وقد يتساوى معرّفان مختلفان ⇒ يمرّ تعديل PII كأنه «لا تغيير». مخاطرة منخفضة بالهويات السعودية العشارية، قائمة نظريًا.

---

## ما ينقص عرضيًا في التغطية الاختبارية
كثافة الاختبارات ممتازة (17 ملف للاعتمادات، 8 للـoffboarding، 3 للأصول) لكن **الفجوات تقع بدقة على النقاط الحرجة أعلاه**، وكلها غير مغطّاة بأي اختبار:
- حفظ مخطط الحقول مرّتين (العطل 🔴).
- `POST /me/offboarding/withdraw` عبر HTTP، ومن حالة `settled`، ولموظف مفصول (الثغرة 🔴).
- إعادة تقديم بحمولة معدَّلة على خطوة `all_approve` سبق أن اعتمدها آخر.
- `reopenItem` بعد بناء المخالصة.
- `catchUpStalledClearances` من سياق CLI بلا `auth()`.
- `replaceSteps` / `destroy` على مسار له طلبات جارية.

---

# القسم ٤ — الجودة والمخاطر التشغيلية

# تقرير المراجعة — الجودة والمخاطر العامة
**النطاق:** `review2/hr` (Laravel) + `review2/hr-dashboard` (React) · **الفترة:** 2026-07-14 → 2026-09-07 (47 commit باك / 35 فرونت)

---

## الحكم المختصر

**الشغل ماشي كويس تقنيًا — لكن ليس بنفس المستوى في كل مكان، والفجوة الأخطر ليست في الكود بل في التشغيل والصلاحيات.**

الجودة **ثنائية بوضوح**: العمل من 2026-08-18 فصاعدًا (الرواتب Layer 0، التصدير، BioMetric، RTL) من أعلى ما يُرى — قياسات حقيقية، تعليل مكتوب، اختبارات تُثبت الأرقام لا الـstatus. أما العمل من 2026-07-14 إلى 2026-08-10 فغير قابل للمراجعة أصلًا: 19 من 47 commit برسائل مثل `attendance` و`Week cycle` و`new features 2`، وأحدها `enhancements and some edits` بـ **4,623 سطر إضافة في دفعة واحدة** (`a63a2a5`).

وأخطر ما وجدته لا يخص المنطق: **خط النشر (CI/CD) لا يُشغّل الهجرات ولا composer ولا يمسح الكاش — يرفع ملفات عبر FTP فقط**، و**بوابة التكافؤ (parity) التي بُني عليها كل حَوكمة الرواتب لا يمكن تشغيلها في الإنتاج بالتصميم**، و**دور «قراءة فقط» يرى رواتب المنشأة كاملة**.

---

## 1) الاختبارات — عميقة حيث تهم، وغائبة حيث لا يُتوقع

**ما نُفِّذ:** 381 ملف / **2,272 دالة اختبار**. التوزيع: `Hardening` 32، `Phase3Gov` 25، `Phase1Fixes` 25، `Attendance` 24، `Authorization` 17، ومجموعات الرواتب المتخصصة (`PayrollShortfall` 10، `PayrollGovernance` 12، `PayrollProration` 8، `PayrollFlexibleLateness` 7).

**هل التنفيذ صحيح ومكتمل؟** العمق **حقيقي وليس شكليًا**. الأدلة:
- `assertSame` ×1791، `assertEqualsWithDelta` ×46، `assertJsonPath` ×333 مقابل `assertOk` ×912 — أي أن الاختبارات تتحقق من القيم لا من الـHTTP فقط.
- بمسح آلي: **12 ملفًا فقط من 381** لا يحتوي أي تأكيد على قيمة، وأغلبها اختبارات RBAC حيث 403/200 *هو* التأكيد المطلوب.
- `tests/Feature/PayrollShortfall/AmountNeutralityTest.php:113-146` نموذج ممتاز: يثبت أن `round((2/60)×1.00,2)=0.03` وأن السطور تُوزَّع `0.01+0.02` بـ largest-remainder بدل تقريبين مستقلين يعطيان 0.04 — أي أنه يختبر **قرشًا واحدًا** في مسار مالي.

**ملاحظات ومخاطر:**

🔴 **الاختبارات كلها على SQLite بينما الإنتاج MySQL.** `phpunit.xml` يثبّت `DB_CONNECTION=sqlite / DB_DATABASE=:memory:`، و`.github/workflows/deploy.yaml` يُثبّت `pdo_sqlite`. هذا يُسقط طبقة كاملة من الأخطاء: strict mode، ENUM، ترتيب/مقارنة النصوص العربية (collation)، تجاوز التواريخ، دقة DECIMAL. والدليل من داخل الريبو نفسه: `app/Modules/Attendance/Migrations/2026_08_08_000006_create_attendance_calendar_tables.php:42-44` يعترف حرفيًا بأن «MySQL يعامل NULL في الفهارس الفريدة كقيم متمايزة» ويعالجها على مستوى التطبيق — وهذا بالضبط صنف الخطأ الذي يخفيه SQLite. أيضًا commit `1474e5d "fix(calendar): prevent month period date overflow"` من نفس العائلة.

🔴 **لا يوجد قياس تغطية على الإطلاق.** `phpunit.xml:16-45` يشرح بإسهاب لماذا لا يوجد `<coverage>` block (لا Xdebug ولا PCOV)، و`deploy.yaml` يضع `coverage: none`. النتيجة: رقم «381 ملف اختبار» **غير مسنود بأي نسبة تغطية**، وسؤال «ما المكشوف؟» لا إجابة قياسية له داخل المشروع. التعليق نفسه يشير إلى سؤال P0-CI مفتوح حول مدة السويت (24-32 دقيقة).

🟠 **المكشوف فعليًا** (بمسح المسارات في `tests/`):
| المسار | ملفات اختبار تذكره |
|---|---|
| `me/letters` / `letter-requests` (نظام الخطابات) | **0** |
| `payroll/formula-definitions` (محرّك المعادلات، 10 مسارات) | **1** |
| `payroll/bonuses` | 1 |
| `payroll/runs/{id}/export/wps` | 1 |
| `payroll/runs/{id}/export/gosi` | 1 |
| `messages/conversations` | 1 |
| `api/calendar` | 1 |

🟠 **تصدير WPS مختبَر ماليًا لا شكليًا.** `tests/Feature/Phase4/WpsExportTest.php:88-99` يتحقق أن إجمالي الملف = `total_net_salaries`، ويستبعد من لا حساب بنكي له — جيد. لكن **لا يوجد أي تأكيد على تخطيط الملف نفسه**: لا ترتيب أعمدة، لا عدد حقول، لا تحقق IBAN، لا ترميز. ملف WPS بإجمالي صحيح وتخطيط خاطئ يرفضه البنك. نفس الشيء لتصدير GOSI. هذان مخرجان **تنظيميان** يُسلَّمان لجهة خارجية ويعتمدان على ملف اختبار واحد لكل منهما.

🟡 **3 ملفات فقط في `tests/Unit`** أحدها `ExampleTest.php` الفارغ. عمليًا كل السويت اختبارات Feature تضرب قاعدة البيانات — وهذا سبب الـ24-32 دقيقة، ويجعل التغذية الراجعة أثناء التطوير بطيئة.

🟡 **الفرونت أرفع تكوينًا وأضعف تغطية:** `tsconfig.json` بـ `strict: true` + `noUnusedLocals` + `noUnusedParameters`، و**استخدام واحد فقط لـ `any` في 498 ملف** — مؤشر جودة ممتاز. لكن 54 ملف اختبار وحدة فقط (~11% من الملفات)، و`eslint.config.js` يُبقي `jsx-a11y` كتحذيرات (التعليق في `deploy.yml` يذكر **382 تحذيرًا**)، و`include: ["src"]` في tsconfig يعني أن `npm run typecheck` **لا يفحص مجلد `e2e/` إطلاقًا**.

🔴 **12 ملف E2E موجود لكنه ليس بوابة في CI** — `deploy.yml` يستثني `e2e` و`contract:check` صراحةً. والأسوأ: **`test-results/.last-run.json` مُلتزَم في الريبو ويسجّل `"status": "failed"` مع 11 اختبارًا فاشلًا**، مع 21 ملف لقطات وسياق فشل، منذ commit `fc89b60` بتاريخ 2026-08-05 ولم تُنظَّف حتى 2026-09-07. أي أن آخر حالة E2E مسجّلة للمنتج هي: فشل، غير مُعالَج، وغير حاجز.

---

## 2) الصلاحيات — بنية ممتازة، وتوزيع فيه ثغرة حقيقية

**ما نُفِّذ:** `app/Core/Authorization/PermissionRegistry.php` يجمع **137 صلاحية** في 19 مجموعة كمصدر وحيد للحقيقة، مع 17 ملف اختبار في `tests/Feature/Authorization/` منها `RouteCoverageTest`، `OrphanPermissionTest`، `PermissionNamingTest`، `ScopeRegressionTest`، `SensitiveFieldProtectionTest`. والفرونت يعكسها بـ 137 صلاحية أيضًا في `src/lib/authz/permissions.ts`.

**هل التنفيذ صحيح ومكتمل؟** البنية من أفضل ما رأيت في مشروع بهذا الحجم: بوابة آلية تمنع أي مسار مُعدِّل بلا `permission:` (`RouteCoverageTest.php:48-80`)، وأخرى تمنع الإشارة إلى صلاحية غير موجودة في الكتالوج (`:82-110`). لكن **توزيع الصلاحيات على الأدوار به عيب بنيوي**.

**ملاحظات ومخاطر:**

🔴 **دور `read_only` مُعرَّف بمطابقة نصية لا بقرار.**
```php
// database/seeders/DefaultRoleSeeder.php:300-311
->filter(fn (string $p) => str_contains($p, ':read') || str_ends_with($p, ':view'))
->reject(fn (string $p) => str_contains($p, ':salary:') || str_contains($p, ':pii:'))
```
التعليق فوقه (`:294-298`) يقول صراحةً إن الدور «يستثني رؤية الراتب وهوية PII — أقل امتياز». لكن الفلتر يمنح فعليًا:
- `payroll:read` → و`GET payroll/runs/{id}/entries` (`Payroll/Routes/api.php:54`) يعيد **صافي رواتب كل الموظفين**.
- `report:view` → و`GET/POST reports/payroll` (`Report/Routes/api.php:29,50`) يعيد `basic_salary`, `gross_earnings`, `net_salary`, `gosi_*` **لكل موظف في المنشأة**.
- `audit:read` → سجل التدقيق الكامل.
- `gosi:read`, `formulas:read`, `deductions:read`, `allowances:read`, `loans:read`, `leave_balances:read`, `settings:read`.

استثناء `:salary:` و`:pii:` يحمي حقول *مورد الموظف* فقط — ولا يمسّ وحدة الرواتب ولا التقارير. **النتيجة: دور «قراءة فقط» يرى ما مُنع منه صراحةً.** والأسوأ أن العيب **يتمدد تلقائيًا**: أي صلاحية جديدة تنتهي بـ`:read` تُمنح لهذا الدور في التشغيل التالي للـseeder بلا قرار من أحد. ولا يوجد أي اختبار يؤكد أن `read_only` ممنوع من `/api/reports/payroll` — `SensitiveFieldProtectionTest` يغطي مورد الموظف والداشبورد فقط.

🔴 **تقرير الرواتب بلا نطاق فرع، بالتصميم.**
```php
// app/Modules/Report/Services/ReportService.php:307-312
* Deliberately tenant-wide for report:view holders — the payroll report has
* NO EmployeeScopeService restriction on screen... Do not add scoping here.
```
و`ReportController.php:44` يثبّت `'payroll' => [... 'scoped' => false ...]`. هذا يعني أن `branch_manager` — الدور المُعرَّف صراحةً كـ«يدير فرعًا واحدًا» بـ`SCOPE_BRANCH` (`DefaultRoleSeeder.php:337`) والذي يحمل `report:view` (`:247`) — **يصدّر رواتب المنشأة كلها**. موثَّق كـ«قرار مالك»، لكن التوثيق لا يلغي كونه تجاوزًا لنموذج النطاق الذي بُني بعناية في بقية النظام.

🟠 **نطاق الفرع يفشل مفتوحًا (fail-open).**
```php
// app/Modules/Employee/Services/EmployeeScopeService.php:192-195
if ($level === self::SCOPE_BRANCH) {
    if (empty($branchIds)) { return null; }   // null = بلا قيد إطلاقًا
```
مستخدم بدور `branch_manager` أو `data_entry` أُنشئ بلا تعيين فرع يرى **المنشأة كاملة** بصمت. موثَّق كسلوك مقصود، لكنه الافتراض الخاطئ في اتجاه الخطر: إغفال خطوة إدارية يُوسّع الصلاحية بدل أن يُضيّقها.

🟠 **`syncPermissions()` في الـseeder يمسح تخصيصات المسؤول.** الـseeder يستخدم `syncPermissions` لكل دور (`:98, :102, :165, :197, :216, :232, :262, :285, :300`)، و`README.md:129` يوصي بـ`php artisan migrate --seed`. أي إعادة تشغيل للـseeder **تُعيد الأدوار لحالتها الافتراضية وتمسح كل ضبط أجراه المسؤول من شاشة الأدوار**. لاحظ التناقض: دالة `assignScopeLevels()` في نفس الملف (`:356-361`) حرصت على `whereNull('scope_level')` حتى لا تدهس قرار المسؤول — نفس الحرص غائب عن الصلاحيات نفسها.

🟠 **`RouteCoverageTest` يفحص المسارات المُعدِّلة فقط.** `MUTATING = ['POST','PUT','PATCH','DELETE']` (`:21`). **مسار GET يُسرّب رواتب أو PII بلا بوابة يمر دون أن تلاحظه أي بوابة آلية.** يُضاف إلى ذلك أن قائمة الاستثناء فيها أنماط فضفاضة: `'#/token$#'` و`'#/sync$#'` (`:36-37`) تُطابق **أي** مسار ينتهي بهذه الكلمة في أي وحدة، و`'#^api/notifications/[^/]+$#'` (`:40`) تُطابق `api/notifications/settings` أيضًا. (تحقّقت: هذا المسار مُبوَّب فعليًا في `Notification/Routes/api.php:25-27` — فالثغرة كامنة لا مُستغَلّة، لكنها تعني أن البوابة الآلية لن تُنبّه إن أُزيلت البوابة اليدوية.)

🟠 **لا يوجد اختبار تكافؤ بين كتالوجَي الباك والفرونت.** `src/lib/authz/__tests__/permissionCoverage.test.ts` يتحقق من الفرونت مقابل **سجل الفرونت نفسه**، والباك يتحقق من نفسه. 137 نصًا مكرَّرًا يدويًا في مستودعين، بلا أي فحص يربطهما. إعادة تسمية في الباك تمرّ خضراء في السويتين وتنكسر في المتصفح فقط.

🟡 **تسمية غير متسقة رغم التوثيق:** `report:*` و`notification:*` و`payslip:*` بالمفرد بينما مجموعاتها بالجمع (`reports`, `notifications`, `payroll`)؛ و`employees:profile:read_own` و`employees:salary:read` بثلاث مقاطع بينما البقية بمقطعين.

🟡 **تعليق ميت في قلب الملف:** `PermissionRegistry.php:12-15` ما زال يقول *"SCAFFOLD (Phase 1/T001): this class intentionally ships empty"* بينما الملف يحوي 137 صلاحية.

---

## 3) صحة الهجرات — 147 هجرة، انضباط عالٍ ومخاطر محدودة

**ما نُفِّذ:** 115 هجرة داخل `app/Modules/*/Migrations` + 32 في `database/migrations` = **147**. منها 56 هجرة جديدة منذ 2026-07-14.

**هل التنفيذ صحيح ومكتمل؟** نعم إلى حد بعيد. `->change()` مستخدم 19 مرة فقط وكلها **توسيع آمن** (`string(20)→string(64)`، `NOT NULL→nullable`) لا تضييق ولا تغيير نوع مدمّر. لا يوجد `truncate` ولا `DB::statement` خام. عملية الحذف الوحيدة (`2026_08_08_000014_drop_dead_legacy_permission_aliases.php:43-47`) مُعلَّلة بأن الأسماء لم يكن أي مسار يحترمها أصلًا.

**ملاحظات ومخاطر:**

🟠 **تعديل هجرات سبق دمجها.** `app/Modules/Approval/Migrations/2026_07_29_000002_add_admin_override_to_approvals.php` أُضيف في `dab4ac5` ثم **عُدِّل** في `b500c8d`، ومعه `database/migrations/2026_07_09_000004_add_missing_foreign_keys.php`. أي بيئة شغّلت النسخة الأولى لن تُشغّل الثانية أبدًا — تُصبح مخططات البيانات مختلفة بين البيئات بصمت.

🟠 **جدول `employees` يُعدَّل 39 مرة** عبر 16 هجرة منفصلة، منها دورة إضافة-ثم-حذف كاملة: `2026_06_25_000004_add_device_code_to_employees.php` يضيف `device_employee_code` و`2026_07_08_000013_drop_device_employee_code_from_employees.php` يحذفه. ومثلها `2026_07_08_000001_backfill_granular_action_permissions` ثم `2026_07_08_000002_remove_premature_granular_permissions` **في اليوم نفسه**. الأثر التشغيلي محدود لكنه مؤشر على تصميم يُبنى بالتجريب على مخطط حي.

🟡 **فهارس مكرَّرة على أثقل جدول.** `attendance_records` يحمل من الإنشاء `unique(['tenant_id','employee_id','date'])` و`index(['tenant_id','date'])` (`2026_04_15_000001_create_attendance_tables.php:68-70`)، ثم يضيف `2026_04_22_000004_add_performance_indexes.php:12-13` فهرسين آخرين: `(tenant_id, employee_id, date)` — **مطابق تمامًا للفهرس الفريد**، و`(tenant_id, date, status)` — الذي يجعل `(tenant_id, date)` القديم بادئة زائدة. ثلاثة فهارس زائدة على الجدول الذي يستقبل عشرات آلاف السجلات من مزامنة البصمة. تضخيم كتابة بلا مقابل.

🟡 **هجرات تستدعي نماذج التطبيق.** `2026_08_08_000016_hash_biometric_device_tokens.php` يستورد `App\Modules\Attendance\Models\BiometricDevice` ويستدعي `hashToken()`. الهجرة سليمة ومُعلَّلة ببراعة (idempotent بطول 48 مقابل 64)، لكن أي تغيير مستقبلي في `hashToken()` سيُغيّر سلوك هجرة تاريخية بأثر رجعي.

---

## 4) التصدير والتقارير — تحليل قياسي ممتاز، وتطبيق غير مكتمل

**ما نُفِّذ:** `config/exports.php` يعرّف 6 سقوف مُقاسة تجريبيًا (لا مخترعة): موظفون 20,000 (ذروة 308MB)، إخلاء طرف 30,000، حضور 20,000 (بعد قياس 40,000 = 482MB = 94% من الغلاف)، اعتمادات 20,000، جداول ضبط 20,000، صفحة اللوحة الأسبوعية 200. الـCSV يتدفق بلا سقف عبر `lazyById(500)`، والـXLSX محكوم بسقف يُرجع 422 عربيًا بأرقام هندية.

**هل التنفيذ صحيح ومكتمل؟ لا — التطبيق غير متسق.**

🟠 **مُصدِّران بلا سقف إطلاقًا.** بعد كل هذا التوثيق:
- `app/Modules/Leave/Controllers/LeaveExportController.php:58` — `Excel::download()` مباشرة، بلا `count()` ولا حد.
- `app/Modules/Attendance/Controllers/PunchCorrectionExportController.php:55` — نفس الشيء.

طلبات الإجازات لمنشأة كبيرة على مدى سنة تتجاوز 20,000 صف بسهولة. النتيجة: نفاد ذاكرة داخل zipstream — أي **استجابة مقطوعة بلا رؤوس CORS**، وهي بالضبط العَرَض الذي وصفه فريقهم في commit `61094ac` («المتصفح لا يستطيع التمييز بين موت الذاكرة ومضيف غير قابل للوصول»).

🟡 **السقف منسوخ لا مُركَّز.** خريطة تحويل الأرقام العربية `'0' => '٠'` مكرَّرة حرفيًا في **7 متحكمات**: `ApprovalExportController`, `AttendanceExportController`, `OvertimeController`, `TeamTransferController`, `EmployeeImportExportController`, `OffboardingExportController`, `ConfigExportController`. لو كانت في Trait أو خدمة واحدة لما نُسي المصدِّران أعلاه.

🟠 **كل التصدير متزامن داخل الطلب — لا يوجد أي تصدير في الخلفية.** المشروع كله يحوي **3 وظائف مُدرَجة في الطابور فقط** (`RegenerateFutureSchedules`, `RevokeUserTokens`, `SendEmailNotification`). ملف `.env` في الفرونت يوثّق النتيجة بصراحة: تصدير حضور 20,000 صف = **38.9 ثانية في المتصفح**، وتصدير CSV بـ50,000 صف = 30.5 ثانية، ولذلك رُفع `VITE_API_TIMEOUT_MS=60000`. أي أن المستخدم ينتظر 39 ثانية على اتصال مفتوح، ومهلة PHP/nginx على المسار تصبح نقطة فشل غير مُختبَرة.

**الصلاحيات والنطاق في التصدير:** ✅ **مُحترمة بشكل منهجي**. تحققت من كل متحكم يستدعي `Excel::download`/`CsvExporter`: جميعها تمرر `EmployeeScopeService::constraintFor()` أو `visibleEmployeeIds()`، والتعليقات تُثبت التطابق مع استعلام الشاشة («no request key can widen scope»). الاستثناء الوحيد هو تقرير الرواتب المذكور في §2.

🟡 **ازدواجية آليتَي النطاق.** `constraintFor()` (استعلام فرعي، لا يُحضِر المعرّفات للذاكرة — موثَّق في `EmployeeScopeService.php:169-181` بأنه استُحدث لأن السابق كان يسحب 10,000 معرّف) و`visibleEmployeeIds()` (يُحضِرها). المصدِّرون الأحدث يستخدمون الأول والأقدم الثاني — الترحيل غير مكتمل، وأداء `LeaveExportController:41` و`DocumentController:26` و`ContractController:31` ما زال على المسار القديم.

🟠 **تقرير الرواتب يُحمِّل المدة كلها في الذاكرة** لحساب الملخص في **كل طلب شاشة**: `ReportService.php:340` — `$all = (clone $base)->get();` ثم `sum()` على خمسة حقول. لا chunking، لا تجميع في قاعدة البيانات.

✅ **إصلاح RTL (`4cc9b86`) نموذجي:** شخّص السبب الجذري بدقة (DomPDF يُعلن `unicode-bidi` ولا يُنفّذه ولا يملك محرك تشكيل عربي)، نفى السبب الخاطئ صراحةً («الخط لم يكن المشكلة قط»)، حصر التغيير في مُصدِّر التقارير وترك DomPDF لبقية المستهلكين، وتحقّق «حرفًا بحرف مقابل المتصفح».

---

## 5) المخاطر التشغيلية — الأخطر في التقرير كله

**ما نُفِّذ:** `routes/console.php` يُسجّل **11 مهمة مجدولة**: `biometric:sync` (كل 30 دقيقة)، `attendance:mark-absent` (01:00)، `backup:database` (02:00)، `backup:clean` (03:00)، `backup:cleanup-old` (03:30)، `offboarding:run-readiness` (00:15)، `leave:accrue` (شهريًا)، `leave:carry-over` (سنويًا)، `approvals:escalate` (00:30)، `asset:transfer-timeouts` (00:45)، `team:execute-transfers` (01:15)، `attendance:generate-schedules` (01:45).

**ملاحظات ومخاطر:**

🔴 **خط النشر يرفع ملفات ولا شيء غير ذلك.** `.github/workflows/deploy.yaml:58-91`:
- **لا `php artisan migrate`** → 147 هجرة تعتمد على تدخل يدوي بعد كل نشر. كود يفترض عمودًا جديدًا يُنشَر قبل وجود العمود = 500 في الإنتاج.
- **لا `composer install`** و`**/vendor/**` مستثنى (`:88`) → أي تغيير في `composer.lock` (مثل إضافة **mPDF** في commit `4cc9b86`) لا يصل الخادم. `composer install` في السطر 46 داخل وظيفة **الاختبار** لا النشر.
- **لا `config:cache` / `optimize:clear`** → كاش قديم بعد النشر.
- **النشر عبر FTP** (`SamKirkland/FTP-Deploy-Action@v4.3.5` بلا تحديد `protocol`، أي **FTP غير مشفّر افتراضيًا**) مع `dangerous-clean-slate: false` و`local-dir: ./` و`server-dir: /` → رفع ملف-بملف، **بلا تبديل ذرّي وبلا وضع صيانة**: التطبيق يخدم كودًا نصف محدَّث أثناء النشر.
- قائمة `exclude` تستثني `**/.env` و`**/.env.example` — لكن **`.env.uat` غير مستثنى**، وهو مُلتزَم في الريبو ويحوي `APP_KEY=base64:EBJb...` و`APP_DEBUG=true`. كذلك `database/backfill/**` غير مستثنى.

🔴 **بوابة التكافؤ لا يمكن استيفاؤها في الإنتاج — قفل تصميمي.** `PayrollBehaviorFlagService.php:200-226` يشترط لتفعيل أي علم سلوك مالي وجود **تقرير تكافؤ لنفس المستأجر في قاعدة البيانات**. لكن المسار الوحيد لإنشاء تقرير تكافؤ هو `POST payroll/parity-reports` (`Payroll/Routes/api.php:181`) ← `replayAndCompare()` ← `assertReplayAllowed()` الذي **يرفض في الإنتاج صراحةً** (`PayrollParityService.php:101-111`). لا يوجد مسار استيراد/ترقية لتقرير مُولَّد على UAT. **النتيجة: منظومة `PL` كاملة (فصل نقص الساعات، الساعة المرنة، التناسب) غير قابلة للتفعيل في الإنتاج كما هي مكتوبة** — أو أنها تُفعَّل بإدخال صف يدويًا في قاعدة البيانات، وهو أسوأ لأنه يُفرِغ البوابة من معناها بلا أثر تدقيق.

🔴 **لا `onOneServer()` على أي مهمة مجدولة.** إن شُغّل الجدولة على أكثر من عقدة، تُنفَّذ `leave:accrue` مرتين (**استحقاق إجازات مضاعف**) و`attendance:mark-absent` مرتين و`team:execute-transfers` مرتين. النشر الحالي عبر FTP يوحي بخادم واحد، لكن هذا افتراض غير مكتوب في أي مكان.

🟠 **`withoutOverlapping()` على مهمة واحدة فقط** — `biometric:sync` (`console.php:26`)، بتعليل ممتاز. أما `attendance:generate-schedules` (تُولّد تقاويم كل الأنماط النشطة لكل مستأجر حتى الأفق) و`backup:database` و`attendance:mark-absent` فبلا حماية تداخل. و`MarkAbsentCommand.php:29-33` و`GenerateSchedulesCommand.php:35-38,74,121` تستخدم `->get()` على كل المستأجرين/الأنماط بلا تقطيع — بعكس `LeaveAccrueCommand.php:45` الذي يستخدم `chunkById(200)` بشكل صحيح. عدم اتساق داخل نفس الطبقة.

🟠 **لا دليل تشغيل إنتاجي.** `README.md:118-145` يوثّق التطوير المحلي فقط. لا ذكر لـ`schedule:run` في cron، لا supervisor لـ`queue:work`، لا إجراء نشر. و`.env.example:50` يضع `QUEUE_CONNECTION=database` — فإن لم يُشغَّل عامل طابور، فإن `RegenerateFutureSchedules` (يُستدعى من `LeaveServiceProvider.php:40` عند تغيير الإجازات الرسمية) و`SendEmailNotification` **تتراكم في الجدول بصمت ولا تُنفَّذ أبدًا**.

🟠 **إصلاح `6764a6d` صحيح لكنه يترك عطلًا صامتًا.** المعالجة (`config/backup.php`: `?:` بدل `env()` default) صحيحة ومُعلَّلة بدقة، والاختبار المرافق `BackupNotificationConfigTest.php` يعيد إنتاج سيناريو CI على مستوى القيمة بذكاء. **لكن النتيجة التشغيلية**: إن لم يضبط أحد `BACKUP_NOTIFICATION_EMAIL`، تذهب إشعارات **فشل النسخ الاحتياطي** إلى `your@example.com`. الاختبار يؤكد أن العنوان *صالح*، لا أنه *مُستقبَل*. فشل نسخة احتياطية لن يعلم به أحد.

🔴 **بيانات إنتاج ملتزَمة في المستودع.** `database/backfill/zk-2026-08-31/` يحوي **4.4 ميجابايت / 83,622 بصمة حضور حقيقية** لـ676 موظفًا (`punches-el-nasr-1.csv`, `punches-el-nasr-2.csv`)، و`README.md` يذكر **عناوين IP عامة للأجهزة** (`197.44.145.194/195`, `197.44.147.107/108/109`) وعناوينها الداخلية. `device_user_id` يُطابق `employees.biometric_id` مباشرة — أي أن البيانات **قابلة لإعادة التعريف**. وهي مستثناة من لا شيء في قائمة النشر، فتُرفع إلى جذر الخادم أيضًا.

🔴 **بيانات اعتماد حقيقية في تاريخ Git.** commit `746a602` يوثّق حرفيًا: *"A real staging credential used to be committed here in plaintext, against the live CONTRACT_API_URL"* — `admin@techco.hrapp.sa / password123` مقابل `https://hr-backend.logic-leap.ai/api`. الحجب (2026-09-07) **تجميلي**: القيمة باقية في التاريخ لكل من استنسخ المستودع. لا يوجد في التقرير أي إشارة إلى **تدوير كلمة المرور**، وهي الخطوة الوحيدة التي تُصلح هذا فعليًا.

🟠 **بيانات حضور فاسدة كُتبت في الإنتاج، ولا يوجد إجراء تنظيف تلقائي.** commit `71548a3` يوثّق قياسًا بديعًا: فقد 2.54% من حزم UDP أنتج **859 «مستخدمًا متمايزًا» بدل 334** وتاريخًا مُفكَّكًا `2029-11-14`، وكُتبت هذه الصفوف بـ`updateOrCreate` كبيانات موثوقة — **والحضور يُغذّي الرواتب**. الإصلاح (TCP + تأكيد اكتمال صارم) ممتاز، لكن **لا يوجد أمر لتحديد وتصحيح الصفوف الفاسدة السابقة**؛ `README` يصف إعادة سحب يدوية بـ`--full` كإجراء تشغيلي اختياري. لا اختبار ولا تقرير يُثبت أن قاعدة الإنتاج نُظّفت.

---

## 6) آخر commit — ماذا عالج بالضبط

**الباك `9418303` + الفرونت `746a602` (2026-09-07):**

يُصدر **21 إصلاحًا مُتحقَّقًا من تدقيق نظام سابق (spec 017)** بمعرّفات صريحة: `AUD-007, 009, 013, 014, 020, 021, 022, 023, 037, 039, 043, 044, 047, 050, 055-061`. الحجم: 83 ملفًا في الفرونت (+3,923/-738)، و~90 ملفًا في الباك.

**المحتوى الفعلي:**
- **تصدير من الخادم لكل شاشات القوائم** بدل «الكشط» من الصفحة المعروضة — 15 فئة تصدير جديدة (`ApprovalInboxExport`, `OvertimeExport`, `ExitPermitsExport`, `TeamTransfersExport`, `WeeklyBoardExport`, `UsersExport`, `DocumentsExport`, `ContractsExport`...) مع 10 فئات `Request` للتحقق من المدخلات.
- **حدود الترقيم وعقد الاستجابة** لتصحيحات البصمة ولوحة إخلاء الطرف.
- **حدود تسلسل PII والحقول الحساسة** + تلميحات حجب PII في الفرونت.
- **نطاق الموظف في وحدة التدريب** (كان مفقودًا).
- **تكافؤ القائمة/التصدير** — التصدير يُرجع بالضبط ما تُرجعه الشاشة.
- **حذف مخطط تعديل موظف ميت** (`editEmployeeApiSchema.ts`، 281 سطرًا) **واختبار تكافؤ خادع** يختبره — استُبدل باختبار عقد على مسار الإرسال الحقيقي. هذا **الاعتراف الأهم في الـcommit**: كان هناك اختبار يمر بنجاح ويختبر كودًا ميتًا.
- **حجب بيانات الاعتماد** من ملفات env.

**هل يشير إلى تدقيق سابق؟** نعم، صراحةً: `spec 017` بمعرّفات `AUD-*` مرقّمة حتى 061. الفجوات في التسلسل (008, 010-012, 015-019, 024-036, 038, 040-042, 045-046, 048-049, 051-054) تعني أن **الـcommit يُصدر 21 من أصل 61+ بندًا** — أي أن **معظم التدقيق لم يُعالَج بعد**. البند `AUD-040` (بيانات الاعتماد) عولج في الفرونت لا الباك، و`AUD-039` احتاج متابعة في commit تالٍ.

🟡 السطر «Payroll module and Layer 0 files are untouched» يؤكد أن وحدة الرواتب معزولة عن هذه الموجة — انضباط جيد، لكنه يعني أيضًا أن التدقيق لم يشمل الرواتب.

🟠 **مواصفة التدقيق نفسها ليست في المستودع.** `spec 017` و`docs/code-review-frontend-todo-2026-08-02.txt` و`docs/zk-sync-fix-report-2026-08-31-ar.html` مُشار إليها في التعليقات، لكن `/docs/` **مستبعد من git** ومن النشر. لا يمكن لأي مراجع (ولا للفريق بعد دوران الأفراد) معرفة ما هي البنود الـ40 المتبقية.

---

## 7) الدين التقني

| البند | التقدير | الدليل |
|---|---|---|
| **11 مكوّن قائمة تصدير شبه متطابق** (782 سطرًا) | 🟡 | `ConfigExportMenu`, `PageExportMenu`, `ServerExportMenu`, `ApprovalsExportMenu`, `AttendanceExportMenu`, `AttendanceQueueExportMenu`, `PunchCorrectionsExportMenu`, `EmployeesExportMenu`, `LeaveBalanceExportMenu`, `LeavesExportMenu`, `PayslipsExportMenu` |
| **خريطة الأرقام العربية مكرَّرة ×7** | 🟡 | §4 أعلاه |
| **ملف بـ2,232 سطرًا** | 🟠 | `hr-dashboard/src/features/employees/pages/add/AddEmployeeFormContent.tsx`؛ يليه `SettingsHomePageContent.tsx` 1,396 و`routes.tsx` 1,003 |
| **خدمات ضخمة** | 🟠 | `PayrollParityService.php` 1,519 · `ApprovalEngine.php` 1,151 · `PayrollCalculationService.php` 1,126 · `AttendanceCalculationService.php` 958 |
| **منطق في المتحكمات** | 🟡 | 47 من 126 متحكمًا يستعلم النماذج/`DB::` مباشرة. الأحجام معقولة (أكبرها `ProfileUpdateRequestController.php` 353 سطرًا) فالمشكلة انتشار لا تضخّم |
| **TODO/FIXME** | ✅ | **صفر** في الباك والفرونت — انضباط ممتاز |
| **آثار بناء ملتزَمة** | 🟡 | `dist/` (3 ملفات) + `test-results/` (21 ملفًا، منها لقطات فشل) داخل git |
| **آليتان لنطاق المستأجر** | 🟡 | `BaseModel` (يضم `BelongsToTenant`) مقابل استخدام الـTrait مباشرة. بمسح شامل: نموذج واحد فقط يحمل `tenant_id` بلا نطاق — `Subscription.php` (منصّي، على الأرجح مقصود) |
| **`ini_set` لرفع حد الذاكرة داخل الخدمة** | 🟡 | `BiometricSyncService` يرفع الحد إلى 256MB — براغماتي لكنه يُخفي مشكلة سعة |

---

## أخطاء منطقية محددة وجدتها

🟠 **`removeLine` يحذف سطرًا لا يتحقق من ملكيته.**
```php
// app/Modules/Payroll/Actions/AdjustPayrollEntryAction.php:33-47
public function removeLine(PayrollEntry $entry, PayrollEntryLine $line): PayrollEntry {
    ...
    if (!$line->is_manual) { abort(422, ...); }
    $line->delete();
    $this->recalculateEntry($entry);
```
لا يوجد `$line->payroll_entry_id === $entry->id`. والمسار `DELETE payroll/runs/{payroll_run}/entries/{payroll_entry}/lines/{line}` (`Payroll/Routes/api.php:57`) **لا يستخدم `scopeBindings()`** — تحققت: `scopeBindings` غير موجود في المشروع كله. حامل `payroll:process` يستطيع تمرير معرّف سطر يخص **مسير موظف آخر** فيُحذف، ثم يُعاد حساب الإدخال الخطأ. نفس الملاحظة تنطبق على `show()` و`adjust()` في `PayrollEntryController.php:43-51` التي لا تتحقق أن `$payrollEntry` ينتمي إلى `$payrollRun`.

🟠 **إعادة تشغيل محرك الرواتب على مسير معتمَد.** `PayrollParityService.php:181-196` يفتح معاملة، **يُحوّل حالة مسير حقيقي إلى `draft`**، يُعيد حساب الحضور والرواتب فوقه، ثم يعتمد على `finally { DB::rollBack(); }`. مُبرَّر بعناية (`:141-157`) ومحظور في الإنتاج، لكنه على UAT يُبقي أقفال صفوف على `payroll_entries` و`attendance_records` لمستأجر كامل طوال المعاملة. أي عملية رواتب متزامنة تُحجَب أو تدخل في deadlock.

🟠 **الرواتب تُحسب متزامنًا داخل معاملة واحدة طويلة.** `CalculatePayrollAction.php:22` يلفّ الحساب كله في `DB::transaction`، ويُستدعى من `POST payroll/runs/{id}/calculate` مباشرة. لمنشأة بآلاف الموظفين: معاملة واحدة طويلة + طلب HTTP متزامن + `->get()` بلا تقطيع في `PayrollCalculationService.php:291,444,523,578,824,855`.

---

## الخلاصة بترتيب الأولوية

**يجب حسمه قبل أي مسير رواتب إنتاجي:**
1. 🔴 قفل بوابة التكافؤ — لا يمكن تفعيل أي علم سلوك في الإنتاج (§5).
2. 🔴 `read_only` و`branch_manager` يريان رواتب المنشأة كاملة عبر `report:view` و`payroll:read` (§2).
3. 🔴 خط النشر بلا `migrate` ولا `composer install` ولا مسح كاش (§5).
4. 🔴 اختبارات على SQLite بينما الإنتاج MySQL، بلا أي قياس تغطية (§1).
5. 🔴 بيانات اعتماد حقيقية في تاريخ Git بلا تدوير مُوثَّق + 83,622 بصمة حقيقية داخل المستودع (§5).

**مهم:**
6. 🟠 مصدِّرا الإجازات وتصحيحات البصمة بلا سقف XLSX (§4).
7. 🟠 `removeLine` بلا تحقق ملكية + غياب `scopeBindings` على المسارات المتداخلة.
8. 🟠 لا `onOneServer()` — خطر مضاعفة استحقاق الإجازات وتعليم الغياب.
9. 🟠 نطاق الفرع يفشل مفتوحًا عند غياب تعيين الفرع.
10. 🟠 `syncPermissions` في الـseeder يدهس تخصيصات المسؤول، و`README` يوصي بتشغيله.
11. 🟠 11 اختبار E2E فاشل مُلتزَم منذ 2026-08-05، والسويت ليست بوابة.
12. 🟠 40 من 61+ بند تدقيق (`AUD-*`) لم تُعالَج، والمواصفة خارج المستودع.

**ما يستحق الثناء بصدق:** جودة تحليل commits البصمة والـRTL والتقريب المالي من الطراز الأول — قياسات حقيقية، أسباب جذرية، ونفي صريح للأسباب الخاطئة. `tests/Feature/Hardening` (32 ملفًا) يغطي عزل المستأجرين وتفرّد الأرقام الوظيفية وتجميد الساعة وقفل الحسابات — تفكير أمني ناضج. حذف اختبار خادع بدل الاحتفاظ به أخضر (`746a602`) مؤشر نزاهة هندسية نادر. والفرونت بـ`strict: true` و`any` واحد في 498 ملفًا نظيف بمعايير أي فريق.

---

# القسم ٥ — المقارنة بين النظامين

# مقارنة النظامين — حالة سبتمبر 2026

## 0) الخلاصة الحاكمة قبل أي جدول

**المتغيّر الأهم ليس تقنيًا: نظامنا متوقّف.** آخر commit عندنا `d0226b7` بتاريخ **2026-07-14**، و`git log --since=2026-07-14` في `D:/projects/hr_system` يُرجع **صفر**. في نفس النافذة نفّذوا **47 commit في الباك و35 في الفرونت**، منها 8 إصدارات إنتاج موثّقة (`8365979 release: D1-D4 production deployment 2026-08-25`, `20da1b8 release(offboarding-export): D5.3`).

النتيجة المباشرة: المقارنة القديمة («نحن أعمق في العمليات، هم أوسع») **لم تعد صحيحة**. صاروا أعمق منّا في العمليات أيضًا — في الحضور والجدولة والرواتب تحديدًا — واحتفظوا بالاتّساع.

فرق البنية التحتية الذي يفسّر السرعة:

| | نحن | هم |
|---|---|---|
| اختبارات باك | **0** (لا jest، لا `test` script في `api/package.json`) | **381 ملف** تحت `tests/` |
| اختبارات فرونت | **0** | **66 vitest** + Playwright e2e (`hr-dashboard/e2e/`) |
| CI | لا يوجد `.github/workflows` | `deploy.yaml` + husky + lint-staged + `contract:check` |
| أوامر تشغيلية | 4 `@Cron` داخل الخدمات | 15 أمر `app/Console/Commands/` (نسخ احتياطي، تسوية، تحقّق تكافؤ…) |
| تدقيق (Audit) | **لا شيء** — `grep -rn "audit" api/src` = صفر | `app/Core/Middleware/LogAuditTrail.php` + جدول `audit_logs` |

---

## 1) جدول تغطية الموديولات

| المجال | الأنضج | الدليل الحاسم |
|---|---|---|
| **الموظفون** | 🔴 **هم** | عندهم جدول `employee_contracts` مستقل (تاريخ عقود متعدّد) — عندنا العقد مسطّح داخل صف الموظف `api/src/employees/employee.entity.ts:165-179` (عقد واحد فقط، بلا تاريخ تجديد). عندهم حماية حقول: `app/Core/Authorization/SensitiveFields.php:17-39` تحجب الراتب/الآيبان/الهوية قراءةً وكتابةً — عندنا `employees.view` يُرجع الكيان كاملًا بلا أي تصفية (`api/src/employees/employees.controller.ts:28-43`). |
| **الحضور** | 🔴 **هم** | فصل النقص عن الانصراف المبكر + `calculation_profile` لكل صف (`Attendance/Migrations/2026_08_23_000001…`)؛ عميل ZKTeco مكتوب يدويًا بـTCP وتحقّق اكتمال (`Services/ZKTecoClient.php`, 688 سطر) بعد أن أثبتوا أن UDP كان يكتب أيامًا فاسدة (`71548a3`). عندنا `node-zklib` بـ181 سطر بلا نافذة تزايدية ولا حارس معقولية (`api/src/attendance/device-sync.service.ts:66-115`). |
| **الورديات والجدولة** | 🔴 **هم بفارق كبير** | `weekly_schedule_versions` بحالة draft→published→archived وبوابة نشر ولا مسار تعديل بعد النشر (`Migrations/2026_08_08_000008`)؛ + `working_day_patterns` مُنطَّقة ومؤرّخة + `attendance_calendar_versions`. عندنا `weekly_schedule_entries` صفّ قابل للتعديل بلا إصدار ولا نشر ولا مُصدِر (`api/src/attendance/attendance.entities.ts:37-64`). |
| **الإجازات** | 🟠 **هم (بفارق ضيّق)** | دفتر دفعات متعدّد `leave_opening_balances` + تسجيل استهلاك كل طلب من كل دفعة لاسترجاع دقيق عند الإلغاء (`Leave/Migrations/2026_07_12_000002`). عندنا طبقة افتتاحية واحدة (`openingDays/openingTaken/openingExpiry`, `leave.entities.ts:105-121`) والترحيل يدوي لا مجدول. |
| **الطلبات والاعتمادات** | 🟢 **نحن (بتحفّظ)** | 63 نوع طلب / 9 فئات / 17 دورة (`api/src/seed/requests-seed.data.ts`) مقابل 18 مفتاحًا محكومًا عندهم (`database/seeders/SystemRequestTypeSeeder.php:18-40`). لكن **محرّك** الاعتماد عندهم أقوى: سياسات `any/all/majority/quorum/sequential` (`Approval/Migrations/2026_08_02_000001:22-25`) — عندنا `isParallel` فقط، وهو حقل **ميت**: مُخزَّن في `settings.controller.ts:510` وغير مقروء في `requests.service.ts` إطلاقًا. |
| **العهدة** | 🟡 **تعادل** | التحقوا بنا: `asset_transfers` + `asset_movements` سجل غير قابل للتعديل + `AssetClearanceWaiver` + `AssetTransferTimeoutsCommand`. عندنا نقل بتأكيد مزدوج (`destinations.service.ts:597-633`) وحارس ازدواج خصم (`offboarding.service.ts:340-365`). |
| **إنهاء الخدمة** | 🟡 **تعادل — كلٌ في اتجاه** | مدخلات تصفيتنا أوسع (بدل رصيد بالاستحقاق التناسبي + أوفرتايم غير مصروف + عهدة مفقودة بلا ازدواج، `offboarding.service.ts:218-395`). لكن **مكافأتنا غير قانونية**: معامل ثابت `eos.months_per_year=0.5` لكل سنة (`offboarding.service.ts:238-243`) — عندهم شرائح نظام العمل السعودي (نصف شهر أول 5 سنوات ثم شهر كامل) في `Employee/Services/SettlementService.php:216-232`. ولا أحد منّا يطبّق تخفيض الاستقالة (⅓ و⅔). |
| **الرواتب** | 🔴 **هم بفارق حاسم** | تفاصيلها في §3. |
| **التوظيف** | 🔴 **هم** | `job_postings/applicants/interviews/job_offers` + Policies + Events. عندنا جدول `candidates` واحد و4 شاشات موك (`src/app/recruitment/{add,interviews,offers,[id]}/page.tsx` — صفر استيراد من `@/lib/api`). |
| **الأداء** | 🔴 **هم** | `evaluation_cycles/evaluation_templates/template_competencies/performance_reviews/review_competency_ratings/goals` موصولة فعليًا (`hr-dashboard/src/services/performance.service.ts`). عندنا **6 شاشات موك بالكامل**. |
| **التدريب** | 🔴 **هم** | `training_courses/enrollments/certificates` + تصدير. عندنا 5 شاشات موك، و3 أنواع طلب تدريب تشير إلى `training_register` **غير موجود** في خريطة الوجهات. |
| **التقارير** | 🔴 **هم** | 6 تقارير + **بانِي تقارير مخصّصة** محفوظة (`saved_reports`) + تصدير CSV/XLSX/PDF متدفّق ومُدقَّق (`0ee91b9`) + widgets لوحة قابلة للضبط. عندنا 6 تجميعات JSON فقط (`api/src/reports/reports.controller.ts`) و**صفر تصدير**. |
| **الإشعارات** | 🔴 **هم** | جدول `notifications` + `user_notification_preferences` + `SendEmailNotification` job + `SmsNotificationService`. عندنا إشعارات **مشتقّة وقت القراءة** فقط بلا تخزين ولا حالة «مقروء» ولا قناة (`api/src/assets/portal.controller.ts:72-130`). |
| **الصلاحيات** | 🔴 **هم** | 138 صلاحية بصيغة `resource:action` + اختبارات تغطية مسارات + حجب حقول + 2FA + استعادة كلمة مرور + LDAP (`Auth/Controllers/`). عندنا 39 صلاحية (`api/src/auth/permissions.ts`) و`/auth/login` + `/auth/me` فقط (`auth.controller.ts:25,31`). ميزتنا الوحيدة الباقية: دلالة **REVOKE** في `user_permission_overrides` (Spatie عندهم منح فقط). |
| **تعدّد المستأجرين** | 🔴 **هم — ولا نملك شيئًا** | `tenants/plans/subscriptions` + `TenantResolver` + `BelongsToTenant` + `CheckFeatureFlag` + `app/Platform/` لوحة مالك المنصّة. عندنا `grep -rn "tenant" api/src` = **صفر**. |

**النتيجة: 9 مجالات لهم بوضوح · 3 تعادل/ضيّق · 1 لنا.**

---

## 2) ما زلنا متقدّمين فيه فعلًا (بدليل من الطرفين)

1. **كتالوج الطلبات ونمط «الوجهة»** — 63 نوعًا مقابل 18، ونمط `destinationHandler` يكتب في سجل دائم بعد الاعتماد (`api/src/requests/destinations.service.ts:720-754`). **تحفّظ مهم:** 23 من 63 نوعًا تشير إلى handler غير مُنفَّذ (`training_register`, `er_case`, `contracts_register`, `document_vault`, `it_assets`, `meetings_register`, `shift_schedule`…) فتُعتمد الطلبات بلا أن تكتب شيئًا — أي أن التفوّق العددي أقرب إلى **40 نوعًا موصولًا فعلًا**.
2. **مسير قابل للتعريف بالنطاق + لقطة أعضاء مجمَّدة** — `scopeType: COMPANY|BRANCH|DEPARTMENT|TEAM|COST_CENTER|CUSTOM` و`payroll_run_members` (`api/src/payroll/payroll.entities.ts:24-74`). عندهم مسير واحد للمستأجر كله شهريًا، بلا نطاق إطلاقًا (`Payroll/Services/PayrollRunService.php:40-50`).
3. **أنواع الإذن بنافذة تغطية** — `coverage: 'morning'|'evening'|'both'` (`attendance.entities.ts:97-99`) تميّز إذن التأخير عن إذن الانصراف المبكر بدقّة أعلى من `kind: exit_return|late_arrival` عندهم.
4. **الدقائق «بخصم» على مستوى الدقيقة بنسبة** — `deductibleMinutes` + `deductionPct` مع قصّ زمني للتداخل (`attendance.service.ts:1041-1076`, `payroll.service.ts:264`) — تفصيل أدقّ من `deduction_rate` المسطّح عندهم.
5. **سلاسل اعتماد بنسخة لكل فرع** — `@Index(['code','branchId'], {unique:true})` (`approval-chain.entity.ts:6-8`) + 11 دورًا يُحلّ ديناميكيًا من الهيكل (`approval-step.entity.ts:12-23`)، مقابل مسار بفرع اختياري عندهم.
6. **حارس ازدواج خصم العهدة بين التصفية ودفتر المديونيات** (`offboarding.service.ts:340-352`) — لا مقابل له عندهم.

---

## 3) ما صاروا متقدّمين فيه علينا

### 🔴 حرج

**أ) الرواتب — الفجوة الأخطر**

| القدرة | هم | نحن |
|---|---|---|
| التناسب (proration) | `calculateProRating()` خلف علم `prorata_semantics` + 8 اختبارات (`PayrollCalculationService.php:453-511`) | **لا يوجد**. `resolveScope` يصفّي `{ isActive: true }` فقط (`payroll.service.ts:110`) → موظف عُيّن يوم 20 يأخذ **شهرًا كاملًا**، ومنتهي الخدمة يوم 5 **يسقط من المسير كليًا** فيضيع أجر 5 أيام. |
| التأمينات (GOSI) | `GosiCalculationService.php` (سقف/أرضية/سعودي vs غير سعودي/أساس قابل للضبط) | **لا يوجد** — و`src/app/payroll/gosi/page.tsx` شاشة موك (صفر استيراد API). |
| بدلات/خصومات ديناميكية | `allowance_types` + `employee_allowances` بتواريخ سريان + `deduction_types` + `scoped_compensations` مُنطَّقة | **3 أعمدة ثابتة** فقط: `housingAllowance/transportAllowance/otherAllowance` (`employee.entity.ts:203-220`) |
| محرّك المعادلات | `FormulaEvaluator` (399 سطر) + `payroll_formula_versions` + `FormulaOverlayService` **موصول بالمحرّك** (`PayrollCalculationService.php:355-411`) | `safe-formula.ts` (234 سطر) و`payroll-variables.ts` (39 سطر) — **`grep` لا يجد لهما مستوردًا واحدًا: كود ميت** |
| القابلية للتفسير | `payroll_entry_lines.evaluation_state` يسجّل سبب فشل المعادلة بدل إسقاط المبلغ صامتًا (`1ed9d2c`) | لا بنود تفصيلية أصلًا — `PayrollItem` أعمدة إجمالية + `breakdown` JSON |
| الحوكمة | أعلام سلوك محكومة + خطوط أساس مجمّدة بـ`content_hash` + تقرير تكافؤ يمنع التفعيل عند فرق غير مفسَّر + فصل أدوار (الطالب ≠ المعتمِد) — `PayrollBehaviorFlagService.php`, `PayrollParityService.php` (1519 سطر) | لا شيء |

**ب) خصمان معطوبان عندنا في الحضور:**
- **النقص المرن يُعامَل كتأخير**: في الوردية المرنة نكتب العجز في `lateMinutes` (`attendance.service.ts:1097`)، والمسير يمرّره على **شرائح التأخير** (`payroll.service.ts:274`) — فيتحوّل نقص 30 دقيقة إلى «ربع يوم» بدل خصم بالدقيقة. هم كانوا يخلطونه مع `early_departure_minutes` (أخفّ ضررًا) و**فصلوه في `8aaa9ff`**.
- **الانصراف المبكر لا يُخصم أصلًا**: `earlyLeaveMinutes` يُحسب ويُخزَّن (`attendance.service.ts:1128`) ولا يظهر في أي معادلة داخل `payroll.service.ts`.

**ج) لا اختبارات ولا تدقيق ولا CI.** كل تغيير في محرّك رواتب أو حضور عندنا هو مراهنة. مقابل ذلك: 1447 اختبارًا أخضر مذكورة في رسالة `1ed9d2c`، ومجموعات مخصّصة لكل تغيير سلوكي (`tests/Feature/PayrollShortfall/` بها `AmountNeutralityTest`, `LegacyInvariantTest`, `MigrationRollbackTest`, `ReplayProfileTest`).

**د) لا تعدّد مستأجرين.** لا يمكن بيع النظام كـSaaS بدون إعادة كتابة كل استعلام. هم بنوها من اليوم الأول (`tenant_id` في كل migration).

### 🟠 مهم

5. **التصدير — صفر عندنا.** 30 صنف تصدير عندهم عبر CSV/XLSX/PDF متدفّق مع سقف صفوف وتدقيق. عندنا زر «تصدير» في `src/app/employees/page.tsx:198-201` **بلا `onClick`** — وكذلك «استيراد» (`:194-197`). هذه أول ملاحظة سيقولها أي عميل.
6. **أنماط أيام العمل والتقويم المُصدَر.** `working_day_patterns` مُنطَّقة (شركة/فرع/قسم/فريق/موظف) ومؤرّخة بحالة draft/active/archived + قواعد ترتيبية شهرية (`Migrations/2026_08_08_000001`). عندنا `ScheduleExceptionRule` مسطّح: `isActive` + `branchId` فقط، بلا نطاق ولا تاريخ سريان ولا مسوّدة (`attendance.entities.ts:212-238`).
7. **إصدارات الجدول الأسبوعي وبوابة النشر.** عندنا تعليق صريح: «تعديل الوردية يسري فورًا» (`attendance.entities.ts:50-51`) — أي أن تعديل وردية اليوم **يعيد كتابة أساس حساب أيام ماضية**. عندهم لقطة `scheduled_start/scheduled_end` لكل يوم منشور + ربط `schedule_day_id` على صف الحضور.
8. **تصحيح البصمة.** عندهم تصنيف 8 أنواع + لقطة الأصل مقابل المطلوب + مرفق + `recalc_result` + `applied_at` (`Migrations/2026_08_03_000005:20-45`). عندنا `reason` + JSON فقط (`attendance.entities.ts:53-74`)، ولا لقطة للقيمة الأصلية، والـhandler لا يستدعي إعادة حساب اليوم (`destinations.service.ts:213-230`) رغم أن تعليقه يقول «يُطبَّق فعليًا».
9. **Offboarding.** `offboarding_events` سجل زمني غير قابل للتعديل + `OffboardingReadinessService` + مرحلة `pending_finalization` + `ClearanceDashboardService` (341 سطر) + تصدير. عندنا الحالات فقط بلا سجل أحداث.
10. **قوالب المستندات وتوليد PDF.** `document_templates` + `document_template_versions` + `DocumentPdfRenderer` + كل مستند مولَّد مثبَّت على (قالب + إصدار). عندنا `letterHandler` يكتب **سلسلة نصية** `letters/CODE/id.pdf` ولا يولّد ملفًا (`destinations.service.ts:497`)، و`settings/document-templates` شاشة موك.
11. **الإشعارات المخزَّنة + بريد/SMS + تفضيلات** — راجع الجدول.
12. **الأمان التشغيلي**: 2FA، استعادة كلمة مرور، LDAP، نسخ احتياطي مجدول (`BackupDatabaseCommand`, `CleanupBackupsCommand`). لا شيء من ذلك عندنا.

### 🟡 تحسين

13. مسير تكميلي (`type: supplementary`) و`PayrollCycleService` يحلّ فترة الدورة وتاريخ الصرف من الإعدادات — عندنا حدود الشهر ثابتة.
14. سياسات النِصاب في الاعتماد (`quorum/majority`).
15. أوامر CLI للتشغيل (تسوية الإجازات، توليد الجداول، تنفيذ النقل، التحقّق الظلّي).

---

## 4) ما التقوا فيه (قدرات كانت حصرية لنا وصاروا ينفّذونها)

هذه القائمة تستحق الانتباه: **الشكل مطابق حرفيًا لمواصفتنا**، وهو دليل مباشر على أنهم ينفّذون من مستند سُلِّم لهم.

| القدرة | نحن | هم (التاريخ) |
|---|---|---|
| دفتر المديونيات | `employee_obligations` مع `sourceRef: 'asset:37'` (`financial.entities.ts:59-108`) | جدول مطابق **بنفس التعليق** `// e.g. asset:{id}` (`Payroll/Migrations/2026_08_02_000003:29`) — 2026-08-02 |
| فترات الأوفرتايم | `overtime_periods` بـ`effect: OPEN|CLOSED` + `branchId` (`attendance.entities.ts:241-263`) | مطابق حقلًا بحقل (`2026_08_02_000005:39-52`) — 2026-08-02 |
| شرائح خصم التأخير | `lateness_tiers` (`payroll-rules.entities.ts`) | `LatenessTier.php` + `LatenessTierService` |
| قاعدة «آخر سبت دوام» | `ScheduleExceptionRule` (weekday/occurrence/effect) | `schedule_exceptions` بـ`day_of_week/position/effect` + وسّعوها لتاريخ محدّد ومدى (`2026_07_12_000001`) |
| الحقول الديناميكية للطلبات | `requestType.customFields` + `visibleTo` (`request-type.entity.ts:35-42`) | `request_type_fields` + `request_type_visibilities` (`2026_08_02_000002`) — 2026-08-02 |
| نقل العهدة بتأكيد مزدوج | `custodyTransferHandler` (`destinations.service.ts:597`) | `asset_transfers` + acknowledgment (`2026_08_02_000004`) |
| الوردية المرنة + الساعات المطلوبة | `shiftMode: flexible` + `requiredHours` (`attendance.entities.ts:88-95`) | `2026_08_03_000002_add_shift_flexibility` |
| فصل «نقص الساعات» | ❌ لم نفعلها بعد | `shortfall_minutes` + `calculation_profile` (`8aaa9ff`, 2026-08-23) — **سبقونا** |
| نصف اليوم للإجازة | `period: FULL|MORNING|EVENING` (`leave.entities.ts:70-77`) | `2026_08_03_000003_add_period_to_leave_requests` |
| النقل بين الفرق بتاريخ سريان + cron | `runScheduledTransfers()` | `team_transfer_requests` + `ExecuteTeamTransfersCommand` |

**الاستنتاج:** كل ميزة تشغيلية حصرية لنا اسْتُنسِخت خلال 4-8 أسابيع من تسليم المواصفة. **الميزة الوحيدة التي لم يلحقوا بها بعد** هي عمق كتالوج الطلبات ونطاق المسير — وهما أيضًا قابلان للاستنساخ في أسابيع.

---

## 5) جدول القرار — ماذا نبني، وهل يستحق؟

| # | الفجوة | الأثر | الجهد | القرار |
|---|---|---|---|---|
| 1 | **تناسب التعيين/الانفكاك في المسير** | 🔴 خطأ مالي مباشر في كل شهر فيه تعيين أو انفكاك | صغير (يوم-يومان) | **ابْنِ الآن.** هذه ليست فجوة تنافسية بل عطب. |
| 2 | **الانصراف المبكر لا يُخصم** | 🔴 خسارة مالية صامتة للشركة | صغير (ساعات) | **ابْنِ الآن.** |
| 3 | **نقص الوردية المرنة يمرّ على شرائح التأخير** | 🔴 خصم غير عادل للموظف → نزاع عمّالي | صغير (عمود + فرع في الحساب) | **ابْنِ الآن**، بنفس نمطهم (عمود منفصل + بروفايل حساب على الصف). |
| 4 | **مكافأة نهاية الخدمة بمعامل ثابت** | 🔴 مخالفة نظام العمل | صغير | **ابْنِ الآن.** أضف شرائح 5 سنوات + تخفيض الاستقالة (⅓/⅔) — وهم أيضًا لم يبنوا التخفيض، ففرصة تفوّق. |
| 5 | **صفر اختبارات** | 🔴 كل بند أعلاه سيتكسّر ثانيةً | متوسط (أسبوع لبنية + تغطية محرّك الرواتب والحضور) | **ابْنِ الآن.** بدونه لا معنى لبقية البنود. |
| 6 | **التصدير (CSV على الأقل)** | 🟠 أول اعتراض للعميل + أزرار ميتة معروضة اليوم | صغير-متوسط | **ابْنِ.** ابدأ CSV متدفّق للموظفين/الحضور/كشوف الرواتب. |
| 7 | **حجب الراتب/الآيبان/الهوية بالصلاحية** | 🟠 تسريب PII لأي حامل `employees.view` | صغير | **ابْنِ.** |
| 8 | **سجل تدقيق (audit trail)** | 🟠 شرط تعاقدي في أي RFP، وشرط للرواتب | متوسط | **ابْنِ** — interceptor واحد + جدول. |
| 9 | **GOSI + بدلات ديناميكية** | 🟠 حاجز دخول للسوق السعودي | متوسط-كبير | **ابْنِ** إن كانت السعودية سوقًا مستهدفًا؛ وإلا **أجّل**. |
| 10 | **إصدارات الجدول الأسبوعي + بوابة نشر** | 🟠 اليوم تعديل وردية يعيد كتابة تاريخ محسوب | متوسط | **ابْنِ** الحد الأدنى: تجميد `shiftStart/shiftEnd` على `AttendanceDay` (موجود جزئيًا) ومنع إعادة الحساب لأيام مقفلة. |
| 11 | **إشعارات مخزَّنة + بريد** | 🟠 غياب كامل لقناة إبلاغ | متوسط | **ابْنِ** (جدول + حالة قراءة أولًا؛ البريد لاحقًا). |
| 12 | **وصل الـ23 نوع طلب بلا وجهة** | 🟠 وعد بلا تنفيذ في المنتج | متوسط | **قرّر:** إمّا تُوصَل أو تُخفى من الكتالوج. عرضها كما هي أسوأ من عدمها. |
| 13 | **حوكمة الرواتب (أعلام/تكافؤ/خطوط أساس)** | 🟡 قيمتها تظهر عند وجود عملاء أحياء فقط | كبير جدًا | **أجّل.** ممتاز عندهم لكنه استثمار سابق لأوانه لنا. |
| 14 | **الأداء/التدريب/التوظيف الحقيقية** | 🟠 اتّساع تنافسي، لا عمق تشغيلي | كبير | **أجّل**، لكن **احذف أو علِّم الشاشات الموك** — 15 شاشة وهمية في عرض تجريبي تُفقد الثقة أكثر ممّا تكسب. |
| 15 | **تعدّد المستأجرين + SaaS** | 🟡 قرار نموذج عمل لا قرار هندسي | كبير جدًا (إعادة هيكلة) | **قرار تجاري.** إن كان البيع per-installation فلا تبنِه أبدًا؛ إن كان SaaS فكل يوم تأخير يضاعف الكلفة. |
| 16 | **2FA / استعادة كلمة مرور** | 🟠 «استعادة كلمة المرور» غيابها عطب تشغيلي يومي | صغير | **ابْنِ** الاستعادة الآن، 2FA لاحقًا. |
| 17 | **تصلّب مزامنة ZKTeco** | 🟠 نحن معرَّضون لنفس عطب UDP الذي أثبتوه بالأرقام | متوسط | **ابْنِ** حارس معقولية التاريخ + نافذة تزايدية + استبدل الـdedupe بصف-بصف (اليوم استعلام SELECT لكل بصمة، أي عشرات الآلاف كل 5 دقائق — `device-sync.service.ts:82-92`). |

---

## 6) الحكم النهائي

**لا، الشغل عندنا لا يمشي كويس — لأنه لا يمشي إطلاقًا منذ 8 أسابيع.**

عن جودة شغلهم: **ممتازة ومنهجية بشكل غير معتاد.** رسائل الـcommit تحمل أرقام متطلبات، وأدلة قياس ميدانية (`0.00% loss / 34,302 record`)، وحدودًا معلنة صراحةً (`Known limitation, deferred pending business input`)، وهجرات لا تُسقط بيانات عند التراجع. هذا ليس تنفيذًا حرفيًا لمواصفة — هذا فريق يفهم ما ينفّذه ويحمي الإنتاج منه.

**أين ما زلنا أفضل:** في عرض «سبينة الطلبات» — كتالوج 63 نوعًا يكتب في سجلات دائمة، ومسير قابل للتعريف بالنطاق. **هذه ميزتنا الوحيدة الباقية، ومدّتها الافتراضية 4-8 أسابيع** بحسب سرعة استنساخهم المُثبتة في §4.

**الأولوية إن كان الوقت محدودًا:** البنود 1-5 في جدول القرار (يوم إلى أسبوعان مجتمعة) تُغلق أخطاءً مالية وقانونية حقيقية في النظام. لا تبدأ بأي ميزة تنافسية قبلها.