# تقرير المراجعة المستقلة

## 1. الحكم

**لا أوافق.** أثبتُّ محليًا تجاوزًا للتحقق بخطوتين عند فشل قراءة إعداده، وخللًا في عدّ محاولات الرمز المتزامنة؛ كما تعذّر تنفيذ الدورات الحية وقياس الأداء على SQL Server لأن الخدمات غير متاحة.

تاريخ المراجعة: **2026-09-23**. النسخة: `a21744076ba0027c395a362c38504b0e7b62cdea`، الفرع `payroll-2026-09-14`، Node.js `v24.11.1`.

**حدود هذا الحكم:** هذه مراجعة للكود واختبارات محلية مستقلة، وليست شهادة باكتمال اختبار الدورات على قاعدة مؤقتة. لم أُنشئ قاعدة اختبار، ولم أنفّذ دورة SQL حية. الفروق الواردة أدناه شغّلتها في كود المنتج نفسه بدواله وخدماته، مع مستودعات بيانات ومزوّدي مجال وبريد وهميين. لم أتعامل مع هذه المستودعات باعتبارها إثباتًا لسلوك معاملات SQL أو أقفاله أو سرعة HTTP.

قرأت التقارير الستة المطلوبة: `STATUS_2026-09-22.md` و`PAYROLL_PROOF_2026-09-21.md` و`PAYROLL_PROOF_JOINER_LEAVER.md` و`BANK_CASH_PROOF.md` و`ROLES_AUDIT.md` و`TEST_REPORT_2026-09-21.md`. استخدمتها لتحديد الادعاءات المطلوب فحصها، ولم أنقل نجاحاتها السابقة إلى نتائج هذه المراجعة.

## 2. العيوب الحاجزة

### B01 — فشل قراءة إعداد التحقق بخطوتين يسمح بإصدار جلسة بلا رمز

**الخطورة: عالية — ثغرة أمنية.**

- **ماذا فعلت:** شغّلت `AuthService.domainLogin` مع مزوّد مجال وهمي يثبت هوية مستخدم تجريبي، وخدمة `TwoFactorService` الفعلية. جعلت قراءة مفتاح التحقق بخطوتين ترمي خطأً مصطنعًا، مع نجاح بقية عمليات المستودعات. مُوقّع الجلسات وهمي ويعدّ مرات استدعائه.
- **المتوقع:** منع إتمام الدخول عند تعذّر معرفة هل الرمز مطلوب، وعدم إصدار جلسة؛ ففشل القراءة لا يعني أن المالك عطّل التحقق.
- **الناتج:** استُدعي إصدار الجلسة **مرة واحدة**، ولم يُرسل بريد ولم تُنشأ حالة تحقق: `sessions=1, mails=0, challenges=0`.
- **السبب المثبت:** `TwoFactorService.isEnabled()` يلتقط استثناء قراءة الإعداد ويعيد `false`. يتعامل `finishLogin()` مع هذه النتيجة كتعطيل مقصود للتحقق، ويصل إلى `issueSession()`.
- **الموضع:** `api/src/auth/two-factor.service.ts:90`، وبالأخص السطر **97**؛ ثم `api/src/auth/auth.service.ts:211` و**213**.
- **الإعادة:** اختبار **CR-B01** في [ملف الاختبارات المستقلة](api/test/codex-review-offline.test.cjs).
- **حد الإثبات:** خلل قراءة واحد حُقن عند حدود المستودع؛ لم أُسقط قاعدة حقيقية، ولم أُصدر JWT حقيقيًا. لم أختبر قابلية مهاجم خارجي لإحداث هذا الخلل عمدًا. وصول خدمة المصادقة إلى إصدار الجلسة دون الرمز مُثبت.

### B02 — المحاولات الخاطئة المتزامنة تتجاوز الحد الفعلي للرمز

**الخطورة: عالية — ثغرة أمنية.**

- **ماذا فعلت:** أنشأت حالة تحقق بخدمة المنتج، ثم أرسلت **12 استدعاء تحقق متزامنًا** برمز خاطئ من ستة أرقام. أعاد المستودع الوهمي لكل قراءة نسخة من الصف، وحفظ التحديثات بالترتيب الذي وصلته؛ أي إنه يحاكي تداخل القراءة قبل الكتابة دون أن يضيف قفلًا لا يطلبه المنتج.
- **المتوقع:** الحد المعلن **5 محاولات**؛ تُقفل الحالة بعد استنفاده ويُرفض الرمز الصحيح بعد القفل.
- **الناتج:** رُفضت الاستدعاءات الـ12، لكن الصف انتهى بـ**محاولة واحدة فقط**، و`lockedAt` بقي فارغًا. ثم **قُبل الرمز الصحيح** لنفس الحالة.
- **السبب المثبت:** كل استدعاء يقرأ `attempts=0`، ويحسب `row.attempts + 1`، ثم يكتب القيمة **1** بالتعيين، دون زيادة ذرية أو شرط يطابق النسخة المقروءة. في الاختبار المتتابع المقابل، قُفلت الحالة عند المحاولة الخامسة كما ينبغي.
- **الموضع:** `api/src/auth/two-factor.service.ts:178` للقراءة، و**195–197** لحساب العدد وكتابته.
- **الإعادة:** **CR-B02**؛ والاختبار الضابط **CR-P03**.
- **حد الإثبات:** ثبت التداخل على مستوى الخدمة والمستودع الوهمي، ولم يُقَس تحت اتصالات SQL متزامنة أو عبر HTTP. لا أدّعي تخمين رمز صحيح؛ المثبت أن حد المحاولات لا يحتفظ بعدد المحاولات المتوازية.

## 3. عيوب غير حاجزة

### N01 — كشف وجود موظف خارج النطاق عبر تقرير الحضور الشهري

**الخطورة: متوسطة — ثغرة أمنية، كشف وجود سجل.**

- **ماذا فعلت:** استدعيت `AttendanceController.monthly()` وخدمة الحضور الفعلية بحساب خدمة ذاتية في الفرع 1، مرة بمعرّف موظف موجود في الفرع 2، ومرة بمعرّف غير موجود. المستودع وحده وهمي.
- **المتوقع:** نفس حالة الرفض ونصه في الحالتين، حتى لا يصبح النداء وسيلة لاكتشاف وجود الموظف.
- **الناتج:** الموجود خارج النطاق: **400** و«لا تملك صلاحية عرض حضور غيرك». غير الموجود: **404** و«الموظف غير موجود».
- **الموضع:** `api/src/attendance/attendance.service.ts:3493` و**3494**، ثم **3496–3503**. مسار الخدمة الذاتية في `api/src/attendance/attendance.controller.ts:538` وما بعده.
- **الإعادة:** **CR-N01**.
- **حد الإثبات:** حالات استثناءات Nest الناتجة من الكنترولر والخدمة، وليس تبادل HTTP فعليًا. لم يرجع محتوى حضور الموظف الآخر؛ العيب في كشف الوجود.

### N02 — كشف البنك والتقرير المالي لا يتبعان تقسيم الصرف المثبّت بعد الصرف

**الخطورة: متوسطة — مال خاطئ في تصنيف التقرير، دون إثبات صرف مكرر.**

- **ماذا فعلت:** مرّرت بند مسير بحالة `PAID` وصافيه **1,000.00**، وعلامة صرف ثابتة تقول إنه صُرف كله نقدًا. مثّلت تغيير طريقة الصرف الحالية للموظف بعد ذلك إلى تحويل بنكي. شغّلت كنترولر كشف البنك، وبناء شاشة الصرف، ودالة صف التقرير المالي الفعلية على هذه المدخلات.
- **المتوقع:** بيان الصرف المنفذ يحتفظ بـ**بنك 0.00 / نقدي 1,000.00**، أو يفصل صراحةً بين الصرف المثبت وتوجيه الصرف الحالي.
- **الناتج:** شاشة الصرف احتفظت بـ**بنك 0.00 / نقدي 1,000.00**. كشف البنك والتقرير المالي أظهرا **بنك 1,000.00 / نقدي 0.00** لنفس البند. الصافي ظل 1,000.00؛ الفارق في كل قناة **1,000.00**.
- **السبب المثبت:** شاشة الصرف تستخدم `paidMark.bankAmount/cashAmount`. كشف البنك يقرأ طريقة الصرف الحية من الموظف ولا يقرأ علامات الصرف. التقرير المالي يستخدم الطريقة الحية أيضًا. الكشف والتقرير المالي متطابقان مع بعضهما، لكنهما مختلفان عن واقعة الصرف المثبتة.
- **الموضع:** `api/src/payroll/bank-sheet.controller.ts:23` و**25**؛ `api/src/payroll/bank-sheet.ts:90` و**91**؛ `api/src/reports/financial-report.ts:208`؛ مقابل `api/src/payroll/payroll-disbursement.ts:88` و**95–96**. قراءة طريقة الموظف في استعلام التقرير: `api/src/reports/financial-report.service.ts:87`.
- **الإعادة:** **CR-N02**.
- **حد الإثبات:** لم أغيّر طريقة صرف موظف حقيقي، ولم أنفّذ تحويلًا. هذا اختلاف تاريخي قابل لإعادة الإنتاج في مخرجات المنتج، وليس دليلًا على إعادة دفع الراتب أو فساد صافي المسير. استخدام الطريقة الحالية قبل تسجيل الصرف ليس موضع الاعتراض.

### N03 — تحقق بدأ قبل إعادة الإرسال يستطيع قبول الرمز السابق بعد اكتمالها

**الخطورة: منخفضة إلى متوسطة — ثغرة أمنية في تزامن استبدال الرمز.**

- **ماذا فعلت:** أوقفت تسليم نتيجة قراءة صف التحقق لاستدعاء جارٍ بعد أخذ نسخة منه. نفّذت إعادة الإرسال حتى انتهت الخدمة من تغيير `codeHash`، ثم سمحت للتحقق الأول بالاستمرار باستخدام النسخة القديمة.
- **المتوقع:** عند استهلاك الحالة بعد استبدال الرمز، يتحقق المنتج أن الرمز الذي قورِن ما زال رمز الحالة الحالي.
- **الناتج:** **قُبل الرمز السابق** بعد اكتمال إعادة الإرسال.
- **السبب المثبت:** الاستهلاك المشروط يفحص `id` و`consumedAt` و`lockedAt`، ولا يفحص `codeHash` أو إصدار الرمز الذي قورِن. إعادة الإرسال تستبدل الـhash بصورة منفصلة.
- **الموضع:** `api/src/auth/two-factor.service.ts:207` و**208**، مقابل **244–251**.
- **الإعادة:** **CR-B03**؛ بادئة اسم الاختبار لا تعني أنني صنفته حاجزًا في هذا التقرير.
- **حد الإثبات:** هذه حالة تداخل مقصودة بمستودع وهمي؛ لا تعني أن رمزًا قديمًا يُقبل إذا بدأ التحقق بعد اكتمال إعادة الإرسال. لم أثبت جلستين أو اختراق حساب بهذه الحالة، ولذلك لم أجعلها سبب الرفض الرئيسي.

## 4. الأداء

### القياس الحي المطلوب

**لم أتمكن من ترتيب المسارات حسب بطئها الفعلي أو تأكيد زمن 88 ثانية.** محاولة SQL رجعت `ESOCKET`، وفحص الاتصال المحلي رجع `ECONNREFUSED` على `127.0.0.1:1433` و`[::1]:1433`، وكذلك `127.0.0.1:3000` و`:4000`. أمر `docker ps` لم يجد قناة `docker_engine` وأبلغ أيضًا عن تعذر قراءة ملف إعداد Docker. هذا وصف لبيئة المراجعة، وليس عيبًا مثبتًا في المنتج.

| المسار / العملية | الزمن على بيانات SQL الحقيقية في هذه المراجعة | ما أمكن تحديده |
|---|---|---|
| `GET /attendance/daily` | **غير مقاس**؛ رقم 88 ثانية من التقرير السابق غير متحقق هنا | أثبتّ زيادة عدد استدعاءات القراءة خطيًا مع عدد الموظفين، بالتفصيل أدناه |
| `GET /attendance/monthly` | غير مقاس | يعيد استخدام `projectExemptionDays`؛ ويستطيع المسار تجسيد الغياب لصاحب صلاحية الإدارة، فلا يجوز افتراض أن كل GET قراءة فقط |
| حساب مسير 500 موظف | **غير منفذ** | قرأت حلقة الموظفين داخل معاملة الحساب واستدعاء تراكم الأيام لكل موظف؛ لا أملك توزيعًا زمنيًا حقيقيًا لها |
| كشف الرواتب والتكلفة المالي | غير مقاس | تحميل صفوف الشهر وتفاصيلها ثم بناء التقرير في الذاكرة؛ لا قياس لحجم البيانات أو زمن SQL |
| قائمة الموظفين | غير مقاس | تحميل قائمة النطاق ونسخ قواعد الحضور؛ لا قياس لزمن الاستعلام أو النقل للمتصفح |
| شاشة الصرف لكل موظف | غير مقاس | بناء الصفوف يحمّل البنود والأعضاء والعلامات؛ لا قياس HTTP أو ذاكرة إنتاج |

### إثبات مستقل لـN+1 في الحضور اليومي

في **CR-PERF01** شغّلت `AttendanceService.daily()` الفعلية، بما فيها `computeDay()` وقراءة الاستثناءات ومصدر البصمة، على **يوم قديم محفوظ ومقفل ماليًا**. استبدلت المستودعات بقراءات من الذاكرة وعدّادات، دون استبدال خوارزمية الخدمة. اخترت اليوم المقفل لأنه يسلك الفرع القصير الذي يعيد اليوم المحفوظ.

| موظفون اصطناعيون | قراءة استثناءات الموظفين | إعادة قراءة اليوم الواحد | فحص الإقفال المالي | إجمالي استدعاءات القراءة |
|---:|---:|---:|---:|---:|
| 1 | 1 | 1 | 1 | **7** |
| 100 | 100 | 100 | 100 | **304** |
| 500 | 500 | 500 | 500 | **1,504** |
| 616 | 616 | 616 | 616 | **1,853** |

في هذه التجهيزة تحديدًا: **`3 × N + ceil(N / 500) + 3`**. الثلاث الثابتة لقائمة الأيام، وقائمة الموظفين، والتصحيحات، مع دفعات البصمات كل 500 موظف. الأرقام تعدّ استدعاءات واجهة المستودع وSQL؛ ليست عدّادًا من SQL Profiler.

السبب المباشر: `exemptionsFor` يقرأ لكل موظف في `api/src/attendance/attendance.service.ts:3211`؛ و`projectExemptionDays` يعيد حساب **كل صف محفوظ** تباعًا عند **3231**؛ وكل `computeDay` يعيد قراءة اليوم ثم يفحص إقفاله عند **2131–2132**، باستعلام `financiallyClosedDay` عند **276**. حتى الفرع القصير يولّد هذا العدد؛ اليوم غير المقفل له قراءات إضافية لم أقسها هنا.

زمن التشغيل داخل الذاكرة كان **6.84 مللي ثانية لـ616 موظفًا** في آخر تشغيل. **هذا ليس زمن الشاشة أو SQL**: لا شبكة ولا قرص ولا انتظار أقفال فيه، ولا يجوز مقارنته بـ88 ثانية أو استخدامه كتوقع للأداء. الذي ثبت هو شكل تضاعف القراءات، والسبب المرشح لبطء المسار؛ أما سبب زمن 88 ثانية بدقة فلا أعرفه دون قياس حي.

### البنية التحتية: قراءة كود فقط، بلا ادعاء عيوب غير منفذة

- **المعاملات والأقفال:** الحساب يبدأ معاملة عند `api/src/payroll/payroll.service.ts:507`، ويحجز الموظفين عند **644**، ثم يمر عليهم تباعًا عند **700** ويحسب تراكم أيامهم عند **707**. هذا موضع يلزم قياس زمن الاحتفاظ بالأقفال فيه؛ لم أثبت deadlock أو timeout.
- **الاتساق:** إنشاء الموظف يحفظ الملف ومرجع الراتب والمستندات داخل معاملة، ثم ينشئ أرصدة السنة والرصيد الافتتاحي بعد انتهائها (`api/src/employees/employees.service.ts:407` ثم **434–437**). لم أحقن فشلًا حيًا بين المرحلتين؛ لم أدرجه عيبًا مثبتًا.
- **نداء خارجي أثناء قفل:** مسار تزويد حساب المجال بعد إنشاء الموظف يُستدعى بعد انتهاء المعاملة (`api/src/employees/employees.service.ts:443`). قرأته فقط؛ لا أعمم ذلك على كل النداءات الخارجية، ولم أتصل بالمجال أو البريد.
- **الذاكرة:** قائمة الموظفين (`api/src/employees/employees.service.ts:142`) تحمل صفوف النطاق؛ ومسار اشتقاق مصدر الدوام القديم يقرأ جداول العمل كاملة، وقد يقرأ الموظفين جميعًا (`api/src/attendance/attendance.service.ts:3257` و**3258**). التقرير المالي يحمل صفوف الشهر قبل تجميعها (`api/src/reports/financial-report.service.ts:75`). هذه مواضع فحص ذاكرة، وليست إثباتًا لتسرب ذاكرة أو نفادها.
- **الفهارس والمخطط:** قرأت تعريفات كيانات الموظفين والحضور والطلبات والمستخدمين وحالات الدخول. يوجد مثلًا فهرس مركب للبصمات `(employeeId,punchTime)` وقيد فريد لليوم `(employeeId,date)`. **وجود تعريف في الكود لا يثبت وجوده في القاعدة الفعلية**، وعدم وجود علاقة TypeORM لا يثبت غياب FK قد يضيفه ترحيل. لم أتمكن من فحص `sys.indexes` و`sys.foreign_keys` أو خطط التنفيذ أو إحصاءات استخدام الفهارس؛ لا قائمة موثوقة عندي بفهارس ميتة أو مفقودة.
- **SQL Server 2019:** لم أقترح SQL جديدًا ولم أطبّق ترحيلًا. لا يوجد تنفيذ حي يثبت توافق النسخة الحالية كاملةً مع 2019.

## 5. ما فحصته وخرج سليمًا

### نتائج التشغيل التي قستها بنفسي

| الفحص | النتيجة |
|---|---|
| TypeScript للـAPI | **نجاح**، رمز خروج 0 |
| TypeScript للواجهة | **نجاح**، رمز خروج 0 |
| الحزمة المحلية المنتقاة من الاختبارات القائمة | **1,508 نجح / صفر فشل / صفر تخطي**، في **130 ملفًا**، خلال **27.365 ثانية** في تشغيل الملف القابل للإعادة |
| الاختبارات المستقلة الجديدة | **20 اختبارًا: 15 نجح و5 فشل**، صفر تخطي، خلال **3.592 ثانية**؛ الخمسة تعيد إنتاج الفروق الموصوفة في القسمين 2 و3 |
| اختبارات SQL للدورات الكاملة | **لم تُنفذ دورة ناجحة**؛ لا تحتسب ضمن النجاح أعلاه |

استخدمت مترجم TypeScript المثبت مباشرةً مع `--noEmit --incremental false` حتى لا تُكتب ملفات build أو `tsbuildinfo`. لم أعتبر فحص الأنواع اختبارًا لتشغيل الواجهة.

**إفصاح عن الجولة الأولى:** شغّلت 144 ملفًا من اللاحقة `*.test.cjs` بعد استبعاد ثمانية ملفات واضحة الآثار الجانبية. خرجت **1,531 ناجحًا و97 فشلًا** خلال 57.691 ثانية. اتضح أن 13 ملفًا إضافيًا تستدعي تمهيد SQL عبر `migrations-lib.cjs`؛ فشلت عند الاتصال في `before` قبل تطبيق ترحيلات. أزلتها من أمر الإعادة، وفصلت كذلك 23 اختبار بروتوكول بصمة كانت تستخدم محاكي TCP محليًا مؤقتًا من تجهيزات الاختبار القائمة. بذلك صارت الحزمة المحلية الصرفة 1,508 اختبارات، ونجحت كلها. **الـ97 ليست 97 عيب منتج**، ولا يجوز عرض الجولة الأولى باعتبارها نجاحًا لاختبارات قواعد البيانات. لم يتصل محاكي البصمة بجهاز حقيقي.

الحزمة القائمة تضم اختبارات دوال، وخدمات ببدائل للمستودعات، وفحوصًا نصية لشيفرة الواجهة والربط. لذلك **1,508 نجاحات لا تعني 1,508 سيناريوهات مستخدم حية**.

### حسابات ومقارنات مستقلة ناجحة

| الحالة | المتوقع يدويًا | الناتج الذي شغّلته |
|---|---|---|
| القص النقدي | `14500 ÷ 30 ÷ 8 ÷ 60 × 90 × 1.5 = 135.9375`، وبعد القص **135.93** | `roundPayrollMoney` أعادت **135.93**، والسالب قُص باتجاه الصفر أيضًا |
| ملتحق 15 سبتمبر، دورة 23 أغسطس–22 سبتمبر | **8 أيام** شاملة الطرفين | دالة تغطية الخدمة أعادت **8**؛ المستحق اليدوي لراتب 14,400 هو **3,840** |
| منفك آخر يوم عمله 15 سبتمبر في الدورة نفسها | **24 يومًا** | دالة التغطية أعادت **24**؛ المستحق اليدوي **11,520** |
| مرونة 60 دقيقة، وردية 09:00–17:00، حضور 09:40–17:40 | تأخير صفر ونقص صفر | **صفر / صفر** |
| حضور 10:30–18:30 على الوردية نفسها | **90 دقيقة تأخير** ونقص صفر | **90 / صفر** |
| بصمة دخول ناقصة | تحتاج مراجعة، والنقص غير محسوم | `attendanceReviewRequired=true` و`shortfallMinutes=null` |
| إذن مسائي بخصم يغطي 15:00–17:00، سعر الدقيقة 1 | **120** إذن و**صفر** نقص إضافي | **120.00 إجماليًا**، دون ازدواج مع النقص |
| كشف بنك اصطناعي: نقدي 500.99، بنك 1000.01، مختلط 800.03 منه بنك 300.02، مختلط 300.09 بمبلغ بنك أعلى من الصافي، وصف صفر | البنك **1,600.12**، النقدي **1,001.00**، الصافي المستحق **2,601.12** | الأرقام الثلاثة مطابقة |
| إضافة راتب تصفية 700.11 إلى الكشف السابق | **بنك + نقدي + تصفية = 3,301.23** | مطابق؛ راتب التصفية خارج صفوف الصرف العادي |
| تقرير مالي من صفين اصطناعيين | الأول: `6000 + 1800 + 195 - 16.25 - 780 - 500 = 6698.75`؛ الثاني 3000؛ الصافي الكلي **9,698.75** | الإجمالي **10,995.00**، الخصومات **1,296.25**، الصافي **9,698.75**، البنك **5,000.00** والنقدي **4,698.75** |

الصفان الخاصان بالملتحق والمنفك يثبتان **دالة تحديد الأيام**، وليس تشغيل مسير كامل بمبلغيهما. ومثال التقرير يختبر حساب وتجميع التقرير على بنود معطاة، وليس إنشاء تلك البنود بواسطة خدمة احتساب المسير.

### تغطية الدورات العشر وحدودها

| الدورة | ما شغّلته محليًا ونجح | ما لا يشمله النجاح |
|---|---|---|
| الموظفون | تحقق بيانات كاملة اصطناعية، حذف كل حقل إلزامي بالتتابع، رفض تاريخ مستحيل وتاريخ تعيين 2126؛ اختبارات الموظفين القائمة | لا إضافة SQL أو إثبات فريد الهوية/البصمة تحت السباق، ولا دورة إيقاف وأرشفة وإنهاء خدمة حية |
| الحضور | المرونة والتأخير والبصمة الناقصة والإذن المدفوع دون ازدواج؛ اختبارات الحضور المحلية؛ عدّ قراءات اليوم المحفوظ | لا إدخال بصمة حقيقية إلى SQL ولا إعادة حساب وربط فعلي بمسير |
| الإجازات | التمييز بين أيام العمل والتقويم، اشتراط مرجع ملف مع الطلب وتأجيله عند `AFTER_RETURN`، تقسيم محفوظ 3 أيام/4 أيام بين سنتين؛ اختبارات الأرصدة والأنواع | لا إنشاء كل الأنواع واعتمادها وإلغاؤها على قاعدة مؤقتة؛ قبول شكل `file:1` لا يثبت وجود الملف أو ملكيته |
| الطلبات | رفض الرفض/الإعادة بلا سبب قبل أي معاملة؛ الاختبارات المحلية للوجهات والقواعد | لا تجربة كل نوع في سلسلته، ولا تنفيذ النيابة والسرية وإثبات الأثر مرة واحدة على SQL |
| الرواتب | قص الكسور وتغطية التعيين/الانفكاك؛ تقدم سلسلة بأشخاص محددين وعدم قبول قرار نسخة حساب قديمة؛ اختبارات التحصيل والسياسات والصرف القائمة | لا مسودة→حساب→اعتماد→صرف حية، ولا تحصيل متبقٍّ لشهرين، ولا تحقق معاملات التصفية وفصل المهام تحت التزامن |
| البنك والصرف | الأرقام التفصيلية أعلاه وثبات علامة الصرف في باني شاشة الصرف | لا تحويل بنكي حقيقي؛ اختلاف N02 قائم في المخرجات الأخرى |
| التقارير | مجموع الصفوف والأعمدة، البنك والنقدي والتصفية، ورفض مدى مقلوب أو ناقص؛ اختبارات التقارير المحلية | لا مقارنة صفوف التقرير بسجلات SQL، ولا إثبات أن كل فلتر يغير نتائج الاستعلام الحي |
| العهد | نفس رد الأصل الغائب والخارج عن النطاق، منع الكتابة خارج الفرع ومنع النقل بين الفروع لحساب فرع | لا تسليم→إقرار→تأكيد→استرجاع→نقل حية |
| الأمن والعزل | حساب بلا صلاحية لا يفتح اليومي حتى مع نطاق كل الفروع؛ النطاق غير المسند فارغ؛ قواعد عزل الأصول؛ اختبارات الحراس القائمة | ليس مسحًا حيًا لجميع المسارات؛ كشف الوجود في الحضور N01 ما زال قائمًا |
| المجال والرمز | خدمة المصادقة الفعلية مع مزوّد مجال وبريد وهميين: لا جلسة قبل الرمز في المسار الطبيعي، الرمز الناجح لا يُستخدم مرتين، وفشل البريد ينتج 503 بلا جلسة أو حالة معلقة | لا اتصال بمجال أو SMTP حقيقي، ولا إثبات أقفال مستودع SQL؛ B01 وB02 وN03 موثقة منفصلة |

### ملفات الإثبات وأوامر الإعادة

أضفت فقط هذين الملفين الدائمين تحت `api/test/` مع هذا التقرير، دون إصلاح كود المنتج:

- [codex-review-offline.test.cjs](api/test/codex-review-offline.test.cjs): الاختبارات المستقلة؛ يمنع فتح اتصالات أو مستمع شبكة، ولا يقرأ `.env`. **رمز الخروج 1 متوقع حاليًا** لأن خمس تأكيدات للسلوك المطلوب تكشف الفروق غير المعالجة؛ لم أغيّرها لتوافق العيب.
- [codex-review-local-suite.cjs](api/test/codex-review-local-suite.cjs): مشغّل الحزمة القائمة المحلية، ويطبع أسماء الملفات المستبعدة والملخص.

من جذر المشروع:

```powershell
node api/test/codex-review-local-suite.cjs
node --test api/test/codex-review-offline.test.cjs
node node_modules/typescript/bin/tsc --noEmit --incremental false -p tsconfig.json
node api/node_modules/typescript/bin/tsc --noEmit --incremental false -p api/tsconfig.json
```

## 6. ما لم أستطع فحصه ولماذا

1. **كل الدورات الحية المطلوبة على قاعدة مؤقتة:** اتصال SQL غير متاح. لم أبدأ Docker أو API أو Next، ولم أطبّق ترحيلًا لتجاوز العائق. لذلك لم يكتمل الشرط الصريح «سيناريو حقيقي على قاعدة مؤقتة لكل دورة».
2. **الأداء على حجم البيانات الفعلي:** لم أستطع تأكيد عدد الموظفين الحالي أو زمن 88 ثانية أو زمن مسير 500 موظف. استخدمت 616 كحجم اصطناعي مأخوذ من ملف المهمة، وليس كعدد قرأته من قاعدة الشركة.
3. **الأقفال والجمود والعودة بالمعاملة عند الفشل:** لا SQL متاح، وبالتالي لا جلسات متنافسة أو قياسات انتظار أقفال أو إثبات rollback متعدد الجداول. اختبارات الذاكرة لا تعوّض ذلك.
4. **المخطط الفعلي والفهارس:** لا اتصال لقراءة القيود والأنواع والفهارس أو خطط التنفيذ والاستخدام. لم أسجل غياب FK أو فهرس كعيب بناءً على الكيانات وحدها.
5. **الواجهة ودورات رفع الملفات والتصدير من المتصفح:** المنافذ المحلية المتوقعة مغلقة، والتعليمات تمنع تشغيل الخدمات. نجاح TypeScript والفحوص النصية لا يثبت عرض RTL أو تنفيذ الأزرار أو رفع المستندات.
6. **إعادة إثبات الحساب السابق 12,100.00، والتصفيات والمديونيات عبر الأشهر:** قرأت طريقة الحساب وادعاءات التقارير، لكن لم أعد تشغيلها على SQL. الأرقام السابقة ليست قياسات جديدة مني.
7. **توافق التشغيل الكامل مع SQL Server 2019:** لا اتصال بخادم 2019 متاح، ولم أشغّل ترحيلات. لم أستنتج التوافق من مرور TypeScript أو الدوال الصافية.

لم أكتب في `hr_system` أو `hr_review_pre_payroll`، ولم أستدعِ endpoint يكتب عليهما. لم أطبع أو أنقل قيم أسرار، ولم أتصل بمجال أو بريد حقيقي أو شبكة خارجية. فحص Git أظهر عدم تعديل أي ملف منتج متعقّب؛ الموجودان غير المتعقّبين `.next-fr-preview/` و`CODEX_REVIEW_BRIEF.md` كانا موجودين قبل بدء العمل.

**التوقيع: Codex — مراجع مستقل. الحكم على النسخة المحددة أعلاه: لا أوافق، مع بيان صريح بأن المراجعة الحية غير مكتملة بسبب البيئة والقيود الملزمة.**
