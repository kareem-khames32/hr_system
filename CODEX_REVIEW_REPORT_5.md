# لا أوافق على التشغيل

**السبب:** أُغلق تجاوز إعدادات الاعتماد الذاتي، ونجح حجب بطاقة الطلب، لكن صندوق الموافقات ما زال يكشف المسمى الوظيفي الحالي لموظف انتقل خارج نطاق المعتمد. أثبتُّ ذلك بطلب ونقل عبر API؛ عزل بيانات الموظف لم يكتمل.

مراجعة مستقلة، 26 سبتمبر 2026، للفرق `ce9ee15..d352f40` في `api/src` و`src`: **سبعة ملفات منتج**. قرأت الفرق كاملًا، وشغّلت الاختبارات أدناه على SQL مؤقت. كل عيب منتج مسجل هنا له إعادة إنتاج منفذة. لم أعتمد نتيجة المالك بدل تشغيل مستقل، ولم أقس الأداء في هذه الجولة، حسب طلبه.

## 1. العيوب الحاجزة

### CR5-B01 — صندوق الموافقات يسرب المسمى الحالي خارج النطاق؛ إغلاق CR4-B02 اقتصر على التفاصيل

**الخطورة: متوسطة إلى عالية — تسريب بيانات موظف عبر حدود الفروع.** لا يغير مبالغ ولا يمنح اعتمادًا ماليًا. اعتبرته حاجزًا لأن نفس معلومة التنظيم الحالي التي حُجبت لحماية نطاق الموظف يمكن قراءتها من مسار آخر بالحساب نفسه.

**ما شغّلته:** موظف في A بمسمى `OLD JOB R5`، وطلب عادي بسلسلة فيها خطوة HR. قدّم الموظف الطلب عبر `POST /requests`؛ صار `UNDER_REVIEW` وظهر في صندوق معتمد فرعه A. حساب المعتمد دوره `employee` ومعه `approve.hr` و`employees.view`، بلا نطاق C. نقل مدير النظام الموظف إلى C وغيّر المسمى إلى `NEW FOREIGN JOB R5` عبر `PATCH /employees/:id`، بعد قراءة سياق التقويم وتمرير المراجعة وبصمة المصدر المطلوبة. لم أنقل الطلب أو أعدل نطاق المعتمد.

| القراءة بحساب معتمد A بعد النقل | المتوقع | الفعلي |
|---|---|---|
| `GET /employees/:id` | رفض إظهار الملف | 404 |
| `GET /requests/:id` | الطلب التاريخي مسموح، والتنظيم الحالي محجوب | 200؛ `orgHidden: true` و`jobTitle: null` |
| `GET /requests/inbox` | يبقى الطلب قابلًا للمعالجة دون كشف المسمى الحالي خارج النطاق | **200؛ `requesterJobTitle: "NEW FOREIGN JOB R5"`** |

**السبب الذي قرأته:** `inbox()` يختار الطلبات بنطاق **فرع الطلب**، ثم يجلب الموظفين بمعرفاتهم ويقرأ `jobTitle` الحالي دون فحص فرع الموظف الحالي. الإصلاح في `requestPeople(req, user)` لا يستعمله الصندوق. الموضعان: [القراءة عند 1959](api/src/requests/requests.service.ts#L1959)، و[الإرجاع عند 1967](api/src/requests/requests.service.ts#L1967).

**نسبة العيب إلى التغيير:** أعدت نفس السيناريو على مصادر `ce9ee15` المحمّلة من Git في ذاكرة الاختبار، دون تبديل ملفات المنتج. ظهر التسريب في الصندوق هناك أيضًا؛ **هو مسار قديم فات الجولة الرابعة، وليس تراجعًا أحدثه الإصلاح**. عند `d352f40` اختفى التسريب من البطاقة وبقي في الصندوق. لا أدعي تسريب القسم أو الفريق أو المدير من الصندوق؛ الدليل هنا للمسمى فقط.

**الإغلاق المطلوب:** تطبيق قاعدة ظهور التنظيم الحالي نفسها على إثراء بيانات الصندوق، مع الاحتفاظ بالطلب التاريخي وحق معالجته. يجب ألا يعيد الصندوق المسمى الحالي خارج النطاق؛ إن أُريد مسمى تاريخي فيلزم مصدر تاريخي موثق، لا ملف الموظف الحالي.

**الأدلة:** [اختبار النقل عبر API](api/test/codex-review-round5-boundaries.integration.cjs)، [إعادة الإنتاج الحالية](api/test/codex-review-round5-results-inbox-current.cjs)، [إعادة الإنتاج على ce9ee15](api/test/codex-review-round5-results-inbox-baseline.cjs). الاختبار يحتفظ بتوقع المنع الصحيح، ولذلك **يفشل** على النسختين؛ لم أحوّله إلى اختبار يبارك التسريب.

## 2. غير الحاجز والملاحظات المتبقية

- **N04 من الجولة الرابعة:** عرض الرقم القومي الحالي في القسيمة التاريخية ما زال قرارًا معلقًا عند المالك. لم أعد تشغيل PR-05 في هذه الجولة، ولا أدعي إغلاقه.
- **N05 من الجولة الرابعة:** كلفة الاعتماد والردود الكبيرة لم تتغير ضمن هذا الإصلاح، ولم أعد قياسها. تبقى الملاحظة السابقة بحدود قياسها.
- **ملاحظة تحقق، وليست عيب منتج:** نسخة `fulltest-settings` أعادت نجاح 39 سيناريو في Node، لكن سجلها الداخلي يقول **359/360**، وليس نجاحًا كاملًا. التحقق المختلف في A3 يفترض أن الحساب بلا فرع يملك نطاق الشركة؛ تجهيز الحساب والتوكن لا يحدد `scopeAllBranches: true`، لذلك 403 يطابق قاعدة النطاق الحالية. أثبتُّ في اختباري المستقل أن حسابًا يحمل العلم فعلًا يستطيع الكتابة القانونية. كذلك يجمع مشغّل هذا الملف أخطاء التحقق داخليًا دون إسقاط سيناريو Node، فلا تكفي نتيجة Node الخضراء وحدها. [الأثر الداخلي](api/test/codex-review-round5-settings-findings.cjs).
- **اختبار نصي قديم:** التشغيل الأول للوحدة أعطى **1539/1540**؛ RC-UI-11 كان يشترط موضع حارس الفئة قبل قراءة الصف. الإصلاح يتطلب توحيد المفتاح أولًا. أنشأت نسخة مراجعة تتحقق من الترتيب الجديد فنجحت. أثناء العمل تغيّر ملف الاختبار الأصلي من طرف آخر، دون تعديل مني؛ قرأت الفرق وأعدت تشغيله: **11/11**. لا أعد هذا عيبًا وظيفيًا ولا أخفي الفشل الأول. [التشغيل الأول](api/test/codex-review-round5-results-unit.cjs)، [إعادة الملف المتغير](api/test/codex-review-round5-results-unit-followup.cjs).

## 3. الأداء

**لم أقس الأداء، امتثالًا لتعليمات الجولة الخامسة والحمل المتزامن على الجهاز.** أزمنة تشغيل الاختبارات الآلية في ملفات النتائج ليست قياسات أداء للمنتج. لا أقدم ترتيبًا أو أرقامًا جديدة، ولا أنقل أرقام الجولة الرابعة باعتبارها قياسات هذه الجولة.

## 4. إغلاق البنود وما شغّلته وخرج سليمًا

| البند | دليل التشغيل المستقل | الحكم |
|---|---|---|
| **CR4-B01** | خمس صيغ للرخصة: الأصلية، الكبيرة، المختلطة، ومسافات لاحقة؛ كلها 403 مع `PAYRUN-SOD-LICENCE-PERMISSION`، والقيمة بقيت `false`. خمس صيغ لمفتاح الفئة أعادت 400 وبقي الربط محفوظًا. | **مغلق؛ عولج اختلاف المفتاح عند مصدره.** |
| فصل المهام الفعلي ضمن B01 | أنشأ حساب مسيرًا واحتسبه، ثم حاول تعديل الرخصة واعتماد مسيره. PATCH أعاد 403؛ الاعتماد قبل المحاولة وبعدها 403/`PAYRUN-STATE-003`، والحالة `CALCULATED` و`approvedBy: null`. | **مغلق في المسار المالي المعاد اختباره.** |
| أثر توحيد المفتاح على بقية الحراس | 16 رفضًا صحيحًا لقيم غير قانونية بثمانية إعدادات وبصيغ أصلية وكبيرة مع مسافة: الشهر، التحقق بخطوتين، أيام الراحة، السماحية، المرن، مفتاح الجهاز، الإضافي، بريد الشركة. القيم المخزنة لم تتغير. الكتابة القانونية بصيغة `SYSTEM.COUNTRY ` خزنت المفتاح القانوني والقيمة `EG`. حامل صلاحية الرخصة نجح في تعديلها؛ حسابا النطاق المقيد والحساب بلا الصلاحية رُفضوا. | **سليم في الحالات المشغّلة.** |
| **CR4-B02: بطاقة التفاصيل** | بعد النقل حُجبت الحقول التنظيمية الخمسة عن خمسة حسابات، مع بقاء الاسم والكود. صاحب الطلب وحساب A+C ومدير النظام رأوا التنظيم الصحيح. الطلب السري حجب البطاقة والمنشئ عن غير طرفه. | **البطاقة مغلقة؛ عزل المسمى عبر جميع المسارات غير مغلق بسبب CR5-B01.** |
| **N01: تاريخ التعريفات** | SHIFT وWORK_SCHEDULE: تعريف عام وA وB وC، لحساب A+B، وتوكن قديم بفرع A، ومدير النظام، وحساب بلا الصلاحية؛ **32 مقارنة حالة HTTP صحيحة**. التاريخ المحذوف بقي للشركة فقط؛ حُجب عن حساب الفرع بنفس رد المعرّف غير الموجود. | **مغلق.** |
| **N02: 403/404** | الطلب الأجنبي لغير طرفه مطابق للمفقود في الحالة وجسم الرد: 404. هدف الإعفاء الأجنبي مطابق للمفقود: 404 «الموظف غير موجود». داخل النطاق دون صلاحية العرض بقي 403. صاحب الطلب والمنشئ ومن اتخذ قرارًا والمعتمد المسمى احتفظوا بقراءة الطلب خارج النطاق باعتبارهم أطرافًا. | **مغلق في المسارات المحددة.** |
| **N03: تقرير الغياب** | ست حالات محسوبة يدويًا: المباشرة الفعلية، ملف إنهاء مفتوح، ملف ملغى/قبل إعادة التعيين مع الأرشفة، أولوية الإنهاء على الأرشفة، أبكر ملف صالح، والمنتهي بلا تاريخ. الغياب المتوقع والفعلي: **9، 6، 6، 9، 6، 11**. ظل يوم الحضور الحقيقي و480 دقيقة ظاهرين، وتطابق الشهر والمدى. | **مغلق في الحالات المشغّلة.** |

اختبرت أيضًا الأرشفة قرب منتصف الليل: اللحظة `2026-09-10T22:30:00Z` تساوي 11 سبتمبر بتوقيت القاهرة. أعاد `employmentWindowOf` وAPI حدود الخدمة وSQL التاريخ نفسه، واحتفظ التقرير بغياب آخر اليوم المشمول: **1 متوقع و1 فعلي**. لم تثبت شبهة اختلاف المنطقة الزمنية، ولذلك ليست عيبًا في التقرير.

**إعادة الجولة الرابعة:** نسخت ملفاتها إلى بادئة الجولة الخامسة وعدّلت اختبار الاستغلال ليؤكد الحجب والقيمة المحفوظة وحالة المسير، وشددت تحقق حجب حقول البطاقة. النتيجة **15/15**. شملت أيضًا أثر HR المالي **123.45 مرة واحدة** رغم إعادة الإرسال، وفصل مهام صاحب إعفاء الحضور، وأسبقية العطلات ونسخ الجدول، وتراجع معاملة الفئة عند الفشل وتزامن التخصيص. [النتائج](api/test/codex-review-round5-results-adversarial.cjs).

**الاختبارات الإضافية:** ملف الحدود **5/6**؛ الوحيد الفاشل في صورته النهائية هو تسريب الصندوق المثبت أعلاه. أول تشغيل احتوى أخطاء تجهيز مني، أصلحتها في ملف اختباري فقط: خطوة سلسلة مفقودة، دور غير صحيح للمعتمد المسمى، وحقول وردية إلزامية ناقصة. لم أسجل هذه الأخطاء كعيوب منتج. [النتيجة النهائية](api/test/codex-review-round5-results-boundaries-final.cjs).

**اختبارات المنتج المتأثرة التي أعدتها:** [النتيجة المجمعة](api/test/codex-review-round5-results-affected.cjs).

| المجموعة | النتيجة |
|---|---:|
| attendance-employment-window | 6/6 |
| attendance-exemption-workflow | 23/23 |
| leave-contract | 15/15 |
| request-category-chains | 12/12 |
| user-branch-scopes | 10/10 |
| recovery | 33/36؛ حدود PDF موضحة أدناه |
| fulltest-settings، بنسخة توجه دليلها إلى api/test | 39/39 سيناريو Node؛ **359/360 تحقق داخلي** كما سبق |

**الواجهة:** شغّلت تصيير React للبطاقة: إظهار التنظيم المسموح، وإخفاء الحقول الخمسة مع الملاحظة، وعدم عرض بطاقة الطلب السري؛ **3/3**. ومع النسخة المكيّفة من اختبار منشئ الطلبات **14/14**. هذا تصيير للمكوّن، وليس جولة متصفح. [النتائج](api/test/codex-review-round5-results-local.cjs).

**قرأته في الكود:** مسار الإعدادات يستعمل المفتاح المخزّن قبل فحوص الصلاحيات والقيم وفروع الكتابة المؤرخة. بحثت استعمالات مفاتيح الإعداد الآتية من المدخلات في API. راجعت مستهلكي تفاصيل الطلب والإعفاءات في الواجهة؛ عرض الخطأ فيها عام ولا يعتمد على 403 وحده، ومعالجة 403 الخاصة في عميل API تخص تغيير كلمة المرور برمز مستقل. لا أنسب لهذه القراءة وحدها نجاح تفاعل متصفح كامل.

**الفحص الساكن:** `tsc --noEmit --incremental false -p tsconfig.json` نجح في `api/` والجذر. تشغيل الوحدة المستقل شمل 135 ملفًا و1540 اختبارًا، منه اختبارات الجولة الأولى، مع تفاصيل الفشل النصي وإعادته أعلاه. لم أشغّل ملفات الوحدة الـ25 المستبعدة من مشغّل المراجعة لعزل الملفات/الترحيلات/اتصالات الأجهزة؛ قائمتها محفوظة في النتيجة. لا أدعي إعادة رقم 1763 كاملًا.

**الترحيلات:** لا ترحيل جديد في فرق الجولة الخامسة. أعدت تجربة 068 و069 على قاعدة مؤقتة فقط، مرتين لكل نص: **فرق الكيانات صفر، وعدد الصفوف ثابت، بلا تعبئة رجعية**. نجحت كذلك تجربة 068 ضمن user-branch-scopes. المحرك الفعلي **SQL Server 2022 Express 16.0.4255.1**، والتطبيق على مستوى توافق **150**؛ لا أسمي ذلك تشغيلًا على محرك 2019.

## 5. ما لم أستطع فحصه وحدود الحكم

- لم أعد دورة 500 موظف أو قياسات الأداء، ولم أعد جميع دورات الجولات السابقة؛ النطاق هنا الإصلاحات وأثرها الوظيفي والأمني.
- ثلاث حالات في recovery تتعلق بإصدار/معاينة PDF لم تنجح. الأولى أعادت خطأ Puppeteer بأن ملف تعريف المتصفح مستخدم؛ الثانية أعادت 500 بدل 201، والثالثة تعذر تنفيذ وجهة الطلب. لم أثبت السبب المستقل للأخيرتين، ولم أصنفها كتراجع في هذا الفرق؛ ملفات الخطابات ليست ضمن التغيير. لم أوقف متصفحًا أو خدمة لمحاولة تجاوز المشكلة، ولا أدعي نجاح إصدار PDF في هذه الجولة.
- لا اختبار متصفح تفاعلي كامل، ولا اختبار على SQL Server 2019 الفعلي، ولا اتصال LDAP/SMTP حقيقي. لم أستعمل نتيجة التكامل المتزامن للمالك باعتبارها نتيجة تشغيل مني.

### العزل وحفظ الأدلة

أعدت قراءة الـbrief وطبقت توضيح المالك الذي يسمح بالترحيل **على قواعد المراجع المؤقتة فقط**. استعملت قواعد عشوائية وحارسًا يتحقق من الاسم قبل إنشاء القاعدة ومن قاعدة اتصال TypeORM، مع منع مراجع قواعد الشركة في أوامر SQL الاختبارية. جميع طلبات HTTP مرت بحراس Nest والـpipes والـcontrollers داخل العملية؛ لا API مستمع ولا خدمة جديدة. البريد والمجال والمزامنة المجدولة محجوبة في مشغّل المراجعة.

أنشأت **16 قاعدة مؤقتة** عبر المحاولات والتشغيل المرجعي؛ سجّل المشغّل حذفها جميعًا. ثم فتحت اتصالًا مستقلًا بـmaster وطلبت كل اسم على حدة من `sys.databases`: **16/16 غير موجودة**. لم أفتح التطبيق على قاعدة شركة، ولم أكتب أو أرحّل على `hr_system` أو `hr_review_pre_payroll*`، ولم ألمس قواعد تشغيل التكامل الموازي.

كل ملفات المراجعة الجديدة تحت `api/test/codex-review-round5-*` عدا هذا التقرير. أدلة الجولة الرابعة الأصلية بقيت بنفس بصماتها. ملفات المنتج والترحيلات مطابقة لـ`d352f40`، ولا تعديل منتج مني. رأيت تغييرات اختبارية متزامنة من طرف آخر ولم أتراجع عنها أو أنسبها إلى عملي. فحص الأسرار في ملفات الجولة والتقرير لم يجد مطابقة لقيم البيئة الحساسة أو JWT. [دليل النطاق](api/test/codex-review-round5-scope-evidence.cjs)، [التنظيف والتحقق النهائي](api/test/codex-review-round5-final-audit-results.cjs).

لإعادة الحالة الحاجزة وحدها من PowerShell في جذر المشروع، على قاعدة مؤقتة يحذفها الاختبار:

```powershell
$env:REVIEW_CONCURRENCY='1'
$env:REVIEW_RESULT_SUFFIX='recheck-inbox'
$env:REVIEW_PATTERN='transferred employee current title'
Remove-Item Env:REVIEW_BASELINE -ErrorAction SilentlyContinue
node api/test/codex-review-round5-suite.cjs codex-review-round5-boundaries
```

ولإعادة اختبارات الجولة الرابعة المعدلة للحجب:

```powershell
Remove-Item Env:REVIEW_PATTERN -ErrorAction SilentlyContinue
$env:REVIEW_RESULT_SUFFIX='recheck-adversarial'
node api/test/codex-review-round5-suite.cjs codex-review-round5-independent codex-review-round5-edges codex-review-round5-migrations
```
