# مراجعة نظامنا — من الداشبورد لحد ما قبل الرواتب

**النطاق:** الداشبورد · الموظفين · الحضور · الإجازات · الإعدادات · الطلبات والاعتمادات — **المستثنى:** الرواتب · الأداء · التوظيف · التدريب — **الطريقة:** قراءة الكود (شجرة العمل الحالية بالتعديلات غير المعمول لها commit) + قاعدة البيانات قراءةً فقط + مراجع ثانٍ خصامي لكل منطقة. ماحصلش أي تسجيل دخول ولا أي كتابة على الداتا. التاريخ: 2026-09-10.

## الحكم العام في ثلاثة أسطر
1. **المحرّكات حقيقية ومبنية كويس:** الـ state machine بتاع الطلبات، والتحقق وقت التقديم، ومحرك الحضور اللي بيعكس الإجازة والإذن والعطل الممتدة، وإلغاء HR للإجازة. لكن ادعاء «61 شاشة مربوطة» اتنقض جزئيًا: من 69 شاشة/ويدجت راجعناها فيه 29 بس نظيفة بالكامل، والباقي فيه أجزاء ديمو أو أزرار حفظ ميتة. الشاشة الرئيسية لـ 73 موظف ديمو كاملة، وكمان «إنهاء الخدمة» و«أيام العمل». واتنقض 10 ادعاءات من التقارير السابقة (التفاصيل في كل منطقة).
2. **الحاجات اللي هتدخل المسير النهارده مش موثوقة.** الحضور ماشي على وردية افتراضية صامتة، ونوافذ البصمة بتُسقط التأخير، و96% من بصمات الجهاز يتيمة. الإجازة 84 من 86 مستخدم مايقدروش يقدّموها، والأرصدة من غير ترحيل سنوي فمن 2027-01-01 الإجازة هتبقى بلا حد وبلا خصم. وفيه 22 نوع طلب نشط بيتعتمد من غير أي أثر، وطلبات عالقة في APPROVED للأبد.
3. **فيه ثغرات أمان حرجة:** HR يقدر يمنح نفسه `*`، وفيه IDOR على `GET /requests/:id`، و`GET /offboarding/:id` بيكشف الراتب والـ IBAN لأي مستخدم مسجّل. وكمان كل الـ cron jobs من غير catch-up، فعندنا 4 موظفين إنهاء خدمتهم عدّى ولسه نشطين وبيبصموا، ونقل متأخر 9 أيام، وعقد منتهي لسه نشط. **الحكم: مانبدأش الرواتب قبل البنود 1–12 تحت.**

## الصورة بالأرقام
| المنطقة | الحكم في سطر | الشاشات (حقيقية/الإجمالي) | ديمو أو ناقص | 🔴 | 🟠 | 🟡 |
|---|---|---|---|---|---|---|
| الداشبورد والتقويم والإشعارات | الواجهة شكلها كامل والحقيقي منها قليل. لوحة الموظف (رئيسية 73 مستخدم) و5 ويدجتات أرقامها مكتوبة في الكود، وكروت `/dashboard/stats` بتحسب غلط | 6/14 | 8 | 3 | 8 | 7 |
| الموظفين (الملف، العقود، المستندات، العهد، النقل، الأرشيف، إنهاء الخدمة، إخلاء الطرف) | 13 شاشة مربوطة بالـ API والتحقق في الباك كويس، لكن «إنهاء الخدمة» ديمو (alert)، والإنهاء والنقل المجدولين عالقين عشان الـ cron مالوش catch-up، والـ offboarding مكشوف | 1/15 ¹ | 14 | 4 | 9 | 10 |
| الحضور والانصراف | المحرك حقيقي ومتصل، لكن 3 عيوب في الأساس (وردية افتراضية، نوافذ البصمة، البصمات اليتيمة) بتخلّي أرقام 171 موظف نشط مش موثوقة | 1/10 ¹ | 9 | 3 | 7 | 10 |
| الإجازات | الشاشات السبع موصولة وحساب أيام العمل والعطل صح، لكن 84/86 مايقدروش يقدّموا، ومفيش ترحيل سنوي، وفيه سحب على المكشوف وإرجاع رصيد مزدوج | 5/7 | 2 | 2 | 5 | 12 |
| الإعدادات | 13 من 17 شاشة حقيقية، والهب وأيام العمل وقوالب المستندات ديمو. وفيه تصعيد صلاحيات لـ `*`، و22 نوع طلب من غير أثر، وتعطيل السلسلة العامة شكلي | 13/17 | 4 | 5 | 7 | 14 |
| الطلبات والاعتمادات والخدمة الذاتية | المحرّك حقيقي، لكن الإجازة مقفولة على 84 مستخدم، وفيه طلبات عالقة في APPROVED وIDOR، وكل السلاسل خطوة `hr` واحدة | 3/6 | 3 | 5 | 9 | 8 |
| **الإجمالي** | | **29/69** | **40** | **22** | **45** | **61** |

¹ «حقيقية» يعني مربوطة بالـ API **ومن غير** أي جزء ديمو أو زر ميت. في الموظفين 13 من 15 شاشة مربوطة فعلًا، وفي الحضور المحرك كله متصل، بس أغلب الشاشات فيها بقايا ديمو أو أزرار ميتة، فاتحسبت «ناقص».
ملاحظة: الإجمالي فيه تكرار بين المناطق، يعني نفس العيب اتسجّل في أكتر من منطقة. أمثلة: 403 أنواع الإجازة (الإجازات + الطلبات)، والطلبات العالقة وإرجاع الرصيد المزدوج (الإجازات + الطلبات)، وانتهاء العقود (الداشبورد + الموظفين)، والأنواع اللي من غير أثر (الإعدادات + الطلبات). فعدد العيوب الحرجة المستقلة أقل شوية من 22، في حدود 20.

## المطلوب قبل ما نبدأ الرواتب (مرتّب بالأولوية)

**أ) حواجز بتغذّي المسير مباشرة، ولازم تتقفل الأول**

1. **الوردية الافتراضية الصامتة.** نشيل `DEFAULT_SHIFT` الثابت (`attendance.service.ts:31,528`) ونستخدم جدول العمل اللي عليه `isDefault`. اليوم اللي من غير وردية يتعلّم `unscheduled`، من غير تأخير ولا إضافي، ومعاه تنبيه لـ HR. الافتراض ده طلّع 3005 يوم، منهم 2906 غياب لـ 171 موظف، ولازم يتعاد حسابهم. ومعاها نستبدل الجداول المكتوبة في الكود في `/settings/work-days` بـ CRUD حقيقي على `/catalogs/work-schedules` مع تعيين فعلي للموظفين.
2. **نوافذ البصمة بتُسقط التأخير.** في `attendance.service.ts:945-958,1078-1081`: أول بصمة قبل نافذة الخروج تتحسب دخول متأخر، واليوم مايتحسبش «حاضر» من غير دخول، والبصمات اللي برا النوافذ تتعلّم للمراجعة بدل ما تتجاهل.
3. **96% من بصمات الجهاز يتيمة.** المطابقة في ingest تبقى بـ `fingerprintCode` وبعده `employeeCode` (`attendance.service.ts:442-446`)، ونعمل backfill لـ 7452 بصمة يتيمة، ونضيف لوحة للأكواد اللي مش مربوطة في `/attendance/devices`. ونرفض البصمات المستقبلية (فيه 91 بصمة لحد 2026-12-20).
4. **الإجازة مقفولة على 84 من 86 مستخدم.** نعمل endpoint قراءة لأنواع الإجازة من غير `settings.manage` (مثلًا `GET /requests/leave-types`) ونستخدمه في `/requests` و`/leaves/request` (`settings.controller.ts:351-353,409-412`).
5. **الأرصدة من غير ترحيل سنوي.** نعمل cron يوم 1 يناير يشغّل الترحيل وينشئ أرصدة السنة لكل الموظفين النشطين. ولو صف الرصيد مش موجود لنوع سنوي أو مرضي، يرمي خطأ بدل ما يعدّي فحص الحد والخصم في صمت (`leave-balances.service.ts:134-138,160,180`).
6. **سلامة الرصيد.** نحجز أيام الطلبات المعلّقة في `assertSufficient`، ونعيد الفحص جوه معاملة الخصم، ونبطّل قصّ المتبقي عند 0 (`requests.service.ts:494-514`, `leave-balances.service.ts:112-113`). وفي `leaveRestoreHandler` نشترط `status==='APPROVED'` ونمنع طلب إلغاء مكرر لنفس الإجازة. ونرفع حد اللف 92/62 يوم عشان الإجازات المرضية وإجازة الوضع الطويلة تتحسب كاملة (`destinations.service.ts:144-173`, `attendance.service.ts:137-141`, `requests.service.ts:759-766`).
7. **الاعتماد والتنفيذ في معاملة واحدة.** APPROVED لازم يتحفظ مع `executeDestination` في نفس المعاملة، أو يترجع لو التنفيذ فشل، ويبقى فيه endpoint لإعادة التنفيذ. ونصلّح العالقين 292 و295 و303 (`requests.service.ts:706-735`).
8. **أنواع طلبات بتتعتمد من غير أثر.** فيه 22 نوع نشط الـ handler بتاعه مش متنفّذ، ومنهم 15 طلب اكتملوا فعلًا من غير أي أثر (`destinations.service.ts:71-78,720-756`). المطلوب: المأمورية والعمل عن بعد يبقوا حالة حضور، و`TITLE_CHANGE` يحدّث المسمى، و`CONTRACT_RENEWAL` يمد العقد، و`EARLY_LOAN_SETTLEMENT` يقفل الأقساط بدل ما يعمل سلفة بمبلغ NaN. وأي نوع مالوش handler يتقفل لحد ما يتبني.
9. **الإذن من غير نوع.** `permissionType` يبقى إلزامي ويتخزن بالـ id، والإذن اللي من غير نوع يتحسب «بخصم» (`attendance.service.ts:826-834`).
10. **الـ cron jobs من غير catch-up.** نشغّل `finalizeDue` (`offboarding.service.ts:557-565`) و`runScheduledTransfers` (`requests.service.ts:1160-1184`) عند بدء التشغيل. ونصلّح حالات SETTLED العالقة 3 و4 و5 و11 (موظفين عدّى آخر يوم عمل ليهم ولسه نشطين) والنقل المتأخر #3. ونوسّع نافذة تسجيل الغياب لأكتر من يومين (`attendance-scheduler.service.ts:16-24`).
11. **العقود مش بتنتهي.** نعمل job يومي يعلّم العقود المنتهية (الموظف 19 نشط وعقده انتهى 2026-07-31) ويبعت تنبيه قبل الانتهاء، ونضيف سجل تجديدات.
12. **معادلة مكافأة نهاية الخدمة** (`offboarding.service.ts:237-241`): نص شهر عن كل سنة من أول 5 سنين، وشهر كامل بعدها، مع تخفيض الاستقالة حسب المادتين 84/85.

**ب) أمان وصلاحيات**

13. نرفض `*` في GRANT overrides للمستخدم ونمنع إن حد يعدّل صلاحياته هو (`roles.controller.ts:66,172-184`).
14. `GET /requests/:id` يتقفل على المالك أو المنشئ أو المعتمد أو `requests.view_all` (`requests.service.ts:1201-1210`). و`GET /offboarding/:id` يتقفل بصلاحية أو ملكية، ويبطّل يرجّع صف الموظف كامل بالراتب والـ IBAN ورقم الهوية (`offboarding.controller.ts:75-79`).
15. نطاق الفرع ومنع الاعتماد الذاتي على كل عمليات الكتابة: org وusers (`org.controller.ts:40-95`, `users.controller.ts:188-227`)، و`ingestManual` و`upsertSchedule` و`setDayOverride` و`confirmOvertime`. ونتحقق من body الـ PATCH في الكتالوجات (`catalogs.controller.ts:139-150`)، ونضيف source/createdBy/reason للإدخال اليدوي.
16. نغيّر `attendance.device_key` من القيمة الافتراضية `zk-device-key-change-me`، لأن `POST /attendance/punches` بيقبله من غير مصادقة.
17. **سلاسل الاعتماد.** خطوة مدير مباشر وبعدها `hr`، وHR أو `approve.hr` في كل فرع. حاليًا 67 سلسلة خطوة `hr` واحدة، والمعتمد الوحيد hr_manager في الفرع 1. ولازم `resolveChain` يحترم `chain.isActive` للسلاسل العامة (`requests.service.ts:555-558`).
18. **فجوات صلاحيات الواجهة.** رابط «إضافة موظف» و`/employees/add` يتحرسوا بـ `employees.create`، وQuickActions تتفلتر بـ `can()`، وأزرار التعديل والأرشفة تستخبى عن اللي مالوش صلاحيتها. واختيار لوحة الداشبورد يبقى بـ `dashboard.view_all` بدل `role==='employee'` (`src/app/page.tsx:23-26`). وأطراف إخلاء الطرف لازم يقدروا يفتحوا `/offboarding/[id]` (`MainLayout.tsx:21`). وصلاحية «أنواع العهد» تتوحّد مع `custody.assign`.

**ج) سلامة الدورات والبيانات**

19. **صفحة إنهاء الخدمة.** `src/app/employees/[id]/terminate/page.tsx` ديمو، فإما نبنيها بإنهاء حقيقي من طرف الشركة (`POST /offboarding` يفتح حالة من بيانات الموظف والعهد والسلف والأرصدة الحقيقية)، أو نشيل الرابط لحد ما تتبني.
20. **فورم الموظف.** لما حقل يتفضّى في التعديل يتبعت `null` (`EmployeeForm.tsx:640-731`)، والأرشفة تكتب صف في السجل (`employees.service.ts:334-344`)، والمسميات والدرجات تيجي من الكتالوجات بدل القوائم الثابتة. وملفي يعرض العملة والبدلات.
21. **شاشة أنواع الطلبات.** تعديل أي نوع من الـ 31 نوع المبذورين بيرجع 400، والحل إن الشاشة ماتبعتش `destinationHandler` لو ما اتغيّرش (`settings/request-types/page.tsx:278,371`).
22. **المسودات اليتيمة.** نتحقق قبل الإدخال في `create()` أو نلف create+submit في معاملة واحدة، لأن فيه 61 مسودة يتيمة.
23. **صندوق الاعتماد.** المعتمد يشوف الـ payload كامل بتسميات عربية ومعاه المرفقات، ويقدر يعدّل الـ payload في «استكمال وإعادة إرسال».

**د) الداشبورد والإشعارات (مابتمنعش الرواتب، بس الواجهة الأساسية ليها بتكدب)**

24. **`/dashboard/stats`** (`dashboard.controller.ts`): تاريخ محلي بدل UTC (:19). الحاضر = present + late + early_leave (:43). الغياب يتحسب لحظيًا (:46). الإجمالي = النشطين بس (:21-26). «في إجازة» بفلتر الفرع، والإجازات الكاملة بس، وبـ COUNT DISTINCT (:49-52). كارت المعلّق يساوي `inbox(user).length` (:35).
25. **`EmployeeHome`**: نربطها بـ `/requests/leave-balances/mine` و`/requests/mine` وحضور اليوم، ونشيل التحية والبصمة والأرصدة والطلبات والمهام المزيفة.
26. **الويدجتات الثابتة** (AttendanceChart وDepartmentStats وRecentActivities وUpcomingEvents): نربطها بـ endpoints حقيقية أو نخبيها.
27. **الإشعارات.** حالة القراءة تتحفظ (جدول + `PATCH /notifications/read`)، وعدّاد الهيدر يعدّ غير المقروء بس. ونشيل عدّاد الرسائل المزيف «3»، ونربط البحث والإعدادات في الهيدر أو نشيلهم (`Header.tsx:68,185-195`).

## تنظيف البيانات التجريبية

> ترتيب التنفيذ: التنظيف يحصل بسكريبت واحد مراجَع، **بعد** إصلاح البنود 1–3 و7، وقبل أول مسير. بعض بيانات الاختبار دي بتأثّر على حسابات حقيقية دلوقتي، زي العطلة #10 اللي بتغيّر عدد أيام إجازات وحضور حقيقي.

**في الكود (ديمو بيتعرض كأنه حقيقي)**
- **الداشبورد:** `EmployeeHome.tsx:22-125,134-146,254-255` (التحية «أحمد»، وبصمة 09:52، والطلبات REQ-1042/1029/1017، والأرصدة، والإعلانات والمهام). و`AttendanceChart.tsx:13-21` (~220 حاضر في اليوم). و`UpcomingEvents.tsx:13-49` (أحداث فبراير 2026 و«يوم التأسيس»). و`DepartmentStats.tsx:13-21` (7 أقسام مجموعها 248 موظف). و`RecentActivities.tsx:22-69`. و`Header.tsx:185-190` (عدّاد رسائل «3»).
- **الموظفين:** `employees/[id]/terminate/page.tsx:30-44,77-83,187-194` (الموظف «أحمد محمد السعيد» EMP001 وأرقامه، و5 عهد مزيفة، وalert عند التأكيد). و`employees/[id]/page.tsx:481-600` (قوالب مستندات باسم «شركة التقنية المتقدمة» وسجل تجاري 1010123456). و`EmployeeForm.tsx:236-238` (مسميات وجنسيات وبنوك ثابتة)، و`:1164-1166` («الشركة الرئيسية» مش مربوطة)، و`:1225-1232` (Grade 1..5)، و`:2341` («حفظ كمسودة» ميت)، و`:1102,:1604` (تلميح «اتركه فارغاً للإنشاء التلقائي» غلط). و`employees/onboarding/page.tsx:58-67` (قائمة استلام محلية).
- **الحضور:** `manual-entry/page.tsx:52,100-135` (جدول في حالة الجلسة بس، بحالة approved ثابتة، وكروت دايمًا صفر). و`attendance/page.tsx:488-492,520-541` (عمود «التحقق» 👆 ثابت، ودليل طرق تحقق مش متتبَّعة). و`permissions/page.tsx:404-433` (كروت أنواع الأذونات ثابتة). و`overtime/page.tsx:75,172-177` («يتطلب تأكيداً» محلي، والإحصاءات من قائمة DETECTED فاضية). و`weekly-schedule/page.tsx:195,606-614` (WEEKEND_DAYS ثابت، والساعات ×5). و`src/lib/attendance.ts:12,37` (نسخة DEFAULT_SHIFT ميتة).
- **أزرار ميتة:** `attendance/page.tsx:227-230,507-515`، و`attendance/reports/page.tsx:98-105`، و`shifts/page.tsx:417-431`، و`weekly-schedule/page.tsx:796-804`، و`manual-entry/page.tsx:375-380`.
- **الإعدادات:** `settings/page.tsx:392-453,629-656,730,810-815,849-857,1038-1045` (بيانات الشركة، وأعداد الأدوار، و2FA، وشرائح التأخير، والإجازات، والتكاملات). و`settings/work-days/page.tsx:239-304,2085-2102` (جداول وموظفين وأقسام مزيفة). و`settings/document-templates/page.tsx:164-244` (قوالب من غير باك).
- **الطلبات والإجازات:** `src/data/requestsCatalog.ts` (كتالوج ثابت بيتعرض كسلسلة ووجهة حقيقية في `requests/page.tsx:1278-1283` و`requests-console/page.tsx:429,452-500`). و`approvals-inbox/page.tsx:216` (مهلة رد مزيفة `?? 3`). وخرائط أسماء وألوان أنواع الإجازة الثابتة في `/leaves` و`/leaves/calendar` و`/my/leaves` و`/leaves/request`، والمفروض تيجي من الكتالوج.

**في قاعدة البيانات**
- **حضور مستقبلي:** صفوف `attendance_days` من 2026-09-12 لحد 2026-12-20 (32 حاضر، و6 متأخر، و26 إجازة، و5 عطلة، وcomputedAt 2026-07-12، مثلًا ids 130 و137)، و91 بصمة مستقبلية.
- **موظفين ومستخدمين:** 27 موظف اختبار (S3AUD، S6-*، S7 Audit A/B، S8، S10، S11 Payroll Test، «اختبار سيناريو كريم PX49594»). والمستخدمين 30 و32 (دور employee من غير موظف، ومعاهم `approve.payroll`).
- **الأدوار:** `pr_test_42103`، و`payroll_officer` (دور مخصص ماينفعش يتعيّن).
- **الهيكل:** الفروع 4–11 بأكواد اختبار (ABS12007، OBL62816، PR91686، SCP48298…). والأقسام 3 و4 و5 (قسم الاختبار SETUP-B، وقسم1/قسم2 SCP48298). ومراكز التكلفة CC-PI995/CC-PI36048. والفرع 1 مركز تكلفته `CC-100` ومش موجود في الكتالوج.
- **الحضور والإعدادات:** ورديات SC53685/SC43059/SC49225/AUDMORN45/S11SHIFT_K1/HOT34501/PR91686. وجدول العمل «جدول WS85620». وأنواع الأذونات: ids 4–12 (معطّلة)، والنشطة 14 و16 و17 و18 و19 (زي «مسائي KAR18391» و«صباحي اودت45»)، وأكواد PI995/PB9388/PB76007/BF79000. ونوع العهدة «لابتوب 2». و`requests_config.attendance.device_key` لسه على القيمة الافتراضية.
- **العطل:** `public_holidays` #10 «اختبار كلود عطلة ممتدة» (2026-10-12 → 10-14).
- **الطلبات والإجازات:** الطلب العالق #303 والإجازة اليتيمة #72 (موظف 178، `requestId` NULL). وطلبات الاختبار #288–#290 و#328–#331 و#344 (DRAFT) و#345 (إلغاء الإجازة #74). و25 طلب الـ payload بتاعه فيه «اختبار كلود» (منها #333 و#334 و#337 و#338 و#346 و#347). وطلبات «نوع وهمي» #45 و#56. و61 مسودة يتيمة (منها 34 إجازة). وأنواع CUSTOM_62..66 (2 معطّلين و3 محذوفين وليهم طلبات يتيمة). وتحويلات الاختبار المكررة #4 و#5 (الطلبات 286 و287).
- **السلاسل والخطابات:** السلسلة 97 `CH_LEAVE_COMPENSATORY` (فاضية ومعطّلة، فرع 1)، والسلسلة 98 `CHAIN_CY5865` (مش مربوطة). و11 صف في `letter_requests` بمرجع PDF حاجز من غير ملف.

---

## 1) الداشبورد الرئيسية والتقويم والإشعارات
**الحكم في سطر:** الواجهة شكلها كامل، لكن الحقيقي منها قليل. من 14 شاشة/ويدجت فيه 6 بس بياناتها من الـ API. خمس ويدجتات في لوحة الإدارة ولوحة الموظف كلها (الشاشة الرئيسية لـ 73 من 86 مستخدم) أرقامها مكتوبة في الكود. وحتى الكروت اللي بتقرا من `/dashboard/stats` بتحسب الحاضرين والغائبين والإجمالي و«اليوم» غلط. وقراءة/حذف الإشعارات بيتغيروا في الشاشة بس ومش بيتحفظوا.

**الشاشات**
| الشاشة | مصدر البيانات (API / ثابت / مختلط) | الحفظ (API / محلي / لا يوجد) | الحالة ✅/🟡/🔴 | ملاحظة |
|---|---|---|---|---|
| `/` هيكل الصفحة + مبدّل الدور (`src/app/page.tsx`) | محلي (`getCurrentUser` من localStorage) | لا يوجد | 🟡 | بيتحوّل للوحة الموظف لو الدور `employee` بالظبط وبس (`src/app/page.tsx:23-26`). أي دور تاني بيشوف لوحة الإدارة. زرار «لوحة الإدارة/لوحة الموظف» ظاهر لكل المستخدمين (`src/app/page.tsx:36-59`). |
| StatsCards (`src/components/dashboard/StatsCards.tsx`) | API: `GET /dashboard/stats` (`StatsCards.tsx:30`، `src/lib/api.ts:249`) | لا يوجد | 🟡 | الـ 6 كروت بيانات حقيقية وفيها حالة تحميل وحالة خطأ (`StatsCards.tsx:45-55`). المشكلة في طريقة حساب الحاضرين والغائبين والإجمالي وتاريخ اليوم في الـ API (شوف المنطق). |
| QuickActions (`src/components/dashboard/QuickActions.tsx`) | ثابت (8 روابط تنقّل) | لا يوجد | 🟡 | الروابط الـ 8 كلها بترجع 200. مش متفلترة بالصلاحية (`QuickActions.tsx:24-89`). «طلب إجازة» بيودّي على `/leaves` وهي قائمة الطلبات مش شاشة التقديم (`QuickActions.tsx:45`). |
| AttendanceChart (`src/components/dashboard/AttendanceChart.tsx`) | ثابت | لا يوجد | 🔴 | `weekData` مكتوبة في الكود بحوالي 220 حاضر في اليوم (`AttendanceChart.tsx:13-21`)، والشركة كلها 178 موظف. زرار «شهري» بيغيّر `period` والرسم مش بيقراها (`AttendanceChart.tsx:24`). وفيه TODO صريح (`AttendanceChart.tsx:3`). |
| UpcomingEvents (`src/components/dashboard/UpcomingEvents.tsx`) | ثابت | لا يوجد | 🔴 | أحداث وهمية بتاريخ فبراير 2026 وعدّاد أيام ثابت (`UpcomingEvents.tsx:13-49`). فيها «يوم التأسيس» السعودي، مع إن كل العطلات في الـ DB `country=EG`. زرار «عرض التقويم» ملوش handler (`UpcomingEvents.tsx:73-75`). |
| PendingApprovals (`src/components/dashboard/PendingApprovals.tsx`) | API: `/requests/inbox` + `/requests/types` + `/employees` (`PendingApprovals.tsx:54`) | API: `POST /requests/:id/act` (`PendingApprovals.tsx:76`) | ✅ | صندوق الوارد حقيقي، والاعتماد/الرفض طلب HTTP فعلي وبعده بيعيد تحميل الصندوق، وأخطاء السيرفر بتظهر (`PendingApprovals.tsx:104`). ملاحظات UX: الرفض بضغطة واحدة من غير تأكيد ولا سبب، وملخّص الـ payload بمفاتيح JSON إنجليزي (`PendingApprovals.tsx:30-42`). |
| DepartmentStats (`src/components/dashboard/DepartmentStats.tsx`) | ثابت | لا يوجد | 🔴 | 7 أقسام مجموعها 248 موظف (`DepartmentStats.tsx:13-21`). في الحقيقة فيه 4 أقسام، والموارد البشرية لوحدها 147، و27 موظف من غير قسم. زرار «عرض التفاصيل» ملوش handler (`DepartmentStats.tsx:28-30`). |
| RecentActivities (`src/components/dashboard/RecentActivities.tsx`) | ثابت | لا يوجد | 🔴 | 6 نشاطات مخترعة بأسماء وأوقات (`RecentActivities.tsx:22-69`)، واحد منهم «اعتماد مسير يناير 2026». `GET /api/dashboard/activities` بيرجع 404. «عرض الكل» ملوش handler (`RecentActivities.tsx:97-99`). |
| لوحة الموظف EmployeeHome (`src/components/dashboard/EmployeeHome.tsx`) | ثابت بالكامل (مفيش أي import من `@/lib/api`) | لا يوجد | 🔴 | «صباح الخير، أحمد»، ووردية 10–7، وبصمة 09:52 (`EmployeeHome.tsx:134-146`)، ومهام روابطها `#` (`EmployeeHome.tsx:22-63`)، وطلبات REQ-1042 (`EmployeeHome.tsx:84-106`)، وأرصدة 21/8 و30/2 و5/1 (`EmployeeHome.tsx:121-125`)، ورصيد مُرحّل «6 أيام» (`EmployeeHome.tsx:254-255`). زرار «+ طلب جديد» ملوش onClick (`EmployeeHome.tsx:195`). |
| Header (`src/components/layout/Header.tsx`) | مختلط | لا يوجد | 🟡 | قائمة الجرس حقيقية (`Header.tsx:44`). عدّاد الرسائل «3» مكتوب في الكود (`Header.tsx:185-190`). البحث وزرار الإعدادات و«تحديد الكل كمقروء» كلهم مش شغالين. العنوان «لوحة التحكم» ثابت في كل الصفحات (`Header.tsx:83`). |
| Sidebar (`src/components/layout/Sidebar.tsx`) | API: `GET /requests/inbox` (`Sidebar.tsx:228`) | لا يوجد | 🟡 | القائمة مبنية من صلاحيات المستخدم (`Sidebar.tsx:235-290`)، والعدّاد بيعدّ الوارد الفعلي. بس «إضافة موظف» مربوطة بـ `employees.view` بدل `employees.create` (`Sidebar.tsx:69`). |
| `/calendar` (`src/app/calendar/page.tsx`) | API: `GET /calendar?month=` (`calendar/page.tsx:76`) | لا يوجد | 🟡 | العطلات والإجازات المعتمدة حقيقية وبتتعرض على مداها كامل. زراري «تصدير» و«إضافة حدث» ملهمش handler (`calendar/page.tsx:209-216`). زرار «أسبوعي» بيغيّر الشكل بس (`calendar/page.tsx:62,320-327`). |
| `/notifications` (`src/app/notifications/page.tsx`) | API: `GET /notifications` (`notifications/page.tsx:86`) | محلي | 🟡 | القائمة مشتقة من أحداث حقيقية. «مقروء» و«تحديد الكل» و«حذف» بيعدّلوا React state بس (`notifications/page.tsx:115-127`)، فكله بيرجع تاني بعد الـ reload. |
| `/profile` (`src/app/profile/page.tsx`) | API: profile + balances + requests + types + branches + departments + files (`profile/page.tsx:166-188,216`) | لا يوجد | 🟡 | البيانات حقيقية. زرار «تعديل الملف» وزرار الكاميرا ملهمش handler (`profile/page.tsx:288-290,310-313`). الراتب بيظهر من غير عملة ولا بدلات (`profile/page.tsx:451-457`). |

**المنطق والقواعد**
| القاعدة | الحكم ✅/🟡/🔴 | الدليل (`file:line`) | الشرح |
|---|---|---|---|
| «الحاضرون اليوم» = كل من حضر | 🔴 | `api/src/dashboard/dashboard.controller.ts:43`، `StatsCards.tsx:71` | بيعدّ `status='present'` بس، ومش بيدخّل `late` و`early_leave`. يوم 2026-07-25 في الـ DB فيه 5 present و9 late، فالكارت هيقول 5 والحضور الفعلي 14. النهارده 2026-09-10 فيه صف واحد `early_leave`، فالكارت بيقول 0 حاضر مع إن فيه واحد حضر. |
| «غائبون» اليوم = النشطين في يوم عمل من غير بصمة ولا إجازة | 🔴 | `dashboard.controller.ts:46`، `api/src/attendance/attendance-scheduler.service.ts:16-24` | الكارت بيعدّ صفوف `absent` الموجودة بس، وصفوف الغياب مش بتتعمل لليوم الحالي أصلًا. الـ cron الساعة 01:00 بيعمل أمس وأول أمس بس. النهارده فيه 175 موظف `isActive=1` وصف حضور واحد، والكارت بيقول 0 غائب. |
| الغياب يتخزّن صف لكل يوم عمل | 🟡 | `attendance-scheduler.service.ts:15-24`، `api/src/attendance/attendance.service.ts:589-596`، `api/src/app.module.ts:59` | الـ `ScheduleModule` متسجّل، بس النظرة للخلف يومين بس، فأي ليلة يكون الـ API فيها مقفول بتضيع. الـ DB بيأكد ده: صفوف absent في يونيو 952، يوليو 2572 (اتعملوا من تشغيلات المسير)، أغسطس 1 من 57 صف على 14 يوم، سبتمبر 0. |
| «اليوم» = التاريخ المحلي للشركة | 🔴 | `dashboard.controller.ts:19` | بيستخدم `new Date().toISOString().slice(0,10)` وده تاريخ UTC. السيرفر على Africa/Cairo (offset ‎-180، يعني UTC+3)، فمن 00:00 لـ 03:00 كل ليلة اللوحة بتعرض حضور وإجازات امبارح. محرك الحضور نفسه بيبني التاريخ بالتوقيت المحلي (`attendance.service.ts:550-551`). |
| «إجمالي الموظفين» = الموجودين على رأس العمل | 🟡 | `dashboard.controller.ts:21-26`، `StatsCards.tsx:63` | `COUNT(*)` من غير فلتر حالة. الـ DB فيه 166 active و9 notice_period و2 archived و1 terminated، فالكارت بيقول 178 والصح 175. |
| «في إجازة» = إجازات يوم كامل معتمدة، بعدد الموظفين، في نطاق فرع المستخدم | 🟡 | `dashboard.controller.ts:49-52` | ده الاستعلام الوحيد في الدالة من غير فلتر فرع (باقي الاستعلامات بتستخدم `andBranch` في `:17-18`). بيعدّ الإجازات النص يوم كمان (3 MORNING و2 EVENING معتمدين)، وبيعدّ صفوف مش موظفين مختلفين. رقم النهارده 0 وده صح. |
| كارت «طلبات قيد المراجعة» = اللي المستخدم يقدر يتصرّف فيه | 🟡 | `dashboard.controller.ts:35`، `api/src/requests/requests.service.ts:1017-1036`، `api/src/requests/approver-resolver.service.ts:122-127` | الكارت بيعدّ كل الـ UNDER_REVIEW في الفرع، ودول 13 كلهم في فرع 1. ويدجت الوارد في نفس الصفحة بيعدّ الفعلي: hr_manager عنده 5 (282، 283، 297، 298، 301)، ومدير الفرع emp 18 عنده 3 (323-325)، و10 مديري فروع تانيين عندهم 0. نفس الرقم 13 بيطلع كإشعار «13 طلب قيد المراجعة في نطاقك» (`api/src/assets/portal.controller.ts:181-193`). |
| الاعتماد من اللوحة يطبّق الأثر بعد آخر خطوة بس | ✅ | `requests.service.ts:656-711` | سجل التدقيق بيتكتب قبل تغيير الحالة (`:657-668`). الموافقات المتوازية بتستنى بعض (`:689-693`). الانتقال للخطوة اللي بعدها (`:699-704`). `executeDestination` بيتنفّذ بعد الاعتماد النهائي بس (`:706-711`). |
| تنبيه العقود المنتهية / تعليمها منتهية | 🔴 | `api/src/employees/employees.service.ts:180-182`، `api/src/assets/docs.controller.ts:100`، `portal.controller.ts:72-198` | مفيش job ولا إشعار ولا ويدجت بيعلّم العقد إنه انتهى. الموظف 19 (محمد سعيد عبد الله) `contractEnd=2026-07-31` وحالته `active`. مستنداته 4 و5، ومستندات الموظف 9 رقم 7 و9، كلها منتهية، وعلامة `expired` بتتحسب وقت القراءة في شاشة المستندات بس. |
| الأحداث القادمة (عقود، مستندات، ميلاد، عطلات) | 🔴 | `UpcomingEvents.tsx:13-49` | الويدجت كله وهمي. العطلة الحقيقية الجاية (2026-10-06 عيد القوات المسلحة) مش ظاهرة. |
| الحضور حسب القسم والرسم الأسبوعي/الشهري | 🔴 | `DepartmentStats.tsx:13-21`، `AttendanceChart.tsx:13-21` | مفيش endpoint ليهم (`/api/dashboard/attendance-week` بيرجع 404). الأرقام مكتوبة في الكود. |
| العطلة متعددة الأيام تتعرض لحد تاريخ نهايتها | ✅ | `api/src/assets/portal.controller.ts:46-48`، `calendar/page.tsx:146-151` | الـ backend بيضمّ العطلة لو بدايتها أو نهايتها في الشهر، والشبكة بتلوّن كل الأيام من البداية للنهاية (مثلًا عيد الأضحى 26–30 مايو). |
| التقويم يحترم النطاق: الموظف يشوف إجازاته هو بس | ✅ | `portal.controller.ts:39-57` | فيه فلتر فرع، و`calendar.view_all` مطلوبة عشان تشوف إجازات غيرك. |
| مسميات أنواع الإجازة في التقويم | 🟡 | `calendar/page.tsx:42-47,110` | فيه مسميات لـ 4 أكواد بس. الإجازات المعتمدة من نوع HAJJ (5) وCOMPENSATORY (3) وMARRIAGE (1) بتظهر بالكود الإنجليزي. وإحصائية «أيام إجازة» بتجمع `days` كامل للإجازة حتى لو جزء منها في شهر تاني (`calendar/page.tsx:195`). |
| حالة «مقروء» في الإشعارات | 🔴 | `notifications/page.tsx:96,115-127`، `Header.tsx:52,68`، `portal.controller.ts:14` | مفيش جدول إشعارات، وكل عنصر بيوصل `read:false`. العدّاد = عدد العناصر كلها، والقراءة/الحذف بيتغيروا في الـ state بس. `PATCH /api/notifications/read` بيرجع 404. |
| تصنيف الإشعارات | 🟡 | `notifications/page.tsx:71-77,182` | قرارات على طلباتي (`/requests`) بتروح لتصنيف «الموافقات»، وعنصر «موافقات بانتظارك» (`/approvals-inbox`) بيروح لـ«النظام». وفلاتر التدريب والرواتب والحضور عمرها ما هتطابق حاجة، لأن الـ backend مش بيطلع روابط بالشكل ده. |

**ما هو ديمو / بيانات تجريبية**
- لوحة الموظف كلها: `src/components/dashboard/EmployeeHome.tsx:22-125,134-146,254-255`. تحية «أحمد»، وبصمة 09:52، وطلبات REQ-1042/1029/1017، وأرصدة وإعلانات. دي الشاشة الرئيسية لـ 73 مستخدم دورهم `employee`.
- `src/components/dashboard/AttendanceChart.tsx:13-21`: حوالي 220 حاضر في اليوم.
- `src/components/dashboard/UpcomingEvents.tsx:13-49`: أحداث بتاريخ فبراير 2026، و«يوم التأسيس» السعودي.
- `src/components/dashboard/DepartmentStats.tsx:13-21`: 7 أقسام و248 موظف.
- `src/components/dashboard/RecentActivities.tsx:22-69`: 6 نشاطات مخترعة.
- `src/components/layout/Header.tsx:185-190`: عدّاد رسائل «3»، ومفيش backend رسائل خالص (`/api/messages` بيرجع 404).
- جدول `attendance_days` فيه صفوف بتواريخ لسه مجتش: 32 present و6 late و26 leave و5 holiday، من 2026-09-12 لحد 2026-12-20. الصفوف دي `computedAt=2026-07-12` و`checkIn=08:00`، مثلًا id 130 و137 يوم 2026-09-14. هتظهر «حاضرين» في اللوحة لما التواريخ دي تيجي.
- أقسام اختبار في `departments`: «قسم الاختبار SETUP-B» (id 3) و«قسم1/قسم2 SCP48298» (id 4 و5)، والدور `pr_test_42103` في `roles`.

**العيوب**
| # | العيب | الخطورة 🔴/🟠/🟡 | الدليل (`file:line`) | الإصلاح المقترح |
|---|---|---|---|---|
| 1 | الشاشة الرئيسية لكل الموظفين وهمية بالكامل: الأرصدة والطلبات والبصمة والمهام | 🔴 | `EmployeeHome.tsx:84-125,134-146` | اربطها بـ `/requests/leave-balances/mine` و`/requests/mine` (موجودين أصلًا) وسجل حضور اليوم، واشيل المهام والإعلانات الوهمية أو ابنيلها backend. |
| 2 | أربع ويدجتات في لوحة الإدارة بتعرض أرقام مخترعة كأنها بيانات حيّة | 🔴 | `AttendanceChart.tsx:13-21`، `DepartmentStats.tsx:13-21`، `RecentActivities.tsx:22-69`، `UpcomingEvents.tsx:13-49` | ابني endpoints: اتجاه حضور من `attendance_days`، وتجميع بالقسم، وسجل نشاط من `request_approvals`، وأحداث من `contractEnd`/`expiryDate`/`birthDate`/`public_holidays`. أو اخفي الويدجتات لحد ما تتربط. |
| 3 | كارت «غائبون» بيقول 0 كل يوم لأن غياب اليوم مش بيتسجّل، والـ cron بيبص يومين لورا بس | 🔴 | `dashboard.controller.ts:46`، `attendance-scheduler.service.ts:16-24` | احسب الغياب لحظيًا: النشطين في يوم عمل − (صفوف الحضور + الإجازات). ووسّع نظرة الـ cron أو خليه يلحق كل الأيام اللي فاتته من آخر تشغيل. |
| 4 | «الحاضرون» مش بيعدّ المتأخرين واللي انصرفوا بدري | 🟠 | `dashboard.controller.ts:43` | `status IN ('present','late','early_leave','partial_leave')`، أو `checkIn IS NOT NULL`. |
| 5 | «اليوم» محسوب بـ UTC، فمن 00:00 لـ 03:00 بتوقيت القاهرة اللوحة بتعرض امبارح | 🟠 | `dashboard.controller.ts:19` | استخدم نفس دالة `ymd` المحلية اللي في `attendance.service.ts:550-551`. |
| 6 | كارت الطلبات (13) وإشعار «13 طلب في نطاقك» مش متطابقين مع الوارد الفعلي (5 أو 3 أو 0) | 🟠 | `dashboard.controller.ts:35`، `portal.controller.ts:181-193`، `requests.service.ts:1017-1036` | اعرض `inbox(user).length` في الكارت والإشعار بدل عدّ كل الـ UNDER_REVIEW في الفرع. |
| 7 | العقود والمستندات المنتهية مش بتتعلّم ولا بتطلع تنبيه، مثلًا الموظف 19 عقده انتهى 2026-07-31 وهو `active` | 🟠 | `portal.controller.ts:72-198`، `docs.controller.ts:100` | job يومي يعلّم العقود المنتهية ويطلع إشعار قبل الانتهاء بـ 30 يوم، ويظهر في «الأحداث القادمة». |
| 8 | قراءة وحذف الإشعارات محليين بس، والعدّاد بيعدّ كل الإشعارات كغير مقروءة | 🟠 | `notifications/page.tsx:96,115-127`، `Header.tsx:68,125-127` | جدول `notification_reads` (userId, notificationId) وendpoint `PATCH /notifications/read`، والعدّاد يبقى غير المقروء بس. أو شيل أزرار القراءة والحذف. |
| 9 | الهيدر فيه عناصر شكلها شغال وهي مش شغالة: عدّاد رسائل «3» ثابت، والبحث، والإعدادات، و«تحديد الكل»، والعنوان الثابت | 🟠 | `Header.tsx:83,94-104,125-127,185-195` | اشيل عدّاد الرسائل أو اربطه، واربط البحث، وخلّي العنوان ييجي من الـ route. |
| 10 | ثغرة صلاحية: مدير الفرع (11 مستخدم) بيشوف «إضافة موظف» في الـ Sidebar والوصول السريع ويقف عند 403 وقت الحفظ، وكمان بيشوف «مسير الرواتب» و«الإعدادات» من غير صلاحيتهم | 🟠 | `Sidebar.tsx:69`، `QuickActions.tsx:24-89`، `api/src/employees/employees.controller.ts:47` | اربط «إضافة موظف» بـ `employees.create`، وفلتر QuickActions بـ `can()`. |
| 11 | الملف الشخصي بيعرض الراتب الأساسي من غير عملة ولا بدلات (12 موظف عندهم بدلات)، وحالة `notice_period` بتظهر خام لـ 9 موظفين | 🟠 | `profile/page.tsx:39-46,451-457` | اعرض `currency` والبدلات والإجمالي، وزوّد مسمى `notice_period`. |
| 12 | «إجمالي الموظفين» بيعدّ المؤرشفين والمنتهية خدمتهم (178 بدل 175) | 🟡 | `dashboard.controller.ts:21-26` | `WHERE isActive = 1`، أو استبعد `archived`/`terminated`. |
| 13 | «في إجازة» من غير فلتر فرع، وبيعدّ النص يوم، وبيعدّ صفوف مش موظفين | 🟡 | `dashboard.controller.ts:49-52` | زوّد `andBranch` و`period='FULL'` واستخدم `COUNT(DISTINCT employeeId)`. |
| 14 | مبدّل «لوحة الإدارة/الموظف» ظاهر لكل الناس، والتفرقة بتبص على `role === 'employee'` بس. الدور `payroll_officer` (ملوش مستخدمين دلوقتي) معندوش `dashboard.view_all` ولا `employees.view`، فهيشوف 403 في الكروت والوارد | 🟡 | `src/app/page.tsx:23-26,36-59`، `dashboard.controller.ts:9` | اختار اللوحة بالصلاحية `can('dashboard.view_all')`، واخفي المبدّل عن اللي معندوش الصلاحية. |
| 15 | أزرار ملهاش handler: «تصدير» و«إضافة حدث» و«أسبوعي» في التقويم، و«تعديل الملف» والكاميرا في الملف، و«+ طلب جديد» في لوحة الموظف | 🟡 | `calendar/page.tsx:209-216,320-327`، `profile/page.tsx:288-290,310-313`، `EmployeeHome.tsx:195` | اربطها أو اشيلها. |
| 16 | التقويم بيعرض أكواد HAJJ/COMPENSATORY/MARRIAGE خام، وتصنيف الإشعارات مقلوب | 🟡 | `calendar/page.tsx:42-47`، `notifications/page.tsx:71-77` | استخدم مسميات `leave_types.nameAr` من الـ API، وصحّح `mapCategory`. |
| 17 | أخطاء بتتبلع من غير ما تظهر: جلب الإشعارات في الهيدر، وعدّاد الوارد، والطلبات الثانوية في الملف الشخصي | 🟡 | `Header.tsx:65`، `Sidebar.tsx:230`، `profile/page.tsx:166-182` | اعرض تنبيه صغير أو علامة خطأ بدل `catch(() => {})`. |
| 18 | الرفض من اللوحة بضغطة واحدة من غير تأكيد ولا سبب، والـ payload بيظهر بمفاتيح JSON إنجليزي | 🟡 | `PendingApprovals.tsx:30-42,163-169` | نافذة تأكيد تطلب سبب الرفض (`actOnRequest(id,'REJECT',comment)`)، وتسميات عربي لحقول النوع. |

ادعاءات دُحضت: الادعاء إن «فشل ويدجت PendingApprovals بسبب `/employees` بيضرب أي معتمِد» مبالغ فيه. مفيش مستخدم حالي بيتأثر: hr_manager ومدير الفرع عندهم `employees.view`، والمعتمِدين بدور employee (مديرين اتنين) مش بيشوفوا الويدجت أصلًا لأن لوحتهم EmployeeHome، و`payroll_officer` ملوش مستخدمين. بعض الأرقام اتصححت من الـ DB الحالي: 178 موظف (166 active)، و27 من غير قسم، و13 طلب UNDER_REVIEW. تقرير `DISCOVERY_REPORT.md:1226` كان واصف اللوحة إنها «جزئية» وده صح لسه، ومفيش حاجة منه اتصلحت. يعني ادعاء «61 شاشة مربوطة بالكامل» مش منطبق على الداشبورد.

**ما يعمل جيدًا**
- الـ backend بيطبّق أثر الطلب بعد الاعتماد النهائي بس، بسجل تدقيق قبل تغيير الحالة، وبيحترم الموافقات المتوازية (`requests.service.ts:656-711`). وويدجت الوارد بيبعت طلب حقيقي وبيعرض رسالة السيرفر.
- `apiFetch` بيطلع رسالة السيرفر نفسها في `ApiError`، وبيتعامل مع انتهاء الجلسة بتحويل منظّم لصفحة الدخول (`src/lib/api.ts:81-105`). كروت الإحصائيات والتقويم والإشعارات بتعرض الخطأ ده.
- الـ Sidebar مبني من الصلاحيات الفعلية، وعدّاد «بانتظار موافقتي» هو الوارد الحقيقي (`Sidebar.tsx:228-252`).
- التقويم الموحّد بيعرض عطلات حقيقية بمداها كامل وإجازات معتمدة، ومحترم نطاق الفرع و`calendar.view_all` (`portal.controller.ts:34-69`).
- الإشعارات مشتقة من أحداث حقيقية: قرارات على طلباتي، وإلغاء HR لإجازة، وتعارض بصمة مع إجازة (`portal.controller.ts:84-178`).
- الملف الشخصي بيقرا بيانات حقيقية، والموظف يقدر يشوف ملفه من غير `employees.view` (`employee-extras.controller.ts:107`)، والأرصدة بالاستحقاق المتراكم (`employee-extras.controller.ts:114-129`).

---

## 2) مديرية الموظفين كاملة (الملف، الإضافة، التعديل، العقود، المستندات، العهد، النقل، الهيكل، الأرشيف، إنهاء الخدمة، إخلاء الطرف)
**الحكم في سطر:** 13 من 15 شاشة مربوطة بالـ API فعلًا، والباك إند فيه تحقق كويس. لكن «إنهاء الخدمة» من ملف الموظف ديمو كامل بيطلع `alert()` ومبيحفظش حاجة، والتشغيل الآلي معتمد على cron بس ومفيش catch-up. النتيجة في الداتابيز: 4 موظفين عدّى آخر يوم عمل ليهم ولسه نشطين وبيدخلوا عادي، ونقل متأخر 9 أيام، و`GET /offboarding/:id` مكشوف لأي مستخدم مسجّل دخول.

**الشاشات**
| الشاشة | مصدر البيانات (API / ثابت / مختلط) | الحفظ (API / محلي / لا يوجد) | الحالة ✅/🟡/🔴 | ملاحظة |
|---|---|---|---|---|
| `/employees` | API (`src/app/employees/page.tsx:80-84`) | API: الأرشفة فقط (`page.tsx:156-167`) | 🟡 | الاستيراد والتصدير من غير `onClick` (`page.tsx:194-201`). الترقيم ثابت «صفحة 1 من 1» (`page.tsx:376-389`). فلتر الحالة مفيهوش `notice_period` ولا `terminated` (`page.tsx:246-251`)، والشارة بتطبع كلمة `terminated` خام (`page.tsx:45-62`). أيقونات التعديل والأرشفة ظاهرة لأي حد عنده `employees.view`. |
| `/employees/add` | مختلط: القوائم التنظيمية من الـ API (`EmployeeForm.tsx:577-605`)، والمسميات والجنسيات والبنوك والدرجات والشركة ثابتة | API (`add/page.tsx:18` + `save-qualifications.ts`) | 🟡 | الحفظ حقيقي. زر «حفظ كمسودة» ميت (`EmployeeForm.tsx:2341`)، وقائمة الدرجة `Grade 1..5` ثابتة (`EmployeeForm.tsx:1225-1232`). |
| `/employees/[id]` | مختلط: كل التبويبات من `/employees/:id/profile`، وقوالب المستندات ثابتة | لا يوجد | 🟡 | القوالب فيها شركة وهمية (`[id]/page.tsx:481-482`)، والمستند المولَّد مبيتحفظش مع إن الشاشة بتقول «تم إنشاء المستند بنجاح» (`[id]/page.tsx:2089`). زرا «طباعة» (`[id]/page.tsx:1024`) و«تحميل PDF» (`[id]/page.tsx:2102`) ميتين. |
| `/employees/[id]/edit` | API (`edit/page.tsx:69-73`) | API (`edit/page.tsx:177`) | 🟡 | الحقل اللي المستخدم يمسحه مبيتمسحش: `buildPayload` بيبعت القيم غير الفاضية بس (`EmployeeForm.tsx:640-731`). |
| `/employees/[id]/terminate` | ثابت (`terminate/page.tsx:30-44`, `77-83`) | محلي (`alert` في `terminate/page.tsx:187-194`) | 🔴 | ديمو كامل. نفس الموظف «أحمد محمد السعيد EMP001» ونفس المبالغ لأي id. وهي نفس الشاشة اللي بيوديها زر «إنهاء الخدمة» في ملف الموظف (`[id]/page.tsx:1056-1063`). |
| `/employees/[id]/settlement` | API | API (`settlement/page.tsx:184`, `204`, `228`, `248`, `270`) | 🟡 | الشاشة حقيقية، بس لو `GET /offboarding` رجّع 403 بيتبلع ويظهر بداله «لا يوجد ملف» (`settlement/page.tsx:103-110`). |
| `/employees/archived` | API | API (`archived/page.tsx:121`) | 🟡 | «تصدير» ميت (`archived/page.tsx:169-172`). كارت «قابلون لإعادة التفعيل» بيعرض `archivedCount` بس (`archived/page.tsx:216`)، مع إن الباك بيعيد تفعيل المنتهي خدمته كمان (`employees.service.ts:350`). |
| `/employees/contracts` | API (`contracts/page.tsx:125`) | API: PATCH للتجديد (`contracts/page.tsx:226`) | 🟡 | الانتهاء بيتحسب في المتصفح بس (`contracts/page.tsx:133-140`)، والتجديد مبيسجلش تاريخ. بيظهر 4 موظفين بس، اللي عندهم `contractStart`. |
| `/employees/custody` | API (`custody/page.tsx:111-116`) | API (`custody/page.tsx:183`, `203` وما بعدها) | 🟡 | قائمة الأصول فيها المُحالة للتقاعد RETIRED (`custody/page.tsx:176`)، والباك بيرفضها (`assets.controller.ts:247`). نموذج الأصل الجديد مفيهوش خانة قيمة (`custody/page.tsx:101-105`). |
| `/employees/documents` | API | API (`documents/page.tsx:253`, `301`, `303`) | 🟡 | زرا «تحميل» و«إرسال بالبريد» الجماعيين ميتين (`documents/page.tsx:513-520`). فئة «عقد» مكررة. |
| `/employees/onboarding` | مختلط: الموظفين من الـ API، والمهام ثابتة (`onboarding/page.tsx:58-67`) | محلي (`toggleTask` بـ setState بس) | 🔴 | مفيش باك إند للتهيئة. الشاشة بتعرض حالة `probation` بس، والداتابيز فيها 0 موظف بالحالة دي، فهي فاضية على طول. |
| `/employees/org-chart` | API (`org-chart/page.tsx:170-175`) | لا يوجد | 🟡 | بيستبعد `archived` بس (`org-chart/page.tsx:176`)، فالمنتهي خدمته ظاهر في الشجرة. «تصدير PDF» ميت (`org-chart/page.tsx:347-350`). |
| `/employees/transfers` | API (`transfers/page.tsx:51`) | لا يوجد (سجل للعرض بس) | 🟡 | الشاشة بتدّعي إن التنفيذ بيحدّث الفريق والقسم والمدير (`transfers/page.tsx:230-235`, `253-257`)، والباك بيحدّث `teamId` بس. الخمس نقلات كلها بتظهر «من: #0». |
| `/offboarding` | API (`offboarding/page.tsx:47`) | لا يوجد | ✅ | قائمة وإحصاءات حقيقية. |
| `/offboarding/[id]` | API | API (`offboarding/[id]/page.tsx:132` وما بعدها) | 🟡 | الشاشة مربوطة كاملة وأخطاء الباك بتظهر حرفيًا. بس جهات الإخلاء (المدير المباشر والعهدة والمالية) مش قادرة تفتحها بسبب حارس المسار (العيب 7). |

**المنطق والقواعد**
| القاعدة | الحكم ✅/🟡/🔴 | الدليل (`file:line`) | الشرح |
|---|---|---|---|
| مكافأة نهاية الخدمة | 🔴 | `api/src/offboarding/offboarding.service.ts:237-241` | الحساب ثابت: `gross × 0.5 × years`، من غير شرائح ومن غير خصم الاستقالة. مثال: إجمالي 10,000 و8 سنين. الباك بيدي 40,000، والمادة 84 بتدي 5×5,000 + 3×10,000 = 55,000، ولو استقالة (المادة 85، ثلثين) = 36,667. استقالة بعد 3 سنين: الباك 15,000 والنظام 5,000. وده مهم لأن الاستقالة هي المسار الوحيد اللي بيفتح ملف إنهاء خدمة. |
| مكافأة شاشة الإنهاء | 🔴 | `src/app/employees/[id]/terminate/page.tsx:130-164` | بتحسب من أساسي ثابت 12,000 وتاريخ تعيين 2020-03-15. النهارده ده بيطلع 6.4 سنة، يعني 30,000 + 16,800 = 46,800، وبدل إجازة 516.67 × 18 = 9,300، ناقص سلفة 5,000، الإجمالي 51,100 لأي موظف. ومعادلتها (أساسي بشرائح) مختلفة عن الباك (إجمالي × 0.5). |
| بدل رصيد الإجازات في التصفية | ✅ | `offboarding.service.ts:252-299` | الرصيد الافتتاحي الساري + (المستحق المتراكم لتاريخ آخر يوم − المأخوذ) × أجر اليوم (الإجمالي ÷ 30). |
| الإنهاء الفعلي بعد اعتماد التصفية | 🔴 | `offboarding.service.ts:471-472`, `557-565` | بيتنفّذ وقت الاعتماد بس لو آخر يوم عمل عدّى، وإلا بيستنى cron الساعة 00:30، ومفيش catch-up عند تشغيل السيرفر. الداتابيز فيها الحالات 3 و4 و5 و11 بحالة SETTLED وآخر يوم عمل 2026-08-07/08، والموظفين 30 و32 و34 و42 لسه `notice_period` وحساباتهم (21 و23 و25 و35) `isActive=1`. |
| النقل المجدول بتاريخ السريان | 🔴 | `api/src/requests/requests-scheduler.service.ts:15-19`, `requests.service.ts:1160-1184` | cron الساعة 00:15 بس ومفيش catch-up. النقل #3 (طلب 250، سريان 2026-09-01) لسه SCHEDULED والطلب لسه IN_EXECUTION، والموظف 151 `teamId=NULL`. |
| نطاق تحديث النقل | 🟠 | `api/src/requests/destinations.service.ts:322-325` | بيحدّث `teamId` بس، والقسم والمدير المباشر مبيتغيروش، مع إن الشاشة بتقول عكس كده. النقل لفريق في قسم تاني بيسيب `departmentId` قديم ومش متسق. |
| منع النقل وعلى الموظف عهدة مفتوحة | ✅ | `requests.service.ts:242-254` | الفحص صح، بس بيفحص `requesterId` والـ handler بيطبّق على `payload.employeeId ?? requesterId` (`destinations.service.ts:285`). |
| منع تكرار النقل | 🟡 | `requests.service.ts:242-254` | مفيش أي فحص للتكرار. النقلتين #4 و#5 لنفس الموظف 163 بنفس التاريخ 2026-12-01 (الطلبين 286 و287). |
| انتهاء العقود | 🟠 | `src/app/employees/contracts/page.tsx:133-140` | مفيش أي job في الباك يعلّم العقد إنه منتهي أو يبعت تنبيه (الـ grep على `contractEnd` مرجعش غير التحقق والمستند). الموظف 19 عقده خلص 2026-07-31 ولسه `active`. |
| تجديد العقد | 🟡 | `contracts/page.tsx:211-235` | عبارة عن PATCH لـ `contractEnd/Start/DurationMonths` بس: لا صف في السجل الوظيفي، ولا مستند، ولا اعتماد. |
| الأرشفة | 🟠 | `api/src/employees/employees.service.ts:334-344` | بتقفل حساب الدخول، بس مبتكتبش `EmployeeStatusHistory` (مع إن إعادة التفعيل بتكتب، `employees.service.ts:361-369`). في الداتابيز: سجل الموظف 19 بيقفز من `terminated` لـ `archived→active` من غير صف أرشفة، والموظفين 28 و35 مؤرشفين ومالهمش أي سجل. |
| مسح حقل وقت التعديل | 🟠 | `src/components/EmployeeForm.tsx:640-731` | الحقول الفاضية مبتتبعتش، والباك بيعمل `Object.assign` (`employees.service.ts:313`)، فتاريخ نهاية العقد والفريق والمدير والـ IBAN والقسم مبيتمسحوش أبدًا. |
| تفرّد الكود والبريد والهوية وعلاقات الفرع/القسم/الفريق | ✅ | `employees.service.ts:101-175`, `178-185`, `299-301` | 409 و400 برسالة عربية، ونهاية العقد مينفعش تكون قبل بدايته، والموظف مينفعش يبقى مدير نفسه. استثناء بسيط: لما يتبعت `teamId` لوحده مبيتفحصش تبعيته للقسم. |
| تغيير الحالة من فورم التعديل | ✅ | `EmployeeForm.tsx:1133-1139` | الاختيار محصور في probation أو active، فمينفعش حد يلف على الإخلاء ويحط `terminated` من الفورم. |
| بند العهدة في الإخلاء | ✅ | `offboarding.service.ts:160-173` | مبيكتملش طول ما فيه عهدة مفتوحة، والرسالة بتظهر حرفيًا في الشاشة (`offboarding/[id]/page.tsx:139-142`). |
| خصم العهدة المفقودة في التصفية | 🟡 | `offboarding.service.ts:357-379`, `custody/page.tsx:101-105` | منطق منع الخصم مرتين سليم، بس الخصم = `asset.value`. نموذج الأصل مفيهوش قيمة، و18 من 36 أصل في الداتابيز من غير قيمة، يعني الشطب بيخصم صفر. |
| عرض أخطاء السيرفر | ✅ | `src/lib/api.ts:81-105` | `ApiError` بيحمل رسالة الباك، والشاشات بتعرضها. الاستثناء الوحيد الـ 403 في التصفية (`settlement/page.tsx:103-110`). |
| الاستقالة المعتمدة ← فترة إشعار + ملف إخلاء | ✅ | `destinations.service.ts:410-486` | بتحوّل الحالة لـ `notice_period` وبتفتح ملف فيه 5 بنود إخلاء، ومفيش ملف مكرر. |

**ما هو ديمو / بيانات تجريبية**
- شاشة الإنهاء: الموظف ثابت `employeeData` (`src/app/employees/[id]/terminate/page.tsx:30-44`)، و5 عهد وهمية (`terminate/page.tsx:77-83`)، والتأكيد `alert('تم إنهاء خدمة الموظف بنجاح')` (`terminate/page.tsx:187-194`)، والشاشة بتعد بإلغاء الصلاحيات والأرشفة وإنشاء التصفية (`terminate/page.tsx:734-739`) ومفيش حاجة من دول بتحصل.
- قوالب المستندات في ملف الموظف فيها شركة وهمية «شركة التقنية المتقدمة» وسجل تجاري 1010123456 (`src/app/employees/[id]/page.tsx:481-482`، ومتكررة لحد `[id]/page.tsx:600`).
- قوائم ثابتة في فورم الموظف: المسميات والجنسيات والبنوك (`src/components/EmployeeForm.tsx:236-238`)، و«الشركة الرئيسية» مش مربوطة بحاجة (`EmployeeForm.tsx:1164-1166`)، والدرجات `Grade 1..5` (`EmployeeForm.tsx:1225-1232`) والداتابيز فيها 3 درجات بس (ids 1 و2 و3).
- تلميح «اتركه فارغاً للإنشاء التلقائي» غلط في خانة الكود (`EmployeeForm.tsx:1102`؛ الخطوة 2 بترفض الكود الفاضي، `EmployeeForm.tsx:761-763`) وفي خانة بريد العمل (`EmployeeForm.tsx:1604`).
- قائمة مهام التهيئة ثابتة ومحلية (`src/app/employees/onboarding/page.tsx:58-67`)، وكل المواعيد النهائية = تاريخ البدء.
- بيانات اختبار في الإنتاج المحلي: 27 موظف أسماؤهم اختبارية (S3AUD وS6-* وS7 Audit وS8 وS10 وS11 Payroll Test و«اختبار سيناريو كريم PX49594»)، ودور `pr_test_42103`، والنقلتين #4 و#5 (S11). 158 من 177 موظف مالهمش `jobTitle` و28 مالهمش قسم.

**العيوب**
| # | العيب | الخطورة 🔴/🟠/🟡 | الدليل (`file:line`) | الإصلاح المقترح |
|---|---|---|---|---|
| 1 | «إنهاء الخدمة» من ملف الموظف ديمو كامل ومبيحفظش حاجة، والباك مفيهوش أي مسار لإنهاء بقرار الشركة (المسار الوحيد اللي بيفتح ملف هو الاستقالة) | 🔴 | `terminate/page.tsx:30-44`, `187-194`؛ `[id]/page.tsx:1056-1063`؛ `destinations.service.ts:447-483` | endpoint اسمه `POST /offboarding` بسبب (إنهاء/انتهاء عقد/تقاعد) يفتح نفس الملف بنفس البنود، والشاشة تقرا الموظف والعهد والسلف والرصيد الحقيقيين. لحد ما ده يتعمل، يتشال الرابط من القائمة. |
| 2 | `GET /offboarding/:id` من غير `@Perm` ومن غير أي فحص للملكية، وبيرجّع صف `employee` كامل (راتب وIBAN وهوية) مع بنود التصفية، لأي مستخدم مسجّل دخول (73 مستخدم بدور employee) | 🔴 | `api/src/offboarding/offboarding.controller.ts:75-79`؛ `offboarding.service.ts:94-96`, `118-125` | فحص إن المستخدم عنده `offboarding.manage`، أو هو صاحب الملف، أو المدير المباشر، أو جهة عندها بند في الملف، مع تقليل الحقول الراجعة من `employee`. |
| 3 | الإنهاء بعد التصفية معتمد على cron الساعة 00:30 بس. 4 موظفين عدّى آخر يوم عمل ليهم ولسه `notice_period` وبيدخلوا | 🔴 | `offboarding.service.ts:471-472`, `557-565`؛ الداتابيز: الحالات 3 و4 و5 و11 / الموظفين 30 و32 و34 و42 | تشغيل `finalizeDue` عند الإقلاع وتشغيل يدوي ليه، وتصحيح الأربع حالات دي. |
| 4 | معادلة نهاية الخدمة ثابتة 0.5 شهر/سنة، من غير شريحة بعد 5 سنين ومن غير خصم الاستقالة (المادتين 84 و85) | 🔴 | `offboarding.service.ts:237-241` | شرائح (نص شهر لأول 5 سنين، وشهر كامل بعدها) ومعامل استقالة (0 تحت سنتين، ثلث من 2 لـ 5، ثلثين من 5 لـ 10، كامل من 10 وفوق) حسب سبب الإنهاء. |
| 5 | النقل المجدول بيفوت لو السيرفر مكانش شغال الساعة 00:15. النقل #3 متأخر من 2026-09-01 | 🟠 | `requests-scheduler.service.ts:15-19`؛ الداتابيز: transfers.id=3 وrequests.id=250 | تشغيل `runScheduledTransfers` عند الإقلاع (الـ endpoint اليدوي موجود بالفعل). |
| 6 | النقل بيحدّث الفريق بس، والشاشة بتقول إنه بيحدّث «الفريق + القسم + المدير المباشر» | 🟠 | `destinations.service.ts:322-325`؛ `transfers/page.tsx:230-235`, `253-257` | يتحدّث `departmentId` من الفريق الجديد والمدير حسب السياسة، ويتسجّلوا في السجل، أو يتصحّح نص الشاشة. |
| 7 | جهات الإخلاء مش قادرة توصل لبنودها: حارس الواجهة بيطلب `offboarding.manage` لأي مسار تحت `/offboarding`، وشاشة الملف هي الوحيدة اللي بتنادي `completeClearanceItem`. أمين العهدة (user 13) والمحاسبة (user 14 `approve.finance`) والمدير المباشر (user 10، branch_manager) محجوبين، ومحدش عنده `approve.it` | 🟠 | `src/components/layout/MainLayout.tsx:21`؛ `offboarding/[id]/page.tsx:132`؛ `offboarding.service.ts:33-39`؛ جدول `user_permission_overrides` | يتفتح `/offboarding/[id]` لأي مستخدم مسجّل، والباك يطبّق الصلاحية لكل بند (موجود فعلًا)، وإضافة صندوق «بنود إخلائي» للجهات. |
| 8 | شاشة الإضافة محجوبة بـ `employees.view` مش `employees.create`: user 94 عنده `employees.create` بس فمحجوب. ومدير الفرع (11 مستخدم) بيشوف زرار الإضافة والتعديل والأرشفة والباك بيرد عليه 403 | 🟠 | `Sidebar.tsx:69`؛ `MainLayout.tsx:33`؛ `employees/page.tsx:354-366`؛ `employees.controller.ts:47`, `56`, `67` | مسار `/employees/add` يتحجب بـ `employees.create`، والأزرار تستخدم `can('employees.edit'/'employees.archive')`. |
| 9 | مسح أي حقل في التعديل مبيتحفظش | 🟠 | `EmployeeForm.tsx:640-731`؛ `employees.service.ts:313` | في وضع التعديل يتبعت `null` للحقل اللي اتمسح، والـ DTO يسمح بـ null. |
| 10 | الأرشفة مبتكتبش في السجل الوظيفي | 🟠 | `employees.service.ts:334-344`؛ الداتابيز: الموظفين 19 و28 و35 | `history.save({oldStatus, newStatus:'archived', reason})`. |
| 11 | العقود مبتتعلّمش إنها منتهية ومفيش تنبيه، والتجديد من غير أي أثر في السجل | 🟠 | `contracts/page.tsx:133-140`, `211-235`؛ الداتابيز: الموظف 19 (`contractEnd` 2026-07-31) | job يومي يعلّم العقود المنتهية ويبعت تنبيه قبلها بـ 60 يوم، والتجديد يعمل صف في السجل ومستند. |
| 12 | التهيئة مهامها ثابتة وحالتها بتضيع مع الـ reload، والشاشة فاضية على طول (0 موظف `probation`) | 🟠 | `onboarding/page.tsx:58-67`, `87-89`, `116-129` | كيان مهام تهيئة في الباك، أو تتشال الشاشة من القائمة. |
| 13 | المنتهي خدمته والمستقيل متلخبطين في القائمة الافتراضية والهيكل: مفيش فلتر ليهم والشارة خام | 🟠 | `employees/page.tsx:45-62`, `246-251`؛ `org-chart/page.tsx:176` | إضافة `terminated` و`notice_period` للشارة والفلتر، واستبعاد `terminated` من الهيكل. |
| 14 | قائمة الدرجة ثابتة 1..5، والباك مبيتحققش إن `gradeId` موجود | 🟡 | `EmployeeForm.tsx:1225-1232`؛ `employees.service.ts:137-175` | تتحمّل من `/catalogs/grades` ويتضاف فحص وجود في `assertRelations`. |
| 15 | المستند المولَّد مبيتحفظش مع رسالة نجاح، وزرا الطباعة وPDF ميتين، وبيانات الشركة وهمية | 🟡 | `[id]/page.tsx:481-482`, `1024-1026`, `2087-2090`, `2102-2105` | قوالب `/settings/document-templates` وبيانات الشركة من الإعدادات، وحفظ المستند كـ `EmployeeDocument`. |
| 16 | ملف الموظف مبيعرضش الفريق ولا مركز التكلفة ولا جدول العمل ولا استحقاق السنوي، والمبالغ بعملة النظام مش `employee.currency` | 🟡 | `[id]/page.tsx:68-132`, `647`, `1439-1455` مقابل `797`, `1502` | إضافة الحقول دي، واستخدام عملة الموظف في المبالغ. |
| 17 | «حفظ كمسودة» ميت، و«الشركة» مش مربوطة، وأغلب الحقول اللي عليها (*) مش إجبارية فعلًا (الإلزام بس على الاسم الأول والكود والفرع) | 🟡 | `EmployeeForm.tsx:2341`, `1164-1166`, `756-767` | إزالة الزر أو ربطه، والتحقق يتطابق مع النجوم. |
| 18 | قائمة تسليم العهدة فيها أصول RETIRED (والباك بيرفضها)، ومفيش خانة قيمة للأصل الجديد | 🟡 | `custody/page.tsx:176`, `101-105`؛ `assets.controller.ts:247` | فلترة `status==='AVAILABLE'` وإضافة `value` للنموذج. |
| 19 | زرا المستندات الجماعيين ميتين، ونوع العقد متخزّن مرة `contract` ومرة «عقد عمل»، و`PATCH /documents/:id` من غير أي تحقق وقت التشغيل (نوع `Partial<>` بيتحول Object فمبيعدّيش على الـ ValidationPipe، وممكن يغيّر `employeeId`) | 🟡 | `documents/page.tsx:513-520`؛ `employees.service.ts:52`؛ `api/src/assets/docs.controller.ts:115-125` | `UpdateDocumentDto` بـ `PartialType`، وتوحيد `docType` من كتالوج الأنواع. |
| 20 | حذف المؤهلات مبيتأكدش إن `rowId` تبع `:id` ولا من نطاق الفرع | 🟡 | `api/src/employees/qualifications.controller.ts:196-201`, `217-222`, `238-243`, `253-258`, `271-276` | `delete({ id: rowId, employeeId: id })` مع فحص الفرع. |
| 21 | شاشة التصفية بتبلع الـ 403 وتقول «لا يوجد ملف» | 🟡 | `settlement/page.tsx:103-110` | عرض رسالة الصلاحية بدل حالة «لا يوجد». |
| 22 | أزرار تصدير ميتة (القائمة والأرشيف والعقود والهيكل)، وترقيم ثابت، وكارت إعادة التفعيل رقمه غلط | 🟡 | `employees/page.tsx:198-201`, `376-389`؛ `archived/page.tsx:169-172`, `216`؛ `contracts/page.tsx:249-252`؛ `org-chart/page.tsx:347-350` | ربطها بتصدير CSV أو إزالتها، والكارت يبقى `archivedCount + terminatedCount`. |
| 23 | «من: #0» للموظف اللي مالوش فريق | 🟡 | `destinations.service.ts:294`؛ `transfers/page.tsx:57` | تخزين `null` وعرض «بدون فريق». |

ادعاءات دُحضت: (1) ظهور زر «التراجع» في حالة SETTLED مش عيب، ده مقصود: الباك بيسمح بالتراجع طول ما الملف ماتقفلش CLOSED (`offboarding.service.ts:509`). (2) «3 من 177 عقد بس بيظهروا» غلط: الشاشة بتفلتر على `contractStart || contractEnd` (`contracts/page.tsx:129`) وفيه 4 موظفين عندهم تاريخ بداية. (3) «القائمة فيها أصول محجوزة reserved» غلط: مفيش حالة بالاسم ده، الحالات في الداتابيز ASSIGNED وAVAILABLE وRETIRED بس.

**ما يعمل جيدًا**
- القائمة والإضافة والتعديل والملف والعقود والمستندات والعهد والأرشيف وإعادة التفعيل وإخلاء الطرف والتصفية كلها بتقرا وتكتب من الـ API الحقيقي، والصور والمرفقات بترفع وبتتعرض فعلًا (`/files/upload`، `/files/:id`).
- تحقق قوي في الباك: تفرّد الكود والبريد والهوية، وتبعية القسم للفرع والفريق للقسم، وتواريخ العقد، والمدير مينفعش يبقى نفسه (`employees.service.ts:101-185`, `299-301`)، وعزل بالفرع في القراءة والتعديل (`employees.service.ts:81-98`).
- الأرشفة بتقفل حساب الدخول، وإعادة التفعيل بترجّعه وبتوثّق في السجل (`employees.service.ts:342`, `360-369`).
- دورة الاستقالة: اعتمادها بيحوّل الحالة لـ `notice_period` وبيفتح ملف إخلاء بـ 5 جهات من غير تكرار (`destinations.service.ts:410-486`).
- قيود سليمة: مفيش نقل ولا إخلاء عهدة وفيه عهدة مفتوحة (`requests.service.ts:242-254`، `offboarding.service.ts:160-173`).
- التصفية بتتبني آليًا من مالية الموظف الحقيقية (الإجازات المتراكمة، والأوفرتايم المعتمد بمعامله، والسلف غير المسددة، والعهدة المفقودة من غير خصم مرتين)، وتقدر تعدّلها قبل الاعتماد، وبعد الاعتماد بتتقفل ويطلعلها مرجع مستند (`offboarding.service.ts:218-398`, `458-474`).
- أخطاء الباك بتوصل للمستخدم حرفيًا في كل الشاشات تقريبًا (`src/lib/api.ts:96-105`).

---

## 3) الحضور والانصراف كامل (السجل، اليدوي، البصمة، الورديات، الجدول الأسبوعي، الكشف الشهري، الإضافي، الأذونات، التقارير)
**الحكم في سطر:** محرك الحضور حقيقي ومتوصّل كويس بباقي النظام: الإجازة المعتمدة وإلغاؤها والإذن وتصحيح البصمة والعطلات الممتدة كلها بتنعكس فعلًا على اليوم. لكن فيه 3 عيوب في الأساس بتخلّي أرقام الحضور لـ171 موظف نشط غير موثوقة:
- وردية افتراضية صامتة لكل موظف مش متسجّل له وردية.
- نوافذ البصمة بتسقط التأخير تمامًا لو الموظف وصل بعد ما نافذة الدخول تقفل.
- 96% من بصمات الجهاز «يتيمة» لأن `fingerprintCode` بيتحفظ لكن المحرك مش بيستخدمه.

**الشاشات**
| الشاشة | مصدر البيانات (API / ثابت / مختلط) | الحفظ (API / محلي / لا يوجد) | الحالة ✅/🟡/🔴 | ملاحظة |
|---|---|---|---|---|
| `/attendance` (السجل اليومي) | API | لا يوجد | 🟡 | الصفوف والحالة والتأخير جاية من `GET /attendance/daily` (`page.tsx:152`). لكن فيه أجزاء ثابتة: عمود «التحقق» دايمًا 👆 حتى للإدخال اليدوي (`page.tsx:488-492`)، ودليل طرق التحقق (وجه/بطاقة/يدوي) مش متتبَّع أصلًا (`page.tsx:520-541`). زر «تصدير» من غير `onClick` (`page.tsx:227-230`)، والترقيم «1» ثابت وأسهمه ميتة (`page.tsx:507-515`). غائبو النهارده مش بيظهروا لأن الغياب بيتجسّد بالليل بس. |
| `/attendance/manual-entry` | مختلط | مختلط | 🟡 | الحفظ حقيقي: `POST /attendance/punches/manual` (`page.tsx:94`). لكن السبب مش بيتبعت (`page.tsx:83-91`). جدول السجل موجود في state الجلسة بس (`page.tsx:52,100-114`)، وحالة كل صف ثابتة `'approved'` (`page.tsx:109`)، فكروت «قيد المراجعة/مرفوضة» دايمًا صفر (`page.tsx:130-135`). زرّا تعديل/حذف ميتين (`page.tsx:375-380`). |
| `/attendance/devices` | API | API | ✅ | كله موصول فعلًا: CRUD الأجهزة، والمزامنة لجهاز واحد أو للكل، وفاصل المزامنة. نتائج السحب (سُحب/أُدخل/تطابق) بتظهر (`page.tsx:273-321`). ملاحظات صغيرة: كارت «ظهر مؤخراً» معناه الحقيقي «له أي بصمة في أي وقت» (`page.tsx:220`)، وفشل تحميل الإعداد بيتبلع (`page.tsx:107-109`)، والشاشة مش بتعرض البصمات اليتيمة (راجع العيب 3). |
| `/attendance/monthly-sheet` | API | لا يوجد (التصدير CSV من العميل) | 🟡 | الأيام والملخّص من `GET /attendance/monthly`. كارت «إجازة جزئية» بيقرا `summary.partialLeave` (`page.tsx:215`)، والسيرفر مش بيرجّعه (`attendance.service.ts:1418-1425`)، فقيمته دايمًا 0. تصدير CSV مع BOM شغال (`page.tsx:99-138`). |
| `/attendance/overtime` | API (فعليًا فاضي) | مختلط | 🔴 | القائمة `DETECTED + requestId IS NULL` بس (`attendance.service.ts:1432-1435`)، والكرون بيحوّل الصفوف دي لطلبات `OVERTIME_AUTO` كل 3 دقايق (`requests.service.ts:1052-1092`)، فالشاشة فاضية دايمًا (DB: 0 صف DETECTED). كروت SUBMITTED/APPROVED/PAID محسوبة من نفس القائمة، فهي دايمًا 0 (`page.tsx:130-139`). أما checkbox «يتطلب تأكيداً قبل الدفع» فهو `useState` محلي بس (`page.tsx:75,172-177`)، يعني إعداد وهمي. |
| `/attendance/permissions` | مختلط | API | 🟡 | طلبات PERMISSION حقيقية (`page.tsx:125`)، والاعتماد بيحرّك السلسلة ويعيد حساب اليوم. كروت الأنواع الأربعة اللي تحت نصوص ثابتة (`page.tsx:404-433`) ومش مطابقة لكتالوج `permission_types`. نوع الإذن نفسه مش ظاهر في الجدول (`page.tsx:61-73`). |
| `/attendance/reports` | API | لا يوجد | 🟡 | الجداول جاية من SQL (`reports.controller.ts:54-66,138-147`). زرّا «طباعة» و«تصدير Excel» ميتين (`page.tsx:98-105`). تسميات حالات الإضافي PENDING/CONFIRMED (`page.tsx:44-49`) مش هي حالات النظام الحقيقية، فبيظهر الكود الخام، و`APPROVED` بياخد لون التحذير (`page.tsx:264-271`). |
| `/attendance/shifts` | API | API | 🟡 | الإضافة والتعديل وتفعيل/تعطيل الوردية شغالين، والتعديل بيعيد الحساب (`catalogs.controller.ts:150-158`). قسم «تعيين الورديات للموظفين» فيه 3 أزرار من غير `onClick` (`page.tsx:417-431`). مفيش حذف. الشاشة بتعرض «ورديات ليلية» (`page.tsx:46,175`) مع إن المحرك مش بيحسبها. |
| `/attendance/weekly-schedule` | مختلط | API | 🟡 | الإسناد والنسخ والتجاوز اليومي والجماعي كلهم بيكلموا السيرفر فعلًا. الأجزاء الثابتة: `WEEKEND_DAYS = ['friday','saturday']` بيقفل الخليتين لكل الناس (`page.tsx:195,954-963`)، و«الساعات» = الوردية × 5، وخلايا الإجازة = عدد الصفوف × 2 (`page.tsx:606-614`)، وأزرار طباعة/Excel/إعدادات ميتة (`page.tsx:796-804`). |
| `/my/attendance` | API | لا يوجد | 🟡 | خدمة ذاتية، والسيرفر بيفرض إن الموظف يشوف نفسه بس (`attendance.service.ts:1398-1408`). كارت «إجازة جزئية» دايمًا 0 (`page.tsx:133`)، ومفيش عمود للانصراف المبكر (`page.tsx:160-167`). الأرقام نفسها متأثرة بالوردية الافتراضية. |

**المنطق والقواعد**
| القاعدة | الحكم ✅/🟡/🔴 | الدليل (`file:line`) | الشرح |
|---|---|---|---|
| البصمة (دفع/سحب/يدوي) ← يوم حضور | 🟡 | `attendance.service.ts:433-478`، `device-sync.service.ts:76-112` | البصمة بتتحفظ وبعدها `computeDay` بيتنده فورًا، والسحب بيشيل المكرر ±500ms. العيوب: مفيش رفض لتاريخ مستقبلي (`:452-453`). ومفتاح التاريخ بـ `toISOString` UTC (`:463`)، والسيرفر على Egypt Standard Time (UTC+3)، فبصمة بين 00:00 و02:59 بتعيد حساب اليوم اللي قبلها. |
| ربط مستخدم الجهاز بالموظف | 🔴 | `attendance.service.ts:442-446`، `employee.entity.ts:29` | المطابقة بـ `employeeCode` بس. `fingerprintCode` بيتحفظ ويتعرض (`EmployeeForm.tsx:725`، `employees/[id]/page.tsx:1312`) لكن المحرك مش بيقراه أبدًا. DB: ‏7452 من 7774 بصمة `employeeId NULL`، على 36 كود جهاز (10، 26، 3…)، و0 تطابق مع `fingerprintCode`. ولو اتربط الكود بعدين، مفيش أي آلية ترجّع البصمات القديمة لصاحبها. |
| ترتيب تحديد الوردية | 🟡 | `attendance.service.ts:496-529` | الترتيب: تجاوز اليوم ← وردية الأسبوع (بـ `shiftId`، وبتتقري حيّة من الكتالوج، وده تعديل لسه مش متعمله commit) ← ساعات جدول العمل ← `DEFAULT_SHIFT`. الترتيب نفسه سليم، لكن آخر خطوة افتراض صامت. |
| الموظف اللي من غير وردية لازم يومه يتعلَّم، مش ياخد افتراض | 🔴 | `attendance.service.ts:31,528,100-108,912-917` | بيرجّع `{صباحي 08:00-17:00}` من غير أي علامة على الصف. وبعدين `shiftConfig('صباحي')` بيطابق وردية الكتالوج #1 بالاسم ويستلف منها سماحية 30 (بدل 10 العامة)، ونوافذ 07:00-11:00 / 16:00-21:00، وعتبة إضافي ساعة. وفيه جدول عمل `isDefault=1` (id 1) لكن الكود بيتجاهله. DB: ‏171 من 175 موظف نشط مالهمش جدول عمل ولا وردية لأسبوع 2026-09-06، و3005 يوم اتحسبوا على الافتراض ده، منهم 2906 غياب. |
| التأخير + السماحية (وردية ثابتة) | 🟡 | `attendance.service.ts:1105-1115` | `lateRaw = checkIn − shiftStart`، ويتخصم منه اللي يغطيه إذن أو نصف يوم. لو تعدّى السماحية بيتحسب كله. الحساب صح. مثال مُعاد على موظف من غير وردية بيشتغل فعليًا 10:00-19:00 وبصم 10:15/19:05: التأخير = 615 − 480 = 135 دقيقة (أكبر من سماحية 30)، ويتكشف إضافي وهمي = 19:05 − 17:00 = 2.08 ساعة (≥ 1)، ويتسجّل DETECTED ×1.5. |
| تصنيف البصمات بالنوافذ | 🔴 | `attendance.service.ts:945-958,1078-1081` | لو مفيش بصمة جوّه نافذة الدخول يبقى `checkIn = null`. ومع وجود بصمة خروج، الحالة بتبقى `'present'` بتأخير 0. مثال على الوردية #1: بصمة 11:30 و17:10 تطلع «حاضر» بتأخير 0، رغم تأخير 3.5 ساعة. ولو البصمات بره النافذتين الاتنين (11:30 و15:30)، اليوم يطلع «غائب» مع إن الموظف حضر. وده بيلمس كل الـ171 موظف لأنهم بيستلفوا نوافذ الوردية #1. |
| بصمة واحدة (نسيان الخروج أو الدخول) | 🟡 | `attendance.service.ts:956-957,1078-1081,1120` | بصمة دخول بس = حاضر/متأخر يوم كامل بساعات عمل 0، من غير حالة «بصمة ناقصة». DB: ‏10 أيام دخول بس و8 أيام خروج بس، كلهم «حاضر/متأخر». |
| الانصراف المبكر | ✅ | `attendance.service.ts:1117-1128` | `shiftEnd − checkOut`، ويتخصم منه غطاء الإذن المسائي (أو «كلاهما»)، ويتحسب لو تعدّى السماحية. |
| الوردية المرنة | ✅ | `attendance.service.ts:1082-1101` | العجز عن الساعات المطلوبة (بعد خصم الوقت المعذور) بيتحسب لو تعدّى السماحية، والوردية الليلية متعالجة في المدة. |
| الوردية الليلية (الثابتة) | 🔴 | `attendance.service.ts:904-909,1121` | البصمات بتتجمع باليوم التقويمي. وردية 22:00-06:00 بتتقسم على يومين: يوم «حاضر» من غير خروج، واليوم اللي بعده بيوم «حاضر» وهمي. الكتالوج بيقبل `endTime < startTime` والشاشة بتعرضها كوردية ليلية. |
| الغياب بيتخزن كصف | 🟡 | `attendance.service.ts:543-586`، `attendance-scheduler.service.ts:16-26` | فعلًا بيتجسّد كصفوف `absent` (DB: ‏3634 صف). لكن الكرون بيغطي D-1 وD-2 بس الساعة 01:00: غياب النهارده مش بيظهر، ولو السيرفر وقف أكتر من يومين تفضل فجوة. |
| الويك إند والعطلة الرسمية (بتاريخ نهاية ممتد) | ✅ | `attendance.service.ts:169-170,218-221` | `h.endDate >= date` محترم. DB: ‏0 غياب في أيام عطلات رسمية، منها عيد الفطر 03-20 → 03-23. |
| قواعد الاستثناء (آخر سبت = دوام) | ✅ | `attendance.service.ts:223-233` | القاعدة 21 (SAT/LAST/WORK) طلّعت 158 غياب يوم 2026-06-27، وده سلوك صحيح طبقًا للقاعدة. |
| الإجازة المعتمدة تنعكس على الحضور (on_leave) | ✅ | `attendance.service.ts:969-977,1017-1021`، `requests.service.ts:737-766` | الاعتماد بيعيد حساب مدى الإجازة. DB: ‏108 من 108 يوم إجازة كاملة معتمدة مسجّلين `leave`، و0 صف `leave` يتيم. |
| إجازة بأثر رجعي تقلب الغياب | ✅ | `attendance.service.ts:576-580` | صف `absent` بيتعامل كتصنيف مؤقت وبيتحسب تاني. |
| إلغاء إجازة معتمدة | ✅ | `leaves.controller.ts:62-118`، `requests.service.ts:747-755` | الإلغاء المباشر من HR والإلغاء عن طريق الطلب (`leaveId`) الاتنين بيرجّعوا الرصيد ويعيدوا حساب الأيام. |
| الويك إند جوّه مدى الإجازة | 🟡 | `attendance.service.ts:1017-1022` | `isFullLeaveDay` بيتفحص قبل `isHoliday`، فالجمعة والسبت جوّه الإجازة بيتعرضوا «في إجازة» مش «عطلة» (عرض بس، الرصيد مش بيتخصم منهم). |
| اعتماد الإذن له أثر | ✅ | `requests.service.ts:767-768`، `attendance.service.ts:731-883` | الاكتمال بيعيد حساب اليوم. سقف العدد والدقائق، والتقسيم عند الحد، واتجاه التغطية (صباحي/مسائي) كلهم صح. |
| الإذن من غير نوع أو بنوع مجهول | 🔴 | `attendance.service.ts:826-834`، `requests.service.ts:297`، `request_types.requiredFields=["date","from","to"]` | التعليق بيقول «أو بلا نوع → كامل النافذة بخصم»، لكن الكود بيحط `deductible: !!pt?.isDeductible`، يعني `false`: عذر مجاني كامل من غير سقف. وأصلًا `permissionType` مش مطلوب عند التقديم. DB: الطلبين #38 و#163 COMPLETED من غير نوع. وكمان المطابقة بـ `nameAr` (`:795`)، فأي إعادة تسمية للنوع بتحوّل الأذونات القديمة لـ«بلا نوع». |
| كشف الإضافي وعتبته ومضاعفه | 🟡 | `attendance.service.ts:1195-1279` | الحساب صحيح (بعد نهاية الوردية، أو اليوم كله لو عطلة، ومضاعف من الإعدادات، والفترة المقفولة بتمسح المكتشف)، لكنه بيطلع إضافي وهمي فوق الوردية الافتراضية. |
| تعديل الوردية يعيد الحساب | 🟡 | `catalogs.controller.ts:150-158`، `attendance.service.ts:1335-1378` | بيعيد حساب الأيام المربوطة بـ `shiftId` بس. أيام الافتراض (3005) مالهاش مرجع، وأوقاتها ثابتة 08:00-17:00 في الكود، فتعديل أوقات الوردية #1 مش بيوصلها، في حين إن السماحية والنوافذ الجديدة بتوصلها بالاسم. |
| نطاق الفرع في كتابات الحضور | 🔴 | `attendance.controller.ts:81-84,88-91,102-115,268-275`، `attendance.service.ts:1444-1459` | `ingest` و`upsertSchedule` و`setDayOverride` و`confirmOvertime` مفيهمش فحص نطاق ولا منع اعتماد النفس. و`branch_manager` عنده `attendance.manage` و`overtime.confirm` (`permissions.ts:103`). |
| شرائح التأخير (بتتطبق في المسير، خارج النطاق) | 🟡 | DB `lateness_tiers` | الشريحة 10 (من 0 لـ NULL، ‏0.25) متداخلة مع 6 (من 1 لـ 60) و8 (من 61 لـ 120)، فالتقييم ملتبس. شاشات الحضور بتعرض الدقائق الخام بس. |

**ما هو ديمو / بيانات تجريبية**
- جدول «الإدخالات اليدوية» في `/attendance/manual-entry`: موجود في state الجلسة بس، والحالة `'approved'` ثابتة، وكروت المراجعة والرفض صفرية (`manual-entry/page.tsx:52,100-114,109,130-135`).
- عمود «التحقق» 👆 ثابت، ودليل وجه/بطاقة/يدوي ثابت (`attendance/page.tsx:488-492,520-541`).
- كروت أنواع الأذونات الأربعة نصوص ثابتة (`permissions/page.tsx:404-433`).
- checkbox «المكتشَف من البصمة يتطلب تأكيداً» محلي بس، وشريط دورة الحياة والكروت محسوبين من قائمة DETECTED الفاضية (`overtime/page.tsx:75,130-139,172-200`).
- إحصاءات الجدول الأسبوعي تقديرية: ×5 و×2، والويك إند جمعة/سبت ثابت (`weekly-schedule/page.tsx:195,606-614`).
- أزرار ميتة (مفيش `onClick`):
  - تصدير وترقيم السجل (`attendance/page.tsx:227-230,507-515`)
  - طباعة/Excel التقارير (`reports/page.tsx:98-105`)
  - قسم تعيين الورديات (`shifts/page.tsx:417-431`)
  - طباعة/Excel/إعدادات الجدول (`weekly-schedule/page.tsx:796-804`)
  - تعديل/حذف الإدخال اليدوي (`manual-entry/page.tsx:375-380`)
- `src/lib/attendance.ts:12,37`: نسخة عميل من `DEFAULT_SHIFT`، ومحدش بيعمل لها import (كود ميت).
- بيانات اختبار في القاعدة:
  - ورديات: `نوافذ/مرن/سماحية SC53685|SC43059|SC49225`، `AUDMORN45`، `S11SHIFT_K1`، `HOT34501`، `PR91686`
  - أنواع إذن اختبار: `PI995`، `PB9388`، `PB76007`، `BF79000` (مُعطّلة)
  - طلبات «نوع وهمي» #45 و#56 (DRAFT)
  - فروع `فرع غياب ABS…`/`فرع مديونيات OBL…`
  - عطلة «اختبار كلود عطلة ممتدة» (id 10)
  - جدول عمل `WS85620`
  - 91 بصمة مستقبلية لحد 2026-12-20، و47 يوم حضور محسوب في المستقبل

**العيوب**
| # | العيب | الخطورة 🔴/🟠/🟡 | الدليل (`file:line`) | الإصلاح المقترح |
|---|---|---|---|---|
| 1 | الموظف اللي من غير وردية بياخد «صباحي 08:00-17:00» بصمت، ومعاها سماحية الوردية #1 ونوافذها وعتبة إضافيها. النتيجة: تأخير متضخّم، وإضافي وهمي، وغياب محسوب على افتراض، على 171 موظف و3005 يوم. | 🔴 | `attendance.service.ts:31,520-528,912-917,1244-1248`؛ DB `shifts` id 1 | استخدام جدول العمل `isDefault` بدل الثابت، وإضافة علم `unscheduled` على `attendance_days`. في اليوم غير المجدول: مفيش تأخير ولا إضافي، ويظهر تنبيه لـ HR، ويكون فيه تقرير «موظفون بلا وردية». |
| 2 | نوافذ البصمة بتسقط الوصول المتأخر: لو مفيش بصمة في نافذة الدخول، اليوم يطلع «حاضر» بتأخير 0. ولو البصمات بره النافذتين، اليوم يطلع «غائب» رغم الحضور. | 🔴 | `attendance.service.ts:945-958,1078-1081` | لو مفيش بصمة في نافذة الدخول، أول بصمة قبل نافذة الخروج تتحسب دخول متأخر. البصمات اللي بره النوافذ ما تتجاهلش؛ اليوم يتعلّم «بصمة شاذة» للمراجعة. واليوم ما يبقاش «حاضر» من غير دخول. |
| 3 | 96% من بصمات الجهاز يتيمة (7452/7774): المطابقة بـ `employeeCode` بس، و`fingerprintCode` بيتحفظ لكن مش بيتقري، ومفيش ربط بأثر رجعي، والشاشة مش بتعرض الأكواد اليتيمة. | 🔴 | `attendance.service.ts:442-446`؛ `employee.entity.ts:29`؛ `EmployeeForm.tsx:725` | المطابقة تبقى بـ `fingerprintCode` الأول وبعدين `employeeCode`. إضافة backfill لما الكود يتربط (تحديث `employeeId` + `computeDay`)، ولوحة «أكواد جهاز غير مربوطة» في `/attendance/devices`. |
| 4 | الإذن من غير نوع أو بنوع مجهول = عذر مجاني من غير سقف، عكس التعليق. النوع مش مطلوب عند التقديم، والمطابقة باسم عربي قابل للتعديل. | 🟠 | `attendance.service.ts:795,826-834`؛ `requests.service.ts:297-314`؛ DB طلبات #38 و#163 | `permissionType` يبقى إلزامي ويتخزن بالـ id. الإذن اللي من غير نوع يتحسب «بخصم» (`deductRatio=1`) زي ما التعليق بيقول. |
| 5 | مفيش نطاق فرع على كتابات الحضور: مدير الفرع يقدر يحقن بصمات، أو يغيّر جدول، أو يعمل تجاوز يوم، أو يعتمد إضافي لموظفين في فروع تانية، أو لنفسه. | 🟠 | `attendance.controller.ts:81-84,88-91,102-133,268-275`؛ `attendance.service.ts:1444-1459`؛ `permissions.ts:103` | تمرير `user` وتطبيق `branchScopeOf` على `employeeId`/`employeeCode`، ومنع `confirmOvertime` لو `row.employeeId === user.employeeId`. |
| 6 | الإدخال اليدوي من غير أثر تدقيقي: السبب مش بيتبعت، ومفيش `source`/`createdBy` على البصمة، والأثر بيتنفّذ فورًا من غير مراجعة، والسجل يعرض 👆 كأنها بصمة جهاز. | 🟠 | `manual-entry/page.tsx:83-91,109`؛ `attendance.entities.ts:11-33`؛ `attendance/page.tsx:488-492`؛ DB ‏277 بصمة `deviceSn NULL` | إضافة أعمدة `source` (DEVICE/MANUAL) و`createdByUserId` و`reason`. عرض طريقة التحقق الحقيقية في السجل. واختياري: تمرير الإدخال اليدوي على مسار `PUNCH_CORRECTION`. |
| 7 | البصمات بتاريخ مستقبلي مقبولة وبتتحسب: 91 بصمة لحد 2026-12-20، و47 يوم حضور مستقبلي. | 🟠 | `attendance.service.ts:452-453` | رفض أي `punchTime` بعد الآن + هامش صغير، في `ingest` (للجهاز واليدوي الاتنين). |
| 8 | شاشة الإضافي فاضية دايمًا، لأن الكرون بيحوّل DETECTED لطلبات، والكروت دايمًا 0. وخيار «يتطلب تأكيداً» إعداد وهمي محلي، مع إن المفتاح `overtime.biometric_requires_confirmation` موجود في DB. | 🟠 | `overtime/page.tsx:75,130-139,172-177`؛ `attendance.service.ts:1432-1435`؛ `requests.service.ts:1052-1092` | عرض كل `overtime_entries` للشهر بكل الحالات (مع ربطها بالطلب)، وربط الـ checkbox بـ `PATCH /settings/config`. |
| 9 | البصمة الواحدة (دخول بس أو خروج بس) بتتحسب يوم كامل «حاضر/متأخر» من غير علم «بصمة ناقصة». | 🟠 | `attendance.service.ts:956-957,1078-1081`؛ DB ‏10+8 أيام | إضافة حالة أو علم `missing_punch` لأي يوم ماضي فيه بصمة واحدة، يظهر في السجل والكشف ويدخل قرار HR. |
| 10 | الوردية الليلية الثابتة مش مدعومة في المحرك، والشاشة بتعرضها وبتسمح بإنشائها. | 🟠 | `attendance.service.ts:904-909,1105,1121`؛ `shifts/page.tsx:46,175,278` | إسناد البصمات لنافذة الوردية (من البداية − هامش لحد النهاية + 1440)، أو منع `endTime < startTime` للورديات الثابتة لحد ما ده يتبني. |
| 11 | ملخّص الكشف الشهري و«حضوري» ناقص `partialLeave` وعدد أيام الإجازة والعطلات، فكارت «إجازة جزئية» دايمًا 0. | 🟡 | `attendance.service.ts:1418-1425`؛ `monthly-sheet/page.tsx:215`؛ `my/attendance/page.tsx:133` | إضافة `partialLeave` و`leave` و`holiday` و`totalEarlyLeaveMinutes` للـ summary. |
| 12 | تقرير الإضافي بيستخدم حالات غلط (PENDING/CONFIRMED) وAPPROVED بيظهر بلون التحذير. الطباعة والتصدير ميتين، وتقرير الحضور مفيهوش أعمدة إجازة/عطلة/جزئية. | 🟡 | `reports/page.tsx:44-49,98-105,264-271`؛ `reports.controller.ts:55-61` | استخدام تسميات DETECTED/SUBMITTED/APPROVED/REJECTED/PAID، وتوصيل التصدير، وإضافة `SUM(CASE…)` للحالات الناقصة. |
| 13 | الجدول الأسبوعي:<br>• الويك إند جمعة/سبت ثابت لكل الناس، فموظفو «جدول السبت فقط» (الجمعة عندهم دوام) مايقدروش ياخدوا تجاوز يوم الجمعة.<br>• الساعات ×5 وخلايا الإجازة ×2 تقديريين.<br>• أيام العمل محسوبة بفرع المستخدم نفسه، مش فرع الموظف.<br>• مفيش إلغاء إسناد.<br>• رسالة النجاح بـ `alert()`. | 🟡 | `weekly-schedule/page.tsx:195,606-614,954-963,1287`؛ `attendance.controller.ts:238` | اشتقاق العطلة من `workSchedule`/الفرع لكل موظف، وحساب الساعات من أيام العمل الفعلية، وإضافة `DELETE` للإسناد. |
| 14 | عناصر واجهة ميتة أو مضلِّلة: تصدير وترقيم السجل، قسم تعيين الورديات، تعديل/حذف اليدوي، كارت «ظهر مؤخراً» (معناه أي بصمة في أي وقت)، وفشل تحميل إعداد الأجهزة بيتبلع. | 🟡 | `attendance/page.tsx:227-230,507-515`؛ `shifts/page.tsx:417-431`؛ `manual-entry/page.tsx:375-380`؛ `devices/page.tsx:107-109,220` | توصيلها أو شيلها، وتسمية الكارت «آخر بصمة خلال 24 ساعة». |
| 15 | تواريخ UTC: مفتاح يوم البصمة في السيرفر، والتاريخ/الشهر الافتراضي في 4 شاشات. بين 00:00 و02:59 (UTC+3) اليوم أو الشهر بيطلع غلط. | 🟡 | `attendance.service.ts:463`؛ `attendance/page.tsx:60`؛ `monthly-sheet/page.tsx:36`؛ `my/attendance/page.tsx:33`؛ `reports/page.tsx:51` | استخدام `localDateOf` الموجودة (`attendance.service.ts:42-43`) ونسخة محلية منها على العميل. |
| 16 | تجسيد الغياب D-1..D-2 بس: غائبو اليوم مش بيظهروا في السجل، وتوقف السيرفر أكتر من يومين يسيب فجوة. وإعادة الحساب اليدوية `POST /attendance/recompute` مالهاش أي واجهة. | 🟡 | `attendance-scheduler.service.ts:16-26`؛ `attendance.service.ts:1385-1390,1462-1474`؛ `attendance.controller.ts:280-283` | catch-up من آخر تاريخ متجسّد، و«لم يبصم بعد» للنهارده في `daily()`، وزر إعادة حساب في السجل. |
| 17 | قراءات مفتوحة لأي JWT: `GET /catalogs/devices` بيرجّع `authKey` (فاضي حاليًا)، و`GET /attendance/schedule` و`/schedule/day-overrides` بيكشفوا جداول الشركة كلها لأي موظف. | 🟡 | `catalogs.controller.ts:87-107`؛ `assets.entities.ts:177-178`؛ `attendance.controller.ts:93-97,135-139` | `select:false` على `authKey`، و`@Perm('attendance.view_all')` مع نطاق فرع على قراءات الجدول. |
| 18 | فجوة دور: `branch_manager` بيشوف «الورديات» و«أجهزة البصمة» في القائمة، وكل أفعالهم ترجع 403 (`settings.manage`/`attendance.sync`) مع إن الخطأ بيظهر له. | 🟡 | `permissions.ts:100-108`؛ `catalogs.controller.ts:122,138`؛ `attendance.controller.ts:287,294`؛ `MainLayout.tsx:37` | إخفاء الروابط أو الأزرار بـ `can()`، أو منح صلاحيات فرعية للإسناد. |
| 19 | شاشة الأذونات مش بتعرض نوع الإذن، وكروت الأنواع ثابتة ومش من الكتالوج. | 🟡 | `permissions/page.tsx:61-73,404-433` | قراءة `permissionType` من الـ payload وعرض `permission_types` الحقيقية. |
| 20 | أيام الويك إند جوّه مدى الإجازة بتظهر «في إجازة» مش «عطلة». | 🟡 | `attendance.service.ts:1017-1022` | فحص `isHoliday` قبل `isFullLeaveDay` للحالة المعروضة (من غير ما يأثر على الرصيد). |

ادعاءات دُحضت: 
- «الإجمالي يعدّ الصفوف لا الموظفين» (`attendance/page.tsx:209`): فيه قيد فريد `(employeeId,date)` (`attendance.entities.ts:145`)، فعدد الصفوف = عدد الموظفين في اليوم. العيب الحقيقي إن الموظفين اللي مالهمش صف مش بيظهروا (العيب 16).
- أرقام DB اتحركت بشكل بسيط ومش مؤثرة: 7774 بصمة بدل 7769، و171/175 موظف نشط بدل 170/174، و37 طلب COMPLETED و5 DRAFT للأذونات بدل 36/4.

**ما يعمل جيدًا**
- انعكاس الإجازة على الحضور حقيقي: الاعتماد، والأثر الرجعي، والإلغاء المباشر أو عن طريق الطلب، كلهم بيعيدوا حساب الأيام. DB: ‏108/108 يوم إجازة معتمدة مسجّلين `leave`، و0 صف يتيم.
- العطلات الرسمية الممتدة (`endDate`) وقواعد الاستثناء (آخر سبت) مطبّقة في المحرك. DB: ‏0 غياب في أيام العطلات.
- الغياب بيتخزن كصف فعلي (3634 صف)، والتجسيد idempotent وبيحترم تاريخ الالتحاق والأرشفة.
- محرك الأذونات متقن لما النوع يكون موجود: الأسبق أولًا، وسقف العدد والدقائق مع تقسيم النافذة، ونسبة الخصم، واتجاه التغطية صباحي/مسائي.
- الوردية المرنة، ومضاعف الإضافي حسب نوع اليوم، وفترات فتح/قفل الإضافي، و`min(المعتمد، الفعلي)` للإضافي المطلوب مسبقًا، كلهم منفّذين.
- التعديل اللي لسه مش متعمله commit بيخلّي أوقات الوردية تتقري حيّة بـ `shiftId` ويعيد حساب الأيام المرتبطة لما الوردية تتعدل. والجدول الأسبوعي بقى يعرض «غير مجدوَل» بدل وردية مزيّفة.
- مزامنة ZKTeco الحقيقية (TCP) فيها منع للمكرر، ومنع مزامنتين في نفس الوقت، وتوقيت محلي، ونتائج بتظهر في الواجهة.
- أخطاء السيرفر (4xx) بتوصل للمستخدم برسالتها العربية في كل الشاشات (`api.ts:81-105` + بانرات الخطأ). والـ endpoints المحمية بترجّع 401 من غير توكن (اتجرّب بـ curl: `daily`، `catalogs/devices`، `schedule`).
- `/my/attendance` مقفولة على الموظف نفسه من السيرفر، وبتتعامل مع حالة الحساب اللي مش مربوط بموظف.

---

## 4) الإجازات كامل (الطلب، الأرصدة، الأنواع، العطل الرسمية، التقويم، إجازاتي)
**الحكم في سطر:** كل الشاشات السبع بتقرأ وتكتب على الباك الحقيقي (مفيش شاشة ديمو)، وحساب أيام العمل والعطل متعددة الأيام وإلغاء HR شغالين صح. المشكلة إن 84 من 86 مستخدم (دور موظف ومدير فرع) مش قادرين يقدّموا إجازة من الواجهة أصلًا بسبب 403 على قائمة أنواع الإجازات. كمان الأرصدة من غير ترحيل سنوي، فمن 2027-01-01 الإجازات هتبقى بلا حد وبلا خصم. وفيه ثغرات سحب على المكشوف وإلغاء مزدوج.

**الشاشات**
| الشاشة | مصدر البيانات (API / ثابت / مختلط) | الحفظ (API / محلي / لا يوجد) | الحالة ✅/🟡/🔴 | ملاحظة |
|---|---|---|---|---|
| `/leaves` سجل الإجازات | مختلط | API | 🟡 | الصفوف والإحصاءات من `GET /api/leaves` (`src/app/leaves/page.tsx:84`). الإلغاء `POST /leaves/:id/revoke` حقيقي والخطأ بيظهر في بانر (`page.tsx:102-106`, `274-279`). فلتر النوع ودليل الألوان وأسماء الأنواع قائمة ثابتة `LEAVE_TYPE_META` (`page.tsx:24-36`, `247-251`, `396-401`)، فأي نوع جديد هيظهر بكوده الخام (`page.tsx:39`). زر التصدير من غير `onClick` (`146-149`)، ونفس الكلام لزر العين (`350-352`)، والترقيم ثابت على «1» (`373-389`). نصف اليوم مش ظاهر لأن `ApiLeave` مفيهاش حقل `period` (`src/lib/api.ts:210-214`). |
| `/leaves/balance` أرصدة الإجازات | API | لا يوجد | 🟡 | بيعمل طلب منفصل لكل موظف (179 طلب)، وأي فشل بيتحوّل بصمت لـ`[]` فيظهر «—» (`balance/page.tsx:77-81`). عمود «مستحق» بيعرض المتراكم لحد النهارده (14 يوم في 2026-09-10) مش الـ21 السنوية، مع إن العنوان «استحقاق السنة» (`balance/page.tsx:221-223`, `384-392`؛ `leave-balances.service.ts:109`). أعمدة «الطارئة» دايمًا «—» (`303-311`). أزرار +/- معطّلة على طول (`333-346`)، وزر التصدير ميت (`124-127`). |
| `/leaves/calendar` التقويم | مختلط | لا يوجد | 🟡 | العطل والإجازات المعتمدة جايين من `GET /api/calendar` (`calendar/page.tsx:62-72`)، و`endDate` للعطلة مأخوذ في الاعتبار (`95-98`). الألوان ثابتة (`22-34`)، والدليل فيه 5 أنواع بس (`231-254`). الويك إند متثبّت جمعة/سبت (`182`) بغض النظر عن جدول الموظف أو الفرع. الإجازات المعلّقة مش بتظهر (`portal.controller.ts:49-54`). |
| `/leaves/holidays` العطل الرسمية | API | API | 🟡 | الإضافة والتعديل بينادوا الـAPI فعلًا والخطأ بيظهر في المودال (`holidays/page.tsx:76-98`, `285-290`). مفيش حذف لا في الواجهة ولا في الباك (`DELETE` بيرجّع 404). مفيش تحقق إن النهاية ≥ البداية. الشبكة السنوية بتعرض العطلة في شهر البداية بس (`185-188`). مدير الفرع شايف زرار الإضافة والتعديل بس الحفظ بيرجّع 403 (`catalogs.controller.ts:122`, `138`). |
| `/leaves/request` طلب إجازة | API | API | 🔴 | الإرسال حقيقي `POST /api/requests {typeCode:'LEAVE'}` (`request/page.tsx:147-155`)، والرفع حقيقي (`124`). لكن `GET /settings/leave-types` محتاج `settings.manage` (`settings.controller.ts:351-353`, `409-412`)، فعند الموظف ومدير الفرع الـ`Promise.all` بيفشل (`request/page.tsx:78-86`): مفيش أنواع إجازة، وزر التقديم معطّل على طول. عدّ الأيام بالتقويمي مش بأيام العمل (`98-112`)، ومفيش نصف يوم ولا تقديم نيابة عن حد. |
| `/leaves/types` أنواع الإجازات | API | API | 🟡 | الإنشاء والتعديل والتفعيل/التعطيل حقيقيين وبيعرضوا أخطاء السيرفر (`types/page.tsx:66-71`, `115-137`). مينفعش تمسح «المرفق المطلوب» أو «الحد الأقصى» بعد ما تحطهم، لأن القيم الفاضية بتتشال من الـPATCH (`103-112`). مدير الفرع شايف الصفحة في القائمة الجانبية، بس التحميل بيرجّع 403. |
| `/my/leaves` إجازاتي | API | لا يوجد | 🟡 | الأرصدة والطلبات حقيقية (`my/leaves/page.tsx:74-78`). مفيش زر إلغاء أو سحب. المسودات اللي فضلت من محاولات تقديم فشلت بتظهر «مسودة» (`81-83`, `257-260`). أسماء الأنواع قائمة ثابتة (`41-53`). بطاقتا casual/unpaid عمرهم ما هيظهروا (`30-38`). |

**المنطق والقواعد**
| القاعدة | الحكم ✅/🟡/🔴 | الدليل (`file:line`) | الشرح |
|---|---|---|---|
| مين يقدر يقدّم إجازة من الواجهة | 🔴 | `api/src/settings/settings.controller.ts:351-353`, `409-412`؛ `src/app/leaves/request/page.tsx:78-86`؛ `src/app/requests/page.tsx:492-499`, `688-694`؛ `api/src/auth/permissions.ts:96-116` | الشاشتين اللي بيتقدّم منهم طلب إجازة بيجيبوا الأنواع من endpoint محتاج `settings.manage`. دور `employee` صلاحياته `[]`، و`branch_manager` مفيهوش `settings.manage` (راجعت جدول `roles` في الـDB وهو مطابق). عندنا 73 موظف و11 مدير فرع، يعني 84 من 86 مستخدم قائمة الأنواع عندهم فاضية، و`leaveType` حقل إجباري. الباك نفسه بيقبل الطلب، والدليل 18 طلب LEAVE مكتمل اتعمل من حسابات موظفين آخرها 2026-09-10 09:23، يعني العطل في الواجهة بس. |
| لا أثر قبل الاعتماد | ✅ | `api/src/requests/requests.service.ts:706-711`؛ `api/src/requests/destinations.service.ts:112-139` | سجل `Leave` وخصم الرصيد بيحصلوا بس في الوجهة بعد آخر اعتماد. التقديم بيتحقق من الرصيد من غير ما يلمسه. |
| الاعتماد بيطبّق الأثر فعلًا ويبقى مصدر واحد | 🟡 | `api/src/requests/requests.service.ts:706-711`, `716-735`؛ `api/src/requests/state-machine.ts:16` | `act()` بيحفظ `APPROVED` قبل ما معاملة الوجهة تبدأ. لو الوجهة رمت خطأ، الطلب بيفضل `APPROVED` من غير Leave، ومينفعش يتعمله أي إجراء تاني. DB: الطلب #303 (موظف 178) حالته `APPROVED` و`destinationRef` بـ NULL، لأنه اتصادم مع الإجازة #72 (2026-11-16→11-18، `requestId` بـ NULL). |
| أيام العمل حسب ويك إند الموظف (جدول > فرع > عام) | ✅ | `api/src/requests/requests.service.ts:352-373`؛ `api/src/attendance/attendance.service.ts:111-120`, `179-235` | السيرفر بيعيد حساب `days` وبيتجاهل رقم العميل. DB: الإجازات #14-16 (الخميس 07-16 → السبت 07-18) متسجّلة 1 يوم. |
| استبعاد العطل الرسمية متعددة الأيام | ✅ | `api/src/attendance/attendance.service.ts:218-221` | `h.date ≤ d ≤ h.endDate`. DB: إجازات الحج #48/#49 (10-05 → 10-06) متسجّلة 1 يوم لأن 10-06 عطلة. تحفّظ: عمود `country` مش مستخدم (`attendance.service.ts:192`)، فأي عطلة بتتطبّق على كل الفروع. |
| الإجازات الطويلة (> 92 يوم تقويمي) | 🔴 | `api/src/attendance/attendance.service.ts:137-141`؛ `api/src/requests/requests.service.ts:759-762`؛ `api/src/requests/leaves.controller.ts:108-112` | الحلقة بتقف عند `i<92`. مثال محسوب: مرضية من 2026-01-04 لـ2026-06-30 = 178 يوم تقويمي و121 يوم عمل، والنظام بيعدّ 64 بس. فالخصم من رصيد الـ180 وفحص `maxDays` بيشتغلوا على 64. إجازة وضع 90 يوم عمل بتعدّي حد الـ90 غلط. وإعادة حساب الحضور بتقف عند 62 يوم. |
| كفاية الرصيد ومنع السحب على المكشوف | 🔴 | `api/src/requests/requests.service.ts:494-514`؛ `api/src/requests/leave-balances.service.ts:112-113`, `153-189`؛ `api/src/requests/destinations.service.ts:124-139` | الفحص بيحصل وقت التقديم بس، ضد المستهلك فعلًا، والطلبات المعلّقة مش بتتحجز. مثال: المتبقي 5، طلب 4 أيام في أكتوبر وطلب 4 أيام في نوفمبر (من غير تداخل)، الاتنين بيعدّوا وبيتعتمدوا، فالمستهلك +8 والرصيد الحقيقي −3. `view()` بيقصّ المتبقي عند 0 فالعجز مش بيبان. الـDB النهارده مفيهوش حالة زي دي (أعلى `taken` هو 6). |
| إجازة في سنة مالهاش صف رصيد | 🔴 | `api/src/requests/leave-balances.service.ts:134-138`, `160`, `180`؛ `api/src/requests/requests-scheduler.service.ts:14-26`؛ `api/src/requests/requests.controller.ts:93-97` | الرصيد بيتدوّر عليه بسنة `fromDate`. لو الصف مش موجود: لا حد ولا خصم. في الـDB صفوف 2026 بس، ومفيش cron للترحيل، ومفيش أي شاشة بتنادي `rollover` (بحثت في `src` ومفيش نتيجة). النهارده إجازة سنوية في 2027 بتتقبل ومش بيتخصم منها ولا يوم. ومن 2027-01-01 ده هيحصل مع كل إجازة، و`/my/leaves` هتعرض «لا توجد أرصدة». |
| مصدر الاستحقاق السنوي | 🟡 | `api/src/employees/employees.service.ts:258-283`, `266`؛ `api/src/seed/seed-requests.ts:149` | `entitled` بيتكتب مرة واحدة وقت التعيين من `leave.annual_entitled` (21). المرضي ثابت 180 في الكود. مفيش شرائح حسب مدة الخدمة. DB: السنوي 21 لـ175 صف، و0 لصفين، و30 لصف واحد. |
| شاشة السياسات بتحرّك الأرصدة | 🟡 | `api/src/employees/employees.service.ts:247-255`؛ `api/src/requests/leave-balances.service.ts:118-127`, `225-228` | `accrual_mode` و`probation_months` بيتقروا لحظيًا. لكن `annual_entitled` بيتقرى بس عند إنشاء الصف أو تغيير مفتاح الاستحقاق أو الترحيل. لو غيّرت 21 لـ30 النهارده، صفوف 2026 الموجودة هتفضل 21. |
| وضع الاستحقاق | 🟡 | `api/src/requests/leave-balances.service.ts:56`, `68-74`؛ `src/app/settings/policies/page.tsx:53` | الشهري صح: موظف متعيّن قبل 2026، في 2026-09-10 عنده 8 شهور، يعني 8×21/12 = 14.0. الواجهة بتعرض اختيار «يومي» كمان، بس أي وضع غير `monthly` بياخد الاستحقاق كله مقدّمًا، فـ«يومي» عمليًا زي «سنوي». |
| منع التداخل ومرة واحدة والحد الأقصى والمرفق الإجباري | ✅ | `api/src/requests/requests.service.ts:375-417`, `420-490`؛ `api/src/requests/destinations.service.ts:97-111` | التحقق بيحصل على السيرفر ضد الإجازات المعتمدة والطلبات اللي لسه في المسار، وفيه حارس تاني وقت التنفيذ. |
| الإجازة بأثر رجعي | 🟡 | `api/src/requests/requests.service.ts:318-491`؛ `src/app/requests/page.tsx:866-878` | مفيش منع للتواريخ اللي فاتت، وHR يقدر يقدّم نيابة عن موظف من `/requests`، والاعتماد بيحوّل الأيام من غياب لإجازة. بس مفيش حد أقصى للرجوع ولا قاعدة إن ده لـHR بس، يعني أي موظف يقدر يرجع بتاريخ طلبه أي مدة. |
| إلغاء HR لإجازة معتمدة | ✅ | `api/src/requests/leaves.controller.ts:62-120`؛ `api/src/assets/portal.controller.ts:125-145` | للمعتمدة بس، وفي حدود الفرع، والرصيد بيرجع بالطبقات، والأيام بيتعاد حسابها، والموظف بيوصله إشعار. DB: مفيش ولا صف `leave` في الحضور من غير إجازة معتمدة. تحفّظات: الإلغاء كله أو لا شيء (مفيش رجوع مبكر لجزء من الإجازة)، وإعادة الحساب بتقف عند 62 يوم، والخطأ بيتبلع (`116-118`). |
| إلغاء الموظف لإجازته (`LEAVE_MODIFY_CANCEL`) | 🟡 | `api/src/requests/destinations.service.ts:144-173`؛ `api/src/requests/requests.service.ts:494-497` | بيعدّي على سلسلة الاعتماد وبيرجّع الرصيد. لكن الـhandler مش بيتأكد من `leave.status`، والتقديم مش بيمنع طلب إلغاء مكرر لنفس الإجازة. فطلبين إلغاء، أو إلغاء بعد إلغاء HR، بيرجّعوا الأيام مرتين. جزء «التعديل» مش متنفّذ أصلًا. |
| انعكاس الإجازة المعتمدة على الحضور | 🟡 | `api/src/attendance/attendance.service.ts:967-977`, `1017-1021` | `computeDay` بيحط الحالة `leave`، ولو فيه بصمة يوم الإجازة بيعلّم `leaveConflict`. DB: من 94 يوم عمل فات جوه إجازات كاملة معتمدة، 80 عليهم صف `leave` و14 مالهمش صف خالص (الإجازات #38-#43، الموظفين 92 و93، 3 لـ18 أغسطس 2026)، لا إجازة ولا غياب. |

**ما هو ديمو / بيانات تجريبية**
- عطلة اختبار باقية في الإنتاج المحلي: `public_holidays` #10 «اختبار كلود عطلة ممتدة» (2026-10-12 → 10-14). دي بتأثر فعلًا على حساب أيام أي إجازة في الفترة دي وعلى الحضور.
- طلبات إجازة اختبارية: #288-#290 (`reason` = «اختبار»)، و#328-#331 و#344 (DRAFT) و#345 (`LEAVE_MODIFY_CANCEL` للإجازة #74)، وكلها `reason` فيها «اختبار كلود» (موظفين 10 و204 و1 و169).
- إجازة يتيمة: `leaves` #72 (موظف 178، 2026-11-16 → 11-18، `requestId` بـ NULL) من غير طلب مصدر، ومعاها الطلب العالق #303.
- 34 طلب إجازة `DRAFT` (19 LEAVE و15 LEAVE_ANNUAL) فضلوا من محاولات تقديم فشلت، لأن `create()` بيحفظ DRAFT قبل ما `submit()` يرمي (`api/src/requests/requests.service.ts:225-229`). بيظهروا «مسودة» في `/my/leaves`.
- الأرصدة المبدئية من البذرة: المرضي ثابت 180 (`api/src/seed/seed-requests.ts:133-149`)، وكل الأرصدة لفترة 2026 بس.
- قوائم ثابتة في الواجهة بدل كتالوج الأنواع: `src/app/leaves/page.tsx:24-36`، `src/app/leaves/calendar/page.tsx:22-34`, `231-254`، `src/app/my/leaves/page.tsx:30-53`، `src/app/leaves/request/page.tsx:35-47` (ألوان بس).

**العيوب**
| # | العيب | الخطورة 🔴/🟠/🟡 | الدليل (`file:line`) | الإصلاح المقترح |
|---|---|---|---|---|
| 1 | الموظف ومدير الفرع مش قادرين يقدّموا أي إجازة من الواجهة: قائمة الأنواع بترجع 403 في `/leaves/request` وفي `/requests` | 🔴 | `api/src/settings/settings.controller.ts:351-353`, `409-412`؛ `src/app/leaves/request/page.tsx:78-86`؛ `src/app/requests/page.tsx:492-499` | endpoint قراءة للأنواع المفعّلة بمصادقة بس (مثلًا `GET /requests/leave-types`)، أو استثناء `GET leave-types` من `@Perm` الكلاس. |
| 2 | مفيش ترحيل سنوي تلقائي ولا شاشة ليه. أي إجازة سنتها مالهاش صف رصيد بتتقبل من غير حد ومن غير خصم (من النهارده لتواريخ 2027، ولكل الإجازات من 2027-01-01) | 🔴 | `api/src/requests/leave-balances.service.ts:134-138`, `160`, `180`؛ `api/src/requests/requests-scheduler.service.ts:14-26` | cron يوم 1 يناير بيعمل `rollover` وبينشئ صفوف السنة لكل الموظفين النشطين، ولأنواع `annual/sick` لو الصف مش موجود يترمى خطأ بدل ما يعدّي من غير حد. |
| 3 | سحب على المكشوف: الطلبات المعلّقة مش بتتحجز والاعتماد بيخصم من غير فحص، والمتبقي بيتقصّ عند 0 فالعجز مش بيبان | 🟠 | `api/src/requests/requests.service.ts:494-514`؛ `api/src/requests/destinations.service.ts:124-139`؛ `api/src/requests/leave-balances.service.ts:112-113` | تخصم أيام الطلبات المعلّقة في `assertSufficient`، وتعيد الفحص جوه معاملة `deduct`، وتعرض القيمة السالبة بدل ما تقصّها. |
| 4 | الاعتماد بيتحفظ قبل تنفيذ الوجهة، فلو التنفيذ فشل الطلب بيفضل APPROVED من غير أثر ومش ممكن يتعمله أي إجراء (#303) | 🟠 | `api/src/requests/requests.service.ts:706-711`, `716-735` | تنقل تغيير الحالة لـAPPROVED جوه نفس معاملة `executeDestination`، أو ترجّع الحالة لـUNDER_REVIEW لو التنفيذ فشل. |
| 5 | إلغاء الإجازة من ناحية الموظف بيرجّع الرصيد مرتين: مفيش فحص لحالة الإجازة ولا منع لطلب إلغاء مكرر | 🟠 | `api/src/requests/destinations.service.ts:144-173` | تشترط `leave.status === 'APPROVED'` في الـhandler وعند التقديم، وتمنع وجود طلب إلغاء تاني لنفس `leaveId` لسه في المسار. |
| 6 | سقف 92 يوم في عدّ أيام العمل و62 يوم في إعادة حساب الحضور: الإجازات الطويلة بتتعدّ ناقص (121 بقت 64) وحد `maxDays` بيتخطى | 🟠 | `api/src/attendance/attendance.service.ts:137-141`؛ `api/src/requests/requests.service.ts:759-762`؛ `api/src/requests/leaves.controller.ts:108-112` | ترفع السقف لـ366 يوم على الأقل (أو تشيله بعد ما تحسب السياق مرة واحدة)، وتعيد الحساب للمدى كله. |
| 7 | إعدادات السياسات مش بتسري على الأرصدة الموجودة: `annual_entitled` بيتقرى وقت الإنشاء بس، ووضع «يومي» بيشتغل «سنوي»، والمرضي 180 ثابت في الكود | 🟠 | `api/src/employees/employees.service.ts:247-283`؛ `api/src/requests/leave-balances.service.ts:56`؛ `src/app/settings/policies/page.tsx:53` | الاستحقاق يتقرى لحظيًا من الإعداد (مع override للموظف)، وتنفّذ «يومي» أو تشيله، وتنقل الـ180 لإعداد. |
| 8 | شاشة الأرصدة بتسمّي المتراكم لحد النهارده «مستحق/استحقاق السنة» (14 بدل 21) | 🟡 | `src/app/leaves/balance/page.tsx:221-223`, `384-392`؛ `api/src/requests/leave-balances.service.ts:109` | ترجّع من السيرفر `annualEntitlement` و`accruedToDate` الاتنين وتعرضهم بعنوانين منفصلين. |
| 9 | طلب الإجازة بيعدّ أيام تقويمية، فبيطلع تحذير «تجاوز الرصيد» غلط. ومفيش نصف يوم ولا تقديم نيابة، ورسالة النجاح دايمًا «في مسار الموافقات» | 🟡 | `src/app/leaves/request/page.tsx:98-112`, `288-293`, `156` | تستخدم `fetchWorkingDays` زي `/requests`، وتضيف `period` وخيار نيابة، وتبني رسالة النجاح على `req.status`. |
| 10 | العطل الرسمية: مفيش حذف (الـAPI بيرجّع 404)، والـPATCH من غير تحقق، ومفيش شرط النهاية ≥ البداية، ومفيش إعادة حساب للحضور بعد الإضافة أو التعديل، و`country` مش مستخدم | 🟡 | `api/src/assets/catalogs.controller.ts:122-160`, `175-181`؛ `api/src/attendance/attendance.service.ts:192` | تضيف `DELETE`، وتنادي `validate` في التعديل كمان، وتتحقق من `endDate ≥ date`، وتعيد حساب أيام المدى (زي الورديات `152-158`)، وتربط العطلة بفرع أو دولة. |
| 11 | صفحتي «العطل» و«الأنواع» ظاهرين لمدير الفرع (`leaves.view_all`) بس الحفظ أو التحميل بيرجّع 403 | 🟡 | `src/components/layout/Sidebar.tsx:104-114`؛ `src/components/layout/MainLayout.tsx:38` | تحط `perm: 'settings.manage'` على العنصرين دول في القائمة الجانبية وفي `PATH_PERMS`. |
| 12 | مينفعش تمسح «المرفق المطلوب» أو «الحد الأقصى» لنوع إجازة | 🟡 | `src/app/leaves/types/page.tsx:103-112`؛ `api/src/settings/settings.controller.ts:96-104` | تبعت `null` صريحة وتقبلها في الـDTO. |
| 13 | أزرار ميتة: تصدير وعين وترقيم ثابت في `/leaves`، وتصدير و+/- معطّلة في `/leaves/balance` (ومفيش نوع طلب لتعديل الرصيد) | 🟡 | `src/app/leaves/page.tsx:146-149`, `350-352`, `373-389`؛ `src/app/leaves/balance/page.tsx:124-127`, `333-346` | تشيلهم أو توصّلهم، وتضيف مسار تسوية رصيد (`leave_balances.manage`) بسجل تدقيق. |
| 14 | `/my/leaves` مفيهاش إلغاء أو سحب، وبتعرض 34 مسودة عالقة | 🟡 | `src/app/my/leaves/page.tsx:81-83`, `257-260`؛ `api/src/requests/requests.service.ts:225-229` | زر سحب للـDRAFT والـUNDER_REVIEW، وزر «طلب إلغاء» للمعتمدة، ومتحفظش DRAFT لو التقديم فشل. |
| 15 | أيام إجازة معتمدة مالهاش أي صف حضور (14 يوم للموظفين 92 و93 في أغسطس) | 🟡 | `api/src/attendance/attendance.service.ts:967-977`؛ `api/src/attendance/attendance-scheduler.service.ts:15-26` | تشغّل تجسيد الأيام على كل مدى الإجازة وقت الاعتماد والبذر، مش بس آخر يومين. |
| 16 | التقويم: ويك إند جمعة/سبت ثابت، ودليل 5 أنواع، ومفيش إجازات معلّقة، وعطلة ممتدة على 3 شهور بتختفي من الشهر اللي في النص | 🟡 | `src/app/leaves/calendar/page.tsx:182`, `231-254`؛ `api/src/assets/portal.controller.ts:46-48` | الويك إند من جدول الفرع، والدليل من كتالوج الأنواع، وفلتر التداخل يبقى `date ≤ آخر الشهر && endDate ≥ أول الشهر`. |
| 17 | `GET /requests/leave-balances/:employeeId` من غير نطاق فرع: مدير أي فرع يقدر يقرأ رصيد أي موظف | 🟡 | `api/src/requests/requests.controller.ts:86-90` | تطبّق `branchScopeOf` قبل ما ترجّع الرصيد. |
| 18 | الإجازة بأثر رجعي من غير حد أقصى ولا قاعدة إنها لـHR بس | 🟡 | `api/src/requests/requests.service.ts:318-491` | إعداد `leave.max_backdate_days`، ويتخطاه بس اللي عنده `requests.create_on_behalf`. |
| 19 | نصف اليوم مش ظاهر في السجل، و`ApiLeave` مفيهاش `period` | 🟡 | `src/lib/api.ts:210-214`؛ `src/app/leaves/page.tsx:341-343` | تضيف `period` للنوع وتعرض «نصف يوم صباحي/مسائي». |

ادعاءات دُحضت أو اتصحّحت: مثال المرضية الطويلة رقمه الصحيح 121 يوم عمل واللي بيتعدّ 64 (مش 126 و66). تغطية الحضور الحالية 80 من 94 (مش 85 من 99). السحب على المكشوف ثغرة في الكود ومش موجود فعلًا في الـDB النهارده (أعلى `taken` = 6). الأرقام اتحركت: صفوف المرضي 178 والموظفين 179.

**ما يعمل جيدًا**
- كل الشاشات السبع موصولة بالباك الحقيقي، ومفيش زر حفظ بيعمل `setState` أو toast بس. أخطاء السيرفر (4xx) بتظهر للمستخدم في كل الشاشات اللي بتكتب.
- التقديم بيعدّي على محرك الطلبات، والأثر (سجل Leave والخصم) بيحصل بس بعد آخر اعتماد، جوه معاملة وبحارس ضد التداخل.
- السيرفر بيعيد حساب الأيام بأيام العمل الفعلية حسب جدول الموظف ثم الفرع ثم العام، وبيستبعد العطل متعددة الأيام بـ`endDate`، وبيرفض مدى كله عطلات.
- قواعد النوع (`maxDays`، `oncePerService`، المرفق الإجباري، التفعيل) متطبّقة على السيرفر، ومش مكتفية بالواجهة.
- إلغاء HR المباشر كامل: الرصيد بيرجع بالطبقات، والحضور بيتعاد حسابه، والموظف بيوصله إشعار. ومفيش صف `leave` واحد في الحضور من غير إجازة معتمدة.
- الرصيد بالطبقات (مرحّل بصلاحية بيتخصم منه الأول، بعدين استحقاق السنة) مع تراكم شهري بعد فترة التجربة، وده بيشتغل صح في وضع `monthly`.

---

## 5) الإعدادات كلها (الفروع، الأقسام، الفرق، المسميات، الدرجات، مراكز التكلفة، أيام العمل، السياسات، الاعتمادات، أنواع الطلبات، أنواع الأذونات، أنواع العهد، المستندات وقوالبها، المستخدمين، الأدوار)
**الحكم في سطر:** 13 من 17 شاشة موصولة بالـAPI فعلًا في القراءة والحفظ. في المقابل صفحة الإعدادات الرئيسية وأيام العمل وقوالب المستندات أغلبها ديمو بأزرار حفظ ميتة. وفيه ثغرة تصعيد صلاحيات حرجة: مدير الموارد البشرية يقدر يمنح نفسه `*` ويبقى عمليًا مدير نظام. وكمان 22 نوع طلب نشط بيتعتمد من غير أي أثر، وزر «تعطيل» الدورة العامة شكلي ما بيوقفش حاجة.

**الشاشات**
| الشاشة | مصدر البيانات (API / ثابت / مختلط) | الحفظ (API / محلي / لا يوجد) | الحالة ✅/🟡/🔴 | ملاحظة |
|---|---|---|---|---|
| `/settings` (الصفحة الرئيسية) | مختلط | مختلط | 🔴 | الحقيقي بس 3 لوحات «إعدادات المحرك» (`src/app/settings/page.tsx:67-82`، `GET/PATCH /settings/config` في `:107-149`). الباقي كله قيم مكتوبة في الكود: بيانات الشركة `:392-453`، ونسبة اكتمال 86% محسوبة من booleans ثابتة `:251-262` (6÷7=85.7)، والإشعارات `:572-600`، وعدد الأدوار 2/3/15/230 `:629-634` (الحقيقي في DB: 1/1/11/73)، والأمان و2FA `:692-748`، والتكاملات «متصل» `:1038-1045`. أزرار الحفظ والرفع والربط من غير `onClick` في `:342,381,468,565,683,761,841,869,887,1060` |
| `/settings/branches` | API | API | 🟡 | `fetchBranches/fetchEmployees` في `branches/page.tsx:62`، والحفظ `createBranch/updateBranch` في `:132-136`. المدير ومركز التكلفة ما ينفعش يتمسحوا لأن القيمة الفاضية بتتبعت `undefined` (`:126-127`). مركز التكلفة نص حر مش مربوط بالكتالوج (`:569-577`). و`weekendDays` بتاع الفرع ما لوش أي حقل |
| `/settings/departments` | API | API | 🟡 | `Promise.all` لـ 4 endpoints في `departments/page.tsx:63-68`، والحفظ في `:145-150`. القسم الأب والمدير ما ينفعش يتمسحوا (`:141-142`). الحذف معطّل (`:510`) |
| `/settings/teams` | API | API | 🟡 | الجلب في `teams/page.tsx:56-60` والحفظ في `:137-142`. الشاشة بتقول إن قائد الفريق هو المدير المباشر (`:184,547`)، والمحلِّل فعليًا بيقدّم `managerEmployeeId` عليه |
| `/settings/job-titles` | API | API | 🟡 | CRUD حقيقي على `/catalogs/job-titles` (`job-titles/page.tsx:46,94,101,115`)، لكن الكتالوج مش مستخدم: فورم الموظف فيه قائمة مكتوبة في الكود (`src/components/EmployeeForm.tsx:236,1214-1222`) |
| `/settings/grades` | API | API | 🟡 | CRUD حقيقي (`grades/page.tsx:51,106,109,123`). الفورم بيعرض «Grade 1..5» ثابتة (`EmployeeForm.tsx:1226-1233`) والموجود في DB 3 درجات بس. شريط النطاق دايمًا 100% (`grades/page.tsx:323`) |
| `/settings/cost-centers` | API | API | ✅ | CRUD حقيقي (`cost-centers/page.tsx:43,80,87,101`) ومستخدم فعلًا في فورم الموظف (`EmployeeForm.tsx:589-591,652`). مفيش حذف وده مقصود (`:98`) |
| `/settings/work-days` | مختلط | مختلط | 🔴 | الحقيقي: مفاتيح الأوفرتايم (`work-days/page.tsx:484-486`) والقواعد الاستثنائية وفترات الأوفرتايم (`:338-347`). أما الجداول المسمّاة فمكتوبة في الكود (`:239-304`، بعدد 45/23 موظف والحقيقي 2/2)، والإضافة والتعديل والحذف والتعيين كلها `setSchedules` محلي (`:513-567,1306-1372`)، والموظفين والأقسام وهمية (`:2085-2102`)، وزر الحفظ العلوي معطّل (`:597-604`) |
| `/settings/policies` | API | API | 🟡 | ديناميكية بجد: بتعرض المفاتيح الموجودة في DB بس (`policies/page.tsx:267`)، وبتتحقق من الأرقام (`:139-152`) وبتحفظ لكل مفتاح اتعدّل (`:157-159`). خيار «يومي» في `leave.accrual_mode` مش متنفّذ (`:53`) |
| `/settings/approvals` | API | API | 🟡 | باني دورات حقيقي (خطوات متوازية، عتبات، SLA، فرع، تنفيذ فوري). لكن النص بيقول إن الدورة الفاضية بتتنفذ أوتوماتيك (`approvals/page.tsx:1076-1080`)، و«تعطيل» الدورة العامة شكلي (`:446-454`)، والفرع مقفول في التعديل (`:1045`)، والنسخ والحذف معطّلين (`:727-734,783-790`) |
| `/settings/request-types` | API | API | 🟡 | باني حقيقي (`request-types/page.tsx:192-197,367-392`). تعديل 31 نوع مبذور نشط بيرجع 400 (شوف العيوب). وخيار «بدون دورة (تنفيذ فوري)» (`:874`) كلامه غلط |
| `/settings/permission-types` | API | API | ✅ | CRUD حقيقي (`permission-types/page.tsx:54,121,128,144`) والمحرك بيقرأه. الجدول مليان صفوف اختبار (شوف قسم الديمو) |
| `/settings/asset-types` | API | API | 🟡 | اسمها «أنواع العهد» لكنها في الحقيقة سجل الأصول (`fetchAssets/createAsset/updateAsset/retire/reactivate` في `asset-types/page.tsx:68,136,139,156,169`). جدول `asset_types` (6 صفوف) ما لوش أي واجهة، والفئة نص حر (`:83-87`) |
| `/settings/documents` | API (مشتق) | لا يوجد | 🟡 | الأنواع مشتقة من المستندات الموجودة (`documents/page.tsx:46-57`). زر «إضافة نوع» معطّل (`:86-93`). ومفيش `MainLayout` (`:76`) |
| `/settings/document-templates` | ثابت (الإحصاءات بس من API) | لا يوجد | 🔴 | القوالب `builtInTemplates` مكتوبة في الكود (`document-templates/page.tsx:164-244,282`). الحفظ معطّل (`:640-646`) وتنزيل PDF معطّل (`:734`). ومفيش backend أصلًا: `GET /api/document-templates` بيرجع 404 |
| `/settings/users` | API | API | 🟡 | `Promise.all` لـ 5 endpoints (`users/page.tsx:119-125`)، والحفظ `updateUser/setUserPermissions/createUser` (`:251-274`). الأدوار المخصّصة بتظهر في القائمة (`:592-598`) والباك بيرفضها. والفرع وربط الموظف ما ينفعش يتمسحوا (`:254-255`) |
| `/settings/roles` | API | API | 🟡 | محرر حزم صلاحيات حقيقي (`roles/page.tsx:75-79,143-166`). الأدوار اللي بتتعمل هنا ما ينفعش تتسند لحد، وتعديلها ما بيبطلش الجلسات القائمة |

**المنطق والقواعد**
| القاعدة | الحكم ✅/🟡/🔴 | الدليل (`file:line`) | الشرح |
|---|---|---|---|
| الصلاحيات الفعلية = حزمة الدور + GRANT − REVOKE | ✅ | `api/src/auth/permissions.ts:120-130`، `api/src/auth/auth.service.ts:37-70` | الحساب صحيح، و`super_admin` بيتخطى الفحص (`api/src/auth/guards.ts:39`) |
| منح `*` عبر تجاوزات المستخدم | 🔴 | `api/src/auth/roles.controller.ts:66,172-184` | `validatePermList` بيستثني `*` من الرفض، والمسار محمي بـ`users.manage` أو `roles.manage` من غير فحص للنفس. مستخدم `hr_manager` رقم 12 يقدر يبعت `PUT /users/12/permissions {grants:['*']}` فيتخطى كل `@Perm`. في نفس الوقت إنشاء وتعديل الأدوار بيرفضوا `*` (`:113-115,140-142`) |
| إبطال التوكن عند تغيير المستخدم | ✅ | `api/src/auth/users.controller.ts:215-223`، `api/src/auth/roles.controller.ts:197-199`، `api/src/auth/jwt.strategy.ts:26-37` | تغيير الدور أو الصلاحيات أو التعطيل أو كلمة المرور بيزوّد `tokenVersion` |
| إبطال التوكن عند تعديل الدور نفسه | 🟡 | `api/src/auth/roles.controller.ts:127-154`، `api/src/auth/auth.service.ts:87-96` | الصلاحيات محفوظة جوه الـJWT، وتعديل حزمة الدور ما بيزوّدش `tokenVersion`. يعني السحب ما بيسريش غير بعد إعادة الدخول، والتوكن عمره `8h` (`api/src/auth/auth.module.ts:24`) |
| الدور المخصّص المعطَّل | 🟡 | `api/src/auth/auth.service.ts:39` | `findOne({code})` من غير شرط `isActive`، فالتعطيل ما لوش أثر. والأدوار المخصصة أصلًا ما تتسندش لحد (`api/src/auth/users.controller.ts:35,76,95`) |
| الدورة الفاضية | ✅ باك / 🔴 واجهة | `api/src/requests/requests.service.ts:520-539` | الطلب بيتوقف إلا لو `autoApprove=true`. الواجهة بتقول «يُنفَّذ أوتوماتيكياً» (`approvals/page.tsx:1076-1080`) و«تنفيذ فوري» (`request-types/page.tsx:874`)، والنوع الجديد من غير دورة بيتعمل له دورة فاضية بتوقف الطلب (`settings.controller.ts:665-682`) |
| تعطيل دورة عامة | 🔴 | `api/src/requests/requests.service.ts:555-571` | الدورة العامة بتتحمّل بالـid من غير `isActive`، والفلترة بـ`isActive` على نسخة الفرع بس. «تعطيل» الدورة العامة ما بيغيّرش أي حاجة في التوجيه |
| تنفيذ الوجهة بعد الاعتماد | 🔴 | `api/src/requests/destinations.service.ts:71-78,720-756` | أي handler مش موجود في الخريطة بيرجع `completed:true` ومعاه سجل `REQ` بس. 23 نوع نشط على handlers مش متنفّذة. `PERMISSION` بس ليه أثر لأن محرك الحضور بيقرأه مباشرة (`api/src/attendance/attendance.service.ts:747`)، والـ22 الباقيين من غير أثر |
| تحقق الـconfig | 🟡 | `api/src/settings/settings.controller.ts:376-403` | المفاتيح الرقمية الحرجة محمية (قسمة على صفر). `attendance.weekend_days` نص حر من غير تحقق، وأي قيمة غلط بتتقسم بصمت في `attendance.service.ts:198` |
| طريقة استحقاق الإجازة | 🟡 | `api/src/requests/leave-balances.service.ts:56`، `api/src/offboarding/offboarding.service.ts:273` | «شهري» متنفّذ صح، و«سنوي» = كامل مقدمًا وده صحيح. أما «يومي» (`policies/page.tsx:53`) فبيدّي الاستحقاق كامل مقدمًا، ودي نتيجة غلط |
| أولوية العطلة الأسبوعية | ✅ / 🟡 | `api/src/attendance/attendance.service.ts:187-191,999-1011` | الترتيب: جدول الموظف ثم الفرع ثم الإعداد العام. لكن `branch.weekendDays` ما لوش حقل في `api/src/org/org.dto.ts:14-119` ولا في الواجهة، فمستحيل يتضبط (كل الفروع قيمتها NULL) |
| حلّ المدير المباشر | 🟡 | `api/src/requests/approver-resolver.service.ts:33-54` | الترتيب: `managerEmployeeId` ثم قائد الفريق ثم مدير القسم ثم مدير الفرع. في DB فيه 136 من 178 موظف عندهم `managerEmployeeId`، فكلام شاشة الفرق غلط عند أغلب الموظفين |
| نطاق الفرع في كتابات التكوين | 🟠 | `api/src/org/org.controller.ts:40-95`، `api/src/auth/users.controller.ts:188-227` | القراءة مقصورة على الفرع (`org.service.ts:41-44`، `users.controller.ts:133-138`)، أما الكتابة فمفيهاش نطاق. HR فرع 1 يقدر يعدّل فروع وأقسام ومستخدمين فرع 11 ويعيد تعيين كلمات مرورهم |
| هرمية الأقسام | 🟡 | `api/src/org/org.service.ts:84-86,103-111` | الإنشاء بيتأكد إن القسم الأب في نفس الفرع، والتعديل لأ. ومفيش منع للدوائر (A←B←A) |
| تحقق الكتالوجات عند التعديل | 🟠 | `api/src/assets/catalogs.controller.ts:123-150` | `validate()` بتتنادى في الإنشاء بس، والـbody `Record<string,unknown>` بيعدّي من `whitelist` (`api/src/main.ts:16`). النتيجة إن regex `weekendDays` في جداول العمل (`:231-238`) ممكن يتخطّى، وأي عمود ممكن يتكتب |
| نطاق راتب الدرجة | 🟡 | `api/src/assets/catalogs.controller.ts:208-210` | مفيش فحص `min ≤ max`، والنطاق مش مفروض في أي مكان (ما يظهرش غير في `seed.ts` والكيان) |
| تحقق خطوات الدورة عند الاستبدال | 🟡 | `api/src/settings/settings.controller.ts:556-570` مقابل `:483-494` | `dto` معرّف كنوع inline فالـDTO validation ما بيشتغلش. فحص «موظف بعينه بدون موظف» والعتبة الناقصة موجود في الإنشاء بس |

**ما هو ديمو / بيانات تجريبية**
- صفحة الإعدادات الرئيسية: بيانات شركة وهمية («شركة التقنية المتقدمة» وسجل 1010123456 وضريبي 300123456789012) في `src/app/settings/page.tsx:392-453`، وعدد الأدوار 2/3/15/230 وآخر مستخدمين وهميين في `:629-656`، وتكاملات GOSI/مُدد/ZKTeco معروضة «متصل» في `:1038-1045`، ومفتاح 2FA مفعّل وهو مش موجود في `:730`.
- صفحة الإعدادات الرئيسية كمان: قائمة 8 إجازات ثابتة في `:849-857` (الحقيقي 11 صف في `leave_types`)، وشرائح تأخير ثابتة 4 في `:810-815` (الحقيقي 3 صفوف في `lateness_tiers`).
- أيام العمل: `initialSchedules` في `src/app/settings/work-days/page.tsx:239-304`، و`sampleEmployees` في `:2085-2094`، و`departments` الوهمية في `:2096-2102`.
- قوالب المستندات: `builtInTemplates` في `src/app/settings/document-templates/page.tsx:164-244`. أنواع المستندات نفسها ثابتة في `src/lib/doc-types.ts:4-16`.
- `permission_types` (DB): 12 من 19 صف معطّل وأسماؤهم اختبار («نوع اختبار PI995»، «إذن شهري PB9388»، …). ولسه فيه صفوف اختبار نشطة: 14 «إذن SF18109»، و16 «إذن SF50976»، و17 «مسائي اودت45»، و18 «صباحي اودت45»، و19 «مسائي KAR18391».
- `branches` (DB): 8 من 9 فروع أكوادها اختبار (`ABS12007`، `OBL62816`، `PR91686`، `SCP48298`…). وفرع 1 عليه `costCenter='CC-100'` وده مش موجود في `cost_centers`.
- `cost_centers` (DB): فيه صفّين اختبار بس `CC-PI995` و`CC-PI36048`. و`work_schedules` فيه «جدول WS85620». و`asset_types` فيه «لابتوب 2».
- `roles` (DB): فيه دور اختبار `pr_test_42103` وكمان `payroll_officer`، والاتنين ما ينفعش يتسندوا لأي مستخدم.
- `approval_chains` (DB): الدورة 97 `CH_LEAVE_COMPENSATORY` فاضية ومعطّلة ومربوطة بفرع 1.
- `requests_config`: قيمة `attendance.device_key = 'zk-device-key-change-me'` هي نفس القيمة المبذورة (`api/src/seed/requests-seed.data.ts:251`) وما اتغيّرتش.

**العيوب**
| # | العيب | الخطورة 🔴/🟠/🟡 | الدليل (`file:line`) | الإصلاح المقترح |
|---|---|---|---|---|
| 1 | تصعيد صلاحيات: أي حد عنده `users.manage` (يعني HR) يقدر يمنح نفسه أو غيره `*` عن طريق تجاوزات المستخدم، فيعدّي كل الحراسات | 🔴 | `api/src/auth/roles.controller.ts:66,172-184`، `api/src/auth/permissions.ts:125`، `api/src/auth/guards.ts:39` | رفض `*` في `grants` زي ما الأدوار بترفضه، ومنع تعديل صلاحيات النفس، ومنح الصلاحيات الحساسة لـ`super_admin` بس |
| 2 | 22 نوع طلب نشط بيتعتمد ويتسجّل «مكتمل» من غير أي أثر، منهم `BUSINESS_TRIP` (10 معتمدة) و`REMOTE_WORK` (2) و`SHIFT_SWAP` و`CONTRACT_RENEWAL` و`TITLE_CHANGE` | 🔴 | `api/src/requests/destinations.service.ts:71-78,755` | منع التقديم أو التفعيل لأي نوع الـhandler بتاعه مش متنفّذ، أو تحويله لـ`none` بتسمية صريحة. وربط المأمورية والعمل عن بعد بالحضور |
| 3 | زر «تعطيل» الدورة العامة شكلي: الطلبات بتفضل ماشية عليها | 🔴 | `api/src/requests/requests.service.ts:555-558`، `src/app/settings/approvals/page.tsx:446-454` | احترام `isActive` في `resolveChain` (الدورة المعطّلة توقف الطلب برسالة واضحة)، أو شيل الزر من الدورات العامة |
| 4 | صفحة الإعدادات الرئيسية بتعرض أمان وإشعارات وتكاملات وبيانات شركة كأنها حقيقية، وأزرار حفظها ما بتبعتش أي HTTP | 🔴 | `src/app/settings/page.tsx:342,381,468,565,683,761,841,869,887,1060` | شيل الأقسام الوهمية أو علّمها «غير مفعّل»، وبناء `company_profile` حقيقي |
| 5 | الجداول المسمّاة وتعيينها للموظفين في أيام العمل ديمو، ومفيش أي واجهة لكتالوج `work_schedules` الحقيقي اللي المحرك بيقرأه. كمان التنويه بيقول إن أيام الراحة جاية من الورديات وده غلط | 🔴 | `src/app/settings/work-days/page.tsx:239-304,1306-1372,959-963`، `api/src/assets/catalogs.controller.ts:79-80,109-118`، `api/src/attendance/attendance.service.ts:999-1011` | ربط الشاشة بـ`/catalogs/work-schedules` (CRUD) وتعيين `employee.workScheduleId`، وتصحيح نص التنويه |
| 6 | تعديل أي نوع طلب من 31 نوع مبذور نشط بيرجع 400، لأن الشاشة بتبعت handler تاني مش موجود في `AVAILABLE_HANDLERS` | 🟠 | `src/app/settings/request-types/page.tsx:278,371`، `api/src/settings/settings.controller.ts:718-721` | عدم إرسال `destinationHandler` لو ما اتغيّرش، أو قبول الـhandler الحالي للنوع |
| 7 | الواجهة بتقول إن الدورة الفاضية أو «بدون دورة» معناها تنفيذ فوري، والباك بيوقف الطلب | 🟠 | `src/app/settings/approvals/page.tsx:1076-1080`، `src/app/settings/request-types/page.tsx:874`، `api/src/requests/requests.service.ts:530-535` | تصحيح النصوص: «بلا خطوات = الطلب يتوقف لحد ما تتضبط، إلا لو فعّلت التنفيذ الفوري» |
| 8 | كتابات التكوين والمستخدمين مش مقيّدة بالفرع: HR فرع 1 يقدر يعدّل فروع وأقسام وفرق ومستخدمين أي فرع ويعيد تعيين كلمات مرورهم | 🟠 | `api/src/org/org.controller.ts:40-95`، `api/src/auth/users.controller.ts:188-227` | تطبيق `branchScopeOf` على الكتابة زي القراءة |
| 9 | `PATCH /catalogs/:kind/:id` من غير أي تحقق وبيقبل أي عمود (mass assignment) | 🟠 | `api/src/assets/catalogs.controller.ts:139-150` مقابل `:126` | نداء `validate()` في التعديل وقائمة حقول مسموحة لكل كتالوج |
| 10 | الأدوار المخصّصة ميتة: الشاشة بتعرضها للإسناد والباك بيقبل الأدوار الأربعة بس (400)، وتعطيلها مش متحترم | 🟠 | `src/app/settings/users/page.tsx:592-598`، `api/src/auth/users.controller.ts:35,76,95`، `api/src/auth/auth.service.ts:39` | التحقق من `role` مقابل جدول `roles` النشط، وفلترة `isActive` في `resolvePermissions` |
| 11 | مفتاح جهاز البصمة لسه القيمة المبذورة، و`POST /attendance/punches` بيقبله من غير JWT، فأي حد عنده الكود يقدر يحقن بصمات | 🟠 | `api/src/attendance/attendance.controller.ts:71-75`، `api/src/attendance/attendance.service.ts:433-438`، `api/src/seed/requests-seed.data.ts:251` | رفض القيمة الافتراضية (أو توليد مفتاح عشوائي أول تشغيل) وتنبيه في الإعدادات |
| 12 | كتالوج المسميات والدرجات مش مستخدم في فورم الموظف: المسميات 4 ثابتة، والدرجات «Grade 1..5» والموجود 3 بس. اختيار 4 أو 5 بيحفظ `gradeId` معلّق (مفيش FK على `employees`). 4 من 178 موظف بس مسمّاهم مطابق للكتالوج | 🟠 | `src/components/EmployeeForm.tsx:236,1214-1233,716` | تحميل `/catalogs/job-titles` و`/catalogs/grades` النشطة في الفورم، وإضافة FK أو تحقق في الباك |
| 13 | تعديل صلاحيات الدور ما بيبطلش الجلسات القائمة: السحب ما بيسريش لحد 8 ساعات | 🟡 | `api/src/auth/roles.controller.ts:127-154`، `api/src/auth/auth.module.ts:24` | زيادة `tokenVersion` لكل مستخدمي الدور عند تعديله |
| 14 | استبدال خطوات الدورة من غير DTO validation (نوع inline)، فخطوة «موظف بعينه» ممكن تتحفظ من غير موظف | 🟡 | `api/src/settings/settings.controller.ts:556-570` | استخدام `class ReplaceStepsDto` بـ`@ValidateNested` وتكرار فحوص الإنشاء |
| 15 | الروابط الاختيارية ما ينفعش تتمسح بعد ما تتضبط (مدير، أب، قائد، مركز تكلفة، فرع وموظف المستخدم) | 🟡 | `src/app/settings/branches/page.tsx:126-127`، `src/app/settings/departments/page.tsx:141-142`، `src/app/settings/teams/page.tsx:134`، `src/app/settings/users/page.tsx:254-255` | إرسال `null` للقيمة الفاضية وقبوله في الـDTO |
| 16 | `branch.weekendDays` مستخدم في المحرك ومستحيل يتضبط، ومركز تكلفة الفرع نص حر (`CC-100` مش موجود في الكتالوج) | 🟡 | `api/src/attendance/attendance.service.ts:189`، `api/src/org/org.dto.ts:14-119`، `src/app/settings/branches/page.tsx:569-577` | إضافة `weekendDays` للـDTO والشاشة، وتحويل مركز التكلفة لـselect من `cost_centers` |
| 17 | تعديل القسم ما بيمنعش أب من فرع تاني ولا الدوائر | 🟡 | `api/src/org/org.service.ts:103-111` | تكرار فحص الفرع اللي في `:84-86` وفحص الأسلاف |
| 18 | خيار الاستحقاق «يومي» بيدّي الرصيد كامل مقدمًا، و`weekend_days` من غير تحقق | 🟡 | `src/app/settings/policies/page.tsx:53,68`، `api/src/requests/leave-balances.service.ts:56`، `api/src/settings/settings.controller.ts:376-403` | شيل «يومي» أو تنفيذه، وregex `^([A-Z]{3})(,[A-Z]{3})*$` للمفتاح |
| 19 | شاشة الفرق بتقول «مدير الفريق هو المدير المباشر» والمحلِّل بيقدّم `managerEmployeeId` | 🟡 | `src/app/settings/teams/page.tsx:184,547`، `api/src/requests/approver-resolver.service.ts:37` | تصحيح النص أو توحيد المصدر |
| 20 | شارة «قابل للتفويض» ظاهرة على الـ67 خطوة كلها والتفويض مش موجود | 🟡 | `src/app/settings/approvals/page.tsx:847,935`، `api/src/requests/entities/approval-step.entity.ts:70` | إخفاء الشارة لحد ما يتبني التفويض |
| 21 | الدرجات بتقبل `min > max`، والنطاق مش مفروض على الراتب، والشريط دايمًا 100% | 🟡 | `api/src/assets/catalogs.controller.ts:208-210`، `src/app/settings/grades/page.tsx:323` | تحقق `min ≤ max` وتحذير في الفورم لو الراتب برّه نطاق الدرجة |
| 22 | «أنواع العهد» هي سجل الأصول، وجدول `asset_types` يتيم. والشاشة محروسة بـ`settings.manage` والباك بيطلب `custody.assign`، فمدير الفرع اللي معاه `custody.assign` ما يوصلهاش | 🟡 | `src/components/layout/MainLayout.tsx:35`، `api/src/assets/assets.controller.ts:74-122` | إعادة التسمية، وربط الفئة بـ`asset_types`، وتوحيد الصلاحية |
| 23 | صفحتا المستندات والقوالب من غير `MainLayout` (لا sidebar ولا حارس صلاحية)، والقوالب ما لهاش backend، وخطاب الـPDF مش بيستخدمها | 🟡 | `src/app/settings/documents/page.tsx:76`، `src/app/settings/document-templates/page.tsx:359,640-646` | لف الصفحتين بـ`MainLayout`، وبناء جدول `document_templates` وربطه بـ`letterHandler` |
| 24 | شاشة أنواع الطلبات بتقع كلها لدور عنده `request_types.manage` بس، لأنها بتحمّل الدورات في نفس `Promise.all`. والفرع مقفول في تعديل الدورة مع إن الـAPI بيدعمه | 🟡 | `src/app/settings/request-types/page.tsx:192-197`، `src/app/settings/approvals/page.tsx:1045`، `api/src/settings/settings.controller.ts:536-546` | تحميل الدورات بـ`allSettled`، وفتح حقل الفرع في التعديل |
| 25 | `GET /catalogs/:kind` من غير `@Perm`، فأي موظف مسجّل يقرأ نطاقات رواتب الدرجات وسيريالات الأجهزة | 🟡 | `api/src/assets/catalogs.controller.ts:87-89` | تقييد القراءة الحساسة (grades/devices) بصلاحية |
| 26 | الاختصار «س» مكرر للأحد والسبت | 🟡 | `src/app/settings/work-days/page.tsx:51,57` | «ح» للأحد |

ادعاءات دُحضت: «المحرك ينفّذ `monthly` فقط» مش دقيقة، لأن «سنوي» شغال صح كاستحقاق كامل مقدمًا (`leave-balances.service.ts:56`)، والغلط في «يومي» بس. وملاحظة «الروابط السريعة ناقصة» صحيحة، لكن ما لهاش أثر على الوصول لأن الـSidebar فيه الأربع شاشات (`src/components/layout/Sidebar.tsx:193-199`).

**ما يعمل جيدًا**
- 13 شاشة إعدادات بتقرأ وتكتب على API حقيقي، وبتعرض رسائل الخطأ العربية من السيرفر (`modalError`/`setError` في كل الصفحات)، والطلبات من غير توكن بترجع 401 (`/api/settings/config` و`/api/users` و`/api/catalogs/job-titles`).
- باني الدورات قوي: خطوات متوازية وعتبات شرطية وSLA وتصعيد ونسخة فرع تتقدّم على العامة، والطلبات الجارية ما بتتأثرش بتعديل الخطوات (`settings.controller.ts:553-595`، `requests.service.ts:560-571`).
- باني أنواع الطلبات بيولّد الكود ويتحقق من الحقول المخصصة والجمهور، وبيربط كل نوع جديد بدورة خاصة بيه آمنة ما بتنفّذش أي حاجة من غير اعتماد (`settings.controller.ts:611-700`).
- حماية مفاتيح المحرك الرقمية من صفر أو سالب على الواجهة والباك (`settings.controller.ts:376-403`، `policies/page.tsx:139-152`).
- إبطال فوري للتوكن عند تغيير دور المستخدم أو صلاحياته أو تعطيله أو كلمة مروره، مع فحص `isActive` و`tokenVersion` في كل طلب (`jwt.strategy.ts:26-37`).
- مراكز التكلفة وأنواع الأذونات مستخدمة فعلًا في فورم الموظف والمحرك، مش كتالوجات ميتة.
- الحذف مقفول عمدًا (تعطيل بدل حذف) في الفروع والأقسام والدورات ومراكز التكلفة، فمفيش كسر لمراجع تاريخية.

---

## 6) الطلبات والاعتمادات والخدمة الذاتية (تقديم الطلب، صندوق الاعتماد، لوحة الطلبات، عهدتي، مستنداتي، ملفي)
**الحكم في سطر:** المحرّك نفسه (state machine، التحقق عند التقديم، الخصم بالطبقات، سجل الاعتمادات) حقيقي ومبني كويس. لكن الخدمة الذاتية فيها حاجز قاتل: 84 من 86 مستخدم مايقدروش يقدّموا أي إجازة. وكمان: فيه طلبات بتتعلّق في APPROVED للأبد، وثغرة IDOR بتكشف طلبات الفرع كله، و~15 نوع طلب اعتماده مابيغيّرش أي حاجة.

**الشاشات**
| الشاشة | مصدر البيانات (API / ثابت / مختلط) | الحفظ (API / محلي / لا يوجد) | الحالة ✅/🟡/🔴 | ملاحظة |
|---|---|---|---|---|
| `/requests` طلباتي + تقديم طلب | مختلط | API | 🔴 | القوائم والكتالوج من API (`src/app/requests/page.tsx:354-372`)، والتقديم والسحب وإعادة الإرسال كلهم HTTP حقيقي (`:874-879`, `:902-919`). الثابت: سطر «السلسلة: … • الوجهة: …» جاي من `src/data/requestsCatalog.ts` (`:1278-1283`)، وبيقول «مدير → HR» مع إن كل السلاسل الحية خطوة `hr` واحدة. قائمة نوع الإجازة بترجع 403 للموظف (`:492-500`). زرار «استكمال وإعادة إرسال» مابيفتحش نموذج (`:912-919`). منتقي `toEmployeeId` بيرجع 403 (`:475-483`). أخطاء السيرفر بتظهر (`:1146-1150`, `:978`). |
| `/approvals-inbox` صندوق الاعتماد | مختلط | API | 🟡 | الطلبات من `GET /requests/inbox` والفعل `POST /requests/:id/act` حقيقي (`src/app/approvals-inbox/page.tsx:192-241`, `:263-294`)، والخطأ بيظهر في المودال (`:537-541`). مهلة الرد مزيفة: `?? 3` (`:212-216`). المعتمد مابيشوفش حقول مهمة ولا المرفقات (`:74-122`). الصفحة كلها بتقع لو `GET /employees` رجع 403 (`:195-200`). أخطاء العهد المعلّقة بتتبلع (`:178`). |
| `/requests-console` لوحة الطلبات | مختلط | لا يوجد | 🟡 | الجدول والإحصاءات والدرج من API (`src/app/requests-console/page.tsx:124-191`). قسم «كتالوج الأنواع المُعرَّفة» كله ثابت من `requestsCatalog` (`:452-500`)، ونص عمود «→ الوجهة» ثابت هو كمان (`:429`). زرار «تصدير» من غير `onClick` (`:230-233`). الصفوف اللي نوعها غير فعّال بتختفي من الجدول بس بتتحسب في الإحصاءات (`:358`). |
| `/my/custody` عهدتي | API | API | 🟡 | `GET /custody/mine` + تأكيد الاستلام + التسليم كلها حقيقية (`src/app/my/custody/page.tsx:48-95`)، والأخطاء بتظهر (`:114`). مفيش تسمية لـ `TRANSFERRED`، فالكود الخام بيظهر (`:20-28`). مفيش رفض لعهدة `PENDING_ACK` غلط. |
| `/my/documents` مستنداتي | API | لا يوجد | 🟡 | `GET /documents` من غير `employeeId` (`src/app/my/documents/page.tsx:43-49`)، فاللي عنده `documents.manage` بيشوف مستندات الفرع كله تحت عنوان «مستنداتي». نوع المستند بيظهر خام (`:117`). مفيش رفع ولا تجديد ذاتي. |
| `/profile` ملفي | API | لا يوجد | 🟡 | البيانات من `GET /employees/:id/profile` (مسموح للشخص نفسه، `api/src/assets/employee-extras.controller.ts:107`). زرار «تعديل الملف» وزرار الكاميرا ميتين (`src/app/profile/page.tsx:288-290`, `:310-313`). تسميات حالات العهدة غلط (`:110-114`). الراتب من غير عملة والبدلات مش ظاهرة (`:451-458`). التواريخ بتطلع هجري (`:136-137`). |

**المنطق والقواعد**
| القاعدة | الحكم ✅/🟡/🔴 | الدليل (`file:line`) | الشرح |
|---|---|---|---|
| تقديم `LEAVE` «طلب إجازة»، وده نوع الإجازة الفعّال الوحيد | 🔴 | `api/src/settings/settings.controller.ts:351-353`, `:409-412`; `src/app/requests/page.tsx:492-500`; `src/app/leaves/request/page.tsx:78-86`; `api/src/requests/requests.service.ts:326-328` | الشاشتين بيحمّلوا أنواع الإجازة من `GET /settings/leave-types`، والكنترولر ده كله محمي بـ `settings.manage`. في DB الصلاحية دي عند `hr_manager` بس، مش عند `employee` (73 مستخدم) ولا `branch_manager` (11). يعني القائمة فاضية ورسالة 403، والسيرفر بيرفض لو مفيش نوع («اختر نوع الإجازة قبل التقديم»). |
| التحقق عند تقديم الإجازة | ✅ | `api/src/requests/requests.service.ts:341-373`, `:378-416`, `:420-490`, `:494-514`; `api/src/attendance/attendance.service.ts:169-170`, `:218-219` | نصف اليوم لازم يكون يوم واحد. الأيام بتتحسب أيام عمل بس (من غير الويك إند والعطلات متعددة الأيام، لأن `endDate` محسوب). بيمنع التداخل مع إجازة معتمدة أو معلّقة، وبيطبّق `maxDays` و `oncePerService` والمرفق الإجباري، وبيتحقق من الرصيد بالطبقات. الإجازة بأثر رجعي مسموحة (مفيش شرط على `fromDate`). |
| الرصيد بالطبقات (مثال مُعاد حسابه) | ✅ | `api/src/requests/leave-balances.service.ts:47-75`, `:112-113`, `:181-187` | افتتاحي 5 ساري + استحقاق 21×8/12 = 14 (في 2026-09-10، 8 شهور مكتملة) = متبقي 19. طلب 7 أيام بياخد 5 من الافتتاحي و2 من الاستحقاق (`openingTaken=5`, `taken=7`). المثال مطابق. |
| اعتماد الإجازة ينعكس على الحضور | 🟡 | `api/src/requests/destinations.service.ts:112-139`; `api/src/requests/requests.service.ts:756-766`; `api/src/attendance/attendance.service.ts:1018` | الاعتماد بيكتب صف `leaves` بحالة APPROVED، وبيخصم الرصيد، وبيعيد حساب الأيام فتبقى `leave` (في DB فيه 96 يوم `leave`). بس إعادة الحساب بتقف بعد 62 يوم (`i < 62`)، فإجازة وضع 90 يوم بأثر رجعي بتسيب أيام `absent`. |
| تسجيل الغياب كصف | ✅ | `api/src/attendance/attendance.service.ts:991` | الحالة الافتراضية لليوم `absent` (في DB فيه 3634 صف). |
| إلغاء إجازة معتمدة (`LEAVE_MODIFY_CANCEL` + `revoke`) | 🟡 | `api/src/requests/destinations.service.ts:144-173`; `api/src/requests/leaves.controller.ts:62-120`; `api/src/requests/requests.service.ts:997-1004` | الإلغاء متاح (الموظف بطلب، وHR مباشرة) وبيرجّع الرصيد ويعيد حساب الأيام. لكن الـ handler مابيتحققش إن الإجازة لسه `APPROVED`، والمنتقي مابيستبعدش الإجازة اللي عليها طلب إلغاء معلّق. طلبين إلغاء لنفس الإجازة، أو revoke وبعده إلغاء، بيرجّعوا الرصيد مرتين. |
| التنفيذ بعد الاعتماد النهائي | 🔴 | `api/src/requests/requests.service.ts:706-711`, `:715-735`; `api/src/requests/state-machine.ts:16` | الحالة APPROVED وسجل التدقيق بيتحفظوا الأول، وبعدين الـ handler بيشتغل في معاملة منفصلة. لو رمى خطأ (تداخل إجازة، أصل مش متاح) الطلب بيفضل APPROVED للأبد، ومفيش retry ولا انتقال مسموح غير IN_EXECUTION/COMPLETED. في DB: الطلبات 292 و295 (`CUSTODY_REQUEST`، والأصلين 35 و36 مسنَدين لغيرهم) و303 (`LEAVE` متداخلة مع إجازة #72). |
| إنشاء الطلب ثم التقديم | 🟡 | `api/src/requests/requests.service.ts:217-229`; `src/app/requests/page.tsx:874-879`, `:1064` | `create()` بيحفظ DRAFT قبل ما `submit()` يتحقق. أي رفض (رصيد، تداخل، حقل ناقص) بيسيب مسودة يتيمة، والواجهة مابتقدّمش مسودة أبدًا (بتسحبها بس). في DB: 61 طلب DRAFT من غير `submittedAt`، لـ35 موظف. |
| من يعتمد؟ | 🔴 | DB `approval_chains`/`approval_steps`؛ `api/src/requests/approver-resolver.service.ts:122-127`; `api/src/requests/requests.service.ts:1017-1025`; `api/src/auth/guards.ts:73-74`; `src/components/layout/Sidebar.tsx:252` | كل السلاسل الفعّالة (67) خطوة واحدة `hr`، ما عدا `CH_OVERTIME_AUTO`. خطوة `hr` مابيعتمدهاش غير `hr_manager` (مستخدم واحد، الفرع 1) أو `super_admin`، وصندوق HR مقفول على فرعه. طلبات الفروع 7 و9 مابيشوفهاش غير `super_admin`. الـ11 مدير فرع بيظهرلهم «بانتظار موافقتي» ومايقدروش يعتمدوا حاجة. |
| أنواع الطلبات اللي اعتمادها مابيغيّرش حاجة | 🔴 | `api/src/requests/destinations.service.ts:71-78`, `:720-756`؛ DB `request_types.destinationHandler` | مفيش handler لـ `attendance_trips` و `shift_schedule` و `employee_update` و `contracts_register` و `assignments_register` و `employee_dependents` و `document_vault` و `it_assets` و `access_register` و `facilities_register` و `er_case*`. كلها بتتقفل COMPLETED بمرجع `REQ-…` من غير أي أثر. في DB: `BUSINESS_TRIP` 10، و`REMOTE_WORK` 2، و`SHIFT_SWAP` 1، و`TITLE_CHANGE` 1 (#334، المسمى ماتغيّرش)، و`CONTRACT_RENEWAL` 1. `PERMISSION` مستثنى، لأن أثره بيتقري مباشرة في `attendance.service.ts:731-750`. |
| أيام المأمورية والعمل عن بعد في الحضور | 🔴 | `api/src/attendance/attendance.service.ts:991`, `:1018`؛ مفيش أي مرجع لـ `BUSINESS_TRIP`/`REMOTE_WORK` في `api/src` خارج الـ seed | `computeDay` مفيهوش فرع للمأمورية. يوم المأمورية المعتمدة من غير بصمة بيتحسب `absent`. |
| أنواع من غير حقول | 🟡 | DB: 19 نوع فعّال `requiredFields` بتاعها فاضي ومن غير `customFields`؛ `api/src/requests/destinations.service.ts:365-387`, `:416-434` | `PERSONAL_DATA_UPDATE` الـ handler بتاعه بيقرا `phone/email/bankName`، والنموذج مابيبعتهمش، فالاعتماد بيكتب سجل «تحديث بيانات» فاضي. `RETIREMENT` من غير تاريخ، والاعتماد بيأرشف الموظف ويقفل دخوله فورًا. الخطابات الستة من غير حقل غرض. |
| الخطابات «PDF آلي» | 🔴 | `api/src/requests/destinations.service.ts:490-501` | بيكتب `letter_requests` بمرجع حاجز `letters/…pdf` من غير توليد PDF ولا endpoint تنزيل (11 صف `GENERATED` في DB). الموظف عمره ما بيستلم الخطاب. |
| `EARLY_LOAN_SETTLEMENT` | 🔴 | `api/src/requests/destinations.service.ts:235-243`, `:732`؛ DB `request_types` id 24 | مربوط بـ `loans_installments`، يعني `loanHandler` بيعمل سلفة جديدة بمبلغ `Number(undefined)` (يعني NaN)، بدل ما يسدد السلفة `loanId`. |
| السرّية | 🟡 | `api/src/requests/requests.service.ts:589-594`; `src/app/requests-console/page.tsx:150-158` | السرّي بيتخطى المدير المباشر في السلسلة (صح). لكن إخفاء اسم المقدّم في اللوحة على الواجهة بس، و`GET /requests/all` و`GET /requests/:id` بيرجّعوا `requesterId` والـ payload كامل. |
| صلاحية قراءة طلب | 🔴 | `api/src/requests/requests.controller.ts:127-133`; `api/src/requests/requests.service.ts:1039-1046`, `:1201-1210` | `scoped()` بيتحقق من الفرع بس. أي موظف يقدر يقرا أي طلب في فرعه، بما فيه التظلمات والبلاغات والسلف وسجل الاعتمادات (318 من 321 طلب في الفرع 1). |
| `POST /requests/:id/submit` | 🟡 | `api/src/requests/requests.service.ts:207-213`, `:234-545` | الحقول الإلزامية بتتفحص في `create()` لو `submit=true` بس. إنشاء بـ `submit=false` وبعده `/submit` بيعدّي الطلب ناقص. |
| الإرجاع وإعادة الإرسال | 🟡 | `src/app/requests/page.tsx:912-919`, `:1072-1079`; `src/lib/api.ts:293-294`; `api/src/assets/portal.controller.ts:116-118` | إعادة الإرسال بتتبعت من غير payload، فالطلب بيرجع زي ما هو. سبب الإرجاع أو الرفض مش ظاهر في «طلباتي» (مفيش `fetchRequest`)، وبيظهر في الإشعارات بس. |
| SLA والتصعيد | 🟡 | `src/app/approvals-inbox/page.tsx:212-216`; `api/src/requests/requests.service.ts:1137`؛ DB `approval_steps.slaDays` | كل خطوات السلاسل المربوطة `slaDays=NULL`. الواجهة بتعرض «متبقي 3 يوم للرد» ثابت، والتصعيد مابيشتغلش أبدًا. |
| تأكيد المدير للعهدة | 🟡 | `api/src/assets/assets.controller.ts:186-192`; `api/src/requests/requests.service.ts:908-909`; `api/src/requests/approver-resolver.service.ts:34-57` | قائمة «بانتظار اعتمادي» معمولة على `managerEmployeeId` بس، والتأكيد نفسه بيقبل مدير الفريق أو القسم أو الفرع. فيه 42 موظف نشط من غير `managerEmployeeId`، وعهدهم مابتظهرش في صندوق أي مدير. |

**ما هو ديمو / بيانات تجريبية**
- `src/data/requestsCatalog.ts:95-176` قائمة ثابتة (سلسلة/وجهة لكل نوع) بتتعرض كأنها حقيقية في `src/app/requests/page.tsx:1278-1283` و`src/app/requests-console/page.tsx:429`, `:452-500`. بتتعارض مع DB: كل السلاسل خطوة `hr` واحدة، و`CUSTODY_TRANSFER` في الملف `custody_assignments` وفي DB `custody_transfer`.
- مهلة رد افتراضية مزيفة `current?.slaDays ?? 3` في `src/app/approvals-inbox/page.tsx:216`.
- «آخر قراراتك في الجلسة» حالة محلية بس (`src/app/approvals-inbox/page.tsx:273-285`)، ومكتوب عليها كده.
- بيانات اختبار في DB: 25 طلب payload بتاعه فيه «اختبار كلود» (مثلًا #333، #334، #337، #338، #346، #347)؛ نوع إذن `مسائي KAR18391` (`permission_types` id 19)؛ دور `pr_test_42103`؛ مستخدمين `employee` من غير موظف بصلاحية `approve.payroll` (users 30 و32)؛ أنواع `CUSTOM_62..66` (2 منهم معطّلين، و3 محذوفين وسايبين طلبات يتيمة)؛ 61 مسودة يتيمة؛ سلسلة `CHAIN_CY5865` (id 98) مش مربوطة بأي نوع؛ سلسلة id 97 من غير خطوات.
- `generatedPdfRef` حاجز (`letters/LETTER_*/N.pdf`) في 11 صف `letter_requests`، ومفيش ملف فعلي.

**العيوب**
| # | العيب | الخطورة 🔴/🟠/🟡 | الدليل (`file:line`) | الإصلاح المقترح |
|---|---|---|---|---|
| 1 | الموظف ومدير الفرع مايقدروش يقدّموا أي إجازة: قائمة الأنواع بترجع 403 في `/requests` وفي `/leaves/request` | 🔴 | `api/src/settings/settings.controller.ts:351-353`, `:409-412`; `src/app/requests/page.tsx:492-500`; `src/app/leaves/request/page.tsx:78-86` | endpoint قراءة بس للأنواع الفعّالة من غير صلاحية (مثلًا `GET /requests/leave-types` أو `/catalogs/leave-types`) واستخدامه في الشاشتين |
| 2 | طلب معتمد بيتعلّق في APPROVED لما التنفيذ يفشل، ومفيش retry (3 طلبات في DB: 292، 295، 303) | 🔴 | `api/src/requests/requests.service.ts:706-711`, `:715-735`; `api/src/requests/destinations.service.ts:107-111`, `:528-551` | تحويل الحالة لـ APPROVED والتنفيذ في نفس المعاملة، أو فحص الوجهة قبل حفظ الاعتماد، مع endpoint لإعادة التنفيذ |
| 3 | IDOR: `GET /requests/:id` بيكشف أي طلب في الفرع (تظلمات، بلاغات، سلف) لأي موظف | 🔴 | `api/src/requests/requests.controller.ts:127-133`; `api/src/requests/requests.service.ts:1201-1210` | السماح للمالك أو المنشئ أو معتمد الخطوة أو صاحب `requests.view_all` بس، وإخفاء `requesterId` في السرّي |
| 4 | كل سلاسل الاعتماد خطوة `hr` واحدة، والمعتمد الوحيد hr_manager مقفول على الفرع 1، ومديرين الفروع مالهمش أي دور | 🔴 | `api/src/requests/approver-resolver.service.ts:122-127`; `api/src/requests/requests.service.ts:1017-1025`؛ DB `approval_steps` | سلاسل فيها خطوة `direct_manager_of_requester` وبعدها `hr`، وتعيين HR لكل فرع أو إعطاء `approve.hr` لمدير الفرع |
| 5 | ~15 نوع طلب اعتماده بيقفله COMPLETED من غير أي أثر (مأمورية، عمل عن بعد، تبديل وردية، تغيير مسمى، تجديد عقد، وثائق، …) | 🔴 | `api/src/requests/destinations.service.ts:71-78`, `:720-756`؛ DB (15 طلب مكتمل بلا أثر) | تنفيذ الـ handlers (المأمورية والعمل عن بعد تبقى حالة حضور، `TITLE_CHANGE` يحدّث `jobTitle`، `CONTRACT_RENEWAL` يمد العقد) أو تعطيل النوع لحد ما يتبني |
| 6 | يوم المأمورية المعتمدة بيتحسب غياب | 🟠 | `api/src/attendance/attendance.service.ts:991`, `:1018` | قراءة طلبات `BUSINESS_TRIP`/`REMOTE_WORK` المعتمدة في `computeDay` زي الإذن |
| 7 | إرجاع رصيد مزدوج: إلغاء نفس الإجازة مرتين، أو revoke وبعده إلغاء | 🟠 | `api/src/requests/destinations.service.ts:144-172`; `api/src/requests/requests.service.ts:997-1004` | في الـ handler: التنفيذ لو `leave.status==='APPROVED'` بس، ومنع تقديم إلغاء على إجازة عليها طلب إلغاء معلّق |
| 8 | مسودات يتيمة مع كل تقديم مرفوض (61 في DB) بتظهر للموظف «مسودة» ومالهاش إجراء | 🟠 | `api/src/requests/requests.service.ts:217-229`; `src/app/requests/page.tsx:1064` | التحقق قبل الإدخال، أو لف create+submit في معاملة واحدة، وتنظيف الـ 61 صف |
| 9 | المعتمد مابيشوفش حقول جوهرية: `toTitle`، `toEmployeeId`، `leaveId`، `punchType/time`، المرفق (التقرير الطبي)، و`leaveType` بيظهر كود خام | 🟠 | `src/app/approvals-inbox/page.tsx:74-122` | درج تفاصيل بالـ payload كامل بتسميات عربية، ورابط المرفق، واسم الإجازة المراد إلغاؤها |
| 10 | `PERSONAL_DATA_UPDATE` من غير حقول، فالاعتماد مابيحدّثش حاجة. و19 نوع فعّال عمومًا من غير أي حقل | 🟠 | `api/src/requests/destinations.service.ts:365-387`؛ DB `request_types.requiredFields` | تعريف `customFields` لكل نوع (هاتف، بريد، بنك، غرض الخطاب، …) من «بانِي الطلبات» |
| 11 | الخطابات بتقول «PDF آلي» ومفيش PDF ولا تنزيل | 🟠 | `api/src/requests/destinations.service.ts:490-501` | توليد PDF من قالب وتخزينه في files وendpoint تنزيل للمالك |
| 12 | `EARLY_LOAN_SETTLEMENT` بيعمل سلفة جديدة بمبلغ NaN بدل السداد | 🟠 | `api/src/requests/destinations.service.ts:235-243`, `:732` | handler مخصوص يقفل أقساط `loanId` المتبقية |
| 13 | «استكمال وإعادة إرسال» بيعيد إرسال الطلب زي ما هو، وسبب الإرجاع مش ظاهر في «طلباتي» | 🟠 | `src/app/requests/page.tsx:912-919`, `:1072-1079` | فتح النموذج بالقيم الحالية وتعليق المعتمد، وبعث `payload` المعدّل |
| 14 | منتقي `toEmployeeId` في نقل العهدة، وصندوق الاعتماد كله، بيعتمدوا على `GET /employees` اللي بيرجع 403 للموظف (المديرين 17 و81 بدور employee) | 🟠 | `src/app/requests/page.tsx:475-483`; `src/app/approvals-inbox/page.tsx:195-200`; `api/src/employees/employees.controller.ts:29-30` | endpoint دليل مختصر (id، اسم، كود) للموظفين النشطين، وإخراج الموظفين من `Promise.all` مع fallback للاسم |
| 15 | مهلة رد مزيفة «3 أيام» والتصعيد ميت (`slaDays` كله NULL) | 🟡 | `src/app/approvals-inbox/page.tsx:212-216`; `api/src/requests/requests.service.ts:1137` | عدم عرض مهلة لما `dueAt` مش موجود، وضبط `slaDays/escalateTo` في السلاسل |
| 16 | لوحة الطلبات: كتالوج وعمود وجهة ثابتين، و57 صف مخفي بس محسوب في الإحصاءات، وتصدير ميت، وأكواد أدوار ومفاتيح payload خام | 🟡 | `src/app/requests-console/page.tsx:230-233`, `:358`, `:429`, `:452-500`, `:59-67`, `:546` | الكتالوج من `GET /requests/types`، وعرض الأنواع غير الفعّالة بالاسم، وتصدير CSV، واستكمال `roleLabels` |
| 17 | «مستنداتي» بيعرض مستندات الفرع كله لـ HR/الأدمن، ونوع المستند خام | 🟡 | `api/src/assets/docs.controller.ts:74-86`; `src/app/my/documents/page.tsx:43-49`, `:117`; `src/lib/doc-types.ts:21-22` | بعث `employeeId` المستخدم الحالي دايمًا، واستخدام `docTypeLabel` |
| 18 | عهدتي: `TRANSFERRED` خام، والعدّاد بيتجاهل المنقولة والمفقودة والتالفة، ومفيش رفض لعهدة غلط | 🟡 | `src/app/my/custody/page.tsx:20-28`, `:97-99` | إضافة تسميات وإجراء «رفض الاستلام» بسبب |
| 19 | عهد 42 موظف من غير `managerEmployeeId` مابتظهرش في «بانتظار اعتمادي» لأي مدير | 🟡 | `api/src/assets/assets.controller.ts:186-192`; `api/src/requests/requests.service.ts:908-909` | استخدام `directManagerOf` في الاستعلام |
| 20 | ملفي: أزرار ميتة، حالات عهدة وطلبات غلط (المفقودة بلون أخضر)، راتب من غير عملة وبدلات مش ظاهرة، تواريخ هجري، أخطاء بتتبلع، «المتبقي 19 من 14» | 🟡 | `src/app/profile/page.tsx:288-290`, `:310-313`, `:75-84`, `:110-114`, `:608-616`, `:451-458`, `:136-137`, `:166-174`, `:491-492`; `api/src/employees/employee.entity.ts:197-220` | ربط «تعديل الملف» بطلب `PERSONAL_DATA_UPDATE`، وتوحيد التسميات والألوان، وعرض `currency` والبدلات، و`slice(0,10)` للتاريخ |
| 21 | `POST /requests/:id/submit` بيعدّي من فحص الحقول الإلزامية | 🟡 | `api/src/requests/requests.service.ts:207-213`, `:234` | نقل فحص الحقول جوه `submit()` |
| 22 | إعادة حساب الحضور بعد الإجازة بتقف عند 62 يوم | 🟡 | `api/src/requests/requests.service.ts:759-766`; `api/src/requests/leaves.controller.ts:108-115` | اللف على المدى كامل، أو جدولة في الخلفية |

ادعاءات دُحضت: «صندوق الاعتماد مفيهوش إخفاء لاسم المقدّم في GRIEVANCE/WHISTLEBLOWING» مش عيب، لأن المعتمد الوحيد HR والتخطي المقصود للمدير المباشر مطبّق (`api/src/requests/requests.service.ts:589-594`). وأرقام اللوحة «294 مقابل 237» قديمة: الحالي 321 طلب، يظهر منهم 264 ويختفي 57.

**ما يعمل جيدًا**
- State machine موحّدة بتمنع أي انتقال مش مسموح (`api/src/requests/state-machine.ts:11-38`)، وسجل الاعتمادات بيتكتب قبل تغيير الحالة (`api/src/requests/requests.service.ts:656-668`).
- تحقق قوي للإجازة عند التقديم: أيام عمل فعلية مع العطلات متعددة الأيام، منع التداخل، `maxDays`، `oncePerService`، مرفق إجباري، رصيد بالطبقات. والإجازة بأثر رجعي مسموحة.
- الإجازة المعتمدة بتنعكس `leave` في الحضور فورًا، والغياب بيتسجل كصف، والإذن المعتمد بيدخل في حساب التأخير (`api/src/attendance/attendance.service.ts:731-750`).
- إلغاء الإجازة المعتمدة متاح بطريقتين (طلب ذاتي أو revoke من HR) مع إرجاع الرصيد بالطبقات.
- دورة العهدة متكاملة: طلب ← اعتماد ← `PENDING_ACK` ← تأكيد الموظف ← تأكيد المدير ← `ACTIVE`، مع حراس ضد إسناد نفس الأصل مرتين (`api/src/requests/requests.service.ts:917-943`; `api/src/assets/assets.controller.ts:140-161`).
- الإذن اتحقق أثره: الـ handler بتاعه مش موجود (`attendance_log`)، لكن محرك الحضور بيقرا الطلبات المعتمدة مباشرة.
- أخطاء السيرفر بتظهر للمستخدم في كل شاشات التقديم والاعتماد والعهدة (`src/app/requests/page.tsx:1146-1150`; `src/app/approvals-inbox/page.tsx:537-541`; `src/app/my/custody/page.tsx:114`).
- كل الـ endpoints محمية (401 من غير توكن لـ `/requests/*` و`/documents` و`/custody/mine`)، و`GET /api/health` بس هو المفتوح.

---

## ٧) الجولة الحيّة — اختبرتها بنفسي على الـAPI والداتا

**الطريقة:** توكن تجريبي موقَّع محليًا بصلاحيات أربعة أدوار حقيقية من قاعدة البيانات (أدمن · موارد بشرية · مدير فرع · موظف) — بلا أي كلمة مرور — ثم قراءة الأثر مباشرة من الجداول بعد كل خطوة. 14 سيناريو، أكثر من 120 نداء.

### ما يعمل صح ✅

| الدورة | ما اختبرته | النتيجة |
|---|---|---|
| **الموظف** | إضافة بـ55 حقلًا ← قراءة ← تعديل ← أرشفة ← إعادة تفعيل | **كل الحقول رجعت مطابقة 100%** · التعديل والأرشفة (بالسبب) وإعادة التفعيل سليمة · كود مكرر ← 409 برسالة واضحة · آيبان خاطئ ← 400 |
| **الإجازات** | طلب الموظف ← اعتماد ← رصيد ← حضور | **يوما الإجازة كُتبا «إجازة» في الحضور فورًا** · الإجازة بأثر رجعي بالنيابة تعمل وتقلب اليوم · **السيرفر يعيد حساب الأيام** (أرسلت «يوم» لنطاق يومين فسجّل يومين) |
| | عطلة رسمية ممتدة 12→14 أكتوبر + إجازة 11→15 | **احتُسبت يومين فقط** (العطلة محترمة بتاريخ نهايتها) |
| | موظف جديد عُيّن 1 سبتمبر | الرصيد متناسب **1.75 يوم** (21÷12) ورفض طلب يومين برسالة واضحة |
| | إلغاء إجازة معتمدة بطلب + سحب إداري مباشر | الرصيد رجع صح في الحالتين (بما فيه الرصيد الافتتاحي) |
| | شرط «تقرير طبي» للمرضية | **الإعداد يتحكم فعلًا:** أزلته فقُبلت بلا مرفق، وأعدته |
| **الحضور** | إسناد وردية بعد البصمة | **يعيد الحساب فورًا** (التأخير نزل من 165 إلى 45) |
| | انصراف 21:30 على وردية تنتهي 19:00 | إضافي تلقائي **2.5 ساعة × 1.5** |
| | استئذان صباحي 10:00–10:45 معتمد | **التأخير 45 ← 0 والدقائق المعذورة 45** |
| | غياب وعطلات | الغياب يُكتب كصف (مهمة 01:00) · العطلة الأسبوعية حسب جدول الموظف (موظفو «جدول السبت فقط» يُحاسَبون على الجمعة — صحيح) |
| **الاعتمادات** | موظف غريب يعتمد · صاحب الطلب يعتمد · إرجاع · تقديم من جديد · رفض | 403 · 403 · «أُعيد للاستكمال» · «قيد المراجعة» · «مرفوض» بلا أثر على الرصيد — كلها سليمة |
| **العهدة** | تسليم ← تأكيد الموظف ← تأكيد المدير ← نقل ← تأكيد المستلم ← إرجاع | الأصل «مسلَّم» باسم الحامل · **التسليم المزدوج ممنوع** برسالة واضحة · النقل مقصور على مسؤول العهدة · الإرجاع يعيد الأصل «متاح» |
| **إنهاء الخدمة** | استقالة ← إخلاء طرف ← بنود ← تصفية ← اعتماد | «فترة إشعار» · **ملف إخلاء طرف بـ5 بنود تلقائيًا** · بنود تصفية تلقائية · **زر اعتماد التصفية يعمل** (صافي 16,053.66 + رقم مستند + شهادة إخلاء طرف) · المكافأة على **الأجر الكامل** لا الأساسي |
| **طلبات لها أثر حقيقي** | ترقية · زيادة راتب · عمل إضافي مسبق · تصحيح بصمة · تغيير آيبان · سلفة | المسمى تغيّر · الراتب 6000←6500 · قيد إضافي معتمد · تصحيح مسجَّل وأعيد الحساب · الآيبان تغيّر |

### العيوب التي ظهرت حيًّا

| # | العيب | الدليل الحيّ | مكانه في الكود | الإصلاح المقترح |
|---|---|---|---|---|
| L1 🔴 | **الوردية الافتراضية الصامتة** — موظف بلا جدول يُحاسَب على «صباحي 08:00–17:00» ولا يظهر أنها افتراضية | بصمة 10:45 ← **تأخير 165 د** · **3,013 من 3,886 يوم (78%)** على «صباحي» · جداول الأسبوع موجودة لـ**6 أسابيع فقط** (14 يونيو ← 26 يوليو) | `api/src/attendance/attendance.service.ts:31` و`:528` | استعمل جدول العمل المعلَّم `isDefault` («الجدول الأساسي» موجود ومعلَّم فعلًا ويُتجاهَل) + علامة «بلا وردية» على اليوم + تخصيص دائم بالنطاق |
| L2 🔴 | **الإضافي الشبح** — إضافي اكتُشف على الدوام الافتراضي يبقى بعد تصحيح الوردية | قيد 2 ساعة ليوم 09-08 بقي بعد الإسناد وإعادة الحساب، ثم **تحوّل تلقائيًا إلى طلب «مقدَّم» للاعتماد** | `detectOvertime` في `attendance.service.ts` — يرجع مبكرًا حين يقل الإضافي عن الحد دون حذف القيد السابق | احذف القيد المكتشف غير المعتمد عند إعادة الحساب، وأبلغ إن كان قد قُدِّم |
| L3 🔴 | **البصمة اليدوية تقبل تاريخًا مستقبليًا** | بصمة 20 ديسمبر قُبلت ← «متأخر 90 د» · **42 صفًا مستقبليًا** (غير إجازة) في الداتا | `ingest()` في `attendance.service.ts` بلا حارس | ارفض أي طابع زمني بعد الآن |
| L4 🔴 | **إلغاء إجازة مستقبلية يكتب أيامها «غائب» مقدمًا** | 14–15 و21–22 سبتمبر صارت «غائب» قبل حلولها | `api/src/requests/leaves.controller.ts` — السحب يستدعي `computeDay` لكل يوم بلا تمييز | الأيام المستقبلية تُحذف صفوفها ولا تُحسب |
| L5 🔴 | **إجازة تاريخ نهايتها قبل بدايتها تُقبل وتخصم** | 30 ← 28 سبتمبر اعتُمدت ← **خُصم يوم من الرصيد ولم يُكتب أي يوم في الحضور** | `api/src/requests/requests.service.ts:352` — يتخطّى الحساب بصمت ويأخذ أيام العميل | ارفض `fromDate > toDate` |
| L6 🔴 | **المأمورية والعمل عن بُعد لا يقرؤهما محرّك الحضور** | اعتُمدا ليومين بلا بصمة ← لا صف، والمهمة الليلية ستصنّفهما «غائب» | المحرّك يقرأ `PERMISSION` فقط (`attendance.service.ts:747`) | اعتبر الطلبين المعتمدين يومًا معذورًا |
| L7 🔴 | **طلبات «صورة»** — تُعتمد وتُسجَّل «مكتمل» بلا أي أثر لأن معالجها غير موجود | تغيير المسمى · **تبديل الوردية** (لا تجاوز كُتب) · **تجديد العقد** (تاريخ النهاية لم يتغيّر) · مأمورية · عمل عن بُعد — كلها `REQ-…` | `api/src/requests/destinations.service.ts:71-78` — بلا معالج ← `completed: true` | إمّا معالج حقيقي، أو إيقاف النوع، أو تعليمه «سجل فقط» صراحةً للمستخدم |
| L8 🔴 | **الخطابات بلا ملف PDF** | «تعريف راتب» اعتُمد ← الحالة «تم التوليد» ومسار `letters/LETTER_SALARY/355.pdf` — **الملف غير موجود ولا مكتبة PDF في الـAPI** | `destinations.service.ts:490-500` (تعليق: «توليد PDF فعلي لاحقاً») | توليد فعلي من القوالب، أو حالة «قيد الإعداد» بدل «تم التوليد» |
| L9 🔴 | **جهة الطوارئ لا تتحدّث أبدًا** | الطلب اعتُمد مرتين بمفاتيح مختلفة والحقلان بقيا كما هما | المعالج المشترك يسمح بـ`phone`/`email`/`bankName` فقط — `destinations.service.ts:370` | معالج خاص بحقول الطوارئ |
| L10 🔴 | **التصفية بلا تخفيض الاستقالة وبلا شريحة ما بعد 5 سنوات** | خدمة 3.1 سنة استقالة ← المكافأة **كاملة 12,320.33** — تحت نظام العمل السعودي (م.84/85) الاستقالة بين سنتين و5 = الثلث ≈ **4,107** | `api/src/offboarding/offboarding.service.ts:237-241` — معدل واحد × سنوات | قواعد حسب سبب الإنهاء والشرائح (وحسب الدولة: الشركة لها فروع في السعودية ومصر) |
| L11 🟠 | **أجر التصفية يستبعد بدل الهاتف وبدل طبيعة العمل** | الحقلان يُحفظان في الملف ولا يدخلان الإجمالي | `offboarding.service.ts:225-229` | أضفهما لإجمالي الأجر |
| L12 🟡 | **بدل الإجازة لا يحسب آخر شهر كامل** | عمل حتى 30 سبتمبر ← احتُسب حتى أغسطس: **14 يومًا بدل 15.75** | حساب الأشهر في `offboarding.service.ts` | احسب الشهر المكتمل بآخر يوم عمل |
| L13 🟠 | **«فترة السماح» العامة في الإعدادات تتجاوزها سماحية الوردية بصمت** | غيّرتها 10←50 ولم يتغيّر شيء لأن «صباحي» سماحها 30 | `attendance.service.ts:914-917` | أوضحها في شاشة الإعدادات أو وحّد المصدر |
| L14 🟠 | **تحديث البيانات الشخصية شبه صورة** | النوع بلا حقول في الواجهة؛ والمعالج غيّر الجوال وتجاهل العنوان · `bankName` يتغيّر خارج المسار الأمني | `destinations.service.ts:370` | حقول صريحة + مقارنة قبل/بعد + البنك في المسار الأمني فقط |
| L15 🟠 | **20 من 52 نوع طلب نشط بلا أي حقول** | منها تجديد العقد · تغيير نوع العقد · التقاعد · تحديث البيانات · الخطابات الستة | `request_types.requiredFields/customFields` فارغة | تعريف الحقول أو إيقاف النوع |
| L16 🟠 | **لا متابعة لانتهاء العقود** | موظف عقده انتهى 31 أغسطس وحالته «نشط» بلا أي تنبيه | — | حالة «منتهي» + تنبيه قبل الانتهاء |
| L17 🟡 | يوم ببصمة دخول بلا خروج يُصنَّف «حاضر» بساعات عمل صفر | يوم 09-09 بعد تصحيح دخول فقط | منطق `computeDay` | حالة «بصمة ناقصة» |
| L18 🟡 | **مفتاح جهاز البصمة ما زال القيمة الافتراضية** | `attendance.device_key = zk-device-key-change-me` — أي أحد يعرفه يستطيع إرسال بصمات | `requests_config` | فرض تغييره قبل التشغيل |
| L19 🟡 | أثناء نقل العهدة يصبح الأصل «مسلَّمًا» بلا حامل حتى يؤكد المستلم | `currentHolderId = null` في المرحلة الوسيطة | خدمة العهدة | حالة «قيد النقل» |

### ما هو إعداد وتشغيل — ليس عيبًا في الكود (للفصل)

| البند | الواقع |
|---|---|
| مسار اعتماد الإجازة | خطوة واحدة «موارد بشرية» بلا مدير مباشر — **2 فقط من 68 سلسلة** تبدأ بالمدير المباشر (المحرّر يعمل ويمكن تعديلها) |
| تغطية الجداول | **88 موظفًا نشطًا بلا أي جدول أسبوعي** · **173 من 177 بلا جدول عمل** — وهذا ما يغذّي العيب L1 |
| البصمة | **117 موظفًا نشطًا لم يبصموا أبدًا** — مصدر 93% من أيام «الغياب» |

### البيانات التجريبية الموجودة الآن

| البند | العدد | ملاحظة |
|---|---|---|
| ورديات بأسماء اختبار (`SC53685`، `AUDMORN45`، `S11SHIFT_K1`…) | **21 من 26 وردية** | تظهر للمستخدمين في كل قوائم الورديات |
| أنواع أذونات بأسماء اختبار | **12** | |
| جدول عمل اختبار (`جدول WS85620`) · نوعا طلب `CUSTOM_*` · دور `pr_test_*` | 1 · 2 · 1 | |
| مسيرات رواتب لشهور قادمة (أكتوبر ونوفمبر) | **2** | ظاهرة في الداشبورد بإجمالي 1.4 مليون |
| صفوف حضور بتاريخ مستقبلي (غير إجازة/عطلة) | **42** | ناتجة عن L3 |
| ترقيات بنص «????» (تلف ترميز في داتا اختبار قديمة) | **2** | |
| **من اختباري أنا اليوم** | 3 موظفين `CLT-*` · أصلان · عطلة · 30 طلبًا · 4 أيام «غائب» مستقبلية للموظف التجريبي | كلها معلَّمة «اختبار كلود» |

### تحقق حيّ من أخطر ادعاءات المراجعين (بعد انتهاء المراجعة)

| الادعاء | ما جرّبته | النتيجة |
|---|---|---|
| الموظف ومدير الفرع لا يستطيعان تقديم إجازة من الواجهة | `GET /settings/leave-types` بصفة موظف وبصفة مدير فرع | 🔴 **مؤكَّد** — **403** لكليهما. الـAPI نفسه يقبل الإجازة (قدّمتها مباشرةً ونجحت)، لكن **الشاشة** لا تستطيع جلب الأنواع فلا يستطيع 84 من 86 مستخدمًا التقديم منها |
| IDOR على الطلبات | موظف (u11) يفتح طلب إجازة موظف آخر `GET /requests/352` | 🔴 **مؤكَّد** — **200** والطلب كاملًا |
| ملف إخلاء الطرف مكشوف | موظف عادي يفتح `GET /offboarding/18` لموظف آخر | 🔴 **مؤكَّد** — **200** والراتب ظاهر (6,000)؛ الآيبان والهوية فارغان هنا فقط لأن موظف الاختبار بلا بيانات |
| **تصعيد صلاحيات إلى مدير نظام** | مدير الموارد البشرية ينفّذ `PUT /users/12/permissions {"grants":["*"]}` على نفسه | 🔴 **مؤكَّد** — **200** وصارت صلاحياته `*` (مدير نظام فعليًا). السبب: `api/src/auth/roles.controller.ts:66` يستثني `*` من التحقق صراحةً، ولا شيء يمنع المستخدم من تعديل صلاحياته هو. **أعدتها فورًا بالأدمن** (33 صلاحية) |
| نوافذ البصمة تُسقط التأخير | دخول 11:30 (نافذة الدخول 07:00–11:00) وخروج 17:05 على «صباحي 08:00» | 🔴 **مؤكَّد** — **«حاضر» بتأخير 0 وساعات عمل 0** والدخول فارغ: تأخير 3.5 ساعات اختفى |
| موظفون تجاوزوا آخر يوم عمل وما زالوا نشطين | قراءة ملفات الإخلاء المستحقة | 🔴 **مؤكَّد** — الحالات 3 و4 و5 و11: تصفية معتمدة وآخر يوم 7–8 أغسطس، والموظفون «فترة إشعار» ونشطون؛ مهمة الإنهاء الليلية لا تعوّض الأيام الفائتة |
| 96% من بصمات الجهاز يتيمة | قراءة جدول البصمات ومطابقة الأكواد | 🟠 **مؤكَّد مع تدقيق** — 7,452 من 7,778 بصمة بلا موظف، من **36 كود جهاز**، لكن **ولا موظف واحد** كوده أو `fingerprintCode` يطابقها: السبب الأساسي ربط تشغيلي ناقص، والعيب البرمجي غياب شاشة «أكواد جهاز غير مربوطة» والربط بأثر رجعي |

**كل ما غيّرته أثناء الاختبار أُعيد لأصله:** صلاحيات مدير الموارد البشرية · فترة السماح (10) · شرط التقرير الطبي · الإجازات الاختبارية (سُحبت أو أُلغيت) · العهد (أُرجعت). بيانات الاختبار نفسها باقية ومعلَّمة «اختبار كلود» — انظر جدول التنظيف.

---

## ٨) الفروقات مع نظام الشريك — ما نبنيه عندنا

**المصدر:** ما شاهدته بعيني على النظامين في الاختبار الحيّ (نظام الشريك يومَي 9 و10 سبتمبر، ونظامنا اليوم). مرتّبة بالأثر على التشغيل.

### ما عندهم وينقصنا (مقترح للبناء)

| # | الميزة | عند الشريك | عندنا | لماذا نحتاجها |
|---|---|---|---|---|
| G1 | **تخصيص الجداول بالنطاق — دائم** | شاشة «تخصيص الجداول» بنطاق الشركة/الفرع/القسم/الفريق/الموظف وقاعدة أولوية «الأكثر تخصيصًا يفوز» | جدول أسبوعي لكل موظف **لكل أسبوع على حدة** + تجاوز يوم + جدول عمل للموظف | هذا وحده يقضي على العيب L1: وردية القسم تسري على كل أفراده دائمًا بلا تعبئة أسبوعية، والافتراضي يصبح استثناءً ظاهرًا |
| G2 | **طلب تعديل بيانات بحقول ومقارنة قبل/بعد** | نافذة تعرض القيمة الحالية لكل حقل + شاشة للموارد البشرية فيها «مقارنة قبل/بعد» واعتماد يطبّق التعديل (جرّبتها ونجحت) | نوع `PERSONAL_DATA_UPDATE` بلا حقول، والمعالج يطبّق 3 حقول فقط | الموظف يصحّح بياناته بنفسه بدل مكالمات الموارد البشرية — مع حماية الحقول الحسّاسة (عندهم ثغرة الآيبان هنا؛ نبنيها بالمسار الأمني من البداية) |
| G3 | **تنبيه العقود التي تنتهي قريبًا** | بطاقة «تنتهي قريبًا» وشريط تنبيه في شاشة العقود | لا شيء | نبنيها أفضل منهم: تنبيه قبل الانتهاء **وحالة «منتهي» فعلية** (هم لا يكتبونها أبدًا) |
| G4 | **إنشاء مستند سريع من ملف الموظف** | «خطاب تعريف · خطاب تعريف بالراتب · عقد عمل» من تبويب المستندات بقوالب | طلبات خطابات بلا ملف فعلي (L8) | توليد PDF حقيقي من قالب — للموظف عبر الطلب وللموارد البشرية مباشرةً من الملف |
| G5 | **رفض استلام العهدة من المستلِم** | زرّا «استلام / رفض» عند المستلِم | «تأكيد الاستلام» فقط | المستلِم الذي يجد الأصل تالفًا أو ناقصًا يحتاج أن يرفض بسبب |
| G6 | **المأمورية كنوع إذن يعذر اليوم** | نوع إذن «مهمة عمل رسمية» ضمن أنواع الأذونات | طلب مأمورية بلا أثر على الحضور (L6) | يكفي أن يقرأ محرّكنا الطلب المعتمد كيوم معذور — أصغر إصلاح في القائمة |
| G7 | **مؤشرات متابعة على لوحة إخلاء الطرف** | «بنود متأخرة · معلّقة لفترة طويلة · بدون مسؤول · دخول نشط بعد آخر يوم عمل» | ملف الإخلاء وبنوده | تحويل الإخلاء من قائمة إلى متابعة — أهمها «حساب ما زال يدخل بعد آخر يوم عمل» |

### ما عندنا وهم يفتقدونه — لا نغيّره

| عندنا ويعمل | عندهم |
|---|---|
| الغياب والإجازة يُكتبان في سجل الحضور | الغياب لا يُخزَّن أبدًا، والإجازة المعتمدة تظهر «غائب» |
| العطلة الممتدة محترمة في حساب أيام الإجازة | تُخصم أيام العطلة من رصيد الموظف |
| زر اعتماد التصفية يعمل وينتج مستندًا وشهادة إخلاء طرف | الزر لا يصدر أي نداء، ورقمان متضاربان للتصفية |
| سلسلة العهدة كاملة (تسليم · تأكيد · مدير · نقل · إرجاع) ومنع التسليم المزدوج | التسليم قبل الاعتماد، والاستلام يعلّق الجلسة |
| شاشات الإعدادات التي اختبرتها تتحكم في المحرّك فعلًا | ست شاشات «حفظ» بلا أي نداء |
| الإجازة بأثر رجعي والإلغاء واسترجاع الرصيد | الرجعي ممنوع، والمعتمدة لا تُلغى من الواجهة |
| إسناد الوردية يعيد حساب الماضي فورًا | الإسناد اللاحق لا يعيد الحساب |

### الترتيب المقترح للتنفيذ عندنا

1. **G1 + إصلاح L1** — تخصيص الجداول بالنطاق، واستعمال «الجدول الأساسي» المعلَّم افتراضيًا بدل الثابت، وعلامة «بلا وردية» على اليوم. هذا يصحّح 78% من أيام الحضور.
2. **L2 · L3 · L4 · L5** — أربعة إصلاحات صغيرة في الحضور والإجازات، كل واحد بضعة أسطر.
3. **G6 + L6** — المأمورية والعمل عن بُعد كأيام معذورة.
4. **L10 · L11 · L12** — قواعد التصفية حسب سبب الإنهاء والشرائح والدولة، وإدخال كل البدلات.
5. **G2 · G3 · G4 · G5** — مزايا الموظف والعقود والمستندات.
6. **تنظيف البيانات التجريبية** — قبل أول مسير رواتب حقيقي.
