# حفظ أولوية التحصيل وشاشتها — 13 سبتمبر 2026

أصبح اختيار المالك لتقديم السلف أو الحضور والاستردادات محفوظًا ضمن نسخة سياسة الرواتب، وله شاشة فعلية في `/payroll/policies`. معاينة الحساب الكاملة تقرأ هذا الترتيب من النسخة المحفوظة. هذا يغلق بند **حفظ ترتيب التحصيل وشاشته وربطه بمعاينة المحرك**؛ إسناد السياسة للمسير ومزودات المصادر الفعلية ولقطة المسير ما زالت مطلوبة قبل تفعيل هذا المحرك على الرواتب القائمة.

## السلوك المنفذ

- لا ترتيب افتراضي مفروض. يختار المستخدم تصنيف كل خصم وترتيبه، وله اقتراحان قابلان للتعديل: السلف أولًا، أو الحضور والاستردادات أولًا. تشمل القائمة البنود غير المفعلة دون تفعيلها، وتبقى الاستقطاعات المحمية خارج قائمة الأولوية القابلة للتغيير.
- يحفظ القرار مع سبب التعديل ومراجعة النسخة وسجل قبل/بعد داخل معاملة واحدة. التعديل المتزامن القديم يُرفض دون الكتابة فوق القرار الأحدث. النسخة المنشورة أو المستخدمة تُنسخ إلى مسودة جديدة ويظل مصدرها محفوظًا.
- تعديل البنود بما يؤثر على التصنيفات أو الترتيب يتطلب إرسال إصلاح القرار معها في الحفظ نفسه. لا يترك النظام ترتيبًا ناقصًا بعد إضافة خصم أو حذفه.
- تحفظ القيم التاريخية الفاسدة كما هي عند النسخ وتظهر `INVALID`؛ SQL NULL وحده يعني عدم وجود قرار. شمل الإصلاح نص JSON `null` والأرقام التي قد تفقد دقتها عند تحويل JSON.
- واجهة المعاينة `POST /api/payroll/policies/:id/versions/:versionId/execution/preview` تثبت التعريف والإعدادات والقرار من نفس مراجعة النسخة. ترفض محاولة العميل تمرير ترتيب أو إعدادات بديلة، وتعيد تفصيل البنود والصافي ولقطة قابلة لإعادة التشغيل وبصمة تشمل إصداراتها.
- تتبع القراءة والتعديل صلاحيات الرواتب وملكية الفرع. الواجهة تحفظ المسودة عند تعارض الحفظ، وتطلب مراجعتها بعد إعادة القراءة؛ فشل تحديث القائمة بعد نجاح الحفظ لا يكرر الطلب.

## التحقق

**179 حالة منفصلة ناجحة** في نطاق هذه الخطوة والوظائف المتأثرة، دون فشل أو تخطٍ. لا نجمع تكرار نفس الاختبار مرتين:

| المجموعة | الحالات | الدليل |
|---|---:|---|
| SQL/HTTP للتنفيذ والتحصيل، ثم التعريف والمسودات والإعدادات السابقة |84: منها34 للتنفيذ و18 للتعريف و16 للمسودات و16 للإعدادات|`output/payroll-collection-policy-http-final.txt`|
| نواة التحصيل، محول التخزين الفعلي، ومنطق الواجهة وReact SSR وطبقة API |46: منها22 +8 +16|`output/payroll-collection-policy-pure-final.txt`|
| ترحيلات006–010 والتحقق من المخطط والتراجع |49|`output/payroll-collection-policy-migration-final.txt`|

أعيدت حالات010 الإحدى عشرة فقط بعد تشديد محول التخزين النهائي، ونجحت في `output/payroll-collection-policy-migration-transformer-final.txt`؛ هي ضمن49 وليست إضافة إلى العدد. تحقق SQL الفعلي من حفظ النصوص التالفة حرفيًا عند النسخ. نجح بناء API في `output/payroll-collection-policy-build.txt` وTypeScript الواجهة في `output/payroll-collection-ui-typecheck.txt`.

اختبارات HTTP تثبت ترتيبين على الأرقام نفسها، رفض المدخلات المزورة، أذونات الفروع، التزامن، نسخ النسخة المستخدمة، إصلاح التعريف والقرار معًا، وإرجاع المعاملة عند إخفاق سجل التدقيق عمدًا. بقيت السجلات المالية والحضور والمطالبات دون أثر من حفظ السياسة أو المعاينة داخل قواعد الاختبار. قواعد الاختبار العشوائية ومجلداتها المؤقتة حُذفت، وأدلة التحقق مسجلة في السجلات.

كشفت المراجعة فقد التمييز بين SQL NULL ونص JSON null، ثم فقد دقة أرقام داخل كائن فاسد عند النسخ. أُصلح محول التخزين وأعيدت الاختبارات المتأثرة. لا توجد نتيجة فاشلة متبقية في الأدلة النهائية.

## الشاشة والتشغيل

توجد روابط الشاشة من الرواتب والإعدادات والقائمة الجانبية. محرر إنشاء السياسة وتعريف بنودها الكامل لم يكتمل؛ شاشة التحصيل تدير النسخ الموجودة ذات التعريف المكتمل، وتعرض سبب عدم الجاهزية عند غياب النسخة أو خدمة API المطلوبة.

الصورتان من **React SSR للمحرر الفعلي مع CSS النظام وبيانات اختبار معزولة**. فُحصتا بصريًا على الكمبيوتر والموبايل؛ ليستا صورتين لبيانات الشركة أو دليل اختبار تفاعلي مع خدمة المراجعة:

- `output/payroll-collection-ui-desktop.png`
- `output/payroll-collection-ui-mobile.png`

دليل `output/payroll-collection-policy-runtime-readiness.json` في16:23:49UTC يؤكد استمرار API4000 بالعملية5552 وبناء10:37:35UTC السابق، وواجهة3000، وhealth200/db:up والدخول201 والقسيمة44 بصافي12500. لم تحدث إعادة تشغيل أو إعادة حساب لمسير تاريخي. لم تُعدل بيانات الموظفين أو البصمات أو قاعدة المصدر.

أضيف ترحيل010: عمود `collectionPolicy` قابل لـNULL دون تعبئة تاريخية. ما زالت ترحيلات006–010 غير مطبقة على قاعدة المراجعة. الخطة المقروءة `output/payroll-collection-policy-migration-plan.json` تعرض39عملية وunsafeDrift=0؛ العدد لم يزد لأن جدول النسخ غير موجود بعد على المراجعة فيدخل العمود في أمر إنشائه. التحميل الفعلي وخطواته في `docs/PAYROLL_POLICY_REVIEW_UPDATE_2026-09-13.md`.

## المتبقي

التالي: مزودات البيانات الفعلية وPL-02/07 لإسناد السياسة وتثبيت لقطة المسير، ثم التفصيل والمحاكاة والمقارنة والتكافؤ. النتيجة الحالية موسومة `CALLER_UNVERIFIED`؛ قرار التحصيل خادمي محفوظ، لكن حقائق الموظف والأيام والدفتر ما زالت مدخلات صريحة للمعاينة. `approvalEligible` صلاحية مالية للناتج فقط، ولا يسمح هذا المسار باعتماد أو صرف راتب.

تبقى مصادر الشرائح الأخرى ومديونياتDEBIT غير السلف وحوكمة ترحيلDD-11، وكتالوجات البدلات والخصومات والإعفاءات وحوكمة السلف والتقارير. لا نعد الاختبارات الصناعية تكافؤًا تشغيليًا لثلاث فترات كاملة متتالية أو بديلًا للظل لشهرين. الحالة العامة في `PAYROLL_REMAINING_2026-09-13.md`، والعقد الفني في `docs/PAYROLL_COLLECTION_POLICY_CONTRACT_2026-09-13.md`.
