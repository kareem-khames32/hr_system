# استكمال الشغل المقطوع — 11 سبتمبر 2026

> هذا تقرير الاستلام والإصلاح الأولي. نتائج الاستكمال اللاحق والـ105 اختبارات والصور موجودة في [تقرير الاستكمال النهائي](D:/projects/hr_system/COMPLETION_REPORT_2026-09-11.md).

## حالة النسخة

استُكملت الأجزاء المقطوعة التي كانت تمنع تجميع الواجهة والباك، وأُصلحت عيوب إضافية ظهرت في اختبارات التشغيل على SQL Server. هذه نتيجة للنسخة الحالية، وليست إعلان إغلاق البنود الـ174 في التقرير الفني القديم.

بدأت المراجعة من تعديلات غير معمولة لها commit فوق `d0226b7`. تقرير `FIX_REPORT_W12.md` سبق تعديلات إضافية في نفس اليوم؛ لذلك وصفه «الواجهة صفر أخطاء والباك به أخطاء الرواتب فقط» لم يكن يطابق النسخة الموجودة عند الاستلام.

## ما تغيّر في هذه الجولة

| الجزء | التغيير |
|---|---|
| تجميع المشروع | إصلاح التعريفات والاستيرادات الناقصة في ملف الموظف والإشعارات والمستندات وأيام العمل والهيدر، واستكمال دوال الخدمات التي تناديها الكنترولرات. |
| أيام العمل | ربط إنشاء الجدول وتعديله واختيار أيامه وإسناده للموظفين بالكتالوج الحقيقي. حذف نماذج البيانات التجريبية من هذه الشاشة، وإظهار أخطاء الحفظ وحالته. |
| ملف الموظف | استكمال عرض الفريق ومركز التكلفة والجدول والعملة والاستحقاق، واستخدام المتبقي المحسوب من الخادم وبيانات الشركة عند إنشاء المستند. |
| الإشعارات | استخدام تصنيف وحالة القراءة من الخادم، وحفظ القراءة والحذف، وتحديث الجرس، وإعادة إظهار إشعار مجمّع عند وصول عنصر جديد فقط. |
| الحضور اليومي | تنفيذ `/attendance/my-today` كقراءة لصف اليوم والجدول والإجازة، بدون إنشاء غياب عند فتح اللوحة. |
| الوردية الليلية | تخزين ساعة الحائط `06:00` بدلاً من `30:00`، وحساب نهاية الوردية على خط اليوم الممتد لمنع 24 ساعة أوفرتايم وهمية. وصول بصمة الصباح في دفعة لاحقة يعيد حساب يوم العمل السابق. |
| الرواتب | تصحيح أنواع الحقول nullable في `PayrollRun` مع تحديد أنواع أعمدة قاعدة البيانات صراحةً. هذا إصلاح تجميع، وليس اعتماداً لمحرك الرواتب مالياً. |
| إنهاء الخدمة | المعاينة لا تحفظ بنوداً؛ التصفية تستخدم نفس حساب المكافأة المعروض. إعادة توليد البنود الآلية تتم بمعاملة، واليدوية تبقى. |
| توقيت الإنهاء | الحساب لا يُغلق أثناء آخر يوم عمل. الإنهاء في اليوم التالي بالتاريخ المحلي، بمعاملة تشمل الموظف والحساب والسجل والملف، مع إعادة فحص الحالة لمنع التنفيذ المكرر. تشغيل يدوي بنطاق الفرع ولحاق عند الإقلاع. |
| التقاعد | يفتح فترة إشعار وملف إخلاء بخمس جهات وسبب `retirement` بدلاً من الأرشفة الفورية. الموظف لا يلغي قرار إنهاء صادر من الشركة بنفسه. |
| الطلبات | التقديم الفاشل لا يترك مسودة يتيمة؛ إعادة التقديم الفاشلة تُبقي الحالة والحمولة الأصلية. الحقول المطلوبة والمخصصة تُفحص عند تقديم المسودة أيضاً. |
| الاعتمادات | الخطوة الهيكلية التي لا يوجد لها مدير محدد ترفض التقديم برسالة واضحة بدلاً من تعليق الطلب. صندوق تأكيد العهدة يستخدم تسلسل المدير الفعلي، مع نطاق الفرع. |
| نطاق الفرع | منع التقديم نيابة عن موظف خارج نطاق مقدم الطلب. |
| البيانات الشخصية | إخراج البريد والبنك من معالج التحديث الشخصي، وإتاحة حقول شخصية محددة. جهة الطوارئ تكتب حقولها، والطلب الذي لا يغير شيئاً يُرفض. |
| السلف | السداد المبكر يغلق أقساط السلفة الموجودة المملوكة للموظف، ولا ينشئ سلفة جديدة. رفض المبالغ غير الصالحة وعدد الأقساط غير الصحيح. |
| الخطابات | إيقاف الادعاء بإنشاء PDF غير موجود: السجل `PENDING_PREPARATION` والطلب يبقى تحت التنفيذ، مع إزالة شارة PDF آلي لهذا المسار. توليد وتسليم الملف فعلياً ما زال مطلوباً. |
| إقلاع التطوير | تحديد نوع عمود `terminationReason` صراحةً؛ التشغيل بـtranspile-only كان يراه `Object` ويرفض الاتصال بالقاعدة. |

## الاختبارات

ملف الاختبارات: `api/test/recovery.integration.cjs`.

التشغيل من جذر المشروع:

```powershell
npm --prefix api run test:integration
node node_modules/typescript/bin/tsc -p api/tsconfig.json --noEmit --incremental false --pretty false
$env:NEXT_BUILD_DIR = '.next-recovery'
node node_modules/next/dist/bin/next build
```

**28 اختبار تكامل ناجح**، باستخدام NestJS الحقيقي وSQL Server حقيقي، وبعض الاختبارات تنادي HTTP وبعضها الخدمات داخل معاملات قاعدة البيانات. التغطية تشمل:

- رفض المستخدم غير المسجل والتوكن القديم بعد تغيير إصداره.
- قراءة الحضور اليومي دون كتابة وقراءة الجدول بعد إسناده.
- حفظ الجداول وإسنادها وعزل الفروع ومنع الموظف من تعديلها.
- إنشاء كتالوج المستندات وتعديل الاسم ورفض الكتابة غير المصرح بها.
- عدم كتابة معاينة التصفية وتطابقها مع البنود المحفوظة.
- رفض السلسلة المعطلة والخطوة التي ليس لها مدير.
- رفض تغيير البريد والبنك من المسار الشخصي.
- السداد المبكر ورفض بيانات السلفة غير الصالحة.
- رفض البصمات المستقبلية والمفتاح الضعيف، وعدم حفظ غياب مستقبلي.
- وردية ليلية تصل بصماتها في دفعة واحدة أو دفعتين، دون أوفرتايم وهمي.
- خصم الرصيد الافتتاحي أولاً ثم استعادته دون فقد.
- حفظ قراءة الإشعارات وحذفها وخصوصيتها، وتجدد الإشعار المجمع عند وصول عنصر جديد.
- المسودات وإعادة التقديم والحقول المطلوبة والوجهات غير المبنية.
- منع قراءة طلب موظف آخر والتقديم نيابة عن خارج الفرع.
- رفض السماح السالب ونطاق العطلة المقلوب.
- الإنهاء بعد آخر يوم فقط، وعزل الفرع وعدم تكراره، ودورة التقاعد.
- عدم إرجاع مرجع PDF وهمي.

الاختبارات تنشئ قاعدة باسم عشوائي يبدأ بـ`hr_recovery_test_` ثم تحذف القاعدة التي أنشأتها فقط. تستخدم بيانات الاتصال المحلية وتحتاج صلاحية إنشاء قاعدة اختبار. لا تُشغّل seed أو synchronize على قاعدة `hr_system` الأصلية.

فحص TypeScript للباك: **ناجح**. بناء Next.js للإنتاج: **ناجح بعد آخر تعديلات الواجهة**، بما فيه فحص الأنواع وتوليد الصفحات الـ84. مجلد البناء المعزول `.next-recovery` مستبعد من Git لتجنب تعارضه مع سيرفر التطوير.

## ما لم يُغلق بهذا التقرير

- الـ28 اختباراً ليست 28 بنداً من التقرير القديم، ولا تثبت كل بنود الموجتين 1 و2 أو الموجات التالية.
- لم يُنفّذ اختبار بصري يدوي لكل شاشة أو اختبار جهاز بصمة فعلي. دعم مفتاح اتصال الجهاز `ATT-24` ما زال يحتاج اختيار مكتبة متوافقة واختبار الجهاز.
- توليد وتسليم ملفات الخطابات من دورة الطلبات ما زال غير مكتمل؛ الحالة الآن تصف ذلك بوضوح.
- الوجهات غير المسجلة، مثل تبديل الوردية وتجديد العقد، ما زالت مرفوضة بدلاً من ادعاء التنفيذ. يلزم استكمال مساراتها واختبارها قبل إتاحتها.
- بقية بنود التسمية والديمو والمعالجات في `OUR_TECH_DEFECTS.md` تحتاج مطابقة واختبارات بنداً بنداً؛ لم تُعلَّم مغلقة تلقائياً.
- الاختبار كان على قاعدة جديدة منفصلة. لم نحدّث مخطط قاعدة المشروع الأصلية أو نعد حساب حضورها أو نصرف رواتب أو نغيّر أسرارها أو ننظف بياناتها التجريبية.
- لم يتم عمل commit أو نشر النسخة. التعديلات السابقة وهذه الجولة موجودة في شجرة العمل.
