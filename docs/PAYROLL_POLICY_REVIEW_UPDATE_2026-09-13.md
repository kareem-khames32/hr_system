# تحديث نسخة المراجعة — ترحيلات006 إلى011 وسجل الأجر المؤرخ

**آخر بناء — 14 سبتمبر:** يشمل الآن التقويم المؤرخ وأدلة الحضور العادي وصفحات سريان العطلات والراحة والفرع. لا ترحيل جديد بعد011. تقرير التحقق الأحدث `PAYROLL_BATCH2_CALENDAR_ATTENDANCE_REPORT_2026-09-14.md` يسجل186حالة ناجحة. الفحص عند21:02:17UTCيوم13، الموافق00:02بتوقيت القاهرة يوم14، لم يجد استجابة صالحة من4000/3000. لم يُحمّل البناء ولم يُطبق ترحيل من هذه الشريحة. النص التالي يضم مسار التحميل السابق؛ الحدود الحالية في عقد التقويم الجديد.

البناء جاهز في api/dist، والأدلة الأحدث في PAYROLL_BATCH2_DATED_SOURCES_REPORT_2026-09-13.md. آخر فحص19:39:36UTCلم يتلق استجابة منAPI4000والواجهة3000، دون أمر إيقاف/تشغيل من هذه الخطوة. الخطوات التالية تطبق ترحيلات006–011 وتحمل البناء الجديد. التحديث يحتفظ بعمل الإضافي والمرونة والأقساط والتحصيل، ويضيف سجل الأجر المؤرخ بمراجعات صريحة وشاشته المستقلة وقراءته ضمن المصادر، وأدلة الوردية ومرونة الموظف لكل يوم. السجل الجديد فارغ إلى أن تُوثق الفترات حسب العقد/القرار؛ لا تُنسخ الأجور الحالية تلقائيًا على تاريخ الموظفين. تقويم العمل التاريخي الكامل وإسناد المحرك للمسير والنشر واللقطة لم تكتمل بعد.

تُنفذ محليًا في PowerShell. الرابط داخل علامات الاقتباس نص عادي؛ لا تنسخ رابط Markdown أو شرطات هروب قبل underscore.

```powershell
Set-Location -LiteralPath 'D:\projects\hr_system'

node api/scripts/migrations-review-server.cjs stop
if ($LASTEXITCODE -ne 0) { throw 'تعذر طلب إيقاف خدمة المراجعة' }
Start-Sleep -Seconds 3

node api/scripts/payroll-migrations.cjs apply
if ($LASTEXITCODE -ne 0) { throw 'الترحيل لم يكتمل؛ احتفظ برسالة الخطأ للمراجعة' }

node api/scripts/payroll-installment-review-setup.cjs apply
if ($LASTEXITCODE -ne 0) { throw 'نوع طلب التأجيل لم يجهز؛ احتفظ برسالة الخطأ للمراجعة' }

$env:HR_REVIEW_API_PORT = '4000'
$env:HR_REVIEW_FRONTEND_URL = 'http://localhost:3000'
node api/scripts/migrations-review-server.cjs start
```

بعد نجاح تشغيلAPI، افتح نافذةPowerShell ثانية لتشغيل الواجهة المتوقفة، واتركها مفتوحة طوال المراجعة:

```powershell
Set-Location -LiteralPath 'D:\projects\hr_system'
$env:NEXT_PUBLIC_API_URL = 'http://localhost:4000/api'
npm run dev -- --hostname 127.0.0.1 --port 3000
```

الـrunner يقبل قاعدة المراجعة المثبتة في manifest فقط، ويتأكد من توقفAPI، ويأخذ COPY_ONLY مع تحققCHECKSUM قبل التطبيق، ويحفظ الأعمدة والأعداد السابقة داخل معاملة. لا يستخدم synchronize ولا يمس قاعدة المصدر. إذا بقي المنفذ مفتوحًا بعد مهلة الانتظار سيرفض الترحيل قبل أي كتابة؛ تُراجع رسالة الرفض أولًا.

أداة تهيئة التأجيل تضيف نوع الطلب الناقص فقط وتحافظ على سلسلة LOAN الحالية وتجاوزاتها حسب الفرع. لا تشغّل بذر النظام العام أو إعادة إنشاء بياناته. فحص نوع التأجيل السابق أثبت أن النوع ناقص وسلسلة LOANالحالية رقم43. خطة المخطط الأحدث أثبتت45عملية إضافية معلقة وunsafe0.

بعد رسالة startRequested نتحقق من health والدخول وواجهة السياسات والقسيمة44. يلزم تسجيل دخول جديد لأن خدمة المراجعة تغيّر مفتاح الجلسة عند كل تشغيل. الواجهة الرئيسية http://localhost:3000؛ خيارات الأقساط في /settings/policies، وطلب التأجيل في /payroll/loans، وتصنيفات الخصومات وأولويتها وفحص المصادر في /payroll/policies. شاشة التحصيل تحتاج نسخة سياسة مجهزة بتعريف كامل؛ فحص المصادر يوضح النواقص حتى للنسخة غير المكتملة. لا تُنشئ بيانات شركة افتراضية. محرر إنشاء السياسة وتعريف بنودها الكامل ما زال لاحقًا.

سجل الأجر الجديد في /payroll/salary-history، وله رابط من رأس شاشة الرواتب؛ لا يحتاج إنشاء سياسة أولًا. اختر الموظف ثم «عرض سجل الأجر». الإضافة والتصحيح بصلاحية اعتماد الرواتب، وتحتاج تاريخًا ومكونات الأجر الستة ومرجع العقد أو القرار والسبب. كل حفظ يحتفظ بالسابق دون تغيير أجر الملف الحالي أو المسيرات. فحص المصادر يستخدم السجل بعد توثيقه، ويبقي الفجوات واختلاف العملة وتغير الأجر الحالي ظاهرة.

أضيف لاحقًا تغيير الراتب من ملف الموظف نفسه: تعديل الموظف ← البيانات المالية ← تغيير المبلغ ← «موعد تطبيق تعديل الراتب». تظهر «يسري من» والسبب ومرجع القرار؛ تظل فارغة حتى إدخالها. الحفظ بصلاحية تعديل الموظفين يسجل المراجعة والأجر الحالي معًا. تعديل الاسم أو الهاتف لا يحتاج بيانات راتب جديدة. إذا لم يطبق011 تُعطل حقول الأجر وتبقى بقية البيانات قابلة للحفظ؛ لا تخمّن تاريخًا أو تملأ بيانات الموظفين جماعيًا لتجربة الشاشة.

الزيادة المستقبلية من «طلب زيادة راتب»: الأساسي الجديد وتاريخ السريان والسبب، مع الحقول المخصصة الموجودة. نسبة الزيادة محسوبة بالخادم لتحديد الموافقات؛ تبقى السلسلة الحالية كما هي. بعد الاعتماد تنتظر الزيادة الموعد وتنفذها المهام اليومية أو لحاق الإقلاع. تغيير الأجر أو الفرع أثناء الانتظار يوقف تطبيق قرار على بيانات قديمة؛ إغلاق الزيادة غير المنفذة متاح للإدارة من تفاصيل لوحة الطلبات بسبب إلزامي، ثم تُقدم زيادة جديدة. الواجهة تعرض الخيار فقط للحالة المعنية وتتحقق الخدمة من عدم وجود أي أثر منفذ. لا يلزم ترحيل جديد بعد011 لهذه الإضافة.

للمراجعة: صافي قبل السلف900 وحد أدنى500 وقسط1666 ينتجان خصم400 وباقي1266؛ اختيار التأجيل الكامل ينتج خصم0 وراتب900؛ الطلب اليدوي يؤجل رغم كفاية الراتب بعد الموافقات. هذه أمثلة الاختبارات المعزولة، وليست بيانات الموظفين الحقيقيين. لا تُعد حساب مسير تاريخي لمجرد تجربة التحديث؛ المسيرات القديمة ذات الأقساط تحتاج مراجعة وإعادة حساب صريحة قبل صرفها بالدفتر الجديد.

التنفيذ اليدوي لأمر البدء مطلوب هنا بسبب رفض المراجعة التلقائية السابق له بعبارة «blocked by policy». الخطوات معروضة للمستخدم؛ لم تنفذها الأدوات عبر مسار بديل ولم تُوقف الخدمة الحالية.

لمراجعة التقويم بعد تحميل البناء: من العطلات أو السياسات افتح «تأكيد التقويم الحالي من تاريخ» وراجع جميع العطلات والقواعد قبل إدخال تاريخ موثق. من تعديل الفرع يظهر تقويمه، ومن ملف الموظف يظهر سريان فرعه. تغيير عطلة أو إيقاف قاعدة يحتاج تاريخ التطبيق والسبب مستقلين عن مدى العطلة. لا تدخل تاريخًا افتراضيًا لمجرد فتح فحص المصادر. في السياسات ← فحص المصادر، الجدول يعرض نوع كل يوم وحالة التقويم، والحضور يعرض أدلته وثواني التأخير أو سبب النقص. الأيام المركبة تظل غير قابلة لتنفيذ المحرك الجديد حتى استكمال أدلتها.
