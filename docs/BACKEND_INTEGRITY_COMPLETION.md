# Backend integrity completion — 2026-09-11

This records only work verified in the backend-integrity lane. It does not declare all 174 original defects closed, certify payroll, migrate customer data, or authorize deployment.

## Implemented and verified in this continuation

| Original ID / finding | Result and evidence |
| --- | --- |
| SET-11 | Replacing approval steps now uses a nested validated DTO. Creation, replacement and single-step edits share conditional-field and specific-employee checks. Invalid role/SLA/threshold/employee values return 400 before changing existing steps. Chain creation and step replacement use database transactions. Tested valid parallel levels and preservation of old steps after seven invalid replacements. |
| Additional chain scope defect | Branch operators may change only their branch's chains; a global or another branch's chain is rejected. The global duplicate lookup uses IsNull explicitly, so a branch-specific chain does not prevent creating the global counterpart. SQL HTTP tests cover both. |
| EMP-8 | Generic employee patch accepts active/probation/suspended only and cannot reopen notice_period/terminated/archived. Lifecycle transitions write history and change associated login activity/tokenVersion in one transaction. A branch operator cannot move the employee outside their branch. Tested prohibited states, suspended status, recorded prior state and revoked login. |
| EMP-12, backend portion | Relation validation uses the resulting employee, including retained department/team, and preserves explicit null when checking cleared contract dates. Tested clearing contract end/department/manager, including changing start beyond old end while explicitly clearing end, and rejecting a branch move with incompatible retained department. Frontend null submission is owned by the UI lane; not all optional fields are certified by this test. |
| EMP-13 | Archive and reactivation lock the employee and atomically save old/new status history, archive reason with actor user ID, login activity and token version. Archive rejects an existing open offboarding case. Tested active → archived → active, actor trace and token invalidation. |
| EMP-4 | Settlement gross uses six independent components: basic, housing, transport, phone, work nature and other allowance. Added a named grossMonthlySalary helper; settlement employee projection includes phone and work nature. SQL test verifies a 6,900 total and EOS formula agreement. |
| EMP-5 / accrual consistency | Settlement now reads the shared LeaveBalancesService calculation with an optional inclusive final day for monthly accrual. Daily accrual, current policy entitlement and employees not eligible for annual leave are honored. 30 September includes the ninth complete month: 15.75 days at 21 days/year. Verified daily mode and zero annual entitlement; preview remains a read. |
| Direct custody integrity | Asset update now uses an actual DTO; status/current holder mass assignment is removed. Direct assign, return and write-off check employee branch and use transactions/locks. All four open custody statuses reserve stock; pending assignments block retirement. Loss response explains the automatic settlement debit to avoid instructing a second manual charge. Tested foreign-branch writes, duplicate reservation, retirement block, invalid value/lost type and real return persistence. |
| Device catalog branch isolation | Device reads filter by branch; creation and update reject foreign-branch devices/moves. Secrets remain absent from responses. Work schedule employee counts now reflect the caller branch. SQL HTTP tests cover device create/read/update isolation. |
| Onboarding lifecycle activation | The existing onboarding module was never imported into AppModule: all /onboarding calls returned 404. Registered it. SQL tests confirm a real checklist, no duplicate template tasks under two concurrent lists, permission isolation and persisted DONE actor. Existing schema additions require an explicit customer database migration before production. |
| Additional P1 attachment privacy | File download formerly treated any request payload containing a guessed file ID as permission to download. A forged owned draft bypassed file ownership. Request-based access now excludes drafts and requires file owner/requester or uploader/creator alignment before applying request visibility. Tested forged legacy draft, forged submitted request, legitimate request visibility and draft exclusion. Request-creation validation is separately owned by the request lane. |
| Production schema safeguard | NODE_ENV=production now rejects DB_SYNCHRONIZE=true at startup. Verified rejection and acceptance with explicit false; known published signing secret is also rejected in production. |

## Existing repairs independently verified

- SET-13: branch weekend symbols normalize, malformed symbols and unknown cost-center codes reject. Existing DTO intentionally takes comma-separated symbols without embedded spaces.
- SET-14: cross-branch parent, department cycles and moving a parent while children remain in its branch reject; clearing parent persists.
- SET-15: invalid weekend/accrual values reject; added rejection of whitespace-only numeric configuration values.
- SET-18: updating minimum salary beyond maximum rejects using merged values.
- SET-23: obsolete hardcoded /settings/roles endpoint is absent (404); /roles remains the live role API.

## Verification

```powershell
node --test api/test/integrity.integration.cjs
node node_modules/typescript/bin/tsc -p api/tsconfig.json --noEmit --incremental false --pretty false
```

At this report: **30/30 integration tests pass**, using a real Nest application and an isolated SQL Server database named hr_integrity_test_<random>. API and frontend typechecks pass. The test database is created and removed by the suite; the configured original database is never seeded, synchronized or changed. File-security tests insert metadata pointing to nonexistent test files and therefore make no upload-directory changes.

The suite is separate from api/test/recovery.integration.cjs. The root lane is responsible for the combined test run, production build, screenshots and consolidated completion report.

## Remaining evidence and release boundaries

- Historical otherAllowance may already contain phone + work nature from the old form. Correct independent-component calculations can overstate such historical rows until payroll owners classify them. No heuristic migration or rewrite of original salaries has been performed, since an equal numeric total is not proof of its meaning.
- Financial certification remains out of scope: original payroll audit, regulatory policy selection and all existing settlement financial source-consumption cases still need explicit review. The tests above establish the implemented arithmetic, not legal correctness.
- SET-18 frontend range visualization and all EMP-12 frontend clearing controls belong to the UI lane; no blanket closure of these full-stack rows is made here.
- MySQL, multiple API replicas, real biometric hardware, crash/recovery under workload and customer-data migration have not been certified by this SQL Server suite.
- Onboarding's existing lazy checklist materialization uses an in-process lock; a multi-replica deployment would need a database uniqueness/locking design before being supported.
- The repository currently has no complete verified migration/deployment/backup-and-restore runbook matching the current entities. Existing README instructions still describe seed-based development bootstrap. A sellable release needs a versioned migration plan tested on a restored customer snapshot and a restore drill covering both database and UPLOADS_ROOT.
- Letter generation review found escaped text, disabled browser JavaScript, blocked external requests, real PDF storage and request-row transaction locking. A rollback after PDF generation may leave an unreferenced disk object (already documented in implementation); operational orphan cleanup/retention is not implemented here. No additional confirmed letter access defect was found in this bounded review.
- No commit, deployment, production environment changes or original database operations were performed in this lane.

## Follow-up verification during the wave matrix review

- Closed a P1 empty-branch bypass: non-admin accounts with no branch now have empty scope rather than global scope. SQL test verifies no employee/list/write leakage.
- Closed a P1 document-list filter bypass: requesting another branch employee can no longer replace the scoped employee filter.
- Direct custody return/write-off now locks Asset before Assignment and rejects pending-transfer siblings; test verifies no stock release or loss during transfer.
- LEV-7 direct revoke now rechecks state under a Leave row lock; concurrent requests restore once. The request lane independently locked cancellation destinations.
- LEV-3 deduction/restoration locks the balance and rechecks the actual remaining balance in the deduction transaction; competing 6-day deductions from 10 days produce one success and one rollback.
- LEV-2 year rollover now catches up on startup/daily execution, persists its completion marker and includes December completed accrual. A pre-created current-year row receives carry once without changing prior deductions.
- JWT11 compatibility uses validated numeric lifetime seconds instead of an unchecked string/cast.
- Root reported the upgraded frontend/API production npm audits are now clear. Runtime minimum Node 22.12 is documented in RUNTIME_DEPLOYMENT_REQUIREMENTS.md and the environment example; final production build/combined test report remains owned by root.

## Final bounded quality and privacy pass

- EMP-2: added `api/test/eos-policy.test.cjs`, **11/11 pass**. Literal synthetic expectations cover default resignation and termination at 1.99/2/2.01, 4.99/5/5.01 and 9.99/10/10.01 service years, custom tiers and factors, every existing termination reason, two-stage rounding, inclusive dates, the existing leap-anniversary convention and invalid inputs. Actual OffboardingService preview/buildSettlement results are compared at six default boundaries and custom/zero cases using repository doubles. Root's separate SQL settlement tests cover persistence. No policy rules changed; this is implementation coverage, not legal certification or per-country policy approval.
- SET-4: replaced `/settings` demo tabs with 20 links to existing working pages, filtered by the same permission used by each destination. Removed fictional company data, unwired saves, fake 2FA/tax forms and connected integrations. Company data links to its real page. Moved the three otherwise-orphaned real controls (system currency, device ingestion key and absence catch-up limit) to the existing policies page, preserving secret masking/generation, weak-key warning, currency cache invalidation and shift-grace override note. Code review and frontend typecheck passed; root owns browser screenshots.
- NAM-14 security consequence: a shared response projector now hides salary/allowances, bank/payment/GOSI data and loans from a bare employees.view reader in employee list/detail/profile. It hides both financial history values and reasons in profile/history. Own employee, payroll.view and employees.edit retain the values. A real SQL HTTP test verifies all routes, branch denial, permitted readers and unchanged source rows. The original generic history-column design and plaintext storage remain migration/design work; this is not blanket closure of NAM-14.
- NAM-28: direct custody assignment now records the actor's employeeId, matching the existing column's employees.id semantics. An unlinked account stores NULL instead of incorrectly writing users.id. SQL test verifies both; adding a dedicated actorUserId audit field still needs an explicit schema migration.
- Attachment lifecycle: DocsController create/update and the actual EmployeeForm create/update path now link preliminary uploads to the target employee while saving document metadata in the same transaction. The shared helper locks files in ID order, verifies uploader/target ownership and rejects existing other-employee associations. Photo, contract and document refs are covered. SQL HTTP regression verifies target-owner download passes authorization (the synthetic disk file is deliberately absent), foreign/forged references reject and both employee writes and file reassignment roll back on failure. Existing customer attachments were not migrated.
- NAM-34 backend country default: removed hardcoded EG from PublicHoliday entity. Holiday API creation derives omitted country from system.country; explicit blank still means all countries and an explicit country overrides configuration. SQL HTTP test verifies SA from configuration, blank/EG overrides and unchanged country on a name-only edit. Removing the old SQL default constraint remains a reviewed deployment migration, documented in RUNTIME_DEPLOYMENT_REQUIREMENTS.md.
- Registered the existing DocTypesDefaultsService provider. Fresh application startup now materializes base document types; a regression proves repeated bootstrap preserves edited names, disabled states and code counts. It inserts only missing codes and does not replace customer rows.
- LEV-16: implemented audited current-year adjustment with existing leave_balances.manage permission, branch scope, mandatory reason and UUID idempotency key, optional stale-value check, independent adjustmentDays layer and transactionally stored before/after values plus actor. It preserves taken/leave rows, survives entitlement-policy changes and is included in deduction, restoration, settlement and carryover. Five SQL regression cases cover validation/access, policy/settlement/carryover agreement, concurrent duplicate and conflicting submissions, a real twice-run migration against a disposable legacy table, and forced audit-storage failure rolling back the balance before a safe same-key retry. The contract and migration are documented in LEV16_MANUAL_BALANCE_ADJUSTMENTS.md.
- NAM-22 defensive clearance check: editableCase now rejects any clearance item not DONE even if a historical case already says IN_SETTLEMENT. SQL regression proves legacy BLOCKED prevents automatic transition and direct settlement approval without changing its historical row. This accompanies root's removal of the unused BLOCKED public type; it does not rewrite old data.
- The NAM-14 regression was extended to the known offboarding finalization reason, which embeds settlement net while its status columns contain normal lifecycle states. Bare readers retain the termination event with a generic reason, while the same owner/payroll/editor permissions preserve the financial detail. Profile/history and list/detail projections are exercised; this is not a claim that every financial endpoint was audited.
- Balance dates now use the existing localDateOf helper consistently for the business year boundary. After that final date adjustment, the seven focused LEV-16/NAM-14/NAM-22 cases passed again; the immediately preceding full integrity run passed 30/30.

## Pre-payroll structured history and migration completion

The later NAM-14/NAM-28/NAM-30 work supersedes the earlier migration/design caveats above for the retained review copy: structured history and centralized writers, masked bank audit at rest and at HTTP boundaries, explicit physical actor/ID column names with old API compatibility, and exact historical workType alias migration are implemented. The integrity suite now passes 31/31; history/identity data migrations pass 2/2 on synthetic SQL, and legacy title/renewal API checks pass. Six versioned migrations are applied to the independent backup-restored review copy only, with zero pre-payroll schema diff and 14 preserved-data hashes. Source schema/62 table counts remain unchanged. See `docs/prepayrollmigration/NAM14_NAM28_NAM30_COMPLETION.md` and its runbook for details; payroll remains excluded and the original database has not been migrated.

The exact original 67-issue wave status matrix is RELEASE_ISSUE_MATRIX_W12.md. Subsequent discoveries are recorded there instead of assuming all original reported fixes were complete.
