# Release issue matrix — original waves 1 and 2

Audit date: 2026-09-11. The list below contains exactly the 67 IDs from FIX_REPORT_W12.md, matched to OUR_TECH_DEFECTS.md. No row is assumed fixed merely because the old report said so.

Status vocabulary: **verified test** = an identified executable regression passes (scope/limits are stated); **code-reviewed fix** = implementation is present and reviewed, but the complete behavior was not independently exercised; **still open** = original requirement not implemented; **needs data/deployment** = code may be ready but installation/customer data is the actual remaining action. Rows are not legal/financial certification.

Current counts: 49 code-reviewed fix; 18 verified test; 0 still open in implementation. Physical terminal acceptance remains a stated limit for ATT-24.

Final consolidated evidence: **161/161 passed**, including the original regressions, letter templates, decision races, device authentication, leave contracts, calendar/session behavior, and migration preservation. Database suites create and remove their own isolated SQL Server databases. Migrations 001–006 were applied and verified on a restored review copy only; the original customer database was not migrated. API and frontend production builds passed (84 frontend pages). See [the current completion report](D:/projects/hr_system/PRE_PAYROLL_COMPLETION_2026-09-11.md) for evidence and current runtime limitations.

| ID | Status | Current evidence and remaining limit |
| --- | --- | --- |
| SEC-SET-1 | code-reviewed fix | api/src/auth/users.controller.ts validatePermissions/update; roles.controller.ts validatePermList/setUserPermissions; permissions.ts adminGrantViolation block wildcard, self role/permissions and privileged grants. Previous FIX report cites 39 denied attempts; that full attack matrix was not recreated in this continuation. |
| SEC-SET-5 | verified test | api/src/auth/jwt-secret.ts validateEnv and jwtLifetimeSeconds; integrity tests production bootstrap and JWT11 duration. auth.module.ts/jwt.strategy.ts use getOrThrow with no dev-secret. |
| SEC-SET-6 | code-reviewed fix | api/src/auth/users.controller.ts update: self identity/branch changes reject; assertAssignableBranch/assertLinkableEmployee run on changed references; tokenVersion increments. Recovery old-JWT test verifies revocation mechanism, not every linkage combination. |
| SET-9 | code-reviewed fix | api/src/auth/roles.controller.ts update increments tokenVersion for the assigned role when permissions/activation change; name-only change does not. No new complete role-edit HTTP matrix. |
| SET-8 | code-reviewed fix | api/src/auth/users.controller.ts assertAssignableRole reads active database roles. auth.service.ts resolves database roles; approver-resolver.service.ts resolves finance/IT/custody by permissions. Custom role name alone is not approval authority. |
| SEC-SET-2 | verified test | org.service.ts enforces scope on branch/department/team writes; users.controller.ts enforces target scope. NEW related P1 closed: auth/guards.ts branchScopeOf maps unassigned non-admins to -1; integrity null-branch test proves employee list/detail/write and branches cannot become global. Org integrity test verifies cross-branch tree rejection. User password takeover combinations remain code-reviewed. |
| EMP-15 | code-reviewed fix | src/components/layout/MainLayout.tsx explicit employees/add, edit and archive route permissions precede employees listing route; Sidebar.tsx uses API-matching permissions. No exhaustive per-role browser replay. |
| ATT-28 | code-reviewed fix | MainLayout.tsx and Sidebar.tsx use settings.manage for shifts and attendance.sync for devices before general attendance gate. |
| LEV-14 | code-reviewed fix | MainLayout.tsx and Sidebar.tsx gate holidays and leave types with settings.manage. |
| SET-22 | code-reviewed fix | src/app/settings/roles/page.tsx loads fetchRolesFull and fetchPermissionsRegistry, not /users. RolesController.list supplies scoped user counts with roles.manage OR users.manage. |
| SET-19 | code-reviewed fix | src/app/settings/asset-types/page.tsx is the real asset register; MainLayout/Sidebar use custody.assign, matching /assets API. |
| SEC-REQ-1 | verified test | api/test/recovery.integration.cjs request details cannot be read by unrelated employees; RequestsService.detail restricts owner/creator/resolved approver/scoped view_all. Root reported combined suite pass after dependency update. |
| SEC-REQ-3 | code-reviewed fix | RequestsService.seesConfidential/maskConfidential remove requester/creator/payload for non-parties, used for lists and detail. Forged-file request privacy regression also passes, but a full confidential-type identity matrix is not claimed. |
| SEC-LEV-1 | code-reviewed fix | RequestsController.leave-balances/:employeeId loads employee and compares branchScopeOf before returning balance; own route is separate. |
| SEC-REQ-2 | verified test | Recovery personal updates cannot change email or bank outside controlled routes test passes. destinations.service.ts PERSONAL_RECORD_FIELDS excludes email/bank; RequestsService.assertPayloadKeys and approvals inbox show declared fields. |
| REQ-17 | code-reviewed fix | src/app/approvals-inbox/page.tsx uses inbox/types/branches; request employee picking uses /employees/directory and requesterName from inbox. No blanket GET /employees permission dependency. |
| SEC-SET-7 | verified test | Recovery future punches and weak device keys cannot write attendance test. attendance/device-key.ts rejects placeholders/short keys; actual key editor/generator and warning were moved from demo overview to settings/policies. Real configured device key is deployment-owned and was not changed. |
| SEC-ATT-1 | code-reviewed fix | AttendanceController passes CurrentUser to manual punch/schedules/overrides/confirmation/recompute; AttendanceService scopes writes and rejects own overtime confirmation. New null-branch fail-closed helper applies here too. Full mutation matrix still requires dedicated tests. |
| ATT-19 | code-reviewed fix | src/app/attendance/permissions/page.tsx builds actionable IDs from inbox; per-row actions require actionable membership and show permission type. |
| SEC-SET-3 | verified test | Recovery negative shift grace/invalid holiday range test; CatalogsController.pick allowlist and validate merged row on update. Integrity asset typed-patch/mass-assignment tests cover equivalent direct-assets hole closed in this continuation. |
| SEC-ATT-2 | verified test | Integrity device catalogs isolate branches and protect secrets test. READ_PERMS restrict devices/grades/cost-centers; BiometricDevice.authKey select:false; list strips grade salary ranges for non-financial readers. Weekly/override reads are separately permission-scoped in AttendanceController. |
| SEC-EMP-4 | verified test | DocsController uses concrete UpdateDocumentDto and employeeInScope for writes. NEW P1 read filter overwrite closed in list; integrity SEC-EMP-4 document employee filter test verifies foreign employee404, malformed400 and own branch records. Broad document update DTO is code-reviewed. |
| LEV-1 | code-reviewed fix | RequestsController GET leave-types returns active safe fields without settings.manage; leaves/request and requests forms use that employee-accessible catalog. |
| SET-6 | code-reviewed fix | settings/request-types page sends destinationHandler only when changed; SettingsController handlerOptions/isKnownHandler derive from actual handlers; current handler can remain while editing other fields. |
| SET-21 | code-reviewed fix | settings/request-types page uses Promise.allSettled; types/handlers are required, chains/departments/employees optional. Missing chain permission sets chainsAvailable=false without dropping core screen. |
| SEC-EMP-1 | code-reviewed fix | OffboardingService.accessOf/detail/list scope cases and project employee fields; settlement amounts require settlement.edit/approve. completeItem and settlement operations scope employee. Recovery preview/finalization tests prove branch checks, but unrelated-detail money masking is not newly exercised end-to-end. |
| EMP-7 | code-reviewed fix | OffboardingService.accessOf/completeItem allows responsible clearance party or HR; detail returns canAct. src/app/offboarding/[id]/page.tsx renders actions from canAct; MainLayout allows case detail and server enforces access. |
| SEC-EMP-2 | code-reviewed fix | employee profile print uses escapeHtml(generatedDocument); destinations assertIban/assertBankName/assertJobTitle reject invalid bank/title values. Real new PDF generator also escapes every interpolated field. |
| SEC-EMP-3 | code-reviewed fix | qualifications.controller.ts assertEmployee enforces branch; all five nested deletes use {id:rowId,employeeId:id} and assertDeleted. |
| DSH-13 | code-reviewed fix | QuickActions.tsx filters every required permission; leave action links /leaves/request. |
| DSH-17 | code-reviewed fix | src/app/page.tsx chooses dashboard via dashboard.view_all; PendingApprovals.tsx uses Promise.allSettled so one auxiliary failure cannot erase inbox. |
| ATT-5 | verified test | Recovery future attendance recalculation does not store absence, and future punches rejection. AttendanceService.computeDay deletes unexcused future rows rather than creating absence. |
| ATT-1 | code-reviewed fix | AttendanceService.shiftFor order: day override, weekly, employee schedule, active isDefault schedule, none; computeDay stores unscheduled/source/shiftId. Recovery schedule/my-today tests cover assigned and absent schedules, not every default-precedence permutation. |
| ATT-2 | code-reviewed fix | computeDay in/out window selection falls back to first pre-checkout late punch; punchAnomalies records dropped-window times. No new late-after-window boundary matrix. |
| ATT-6 | code-reviewed fix | AttendanceService.approvedExcusedDays reads BUSINESS_TRIP/REMOTE_WORK; computeDay uses mission/remote. RequestsService.executeDestination recomputes approved range. No real mission/remote regression in the present suite. |
| ATT-4 | verified test | Recovery future punches and weak device keys cannot write attendance checks device ingest and manual rejection. |
| ATT-8 | code-reviewed fix | computeDay persists graceUsed from actual shift/global grace; attendance page displays it, settings/policies lists shift-specific override count after removing demo overview. |
| ATT-10 | code-reviewed fix | approvedPermissionWindows resolves permissionTypeId before legacy name; unknown or deductible types are charged instead of granting free excuse; RequestsService validates ID. |
| ATT-20 | code-reviewed fix | AttendanceService exports localDateOf and uses local date keys; DashboardController uses localDateOf. Overnight integration tests pass; a full Cairo DST/midnight clock-boundary test and explicit deployment TZ remain outstanding. |
| ATT-3 | code-reviewed fix | AttendanceService cleanupDetectedOvertime removes unsupported BIOMETRIC_DETECTED DETECTED/SUBMITTED rows and related pending workflow after recalculation. No dedicated phantom-overtime deletion regression here. |
| ATT-7 | code-reviewed fix | computeDay assigns missing_punch for one-sided completed days; AttendanceStatus and monthly summary include it, shared frontend labels render it. |
| ATT-9 | code-reviewed fix | AttendanceService.ingest maps fingerprintCode before employeeCode; EmployeesService.relinkPunches runs on create/code change; relink only claims codes not owned by another fingerprint mapping. Original orphan punch population was not rewritten. |
| ATT-14 | verified test | Recovery overnight punches attributed to starting day and morning punch in later batch completes prior overnight shift tests pass; persisted clocks remain 06:00 rather than 30:00. |
| ATT-15 | code-reviewed fix | AttendanceService.employeeMonth returns partialLeave/leave/holiday/missingPunch/mission/remote plus totalEarlyLeaveMinutes; monthly-sheet consumes them. |
| ATT-22 | code-reviewed fix | computeDay derives non-working day before full leave status; employee/branch/global weekend and holiday rules determine whether range day is holiday. |
| ATT-26 | code-reviewed fix | AttendanceService.ingest validates each payload timestamp/code and returns rejection reasons; AttendanceController DTO validates outer payload. Recovery covers future rejection, not every malformed batch shape. |
| ATT-27 | code-reviewed fix | AttendanceService bulk overrides accumulates per-employee failures; single override recomputes its complete affected date range. |
| ATT-25 | code-reviewed fix | device-sync.service.ts pulls logs, fetches existing punches by range, deduplicates with Set and submits chunks. No device hardware/throughput benchmark; this is a code fix only. |
| LEV-8 | code-reviewed fix | AttendanceService.workingDaysBetween/workingDatesBetween and RequestsService post-execution loop traverse full validated ranges without old 62/92-day truncation. |
| ATT-21 | code-reviewed fix | AttendanceScheduler runs bootstrap delayed catchup, daily and hourly retry; materializeAbsencesCatchUp persists completed-through key only after success; liveAbsences feeds today. Crash and restart workload simulation remains untested. |
| NAM-2 | verified test | Final override mismatch was repaired by request lane: day, bulk and weekly schedule paths resolve active shiftId and persist the catalog snapshot; name cannot override ID. api/test/request-execution.integration.cjs NAM-31 day and bulk overrides test verifies ID-only input, rename stability and invalid IDs. Root reported 25/25 request tests passing. Legacy name-only records retain compatibility rather than being silently rewritten. |
| LEV-2 | verified test | NEW integrity missed annual rollover catches up once test: startup/daily scheduler calls catchUpRollover, persisted marker prevents duplicates, existing early-created year rows receive carry and December accrual is included. ensureRow creates missing year row; splitByYear is used for cross-year deductions/restoration. Cross-year scenario previously tested per FIX report; current catchup tested on isolated SQL. |
| LEV-22 | code-reviewed fix | LeavesController.list uses filtered query with overlap dates, skip/take/count and filtered stats; leaves page consumes items/total/page/pageSize. Previous FIX live pagination noted; no new full paging browser test. |
| LEV-10 | code-reviewed fix | LeaveBalancesService BalanceView separates annualEntitlement/accruedToDate/entitled; balance and my/leaves labels consume distinct values. Integrity settlement test checks policy accrual, not both UI labels. |
| LEV-11 | code-reviewed fix | leaves/request fetches server working-day count, period/full/half selections and actual response statuses; no independent form submission browser matrix. |
| LEV-21 | code-reviewed fix | ApiLeave exposes period; leave tables display MORNING/EVENING and calendar half-day marks. |
| LEV-4 | code-reviewed fix | RequestsService derives days and rejects reversed/missing dates; LeaveBalancesService.deduct rejects nonpositive/nonfinite days and now checks available balance under lock. Existing report validated tampered days; current concurrency balance test verifies no overdraw. |
| LEV-7 | verified test | NEW integrity concurrent direct revocations restore once test passes; LeavesController rechecks APPROVED under lock; request cancellation handler now locks Leave too (request agent). Restore balances lock affected row. Cancellation remains supported; modifying an existing leave is still a distinct unimplemented feature removed from the advertised type name. |
| LEV-3 | verified test | NEW integrity concurrent balance deductions test proves one of two competing 6-day claims against10 succeeds and one rolls back. RequestsService reserves pending days and rechecks before final approval; balance view reports deficit. |
| LEV-20 | code-reviewed fix | RequestsService leave-date validation checks leave.max_backdate_days for employee submissions; HR bypass uses permissions; policy page edits configured limit. Previous FIX live role-specific test not fully repeated. |
| DSH-7 | code-reviewed fix | DashboardController.stats attended CASE includes present/late/early_leave and other actual punches except full leave/absence; trend reuses attendedSql. |
| DSH-9 | code-reviewed fix | DashboardController.stats underReview equals RequestsService.inbox(user).length rather than all branch requests. |
| DSH-15 | code-reviewed fix | DashboardController employee counts require isActive=1 and status NOT IN archived/terminated. |
| DSH-16 | code-reviewed fix | DashboardController onLeaveToday groups by employee, filters active employees and branch, separates half-day, counts complementary halves as full. |
| ATT-24 | verified test | Local ZKTeco TCP adapter authenticates with Comm Key, validates checksum/session/reply/chunks and rejects incomplete pulls; UI keeps/replaces/clears the key without exposing it. Branch authorization precedes network access. 22 TCP fixture tests +4 SQL/HTTP tests pass. No physical terminal was connected: see docs/ATT24_DEVICE_AUTH_COMPLETION.md for supported protocol and hardware acceptance boundary. |
| LEV-12 | verified test | Recovery negative shift grace and invalid holiday ranges rejects end before start; CatalogsController remove/update recompute old/new holiday ranges; OrgService country change triggers recomputation. Deletion/recomputation completeness remains code-reviewed. |
| LEV-9 | verified test | Integrity EMP4/5 policy test changes annual entitlement/mode and observes live settlement balance; daily and non-entitled cases pass. LeaveBalancesService annualEntitlementOf/accruedEntitlement read current annual/sick settings; policy screen exposes modes and sick entitlement. |

## Shared deployment and evidence constraints

- Original database schema updates are not applied by this continuation. New attendance/onboarding/employment columns and tables need a versioned migration tested against a restored snapshot. Production startup rejects automatic synchronization.
- Existing placeholder secrets, device connection keys, unmatched fingerprint codes, employee schedules, historical folded allowance totals and existing stale rows require customer-data review. Neither data cleanup nor live recalculation was performed.
- Tests use SQL Server. MySQL, actual ZKTeco communication, Cairo DST boundary cases, multiple API replicas and backup restoration are not certified by these tests.
- The dependency audit initially found critical/high findings. Root upgraded dependencies and reported both production npm audits at zero vulnerabilities; final build is in root validation. This historical audit is not presented as an unresolved blocker after its fix.
- A successful build and unit/integration suite do not prove all 67 paths were exercised. Code-reviewed rows are intentionally distinguished from tested rows.
