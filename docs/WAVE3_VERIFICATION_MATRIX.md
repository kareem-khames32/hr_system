# مصفوفة الموجة الثالثة — 32 بنداً

تطابق هذه القائمة البنود الـ32 في قسم «الموجة ٣» من `OUR_TECH_DEFECTS.md` حرفياً. تاريخ المراجعة: 11 سبتمبر 2026. «متحقق» يعني إصلاح الكود مع اختبار تكامل محدد، وليس اعتماداً قانونياً أو شهادة جاهزية للبيع. «كود فقط» يعني تنفيذ ظاهر بالمراجعة دون اختبار يغطي المشهد الأصلي كاملاً. «جزئي» يعني بقاء جزء من البند. لا تُغلق واجهة لمجرد نجاح الباك.

مصادر التحقق: `api/test/request-execution.integration.cjs` (40 ناجحاً في تشغيل12سبتمبر)، `api/test/request-decision-race.integration.cjs` (8 ناجحة)، `api/test/integrity.integration.cjs` وتقرير `docs/BACKEND_INTEGRITY_COMPLETION.md`، و`api/test/recovery.integration.cjs`. التشغيل المجمّع الأخير172/172؛ الأدلة الجديدة وحدود التشغيل في [تقرير المتابعة الفنية](D:/projects/hr_system/TECHNICAL_FOLLOWUP_2026-09-12.md)، والأدلة البصرية السابقة في [تقرير المرحلة قبل الرواتب](D:/projects/hr_system/PRE_PAYROLL_COMPLETION_2026-09-11.md).

| # | البند | الحالة وقت المراجعة | الدليل والجزء المتبقي |
|---|---|---|---|
| 1 | REQ-2 | متحقق | القرار والتدقيق والوجهة داخل معاملة واحدة وقفل طلب، بما يشمل الإلغاء وإعادة التقديم وإعادة التنفيذ؛ `executeDestinationLocked` يستخدم المعاملة القائمة. اختبارات rollback والتقديم المتزامن في request-execution، ومسار failed resubmission/retry في recovery، وثمانية اختبارات للقرارات والسبب المطلوب في request-decision-race. |
| 2 | REQ-3 | متحقق للعيب المذكور | حارس supports يرفض غير المبني، وnone سجل صريح. اختبارات title/contract/SHIFT_SWAP/unsupported في request-execution؛ مأمورية/عمل عن بعد لهما سجلهما الذي يقرؤه الحضور. الأنواع الأخرى غير المبنية لا تزال محجوبة. |
| 3 | REQ-9 | متحقق | اختبار `personal and emergency request forms...` يثبت حفظ emergencyContactName/Phone وعدم تغيير هاتف الموظف؛ الخريطة تشمل العلاقة والهاتف البديل. |
| 4 | REQ-10 | متحقق | اختبارات12سبتمبر تثبت تعديل ومسح address/maritalStatus/phone/phone2، ورفض no-op والحقول غير المسموحة، والتراجع بعد فشل التدقيق. أُصلح قبول الموظف المفقود، وأضيف قفل صف الموظف طوال التحديث والتدقيق؛ اختبار تزامن حتمي يثبت القيم القديمة والجديدة بالترتيب الصحيح. |
| 5 | REQ-13 | متحقق | اختبار recovery `failed direct submission leaves no orphan draft`؛ request-execution يثبت كذلك أن فشل العنوان والمرفق لا يحفظ مسودة. |
| 6 | REQ-23 | متحقق | اختبارا12سبتمبر يثبتان ظهور عهد الموظف في صندوق قائد الفريق عند غياب managerEmployeeId، ثم التأكيد واكتمال الطلب وتفعيل الأصل؛ يُمنع الاعتماد الذاتي وغير المختص وخارج الفرع. نفس directManagerOf يحكم الصندوق والتأكيد، ولم يلزم تعديل الخدمة. |
| 7 | SET-3 | متحقق | اختبار recovery `disabled approval chains stop new submissions`؛ resolveChain يرفض السلسلة المعطلة. |
| 8 | REQ-8 | كود + اختبار الجذر | LettersService يولد PDF عربي فعلياً ويخزنه مع تنزيل محمي. recovery يحتوي `letters generate real Arabic PDFs...` و`letter generation without required company identity...`؛ نتائج الملف والصور في تقرير الجذر. |
| 9 | REQ-11 | متحقق | اختبارات recovery `early loan settlement...` و`invalid loan amounts...` تثبت تسديد السلفة الموجودة ورفض الأرقام غير الصالحة. |
| 10 | REQ-12 | متحقق | recovery `retirement opens clearance and notice period...` يثبت فترة الإشعار وخمس جهات دون تعطيل مبكر. |
| 11 | REQ-18 | متحقق | recovery `a structural approval with no manager fails before entering the workflow`؛ لا يدخل الطلب سلسلة لا يمكن حسمها. |
| 12 | REQ-14 | متحقق | recovery `required fields are enforced when submitting a previously saved draft` و`failed resubmission preserves returned status...`؛ assertSubmitValues واحد للمسارات. |
| 13 | SET-11 | متحقق | integrity `SET-11 nested chain validation rejects invalid replacements...`؛ DTO متداخل وفحوص مشتركة ومعاملة تحفظ الخطوات القديمة عند الرفض. |
| 14 | REQ-15 | متحقق بالمتصفح | تحقق الجذر فعلياً: تعليق الإرجاع وحالة RETURNED_FOR_INFO محفوظان، تعديل العنوان ثم resubmit يثبت بعد إعادة التحميل. runtime fields الشخصية والطوارئ تعرض حقول التعديل الفعلية. |
| 15 | REQ-16 | متحقق بالمتصفح | تحقق الجذر من نافذة التفاصيل بكل الحقول ثم اعتماد الطلب واختفائه من صندوق الموافقات. RequestPayload + fetchRequest في الدرج ونافذة القرار يعرضان المفاتيح والمراجع والمرفقات وأسماء الكتالوج. |
| 16 | EMP-2 | متحقق للسياسة المطبقة | eos-policy.test.cjs: 11/11 ناجحة وفق مسار backend_integrity؛ حدود الاستقالة/الإنهاء 1.99/2/2.01 و4.99/5/5.01 و9.99/10/10.01، الشرائح والمعاملات المضبوطة، التقريب والتواريخ الشاملة والسنة الكبيسة، والمقارنة بين preview/buildSettlement. هذا تحقق من السياسة المطبقة وليس اعتماداً تشريعياً لكل دولة. |
| 17 | EMP-1 | متحقق بالمتصفح | تحقق الجذر من /employees/2/terminate ببيانات DEMO002 ومعاينة API مقدارها 31992.88 SAR، ثم التأكيد وفتح /offboarding/1 بخمس بنود إخلاء معلقة 0/5 وآخر يوم 2026-09-30؛ إعادة التحميل أبقت الملف والبيانات. دليل الصورة output/screenshots/offboarding-preview.png وصورة الملف في تقرير الجذر. أزيلت البيانات الديمو والحساب المالي المحلي والنجاح الوهمي من الصفحة. |
| 18 | EMP-4 | متحقق | integrity `EMP-4/5 settlement includes independent allowances...` يثبت إجمالي المكونات الستة. البيانات التاريخية التي كان otherAllowance فيها مجموعاً مشتقاً تحتاج مراجعة بيانات مستقلة. |
| 19 | EMP-8 | متحقق | integrity `EMP-8 lifecycle statuses cannot be changed through generic patch...` يثبت رفض تجاوز دورة الإنهاء وتسجيل التعليق/تحديث التوكن. |
| 20 | EMP-9 | متحقق | request-execution `transfer changes team, department, branch, direct manager and login scope...` يثبت الحقول الأربعة والأثر على التوكن. |
| 21 | EMP-11 | متحقق | request-execution `TEAM_TRANSFER checks the target employee custody...` و`duplicate scheduled transfers...`؛ الموظف الفعلي يخضع للصلاحية والعهدة ومنع التكرار. |
| 22 | EMP-13 | متحقق | integrity `EMP-13 archive and reactivation each record prior state...` يثبت الحالة القديمة والسبب والمنفذ وتحديث الحساب. |
| 23 | EMP-5 | متحقق | integrity EMP-4/5 يثبت 15.75 يوم حتى 30 سبتمبر، مع اليوم الأخير ضمن الشهر، والسياسة اليومية وعدم استحقاق السنوي. |
| 24 | EMP-6 | متحقق | recovery `manual finalization respects branch scope, last working day and idempotency` يثبت المقارنة المحلية بعد آخر يوم فقط. |
| 25 | EMP-25 | متحقق | اختبارات custody transfer/reject في request-execution تثبت بقاء الحامل ثم انتقاله عند التأكيد وإمكانية إلغاء المعلق؛ المسارات القديمة بلا حامل لا تُصحح بمالك مفترض. |
| 26 | EMP-29 | متحقق للباك + مراجعة واجهة | Transfer.fromTeam أصبح nullable، والنقل بلا فريق يخزن NULL، وGET /transfers يرجع «بدون فريق» للصفر القديم وNULL الجديد؛ اختبار target custody/transfer يثبت التخزين والعرض في API. الواجهة أيضاً تستخدم fallback «بدون فريق» للصفر/NULL. migration SQL Server/MySQL معدة ولم تطبق على الأصلية. |
| 27 | REQ-26 | متحقق للباك + مراجعة واجهة | API الرفض وحالة REJECTED ودورة المخزون مجرّبة بالتكامل؛ الواجهة تحتوي زر الرفض والسبب والتسمية. نُفذت عدادات المنتهية والمفقودة في12سبتمبر، مع بيان أن المفقودة جزء من الإجمالي. بناء الواجهة وفحص الأنواع ناجحان؛ تجربة العدادات الفعلية بالمتصفح ما زالت معلقة لتوقف خادم المراجعة. |
| 28 | EMP-12 | متحقق للحفظ؛ الواجهات مراجعة كود | integrity يثبت null لحقول العقد والعلاقات وEmployeeForm يرسل null للقيم الممسوحة. أصلحت الفروع (nameEn/city/address/phone/email/manager) والأقسام (nameEn/code/manager) والفرق (code/leader) كي ترسل null عند التعديل. المستخدمون وأنواع الإجازة كانوا يرسلون null بالفعل. اختبار EMP-12 ضمن 31/31 request-execution يرسل PATCH ثم GET فعلياً للخمسة ويثبت null لكل الحقول الاختيارية المحددة، بما فيها country/costCenter/weekendDays/parentId/user branch+employee/leave attachment+maxDays. DTOs وhandlers تدعم null دون تعديل؛ TypeScript للواجهة ناجح. لم يُجرّب النقر البصري على كل نموذج منفرد. |
| 29 | NAM-13 | متحقق للباك | مسار التقاعد يستخدم ملف الإخلاء ثم terminated مثل الاستقالة؛ recovery retirement + finalization. الأرشفة اليدوية منفصلة في EmployeesService. |
| 30 | EMP-3 | متحقق | recovery finalization + OnApplicationBootstrap.finalizeDue + POST /offboarding/finalize-due. سيناريو تعطل خادم فعلي عند موعد cron غير مجرّب تحت حمل. |
| 31 | EMP-10 | متحقق | RequestsScheduler.onApplicationBootstrap يشغل catchUp بعد30 ثانية للنقل والتغييرات الوظيفية والتصعيد والترحيل مع عزل الأخطاء. اختبار `startup catch-up executes overdue transfers and escalations...` يثبت نقلًا متأخراً وتصعيداً وعدم التكرار، وكذلك تصعيدين متزامنين بسجل واحد. |
| 32 | EMP-14 | متحقق للتجديد؛ التنبيهات مشتقة عند القراءة | POST /employees/:id/contract/renew يحفظ التواريخ القديمة والجديدة والفاعل والسبب والمستند الاختياري في معاملة واحدة؛ صلاحية employees.edit ونطاق الفرع وملكية الملف مع تحقق التواريخ ومنع التداخل والطلب المعلق. ثلاثة اختبارات EMP-14 ضمن 28/28 request-execution تثبت النجاح والمنع والتراجع بعد حفظ المستند. واجهة contracts تستخدم المسار والرفع الحقيقي بدلاً من PATCH؛ TypeScript ناجح. تنبيهات العقود/المستندات تُشتق في كل GET /notifications ضمن 60 يوماً وعند الانتهاء، باختبار expiry reminders في recovery لدى الجذر؛ ليست job يومية ولا إشعارات مخزنة مسبقاً. انتهاء العقد لا ينهي خدمة الموظف تلقائياً؛ يبقى قرار HR. |

هذه المصفوفة تمنع وضع علامة إغلاق عامة للموجة الثالثة. تُحدّث فقط عندما يصل دليل إصلاح واختبار إضافي للبنود الجزئية والباقية.
