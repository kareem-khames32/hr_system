# قرارات 14 سبتمبر 2026 — الرواتب وقاعدة البيانات

> سجل القرارات المعتمد لاستكمال الرواتب. الأسبقية: قواعد المالك المؤكدة اليوم ← خطة `PAYROLL_REVIEW_2026-09-14.md` §7 ← `PAYROLL_OWNER_SCOPE_REVIEW_2026-09-14.md` و`PAYROLL_HANDOFF_LATEST.md` §2 ← التقرير الأساسي وSRS.
> كل مسار يضيف قسمه هنا دون حذف أقسام غيره. القسم 3 وما بعده كتبه مسار قاعدة البيانات، والأدلة كلها قابلة لإعادة التشغيل (قراءة فقط).

## 1. قواعد المالك المؤكدة اليوم

- **الوردية الليلية** (مثل 20:00 → 01:00 في رمضان) تتبع يوم بدايتها بالكامل: الساعات والتأخير والنقص والغياب على يوم البداية، ولا شيء على اليوم التالي.
- **الموظف الجديد**: التغطية تبدأ من تاريخ التعيين؛ أيام الفترة قبله ليست غيابًا ولا خصمًا، ويُصرف له عن أيامه المغطاة.
- **تغيير الراتب** يسري على شهر مسير كامل، بلا تقسيم أو متوسط داخله. «مسير سبتمبر» = 23 أغسطس → 22 سبتمبر (`payroll.cycle_start_day=23`).
- **مسار الأولوية**: مجموعة معدلات مسماة (سياسة) بدورتها ← اختيار موظفين بفلاتر مترابطة (فرع، قسم، فريق، قائمة، مع استبعاد) ← مسير مربوط بالمجموعة بمعاينة أرقام حقيقية.
- **الخصومات والمكافآت** لموظف أو اختيار أو فريق أو قسم أو فرع، بمعاينة واستبعاد قبل الإرسال؛ أنواع الخصم بطريقة قيمة وحدود ونطاق منشئ وسقوف وتصعيد واعتماد وفترة مستهدفة؛ الإعفاء المالي منفصل عن إعفاء الحضور؛ المكافأة الفردية تحتاج اعتمادًا.
- **كل بيانات `hr_system` الحالية بيانات اختبار**: إصلاح التناقضات التي توقف العمل مقدم على الحذف، ولا مسح جماعي.
- **المكتبات المرقّاة تبقى**: Nest 11، Next 15، mssql 12، puppeteer، pdfjs-dist.

## 2. القرارات الافتراضية المختارة للمالك (D1–D13)

تُنفَّذ كإعدادات قابلة للتعديل تُبذر عبر `configSeed` (`api/src/seed/requests-seed.data.ts`) وتُتحقق في الإعدادات. المسار المنفذ يوثق مفتاح الإعداد وحالته تحت هذا الجدول.

| الرمز | القرار |
|---|---|
| D1 | الخروج المبكر على وردية ثابتة يُخصم بسعر الدقيقة (إعداد). |
| D2 | أساس سعر الساعة = `payroll.daily_hours` (8). |
| D3 | سعر يوم الغياب ونهاية الخدمة = الأجر الشهري الكامل للمكونات الستة ÷ 30. |
| D4 | المال بكسور عشرية دقيقة وتقريب واحد مشترك (منزلتان، نصف لأعلى) في المسير والقسيمة ونهاية الخدمة والتصفية. |
| D5 | مفاتيح النقص/التداخل/سقف اليوم/المرونة بافتراضيات المواصفة: النقص بسعر الدقيقة بعد السماح، `NET_OF_LATENESS`، سقف خصم الحضور اليومي يوم واحد، المرونة مقفلة ما لم تُضبط. |
| D6 | نصف اليوم يبقى على SRS FX-08.3. |
| D7 | لو خصم التأخير مقفول لا تُطرح دقائق التأخير من النقص. |
| D8 | معادلات `rateBase` مؤجلة؛ العملات SAR وEGP. |
| D9 | نافذة المرونة تقبل `flexFrom/flexTo` صريحين (وصيغة الطول تبقى مقبولة). |
| D10 | حد أقصى قسط متأخر واحد لكل مسير إضافة للقسط الحالي (إعداد). |
| D11 | الإضافي خارج النوافذ وفق SRS LOT-14 (مغلق) مع نافذة افتراضية مبذورة «بعد نهاية الوردية» ليستمر الكشف القائم. |
| D12 | أنواع الطلبات بلا معالج حقيقي تُوسم «تسجيل فقط» في الـAPI والواجهة؛ التظلم والإبلاغ يبقيان تسجيلًا سريًا. |
| D13 | `engine_mode` لكل مسير: SHADOW افتراضيًا (المصروف = الحساب القديم، ومحرك السياسة يُحسب بجانبه بتقرير تكافؤ لكل موظف)؛ حامل `payroll.approve` يحوّل المسير إلى POLICY لو تقرير التكافؤ صفر أو كل فرق له سبب مسجل. |

### 2-أ. مفتاح كل قرار وحالته (مسار path-core، الجزء B2 — الخطوتان 12 و15)

- **الترحيل:** `docs/migrations/payroll/20260914_017_b2_policy_manage_decision_settings.sql` — يضيف مفاتيح القرارات الغائبة فقط (القيمة القائمة لا تُلمس) ويضيف `payroll.policy.manage` لدور `hr_manager`. طُبق على `hr_system` في 2026-09-14 14:13:52 UTC (سجل `docs/migrations/runs/20260914141352_apply_hr_system.json`، بصمة الملف `dd619d2b…`).
- **البذرة:** `PAYROLL_DECISION_CONFIG_SEED` في `api/src/payroll/payroll-decision-settings.ts` مضافة إلى `configSeed`.
- **التحقق:** `payrollDecisionConfigError` داخل `PATCH /settings/config` (`api/src/settings/settings.controller.ts`). القيم المغلقة لا تقبل إلا الخيار المنفذ فعلًا.
- **الشاشة:** «الإعدادات ← سياسات النظام»: مجموعة «نقص ساعات العمل والمرونة»، و«العمل الإضافي»، و«أقساط السلف وحماية الصافي»، و«المسير ونهاية الخدمة».
- **الاختبارات:** `api/test/payroll-decision-settings.test.cjs` و`api/test/payroll-decision-settings.integration.cjs`.

| الرمز | مفتاح الإعداد = القيمة المختارة | التحقق في `PATCH /settings/config` | الحالة في الكود |
|---|---|---|---|
| D1 | `payroll.early_leave_deduction_enabled` = `true` | `true` أو `false` | **منفذ.** `attendance-deductions.ts` (`earlyLeaveEnabled`): على الوردية الثابتة يمثل النقص بعد طرح التأخير الخروجَ المبكر، ويُخصم بسعر الدقيقة بعد السماحية. `false` يُسقطه للوردية الثابتة فقط. المسير يقرأ المفتاح في `payroll.service.ts`، واللقطات القديمة بلا الحقل تُعامل كمفعّل. |
| D2 | `payroll.daily_hours` = `8`، و`payroll.hourly_rate_basis` = `DAILY_HOURS` | الساعات أكبر من صفر حتى 24 بمنزلتين؛ الأساس `DAILY_HOURS` فقط | **قائم.** سعر الساعة في `payroll.service.ts` = سعر اليوم ÷ `daily_hours`، وفي `overtime-financial.ts` = الأجر ÷ `monthly_days` ÷ `daily_hours`. ساعات الوردية غير منفذة، فلا تُقبل. |
| D3 | `payroll.monthly_days` = `30`، و`payroll.day_rate_basis` = `MONTHLY_FIXED_COMPONENTS_30` | `monthly_days` = `30` فقط (قبول الخطوة 12)؛ الأساس قيمة واحدة | **قائم في المسير.** سعر اليوم = مجموع المكونات الستة ÷ 30 (غياب وتأخير ونقص). نهاية الخدمة تُحسب بالأشهر من الأجر الكامل (`eos.ts`). |
| D4 | `payroll.policy.rounding_mode` = `HALF_UP`، و`payroll.policy.rounding_scale` = `2` | حدود إعدادات السياسة (`validatePayrollPolicyDefaultConfig`) | **جزئي.** نسخ السياسة ومحركها بكسور دقيقة وتقريب مُعلن، والمسير يستخدم `roundPayrollMoney` (`payroll-money.ts`). `eos.ts:12` ما زال بدالة `round2` خاصة، وتوحيدها في الخطوة 22 (CQ-04). |
| D5 | `payroll.shortfall_enabled` = `true`، `payroll.shortfall_mode` = `MINUTES`، `payroll.shortfall_value` = `1`، `attendance.flex.shortfall_grace_minutes` = `10`، `payroll.attendance_overlap_policy` = `NET_OF_LATENESS`، `payroll.attendance_daily_cap_days` = `1`، وبقية `attendance.flex.*` بقيم البذرة | قوائم مغلقة؛ دقائق صحيحة حتى 1440؛ السقف من 0 إلى 31 بمنزلتين؛ قيمة النقص من 0 إلى 1000 | **منفذ ومسجل في `hr_system`.** المرونة لا يفعّلها مفتاح عام؛ تُفعّل صراحةً في تعريف الوردية أو جدول العمل (`flexEnabled`)، والفارغ لا يفعّلها. |
| D6 | لا مفتاح (قاعدة كود) | — | **قائم.** `attendance-flex-calculator.ts` (`halfLeaveWindows`) بقاعدة SRS FX-08.3. |
| D7 | لا مفتاح مستقل؛ يتبع `payroll.late_deduction_enabled` مع `payroll.attendance_overlap_policy` | — | **منفذ.** `attendance-deductions.ts` لا يطرح دقائق التأخير من النقص إلا إذا كان خصم التأخير مفعّلًا. لا أثر رقمي على `hr_system` اليوم لأن خصم التأخير مفعّل. |
| D8 | `payroll.policy.rate_base` = `GROSS` (أو `BASIC`)، و`system.currency` = `SAR` | `rate_base` من قائمة السياسة؛ العملة `SAR` أو `EGP` فقط | **قائم.** معادلات `rateBase` مؤجلة؛ الاختيار بين القيمتين فقط. |
| D9 | لا مفتاح (حقول تعريف الوردية) | — | **غير منفذ في هذا الجزء.** لا يوجد `flexFrom/flexTo` في الكود وقت كتابة هذا القسم؛ صيغة طول النافذة هي المقبولة. |
| D10 | `payroll.loan_catchup_max_overdue` = `1` | عدد صحيح من 0 إلى 120 | **منفذ.** `buildPayrollInstallmentPlan` (`payroll-installment-ledger.ts`) يخصم أقساط الشهر الحالي وأقدم N قسط متأخر، ويبقى الباقي مستحقًا بلا حجز (`catchUpDeferredIds` في خطة المسير). الحد يُثبت في الخطة؛ الخطط المحسوبة قبل القرار بلا الحقل تبقى بلا حد حتى «تحديث سياسة الأقساط» عند إعادة الحساب. |
| D11 | `overtime.default_window` = `AFTER_SHIFT_END`، و`overtime.outside_window_policy` = `CLOSED` | قيمتان مغلقتان | **مسجل ويطابق الكشف القائم.** الكشف بعد نهاية الوردية فقط، والحضور المبكر يحكمه `overtime.allow_early_overtime` (= `false`). لا يوجد محرر نوافذ متعددة بعد. |
| D12 | لا مفتاح | — | خارج هذا الجزء: مسار أنواع الطلبات (ترحيل `20260914_016_r1_record_only_request_types.sql`). |
| D13 | لا مفتاح (عمود على المسير) | — | **جزئي (خصومات الحضور، الخطوة 28).** كل حساب مسير (`calculateDefined`) يحسب محرك السياسة بوضع SHADOW بجانب القديم لكل موظف: مزودات المصادر الحية (الجدول المؤرخ والخدمة والبصمات الخام، ومنها نسب الوردية الليلية ليوم بدايتها) ← منفذ البنود `executePayrollComponentsWithSources` بسياسة افتراضية تقلّد إعدادات المسير القديم (شرائح التأخير، النقص صافيًا من التأخير مع سماح اليوم، السقف اليومي، معامل الغياب) ← تكافؤ يومي ومجموع الفترة مع سبب لكل فرق. النتيجة في `breakdown.policyShadow` لكل بند (`api/src/payroll/payroll-shadow-attendance.ts`) وتظهر في شاشة المسير «ظل السياسة». المصروف دائمًا القديم؛ `switchEligible` = تطابق كامل. **المتبقي للخطوة 20:** عمود `engine_mode` على المسير، وتحويل POLICY بصلاحية `payroll.approve`، وبقية البنود (الراتب والإضافي والإجازة بلا أجر والأقساط والقيود). |

### 2-ب. الخطوة 15 — مجموعات سياسات الرواتب ونشرها

- **الصلاحية:** `payroll.policy.manage` مستقلة (`api/src/auth/permissions.ts`)، ومضافة لحزمة `hr_manager` وللدور القائم بالترحيل 017. `payroll.calculate` وحدها تقرأ وتجرب المعاينات فقط؛ لا إنشاء ولا تعديل ولا نسخ ولا أرشفة ولا نشر.
- **المسارات:** الإنشاء، وتعديل الهوية، وتعديل النسخة ونسخها، والأرشفة، وتعريف البنود، وترتيب التحصيل، والنشر كلها تتطلب `payroll.policy.manage` مع نطاق الفرع.
- **مراجعة النشر** (`GET /payroll/policies/:id/versions/:versionId/publish-check`، قراءة فقط) تعرض المشكلات المانعة:
  - إعدادات ناقصة.
  - سريان لا يبدأ ببداية فترة مسير أو لا ينتهي بنهايتها، مع تاريخ مقترح.
  - تعريف بنود غير صالح، أو ترتيب تحصيل ناقص لنسخة بها خصومات.
  - تداخل مع نسخة منشورة لا يمكن إغلاقه.
  - مسير معتمد أو مصروف بالسياسة داخل السريان.

  وتعرض أيضًا التحذيرات، وأول ثلاث فترات، والنسخ التي ستُغلق.
- **النشر** (`POST .../publish`): الحالة `ACTIVE`، وتُسجل `publishedAt` و`publishedBy` و`frozenAt`، وبصمة محتوى SHA-256 وحدث `PUBLISHED`.
  - أي تعديل على نسخة منشورة ينشئ مسودة جديدة، والأصل لا يتغير.
  - نسخة منشورة سابقة تبدأ قبل الجديدة تُغلق عند اليوم السابق لبدايتها (حدث `VERSION_SUPERSEDED`).
- **الفترات:** نفس قاعدة الخطوة 14. نهاية الشهر M = الأصغر من (يوم البداية − 1، آخر أيام M)، والبداية = نهاية الفترة السابقة + يوم.
- **الشاشة:** «الرواتب ← سياسات الرواتب».
  - زر «مجموعة سياسة جديدة»: الاسم، والكود، وبداية السريان المقترحة، والدورة، والمعدلات، وحماية الصافي.
  - لوحة النسخة: تعديل الإعدادات والدورة، ومسودة جديدة منها، ومراجعة ونشر.
- **الاختبارات:** `api/test/payroll-policy-publish.integration.cjs` (القبول: «مجموعة القاهرة» بدورة 23 ← 22 تُنشأ وتُنشر وتتجمد، ومستخدم `payroll.calculate` وحده يُرفض)، و`api/test/payroll-policy-publish.test.cjs`، و`api/test/payroll-policy-editor-ui.test.cjs`.

#### 2-ب-1. إصلاحات مراجعة الخطوة 15 (ترحيل 022)

- **يوم النهاية الثابت (FIXED_DAY):** الفترة الشهرية المتصلة يحددها يوم البداية وحده.
  - لذلك يُقبل يوم النهاية الثابت فقط إذا كان اليوم السابق للبداية، أو 31 (آخر الشهر) لبداية 1. حدوده هي حدود الاشتقاق نفسها، ومنها قص 29 و30 و31 على الشهر القصير.
  - أي يوم آخر يُرفض في التحقق من الإعدادات، وفي الإنشاء، وفي `PATCH` النسخة، وفي معاينة الحقائق. أمثلة: 23→23 تداخل يوم كل شهر، و1→15 و23→25 فجوات.
  - يُرفض أيضًا في `PATCH /settings/config` إذا فصل مفتاح واحد يوم النهاية عن البداية. `payroll.cycle_start_day` يبقى حرًا ما دامت النهاية مشتقة.
  - القاعدة في `payrollCycleSettingsIssue` (`api/src/payroll/payroll-period.ts`)، ولها مرآة في الواجهة `policyCycleIssue`.
- **مصدر واحد للفترات:** `payrollPolicyPeriodRanges` و`payrollPolicyPeriodBounds` و`payrollPolicyPeriodOfDate` في `payroll-period.ts` تقرأ نوع الفترة ووضع النهاية ويومها.
  - مراجعة النشر (`payroll-policy-publish.ts`) ومعاينة الحقائق (`verifyMonthlyCycle`) تستخدمانها حرفيًا، فالفترات المعروضة والمجمدة عند النشر هي ما ستستخدمه المسيرات.
  - القص المستقل القديم في معاينة الحقائق (مثلًا مسير مارس لدورة 31 يبدأ 28/2) لم يعد مقبولًا.
- **مراجعة النشر:** مشكلتان مانعتان جديدتان:
  - `POLICY_CYCLE_INVALID`.
  - `POLICY_CYCLE_NOT_CONTIGUOUS`: فحص تجاور 48 شهرًا من بداية السريان. التفاصيل تذكر أول الفجوات والتداخلات بتواريخها وشهريها، وتظهر حتى لصف تاريخي مخزن بدورة مكسورة.
- **الإيقاف بنسخة أحدث لا يعدّل النسخة المنشورة:**
  - نهاية السريان الفعلية `effectiveUntil` مشتقة (`payrollPolicyEffectiveEnds`): النسخة المنشورة التالية تبدأ بعدها فتوقفها في اليوم السابق لبدايتها.
  - صف النسخة السابقة وختمها لا يتغيران. يُسجل حدث `VERSION_SUPERSEDED` على النسخة السابقة وفيه `versionRowChanged: false`.
  - فحص التداخل يقارن بالنهاية الفعلية. قراءة المصادر الحية تستخدمها أيضًا.
- **ختم المحتوى:** جدول `payroll_policy_version_seals` فيه صف لكل نسخة منشورة: `contentHash` و`sealVersion` و`sealedBy` و`sealedAt` بتوقيت UTC.
  - يظهر في عرض النسخة (`contentHash`).
  - `publish-check` يعيد حساب البصمة ويقارنها (`integrity`)، ويحذر بـ`POLICY_CONTENT_SEAL_MISMATCH` أو `POLICY_CONTENT_SEAL_MISSING`.
  - الختم في جدول مستقل حتى يبقى مخطط `payroll_policy_versions` كما تثبته ترحيلات 006–010 واختباراتها.
- **التجميد في القاعدة (مشغلات 022):**
  - لا يتغير في النسخة المنشورة أو المجمدة: الإعدادات، والدورة، والسريان، وبيانات النشر، والبيانات الوصفية.
  - الانتقال الوحيد المسموح لحالتها هو `ACTIVE → ARCHIVED`.
  - لا تُضاف معاملات ولا مجموعات شرائح ولا شرائح ولا بنود لنسخة مجمدة، ولا يُعدل الختم.
  - المُرحّل يمنع كلمة الحذف في الملفات، فمنع حذف صفوف البنود ليس مشغلًا. حذف صف النسخة يمنعه مفتاح حدث النشر، وأي حذف للبنود يظهر كعدم تطابق مع الختم.
- **الوقت:** `publishedAt` و`frozenAt` و`sealedAt` تُخزن بساعة UTC مثل `createdAt/updatedAt`.
  - محوّل العمود `utcDateTime` يحافظ على اللحظة نفسها في الـAPI.
  - ترحيل 022 أعاد تمثيل وقت النسخة المنشورة القائمة (17:22:37 محلي ← 14:22:37 UTC)، بفرق محسوب من حدث نشرها.
- **الكود:** اختياري في الإنشاء. غيابه يولّد `PS-YYYYMMDD-NN` فريدًا تحت قفل، والكود المكرر صراحة يُرفض بـ`POLICY_CODE_EXISTS` برسالة واضحة.
- **محرر ترتيب التحصيل:** التعديل يتبع `capabilities.canEdit` من الخادم (`payroll.policy.manage` + نطاق الفرع) ولا يشترط `payroll.calculate`.
- **قفل الفترات:** `POLICY_PERIOD_LOCKED` يشمل المسيرات المرتبطة بالسياسة (`policyId`).
  - إذا وُجد عمود `payroll_runs.policyVersionId` (الخطوة 16/19)، يشمل أيضًا المسيرات المرتبطة بأي نسخة من نسخ السياسة.

## 3. ترحيلا ما قبل الرواتب 005 و006 (الخطوة 4 أ و ب)

**ما يفعلانه على `hr_system`** (من سجل التطبيق `docs/migrations/runs/20260914121952_apply_hr_system.json`):
- 005: `employee_status_history` 46 → 46 صفًا، الصفوف بلا `changeType` 46 → 0، صف بنكي واحد، 0 قيمة IBAN مكشوفة بعده (تقنيع)، ونوع عمل قديم واحد (`fulltime`) → `full_time`.
- 006: `requests` 341 → 341، `typeCode='LEAVE'` 80 → 130 مع `definitionCode` على الـ130 (إعادة كتابة أسماء طلبات الإجازة)، وإعادة تسمية أعمدة `leaves.leaveType` و`leave_types.balanceSource` بقيمها.

**الحالة:** مطبقان على `hr_system` في 2026-09-14 12:19:47–51 UTC ضمن تطبيق مسار قاعدة البيانات.

**الموافقة:** لا توجد موافقة مكتوبة من المالك خاصة بتقنيع IBAN وتحويل `oldStatus/newStatus` (005) أو إعادة كتابة 130 طلب إجازة (006) قبل التطبيق. الأساس الوحيد المسجل هو قاعدة المالك اليوم (البيانات بيانات اختبار، واستكمال الرواتب اليوم). **هذا البند ينتظر تأكيد المالك صراحةً؛ لا يوصف بأنه «معتمد من المالك».**

**الحفظ:** الفحوص الأربعة عشر (SHA-256) نجحت: نقطة الأساس ↔ نسختها بعد الترحيل، ونقطة الأساس ↔ `hr_system` الحالية لكل صفوف الأساس (`node api/scripts/db-data-invariants.cjs`، السجل `docs/migrations/runs/20260914132255_invariants_hr_system.json`): هوية وتواريخ سجل الحالة، الأسباب مع التقنيع المقصود، القيم البنكية، أنواع العمل، قيم الهوية السبعة المعاد تسميتها، والحقول المحفوظة للطلبات وأنواعها وسلاسلها وخطواتها واعتماداتها.

**الرجوع لو رفض المالك:** القيم القديمة موجودة فقط في نقطة الأساس `D:/projects/hr_system_backups/pre-payroll-2026-09-14/hr_system_pre_payroll_20260914.bak` (sha256 `ccb5d8ca71238bdd457482ca5ab00b5598b0e2628f87c4f112127263f3a836d7`). الرجوع دون فقد ما كُتب بعدها: استعادتها في قاعدة مؤقتة ثم ترحيل جديد يعيد الأعمدة المعاد كتابتها للصفوف المعنية فقط (46 صف سجل حالة، موظف واحد، 130 طلبًا). لم يُنفذ.

## 4. نافذة 11:27 → 12:19 UTC قبل التطبيق الأول (الخطوة 11)

- التطبيق الأول (12:19) اعتمد على نسخة 11:27 وليس نسخة جديدة لحظة التجميد.
- سجل معاملات `hr_system` لا يغطي النافذة: القاعدة FULL بلا أي نسخة سجل (`last_log_backup_lsn` NULL)، فالسجل يُقتطع عند كل checkpoint (أقدم معاملة في `fn_dblog` كانت 12:45).
- **المثبت:** الجداول الـ62 القائمة بنفس أعداد الصفوف، والثوابت الأربعة عشر على `hr_system` تطابق نقطة الأساس لكل صفوفها. أي كتابة في النافذة غيّرت هذه الحقول كانت ستظهر.
- **غير القابل للإثبات:** كتابة في النافذة على الحقول التي أعاد 005/006 كتابتها بالكامل (`requests.typeCode/definitionCode/payload` وأعمدة سجل الحالة المحوّلة) ثم طمسها الترحيل. لم يُعثر على كاتب معروف (الـAPI كان موقوفًا وقت التطبيق)، لكن لا يوجد دليل سجل.
- **المعالجة للمستقبل:** المُرحّل يأخذ الآن نسخة `COPY_ONLY, CHECKSUM` لحظة التجميد في كل `apply --company`، ينسخها خارج الـvolume ويتحقق منها، ويرفض أي نسخة أقدم من آخر ترحيل أو آخر كتابة أو إعادة تشغيل للخادم. أول تطبيق بها: `D:/projects/hr_system_backups/freeze/hr_system_freeze_20260914132438_f7091168.bak` (انتهت 13:24:38 UTC، sha256 `b3c0a2caabd3cc28…` الكامل في `docs/migrations/runs/20260914132441_apply_hr_system.json`) — نقطة الرجوع قبل 014 و015.

## 5. ترحيل 013 — النسخ المؤرخة للتقويم والفرع وجدول العمل

- **القرار:** مقبول كتثبيت للقيم القائمة بتاريخ سريان 2026-04-23 (بداية الدورة التي تضم أقدم يوم حضور/مسير). أي تغيير قيمة لاحق يمر من `beginCalendarChange` أو شاشة التقويم.
- **تخطي أقفال الشاشات:** 013 استدعى `finishCalendarChange` مباشرة، فلم يمر `assertCalendarPeriodOpen` (تصفية مقفلة ومسير معتمد/مصروف). عشر حالات تصفية SETTLED/CLOSED داخل النطاق: 2 (موظف 19)، 3 (30)، 4 (32)، 5 (34)، 6 (9)، 11 (42)، 12 (153)، 16 (185)، 17 (186)، 18 (205).
- **الإثبات أن لا قيمة تغيرت داخل فترة مقفلة** (`datedVersionLockProof`): 190 نسخة مؤرخة (عام 1، فروع 9، `EMPLOYEE_ORG` 180) = قيمها السابقة المحفوظة في النسخة 0 حرفيًا، و0 مسير معتمد أو مصروف، والحالات العشر كلها نسختها مطابقة. `saveEmployeeAttendanceRule` (إسناد الجدول) مر بفحص الفترة المفتوحة.
- **16 موظفًا** تعيينهم بعد 2026-04-23 (آخرهم 2027-06-01) لهم نسخة فرع من 2026-04-23. النسخة تثبت الفرع فقط ولا تنشئ أيام حضور؛ التغطية المالية قبل التعيين محكومة بقاعدة المالك (القسم 1) لا بهذه النسخة.
- **موظف بلا فرع:** مسار دفاعي؛ `employees.branchId` غير قابل لـNULL في `hr_system` (0 موظفين). مغطى باختبار `api/test/db-dated-calendar-versions.test.cjs`.

## 6. البيئة وحراسة القاعدة (الخطوات 1 و2 و5)

- **مفتاح جهاز البصمة:** ترحيل 014 استبدل القيمة المنشورة `zk-device-key-change-me` (23 حرفًا) بمفتاح عشوائي 48 حرفًا دون طباعته. يقرؤه مدير النظام من «الإعدادات ← الحضور» ويضبطه على الجهاز رقم 1. البذر صار فارغًا (الاستقبال موقوف حتى ضبط مفتاح قوي).
- **الويب:** `npm run dev` مربوط على 127.0.0.1. Chrome الخاص بـpuppeteer في ملف المستخدم Kareem؛ تشغيل الـAPI بمستخدم آخر يحتاج `PUPPETEER_EXECUTABLE_PATH`.
- **البذر والتصفير:** `npm run seed` يرفض أي قاعدة غير مؤقتة إلا بـ`--fresh-install` على قاعدة غير موجودة أو بلا جداول، و`npm run reset` يرفض أي قاعدة غير مؤقتة بلا تجاوز.
- **المُرحّل:** فحص نصي لـSQL وسكربتات `.cjs`، وفحص فعلي داخل المعاملة (لا إسقاط ولا تضييق ولا حذف صفوف قائمة ولا تعطيل ولا إطفاء XACT_ABORT)، ونسخة لحظة التجميد. التفاصيل في `docs/migrations/README.md`.

## 7. الخطوة 9 — تنظيف بيانات التشغيل، بندًا بندًا

الإثبات: `node api/scripts/db-step9-check.cjs` (18/18: عشرة قرارات بحالتها الجديدة، وثمانية بنود مسجلة بلا تغيير). الترحيل: `docs/migrations/payroll/20260914_015_db_step9_data_decisions.sql`.

> **تحديث مسار R2:** بنود «اسم الشركة والمسميات» و«بيانات العقود» و«أرضية الصافي وسقف الخصم» و«الموظفان 1 و154» وتأكيد قواعد HRC-10 قرارها وتنفيذها في القسم 13 (ترحيلا `20260914_021_r2_step9_data_completion.sql` ثم `20260914_024_r2b_step9_placeholder_restore.sql` الذي أعاد القيم المؤقتة والأرضية/السقف وبيانات فحص الموظف 1 إلى أصلها). صفوف الجدول أدناه تبقى كسجل لحالة 015. الفحص الحالي: 19/19.

| البند | القرار | الإجراء | الحالة بعد القرار |
|---|---|---|---|
| شريحة التأخير 10 (0 → بلا حد، ربع يوم) تُطابَق قبل 6 و8 | تعطيلها (الصف باقٍ) | 015 | 10 معطلة؛ 6 (1–60 ربع يوم) و8 (61–120 نصف يوم) فعالتان؛ ما فوق 120 دقيقة بخصم الدقيقة |
| 7 قيود إضافي مسبق معتمدة بلا ساعات (توقف المسير) | إعادة تسعير بقاعدة القيود المسبقة الأخرى: المستحق = الأقل من المطلوب والفعلي من يوم الحضور؛ بلا دليل = 0 | 015 + حدث `LEGACY_HOURS_RESOLVED` لكل قيد | 2→0، 30→0، 31→0، 34→2 ساعة، 38→0، 39→0، 51→0؛ 0 قيد معتمد بلا ساعات |
| 13 قيد `BIOMETRIC_DETECTED` SUBMITTED | 19 و26 طلباهما مرفوضان ← يُغلقان بنفس الحالة؛ الأحد عشر الباقية طلباتها UNDER_REVIEW فتبقى كما هي | 015 + حدث `LEGACY_STATUS_ALIGNED` | 19 و26 REJECTED؛ تحذير «الإضافي المعلق» في المسير 6: قبل [60,61,26,45,43,44] وبعد [60,61,45,43,44] |
| القيد المالي 1 (غرامة اختبار 500، APPLIED بلا مسير) | إلغاؤه لا إرجاعه PENDING (PENDING يخصم 500 في أول مسير قادم) | 015 | CANCELLED، لا قيد APPLIED بلا مسير |
| الطلبات 292 و295 (عهدة) و303 (إجازة) معتمدة بلا تنفيذ | إعادة التنفيذ أولًا عبر الـAPI، ثم الإغلاق بالرفض بسبب مكتوب لو رفضها المعالج | `POST /requests/:id/retry-execution` ثم `reject-execution` (مستخدم 1) | رُفض التنفيذ: 292 الأصل له عهدة مفتوحة، 295 الأصل لم يعد في المخزون، 303 إجازة معتمدة متداخلة (2026-11-16 → 18). الثلاثة REJECTED بسجل اعتماد REJECTED وسببه |
| دور الاختبار `pr_test_42103` (payroll.calculate بلا مستخدمين) | تعطيله (الصف باقٍ) | 015 | معطل، 0 مستخدمين |
| الموظفان 1 و154 بلا راتب أساسي | بلا تغيير (لا يُخترع راتب) | — | يجب أن تستبعدهما معاينة العضوية بـ`NO_SALARY_DEFINED` (الخطوة 17) |
| اسم الشركة فارغ؛ 148 من 167 نشطًا بلا مسمى؛ 20 بلا تاريخ تعيين | بلا تغيير (بيانات المالك) | — | الخطابات ترفض برسالة عربية حتى إدخال اسم الشركة؛ مستندات HR تعمل بقوالب لا تستخدم بيانات الشركة |
| 162 من 167 نشطًا بلا نوع/نهاية عقد | بلا تغيير؛ قرار تعطيل طلب تجديد العقد ضمن حالة الأنواع الـ16 (الخطوة 7) | — | مسجل |
| أرضية الصافي وسقف الخصم | لا قيمة مخترعة؛ `null` حتى قرار المالك/مسار السلف (D10) | — | 18 قسطًا متأخرًا لسبعة موظفين، أقدمها 2026-08-01 |
| منح `attendance_exemption.*` لأدوار (الخطوة 24، مسار C1) | بعد إغلاق 8-أ (`SUPER_ADMIN_ONLY_GRANTS` تضم `approve` و`approve_executive`): `hr_manager` = view/manage/approve، `branch_manager` = view/manage (طلب لموظفي فرعه)، والاعتماد التنفيذي لمدير النظام فقط. فصل المهام في الخدمة لكل الأدوار: منشئ الطلب لا يعتمده ولا يرفضه (يلغيه فقط)، ومعتمد خطوة الموارد البشرية لا يتخذ القرار التنفيذي | ترحيل `20260914_016_c1_attendance_exemption_roles.sql` + `ROLE_PRESETS` | مطبق على hr_system (14:13 UTC)؛ رُفع `tokenVersion` لمستخدمي الدورين كما تفعل شاشة الأدوار |
| منح `payroll.reopen/cancel` لأدوار | مؤجل حتى تُغلق الخطوة 8-أ (`SUPER_ADMIN_ONLY_GRANTS` كانت 3 مفاتيح وقت الكتابة) | — | لا دور غير super_admin يحملها |
| النقل المجدول #3 (عهدة قائمة) و#4/#5 (مكرران) | ضمن الخطوة 7 | سجل الإقلاع صار WARN بالسبب بدل ERROR | #3 و#4 و#5 SCHEDULED |

## 8. الخطوة 10 — فحص التشغيل على `hr_system` بعد التحويل

- صحة الخادم، توكن موقّع لمستخدم حقيقي، ملف موظف (نطاق: 10 لصاحبه 200 و19 → 403)، طلب الإجازة #398 (صاحبه 200 وغيره 403)، يوم حضور 2026-09-13 (162 صفًا؛ الموظف 403)، المسيرات الـ16 (كلها 200)، السلف (`/loans` 9)، PDF خطاب بمعاينة قالب منشور (151,164 بايت).
- **PDF عقد وPDF مستند HR** من `hr-documents`: قالبا فحص (عقد #1، شهادة #2) نُشرا ثم أُصدرا للموظف 10 بواسطة مدير الموارد البشرية ثم عُطّل القالبان: مستند #1 (عقد، ملف 20، 110,656 بايت) و#2 (شهادة، ملف 21، 108,030 بايت)، ينزلان لـHR ولصاحبهما و403 لغيرهما.
- الخطابات القديمة الاثنا عشر بلا ملف مخزن (`generatedPdfRef` قديم): تنزيلها 404 برسالة عربية؛ إعادة توليدها قرار بيانات منفصل.
- قائمة الفحص للشاشات: `docs/STEP10_SCREEN_CHECKLIST_2026-09-14.md`.
- **كتابات الإقلاع منذ نقطة الأساس** (أسماء فقط): `requests_config` 45 → 85 (40 مفتاحًا: 7 مرونة، 12 إضافي، 1 سلف، 20 رواتب/سياسة — مذكورة في سجل الثوابت)، `doc_types` 0 → 12 (كل `DOC_TYPES_SEED`). الأرقام المتوقعة في الخطة (+26 و+13) قيست على كود 09-11 قبل إضافة مفاتيح الدفعات 1–2.

## 9. بيانات باقية من الفحوص

الطلب #398 (CANCELLED)، قالبا مستندات HR #1 و#2 (معطلان)، المستندان المصدران #1 و#2 وملفاهما 20 و21 وسجلا مستندات الموظف 21 و22، تسعة أحداث إضافي من 015، ونسخ `freeze/`. لم يُحذف شيء.

فحص الخطوة 24 (مسار C1): استثناء الحضور #1 للموظف 36 — أنشأه المستخدم 12 (hr_manager)، ورُفض اعتماده منه (`EXEMPT-SOD-CREATOR`)، واعتمده المستخدم 1، ثم أُنهي من تاريخ بدايته 2026-08-23 فلا يغطي أي يوم (الحالة ENDED)، ومعه أحداثه الثلاثة (CREATED، HR_APPROVED، TERMINATED). السجل باقٍ للتدقيق؛ لا حذف.

## 10. الخطوة 7 — انحدارات الشغل اليومي (مسار R1)

### D12 — حالة الأنواع الستة عشر (القرار في الكود والبيانات)

القرار لكل نوع **«تسجيل فقط»**: الطلب المعتمد نفسه هو السجل الدائم، بلا أثر آلي على الملف أو الراتب أو العهدة؛ **لا نوع معطّل**. الجدول في الكود `RECORD_ONLY_REQUEST_TYPES` (`api/src/requests/destinations.service.ts`)، والبذر `handler: 'none'`، وترحيل `20260914_016_r1_record_only_request_types.sql` يحوّل الوجهة القديمة غير المبنية إلى `none` بشرط القيمة القديمة بالضبط (وجهة اختارها المالك لا تُمس). الكتالوج وبانِي الطلبات يرجعان `executionMode = RECORD_ONLY` و`executionLabel = «تسجيل فقط»`، والمنتقي يعرض الشارة بدل إخفاء النوع.

| النوع | الوجهة القديمة | القرار | سري | الإجراء الفعلي بعد الاعتماد |
|---|---|---|---|---|
| ACCESS_REQUEST | access_register | تسجيل فقط | لا | تقنية المعلومات تمنح الصلاحية يدويًا |
| APPRAISAL_OBJECTION | appraisal_register | تسجيل فقط | لا | لا موديول تقييم — سجل للمراجعة |
| CERT_REIMBURSEMENT | training_expense | تسجيل فقط | لا | لا صرف آلي — أي مبلغ بقيد مالي منفصل |
| CONFERENCE | training_register | تسجيل فقط | لا | سجل متابعة |
| DEPENDENTS_UPDATE | employee_dependents | تسجيل فقط | لا | الموارد البشرية تحدّث الملف يدويًا |
| DOCUMENT_RENEWAL | document_vault | تسجيل فقط | لا | المستند يُرفع من ملف الموظف |
| EDUCATION_ASSISTANCE | training_register | تسجيل فقط | لا | لا صرف آلي |
| FACILITY_CARD | facilities_register | تسجيل فقط | لا | الإدارة تصدر الكارت يدويًا |
| GRIEVANCE | er_case | تسجيل فقط | نعم | تسجيل سري لأطرافه فقط |
| HR_MEETING | meetings_register | تسجيل فقط | لا | الموعد خارج النظام |
| IT_EQUIPMENT | it_assets | تسجيل فقط | لا | التسليم بطلب عهدة منفصل |
| PENALTY_OBJECTION | er_case | تسجيل فقط | نعم | لا يلغي الجزاء آليًا |
| SECONDMENT | assignments_register | تسجيل فقط | لا | لا تغيير فرع/فريق آلي — النقل بطلب نقل |
| SUGGESTION | suggestions_register | تسجيل فقط | لا | سجل اقتراحات |
| TRAINING_REQUEST | training_register | تسجيل فقط | لا | لا موديول تدريب |
| WHISTLEBLOWING | er_case_anonymous | تسجيل فقط | نعم | تسجيل سري لأطرافه فقط |

طلب تجديد العقد (بند الخطوة 9) ليس ضمن الستة عشر: له معالج حقيقي (`contracts_register`) ويبقى مفعّلًا برسائله العربية حتى تُستكمل بيانات العقود.

### النقل المجدول

- المكرر لنفس الموظف: الأول بالترتيب (الأقدم تسجيلًا) وحده يبقى للتنفيذ، والباقي `transfers.status = CANCELLED` وطلبه `CANCELLED` مع سطر تدقيق نظامي (`approverId = 0`, `CANCELLED`) يحمل السبب؛ يظهر في «لوج النقل» (`statusReason`) ولصاحب الطلب ولحامل `employees.edit` في نطاقه.
- تعذّر التنفيذ (عهدة قائمة، فريق غير صالح…): سطر `EXECUTION_FAILED` على الطلب مرة لكل سبب مختلف، ويبقى النقل مجدولًا ويُعاد يوميًا؛ تنبيه الموارد البشرية قائم ما دام الطلب بانتظار التنفيذ. نفس التسجيل للتغييرات الوظيفية المجدولة.
- على `hr_system` (إقلاع الـAPI 14:04:44 UTC، مهمة اللحاق نفسها لا ترحيل): النقل #5 (الطلب #287) `CANCELLED` بسبب ظاهر يشير للنقل #4؛ النقل #4 (الطلب #286) يبقى `SCHEDULED` وسيُنفَّذ في تاريخ سريانه 2026-12-01؛ النقل #3 (الطلب #250) مسجل عليه `EXECUTION_FAILED` بسبب العهدة المفتوحة ويبقى مجدولًا.

### بقية البنود

- الإجازة العابرة للسنة: الخصم عند التنفيذ بتاريخ البوابة نفسه (`balanceGateDate`: سنة البداية = تاريخ البداية، وما بعدها = 31 ديسمبر)، وصلاحية الطبقة الافتتاحية تُقاس على أول يوم فعلي للإجازة في السنة.
- الخطاب: اسم الشركة والمسمى وتاريخ التعيين تُفحص عند التقديم (إنشاء+تقديم وتقديم مسودة) بنفس دالة التنفيذ (`letter-issuance.ts`)؛ التقديم المرفوض لا يترك صفًا.
- التصفية (HRC-07): الاعتماد يرفض 409 لو بند المكافأة التلقائي المحفوظ لا يطابق `computeEos` بالسياسة الحالية (غير موجود، مكرر، معدّل يدويًا، أو تسمية آلية غُيّرت)؛ العلاج «إعادة التوليد» ثم بند تسوية يدوي لأي فرق متفق عليه. الحالة #15 تُرفض الآن حتى إعادة توليد بنودها.
- الدرجة الوظيفية (HRC-08): رقم غير موجود 400 في الإنشاء والتعديل؛ درجة معطلة لا تُسند من جديد، والدرجة الحالية المعطلة لا تمنع تعديل باقي الملف.
- `GET /employees` (HRC-09): نسخ قواعد الحضور لموظفي النطاق باستعلام واحد (`pickAttendanceRule` بنفس قاعدة التفاصيل).

## 11. منح الأدوار بأقل امتياز بعد 8-أ (الخطوة 9، البند الأخير — مسار الأمان SEC2)

**الشرط محقق:** الخطوة 8-أ مغلقة. `SUPER_ADMIN_ONLY_GRANTS` (`api/src/auth/permissions.ts`) تضم `payroll.reopen` و`payroll.cancel` و`overtime.adjust` و`attendance_exemption.approve` و`attendance_exemption.approve_executive`. لذلك المنح أدناه لا يفتح طريقًا لتمريرها: حامل `users.manage` (مدير الموارد البشرية) لا ينشئ حسابًا يحملها ولا تجاوزًا ولا يغيّر كلمة مرور حساب يحملها، ومن يملك `roles.manage` من غير مدير النظام لا يصنع دورًا يحملها. هذا القسم يحل محل صف «منح payroll.reopen/cancel وattendance_exemption.* لأدوار» في القسم 7.

| الصلاحية | super_admin | hr_manager | branch_manager | payroll_officer (مسؤول رواتب) | employee | المصدر |
|---|---|---|---|---|---|---|
| `payroll.reopen` | نعم (`*`) | نعم | — | — | — | ترحيل 019_sec2 |
| `payroll.cancel` | نعم (`*`) | نعم | — | — | — | ترحيل 019_sec2 |
| `overtime.adjust` | نعم (`*`) | نعم | — | — | — | ترحيل 019_sec2 (مواءمة مع حزمة الكود) |
| `payroll.policy.manage` | نعم (`*`) | نعم | — | — | — | ترحيل 017_b2 |
| `attendance_exemption.view` | نعم (`*`) | نعم | نعم | — | — | ترحيل 016_c1 |
| `attendance_exemption.manage` | نعم (`*`) | نعم | نعم | — | — | ترحيل 016_c1 |
| `attendance_exemption.approve` | نعم (`*`) | نعم | — | — | — | ترحيل 016_c1 |
| `attendance_exemption.approve_executive` | نعم (`*`) | — | — | — | — | لا منح لأي دور |

**الأسباب:**
- **إعادة الفتح والإلغاء لدور الاعتماد نفسه** (`hr_manager` هو الدور الوحيد الذي يحمل `payroll.approve`). إعادة الفتح تعيد المسير المعتمد (لا المصروف) إلى `CALCULATED`، وتُسقط `approvedBy`، وتحرر المطالبات والأقساط، فيحتاج اعتمادًا جديدًا قبل الصرف. أي أنها لا تمنح قدرة فوق الاعتماد. الإلغاء لمسودة `CALCULATED` فقط. كلاهما بسبب إجباري (500 حرف كحد أقصى)، وحدث `REOPENED`/`CANCELLED` في سجل المسير، ونطاق الفرع يُفحص.
- **مسؤول الرواتب** (`payroll.calculate`) لا يأخذ أيًّا منها: من يحسب لا يلغي ولا يعيد فتح ولا ينشر سياسة. لا مستخدمين على الدور الآن.
- **مدير الفرع** يطلب استثناء الحضور لموظفي فرعه ويتابعه، والقرار للموارد البشرية.
- **`approve_executive` بلا دور:** الاعتماد التنفيذي لاستثناء القيادات يبقى لمدير النظام، والخدمة تمنع من اعتمد خطوة الموارد البشرية من اعتماد الخطوة التنفيذية.
- **`overtime.adjust`** ليست في نص البند، لكنها في قائمة الخطوة 9 نفسها (صلاحيات جديدة غير ممنوحة لأي دور) وموجودة في حزمة `hr_manager` بالكود. منحها يطابق القاعدة مع الكود. هي تخفيض لدقائق الإضافي فقط وبسبب موثق.
- **ما يبقى مفتوحًا (ليس منحًا):** `hr_manager` يجمع الحساب والاعتماد والصرف وإعادة الفتح في دور واحد. فصل الحاسب عن المعتمد (SRS PAYRUN-STATE-003، المراجعة §3 بند 2) بند مستقل. أي دور آخر يأخذ هذه الصلاحيات من شاشة الأدوار بيد مدير النظام فقط.

**التنفيذ:**
- الترحيل `docs/migrations/payroll/20260914_019_sec2_payroll_state_role_grants.sql` إضافي: يُلحق المفتاح الناقص بآخر مصفوفة الدور، ويتخطى الدور المعطل أو الحامل لـ`*`، ويرفع `tokenVersion` لمستخدمي الدور الذي تغيّر فعلًا (نفس سلوك شاشة الأدوار)، وإعادة تشغيله لا تغيّر شيئًا. يرفض بـ`THROW` في ثلاث حالات: صلاحيات `hr_manager` ليست JSON صالحة (53901)، أو تعذر المنح (53902)، أو دور نشط غير `hr_manager` يحمل إعادة الفتح أو الإلغاء، أو أي دور يحمل الاعتماد التنفيذي (53903).
- حزمة `hr_manager` في `ROLE_PRESETS` تضم `payroll.reopen` و`payroll.cancel` للتثبيت الجديد (البذر يضيف مفاتيح الحزمة الناقصة للأدوار الأساسية).
- الإثبات على قاعدة مؤقتة: `node --test --test-concurrency=1 api/test/security-role-grants.integration.cjs`. وعلى `hr_system`: `node api/scripts/db-step9-check.cjs`، حيث صار البند `roleGrants` قرارًا يُفحص بدل بند مسجل.
- **الحالة على `hr_system`:** طُبق 019_sec2 في 2026-09-14 14:26 UTC مع 018_c6 و020_c2 (`docs/migrations/runs/20260914142617_apply_hr_system.json`، نسخة لحظة التجميد `freeze/hr_system_freeze_20260914142611_96cb0e9d.bak`، الدفتر 28 والفرق 0). `roleGrants` = OK: `hr_manager` يحمل المفاتيح السبعة، و`branch_manager` يحمل view وmanage فقط، و`payroll_officer` و`employee` ودور الاختبار المعطل بلا شيء منها. رُفع `tokenVersion` لمستخدم `hr_manager` الوحيد، فيعيد تسجيل الدخول ليحمل توكنه المنح الجديد. الفحص الحي على المنفذ 4000 (14/14، بلا كتابة بيانات): إعادة الفتح والإلغاء لـ`hr_manager` تتجاوز البوابة (404 لمسير غير موجود) و403 لمدير الفرع والموظف، والاعتماد التنفيذي 403 لـ`hr_manager` و404 لمدير النظام، وإنشاء حساب بتجاوز `payroll.reopen` بيد `hr_manager` يعطي 403 ولا يُنشأ صف.


## C6 — السلف (الخطوة 29): السقوف والاستثناء والسداد المبكر والرصيد بعد الإنهاء ودفتر الموظف

**الترحيل:** `docs/migrations/payroll/20260914_018_c6_loan_caps_recovery.sql` (جداول `loan_cap_policies` و`loan_repayments` و`loan_recovery_balances` و`loan_recovery_events`، أعمدة NULL على `loans`، مفتاحا إعداد، وصلاحيات مدير الموارد البشرية). **الاختبارات:** `api/test/loan-caps.test.cjs` (نقي) و`api/test/loan-completion.integration.cjs` (قاعدة مؤقتة).

| الرمز | القرار الافتراضي | المفتاح / الموضع |
|---|---|---|
| C6-L1 | لا تُبذر سياسة سقوف افتراضية. بلا سياسة سارية لا يُطبق سقف (سلوك ما قبل الخطوة 29)، والمعاينة تقول ذلك صراحة. السقف قرار المالك من «السلف والقروض ← سياسات السقوف». | `loan_cap_policies` |
| C6-L2 | نطاق السياسة: الشركة، أو فروع، أو أقسام، أو فرق، أو موظفون محددون. الدرجة الوظيفية والفئة التعاقدية في SRS AD-01 غير موجودتين كحقول مرجعية في النظام، والبديل قائمة موظفين. الأخص نطاقًا يحكم، ثم الأولوية الأعلى، ثم أحدث نسخة. | `selectLoanCapPolicy` |
| C6-L3 | التعديل نسخة جديدة بنفس `policyKey`: السابقة تُقفل باليوم السابق لسريان الجديدة، أو تُوقف بسبب محفوظ لو تساوى يوم السريان. لا تُحفظ سياسة بلا أي سقف، ولا سقف مقطوع ≤ 0. | `POST /loans/cap-policies/:id/versions` |
| C6-L4 | قاعدة النسبة `BASIC` أو `GROSS` (المكونات الست) من الراتب الشهري الموثق لفترة المسير الحالية. لو غاب، يُستخدم الراتب الحالي في الملف مع ملاحظة ظاهرة. `NET_LAST_PERIOD` مؤجل. التقريب D4 (منزلتان، نصف لأعلى). | `evaluateEmployeeLoanCap` |
| C6-L5 | «الشهر» الافتراضي فترة المسير (دورة 23)، والشهر التقويمي خيار في السياسة. عداد الشهر يحسب طلبات السلف بحالات SUBMITTED/UNDER_REVIEW/APPROVED/IN_EXECUTION/COMPLETED/RETURNED_FOR_INFO بتاريخ التقديم، وبالمبلغ المعتمد إن خُفّض. المسودة والمرفوض والملغي لا تُحسب. | AD-04 |
| C6-L6 | المديونية القائمة = أقساط DUE المفتوحة لكل السلف (العادية والاستثنائية) + أرصدة `PENDING_RECOVERY` غير المحصلة. | AD-06 |
| C6-L7 | التقديم يُرفض بـ`LOAN_CAP_EXCEEDED` مع القيد الحاكم نصًا، وتُحفظ لقطة التقييم في `payload.capCheck`. عند **كل** خطوة اعتماد يُعاد التقييم تحت القفل المالي للموظف. التجاوز يعيد `LOAN_CAP_EXCEEDED_AT_APPROVAL` بالخيارات: الرفض بسبب، أو التخفيض (`approvedAmount` مع سبب في الملاحظة)، أو استثناء موثق بسبب لحامل `loans.cap_override`. تغيّر السياسة منذ التقديم يظهر كتنبيه في صندوق الموافقات. المطلوب والمعتمد يُحفظان منفصلين (`loans.requestedAmount` و`loans.amount`)، وقرارات الخطوات في `loans.capSnapshot`. | AD-05/07 |
| C6-L8 | السلفة الاستثنائية لحامل `loans.exceptional` لموظف غيره فقط. تشترط: تصنيفًا (طبية/عائلية/دراسية/أخرى)، وسببًا لا يقل عن `loan.exceptional_reason_min_length` (10)، وشهر أول قسط من فترة المسير الحالية حتى `loan.first_installment_max_months_ahead` (12) شهرًا. تتجاوز السقوف مع حفظ مقدار التجاوز، وتمر بسلسلة اعتماد السلف، ومنشئها لا يعتمدها. المرفق الإلزامي وعتبة الاعتماد الثاني يُضبطان من سلسلة الاعتماد (خطوة بعتبة مبلغ). | AD-09 |
| C6-L9 | السداد المبكر يسجله حامل `loans.repay` مباشرة، أو يمر عبر طلب «سداد سلفة مبكر». المبلغ الفارغ = سداد كلي بقيمة الإقفال الدقيقة. الجزئي يتطلب مرجعًا (إيصال/تحويل) وطريقة: `SHORTEN_TERM` يسدد من آخر الأقساط، و`REDUCE_INSTALLMENT` يوزع المتبقي بالتساوي ثم بالتناسب. القسط المسدد جزئيًا يصبح PARTIAL، وابنه يحمل الباقي في اليوم التالي لموعده (نفس فترة المسير). المرجع فريد لكل سلفة، وتكراره بنفس المبلغ لا يكرر الحركة. الزيادة عن الرصيد تُرفض مع قيمة الإقفال. | AD-14 |
| C6-L10 | عند اعتماد تصفية فيها رصيد سلف وصافيها سالب، يُسجل المتبقي غير المغطى `PENDING_RECOVERY`، بعد مقاصة المستحقات مع باقي الخصومات أولًا. التحصيل بمرجع لحامل `loans.repay`. الشطب لحامل `loans.write_off` بسبب لا يقل عن 10 أحرف، ولا يُمنح لأي دور افتراضيًا. `loans.write_off` و`loans.cap_override` ضمن `SUPER_ADMIN_ONLY_GRANTS`. الأقساط المطالَب بها في التصفية تبقى محجوزة بلقطتها كما كانت. | AD-13 |
| C6-L11 | دفتر الموظف: `GET /loans/mine` وشاشة «سلفي» (`/my/loans`) بلا صلاحية ولا معرف من العميل، وتعرض الخصم والتأجيل والسداد دون حجوزات المسير الداخلية. `GET /loans/:id/ledger` لغير صاحبها يتطلب `payroll.view` في نطاق الفرع. | AD-15 |
| C6-L12 | AD-08 (تغيّر الراتب بين الاعتماد والصرف) لا ينطبق حاليًا: السلفة تُنشأ في معاملة الاعتماد النهائي نفسها ولا توجد مرحلة صرف منفصلة. | — |

**بيانات قائمة على `hr_system`:** السلف 6–14 تبقى بأعمدتها الجديدة NULL (سلف عادية بلا لقطة). ملفات الإنهاء المغلقة 3 و4 و5 و11 سُويت قبل لقطات المصادر (`settlementFinancialSnapshot` = NULL)، وصافيها موجب، وأقساط سلفها 6–9 ما زالت DUE. لم تُنشأ لها أرصدة `PENDING_RECOVERY` بأثر رجعي ولم تُغلق أقساطها. هذا قرار مراجعة مالية منفصل.


## 12. مسار B1 — راتب شهر المسير، اشتقاق الفترة، والموظف الجديد (الخطوتان 13 و14 وقاعدة المالك)

### راتب الشهر داخل المسير (الخطوة 13)

- **الإعداد الجديد `payroll.salary_evidence_mode`** (مبذور `MONTHLY_HISTORY`، يُتحقق في `PATCH /settings/config`، ويظهر في «الإعدادات ← السياسات ← المسير ونهاية الخدمة»):
  - `MONTHLY_HISTORY` (الافتراضي): `calculateDefined` يختار مكونات الراتب الستة من سجل الأجر الشهري حسب `run.period` (`selectPayrollRunSalary` في `api/src/payroll/payroll-run-salary.ts`). بلا دليل للشهر يُستبعد الموظف ولا يُوقف المسير.
  - `MONTHLY_HISTORY_OR_CURRENT_FILE` (انتقالي صريح): من لا يملك سجلًا شهريًا يُحسب براتب الملف ويُوسم `CURRENT_FILE_UNVERIFIED` مع تحذير في لقطة العضو وشاشة المسير. من يملك سجلًا شهريًا لا يرجع لراتب الملف أبدًا حتى في هذا الوضع.
- **المصدر محفوظ** في `payroll_run_members.snapshot.salarySource` وفي `breakdown.salarySource` للبند: السجل والمراجعة والبصمة و«يسري من» والمبالغ النصية الدقيقة والعملة.
- **أكواد الاستبعاد الظاهرة** (`exclusionReason` و`snapshot.salaryIssue`، بتسميات عربية في شاشة المسير): `NO_SALARY_DEFINED` (لا سجل)، `SALARY_DAILY_HISTORY_ONLY` (سجل يومي قديم لا يحدد شهرًا)، `SALARY_PAYROLL_PERIOD_GAP` (الشهر غير موثق في السجل الشهري)، `SALARY_PAYROLL_PERIOD_INVALID`، `SALARY_HISTORY_INVALID`، `SALARY_HISTORY_SCHEMA_MISSING`، `SALARY_COMPONENT_INVALID`.
- **الاعتماد يعيد التحقق من راتب شهر المسير** ويرفض بـ`PAYRUN-SALARY-CHANGED` حتى إعادة حساب المسودة في حالتين:
  - لو كان المصدر سجلًا شهريًا وتغيّر **راتب شهر المسير نفسه** بعد الحساب: العملة أو أي من المكونات الست، أو صار الشهر بلا راتب.
  - لو كان المصدر راتب ملف غير موثق ثم عاد الوضع الافتراضي، أو صار للموظف سجل شهري يحكم الشهر.
  - **مراجعة الإصلاح (الاعتماد كان مفرط الصرامة):** المقارنة كانت ببصمة السجل كله، فأي مراجعة جديدة (زيادة أكتوبر وحدها، أو تصحيح شهر سابق لا علاقة له) كانت توقف اعتماد كل مسودة محسوبة للموظف. صارت بقيمة الشهر (`samePayrollRunSalarySource`)؛ مراجعات السجل تُلحق ولا تُعدّل، فالمراجعة المحفوظة في اللقطة تبقى مقروءة كدليل.
  - تعديل قيمة الملف وحده لا يمنع اعتماد راتب غير موثق محفوظ في اللقطة، وهو السلوك القديم المختبر في PR-05.
- **سعر ساعة الإضافي** عند آخر اعتماد = راتب شهر المسير الذي يقع فيه يوم العمل (`approval.wagePayrollPeriod` و`approval.wageSource`). بلا دليل: `OT_SALARY_MONTH_EVIDENCE_REQUIRED` (409)، والمعاملة كلها ترجع فيبقى الطلب في خطوته الأخيرة `UNDER_REVIEW` والقيد `SUBMITTED`، ثم يكتمل الاعتماد نفسه بعد توثيق الشهر بلا إعادة تقديم. اللقطات المعتمدة سابقًا لا يُعاد تسعيرها.
  - **يظهر قبل الاعتماد النهائي:** `GET /requests/:id` يعيد `overtime.wageEvidence` للقيد المعلق (`wagePayrollPeriod`، `ready`، `code`، `reason`، `message`، `sourceKind`؛ بلا مبالغ الأجر)، وقسم «مراجعة العمل الإضافي» في شاشة الطلب ينبّه «الاعتماد النهائي سيُرفض حتى يُثبت راتب الشهر» مع رابط سجل الأجر.
  - **حاجز شغل يومي على `hr_system` (لم يُذكر في التسليم الأول):** من 11 قيد إضافي `SUBMITTED` (كلها `UNDER_REVIEW` في الخطوة 1)، **9 سيُرفض اعتمادها النهائي** في الوضع الافتراضي حتى يُوثَّق راتب شهرها: القيود 43 و44 و45 و60 و61 للموظف 9 (راتب شهر 2026-07)، و46 و47 و48 للموظفين 75 و76 و79 (2026-09)، و59 للموظف 155 (2026-06)، والسبب `NO_SALARY_DEFINED`. القيدان 49 و50 للموظف 204 جاهزان (سجل شهري من 2026-09). ثبت هذا حيًا عبر `overtime.wageEvidence` (القسم «الإثبات»). المعالجة: توثيق راتب هذه الشهور للموظفين 9 و75 و76 و79 و155 من سجل الأجر (والموظفون 75 و76 و79 و155 بلا عملة في الملف فتلزم العملة)، أو الوضع الانتقالي مؤقتًا فيُسعَّر على راتب الملف ويُوسم «غير موثق».
- **إنشاء الموظف يوثّق أجر التعيين** «يسري من راتب شهر» في معاملة الإنشاء نفسها (`documentInitialEmployeeSalary` في `payroll-salary-change.ts`)، فلا يُستبعد الموظف الجديد `NO_SALARY_DEFINED` في أول مسير له. المسار نفسه يخدم تعيين المرشح (`POST /candidates/:id/hire`).
  - الحقلان في `CreateEmployeeDto`: `salaryEffectivePayrollPeriod` (YYYY-MM) و`salaryEvidenceReference`. نموذج الإضافة يعرض «يسري من راتب شهر» في الخطوة المالية بحدود `GET /employees/salary-start-context?hireDate=` (صلاحية `employees.create`، لا يقرأ بيانات موظف).
  - **الافتراض** = شهر التعيين (`actualStartDate` وإلا `joinDate`) أو شهر المسير الجاري أيهما أحدث؛ فملف منقول بتاريخ تعيين قديم لا يُنسب أجره الحالي لشهور سبقت إنشاءه، ويُقبل اختيار شهر التعيين صراحةً. المدى المقبول من شهر التعيين حتى الافتراض؛ الأقدم `EMPLOYEE_SALARY_START_BEFORE_HIRE` والأحدث `EMPLOYEE_SALARY_START_TOO_LATE` (الزيادة اللاحقة بطلب زيادة بعد الإنشاء).
  - المبالغ هي مبالغ الملف المحفوظة نفسها من SQL (المكوّن الفارغ = 0.00)، والسبب «أجر التعيين المثبت عند إنشاء ملف الموظف»، والمرجع `salaryEvidenceReference` أو «عقد <رقم العقد>» أو «إنشاء ملف الموظف #id»، والفاعل المستخدم المنشئ.
  - أجر بمكونات صفرية لا يُوثَّق (يُستبعد بسبب ظاهر)، وإرسال شهر معه يُرفض `EMPLOYEE_SALARY_START_WITHOUT_SALARY`. عملة غير SAR/EGP مع أجر تُرفض `EMPLOYEE_SALARY_CURRENCY_REQUIRED` (D8)، ولذلك أُزيل خيار AED من نموذج الإضافة. أي رفض يرجّع إنشاء الملف كله.
- **ملف الموظف وطلب زيادة الراتب** صارا «يسري من راتب شهر» (YYYY-MM):
  - DTO الملف: `effectivePayrollPeriod` و`previousEffectivePayrollPeriod`.
  - طلب الزيادة: حقل `effectivePayrollPeriod` (نوع `month`) ودليل الطلب `SALARY_REQUEST_BASIS_V2_20260914`. التاريخ اليومي يُرفض بـ`SALARY_REQUEST_PAYROLL_PERIOD_REQUIRED`، والطلب القديم بلا شهر يُرجع لاستكمال الشهر دون افتراضه.
  - الملف يقبل شهر المسير الجاري أو شهرًا سابقًا. الشهر اللاحق يُقدَّم بطلب زيادة يُنفَّذ عند بداية دورة شهره (مثال: «راتب أكتوبر» بدورة 23 يُنفَّذ في 23 سبتمبر).
  - الكتابة مراجعة شهرية (`SALARY_PAYROLL_PERIOD_HISTORY_V2_20260914`). السجل اليومي القديم يُحوَّل من شاشة سجل الأجر قبل التعديل (`SALARY_CHANGE_MONTHLY_HISTORY_REQUIRED`).
  - شهر دخل مسيرًا معتمدًا أو مصروفًا لا يُعدَّل: من الملف بـ`SALARY_CHANGE_CLOSED_PERIOD`، ومن شاشة السجل بـ`SALARY_HISTORY_CLOSED_PERIOD` لو تغيّرت قيمته.
- **أثر على `hr_system` (مهم للتشغيل اليوم):**
  - لم يكن يوجد سجل أجر شهري لأي موظف قبل هذا المسار. الآن (فحص 2026-09-14): سجل شهري للموظفين 189 (فحص C2) و204 (فحص B1) فقط، وسجل يومي واحد للموظف 1، و168 من 173 موظفًا نشطًا بلا عملة في الملف.
  - في الوضع الافتراضي يظهر كل موظف بلا سجل شهري مستبعدًا `NO_SALARY_DEFINED` حتى يُوثَّق راتبه «يسري من راتب شهر» من شاشة سجل الأجر أو من ملف الموظف، ويُرفض الاعتماد النهائي لإضافيه (القيود التسعة أعلاه). الموظفون الذين يُنشؤون من الآن يُوثَّق أجرهم عند الإنشاء.
  - البديل أن يختار المالك الوضع الانتقالي مؤقتًا من الإعدادات.
  - لم يُعبَّأ أي سجل تلقائيًا للموظفين الحاليين، التزامًا بقرار «لا تاريخ مفترض».
  - **تصحيح لما ورد في تقرير التسليم الأول** («4 طلبات راتب DRAFT تحمل effectiveDate»): على `hr_system` 7 طلبات DRAFT من الأنواع المخصصة `CUSTOM_62..66` (الطلبات 105 و110 و116 و121 و126 و144 و149) حمولتها `{"newSalary": 99999}` بلا `effectiveDate` ولا شهر، وكل طلبات `SALARY_INCREASE` (243 و244 و284 و285 و336) `COMPLETED`. المسودات السبع لا تُنفذ ولا تدخل المسير؛ لو قُدمت يطلب منها النموذج «يسري من راتب شهر».

### اشتقاق الفترة (الخطوة 14)

- `api/src/payroll/payroll-period.ts`: نهاية شهر M = يوم (بداية الدورة − 1) مقصوصًا داخل M، والبداية = نهاية الشهر السابق + يوم.
- دورة 23 لم تتغير: «راتب سبتمبر» = 23 أغسطس → 22 سبتمبر.
- دورات 29/30/31 لم تعد تتداخل. مثال دورة 31: فبراير 31/1 → 28/2، ومارس 1/3 → 30/3 بدل 28/2 → 30/3. لذلك اعتماد شهر لا يوقف الشهر التالي بـ`PAYRUN-DUP-002`.
- نفس الاشتقاق يُستخدم في حدود سجل الأجر الشهري المشتقة (`salaryPayrollPeriodBounds`) وفي بداية الفترة الجارية لاستثناء الحضور.
- **كشف الفجوة والتداخل**:
  - `payrollPeriodSequenceIssues` دالة صرفة.
  - `findPayrollPeriodContinuity` قراءة فقط لمسيرات الشهر السابق والتالي لنفس الموظفين. تظهر في `GET /payroll/runs/:id` (`periodContinuity`، ورقم مسير خارج النطاق لا يُكشف)، وفي شاشة المسير، وفي حدث الحساب.
  - التداخل الفعلي مع مسير معتمد يبقى حاجزًا عبر حارس العضوية.
- **غير مواءم بعد (خارج B1):** تحقق دورة نواة المحرك `verifyMonthlyCycle` في `payroll-input-facts.ts` ما زال بالقص القديم لدورات 29–31، واختباره `payroll-input-facts.test.cjs` يؤكد ذلك. على مسار SHADOW/POLICY مواءمته مع `payrollPeriodBounds` حتى لا يظهر فرق تكافؤ في هذه الدورات.

### الموظف الجديد (قاعدة المالك)

- التغطية من `actualStartDate`، وإلا `joinDate`. أيام الفترة قبله ليست غيابًا ولا خصمًا، والتناسب = أيام التغطية ÷ 30، وسعر يوم الغياب من الشهر الكامل (D3).
- **إصلاح:** تجسيد الغياب (`materializeAbsences`) كان يبدأ من `joinDate` فقط، فينشئ غيابًا بين الالتحاق والبداية الفعلية. صار يبدأ من نفس تاريخ التعيين المستخدم في التغطية.

### الإثبات

- اختبارات صرفة:
  - `api/test/payroll-period.test.cjs`: 5 دورات × 24 شهرًا × سلسلتان بلا فجوة ولا تداخل.
  - `payroll-period-salary.test.cjs`، و`payroll-salary-change.test.cjs`، و`salary-change-requests.test.cjs`، و`employee-salary-change-ui.test.cjs`.
  - `api/test/employee-create-salary-month.test.cjs` (جديد، 4/4): حدود أجر التعيين بدورات 23 و1 و31 (الملتحق الجاري/السابق/اللاحق، الملف القديم، بلا تاريخ، التاريخ غير الصالح)، ومساعد الواجهة (بلا أجر لا شهر، العملة، المدى)، وجلب الحدود، وعرض خطوة البيانات المالية في نموذج الإضافة بلا AED. مع الملفات الصرفة الثلاثة السابقة: 65/65.
- اختبار SQL/HTTP `api/test/payroll-run-salary-period.integration.cjs` (8/8):
  - 9,000 ثم 10,000 من سبتمبر. أغسطس بعد الزيادة = 9,000، وسبتمبر = 10,000 ويبقى 10,000 بعد زيادة أكتوبر ثم يُعتمد.
  - **دقة الاعتماد (جديد):** مسير مارس محسوب من المراجعة 1؛ تغيير عملة مارس وحدها → 409 `PAYRUN-SALARY-CHANGED`؛ تغيير مبلغ مارس → 409؛ مراجعة 4 تضيف زيادة أبريل وتصحح بداية شهر سابق ومارس كما حُسب → 201 بلا إعادة حساب، واللقطة المعتمدة باقية على المراجعة 1.
  - **إنشاء الموظف (جديد):** `GET /employees/salary-start-context` (200 بالحدود، 400 لتاريخ غير صالح، 403 بلا `employees.create`)؛ ملتحق في الشهر الجاري يُوثَّق بمراجعة شهرية 1 بمبالغ الملف الدقيقة ومرجع «عقد C-77» ويدخل المسير مشمولًا بـ6,000 + 1,000؛ ملف تعيينه 2020 يُوثَّق افتراضيًا من الشهر الجاري ويقبل 2020-01 صراحةً؛ تعيين مستقبلي من شهره؛ الرفض قبل الحفظ (`BEFORE_HIRE` ومنه البداية الفعلية اللاحقة، `TOO_LATE`، `CURRENCY_REQUIRED` لـAED، `WITHOUT_SALARY`) يترك 0 صف موظف؛ بلا أجر المراجعة 0.
  - يوم إضافي في أغسطس يُسعَّر على 9,000، ويوم 23 أغسطس على راتب سبتمبر.
  - أكواد الاستبعاد والوضع الانتقالي.
  - دورة 31: اعتماد فبراير ثم حساب مارس واعتماده بلا تعارض، وفجوة 31 مارس بعد تغيير الدورة.
  - ملتحق في اليوم العاشر: 9000×22/30=6600، غياب داخل التغطية فقط، ولا صفوف قبل التعيين حتى مع بداية فعلية متأخرة.
- اختبار SQL/HTTP جديد على **مسار اعتماد الطلب الحقيقي** `api/test/payroll-overtime-wage-month.integration.cjs` (2/2، دورة 23 والوضع الافتراضي، `POST /requests` ثم `/requests/:id/act` بثلاث خطوات):
  - راتب الملف 12,000 والسجل 9,000 حتى شهر يوم العمل ثم 10,000 من الشهر التالي: `wageEvidence` جاهز من `MONTHLY_HISTORY`، والاعتماد النهائي يثبت `wagePayrollPeriod` = شهر يوم العمل و`wageBase` = 9,000 وسعر 37.5 والقيمة 112.50 لساعتين × 1.5.
  - بلا سجل: `wageEvidence.ready=false` بسبب `NO_SALARY_DEFINED` وبلا مبالغ؛ خطوتا المدير ورئيس القسم 201؛ الموارد البشرية 409 `OT_SALARY_MONTH_EVIDENCE_REQUIRED` والطلب `UNDER_REVIEW` في الخطوة 3 والقيد `SUBMITTED` بلا لقطة؛ بعد توثيق الشهر الاعتماد نفسه 201 `COMPLETED` على 9,000.
  - (ملف `payroll-overtime-request.integration.cjs` ما زال يفشل 49/52 على `CALENDAR_SNAPSHOT_INVALID` لأن دولة فرع التجهيز `T<n>` لا تطابق `^[A-Z]{2,5}$`؛ هذا الملف الجديد يستخدم دولة صالحة ويؤكد تقويم GLOBAL والفرع، وإصلاح ذلك الملف للخطوة 8. وهو مضبوط على الوضع الانتقالي، فموظفوه المنشؤون بالمستودع لا يحتاجون سجلًا.)
- انحدار بعد المراجعة: `attendance-calendar-history` 17/17، `payroll-compensation` 13/13 (حالة F1 تنشئ عبر HTTP بتاريخ تعيين 2020 ومسير 2026-07 فتختار الشهر صراحةً)، `r1-daily-regressions` 10/10، `payroll-night-shift` 3/3، `payroll-salary-change` 21/21، `payroll-salary-request` 16/16، ومسار الاعتماد: `payroll-membership` 14/14، `payroll-membership-history` 5/5، `payroll-installment-ledger` 20/20، `payroll-period-salary.integration` 10/10، `overtime-immediate-dispatch` 9/9، والصرفة ذات الصلة (مزود التعويض، الفترة، سجل الأجر وواجهاته) 101/101. `payroll-settlement-boundary` 13/14 والفشل الوحيد فحص مكافأة نهاية الخدمة (409، الخطوة 7) كما كان قبل المراجعة. فحص الأنواع للـAPI وللجذر 0 خطأ. `payroll-flex` 5/32 كما كان قبلها، والفشل الـ27 كله `CALENDAR_SNAPSHOT_INVALID` (خطوة 8)؛ أُضيف لإنشائه عبر HTTP شهر 2026-07 صراحةً حتى لا يصير فجوة بعد إصلاح التجهيز.
- **فحص حي لمراجعة B1 على `hr_system` (قراءة فقط + إنشاء مرفوض قبل الحفظ؛ لم يبق شيء):** `scratchpad/b1x-live.js` بالمستخدم 1:
  - `GET /employees/salary-start-context?hireDate=2026-09-01` → 200: الدورة 23، الشهر الجاري والافتراضي 2026-09 (2026-08-23 → 2026-09-22)؛ و`hireDate=2020-01-01` → الأدنى 2020-01 والافتراضي 2026-09؛ والمستخدم 5 (موظف) → 403.
  - `POST /employees` بتعيين 2026-09-01 و«يسري من» 2026-08 → 400 `EMPLOYEE_SALARY_START_BEFORE_HIRE`، وصفوف الموظف بالكود قبل/بعد 0/0.
  - `overtime.wageEvidence` للقيود الـ11 `SUBMITTED`: 9 غير جاهزة `NO_SALARY_DEFINED` (43/44/45/60/61 → 2026-07، 46/47/48 → 2026-09، 59 → 2026-06) و49/50 جاهزان `MONTHLY_HISTORY` (2026-09). لم يُعتمد أو يُعدَّل أي طلب.
  - فرق المخطط بعد التغييرات = 0 (`pending schema queries: 0`)؛ لا ترحيل.
- **فحص حي على `hr_system` (الموظف الاختباري 204، التحق 2026-09-01 = اليوم العاشر من دورة 2026-09):**
  - بلا سجل شهري: مستبعد `NO_SALARY_DEFINED` بسبب ظاهر، ولا يوقف المسير.
  - بعد توثيق راتب شهر 2026-09: مشمول، التغطية 2026-09-01 → 2026-09-22 (22 يومًا، المعامل 0.733333)، والأساسي 4,766.67 = 6,500×22/30.
  - الغياب 10 و13 و14 سبتمبر فقط، و0 صف حضور قبل التعيين قبل الحساب وبعده.
  - أُلغي المسير #22.
- **ما بقي على `hr_system` من الفحص:**
  - المسير #22 `CANCELLED` بأعضائه وبنوده وأحداثه.
  - مراجعة سجل أجر شهري واحدة للموظف 204 (راتب الملف نفسه، يسري من 2026-09، المرجع `live-check:B1:2026-09-14`).
  - صفوف غياب مجسدة للموظف 204 في 10 و13 و14 سبتمبر، وكانت ستُجسَّد بالمهمة الليلية أيضًا.
- لا ترحيل جديد: لا تغيير مخطط (اللقطة `simple-json`)، والمفتاح يُضاف بـ`configSeed` عند الإقلاع. فرق المخطط = 0.

## C2 — الخصومات المصنفة (الخطوة 25): الكتالوج، نطاق المُنشئ، الاعتماد والتصعيد، الفترة المستهدفة، الحجز، حماية الصافي، والإنشاء الجماعي

**الترحيل:** `docs/migrations/payroll/20260914_020_c2_typed_deductions.sql` (مطبق على `hr_system` 14:26:16 UTC ضمن تطبيق `docs/migrations/runs/20260914142617_apply_hr_system.json`، و`verify` = OK والفرق 0): جداول `deduction_types` و`deduction_requests` و`deduction_request_events` و`deduction_batches`، وستة أعمدة NULL على `employee_obligations` (`deductionRequestId`، `targetPeriod`، `reservedPayrollRunId`، `reservedAt`، `appliedAmount`، `carriedFromObligationId`) وفهرسان، وبذرة الأنواع الأربعة (جودة، التزام، إداري، إنتاجية)، ومفاتيح `deductions.*`، وإلحاق `deductions.view/approve/manage` بدور `hr_manager` و`deductions.view` بدور `branch_manager`. القيود الخمسة القائمة في الدفتر لم تتغير.
**الكود:** `api/src/payroll/typed-deductions*.ts` (قواعد نقية + خدمة + متحكم `/deductions`)، `payroll-obligation-protection.ts` (حماية الصافي)، `payroll-obligation-ledger.ts` (حجز/صرف/تحرير)، وتعديلات موضعية في `payroll.service.ts` و`obligations.service.ts` و`destinations.service.ts`. **الشاشات:** `/payroll/deductions` (القائمة والاعتماد، الإنشاء الجماعي بفلاتر مترابطة ومعاينة واستبعاد، كتالوج الأنواع) و`/my/deductions` («خصوماتي» + مساحة المدير الهيكلي).
**الاختبارات:** `api/test/payroll-typed-deductions.test.cjs` (8/8) و`api/test/payroll-typed-deductions.integration.cjs` (7/7، قاعدة مؤقتة).

| الرمز | القرار الافتراضي | المفتاح / الموضع |
|---|---|---|
| C2-D1 | **صلاحية المُنشئ = علاقته الهيكلية بالموظف × نطاق النوع**: مدير مباشر (`managerEmployeeId`)، قائد الفريق، مدير القسم (مع الأقسام الفرعية)، مدير الفرع — بلا صلاحية نظام. الموارد البشرية = `deductions.manage` في نطاق الفرع. تعطيل إنشاء المدير الهيكلي كله بإعداد. | `deduction_types.creatorScopes`، `deductions.manager_creation_enabled` (true) |
| C2-D2 | لا خصم على النفس (`DEDUCTION_SELF`) ولا على من يعلو المُنشئ في سلسلة المدير المباشر بنفس حل `ApproverResolver` (`DEDUCTION_SUPERIOR`). خارج النطاق لا يظهر في قائمة الاختيار، وفي المعاينة الجماعية يُعد فقط دون أسماء. | DD-03/04 |
| C2-D3 | **المبلغ**: مبلغ ثابت، أيام (× المكونات الست ÷ 30 = D3)، ساعات (÷ `payroll.daily_hours` = D2)، نسبة من الأساسي أو الإجمالي؛ منزلتان نصف لأعلى (D4). حدود افتراضية لكل نوع: أدنى 1.00، أقصى 25% من الإجمالي للخصم الواحد، خطوة 0.25 للأيام/الساعات. الراتب = راتب **الشهر المستهدف** بنفس اختيار المسير (`selectPayrollRunSalary` و`payroll.salary_evidence_mode`)؛ بلا راتب للشهر يُرفض الخصم (`DEDUCTION_NO_SALARY_DEFINED`) لأن المسير سيستبعد الموظف. يُعاد فحص الحدود عند كل خطوة اعتماد، ويُثبت المبلغ النهائي عند آخر اعتماد. **انحراف موثق عن DD-02 قاعدة 1:** لا تحويل للأيام عند الاستهلاك، لأن الراتب يسري على شهر مسير كامل (قاعدة المالك) فراتب الشهر المستهدف معروف عند الاعتماد؛ والتقسيط يقسم المبلغ لا الوحدات (DD-10 قاعدة 2). | `computeDeductionAmount` |
| C2-D4 | **السلسلة**: خطوات النوع وتنتهي دائمًا بالموارد البشرية (`deductions.approve` في نطاق الفرع). تجاوز `escalationDays` × سعر اليوم (افتراضي يوم واحد) يضيف `escalationStep` (افتراضي مدير القسم) قبل HR. تُحل الأدوار الهيكلية لموظف محدد وتُلتقط؛ الخطوة التي معتمِدها هو المُنشئ أو الموظف أو معتمِد سابق أو غير موجود تُتخطى بسبب مسجل. المُنشئ والموظف لا يعتمدان. تعديل المبلغ لخطوة HR بصلاحية `deductions.manage` وسبب. خروج الموظف عن نطاق المُنشئ عند الاعتماد يتخطى الخطوات الهيكلية ويحوّله لـHR. | `buildDeductionChain` |
| C2-D5 | **الفترة المستهدفة** YYYY-MM إلزامية (حتى 12 شهرًا بعد الشهر الجاري). القيد يُنشأ عند آخر اعتماد بتاريخ سريان = بداية دورة الشهر (مسير 2026-10 = 2026-09-23) و`targetPeriod`. المسير يأخذه فقط لو `targetPeriod ≤ run.period` وتاريخه ≤ نهاية الفترة. شهر معتمد/مصروف للموظف يرفض الإنشاء (`DEDUCTION_PERIOD_CLOSED`)؛ الاعتماد بعد قفل الشهر يدخل أول مسير تالٍ مع حدث يذكر ذلك. التقسيط لأشهر متتالية (الحد من النوع). | DD-05/07/10 |
| C2-D6 | **التكرار**: نفس الموظف والنوع وتاريخ الواقعة والمدخل خلال 24 ساعة بحالة قيد الاعتماد/معتمد → `DEDUCTION_DUPLICATE` ما لم يُؤكد صراحة. **عمر الواقعة** 90 يومًا افتراضيًا لكل نوع، والأقدم للموارد البشرية فقط مع تسجيل التجاوز؛ لا واقعة مستقبلية. **السبب** ≥ 20 حرفًا. المرفق الإلزامي مرجع نصي في هذه المرحلة. | `deductions.duplicate_window_hours` (24)، `deductions.reason_min_length` (20) |
| C2-D7 | **الجماعي**: موظفون محددون أو فريق أو قسم (مع الفرعية) أو فرع، مع استبعاد، والحد 500 في الدفعة. المعاينة تُقيّم كل موظف (النطاق، الحد، التكرار، الفترة، السلسلة) وتُنتج بصمة؛ الإرسال يعيد التقييم تحت القفل المالي ويرفض بصمة مختلفة (`DEDUCTION_PREVIEW_STALE` مع المعاينة الجديدة). لكل موظف طلب وسلسلة وأحداث مستقلة، والدفعة سجل تدقيق بالمنشأ والمتخطى وسببه. | `deductions.bulk_max_employees` (500) |
| C2-D8 | **الحجز والاستهلاك (DD-09)**: اعتماد المسير يحجز قيود الدفتر المحصلة باسمه؛ مسير معتمد آخر يُرفض `PAYRUN-OBLIGATION-RESERVED`؛ إعادة الفتح/الإلغاء تحرر؛ الصرف يثبت `appliedAmount`. | `payroll-obligation-ledger.ts` |
| C2-D9 | **حماية الصافي لكل الخصومات (DD-11) في المسير الحالي**: الأرضية والسقف من `payroll.policy.min_net_guarantee/net_floor_pct/max_deduction_pct_of_gross` (null على `hr_system` = أرضية صفر وبلا سقف). الإجازة بلا أجر محمية ولا تستهلك السقف؛ خصومات الحضور أولًا وفائضها يسقط (النقص ثم التأخير ثم الغياب) ولا يتحول دينًا؛ ثم قيود الدفتر المدينة (الاستردادات غير المصنفة أولًا ثم المصنفة بالأقدم) وفائضها يُرحّل عند الصرف كقيد PENDING جديد مرتبط بالأصل للشهر التالي؛ ثم الأقساط من الباقي. الصافي السالب المحفوظ يمنع الاعتماد والصرف (`PAYRUN-NET-NEGATIVE`). **لم يُنفذ من DD-11:** ترتيب «الإداري قبل باقي المصنفة»، وحد مرات الترحيل والتعليق. | `protectPayrollObligations` |
| C2-D10 | **قفل الخصم المباشر**: `POST /obligations` بنوع DEBIT → 403 `OBLIGATION_DIRECT_DEBIT_LOCKED` (CREDIT باقٍ لمسار المكافآت). `obligationHandler` لم يعد يقرأ `payload.type`. قيد فقد العهدة المعتمد بطلبه يبقى DEBIT بفئته. | `obligations.service.ts`، `destinations.service.ts` |
| C2-D11 | **الإلغاء (DD-12)** لحامل `deductions.manage` بسبب ≥ 20 حرفًا: الأقساط المعلقة تُلغى والطلب يبقى مرئيًا؛ المحجوز في مسير معتمد يُرفض `DEDUCTION_RESERVED`؛ المستهلك يُرفض `DEDUCTION_CONSUMED` (العكس بقيد عكسي خارج هذه الخطوة). المُنشئ يسحب قبل اكتمال الاعتماد. | `/deductions/:id/cancel`، `/withdraw` |

**إعادة العمل بعد مراجعة S25 (C2-R):** الترحيل `docs/migrations/payroll/20260914_023_c2r_deduction_type_ownership.sql` (ثلاثة أعمدة NULL على `deduction_types`: `ownerDepartmentId`، `functionalScope`، `basisEscalationDays`؛ لا تغيير لأي صف) طُبق على `hr_system` 15:18:02 UTC ضمن `docs/migrations/runs/20260914151803_apply_hr_system.json` (الفرق 0). المفاتيح الجديدة تُبذر بـ`DEDUCTION_CONFIG_SEED` عند الإقلاع. الاختبارات: `payroll-typed-deductions.test.cjs` 15/15 و`payroll-typed-deductions.integration.cjs` 14/14.

| الرمز | القرار الافتراضي | المفتاح / الموضع |
|---|---|---|
| C2-R1 | **النطاق قبل حالة الموظف**: الترتيب نفسك ← خارج النطاق ← من يعلوك ← غير نشط. غير الموجود يرد بنفس «خارج النطاق» لمن لا يملك نطاقًا عامًا (404 لحامل `deductions.manage` بلا فرع فقط). المعاينة بالمعرفات تعيد `DEDUCTION_OUT_OF_SCOPE` بلا اسم ولا كود ولا فرع. | `evaluate`، `create` |
| C2-R2 | **لا عدّاد مخفي**: المعاينة الجماعية تعرض وتعد وتقفل وتقيس حد الدفعة على من في نطاق المُنشئ لهذا النوع فقط؛ `hiddenOutOfScope` أُزيل من الرد، وعدد من تُجوهل يبقى في سجل الدفعة الداخلي فقط. | `buildPreview` |
| C2-R3 | **الاستبعاد في الواجهة**: المشمولون بدلالة الخادم (القسم مع فروعه عبر `departmentPath` من `/deductions/candidates`)؛ البحث للعرض فقط؛ الاستبعاد يُرسل كاملًا ويُصفّر عند تغيير الوحدة؛ «إرجاع» للمستبعد في المعاينة؛ الفلاتر الأدنى من نمط الاختيار معطلة. | `TypedDeductionsWorkspace` |
| C2-R4 | **التتبع في القسيمة والمسير**: `GET /payroll/items/:id` يعيد `obligationDetails` (القيد، التصنيف، النوع، السبب، رقم الطلب، الوحدات وسعر اليوم/الساعة، المحصل والمرحّل)؛ يعرضه `PayrollObligationBreakdown` في القسيمة وتحت «خصومات الدفتر» في جدول المسير، مع سطر حماية الصافي (DD-11 قاعدة 5) ورابط `/payroll/deductions?request=ID` أو «خصوماتي». | `payroll-obligation-trace.ts` |
| C2-R5 | **بديل المعتمِد المفقود**: الدور الهيكلي بلا معتمِد يصعد (قائد الفريق ← مدير القسم ← مدير الفرع، بشرط ألا يكون المُنشئ أو الموظف)؛ خطوة التصعيد التي لا تجد أحدًا تُسند للإدارة التنفيذية (`approve.executive`)؛ حدث `STEP_FALLBACK`. على `hr_system` 28 موظفًا نشطًا بلا قسم وقسمان بلا مدير. | `deductions.missing_approver_fallback` = NEXT_LEVEL (SKIP = السلوك السابق) |
| C2-R6 | **الجهة المالكة والنطاق الوظيفي ومصفوفة التصعيد (DD-01/03/04)**: دور مُنشئ `FUNCTION_OWNER` = مدير القسم المالك (أو قسم أعلى) على نطاقه الوظيفي (أقسام بفروعها/فرق/موظفون؛ فارغ = شجرة القسم المالك). النوع المملوك لا تُنشئه الأدوار الهيكلية إلا لمن ينتمي للجهة؛ الموارد البشرية دائمًا. حد التصعيد لكل دور مُنشئ بأيام الراتب (غياب = حد النوع، null = بلا تصعيد). كل ذلك في لقطة الطلب ويرفع النسخة. **انحراف موثق:** المصفوفة على الأدوار الهيكلية (هي أدوار الإنشاء الفعلية) لا أدوار النظام؛ `can_edit_own` لا ينطبق (لا تعديل بعد الإرسال: سحب ثم إنشاء)، و`can_cancel_own` = سحب المُنشئ. | `deduction_types`، `deductionEscalationDaysFor` |
| C2-R7 | الصافي السالب المحفوظ يمنع **الصرف** أيضًا (`PAYRUN-NET-NEGATIVE` في `pay()`)، لا الاعتماد وحده. | `payroll.service.ts` |
| C2-R8 | **مهلة الخطوة (DD-06 قاعدة 1)**: فحص كل 15 دقيقة؛ بعد 48 ساعة من آخر فعل: ESCALATE يتخطى الخطوة بسبب مسجل (`SLA_ESCALATED`)، وخطوة الموارد البشرية الأخيرة لا تُتخطى أبدًا (تسجيل `SLA_BREACHED` مرة)؛ WAIT يسجل فقط؛ AUTO_REJECT يرفض آليًا. | `deductions.step_sla_hours` (48، 0 = بلا مهلة)، `deductions.sla_breach_action` (ESCALATE) |
| C2-R9 | **الإخطار والاعتراض (DD-08)**: إشعار مشتق للموظف بكل تغيير حالة (إنشاء، اعتماد، رفض، إلغاء، عكس، ترحيل، تعليق، رد على اعتراض). الموظف يعترض خلال المهلة من الإنشاء؛ الاعتراض بلا رد يمنع الاعتماد؛ الرد من المعتمِد الحالي أو الموارد البشرية ويراه الموظف. نوع الطلب العام `DEDUCTION_OBJECTION` يبقى «تسجيل فقط». | `deductions.objection_window_days` (5)، `deductions.objection_blocks_approval` (true) |
| C2-R10 | **العكس بعد الصرف (DD-12 قاعدة 3/4)**: قيد إضافة `deduction_reversal` كلي أو جزئي بحد المستهلك غير المعكوس، مستحق للمسير التالي؛ المسير المصروف لا يتغير. الإلغاء يمس القيود المدينة فقط ويشمل المعلقة. | `/deductions/:id/reverse` |
| C2-R11 | **DD-11 الترتيب والتعليق**: النظامي والحكم القضائي أولًا فوق الأرضية ويستهلك سعة السقف؛ ثم الحضور؛ ثم الاستردادات ← المصنفة بأولوية ترحيل أعلى ← الإدارية آخرًا. الترحيل رقم (حد + 1) منذ آخر استئناف يُنشئ القسط `SUSPENDED` (حدث `CARRY_SUSPENDED`) ولا يدخل مسيرًا حتى قرار الموارد البشرية: استئناف لشهر مفتوح أو إسقاط موثق. | `deductions.max_carry_forward_count` (3)، `/deductions/obligations/:id/decision` |
| C2-R12 | **التقسيط بالوحدات (DD-10 قاعدة 2)**: الأيام/الساعات تُقسم بالوحدة (4 منازل، الباقي في الأخير) ويُسعّر كل قسط براتب شهره عند الإنشاء وعند كل اعتماد؛ المبلغ النهائي = مجموع الأقساط، ووسم القيد يذكر الوحدات والسعر. المبلغ المعدل من الموارد البشرية عملة فيُقسم بالمبلغ. | `splitDeductionUnits`، `priceParts` |
| C2-R13 | **التقارير (DD-13)**: بحسب النوع والجهة المُنزِلة لكل فترة، بحسب الموظف (3/6/12 شهرًا ووسم من تجاوز الحد خلال 90 يومًا)، المرحّل والمعلق والقيود بلا مسير، زمن دورة الاعتماد ونسبة التصعيد، الاعتراضات، والمطابقة (سطور المسير = الدفتر، فرق صفر)؛ من الدفتر واللقطات بنطاق المستخدم، وتصدير CSV بنفس الأعمدة. | `GET /deductions/reports`، `deductions.repeat_deduction_threshold` (3) |
| C2-R14 | **الإعدادات والقائمة والكتالوج**: حدود دنيا وعليا مشتركة تُرفض عند الحفظ وتُقصّ عند القراءة (لا رجوع صامت للافتراضي)؛ القائمة تقرأ صفحات حتى 500 صف مرئي فعلًا؛ فئة النظامي/القضائي ثابتة لا تُحوّل (`DEDUCTION_TYPE_PROTECTED_CATEGORY`). | `deductionSettingError`، `list` |

**ما زال مفتوحًا (موثق):**
- ربط القيود المصنفة بمحرك السياسة SHADOW/POLICY: `payroll-live-ledger-provider.ts` ما زال يوسم DEBIT غير الأقساط `NON_INSTALLMENT_DEBIT_UNSUPPORTED` (ملف مسار path-core، لم يُعدّل تفاديًا للتعارض). المسير المصروف (القديم) يطبق كل ما سبق.
- DD-02 قاعدة 1 (التسعير عند الاستهلاك): التسعير عند آخر اعتماد براتب شهر كل قسط، اعتمادًا على قاعدة المالك «الراتب لشهر مسير كامل». تعديل رجعي لراتب شهر بعد الاعتماد لا يعيد تسعير القسط المعلق؛ الحل الحالي إلغاء الخصم وإعادة إنشائه.
- خارج قبول الخطوة 25: `cutoff_at` لكل مسير (DD-07)، قيد فريد لسطر القسط في طبقة التخزين (DD-09 قاعدة 2)، الإعادة للتعديل قبل الاعتماد (DD-12 قاعدة 1)، سياسة الفائض «إسقاط» كبديل عام (DD-11)، إعادة جدولة الأقساط والتسوية النهائية (DD-10 قاعدة 5/6)، وإعفاء الخصم المصنف في مسير (EX-01، مسار الإعفاءات).

**بيانات `hr_system` بعد إعادة العمل:** الفحص الحي أنشأ الطلب #3 (الموظف 189، يومان = 400.00، السلسلة: مدير الفرع بديلًا عن مدير القسم ثم الموارد البشرية) ثم ألغاه؛ باقٍ `CANCELLED` بثلاثة أحداث. لا قيد ولا دفعة جديدة. البند 1351 في المسير 16 (صافي -900) باقٍ `CALCULATED`؛ اعتماده وصرفه يُرفضان حتى إعادة حسابه (سلوك مقصود). سجل الأجر الشهري للموظف 189 (راتب ملفه نفسه 6,000) باقٍ: حذف الصفوف ممنوع، وواجهة الأجر الشهري لا تقبل سجلًا فارغًا.

**الفحص الحي على `hr_system` (المستخدم 17 = الموظف 9 المدير المباشر للموظفين الاختباريين 189 و190، والموارد البشرية المستخدم 12):**
- راتب 189 لم يكن موثقًا شهريًا، فوُثق راتب الملف نفسه (6,000 SAR، يسري من 2026-09، المرجع `live-check:C2:2026-09-14`).
- خصم التزام يوم واحد لشهر 2026-10: 201 بمبلغ 200.00 وأساس `DIRECT_MANAGER` وخطوة HR فقط، ومصدر الراتب السجل الشهري. المستخدم 11 (خارج النطاق) 403 `DEDUCTION_OUT_OF_SCOPE`، والمُنشئ لا يعتمد 403. اعتماد HR: قيد #6 بمبلغ 200.00، `targetPeriod` 2026-10، سريان 2026-09-23.
- جماعي [189، 190] مع استبعاد 190: المعاينة 189 جاهز 100.00 و190 مستبعد؛ الإرسال بالبصمة أنشأ طلبًا واحدًا (دفعة #1) ولا طلب لـ190.
- مسودة مسير 2026-10 (#23) للموظف 189: `otherDeductions` = 200، الصافي 5,800، `obligationIds` = [6] (طلب الدفعة غير المعتمد لم يدخل).
- **ما بقي من الفحص:** مراجعة سجل أجر شهري للموظف 189 (مما يجعله مشمولًا في مسيرات الوضع الافتراضي بعد أن كان `NO_SALARY_DEFINED`)، الطلبان #1 و#2 `CANCELLED` بأحداثهما، الدفعة #1، القيد #6 `CANCELLED`، والمسير #23 `CANCELLED` بعضوه وبنده وأحداثه. لم يُحذف شيء.

## 13. مسار R2 — استكمال بنود بيانات الخطوة 9 قبل أول مسير حقيقي

**قاعدة المالك:** الإصلاح لا الحذف، ولا قيمة مالية أو بيان مخترع لموظف أو للشركة. **الترحيلان:** `docs/migrations/payroll/20260914_021_r2_step9_data_completion.sql` (14:48 UTC، الدفتر 29) ثم — بعد مراجعة S9-data — `docs/migrations/payroll/20260914_024_r2b_step9_placeholder_restore.sql`، طُبق على `hr_system` في 2026-09-14 15:39 UTC في وضع الشركة والـAPI متوقف (سجل `docs/migrations/runs/20260914153948_apply_hr_system.json`، نسخة لحظة التجميد `freeze/hr_system_freeze_20260914153944_ea78fa73.bak`، بروفة `runs/20260914153850_rehearse_…json` OK؛ الحارس: 97 جدولًا و17,755 صفًا قائمًا باقية؛ الدفتر 32 كامل؛ الفرق 0؛ `employee_status_history` 213 → 381). 024 يعيد ما كتبه 021 أو فحص سابق بلا قرار صريح: القيمتان المؤقتتان، الأرضية والسقف، وبيانات فحص الموظف 1 — كل تحديث مشروط بالقيمة القديمة بالضبط وبسطر تدقيق، ولا حذف. **الإثبات:** `node api/scripts/db-step9-check.cjs` (19/19: 16 قرارًا بحالتها و3 بنود مسجلة).

> **الجدول أدناه هو الحالة الحالية بعد 024.** ما كتبه 021 وأُعيد: القيمة المؤقتة لاسم الشركة ظهرت كاسم حقيقي (رأس مستندات HR، واختفاء تنبيه قوالب الخطابات، و`GET /settings/company`)، والمسمى المؤقت ظهر في القسيمة ولقطة عضو المسير وملف الموظف، و50/50 كُتبا بلا قرار من المالك.

| البند | القرار | التنفيذ | الحالة على `hr_system` |
|---|---|---|---|
| شريحة التأخير 10، قيود الإضافي السبعة، القيدان 19 و26، الطلبات 292/295/303، دور الاختبار | بلا تغيير — منفذة بترحيل 015 وإجراءات الـAPI (القسم 7) | — | مُعاد فحصها: 10 معطلة؛ 7 قيود بساعات مستحقة وحدث؛ 19 و26 REJECTED؛ الطلبات الثلاثة REJECTED بسببها؛ تحذير «الإضافي المعلق» على المسير 6 = [60,61,45,43,44] |
| القيد المالي 1 (غرامة اختبار 500، كان APPLIED بلا مسير) | يبقى `CANCELLED` كما قرر 015، لا PENDING: إرجاعه PENDING يخصم غرامة اختبار 500 من الموظف 184 في أول مسير حقيقي. الصف والمبلغ باقيان؛ إرجاعه PENDING قرار للمالك بترحيل جديد | — | CANCELLED، 0 قيد APPLIED بلا مسير |
| الموظفان 1 و154 بلا راتب | لا يُخترع راتب. قاعدة الكود: مكونات الراتب الست كلها صفر (من الملف في الوضع الانتقالي أو من السجل الشهري) = `NO_SALARY_DEFINED` بدل بند بصافي صفر (`selectPayrollRunSalary` في `payroll-run-salary.ts`؛ يسري على تسعير الإضافي والخصومات المصنفة). **الموظف 1:** كُتبت عليه 13:33:42 UTC في ثانية واحدة بيانات فحص من المستخدم 1 عبر ملف الموظف (راتب 10,000+5,000+2,500+1,000+200+300 EGP، تعيين 2026-09-01، عقد 2026-09-01 → 2027-08-31، ومراجعة أجر يومية بسبب «0» ومرجع «2»). نسخة التجميد 13:24 UTC وأسطر التدقيق 53–61 تثبت القيم السابقة (راتب فارغ/صفر، بلا عملة ولا تعيين ولا عقد، بلا سجل أجر) ← أُعيدت بشرط أن تكون القيم الحالية قيم الفحص بالضبط وألا يشمله مسير غير ملغى، مع 10 أسطر تدقيق (SALARY/CONTRACT/DATA). مراجعة الأجر اليومية تبقى سجلًا ملحقًا (لا حذف) | 024 + الكود | فحص حي بمعاملة مُرجعة (`selectPayrollRunSalary`، راتب شهر 2026-09): **1** = `SALARY_DAILY_HISTORY_ONLY` في الوضع المضبوط `MONTHLY_HISTORY` و`NO_SALARY_DEFINED` في الوضع الانتقالي؛ **154** = `NO_SALARY_DEFINED` في الوضعين. لا يُصرف لأي منهما في أي وضع حتى يُثبت راتب «يسري من راتب شهر» بسبب ومرجع حقيقيين |
| اسم الشركة فارغ | بيانات المالك: لا اسم مؤقت ولا مخترع. 024 أعاد الفارغ الأصلي. حراسة دائمة لنص 021 المؤقت (`api/src/common/data-placeholders.ts`): مستندات HR تعامله كبيان غير متوفر ولا تطبعه في الرأس (`hr-documents.service.ts`)، و`companyNameConfigured` (`letter-templates.service.ts`) و`GET /settings/company` يعتبرانه غير مضبوط، و`PATCH /settings/config` يرفض حفظه لأي مفتاح `company.*` | 024 + الكود | الاسم فارغ؛ `GET /letters/templates` ← `companyNameConfigured=false` فيظهر تنبيه «أكمل اسم الشركة الرسمي»؛ `GET /settings/company` ← `name=""`؛ PATCH بالقيمة المؤقتة ← 400 والقيمة باقية؛ الخطابات ترفض «اسم الشركة في الإعدادات»؛ مستندات HR ترفض رمز `{{company.name}}` «بيان مطلوب غير متوفر» وتصدر القوالب التي لا تستخدمه برأس بلا اسم (كما قبل 021، القسم 7) |
| المسمى الوظيفي فارغ (158 موظفًا، منهم 148 نشطًا) | بيانات المالك: لا مسمى مؤقت. 024 أعاد القيمة القديمة المسجلة في سطر تدقيق 021 (NULL للـ158) مع سطر تدقيق `TITLE` جديد لكل موظف. الحراسة: `CreateEmployeeDto`/`UpdateEmployeeDto` (`@IsNotDataPlaceholder`)، والتقديم (`assertEmploymentValues` لتغيير المسمى والترقية — كان النص يمر حتى التنفيذ)، والتنفيذ (`assertJobTitle`)، ومستندات HR (`{{employee.jobTitle}}`)، والخطابات. القسيمة ولقطة عضو المسير الجديدة تقرآن المسمى من الملف فتعرضان «—»/null | 024 + الكود | 0 قيمة مؤقتة؛ 158 بلا مسمى (148 نشطًا)، و20 نشطًا بلا تاريخ تعيين؛ 158 سطر إرجاع (+158 من 021)؛ `GET /employees/154` ← `jobTitle=null`؛ PATCH ملف الموظف 10 والتقديم `TITLE_CHANGE`/`PROMOTION` بالقيمة المؤقتة ← 400 بلا صف. لقطة عضو المسير #24 (ملغى) تحمل النص المؤقت للموظف 154 وتبقى سجلًا مجمدًا |
| 162 من 167 نشطًا بلا نوع عقد أو بعقد محدد بلا نهاية | «تجديد عقد» معطّل (`request_types.isActive = 0`، الصف باقٍ) حتى تكتمل البيانات؛ «تغيير نوع العقد» يبقى مفعّلًا لأنه طريق الاستكمال. الترحيل يرفض (THROW 54101) لو كان على النوع طلب مفتوح. إعادة التفعيل من «الإعدادات ← أنواع الطلبات» بعد الاستكمال، وحارس التجديد لكل موظف (`contractChanges`: «التجديد يتطلب عقداً حالياً له تاريخ انتهاء») يبقى عند التقديم | 021 | معطّل؛ غائب عن كتالوج التقديم؛ التقديم 404 «نوع الطلب غير موجود» بلا صف؛ الطلب #338 (COMPLETED) لم يتأثر |
| أرضية الصافي وسقف الخصم | **بانتظار قرار المالك — لا نسبة مخترعة.** 021 كتب 50/50 بلا قرار صريح؛ 024 أعاد `null` (قيمة ما قبل 021، والبذرة `PAYROLL_POLICY_DEFAULT_CONFIG_SEED`، ونسخة السياسة المنشورة #1)، فلا فرق تكافؤ SHADOW بسببهما ولا اختلاف بين التثبيت الجديد وقاعدة الشركة. الحماية الفعلية مع null: الصافي لا يقل عن صفر؛ خصومات الحضور تُطبق كاملة؛ القيود والأقساط تُحصّل من المتبقي ويُرحّل الباقي (`loan.insufficient_net_behavior` = PARTIAL_THEN_CARRY)؛ D10 قسط متأخر واحد إضافي لكل مسير. `min_net_guarantee` null | 024 | null وnull وnull؛ لا نسخة سياسة فعالة بأرضية أو سقف؛ لا مسودة محسوبة بـ50 (البند الوحيد بها في المسير #24 الملغى) |

**خيارات المالك للأرضية والسقف وأثرها:** (1) البقاء على null (الحالي). (2) 50/50 كما في SRS DD-11: `protectPayrollObligations` يطبق الحماية على خصومات الحضور أولًا ويُسقط فائضها بلا ترحيل — في قياس R2 على المسودات الست عشرة، 164 من 519 بندًا كانت ستفقد جزءًا من خصم حضورها (`ATTENDANCE_EXCESS_DROPPED`)، وهذا يُضعف عقوبة الغياب. لو اختير: يُضبط من «الإعدادات ← السياسات»، وتُنشر نسخة سياسة جديدة بنفس القيم (وإلا يظهر فرق تكافؤ SHADOW)، ويُحدَّث `PAYROLL_POLICY_DEFAULT_CONFIG_SEED` مع اختبار `payroll-policy-settings.integration.cjs`. (3) أرضية تحمي من القيود والأقساط فقط دون خصومات الحضور تحتاج تغيير ترتيب الحماية في الكود. الأقساط المتأخرة: 18 قسطًا لسبعة موظفين (30، 32، 34، 42 قسطان 2,000 لكل منهم؛ 140 ستة أقساط 6,000؛ 150 و151 قسطان 1,000)، أقدمها 2026-08-01.

### قواعد HRC-10 لطلبات شؤون الموظفين — مطبقة، بانتظار تأكيد المالك

بند العقود في الخطوة 9 يطلب تأكيد المالك على القواعد الأشد (المراجعة ٤-ج بند 10). **القرار الافتراضي: تبقى كما هي** — تمنع تغيير الفرع والمسمى والعقد بلا صلاحية صريحة، والتخفيف قرار صريح:

| القاعدة | الموضع | الإثبات الحي على `hr_system` (بلا صف) | لو أراد المالك التخفيف |
|---|---|---|---|
| النقل إلى فريق في فرع آخر يقدمه مدير النظام فقط | `validateTransfer` في `api/src/requests/employment-destinations.ts` (عند التقديم والتنفيذ) | `TEAM_TRANSFER` للموظف 10 (الفرع 1) إلى الفريق 4 (الفرع 11) من مدير الموارد البشرية (المستخدم 12) ← 403 «النقل إلى فرع آخر متاح لمدير النظام فقط» | تعديل كود صريح يسمح لدور محدد |
| الترقية/النقل بـ`payload.employeeId` تُعامل طلب نيابة: `requests.create_on_behalf` ونطاق الفرع | `RequestsService.create` في `api/src/requests/requests.service.ts` | `PROMOTION` بـ`employeeId=10` من مدير الفرع (المستخدم 10، بلا الصلاحية) ← 403 «لا تملك صلاحية التقديم نيابة عن الغير» | منح `requests.create_on_behalf` للدور من شاشة الأدوار (بلا كود) |
| تجديد العقد يتطلب نوع عقد حاليًا وتاريخ نهاية | `contractChanges` في `employment-destinations.ts` | 162 من 167 نشطًا ناقصو البيانات ← «تجديد عقد» معطّل (021)؛ التقديم ← 404 «نوع الطلب غير موجود» | استكمال البيانات بطلب «تغيير نوع العقد» ثم تفعيل النوع؛ القاعدة نفسها شرط صحة لا يُخفف |

**الاختبارات:** `api/test/r2-step9-data.test.cjs` (8/8 صرفة: الصفر في الوضعين، المكون الموجب يبقى، الخطاب يرفض القيم المؤقتة، تطابق نص 021؛ مستندات HR ترفض رمزي الشركة والمسمى المؤقتين ورأسها بلا اسم مؤقت؛ `companyNameConfigured` و`GET /settings/company` و`PATCH /settings/config`؛ DTO الموظف و`assertJobTitle` و`assertEmploymentValues`؛ 024 بلا حذف وكل UPDATE مشروط وTHROW 54111/54112 فريدان). `api/test/r2b-step9-restore-migration.integration.cjs` (3/3 على قاعدة مؤقتة: 021 ثم 024 يعيدان الأصل بسطر تدقيق وبلا تكرار؛ قيم المالك وملف معدّل وسجل أجر بسبب حقيقي لا تُلمس؛ الموظف 1 المشمول في مسير غير ملغى يرجّع الملف كله). `api/test/r2-step9-data-migration.integration.cjs` (3/3). `api/test/payroll-run-salary-period.integration.cjs` (ملف مسار B1، 8/8 بعد مواءمته: الاعتماد يقارن قيمة شهر المسير نفسه — زيادة أكتوبر لا توقف اعتماد سبتمبر، وتغيير عملة أو مبلغ الشهر نفسه ← 409 `PAYRUN-SALARY-CHANGED` — وحالة الراتب الصفري).

**ما بقي على `hr_system`:** المسير #24 `CANCELLED` بأعضائه وبنده وأحداثه (الجولة الأولى)؛ أسطر التدقيق: 158 من 021 و158 من 024 للمسميات و10 للموظف 1؛ مراجعة الأجر اليومية للموظف 1 (ملحقة)؛ «تجديد عقد» معطّل؛ نسختا التجميد وسجلات البروفة والتطبيق. فحوص الجولة الثانية الحية كلها رُفضت قبل الحفظ (الطلبات 342 والاعتمادات 305 وسجل التغييرات 381 والنقل 5 والترقيات 3 قبل وبعد). لم يُحذف شيء.
