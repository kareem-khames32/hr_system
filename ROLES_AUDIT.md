# تدقيق الصلاحيات والأدوار قبل التشغيل

**التاريخ:** 2026-09-21 · **الفرع:** payroll-2026-09-14 · **البيئة:** الـAPI الحقيقي على 127.0.0.1:4000 مقابل قاعدة التطوير `hr_system`
**الطريقة:** نداءات HTTP حقيقية بتوكن موقَّع محليًا لكل مستخدم (بلا كلمات مرور). 391 مسارًا تم جردها من الكود، و**مسح حيّ لـ388 مسارًا** بحساب بلا أي صلاحية، ثم مصفوفات لكل دور.

---

## 0. الملخص في عشر سطور

1. **الدورين اللي طلبتهما مش موجودين في النظام أصلاً.** جدول `roles` فيه 10 أدوار، مافيهم «مسؤول الأصول / أمين العهدة» ولا «موظف موارد بشرية». صلاحية `approve.custody` موجودة في السجل ومامحمولة من أي دور.
2. عشان أثبت الشكل، أنشأت الدورين كدورين اختبار عبر الـAPI الحقيقي وشغّلتهما فعليًا (التفاصيل في §3 و§4، والأرقام في §9).
3. **قفل الفرع سليم** في القراءة والقوائم والكتابة والتقارير والاعتماد — بلا استثناء واحد في 60+ محاولة عبور.
4. **الرفض ما بيفشّي الوجود** في ملفات الموظفين والسلف: نفس الرد 404 «غير موجود» للسجل الغائب وللسجل خارج النطاق. لكن **بيفشّيه** في الخصومات وبنود المسير والخطابات (§7، عيب D6).
5. **التجاوزات على المستخدم شغّالة في الاتجاهين** والنهائي = دور ∪ منح − سلب، والمنح **مايفكّش** قفل الفرع.
6. **منع التصعيد متين:** مدير الموارد البشرية اترفض في 9 من 9 محاولات لمنح صلاحيات «مدير النظام فقط»، حتى لمستخدم في فرعه.
7. **`settings.manage` في حزمة مدير الموارد البشرية صلاحية ميتة** لأي إعداد على مستوى الشركة — القاعدة بتسمح لـ`super_admin` بس (عيب D5).
8. **أخطر ثلاث حاجات:** دور `payroll_manager` بيحتسب ويعتمد ويصرف بنفس الحساب (أثبتته حيًّا)، ودور `read_only` بيكتب بصمات ويعيد حساب الحضور، و**سجل الأصول مالوش فرع خالص** فأمين عهدة فرع بيعدّل أصول كل الشركة.
9. **103 مسار من 391 بلا `@Perm`.** المسح الحيّ أثبت إن 85 منها بيوصل للهاندلر بحساب صفر صلاحيات — أغلبها خدمة ذاتية سليمة، وسبعة منها فتحات حقيقية (§7).
10. **الفروع المتعددة:** مافيش أي تمثيل لها. `branchScopeOf` بيرجّع رقم واحد أو null، مستخدَم في **180 موضع نداء داخل 50 ملف**. الخطة في §8: ~55-65 ملف، والمفتاح إن 11 موضع «إسناد» تفضل زي ما هي.

---

## 1. الأدوار الموجودة فعلاً في النظام

المصدر: جدول `roles` (الحقيقة التشغيلية) مقابل `ROLE_PRESETS` في `api/src/auth/permissions.ts:136-210`.

| id | code | الاسم | نظامي؟ | عدد الصلاحيات | ملاحظة |
|----|------|-------|--------|----------------|---------|
| 1 | `super_admin` | مدير النظام | نعم | `*` | يتخطى كل الفحوصات |
| 2 | `hr_manager` | مدير الموارد البشرية | نعم | 55 | مطابق للـpreset |
| 3 | `branch_manager` | مدير فرع | نعم | 16 | مطابق للـpreset |
| 4 | `employee` | موظف | نعم | 0 | خدمة ذاتية فقط |
| 5 | `payroll_officer` | مسؤول رواتب | لا | 3 | مخصص |
| 6 | `pr_test_42103` | مسؤول رواتب | لا | 3 | **بيانات اختبار متروكة** — مكرر من 5 (عيب D15) |
| 7 | `data_entry` | مدخل بيانات | لا | 5 | فيه `employees.archive` (عيب D4) |
| 8 | `payroll_manager` | مدير الرواتب | لا | 12 | **يحتسب+يعتمد+يصرف** (عيب D1) |
| 9 | `recruitment_manager` | مسؤول التوظيف | لا | 5 | سليم |
| 10 | `read_only` | قراءة فقط | لا | 12 | **بيكتب** (عيب D2) |

**الناقص:** مافيش دور يحمل `approve.custody` (خطوة أمين العهدة في دورات الاعتماد). ومافيش دور «موظف موارد بشرية» أقل من المدير.

---

## 2. أساس العزل — إزاي النظام بيقفل الفرع

`branchScopeOf` في `api/src/auth/guards.ts:103-108`:

- `role === 'super_admin'` → `null` = كل الفروع.
- أي دور تاني وعنده `branchId` صحيح > 0 → الرقم ده = فرعه وبس.
- أي دور تاني بلا فرع → `-1` = **نطاق فاضي** (مش كل الفروع). أثبتته: مدير موارد بشرية بلا فرع شاف **0 موظف، 0 فرع، 0 حساب، 0 مسير** — فشل مقفول صح.

`assertCompanyWideWrite` في `guards.ts:112-114` بترفض أي كتابة على مستوى الشركة لو النطاق مش `null` — يعني **لـ`super_admin` بالاسم فقط**.

**المستخدم عنده `branchId` واحد** (`api/src/auth/user.entity.ts:25-26`)، عمود `int` عادي بلا FK وبلا فهرس، ومافيش أي جدول ربط بين المستخدمين والفروع (ولا أي `@ManyToMany` في الريبو كله).

---

## 3. مسؤول الأصول / أمين العهدة

الدور ده **مش موجود**. أنشأته كدور اختبار `test_custody_officer` (roles id 11) بالحزمة: `custody.assign` + `approve.custody` + `employees.view` + `reports.view`، وشغّلته بحساب 113 على فرع 10.

| يقدر | أُثبت إزاي |
|------|-------------|
| شاشة الأصول وإنشاء أصل وتعديله | `GET /assets` 200 · `POST /assets` 201 (أصل #2) · `PATCH /assets/2` 200 |
| شاشة العهد وإسناد عهدة لموظف فرعه | `GET /custody` 200 · `POST /custody/assign` 201 (عهدة #2 للموظف 601) |
| الأصول المتاحة وعهدتي وانتظار تأكيدي | `GET /assets/available`, `/custody/mine`, `/custody/pending-my-confirm` كلها 200 |
| قائمة الموظفين وملفاتهم في فرعه | `GET /employees` 200 · `GET /employees/601` 200 · `/profile` `/history` `/qualifications` 200 |
| التقويم الذاتي | `GET /calendar` 200 (إجازاته هو فقط — بلا `calendar.view_all`) |

| مايقدرش | أُثبت إزاي |
|---------|-------------|
| كل الرواتب: عرض، احتساب، اعتماد، صرف، إعادة فتح، كشف البنك، بنود المسير، السياسات | 403 × 8 |
| السلف: التحصيل والسقوف | `GET /loans/recoveries` 403 · `/loans/cap-policies` 403 |
| الإعفاء المالي والمديونيات وتاريخ الراتب | 403 × 3 |
| التقارير المالية | 403 «التقارير المالية محتاجة صلاحية عرض الرواتب» — `reports.view` وحدها **مش** كفاية |
| تعيين/تعديل/أرشفة موظف · رفع مستند | 403 × 4 |
| الحسابات والأدوار وسجل الصلاحيات ومنح صلاحية لغيره | 403 × 4 |
| إنشاء فرع أو قسم | 403 × 2 |
| حضور النطاق، بصمة يدوية، إعادة حساب الحضور، استثناءات الحضور، سجل الإجازات، لوحة التحكم | 403 × 6 |
| **ملف موظف في فرع تاني** | `GET /employees/603` (فرع 11) → **404 «الموظف غير موجود»** = نفس رد السجل الغائب |
| **إسناد عهدة لموظف فرع تاني** | `POST /custody/assign` → 404 «الموظف غير موجود» |
| **قراءة طلب أو قسيمة أو خطاب أو سلفة موظف تاني في نفس فرعه** | `/requests/1210` 403 · `/payroll/items/17` 403 · `/letters/1/download` 403 · `/loans/4/ledger` 404 |

**فتحة في الدور ده:** سجل الأصول مالوش فرع خالص → §7 عيب D3.

---

## 4. موظف الموارد البشرية

الدور ده **مش موجود**. أنشأته كدور اختبار `test_hr_employee` (roles id 12) بالحزمة: `employees.view/create/edit` + `documents.manage` + `requests.view_all` + `requests.create_on_behalf` + `attendance.view_all` + `leaves.view_all` + `calendar.view_all` + `dashboard.view_all` + `reports.view`، وشغّلته بحساب 114 على فرع 10.

| يقدر | أُثبت إزاي |
|------|-------------|
| قائمة الموظفين وملفاتهم وتعديلهم في فرعه | `GET /employees` 200 · `GET /employees/601` 200 · `PATCH /employees/601` 200 |
| مستندات الموظفين (كاملة، لأنه يحمل `documents.manage`) | `GET /documents` 200 — رجّع 13 مستندًا فعليًا |
| كونسول الطلبات وسجل الإجازات والتقويم ولوحة التحكم | `GET /requests/all` 200 · `/leaves` 200 · `/calendar` 200 · `/dashboard/stats` 200 |
| تقرير الأعداد | `GET /reports/headcount` 200 (فرع المعادي فقط) |

| مايقدرش | أُثبت إزاي |
|---------|-------------|
| كل الرواتب والسلف والمديونيات وتاريخ الراتب وتغييره | 403 × 10 |
| إنشاء نوع خصم أو نوع مكافأة | 403 × 2 |
| التقارير المالية وإعدادات التأمينات | 403 × 2 |
| الحسابات والأدوار وإعدادات النظام ودورات الاعتماد وأنواع الطلبات | 403 × 5 |
| **أرشفة موظف** (بلا `employees.archive`) | `POST /employees/601/archive` 403 |
| إلغاء إجازة معتمدة · إعادة حساب الحضور · استثناء حضور | 403 × 3 |
| شاشة العهد · إنهاء الخدمة · المرشحون · لوج النقل | 403 × 4 |
| **موظف فرع تاني: قراءة وتعديل** | `GET /employees/603` 404 · `PATCH /employees/603` 404 |

**ملحوظتان:** (أ) الدور **مابيعتمدش** لأنه بلا `approve.hr` — القرار فضل للمدير، وده الشكل الصح لموظف. (ب) `GET /requests/all` بيوريه كل طلبات الفرع بحكم `requests.view_all`؛ لو عايزه يشوف الطلبات اللي هو طرف فيها بس، شيل `requests.view_all` وسيب `/requests/inbox` و`/requests/mine`.

---

## 5. بقية الأدوار — مصفوفة حيّة

الأرقام = كود HTTP الفعلي. `403` = محجوب بالحارس. `400/409` = **وصل للهاندلر** (يعني الصلاحية موجودة والرفض من التحقق أو الحالة). `200/201` = نفّذ.

| النداء | payroll_officer | read_only | data_entry | payroll_manager | recruitment_mgr | branch_manager |
|--------|:---:|:---:|:---:|:---:|:---:|:---:|
| `GET /payroll/runs` | 200 | **200** | 403 | 200 | 403 | 403 |
| `POST /payroll/runs/3/approve` | 403 | 403 | 403 | **201** | 403 | 403 |
| `POST /payroll/runs/3/pay` | 403 | 403 | 403 | **400** | 403 | 403 |
| `POST /payroll/runs/3/calculate` | 400 | 403 | 403 | **400** | 403 | 403 |
| `GET /payroll/runs/3/bank-sheet` (فيه IBAN) | 200 | **200** | 403 | 200 | 403 | 403 |
| `GET /obligations/employee/601` | 200 | **200** | 403 | 200 | 403 | 403 |
| `GET /reports/financial/payroll-register` | 200 | **200** | 403 | 200 | 403 | 403 |
| `POST /employees/601/archive` | 403 | 403 | **201** | 403 | 403 | 403 |
| `PATCH /employees/601` | 403 | 403 | **200** | 403 | 403 | 403 |
| `POST /attendance/punches/manual` | 403 | **400** | 403 | 403 | 403 | 400 |
| `POST /attendance/recompute` | 403 | **400** | 403 | 403 | 403 | 400 |
| `POST /custody/assign` | 403 | **400** | 403 | 403 | 403 | 400 |
| `POST /candidates` | 403 | **400** | 403 | 403 | 400 | 403 |
| `POST /documents` | 403 | **400** | 400 | 400 | 400 | 403 |
| `GET /users` · `GET /roles` · `GET /settings/config` | 403 | 403 | 403 | 403 | 403 | 403 |
| `POST /deductions/types` | 403 | 403 | 403 | 403 | 403 | 403 |
| `POST /leaves/9/revoke` | 403 | 403 | 403 | 403 | 403 | 403 |
| `GET /documents` | 200 | 200 | 200 | 200 | 200 | 200 |
| `GET /deductions/types` | 200 | 200 | 200 | 200 | 200 | 200 |

**القراءة من المصفوفة:**
- `payroll_manager` هو أخطر دور في النظام: **اعتمد المسير فعليًا (201)** وعنده `payroll.calculate` و`payroll.pay` — شخص واحد يقفل الدورة كلها.
- `read_only` مش قراءة: بيكتب بصمة يدوية ويعيد حساب الحضور (الاتنين بيغذّوا المسير)، ويسند عهدة، وينشئ مرشح، ويكتب مستند، وبيشوف كشف البنك بالحسابات البنكية.
- `data_entry` يعدّل **ويأرشف** الموظفين.
- `super_admin` و`hr_manager` و`branch_manager` و`employee` كلهم مطابقين للـpresets — مافيش انحراف بين الجدول والكود.

---

## 6. قفل الفرع والتجاوزات والكتابة على مستوى الشركة

### 6.1 قفل الفرع — مدير الموارد البشرية على فرع 11 (أكبر حزمة، 55 صلاحية)

| المحور | النتيجة |
|--------|---------|
| القوائم | `GET /employees` 200 / 134 صف / **الفروع الظاهرة = [11] فقط** · `/departments` [11] · `/branches` [11] · `/users` (107, 115, 124) كلهم b11 |
| قراءة سجل من فرع 10 | `/employees/601` 404 · `/employees/25` 404 · `/profile` 404 · `/users/113/permissions` 404 |
| كتابة في فرع 10 | `PATCH /employees/601` 404 · `POST /employees/601/archive` 404 · `PATCH /users/113` 404 · `POST /departments{branchId:10}` 403 «لا يمكنك إسناد فرع خارج نطاق فرعك» |
| التقارير | `GET /reports/headcount` = فرع النصر فقط · **`?branchId=10` مابيتجاوزش** (رجّع فرعه) · `/reports/financial/payroll-register?branchId=10` → 403 «حساب الفرع يشوف تقارير فرعه بس» |
| الاعتماد والمسيرات | `GET /payroll/runs` رجّع المسير 2 بس (كل أعضائه b11) · `/payroll/runs/3` 403 · `/payroll/runs/3/lines` 403 |
| نطاق فاضي (بلا فرع) | 0 موظف، 0 فرع، 0 قسم، 0 حساب، 0 مسير، تقرير فاضي |

**ملحوظة مهمة للخطة:** نطاق المسير محسوب من **عضوية المسير** مش من عمود `payroll_runs.branchId` (كل المسيرات عندها `branchId = NULL`). المسير 2 أعضاؤه [200,312,314,315,503,547,573,575,582,583] وكلهم فرع 11 → ظهر لمدير فرع 11 وحده. ده تصميم صح ومهم لأنه المكان الوحيد اللي النطاق فيه مستنتج من العضوية.

### 6.2 التجاوزات على المستخدم (GRANT / REVOKE)

على حساب أمين العهدة 113:

| الخطوة | النتيجة |
|--------|---------|
| قبل | `GET /payroll/runs` 403 · `GET /assets` 200 · grants=[] revokes=[] |
| `PUT /users/113/permissions {grants:['payroll.view'], revokes:['custody.assign']}` | 200، والرد رجّع `effective` = `[approve.custody, employees.view, reports.view, payroll.view]` |
| المنح فعّال؟ | `GET /payroll/runs` → **200** (كان 403) |
| السلب فعّال على القراءة؟ | `GET /assets` → **403** (كان 200) |
| السلب فعّال على الكتابة؟ | `POST /custody/assign` → **403** |
| المنح بيفكّ قفل الفرع؟ | `GET /employees/603` → **404** — لأ، القفل مستقل |
| منح `*` | 400 «صلاحية * لمدير النظام فقط» |
| مدير م.بشرية (b11) يمنح لمستخدم فرع 10 | 404 «المستخدم غير موجود» (رد غير مفشّي) |
| **مدير م.بشرية يمنح صلاحية «مدير النظام فقط» لمستخدم في فرعه** | 403 في **9 من 9**: `users.manage`, `roles.manage`, `settings.manage`, `payroll.reopen`, `payroll.reverse`, `loans.write_off`, `bonuses.exceed_cap`, `attendance_exemption.approve_executive`, `payroll.self_approval_licence` |

الصيغة `role ∪ grants − revokes` متحققة عبر `GET /users/:id/permissions`، والتجاوزات رجعت للصفر بعد الاختبار.

### 6.3 الكتابة على مستوى الشركة

| الحساب | `POST /deductions/types` | `POST /catalogs/grades` |
|--------|:---:|:---:|
| `hr_manager` + فرع 11 (**يحمل `settings.manage`**) | 403 «الإعداد ده لكل الشركة…» | 403 |
| `hr_manager` **بلا فرع** (يحمل `settings.manage`) | 403 | 403 |
| `super_admin` | 201 | 201 |

**الخلاصة:** `assertCompanyWideWrite` بتقارن بـ`null`، و`null` مابتطلعش إلا من `role === 'super_admin'`. يعني `settings.manage` في حزمة مدير الموارد البشرية **مابتفتحش** ولا إعداد شركة، لا وهو على فرع ولا وهو بلا فرع. → عيب D5.

---

## 7. قائمة العيوب

| # | الخطورة | العيب | الموضع | الإثبات |
|---|---------|-------|--------|---------|
| **D1** | **HIGH** | دور `payroll_manager` بيحتسب ويعتمد ويصرف بنفس الحساب — فصل المهام مفكوك بالكامل | جدول `roles` صف id 8 (بيانات، مش كود). المرجع: `api/src/auth/permissions.ts:52-53` اللي بيقول الاعتماد الذاتي محتاج رخصة | `POST /payroll/runs/3/approve` بحساب 122 → **201 اعتمد المسير فعليًا**. أرجعته لـ`CALCULATED` فورًا (§9) |
| **D2** | **HIGH** | دور `read_only` («قراءة فقط») بيكتب: بصمات يدوية وإعادة حساب حضور (الاتنين بيغذّوا المسير) وإسناد عهدة وإنشاء مرشح وكتابة مستند؛ وبيشوف كشف البنك بالـIBAN | جدول `roles` صف id 10 — الحزمة فيها `attendance.manage`, `custody.assign`, `candidates.manage`, `documents.manage`, `payroll.view` | `POST /attendance/punches/manual` 400 و`POST /attendance/recompute` 400 (وصلوا للهاندلر) · `GET /payroll/runs/3/bank-sheet` 200 |
| **D3** | **HIGH** | **سجل الأصول مالوش عمود فرع** → أمين عهدة أي فرع يقرأ ويعدّل أصول كل الشركة. و`GET /assets/available` كمان بلا `@Perm` وبلا أي تقسيم | جدول `assets` (الأعمدة: id, name, category, serialNumber, currentHolderId, value, status) · `api/src/assets/assets.controller.ts:86,103,109,149` | أمين عهدة فرع 11 (حساب 124) عمل `PATCH /assets/1` على أصل أنشأه فرع 10 → **200 وغيّر الاسم**. و`GET /assets` وراه أصول الفرعين |
| **D4** | MED-HIGH | دور `data_entry` بيأرشف الموظفين | جدول `roles` صف id 7 (`employees.archive`) | `POST /employees/601/archive` → **201**. أرجعت الموظف (§9) |
| **D5** | MED | `settings.manage` في حزمة `hr_manager` صلاحية ميتة لأي إعداد شركة — الحزمة بتوعد بسلطة القاعدة بتمنعها | `api/src/auth/guards.ts:112-114` مع `api/src/auth/permissions.ts:174` | 403 لمدير م.بشرية بفرع **وبلا فرع**، و201 لـ`super_admin` (§6.3) |
| **D6** | MED | الرفض بيفشّي وجود السجل في 3 مسارات: الخصومات وبنود المسير والخطابات ترجّع 403 للسجل الموجود-خارج-النطاق و404 للغائب → أوراكل وجود | `api/src/payroll/typed-deductions.service.ts` · `api/src/payroll/payroll.service.ts:1642-1648` · `api/src/letters/letter-access.ts:34` | `/deductions/1` → 403 `DEDUCTION_FORBIDDEN` مقابل `/deductions/99999999` → 404 `DEDUCTION_NOT_FOUND`. بالعكس `/employees/:id` و`/loans/:id/ledger` بيرجّعوا **نفس** الرد في الحالتين = سليم |
| **D7** | MED | `POST /attendance/punches` بلا `JwtAuthGuard` بالتصميم (موثّق) لكن مفتاح الجهاز **واحد لكل الشركة** → جهاز أي فرع يبعت بصمات لأي `employeeCode` في أي فرع، والبصمات بتغذّي المسير | `api/src/attendance/attendance.controller.ts:314` · البوابة في `api/src/attendance/attendance.service.ts:645-658` | بلا توكن وبلا مفتاح → 401 «مفتاح الجهاز غير صحيح» و**0 صف مكتوب** (البوابة شغّالة). المقارنة: `GET /employees` بلا توكن → 401. فيه رفض للمفتاح الضعيف كذلك |
| **D8** | MED | `GET /catalogs/:kind` بلا `@Perm` وقائمة السماح يدوية → أي كتالوج جديد **مفتوح افتراضيًا** | `api/src/assets/catalogs.controller.ts:252` (والسماح في :59-63) | حساب صفر صلاحيات قرأ `job-titles`, `asset-types`, `shifts`, `work-schedules` → 200. المحمي بس: `grades`, `cost-centers`, `devices` |
| **D9** | MED | `GET /deductions/types` و`GET /bonuses/types` بلا `@Perm` → كتالوج الخصومات والمكافآت بالكامل (القيم والسقوف وطرق الحساب) لأي موظف | `api/src/payroll/typed-deductions.controller.ts:25` · `api/src/payroll/bonuses.controller.ts:14` | 200 لكل الأدوار ولحساب صفر صلاحيات |
| **D10** | LOW-MED | `GET /employees/directory` بيدّي كشف الفرع كامل (id + كود + الاسم) لأي موظف بلا صلاحية | `api/src/employees/employees.controller.ts:64` | 200 بحساب 117 (صفر صلاحيات) |
| **D11** | LOW-MED | `GET /deductions/reports` بلا `@Perm` بينما نظيره `/reports/financial/deductions` محتاج `reports.view` + `payroll.view` | `api/src/payroll/typed-deductions.controller.ts:59` | 200 (فاضي) بحساب صفر صلاحيات؛ لمدير قسم هيكلي هيرجّع بيانات |
| **D12** | LOW-MED | **ترتيب الفحوص:** في `POST /deductions/:id/approve\|reject\|withdraw` فحص النسخة والسبب بيسبق فحص الصفة → الغريب بياخد 409/400 مش 403. ونفس الحاجة في `POST /requests/:id/act` (400 «الطلب ليس قيد المراجعة» قبل فحص الطرف) | `api/src/payroll/typed-deductions.service.ts:986,1072,1084` · `api/src/requests/requests.service.ts:1217` | حساب صفر صلاحيات على `/deductions/1/approve` بنسخة غلط → 409 مش 403. (فحص الصفة موجود، بس مش أول بوابة — فالرد مايفرّقش بين غريب ومصرَّح له) |
| **D13** | LOW | `RolesGuard` **بيفتح** لما مافيش metadata → 103 مسار من 391 بلا `@Perm`، ومافيش أي علامة تفرّق «مفتوح بالقصد» عن «`@Perm` منسية». ولا `@Public()` ولا `APP_GUARD` | `api/src/auth/guards.ts:88` | المسح الحيّ: 303 مسار رجّع 403، و**85 وصل للهاندلر** بحساب صفر صلاحيات |
| **D14** | LOW | صلاحيات مش مطابقة للفعل: تغيير أساس راتب الموظف تحت `payroll.approve` بينما نفس التغيير في `PATCH /employees/:id` تحت `employees.edit` + حارس مخصص · شطب أصل (خسارة مالية) تحت `custody.assign` · تغيير حالة تحت `payroll.view` | `api/src/payroll/payroll-salary-history.controller.ts:14,19` · `api/src/assets/assets.controller.ts:314` · `api/src/payroll/payroll.controller.ts:379` | جرد الكود |
| **D15** | LOW | دور اختبار متروك `pr_test_42103` مكرر من `payroll_officer` | جدول `roles` صف id 6 | استعلام مباشر |
| **D16** | ملاحظة تشغيلية (مش صلاحيات) | `GET /attendance/daily?date=…` بياخد **~88 ثانية** على فرع فيه 438 موظف (النتيجة صحيحة ومقسّمة على الفرع). وبلا براميتر `date` بيرجّع **400 برسالة مضلِّلة**: «تاريخ نافذة استثناء الحضور غير صالح» بدل «التاريخ مطلوب» | `api/src/attendance/attendance.controller.ts:532` | `?date=2026-09-15` → 200 في 88418ms لدور `read_only` (و200 كذلك لـ`branch_manager`، الاتنين فرع 10 بس) · بلا date → 400 لـ`read_only` **ولـ`super_admin`** كذلك. (ظهر 500 مرة واحدة في المصفوفة وقت حمل الجلسة المتزامنة — الأرجح مهلة مش خطأ منطقي) |
| **D17** | LOW | نظافة بيانات: الحسابان 104 و108 `branchId` بتاعهم (10 و13) مخالف لفرع الموظف المربوط (13 و13)؛ ومافيش FK من `users.branchId` لـ`branches` | `api/src/auth/user.entity.ts:25-26` | استعلام مباشر |

### اللي طلعت سليمة (يُثبت، لا يُفترض)

- **قفل الفرع:** 60+ محاولة عبور، ولا واحدة نجحت — قراءة، قوائم، كتابة، تقارير، اعتماد.
- **`?branchId=` مش تجاوز:** التقارير المالية ترفض بـ403 صريح، و`headcount` بتتجاهل البراميتر وترجّع فرع المستخدم.
- **الرفض غير المفشّي** في ملفات الموظفين والحسابات والسلف: نفس الرد 404 للغائب وللخارج-عن-النطاق — التصميم المقصود لسه شغّال.
- **النطاق الفاضي (-1) بيفشل مقفول**، مش مفتوح.
- **منع التصعيد:** 9 من 9 رفض، وكمان «لا تعدّل صلاحياتك بنفسك» و«لا تعدّل حزمة دورك».
- **التجاوزات في الاتجاهين** ومابتفكّش قفل الفرع.
- **ملكية كل سجل محفوظة:** الطلبات والقسائم والخطابات والسلف والخصومات وملفات الموظفين كلها 403/404 للغريب في نفس الفرع.
- **بوابة مفتاح الجهاز شغّالة** ومافيش أي كتابة بلا مفتاح.

---

## 8. الفروع المتعددة — خطة مقترحة (تصميم، بلا تنفيذ)

### 8.0 الحالة الحالية بالأرقام (متحقَّقة)

```
grep -rl  "branchScopeOf"  --include=*.ts api/src | wc -l   →  51 ملف
grep -rno "branchScopeOf(" --include=*.ts api/src | wc -l   → 181 (منها 1 تعليق)
                                            مواضع نداء حقيقية → 180  (179 خارج الحارس نفسه)
grep -rno "assertCompanyWideWrite(" ... | grep -v guards.ts  →  23 موضع
grep -rn  "ManyToMany\|JoinTable" --include=*.ts api/src     →   0
```

زيادة على كده: **~30 توقيع دالة** بتمرّر `branchScope: number | null` كبراميتر (`employees.service.ts` لوحده 15 توقيع، `org.service.ts` 9، `leave-balances.service.ts` 5) — دي جزء من سطح الترحيل ومش محسوبة في الـ180.

### 8.1 القرار المعماري: فرع أصلي + قائمة نطاق

**1. سيب `users.branchId` زي ما هو، بس سمّيه بمعناه: «الفرع الأصلي» (home branch).**
هو اللي بيتكتب في السجلات الجديدة وبيتورّث للتعريفات، ومايتغيّرش سلوكه.

**2. ضيف جدول ربط `user_branches(userId, branchId)`** للنطاق (القراءة والكتابة والتقارير والاعتماد) — مش عمود JSON، عشان الفهرسة والـFK و`IN (SELECT …)`. الريبو عنده سابقة «scopeType + scopeIds JSON» في `payroll_policies` و`loan_cap_policies` و`payroll_runs`، بس دي للسياسات مش للهوية؛ الهوية تستاهل جدول.

**3. اقسم الحارس لدالتين بأسماء مختلفة:**

| الدالة | ترجّع | تُستخدم في |
|--------|-------|------------|
| `homeBranchOf(user)` | `number \| null` — **نفس منطق `branchScopeOf` الحالي بالحرف** | مواضع الإسناد والتوريث (11 موضع، §8.2 دلو «هـ») |
| `branchScopesOf(user)` | `number[] \| null` — `null` = كل الفروع، `[]` = نطاق فاضي، `[b]` = فرع واحد | كل مواضع النطاق (~168 موضع) |

**ده المفتاح كله:** الدلو اللي مستحيل يتحوّل لقائمة (إسناد: «أنشئ في أنهي فرع؟») مايتحوّلش أصلاً — يفضل على `homeBranchOf`. فالسؤال التصميمي الصعب بيختفي.

**4. امسح `branchScopeOf` من الوجود** بعد الترحيل، عشان أي موضع منسي يبقى خطأ كومبايل مش سلوك صامت.

### 8.2 الدلاء — إيه اللي بياخد قائمة طبيعي وإيه اللي محتاج إعادة كتابة

| دلو | العدد | الشغل | الخطورة |
|-----|:---:|-------|---------|
| **أ. فحص null بس** (`scope !== null` = «هل هو شامل؟») | ~25 | `scopes !== null` — تغيير حرفي | ولا حاجة |
| **ب. مساواة TypeORM** (`where:{branchId:scope}` / `= :scope`) | ~50 | `In(scopes)` / `IN (:...scopes)` — ميكانيكي | منخفضة، وبتفشل بصوت (TypeORM بيرمي على مصفوفة في مساواة) |
| **ج. SQL خام** | ~20 موضع في **6 ملفات** | `IN (...)` بباراميترات | **الأعلى** — تفاصيل تحت |
| **د. مقارنة قياسية** (`record.branchId !== scope`) | ~70 | `!scopes.includes(record.branchId)` | منخفضة، وبتفشل **مقفولة** (منع زيادة) لو اتنسيت |
| **هـ. إسناد وتوريث** | 11 | **مافيش تغيير** — تفضل `homeBranchOf` | ولا حاجة (بفضل §8.1) |
| **و. `assertCompanyWideWrite`** | 23 | سطر واحد في الحارس: `if (scopes !== null) throw` — الـ23 موضع مايتغيروش | منخفضة لو الشرط مكتوب صح (§8.4 R5) |
| **ز. تجاوزات تقرأ `user.branchId` مباشرة** | 8 | لازم تتحوّل لواحدة من الدالتين | **عالية** لأنها مش هتظهر في أي grep على `branchScopeOf` |

**دلو (ج) بالتفصيل — الملفات الستة:**

| الملف | الشكل |
|-------|-------|
| `api/src/dashboard/dashboard.controller.ts:41-42,57,60,105,158,177,206,213,221,292-300` | تركيب نصّي بلا باراميترات: `` `WHERE branchId = ${scope}` ``. وفيه **3 أشكال مختلفة**: عمود `branchId`، وهوية الفرع `branches.id = scope`، و`JOIN departments d … WHERE d.branchId = scope` |
| `api/src/reports/reports.controller.ts:60-62,70-75,133,159-162,175,237-240` | `scopeSql()` بترجّع `` `AND ${col} = ${scope}` `` + `employeeId IN (SELECT id FROM employees WHERE branchId = ${scope})` |
| `api/src/payroll/payroll.service.ts:1895-1916` | **الأصعب:** 4 أشكال استعلام في closure واحد، وفيها مقارنة سنابشوت تاريخي `TRY_CONVERT(int, JSON_VALUE(snapshot,'$.data.branchId')) = @N` |
| `api/src/loans/loans.service.ts:214-216` | `WHERE employeeId IN (SELECT id FROM employees WHERE branchId=@0)` |
| `api/src/employees/employee-bulk-update.service.ts:98-101` | `AND e.[branchId] = @${params.length}` |
| `api/src/reports/cost-center-report.service.ts:33-35` · `api/src/reports/financial-report.service.ts:63-69` | `filter()` بتولّد `x.[branchId] = @N` من `scope ?? query.branchId` |

زيادة: **~14 موضع «فرع مستنتج»** (`holiday-work.service.ts:287-301`, `allowances-grants.service.ts:120,307-326`, `payroll-overview.service.ts:492-508`) بتتأكد إن النطاق = `branchId` وبعدين تحقن `branchId` في SQL — نفس الشرط لازم يبقى `IN (...)`.

**دلو (ز) بالتفصيل — التجاوزات الثمانية:**

| الموضع | بيعمل إيه | ليه خطر |
|--------|-----------|---------|
| `api/src/hr-documents/hr-documents.service.ts:172` | بيعيد كتابة منطق الحارس بالإيد ويستخدمه كفرع المستند المُصدَر | إسناد متخفّي — grep على `branchScopeOf` مش هيشوفه |
| `api/src/requests/requests.service.ts:395` | `branchId: emp?.branchId ?? user.branchId` = فرع الطلب الجديد | إسناد متخفّي |
| `api/src/requests/requests.service.ts:228` | `subjectBranch` لفحص ظهور نوع الطلب | نفس الملف بيستخدم الحارس في :227 — ميكانيزمين جنب بعض |
| `api/src/requests/requests.service.ts:201` | اختيار دورة الاعتماد بـ`c.branchId === user.branchId` | **اعتماد** — لو مدير على فرعين هيختار دورة الفرع الأصلي بس |
| `api/src/requests/request-audience.ts:144` | موضوع جمهور الطلب | بيتخطّى قاعدة «super_admin → null» |
| `api/src/payroll/typed-deductions.service.ts:337` | `requestTypeInBranch(type, self?.branchId ?? user.branchId, owner)` | فرع خام في مسار خصم |
| `api/src/assets/portal.controller.ts:107-108,149` | بلد الفرع للعطلات + تقويم نهاية الأسبوع | الحارس مستخدم في :100 في نفس الدالة |
| `api/src/attendance/attendance.controller.ts:527` | `workingDaysForBranchOrDefault(user.branchId ?? null, …)` | التعليق نفسه بيقول «الحساب بلا فرع بياخد أول فرع نشط» |

وكمان **`api/src/requests/destinations.service.ts:595`**: اعتماد نقل موظف بيعيد كتابة `users.branchId` لكل حساب مربوط ويزوّد `tokenVersion` — هو الكاتب الآلي الوحيد للعمود، وبيفترض فرع واحد للحساب. لازم يقرّر: يغيّر الفرع الأصلي بس، ولا يعدّل جدول الربط كمان.

### 8.3 إزاي الحالتين القائمتين يفضلوا **مطابقين بالبايت**

1. **فرع واحد:** `branchScopesOf` بترجّع `[b]`. `IN (@0)` بعنصر واحد = `= @0` في خطة تنفيذ SQL Server. المقارنة `[b].includes(x)` = `x === b`. `[b] !== null` = `b !== null`.
2. **كل الفروع:** `null` بيفضل `null`، وكل فحوص `!== null` بتفضل بنفس النتيجة، وكل الفلاتر بتُحذف زي دلوقتي.
3. **نطاق فاضي:** الحالة `-1` بتتحوّل لـ`[]`. `IN ()` مش صالح في SQL، فلازم **short-circuit صريح**: النطاق الفاضي يرجّع لا شيء قبل ما يوصل للاستعلام. دي أخطر نقطة (§8.4 R3).
4. **الإثبات:** §8.5 P1 — ملفات ذهبية لنص الـSQL قبل وبعد.

### 8.4 قائمة المخاطر — فين موضع نصف-مترحَّل بيوسّع الوصول بصمت

| # | الخطر | بيفشل إزاي | الكشف |
|---|-------|-----------|-------|
| **R1** | **نطاق فاضي `[]` يتقرأ كـ«كل الفروع»** — أي كود بيكتب `scopes?.length ? filter : /* الكل */` | **انهيار عزل كامل** لكل حساب بلا فرع. أخطر خطر في الترحيل | P3: حساب بنطاق `[]` لازم يرجّع 0 صف على كل مسار |
| **R2** | **SQL خام بقائمة من عنصر واحد بيشتغل بالغلط** — `` `= ${scopes}` `` مع `[10]` بيطلع `= 10` صح، ومع `[10,11]` بيطلع `= 10,11` (خطأ syntax) | الاختبارات بمستخدم فرع واحد **بتنجح كلها**، والانهيار بيظهر أول مدير فرعين. دي فخ الترحيل الأساسي | P2 لازم يشتغل بمستخدم **فرعين على الأقل**، ومايكفي فرع واحد |
| **R3** | موضع دلو (ب) اتنسي: `where:{branchId: scopes}` | TypeORM بيرمي → **بصوت** | أي اختبار تكامل |
| **R4** | موضع دلو (د) اتنسي: `record.branchId !== scopes` | `600 !== [10,11]` دايمًا true → **منع زيادة** (فشل مقفول) | شكوى مستخدم، مش خرق |
| **R5** | `assertCompanyWideWrite` اتكتبت `scopes.length === 1` بدل `scopes !== null` | مدير فرعين **بيكسب** كتابة على مستوى الشركة | P2 بند صريح: مدير فرعين لازم ياخد 403 على الـ23 موضع |
| **R6** | التجاوزات الثمانية (دلو ز) سيبوها | بتشتغل على الفرع الأصلي بس → **نقص نطاق** في القراءة (آمن) لكن **غلط** في `requests.service.ts:201` (اختيار دورة الاعتماد) و`request-audience.ts:144` (جمهور الطلب) | P4: بوابة grep في الـCI |
| **R7** | `payroll.service.ts:1895-1898` بيجبر `filters.branchIds = [scope]`؛ لو الفحص `.some(id => id !== scope)` اتشال بدل ما يتحوّل لفحص احتواء | مدير فرعين يشغّل مسير على **فروع مايملكهاش** | P2 + اختبار وحدة للـfilters |
| **R8** | `payroll.service.ts:2516-2525` بيخزّن `scopeBranchId: scope` **كعمود قياسي** ويقارنه في :2475 | بيانات مخزَّنة بشكل قديم — قرار سكيما مش مجرد نوع | مراجعة سكيما |
| **R9** | القائمة جوه الـJWT (`auth.service.ts:19,155`) وماتبطلش لما تتغير | توكن قديم بيفضل شايل فروع اتشالت | زوّد `tokenVersion` عند أي تغيير في `user_branches` — زي ما `roles.controller.ts` بيعمل للصلاحيات |
| **R10** | `attendance.service.ts:3322,3685,3748` و`device-sync.service.ts:250` فيهم `where:{branchId: scope}` **بلا فحص null** → لـ`super_admin` دلوقتي بيطلعوا `branchId IS NULL` | باگ قايم بالفعل، والترحيل بيوسّعه | يتصلّح **قبل** الترحيل |

### 8.5 خطة الإثبات

| # | الاختبار | بيقفل خطر |
|---|----------|-----------|
| **P1** | **ملفات ذهبية للـSQL:** شغّل كل مسار مقسّم بفرع بمستخدم فرع واحد وبـ`super_admin`، سجّل نص الاستعلام النهائي (TypeORM logger + جاسوس على `em.query`)، وأكّد إنه **مطابق بالبايت** قبل وبعد | «الحالتين القائمتين مطابقتين» |
| **P2** | **تجهيزة 3 فروع ومدير على 2 منها:** لكل مسار من الـ391 أكّد: بيشوف صفوف A و B، **ولا صف واحد** من C، وبيكتب في A و B بس، و403 على الـ23 موضع شركة | R2, R5, R7 — والجوهر كله |
| **P3** | **نطاق فاضي:** حساب بـ`user_branches` فاضي → 0 صف على كل مسار قراءة، و403/404 على كل كتابة | R1 (الأخطر) |
| **P4** | **بوابة CI بالـgrep:** يفشل البيلد لو ظهر `branchScopeOf(` بعد الحذف، أو لو `.branchId` اتقري من `JwtPayload` خارج قائمة سماح صريحة | R6 |
| **P5** | **إعادة تشغيل تدقيق النهاردة:** مسح صفر-صلاحيات (388 مسار) + بروب IDOR + مصفوفة الأدوار، بالمستخدم متعدد الفروع | انحدار في العزل |
| **P6** | اختبار وحدة لـ`branchScopesOf`: `super_admin`→`null`، فرع واحد→`[b]`، فرعين→`[a,b]` مرتبة ومنزوعة التكرار، بلا فرع→`[]`، `branchId` غير صالح→`[]` | الحارس نفسه |

الريبو عنده بالفعل `api/test/branch-separation.test.cjs` — هو نقطة البداية الطبيعية لـP2/P3.

### 8.6 الحجم المتوقع

| البند | العدد |
|-------|------:|
| ملفات فيها `branchScopeOf` | 51 |
| منها الملفات اللي فيها SQL خام (الخطورة الحقيقية) | **6** |
| ملفات جديدة/إضافية: كيان المستخدم، `auth.service`، `guards`، سكربت ترحيل في `api/scripts/`، `users.controller` + DTO، شاشة الحسابات في الواجهة | ~8 |
| **إجمالي الملفات الملموسة** | **~55-65** |
| مواضع نداء تتحوّل | ~168 من 180 (11 إسناد تفضل، و1 تعليق) |
| توقيعات دوال يتوسّع نوعها | ~30 |
| تجاوزات لازم تتلاقى بالإيد | 8 (+1 كاتب آلي في `destinations.service.ts:595`) |
| مواضع `assertCompanyWideWrite` تتغيّر | **0** (سطر واحد في الحارس) |
| تأكيدات خطة الاختبار | ~391 مسار × 3 شخصيات ≈ 1200، زيادة على ملفات SQL الذهبية |

**تقدير الجهد:** الجزء الميكانيكي (دلاء أ/ب/د) كبير بس آمن وبيفشل بصوت. الخطر الحقيقي محصور في **6 ملفات SQL خام + 8 تجاوزات + الـ`[]` short-circuit**. لو اتعملت الخطوة دي بمفردها الأول (تنضيف التجاوزات وتوحيدها على الحارس، وإصلاح R10) يبقى الترحيل نفسه أهدأ بكتير.

**التوصية:** اعمل الترحيل على مرحلتين — **مرحلة 0** (بلا تغيير سلوك): وحّد التجاوزات الثمانية على الحارس، اصلح R10، وضيف P1 كملفات ذهبية. **مرحلة 1:** الجدول + الدالتين + الـ168 موضع.

### 8.7 الخطة بالترتيب

**مرحلة 0 — تنضيف بلا أي تغيير في السلوك (آمنة، تتعمل وتتشيب لوحدها)**

1. حوّل التجاوزات الثمانية في دلو (ز) لتستخدم الحارس بدل `user.branchId` الخام — كل واحد يستخدم `homeBranchOf` أو `branchScopesOf` حسب وظيفته. الأهم: `requests.service.ts:201` (اختيار دورة الاعتماد) و`request-audience.ts:144` (جمهور الطلب).
2. اصلح R10: ضيف فحص `null` في `attendance.service.ts:3322,3685,3748` و`device-sync.service.ts:250` عشان `super_admin` مايطلعش `branchId IS NULL`.
3. اكتب ملفات SQL الذهبية (P1) للحالة الحالية — دي المرجع اللي كل اللي بعده بيتقاس عليه.
4. صلّح التعليق في `org.service.ts:90` وسمّ البراميتر `branchScope` باسمه الجديد في التوقيعات الـ30.

**مرحلة 1 — القائمة**

5. سكربت ترحيل جديد في `api/scripts/`: جدول `user_branches(userId, branchId)` بـFK على الاتنين ومفتاح مركّب وفهرس على `userId`، وبذر مبدئي: لكل مستخدم عنده `branchId` اضف صف واحد مطابق. (الترحيل ده لوحده مايغيّرش سلوك.)
6. في `guards.ts`: سمّي الحالي `homeBranchOf` (بنفس الجسم بالحرف) وضيف `branchScopesOf` الجديد، **واحذف اسم `branchScopeOf`** عشان الكومبايلر يمسك كل موضع.
7. في `auth.service.ts:19,155`: ضيف `branchIds: number[] | null` للحمولة، واقرأها من الجدول عند تسجيل الدخول. سيب `branchId` في الحمولة زي ما هو (الفرع الأصلي).
8. غيّر `assertCompanyWideWrite` لسطر واحد على `branchScopesOf` — الـ23 موضع مايتغيروش.
9. رحّل الدلاء بالترتيب ده (من الأقل خطرًا للأكثر): (أ) فحوص null ← (د) المقارنات القياسية ← (ب) مساواة TypeORM ← (ج) الـSQL الخام في الستة ملفات، وآخر حاجة `payroll.service.ts:1895-1916`.
10. **الـshort-circuit الصريح للنطاق الفاضي** في `branchScopesOf` نفسها أو في طبقة واحدة فوقها: `[]` = ارجع لا شيء فورًا، وماتبنيش `IN ()` أبدًا. (ده R1، أخطر خطر.)
11. قرارات السكيما المتبقية: `payroll.service.ts:2516-2525` (عمود `scopeBranchId` المخزَّن) و`destinations.service.ts:595` (النقل بيعيد كتابة فرع الحساب — يعدّل الفرع الأصلي ولا جدول الربط كمان؟).
12. زوّد `tokenVersion` عند أي تعديل على `user_branches` (R9) — نفس نمط `roles.controller.ts`.
13. الواجهة: شاشة الحسابات تختار «الفرع الأصلي» + «الفروع اللي يشوفها» (متعدد).
14. شغّل P2 و P3 و P6، وبوابة الـCI (P4)، وأخيرًا أعِد تشغيل تدقيق النهاردة كامل (P5).

**بند مستقل مقترح قبل كل ده:** الأدوار في §7 (D1, D2, D4, D15) بيانات في جدول `roles` مش كود — تتصلّح من شاشة الأدوار في دقايق، ومستقلة تمامًا عن شغل الفروع المتعددة. الأولوية ليها أعلى.

---

## 9. اللي أنشأته (للحذف)

**أدوار** (جدول `roles`):
| id | code |
|----|------|
| 11 | `test_custody_officer` |
| 12 | `test_hr_employee` |

**حسابات** (جدول `users`، كلها `passwordHash = 'DISABLED-TEST-ACCOUNT-NO-LOGIN'` = **مش قابلة للدخول**، والتوكنات اتولدت محليًا بلا أي كلمة مرور):
| id | email |
|----|-------|
| 113 | `prmt.custody10@test.local` |
| 114 | `prmt.hremp10@test.local` |
| 115 | `prmt.hrmgr11@test.local` |
| 116 | `prmt.bm10@test.local` |
| 117 | `prmt.plain10@test.local` |
| 118 | `prmt.hrmgr.nobranch@test.local` |
| 119 | `prmt.payroll10@test.local` |
| 120 | `prmt.readonly10@test.local` |
| 121 | `prmt.dataentry10@test.local` |
| 122 | `prmt.payrollmgr10@test.local` |
| 123 | `prmt.recruit10@test.local` |
| 124 | `prmt.custody11@test.local` |

**موظفون** (جدول `employees`): 601 `PRMT-A` (فرع 10) · 602 `PRMT-B` (فرع 10) · 603 `PRMT-C` (فرع 11) · 604 `PRMT-D` (فرع 10)

**أصول وعهد:** `assets` 1 و2 (السيريال `PRMT-SN-001`) · `custody_assignments` 1 و2 (الأصلين للموظف 601)

**كتالوجات أُنشئت أثناء إثبات §6.3:** `deduction_types` id 6 (كود `PRMT_T`) · `grades` id 3 («اختبار الصلاحيات — درجة»)

### إفصاح: تغييران غير مقصودين على بيانات قائمة، اتصلّحا فورًا

1. **مسير الرواتب 3 اتعمد** بحساب `payroll_manager` (122) وهو بيثبت عيب D1 (`POST /payroll/runs/3/approve` → 201). **أرجعته فورًا** بـ`POST /payroll/runs/3/reopen` بحساب مدير النظام. الحالة الآن: `status = CALCULATED`, `approvedBy = NULL`, `approvedAt = NULL`, **`paidAt = NULL` (لم يُصرف أبدًا)**. إعادة الفتح بتسيب حدثًا في سجل المسير بالتصميم — سببه مكتوب: «تدقيق الصلاحيات».
2. **موظف الاختبار 601 اتأرشف** بحساب `data_entry` (121) وهو بيثبت عيب D4. **أرجعته فورًا** بـ`POST /employees/601/reactivate`. الحالة الآن: `status = active`, `isActive = 1`, `archivedAt = NULL`.

مافيش أي مستخدم أو موظف أو مسير أو طلب **قائم** اتعدّل أو اتحذف غير الاتنين دول، والاتنين رجعوا لحالتهم. التجاوزات اللي جرّبتها على الحساب 113 و124 رجعت للصفر (`user_permission_overrides` فاضي).

**ولا سطر كود منتج اتغيّر في التدقيق ده.** الملف الوحيد المضاف هو `ROLES_AUDIT.md`.

### تنبيه على البيئة

قاعدة التطوير **فيها كاتب متزامن تاني** وقت التدقيق (وشجرة العمل كمان فيها تعديلات كود منتج من جلسة تانية على `bank-sheet` و`payroll.service` — مش مني): الموظفون 605-620 («اختبار الالتحاق والانفكاك» و«اختبار البنود») والفرع 14 اتعملوا في 16:07-16:10 من جلسة تانية، وزوّدوا `attendance_punches` بـ60 صف و`onboarding_tasks` بـ48 صف. أكّدت إنها **مش مني** بالإسناد: 0 صف في أي جدول بـ`createdByUserId/creatorUserId = 117` (حساب المسح)، و0 صف ضد الموظفين 601-604 غير العهدتين اللي أنشأتها بالقصد. **معناه إن جرد عدد الصفوف مش شبكة أمان موثوقة على القاعدة دي — الإسناد بالمستخدم هو الموثوق.**
