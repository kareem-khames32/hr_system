# كرّاسة مواصفات — منظومة الرواتب والحضور المرتبط بها
**نوع المستند:** مواصفة متطلبات برمجية (SRS) · **الإصدار:** 1.1 · **التاريخ:** 2026-09-09

---

## أ) مقدمة ونطاق العمل

### ١-١ الغرض من المستند
يحدّد هذا المستند **المتطلبات الوظيفية التفصيلية** لمنظومة رواتب الموظفين، وما يرتبط بها من قواعد حضور تؤثر مباشرة في المبالغ المستحقة. المستند موجَّه إلى **جهة التطوير** لبناء المنظومة، ويصلح أساسًا للتعاقد والتسليم والاختبار.

### ١-٢ نطاق المنظومة
تغطي هذه المواصفة الوظائف التالية:

| المجال | الملخّص |
|---|---|
| **مسارات الرواتب** | إنشاء مسار احتساب لمجموعة موظفين محدّدة بنطاق تنظيمي أو بقائمة مخصّصة، بفترة يحدّدها المستخدم |
| **قواعد الحضور المؤثرة في الأجر** | الساعة المرنة (نافذة حضور مشروطة بإكمال الساعات) وشرائح خصم التأخير القابلة للتعريف |
| **محرّك السياسات والمعادلات** | أطقم حسابية متعددة، كل مسار يُحتسب بطقم مستقل، مع معادلات يعرّفها المستخدم |
| **البدلات** | كتالوج بدلات وإسنادها للموظفين بشكل دائم أو مؤقت |
| **الخصومات المصنّفة** | أنواع خصومات يعرّفها المستخدم، يُنزلها أصحاب الصلاحية على نطاقهم، وتمرّ بدورة اعتماد |
| **الإعفاءات والاستثناءات** | إعفاء موظف من خصم بعينه، ومنح مكافآت فردية، وتعريف موظفين مستثنين من الحضور |
| **السلف** | سقوف للسلف العادية، وسلفة استثنائية بتقسيط تحدّده الموارد البشرية |
| **العمل الإضافي** | نوافذ فتح وإغلاق، واكتشاف من أجهزة البصمة، وطلب من الموظف — بدورات اعتماد |
| **التقارير** | حزمة تقارير تشغيلية ورقابية وتحليلية |

### ١-٣ خارج النطاق
لا يتناول هذا المستند: المحاسبة العامة والقيود المحاسبية · التكامل مع البنوك وملفات التحويل · حساب الضرائب والتأمينات الاجتماعية بقوانين دولة بعينها · إدارة التوظيف والأداء والتدريب. (يمكن التعامل معها كمراحل لاحقة أو تكاملات.)

---

## ب) كيف تُقرأ هذه الوثيقة

### ٢-١ ترقيم المتطلبات
كل متطلب يحمل **معرّفًا ثابتًا** يُستخدم في التتبّع والاختبار والتسليم:

| البادئة | المجال |
|---|---|
| `PR-xx` | مسارات الرواتب وفترة الاحتساب |
| `FX-xx` | الساعة المرنة |
| `LT-xx` | شرائح خصم التأخير |
| `PL-xx` | سياسات ومعادلات الرواتب |
| `AL-xx` | البدلات |
| `DD-xx` | الخصومات المصنّفة |
| `EX-xx` | الإعفاءات والاستثناءات |
| `AD-xx` | السلف |
| `OT-xx` | العمل الإضافي |
| `RP-xx` | التقارير |

### ٢-٢ بنية كل متطلب
**الوصف** (ماذا يجب أن يفعل النظام) ← **قواعد العمل** (المنطق التفصيلي) ← **معايير القبول** (نقاط قابلة للاختبار يُقاس عليها التسليم) ← **أمثلة رقمية** و**حالات حدّية** عند الحاجة.

### ٢-٣ صيغة الإلزام
- **يجب** = متطلب إلزامي، عدم تحقيقه يعني عدم قبول التسليم.
- **يُفضَّل** = متطلب مرغوب غير حاجب.
- أسماء الجداول والحقول الإنجليزية الواردة في المستند **اقتراح تصميم** استرشادي لجهة التطوير، وليست إلزامًا بتسمية بعينها.

---

## ج) المصطلحات

| المصطلح | التعريف |
|---|---|
| **مسار الرواتب (Payroll Run)** | وعاء يجمع مجموعة موظفين على فترة محدّدة، يُحتسب لهم فيه الاستحقاق ويُعتمد ويُصرف |
| **فترة الاحتساب (Pay Period)** | المدى الزمني الذي تُحتسب عليه المستحقات (مثال: من 23 الشهر إلى 22 من التالي) |
| **النطاق التنظيمي (Scope)** | المستوى الذي يُشتقّ منه أعضاء المسار: شركة/فرع/قسم/فريق/مركز تكلفة |
| **لقطة الأعضاء (Membership Snapshot)** | تجميد قائمة من دخل المسار لحظة الاحتساب لأغراض التدقيق ومنع الازدواج |
| **التناسب (Pro-rata)** | احتساب الأجر بنسبة أيام تغطية الموظف داخل الفترة عند الالتحاق أو ترك الخدمة داخلها |
| **الساعة المرنة (Flexible Start Window)** | نافذة زمنية يُسمح للموظف بالحضور خلالها دون اعتباره متأخرًا، بشرط إكمال ساعات العمل المطلوبة |
| **نقص الساعات (Shortfall)** | الفارق بين ساعات العمل المطلوبة وما أنجزه الموظف فعليًا |
| **التأخير (Lateness)** | الفارق الزمني بين بداية الدوام الرسمية ووقت الحضور الفعلي |
| **شريحة الخصم (Deduction Tier)** | نطاق من دقائق التأخير تُطبَّق عليه طريقة خصم محدّدة |
| **السياسة (Policy)** | طقم متكامل من قواعد الاحتساب (بنود ومعادلات وشرائح) يُسنَد إلى مسار رواتب |
| **البند (Component)** | عنصر استحقاق أو خصم داخل السياسة، له مصدر قيمة محدّد |
| **دورة الاعتماد (Approval Cycle)** | سلسلة موافقات مرتّبة يمرّ بها إجراء قبل أن يصبح نافذًا |
| **دفتر المستحقات والمديونيات (Ledger)** | سجل القيود المالية الفردية (خصومات ومكافآت) التي يستهلكها المسار |
| **سعر اليوم / سعر الساعة / سعر الدقيقة** | وحدات اشتقاق الأجر المستخدمة في الخصومات والإضافي، تُعرَّف قواعد اشتقاقها في قسم المعادلات |

---

## د) مبادئ عامة تحكم التنفيذ

هذه مبادئ **ملزمة** تسري على كل متطلبات المستند:

1. **قابلية التعريف بدل التثبيت في الشيفرة**: كل قاعدة مالية (شريحة، معامل، سقف، مدة سماح، فترة) يجب أن تكون **قابلة للتعديل من واجهة المستخدم** بصلاحية، لا مثبّتة في الكود.
2. **اللقطة عند الاحتساب**: كل ما يؤثر في رقم مُعتمد (السياسة، الشرائح، أعضاء المسار، أوقات الوردية، أساس الأيام) يجب أن يُخزَّن **كلقطة** مع المسار، بحيث لا يغيّر أي تعديل لاحق مسارًا محسوبًا أو معتمدًا.
3. **قابلية التفسير**: يجب أن يكون كل رقم في قسيمة الراتب **قابلًا للتفسير**: ما مدخلاته، وأي قاعدة طُبِّقت، وكيف نتج.
4. **أثر التدقيق الكامل**: كل إجراء مالي (إنشاء، احتساب، اعتماد، إعفاء، صرف، تعديل) يُسجَّل: من ومتى وماذا كان قبل وبعد.
5. **الاستهلاك مرة واحدة**: كل قيد فردي (خصم، مكافأة، قسط سلفة) يدخل مسارًا واحدًا فقط ويُعلَّم كمستهلَك.
6. **منع الازدواج**: لا يُحتسب الموظف نفسه في أكثر من مسار لنفس الفترة، ولا يُحتسب اليوم نفسه مرتين في العمل الإضافي.
7. **الصلاحيات على مستوى الإجراء والنطاق**: من يملك إجراءً، وعلى أي مجموعة موظفين يملكه.
8. **الرؤية للموظف**: ما يخصّ الموظف من بدلات وخصومات وسلف وطلبات يجب أن يكون مرئيًا له بحالته الحالية.
9. **عدم التدمير**: التعديل والإلغاء لا يمحوان التاريخ؛ تُستخدم حالات وإلغاءات موثّقة بدل الحذف الفعلي.
10. **التحقق قبل الحفظ**: كل مدخل مالي (معادلة، شريحة، سقف) يُتحقَّق من صحته ومنطقيته قبل قبوله.


---

## ٢) مسارات الرواتب وفترة الاحتساب

يحدّد هذا القسم متطلبات إنشاء **مسار الرواتب (Payroll Run)** — وهو الوعاء الذي يجمع مجموعة من الموظفين على فترة زمنية محدّدة ويُحتسب لهم فيه الاستحقاق — ومتطلبات تعريف **فترة الاحتساب (Pay Period)** وأثرها على المبالغ.

---

### PR-01 — مسار رواتب بنطاق تنظيمي (Organizational Scope Run)

**الوصف**
يجب أن يوفّر النظام إنشاء مسار رواتب باسم يحدّده المستخدم، ويُربط بـ**نطاق تنظيمي (Organizational Scope)** واحد أو أكثر، بحيث يُشتقّ أعضاء المسير آليًا من ذلك النطاق دون إدخال يدوي لأسماء الموظفين.

**مستويات النطاق المطلوب دعمها**

| المستوى | المعرّف المقترح | الدلالة |
|---|---|---|
| الشركة | `company` | كل موظفي الكيان القانوني |
| الفرع | `branch` | موظفو موقع/فرع بعينه |
| القسم | `department` | موظفو وحدة تنظيمية |
| الفريق | `team` | موظفو فريق داخل قسم |
| مركز التكلفة | `cost_center` | موظفون محمّلون على مركز تكلفة محاسبي |

**قواعد العمل**

1. يجب أن يسمح النظام بإنشاء **مسير مستقل لكل فرع**؛ مثال: «مسير فرع الرياض» يضم موظفي فرع الرياض، «مسير فرع المعادي» يضم موظفي فرع المعادي، «مسير فرع النصر» يضم موظفي فرع النصر — وكل مسير منها كيان مستقل بفترته وحالته وأرقامه.
2. عند اختيار أكثر من محدّد نطاق (مثل: فرع = الرياض **و** قسم = المبيعات)، يُطبَّق **الدمج بالتقاطع (AND)** بين المحدِّدات المختلفة النوع، و**الاتحاد (OR)** بين القيم المتعددة داخل النوع الواحد (فرع = الرياض أو جدة).
3. عضوية النطاق تُقاس بحالة الموظف **داخل فترة المسير**، لا بحالته لحظة الإنشاء: يدخل الموظف إذا كان مرتبطًا بالنطاق ولو ليوم واحد ضمن الفترة.
4. يجب أن يستبعد النظام آليًا — مع بيان السبب — كل من: (أ) لم تكن له علاقة عمل سارية في أي يوم من الفترة، (ب) كان مرتبطًا بنطاق آخر طوال الفترة، (ج) وقع عليه حارس ازدواج الصرف في **PR-06**.
5. يجب أن يمنع النظام حفظ مسير بنطاق فارغ النتيجة (صفر أعضاء) إلا بتأكيد صريح من المستخدم مع تسجيل السبب.
6. اسم المسير يجب أن يكون **فريدًا داخل الفترة الواحدة**.

**معايير القبول**

- عند إنشاء مسير بنطاق «فرع = الرياض» على فترة معطاة، تُرجع قائمة الأعضاء **جميع** الموظفين المرتبطين بفرع الرياض ولهم علاقة عمل سارية في يوم واحد على الأقل من الفترة، **ولا تُرجع أحدًا** من فرع آخر.
- إنشاء ثلاثة مسارات لثلاثة فروع على الفترة نفسها ينجح، ولا يظهر أي موظف في أكثر من واحد منها.
- تغيير محدِّد النطاق قبل الاحتساب يُعيد اشتقاق القائمة فورًا ويُظهر الفروق (مضاف/محذوف).
- محاولة حفظ مسير باسم مكرّر على الفترة نفسها تُرفض برسالة تحدّد المسير المتعارض.

---

### PR-02 — مسار بقائمة موظفين مخصّصة (Custom List Run)

**الوصف**
يجب أن يوفّر النظام إنشاء مسار رواتب لا يرتبط بأي نطاق تنظيمي، بل يقوم على **قائمة صريحة** من الموظفين يختارهم المستخدم يدويًا، وقد ينتمي أعضاؤها إلى فروع أو أقسام مختلفة.

**قواعد العمل**

1. يجب أن توفّر الشاشة بحثًا وتصفية (بالفرع/القسم/المسمى/رقم الموظف) لاختيار الموظفين، مع دعم الاختيار المتعدد والإضافة الدفعية عبر لصق قائمة أرقام وظيفية.
2. القائمة **ساكنة (Static)**: لا يُضاف إليها أحد آليًا مهما تغيّرت الهياكل التنظيمية، ولا يُحذف منها أحد آليًا إلا لعدم الأهلية (لا علاقة عمل في الفترة) أو لحارس **PR-06**.
3. يجب أن يرفض النظام إضافة موظف لا تتقاطع علاقة عمله مع فترة المسير، مع بيان السبب.
4. يجب أن يرفض النظام تكرار الموظف نفسه داخل قائمة المسير الواحد.
5. يجب أن يسمح النظام بتصدير القائمة واستيرادها لتسهيل تكرار المسير في الفترات التالية، على أن تُعاد كل عمليات التحقق عند الاستيراد.

**معايير القبول**

- إضافة عشرة موظفين من ثلاثة فروع مختلفة إلى مسير مخصّص واحد تنجح، وتظهر القائمة بالعشرة فقط.
- إضافة موظف مكرّر تُرفض برسالة واضحة دون كسر القائمة القائمة.
- إضافة موظف انتهت خدمته قبل بداية الفترة تُرفض مع بيان تاريخ انتهاء الخدمة.
- بعد نقل أحد أعضاء القائمة إلى فرع آخر، يظل عضوًا في المسير المخصّص كما هو.

---

### PR-03 — مسار لبعض موظفي نطاق واحد: آليتان وفرقهما

**الوصف**
يجب أن يوفّر النظام — داخل النطاق التنظيمي الواحد — طريقتين لبناء مسير يضم **بعض** موظفي ذلك النطاق فقط، لأن السلوك المطلوب عند تغيّر أعضاء النطاق لاحقًا يختلف بين الحالتين اختلافًا جوهريًا.

**الآلية (أ) — نطاق + قائمة استثناء أفراد (Scope with Exclusions)**

- يختار المستخدم النطاق (مثال: فرع الرياض)، ثم يرفع من القائمة المشتقّة أفرادًا بعينهم إلى **قائمة استثناء (Exclusion List)** مع سبب لكل استثناء.
- القائمة تبقى **ديناميكية**: العضوية = (كل من ينطبق عليه النطاق) − (المستثنون).
- تُستخدم عندما تكون القاعدة هي «الفرع كله ما عدا حالات معدودة» (مثال: ما عدا ثلاثة موظفين يُصرفون في مسير الإدارة العليا).

**الآلية (ب) — قائمة صريحة داخل النطاق (Explicit In-Scope List)**

- يختار المستخدم النطاق للتصفية فقط، ثم يؤشّر يدويًا على من يدخل المسير.
- القائمة تبقى **ساكنة**: العضوية = ما أُشّر عليه صراحةً، ولا شيء غيره.
- تُستخدم عندما تكون القاعدة هي «مجموعة معدودة من الفرع» (مثال: ستة موظفين بعقود مختلفة داخل فرع النصر).

**فرق السلوك عند إضافة موظف جديد إلى الفرع لاحقًا**

| الحدث | الآلية (أ) نطاق + استثناء | الآلية (ب) قائمة صريحة |
|---|---|---|
| موظف جديد يُضاف للفرع **قبل تجميد لقطة الأعضاء** | **يدخل المسير آليًا** ويظهر في المعاينة كسطر «مضاف جديد» | **لا يدخل**؛ يظهر فقط في تقرير «موظفون بلا مسير» (PR-07) |
| موظف جديد يُضاف للفرع **بعد تجميد اللقطة** | لا يدخل هذا المسير (اللقطة مجمّدة)، ويدخل المسير التالي آليًا | لا يدخل هذا المسير ولا التالي إلا بإضافة يدوية |
| موظف نُقل خارج الفرع | يخرج آليًا من الفترات التالية | يبقى في القائمة حتى يُحذف يدويًا |
| نسخ المسير لفترة جديدة | يُعاد اشتقاق النطاق بالكامل، وتُنقل قائمة الاستثناء كما هي | تُنقل القائمة الصريحة كما هي دون أي إضافة |

**قواعد العمل**

1. يجب أن يخزّن النظام **وضع الاختيار (Selection Mode)** على المسير نفسه بقيمة من: `dynamic_scope` · `dynamic_scope_with_exclusions` · `explicit_list`، وأن يعرضه بوضوح في رأس شاشة المسير.
2. يجب أن يوفّر النظام تحويل مسير من الآلية (أ) إلى (ب) بتثبيت القائمة الحالية كقائمة صريحة، مع تسجيل العملية في أثر التدقيق، ومنع التحويل بعد حالة «معتمد».
3. كل استثناء يجب أن يحمل **سببًا إلزاميًا** من قائمة معرّفة (مصروف في مسير آخر · موقوف بلا أجر · تسوية نهاية خدمة منفصلة · أخرى + نص حر).
4. عند نسخ مسير لفترة جديدة، يجب أن يعرض النظام تنبيهًا يوضّح كم موظفًا سيدخل آليًا وكم سيبقى مستثنى.

**معايير القبول**

- في مسير بالآلية (أ) على فرع فيه 40 موظفًا مع استثناء 3، تُظهر المعاينة 37 عضوًا و3 مستثنين بأسبابهم.
- بعد إضافة موظف جديد للفرع والمسير في حالة «مسودة»، تُظهر إعادة المعاينة 38 عضوًا للآلية (أ) و**لا تتغير** أعداد الآلية (ب).
- محاولة حفظ استثناء بلا سبب تُرفض.
- تحويل مسير من (أ) إلى (ب) يثبّت العدد الحالي، وإضافة موظف جديد للفرع بعدها لا تغيّر العدد.

---

### PR-04 — معاينة الأعضاء قبل الاحتساب (Membership Preview)

**الوصف**
يجب أن يوفّر النظام — قبل تنفيذ أي عملية حساب — شاشة معاينة تعرض **من سيدخل المسير**، و**من استُبعد ولماذا**، بحيث لا يُفاجأ المستخدم بالنتيجة بعد الاحتساب.

**قواعد العمل**

1. المعاينة **عملية قراءة فقط**: لا تُنشئ قيودًا ولا تحجز الموظف ولا تغيّر حالة المسير.
2. يجب أن تعرض المعاينة كتلتين:
   - **الداخلون**: رقم الموظف · الاسم · النطاق الذي أدخله · تاريخ الالتحاق · تاريخ انتهاء الخدمة إن وُجد · **أيام التغطية** · **معامل التناسب** · سبب الدخول (نطاق / إضافة يدوية).
   - **المستبعدون**: رقم الموظف · الاسم · **سبب الاستبعاد** بكود ونص.
3. أكواد الاستبعاد المطلوب دعمها كحدّ أدنى:

| الكود | السبب |
|---|---|
| `EXC_NO_ACTIVE_EMPLOYMENT` | لا توجد علاقة عمل سارية في أي يوم من الفترة |
| `EXC_TERMINATED_BEFORE_PERIOD` | انتهت الخدمة قبل بداية الفترة |
| `EXC_JOINS_AFTER_PERIOD` | تاريخ الالتحاق بعد نهاية الفترة |
| `EXC_MANUAL_EXCLUSION` | مستثنى يدويًا (مع السبب المُدخل) |
| `EXC_ALREADY_IN_RUN` | مُدرج في مسير آخر غير ملغى لنفس الفترة (PR-06) |
| `EXC_OUT_OF_SCOPE` | خارج النطاق المحدّد |
| `EXC_ZERO_COVERAGE` | أيام التغطية داخل الفترة = صفر |

4. يجب أن تعرض المعاينة **إجماليات ملخّصة**: عدد الداخلين · عدد المستبعدين · عدد أصحاب التناسب الجزئي · مجموع الأساسي قبل البنود.
5. يجب أن توفّر المعاينة **تصديرًا** (جدول قابل للتنزيل) للمراجعة خارج النظام.
6. يجب أن يمنع النظام الانتقال إلى الاحتساب إذا وُجد **تعارض حاجب** (ازدواج صرف مثلًا) لم يُعالَج بعد.

**معايير القبول**

- تشغيل المعاينة مرتين متتاليتين بلا تغيير في البيانات يعطي النتيجة نفسها بالضبط.
- كل سطر مستبعد يحمل كودًا ونصًا مفهومًا؛ لا يوجد سطر بسبب فارغ.
- مجموع (الداخلون + المستبعدون) = مجموع من انطبق عليهم النطاق أو القائمة قبل الفلترة.
- المعاينة لا تُغيّر حالة المسير ولا تُنشئ لقطة أعضاء.
- وجود تعارض ازدواج صرف واحد على الأقل يُعطّل زر «احتساب» ويعرض التفاصيل.

---

### PR-05 — لقطة الأعضاء (Membership Snapshot)

**الوصف**
يجب أن يجمّد النظام — لحظة تنفيذ الاحتساب — قائمة الأعضاء وبياناتهم المؤثّرة في الحساب داخل **لقطة أعضاء** ثابتة تُخزَّن على المسير، ولا تتأثر بعد ذلك بأي تغيير لاحق في بيانات الموظف أو الهيكل التنظيمي.

**لماذا اللقطة ضرورية**

1. **قابلية التدقيق (Auditability)**: يجب أن يُعاد إنتاج أرقام أي مسير قديم كما هي بعد شهور، حتى لو تغيّرت رواتب الموظفين وفروعهم ومسمياتهم بعدها.
2. **ثبات القيمة المصروفة**: تعديل راتب موظف بعد الاعتماد يجب ألا يغيّر مسيرًا سبق اعتماده أو صرفه.
3. **تفسير الفروق**: مقارنة مسير الشهر بالشهر السابق تتطلب مرجعًا ثابتًا لكل فترة.
4. **حجز الموظف**: اللقطة هي الأساس الذي يبني عليه حارس ازدواج الصرف (PR-06).
5. **الامتثال والمنازعات**: عند مراجعة خارجية أو خلاف مع موظف، يجب إبراز البيانات كما كانت **وقت الحساب** لا كما هي اليوم.

**الحقول التي يجب تجميدها في اللقطة لكل عضو**

| الحقل | المعرّف المقترح | ملاحظة |
|---|---|---|
| الموظف | `employee_id` | مرجع ثابت |
| الاسم ورقم الموظف وقت الحساب | `employee_name_snapshot`, `employee_code_snapshot` | نصّي مجمَّد |
| الفرع/القسم/الفريق/مركز التكلفة | `branch_snapshot`, `department_snapshot`, `team_snapshot`, `cost_center_snapshot` | نصّي مجمَّد |
| الراتب الأساسي والبنود الثابتة | `base_salary_snapshot` | القيمة السارية داخل الفترة |
| تاريخ الالتحاق / انتهاء الخدمة | `hire_date_snapshot`, `termination_date_snapshot` | يحكم التناسب |
| أيام التغطية | `coverage_days` | محسوبة |
| أساس أيام الاحتساب | `day_basis_value` | من PR-09 |
| معامل التناسب | `prorata_factor` | من PR-10 |
| سبب العضوية | `membership_reason` | نطاق / إضافة يدوية |
| مؤشر الاستثناء من الحضور | `attendance_exempt_snapshot` | يؤثر في بنود لاحقة |

**قواعد العمل**

1. تُنشأ اللقطة عند أول انتقال إلى حالة «محسوب»، وتُستبدل بالكامل عند كل **إعادة حساب** مسموح بها، مع الاحتفاظ بنسخة اللقطة السابقة في سجل الإصدارات.
2. بعد حالة «معتمد» تصبح اللقطة **غير قابلة للتغيير (Immutable)** نهائيًا.
3. أي تغيير في بيانات الموظف بعد إنشاء اللقطة **لا ينعكس** على المسير إلا بإعادة حساب صريحة في حالة تسمح بها (PR-11).
4. يجب أن يعرض النظام «فرق اللقطة (Snapshot Diff)» بين الإصدار الحالي والسابق عند إعادة الحساب: من دخل، من خرج، ما القيم التي تغيّرت.
5. حذف موظف أو أرشفته لاحقًا يجب ألا يكسر قراءة اللقطة أو تقاريرها.

**معايير القبول**

- بعد الاحتساب، تعديل راتب أحد الأعضاء لا يغيّر أي رقم في المسير حتى تُطلب إعادة حساب.
- بعد الاعتماد، أي محاولة تعديل للقطة تُرفض بكود خطأ مخصّص.
- إعادة الحساب في حالة مسموح بها تُنتج إصدار لقطة جديدًا وتُبقي السابق قابلًا للاستعراض.
- نقل عضو إلى فرع آخر بعد الاحتساب يُبقي `branch_snapshot` على قيمته وقت الحساب.

---

### PR-06 — منع ازدواج الصرف (Duplicate Payment Guard)

**الوصف**
يجب ألا يُصرف الموظف نفسه في أكثر من مسير رواتب لنفس فترة الاحتساب. ويجب أن يفرض النظام ذلك على مستوى قاعدة البيانات (قيد صلب) وعلى مستوى التحقق التطبيقي (رسالة مفهومة قبل الوصول للقيد).

**تصميم الحارس**

```
جدول: employee_period_claim   (حجز الموظف على فترة)
--------------------------------------------------
  id                 PK
  employee_id        FK -> employee
  payroll_run_id     FK -> payroll_run
  period_start_date  DATE      -- لقطة من المسير
  period_end_date    DATE      -- لقطة من المسير
  run_type           ENUM(regular, supplementary, off_cycle)
  is_active          BOOLEAN   -- يصبح false عند إلغاء المسير

قيد فريد مُرشَّح (Filtered Unique Index):
  UNIQUE (employee_id, period_start_date, period_end_date)
  WHERE is_active = TRUE AND run_type = 'regular'
```

**قواعد العمل**

1. تُكتب صفوف الحجز **ذرّيًا (Atomically)** مع إنشاء لقطة الأعضاء داخل معاملة واحدة (Transaction): إما تنجح اللقطة والحجوزات معًا أو تفشل معًا.
2. **المسير الملغى لا يحجز**: عند الانتقال إلى «ملغى» تُضبط `is_active = FALSE` لكل حجوزاته، فيصبح الموظف متاحًا لمسير آخر لنفس الفترة.
3. **التطابق التام** في الفترة يمنعه القيد الفريد. أما **التداخل الجزئي** (فترتان مختلفتان تتقاطعان في يوم واحد على الأقل) فيجب أن يُكتشف بتحقّق تطبيقي ويُعامَل كخطأ حاجب، مع إمكانية تجاوزه **فقط** بصلاحية مخوّلة وسبب مكتوب يُسجَّل في أثر التدقيق.

```
شرط اكتشاف التداخل بين مسيرين لنفس الموظف:
  (A.period_start_date <= B.period_end_date) AND (A.period_end_date >= B.period_start_date)
```

4. **المسير التكميلي (Supplementary)**: يجب أن يسمح النظام بمسير تكميلي/خارج الدورة لنفس الفترة لمعالجة فروق أو مستحقات متأخرة، بشرط أن يرتبط بالمسير الأصلي (`parent_run_id`) وأن يكون `run_type ≠ regular` — وهو معفى من القيد الفريد لكنه يظل خاضعًا لأثر التدقيق ولتقرير التسويات.
5. يجب أن يُطبَّق الفحص في ثلاث نقاط: **المعاينة** (تحذير وسبب استبعاد)، **قبل الاحتساب** (منع)، و**عند الكتابة** (القيد الصلب كخط دفاع أخير).
6. يجب أن يوفّر النظام شاشة «تعارضات الصرف» تعرض كل الحالات المكتشفة مع روابط للمسير المتعارض وإجراءات المعالجة (استثناء من هنا · إلغاء هناك · اعتماد التكميلي).

**رسائل الخطأ المطلوبة**

| الكود | الرسالة المعروضة |
|---|---|
| `PAYRUN-DUP-001` | «لا يمكن إدراج الموظف {الاسم} ({الرقم الوظيفي}) في هذا المسير: هو مُدرج بالفعل في المسير «{اسم المسير المتعارض}» لنفس الفترة ({من} – {إلى}). استبعده من أحد المسيرين أو ألغِ المسير الآخر قبل المتابعة.» |
| `PAYRUN-DUP-002` | «تعارض تداخل فترات: الموظف {الاسم} مُدرج في المسير «{اسم المسير}» على فترة ({من} – {إلى}) تتقاطع مع فترة هذا المسير في {عدد} يومًا. المتابعة تتطلب صلاحية تجاوز وسببًا مكتوبًا.» |
| `PAYRUN-DUP-003` | «تعذّر إتمام الاحتساب: اكتُشف حجز متزامن للموظف {الاسم} من مسير آخر. أعد المعاينة ثم الاحتساب.» |

**معايير القبول**

- احتساب مسيرين مختلفين يضمّان الموظف نفسه على الفترة نفسها: الثاني يفشل بالكود `PAYRUN-DUP-001` ولا يُنشئ لقطة جزئية.
- إلغاء المسير الأول ثم احتساب الثاني: ينجح.
- محاولة احتساب مسيرين متزامنين على الموظف نفسه من جلستين في اللحظة نفسها: ينجح واحد فقط، ويفشل الآخر بـ `PAYRUN-DUP-003` دون بيانات معلّقة.
- مسير تكميلي مرتبط بمسير أصلي على الفترة نفسها: ينجح ويظهر في تقرير التسويات.
- تجاوز تداخل جزئي بلا صلاحية: يُرفض؛ ومع الصلاحية: ينجح ويُسجَّل السبب والمستخدم والوقت.

---

### PR-07 — تقرير «موظفون بلا مسير» (Unassigned Employees Report)

**الوصف**
يجب أن يوفّر النظام تقريرًا يحصر كل من كان **على رأس العمل** خلال فترة معطاة ولم يدخل أي مسير رواتب غير ملغى لتلك الفترة — بوصفه حاجزًا وقائيًا ضد نسيان صرف موظف.

**قواعد العمل**

1. مدخلات التقرير: الفترة (بداية/نهاية) أو دورة شهرية، مع تصفية اختيارية بالفرع/القسم/الفريق/مركز التكلفة.
2. يُعرِّف التقرير «على رأس العمل» بأن للموظف علاقة عمل سارية في **يوم واحد على الأقل** من الفترة.
3. المخرجات لكل سطر: رقم الموظف · الاسم · الفرع/القسم · تاريخ الالتحاق · تاريخ انتهاء الخدمة إن وُجد · **أيام التغطية داخل الفترة** · **السبب المرجّح** لعدم الإدراج.
4. الأسباب المرجّحة المطلوب استنتاجها آليًا: خارج نطاق كل المسيرات المنشأة · مستثنى يدويًا في مسير (مع اسم المسير) · التحق بعد تجميد لقطة كل المسيرات · عضو في مسير ملغى فقط · مسير عضويته لم يُحتسب بعد.
5. يجب أن يوفّر التقرير **إجراءً مباشرًا** لإضافة الموظف إلى مسير قائم (في حالة تسمح بالتعديل) أو إنشاء مسير جديد يضمّه.
6. يجب أن يكون التقرير قابلًا للتشغيل في أي وقت، وأن يُعرَض **إلزاميًا** كخطوة تحقق قبل السماح باعتماد آخر مسير في الفترة، مع إمكانية المتابعة بتأكيد صريح.
7. يجب أن يستثني التقرير — مع إمكانية إظهارهم بمفتاح — الفئات المعرّفة كغير مستحقة للصرف في تلك الفترة (مثل الموقوفين بلا أجر) بشرط أن يكون التوقيف مسجّلًا في النظام.

**معايير القبول**

- إنشاء ثلاثة مسيرات فروع تغطي كل الموظفين: التقرير يعود فارغًا.
- إضافة موظف جديد لفرع بعد تجميد لقطة مسيره: يظهر في التقرير بسبب «التحق بعد تجميد اللقطة».
- استثناء موظف يدويًا من مسير دون إدراجه في غيره: يظهر في التقرير مع اسم المسير الذي استُثني منه.
- موظف عضو في مسير ملغى فقط: يظهر في التقرير.
- عدد أسطر التقرير + عدد أعضاء كل مسيرات الفترة (بلا تكرار) = عدد من كانت لهم علاقة عمل سارية في الفترة.

---

### PR-08 — فترة الاحتساب (Pay Period)

**الوصف**
يجب أن يمكّن المستخدم من تحديد الفترة التي تُحتسب عليها الرواتب — **من يوم كذا إلى يوم كذا** — بنمطين، وأن تُخزَّن الفترة الناتجة كتاريخين صريحين (لقطة) على المسير.

**النمط (أ) — دورة شهرية (Monthly Cycle)**

- يحدّد المستخدم **يوم البداية** و**يوم النهاية** (رقم اليوم في الشهر)، مثال: من **23** من الشهر إلى **22** من الشهر التالي.
- يختار المستخدم **الشهر المرجعي (Reference Month)**، فيولّد النظام التاريخين الفعليين آليًا ويعرضهما للتأكيد.
- إذا كان يوم النهاية **أصغر من أو يساوي** يوم البداية، تمتد الفترة إلى الشهر التالي؛ وإلا فهي داخل الشهر نفسه.

**النمط (ب) — فترة مخصّصة (Custom Range)**

- يُدخل المستخدم **تاريخ بداية** و**تاريخ نهاية** صريحين، بأي طول (أسبوع، نصف شهر، فترة انتقالية).

**قواعد العمل**

1. الفترة تُخزَّن دائمًا كحقلَي تاريخ صريحين على المسير: `period_start_date` و`period_end_date`، **شاملين للطرفين (Inclusive)**، ويُخزَّن معهما نمط التوليد ومعاملاته للرجوع إليها.
2. بعد الانتقال إلى حالة «محسوب» تصبح الفترة **مجمّدة**؛ تغييرها يستلزم العودة إلى «مسودة» بإلغاء الاحتساب، ويُمنع نهائيًا بعد «معتمد».
3. **قصّ يوم الشهر (Day Clamping)**: إذا كان رقم اليوم المطلوب غير موجود في الشهر (مثل 31 في شهر من 30 يومًا، أو 29/30/31 في فبراير)، يُقصّ إلى **آخر يوم في ذلك الشهر**، ويُعرض التاريخ الناتج للمستخدم قبل الحفظ.
4. يجب أن يرفض النظام: تاريخ نهاية أسبق من تاريخ البداية · فترة تتجاوز حدًا أقصى معرّفًا (مثل 62 يومًا) دون تأكيد صريح · فترة طولها صفر.
5. يجب أن يكشف النظام **الفجوات والتداخلات** بين فترات المسيرات المتتابعة للنطاق نفسه ويحذّر منها (مثال: انتهت فترة عند 22 وبدأت التالية عند 24 → فجوة يوم واحد لم يُصرف).
6. يجب أن يدعم النظام حفظ **قوالب دورات** معرّفة مسبقًا (مثل «دورة 23→22») لإعادة استخدامها عبر المسيرات.
7. عبور نهاية السنة يجب أن يعمل دون استثناء (مثال: 23 ديسمبر → 22 يناير)، مع إسناد المسير إلى السنة المالية وفق قاعدة معرّفة (شهر النهاية افتراضيًا).

```
توليد النمط (أ) — دورة شهرية:
  المعطيات: cycle_start_day = 23 ، cycle_end_day = 22 ، الشهر المرجعي = أغسطس 2026
  بما أن 22 <= 23 → الفترة تعبر إلى الشهر التالي، والشهر المرجعي هو شهر النهاية
  period_start_date = 23 / يوليو / 2026        (بعد القصّ إن لزم)
  period_end_date   = 22 / أغسطس / 2026        (بعد القصّ إن لزم)
  period_days       = 31 يومًا (شاملة الطرفين)
```

**حالات حدّية**

| الحالة | السلوك المطلوب |
|---|---|
| `cycle_start_day = 31` وشهر البداية 30 يومًا | يُقصّ إلى 30 من ذلك الشهر |
| `cycle_end_day = 30` وشهر النهاية فبراير | يُقصّ إلى 28 أو 29 بحسب السنة الكبيسة |
| `cycle_start_day = cycle_end_day` | تُفسَّر كدورة شهر كامل تنتهي في اليوم نفسه من الشهر التالي، مع عرض التاريخين للتأكيد |
| تغيير أيام الدورة بين فترتين متتاليتين | تحذير بالفجوة أو التداخل الناتج وعدد أيامه، مع طلب تأكيد |
| فترة تعبر نهاية السنة | تعمل عاديًا؛ يُحدَّد انتماء السنة المالية بقاعدة معرّفة |

**معايير القبول**

- إدخال دورة 23→22 بشهر مرجعي أغسطس 2026 يولّد بالضبط 23/07/2026 – 22/08/2026 وطولًا 31 يومًا.
- إدخال دورة 31→30 على شهر من 30 يومًا يولّد تاريخ بداية = آخر يوم في ذلك الشهر ويعرضه قبل الحفظ.
- إدخال فترة مخصّصة تنتهي قبل بدايتها يُرفض.
- بعد الاحتساب، محاولة تعديل الفترة تُرفض إلا بعد إلغاء الاحتساب والعودة لمسودة.
- إنشاء مسيرين متتابعين بفجوة يوم واحد يُظهر تحذير الفجوة مع تحديد اليوم غير المغطّى.

---

### PR-09 — أساس أيام الاحتساب (Day Basis)

**الوصف**
يجب أن يوفّر النظام اختيار **أساس أيام الاحتساب** الذي يُقسَّم عليه الراتب لاستخراج **سعر اليوم (Daily Rate)**، وأن يُخزَّن الخيار وقيمته المحسوبة كلقطة على المسير، لأنه يغيّر قيمة كل بند مرتبط بالأيام (التناسب، خصم الغياب، خصم التأخير المُعبَّر عنه بأجزاء يوم).

**الخيارات المطلوب دعمها**

| الخيار | المعرّف المقترح | قيمة الأساس | متى يُستخدم |
|---|---|---|---|
| ثابت 30 يومًا | `fixed_30` | 30 دائمًا | لتوحيد سعر اليوم عبر كل الشهور بغضّ النظر عن طول الفترة |
| أيام الفترة الفعلية | `actual_period_days` | عدد أيام الفترة شاملة الطرفين (28…31) | لعدالة تقويمية دقيقة لكل فترة على حدة |
| أيام العمل المجدولة | `scheduled_work_days` | عدد أيام الجدول المقرّرة داخل الفترة (بلا عطل أسبوعية ولا عطل رسمية) | حين يُراد ربط الاستحقاق بأيام الدوام لا بأيام التقويم |

```
سعر اليوم:
  daily_rate = monthly_gross / day_basis_value

مثال بأثر الخيار على راتب 9,000 ر.س/ج.م لفترة 31 يومًا فيها 21 يوم عمل مجدولًا:
  fixed_30            → 9,000 / 30 = 300.00  لليوم
  actual_period_days  → 9,000 / 31 = 290.32  لليوم
  scheduled_work_days → 9,000 / 21 = 428.57  لليوم
```

**قواعد العمل**

1. يُحدَّد الأساس على مستوى المسير عند الإنشاء، ويجب أن يُخزَّن كلٌّ من: **نوع الأساس** و**القيمة العددية المحسوبة** (`day_basis_value`) داخل لقطة المسير.
2. عند اختيار `scheduled_work_days`، تُحسب قيمة الأساس **لكل موظف على حدة** إذا اختلفت جداولهم، وتُخزَّن في سطر عضويته باللقطة؛ ويُعرض متوسط/مدى القيم في رأس المسير.
3. عند اختيار `scheduled_work_days` وكانت أيام الموظف المجدولة داخل الفترة = صفر، يجب أن يُستبعد الموظف من الاحتساب مع كود سبب واضح، أو يُطبَّق أساس احتياطي معرّف — والسلوك يجب أن يكون خيارًا معرّفًا لا سلوكًا ضمنيًا.
4. تغيير الأساس بعد الاحتساب يستلزم **إعادة حساب كاملة**، ويُمنع بعد الاعتماد.
5. يجب أن يستخدم كل بند يعتمد على «قيمة اليوم» في النظام **نفس** `day_basis_value` المخزّن على المسير، ضمانًا لاتساق التناسب مع الخصومات.
6. يجب أن يعرض النظام في رأس المسير بوضوح: «الأساس: {النوع} = {القيمة} يومًا» حتى لا يقع لبس عند المراجعة.

**معايير القبول**

- تغيير الأساس من `fixed_30` إلى `actual_period_days` على فترة 31 يومًا يغيّر سعر اليوم من 300.00 إلى 290.32 لراتب 9,000، وينعكس على كل البنود المرتبطة بالأيام.
- قيمة الأساس المخزّنة على المسير تساوي بالضبط القيمة المستخدمة في كل سطور الحساب.
- موظف بلا أيام مجدولة داخل الفترة تحت أساس `scheduled_work_days` يُعامل وفق الخيار المعرّف، ويظهر السبب في المعاينة.
- بعد الاعتماد، تغيير الأساس مرفوض بكود خطأ مخصّص.

---

### PR-10 — التناسب (Pro-rata)

**الوصف**
يجب أن يحتسب النظام للموظف الذي **التحق داخل الفترة** أو **انتهت خدمته داخل الفترة** استحقاقًا **بنسبة أيام تغطيته** فقط، لا الاستحقاق الشهري الكامل.

**المعادلة**

```
أيام التغطية (Coverage Days) — شاملة للطرفين:
  effective_from = MAX(period_start_date, hire_date)
  effective_to   = MIN(period_end_date,  COALESCE(termination_date, period_end_date))
  coverage_days  = (effective_to - effective_from) + 1        ... إذا كان effective_to >= effective_from
  coverage_days  = 0                                          ... خلاف ذلك

معامل التناسب (Pro-rata Factor):
  prorata_factor = MIN( coverage_days / day_basis_value , 1 )

المستحق المتناسب:
  prorated_amount = ROUND( full_amount * prorata_factor , 2 )
```

**قواعد العمل**

1. **التغطية الكاملة لا تتناسب**: إذا غطّى الموظف الفترة كلها (`effective_from = period_start_date` و`effective_to = period_end_date`)، يُثبَّت `prorata_factor = 1` مباشرة دون قسمة — منعًا لتضخّم المعامل فوق الواحد حين يكون طول الفترة أكبر من الأساس (مثل فترة 31 يومًا مع أساس `fixed_30`).
2. **سقف المعامل**: المعامل لا يتجاوز 1 في أي حال.
3. يوما الالتحاق وانتهاء الخدمة **يُحتسبان ضمن التغطية** (شاملان للطرفين).
4. **تعدّد فترات التغطية**: إذا كان للموظف أكثر من علاقة عمل داخل الفترة (انتهت خدمته ثم أُعيد تعيينه)، تُحسب أيام التغطية كـ**مجموع** أيام الفترات المتقاطعة، مع منع ازدواج احتساب الأيام المشتركة، وتُعرض الفترات منفصلة في تفصيل الاحتساب.
5. يجب أن يحدّد النظام — كخيار معرّف على مستوى الشركة — **أي البنود تتناسب وأيها لا**: الراتب الأساسي والبدلات الدائمة تتناسب افتراضيًا؛ البنود المقطوعة لمرة واحدة (مكافأة، سلفة، خصم مقطوع) لا تتناسب. ويجب أن يحمل كل نوع بند سمة `is_proratable`.
6. **التقريب**: يُحتسب التناسب على القيمة الكاملة لكل بند ثم يُقرَّب إلى منزلتين عشريتين لكل بند؛ ويجب معالجة فرق التقريب في الإجمالي بحيث لا يتجاوز الفارق قرشًا/هللة واحدة، مع إسناده إلى أكبر بند.
7. يجب أن يعرض تفصيل الاحتساب لكل موظف متناسب: تواريخ التغطية · أيام التغطية · الأساس · المعامل · القيمة قبل التناسب · القيمة بعده.

**حالات حدّية**

| الحالة | السلوك المطلوب |
|---|---|
| التحق وانتهت خدمته داخل الفترة نفسها | التغطية = ما بين التاريخين شاملة الطرفين |
| التحق في آخر يوم من الفترة | `coverage_days = 1` والمعامل يساوي 1 مقسومًا على الأساس |
| انتهت خدمته في أول يوم من الفترة | `coverage_days = 1` |
| تاريخ الالتحاق بعد نهاية الفترة | استبعاد بكود `EXC_JOINS_AFTER_PERIOD` |
| تاريخ انتهاء الخدمة قبل بداية الفترة | استبعاد بكود `EXC_TERMINATED_BEFORE_PERIOD` |
| تغطية = صفر | استبعاد بكود `EXC_ZERO_COVERAGE` وعدم إنشاء سطر استحقاق |
| أساس `scheduled_work_days` | تُحسب أيام التغطية بأيام الجدول المقرّرة داخل مدى التغطية، لا بأيام التقويم |

**مثال رقمي (تناسب فرد)**

```
المعطيات:
  الفترة            : 23/07/2026 → 22/08/2026   (31 يومًا)
  الأساس            : fixed_30 → day_basis_value = 30
  الموظفة           : ليلى — تاريخ الالتحاق 05/08/2026
  الراتب الشهري     : 6,000 ر.س/ج.م

الحساب:
  effective_from = MAX(23/07/2026 , 05/08/2026) = 05/08/2026
  effective_to   = MIN(22/08/2026 , —)          = 22/08/2026
  coverage_days  = (22 - 5) + 1 = 18 يومًا
  prorata_factor = MIN(18 / 30 , 1) = 0.600000
  prorated_amount = 6,000 × 0.600000 = 3,600.00

النتيجة: 3,600.00 ر.س/ج.م
```

**معايير القبول**

- موظف غطّى الفترة كلها على أساس `fixed_30` وفترة 31 يومًا يحصل على **كامل** راتبه بلا زيادة (المعامل = 1 لا 1.0333).
- موظف التحق في منتصف الفترة يحصل على قيمة تساوي المعادلة أعلاه بدقة منزلتين عشريتين.
- موظف انتهت خدمته داخل الفترة يُحتسب له حتى يوم انتهاء الخدمة شاملًا.
- بند مُعرَّف بـ `is_proratable = false` لا يتأثر بالمعامل.
- مجموع البنود بعد التقريب يطابق الإجمالي المعروض بلا فروق تتجاوز وحدة عملة صغرى واحدة.
- تفصيل الاحتساب يعرض التواريخ والأيام والمعامل بحيث يمكن للمراجع إعادة الحساب يدويًا والوصول للرقم نفسه.

---

### PR-11 — دورة حياة المسير (Payroll Run Lifecycle)

**الوصف**
يجب أن يمر مسار الرواتب بحالات محدّدة ومتتابعة، لكل حالة صلاحياتها وما تقفله من عناصر، بحيث يُمنع تعديل الأرقام بعد الاعتماد.

**الحالات والانتقالات**

```
  [مسودة draft]
        │ احتساب
        ▼
  [محسوب calculated] ──────► [مراجعة in_review] ──────► [معتمد approved] ──────► [مصروف paid]
        │  إعادة حساب ↺           │ إعادة للتعديل ↺            │
        │                         └────────────► [مسودة]        │
        └──────────── إلغاء ──────────────────────────────────► [ملغى cancelled]
                       (متاح من draft / calculated / in_review / approved بصلاحية)
  [مصروف paid] ──► لا انتقال إلا عكس صرف موثّق بصلاحية عليا يُنشئ مسير تسوية مرتبطًا
```

**ما يُقفل في كل حالة**

| الحالة | الاسم | النطاق والقائمة | الفترة والأساس | الأعضاء واللقطة | البنود والمبالغ | إعادة الحساب |
|---|---|---|---|---|---|---|
| مسودة `draft` | قابل | قابل | قابل | لا لقطة بعد | لا مبالغ بعد | — |
| محسوب `calculated` | قابل | مقفول | مقفول | مقفولة (تُستبدل بإعادة حساب) | مقفولة | **مسموح** |
| مراجعة `in_review` | مقفول | مقفول | مقفول | مقفولة | مقفولة (تُقبل ملاحظات المراجع فقط) | **مسموح بصلاحية**، ويُعيد الحالة إلى «محسوب» |
| معتمد `approved` | مقفول | مقفول | مقفول | **غير قابلة للتغيير** | **غير قابلة للتغيير** | **ممنوع** |
| مصروف `paid` | مقفول | مقفول | مقفول | غير قابلة للتغيير | غير قابلة للتغيير | ممنوع |
| ملغى `cancelled` | مقفول | مقفول | مقفول | محفوظة للأرشيف | محفوظة للأرشيف | ممنوع |

**قواعد العمل**

1. **فصل الصلاحيات (Segregation of Duties)**: يجب أن يمنع النظام أن يكون **المعتمِد** هو نفسه **المُحتسِب** أو **المراجِع**، ما لم تُفعَّل رخصة صريحة للمنشآت الصغيرة، ويُسجَّل التفعيل.
2. الانتقال إلى «محسوب» يُنشئ لقطة الأعضاء وحجوزات الفترة ذرّيًا (PR-05، PR-06).
3. **إعادة الحساب** مسموحة في «محسوب» و«مراجعة» فقط، وتؤدي إلى: إصدار لقطة جديد · إبطال ملاحظات مراجعة سابقة إن تغيّرت الأرقام · زيادة عدّاد إعادة الحساب · تسجيل سبب إلزامي.
4. العودة من «مراجعة» إلى «مسودة» تُحرِّر حجوزات الفترة وتُبقي اللقطة السابقة في الأرشيف.
5. الانتقال إلى «معتمد» يستلزم: عدم وجود تعارضات مفتوحة · اطّلاعًا مسجّلًا على تقرير «موظفون بلا مسير» (PR-07) · عدم وجود أعضاء بحالة خطأ في الاحتساب.
6. الانتقال إلى «مصروف» يستلزم أن يكون المسير «معتمدًا»، ويُسجَّل معه تاريخ الصرف وقناته ومرجعه.
7. **الإلغاء** يستلزم سببًا إلزاميًا، ويُبطل حجوزات الفترة (`is_active = FALSE`)، ولا يحذف أي بيانات — الحذف الفعلي للمسير ممنوع في كل الحالات.
8. أي محاولة تعديل مرفوضة يجب أن تُرجع كود خطأ يوضّح الحالة الحالية والحالة المطلوبة للتعديل، لا رسالة عامة.

**رسائل الخطأ المطلوبة**

| الكود | الرسالة |
|---|---|
| `PAYRUN-STATE-001` | «لا يمكن تنفيذ «{الإجراء}» والمسير في حالة «{الحالة الحالية}». الإجراء متاح في الحالات: {قائمة}.» |
| `PAYRUN-STATE-002` | «المسير معتمد ولا يقبل التعديل أو إعادة الحساب. لتصحيحه: ألغِ الاعتماد بصلاحية مخوّلة، أو أنشئ مسيرًا تكميليًا مرتبطًا.» |
| `PAYRUN-STATE-003` | «تعذّر الاعتماد: لا يجوز أن يكون المعتمِد هو من نفّذ الاحتساب أو المراجعة.» |

**معايير القبول**

- تعديل عضو في مسير «معتمد» يُرفض بالكود `PAYRUN-STATE-002`.
- إعادة الحساب في «محسوب» تزيد العدّاد بواحد وتُنشئ إصدار لقطة جديدًا وتحفظ السبب.
- إلغاء مسير يحرّر أعضاءه لمسير آخر على الفترة نفسها.
- محاولة الاعتماد بواسطة نفس المستخدم الذي احتسب تُرفض بالكود `PAYRUN-STATE-003` (ما لم تُفعَّل الرخصة).
- لا يوجد أي مسار في النظام يحذف مسيرًا حذفًا نهائيًا.
- الانتقال إلى «مصروف» من حالة غير «معتمد» يُرفض.

---

### PR-12 — أثر التدقيق (Audit Trail)

**الوصف**
يجب أن يسجّل النظام لكل مسير — بشكل غير قابل للتعديل — **من فعل ماذا ومتى**، بحيث يمكن لأي مراجع لاحق تتبّع المسير من إنشائه إلى صرفه.

**الحقول الملخّصة على المسير**

| الحدث | حقول مقترحة |
|---|---|
| الإنشاء | `created_by`, `created_at` |
| الاحتساب | `calculated_by`, `calculated_at` |
| المراجعة | `reviewed_by`, `reviewed_at`, `review_notes` |
| الاعتماد | `approved_by`, `approved_at` |
| الصرف | `paid_by`, `paid_at`, `payment_reference` |
| الإلغاء | `cancelled_by`, `cancelled_at`, `cancellation_reason` |
| إعادة الحساب | `recalculation_count`, `last_recalculated_by`, `last_recalculated_at` |

**سجل الأحداث التفصيلي**

```
جدول: payroll_run_audit_event
------------------------------
  id                PK
  payroll_run_id    FK
  event_type        ENUM(created, scope_changed, member_added, member_removed,
                         exclusion_added, exclusion_removed, period_changed,
                         day_basis_changed, calculated, recalculated,
                         submitted_for_review, returned_to_draft, approved,
                         paid, cancelled, duplicate_override_granted,
                         snapshot_version_created)
  actor_user_id     FK
  actor_role        نص مجمَّد وقت الحدث
  occurred_at       TIMESTAMP (بمنطقة زمنية)
  reason            نص (إلزامي لأحداث: إعادة الحساب، الإلغاء، التجاوز، الإرجاع للمسودة)
  before_value      JSON
  after_value       JSON
  snapshot_version  INT (إن انطبق)
```

**قواعد العمل**

1. سجل الأحداث **للإلحاق فقط (Append-only)**: لا تعديل ولا حذف لأي سطر، ولأي مستخدم مهما كانت صلاحيته.
2. كل حدث يجب أن يحمل هوية الفاعل ودوره **وقت الحدث** (مجمَّدًا نصًا)، لا الدور الحالي.
3. الأحداث الحسّاسة يجب أن تحمل **سببًا إلزاميًا**: إعادة الحساب · الإلغاء · تجاوز حارس الازدواج · الإرجاع من المراجعة إلى المسودة.
4. يجب أن يوفّر النظام شاشة «تاريخ المسير» تعرض الأحداث بترتيب زمني مع الفاعل والسبب وفرق القيم، وأن تكون قابلة للتصدير.
5. يجب أن يُظهر رأس المسير **عدّاد إعادة الحساب** بوضوح، لأنه مؤشر جودة (مسير أُعيد حسابه مرات كثيرة يستحق تدقيقًا).
6. يجب أن تُحفظ الطوابع الزمنية بمنطقة زمنية صريحة، وأن تُعرض بالتوقيت المحلي للمنشأة.
7. يجب ألا يقلّ زمن الاحتفاظ بسجل التدقيق عن مدة الاحتفاظ القانونية المعرّفة للمنشأة، وألا يُتاح تنظيفه من داخل واجهات التشغيل العادية.

**معايير القبول**

- تنفيذ دورة كاملة (إنشاء → احتساب → مراجعة → اعتماد → صرف) يُنتج ستة أحداث على الأقل، كل منها بفاعل ووقت.
- إعادة الحساب مرتين تجعل `recalculation_count = 2` وتُنشئ حدثين بسببين مسجّلين.
- محاولة تعديل أو حذف سطر في سجل الأحداث تُرفض على مستوى الخدمة وقاعدة البيانات.
- تغيير دور مستخدم بعد الحدث لا يغيّر الدور المسجّل في السطر القديم.
- شاشة التاريخ تعرض للحدث «تغيير الفترة» القيمتين قبل وبعد.

---

### نموذج البيانات المقترح للقسم (Proposed Data Model)

> أسماء إنجليزية قياسية عامة، مقترحة كتصميم مرجعي؛ لفريق التنفيذ اعتماد تسمية مكافئة بشرط الحفاظ على العلاقات والقيود.

```
payroll_run
  id                     PK
  name                   VARCHAR      -- فريد داخل الفترة
  run_type               ENUM(regular, supplementary, off_cycle)
  parent_run_id          FK -> payroll_run   NULL   -- إلزامي حين run_type <> regular
  currency_code          CHAR(3)

  -- النطاق والاختيار (PR-01..PR-03)
  selection_mode         ENUM(dynamic_scope, dynamic_scope_with_exclusions, explicit_list)

  -- الفترة (PR-08)
  period_mode            ENUM(monthly_cycle, custom_range)
  cycle_start_day        SMALLINT NULL      -- 1..31
  cycle_end_day          SMALLINT NULL      -- 1..31
  reference_month        DATE     NULL
  period_start_date      DATE     NOT NULL  -- لقطة، شاملة
  period_end_date        DATE     NOT NULL  -- لقطة، شاملة
  period_days            INT      NOT NULL  -- محسوبة ومخزّنة

  -- الأساس (PR-09)
  day_basis_type         ENUM(fixed_30, actual_period_days, scheduled_work_days)
  day_basis_value        INT      NOT NULL  -- لقطة على مستوى المسير

  -- دورة الحياة والتدقيق (PR-11، PR-12)
  status                 ENUM(draft, calculated, in_review, approved, paid, cancelled)
  snapshot_version       INT DEFAULT 0
  recalculation_count    INT DEFAULT 0
  created_by / created_at
  calculated_by / calculated_at
  reviewed_by / reviewed_at / review_notes
  approved_by / approved_at
  paid_by / paid_at / payment_reference
  cancelled_by / cancelled_at / cancellation_reason

payroll_run_scope_filter          -- محدِّدات النطاق (PR-01)
  id PK
  payroll_run_id  FK
  scope_type      ENUM(company, branch, department, team, cost_center)
  scope_ref_id    BIGINT
  UNIQUE (payroll_run_id, scope_type, scope_ref_id)

payroll_run_exclusion             -- قائمة الاستثناء (PR-03 أ)
  id PK
  payroll_run_id  FK
  employee_id     FK
  reason_code     ENUM(paid_in_other_run, unpaid_suspension, separate_settlement, other)
  reason_note     VARCHAR
  excluded_by / excluded_at
  UNIQUE (payroll_run_id, employee_id)

payroll_run_explicit_member       -- القائمة الصريحة (PR-02، PR-03 ب)
  id PK
  payroll_run_id  FK
  employee_id     FK
  added_by / added_at
  UNIQUE (payroll_run_id, employee_id)

payroll_run_member_snapshot       -- لقطة الأعضاء (PR-05)
  id PK
  payroll_run_id          FK
  snapshot_version        INT
  employee_id             FK
  employee_code_snapshot  VARCHAR
  employee_name_snapshot  VARCHAR
  branch_snapshot / department_snapshot / team_snapshot / cost_center_snapshot  VARCHAR
  hire_date_snapshot      DATE
  termination_date_snapshot DATE NULL
  base_salary_snapshot    DECIMAL(18,4)
  attendance_exempt_snapshot BOOLEAN
  day_basis_value_effective INT           -- قد يختلف بالموظف عند scheduled_work_days
  coverage_days           INT
  prorata_factor          DECIMAL(9,6)
  membership_reason       ENUM(from_scope, manual_addition)
  UNIQUE (payroll_run_id, snapshot_version, employee_id)

employee_period_claim             -- حارس ازدواج الصرف (PR-06)
  ... كما في PR-06 مع القيد الفريد المُرشَّح

payroll_run_audit_event           -- أثر التدقيق (PR-12)
  ... كما في PR-12  (append-only)
```

**خوارزمية اشتقاق الأعضاء (مرجعية للمعاينة والاحتساب معًا)**

```
1) candidates ← حسب selection_mode:
     dynamic_scope*  : كل موظف تنطبق عليه محدِّدات payroll_run_scope_filter داخل الفترة
     explicit_list   : كل موظف في payroll_run_explicit_member
2) استبعاد من لا علاقة عمل له تتقاطع مع [period_start_date, period_end_date]
       → EXC_NO_ACTIVE_EMPLOYMENT / EXC_TERMINATED_BEFORE_PERIOD / EXC_JOINS_AFTER_PERIOD
3) استبعاد من في payroll_run_exclusion            → EXC_MANUAL_EXCLUSION
4) استبعاد من له حجز نشط على الفترة نفسها          → EXC_ALREADY_IN_RUN
5) لكل مرشّح متبقٍّ: احسب coverage_days ثم prorata_factor  (PR-10)
6) استبعاد من coverage_days = 0                    → EXC_ZERO_COVERAGE
7) النتيجة: (قائمة الداخلين + قائمة المستبعدين بأسبابهم)
```

---

### مثال رقمي كامل — مسير فرع بخمسة موظفين على فترة 23 → 22

**المعطيات**

```
اسم المسير       : «مسير فرع الرياض — أغسطس 2026»
وضع الاختيار     : dynamic_scope_with_exclusions   (نطاق + استثناء)
النطاق           : branch = الرياض
نمط الفترة       : monthly_cycle ، cycle_start_day = 23 ، cycle_end_day = 22
الشهر المرجعي    : أغسطس 2026
الفترة المولّدة  : 23/07/2026 → 22/08/2026   (شاملة الطرفين)
period_days      : 9 أيام (يوليو 23–31) + 22 يومًا (أغسطس 1–22) = 31 يومًا
أساس الأيام      : fixed_30 → day_basis_value = 30
العملة           : ر.س/ج.م
أيام العمل المجدولة داخل الفترة (أحد–خميس) : 21 يومًا
```

**الخطوة 1 — معاينة الأعضاء (PR-04)**

الداخلون:

| # | الرقم | الاسم | تاريخ الالتحاق | انتهاء الخدمة | سبب الدخول |
|---|---|---|---|---|---|
| 1 | E-101 | أحمد | 01/03/2023 | — | نطاق: فرع الرياض |
| 2 | E-102 | سارة | 15/09/2024 | — | نطاق: فرع الرياض |
| 3 | E-103 | محمد | 02/01/2025 | — | نطاق: فرع الرياض |
| 4 | E-104 | ليلى | **05/08/2026** | — | نطاق: فرع الرياض (التحقت داخل الفترة) |
| 5 | E-105 | خالد | 10/06/2022 | **10/08/2026** | نطاق: فرع الرياض (انتهت خدمته داخل الفترة) |

المستبعدون:

| الرقم | الاسم | الكود | التفسير |
|---|---|---|---|
| E-106 | فهد | `EXC_ALREADY_IN_RUN` | مُدرج في «مسير الإدارة العليا» لنفس الفترة (23/07–22/08/2026) |
| E-107 | نورة | `EXC_TERMINATED_BEFORE_PERIOD` | انتهت خدمتها في 30/06/2026 قبل بداية الفترة |
| E-108 | ياسر | `EXC_MANUAL_EXCLUSION` | استثناء يدوي — السبب: تسوية نهاية خدمة منفصلة |

**الخطوة 2 — حساب أيام التغطية ومعامل التناسب (PR-10)**

```
E-101 أحمد
  effective_from = MAX(23/07/2026 , 01/03/2023) = 23/07/2026
  effective_to   = MIN(22/08/2026 , —)          = 22/08/2026
  تغطية كاملة للفترة → prorata_factor = 1 (بلا قسمة، وفق قاعدة PR-10/1)
  coverage_days = 31

E-102 سارة  → تغطية كاملة → factor = 1 ، coverage_days = 31
E-103 محمد  → تغطية كاملة → factor = 1 ، coverage_days = 31

E-104 ليلى (التحقت داخل الفترة)
  effective_from = MAX(23/07/2026 , 05/08/2026) = 05/08/2026
  effective_to   = 22/08/2026
  coverage_days  = (22 - 5) + 1 = 18
  prorata_factor = MIN(18 / 30 , 1) = 0.600000

E-105 خالد (انتهت خدمته داخل الفترة)
  effective_from = 23/07/2026
  effective_to   = MIN(22/08/2026 , 10/08/2026) = 10/08/2026
  coverage_days  = 9 (يوليو 23–31) + 10 (أغسطس 1–10) = 19
  prorata_factor = MIN(19 / 30 , 1) = 0.633333
```

**الخطوة 3 — الاستحقاق الإجمالي قبل بنود الحضور والخصومات والبدلات**

| الموظف | الراتب الشهري | أيام التغطية | المعامل | الاستحقاق |
|---|---:|---:|---:|---:|
| E-101 أحمد | 9,000.00 | 31 | 1.000000 | **9,000.00** |
| E-102 سارة | 7,500.00 | 31 | 1.000000 | **7,500.00** |
| E-103 محمد | 6,000.00 | 31 | 1.000000 | **6,000.00** |
| E-104 ليلى | 6,000.00 | 18 | 0.600000 | **3,600.00** |
| E-105 خالد | 8,000.00 | 19 | 0.633333 | **5,066.67** |
| **الإجمالي** | **36,500.00** | — | — | **31,166.67** |

```
تفصيل الحسابين الجزئيين:
  ليلى : 6,000.00 × (18 / 30) = 6,000.00 × 0.600000 = 3,600.00
  خالد : 8,000.00 × (19 / 30) = 8,000.00 × 0.633333 = 5,066.664  → تقريب → 5,066.67
```

**الخطوة 4 — أثر اختيار أساس الأيام على النتيجة نفسها (PR-09)**

| الموظف | أساس `fixed_30` (=30) | أساس `actual_period_days` (=31) | أساس `scheduled_work_days` (=21) |
|---|---:|---:|---:|
| E-101 أحمد | 9,000.00 | 9,000.00 | 9,000.00 |
| E-102 سارة | 7,500.00 | 7,500.00 | 7,500.00 |
| E-103 محمد | 6,000.00 | 6,000.00 | 6,000.00 |
| E-104 ليلى | 18/30 → **3,600.00** | 18/31 → **3,483.87** | 12/21 → **3,428.57** |
| E-105 خالد | 19/30 → **5,066.67** | 19/31 → **4,903.23** | 13/21 → **4,952.38** |
| **الإجمالي** | **31,166.67** | **30,887.10** | **30,880.95** |

```
ملاحظات على العمود الثالث (scheduled_work_days):
  أيام الجدول داخل الفترة كلها      = 21 يومًا
  أيام جدول ليلى داخل تغطيتها (5–22 أغسطس)  = 12 يومًا  → 6,000 × 12/21 = 3,428.571 → 3,428.57
  أيام جدول خالد داخل تغطيته (23 يوليو–10 أغسطس) = 13 يومًا → 8,000 × 13/21 = 4,952.380 → 4,952.38
  الثلاثة أصحاب التغطية الكاملة لا يتناسبون → المعامل = 1 في كل الأعمدة
```

**الخطوة 5 — الاحتساب واللقطة والحجز (PR-05، PR-06)**

```
عند الضغط على «احتساب» تُنفَّذ معاملة واحدة:
  ① إنشاء payroll_run_member_snapshot بخمسة أسطر ، snapshot_version = 1
  ② إنشاء employee_period_claim بخمسة أسطر لـ (23/07/2026 , 22/08/2026) ، is_active = TRUE
  ③ status: draft → calculated ، وتسجيل calculated_by / calculated_at
  ④ حدث تدقيق: event_type = calculated

سيناريو تعارض:
  محاولة إدراج E-106 «فهد» هنا تفشل قبل الكتابة برسالة PAYRUN-DUP-001:
  «لا يمكن إدراج الموظف فهد (E-106) في هذا المسير: هو مُدرج بالفعل في المسير
   «مسير الإدارة العليا» لنفس الفترة (23/07/2026 – 22/08/2026)...»
```

**الخطوة 6 — تحقق «موظفون بلا مسير» قبل الاعتماد (PR-07)**

```
تشغيل التقرير على الفترة 23/07/2026 – 22/08/2026 ، فرع الرياض:
  E-107 نورة  → لا تظهر (انتهت خدمتها قبل الفترة ، لا تغطية)
  E-108 ياسر  → يظهر  : «مستثنى يدويًا من مسير فرع الرياض — تسوية نهاية خدمة منفصلة»
  E-109 ريم   → تظهر  : «التحقت 24/08/2026 — بعد نهاية الفترة» (لا إجراء مطلوب)
  E-110 سلمان → يظهر  : «خارج نطاق كل المسيرات المنشأة» ← إجراء مطلوب قبل الاعتماد
```

**الخطوة 7 — سيناريوهات اختبار مبنية على المثال**

| # | السيناريو | النتيجة المتوقعة |
|---|---|---|
| T-01 | إضافة موظف جديد لفرع الرياض والمسير في «مسودة» بالآلية (أ) | يدخل آليًا؛ عدد الأعضاء يصبح 6 |
| T-02 | نفس T-01 لكن المسير بالآلية (ب) | لا يدخل؛ يظهر في تقرير PR-07 |
| T-03 | إضافة موظف لفرع الرياض بعد الانتقال إلى «محسوب» | لا يدخل؛ اللقطة مجمّدة؛ يظهر في PR-07 |
| T-04 | رفع راتب أحمد إلى 10,000 بعد الاحتساب | المسير يبقى 9,000؛ لا تغيير إلا بإعادة حساب صريحة |
| T-05 | إنشاء «مسير الإدارة العليا» يضم E-101 على الفترة نفسها | يفشل بـ `PAYRUN-DUP-001` |
| T-06 | إلغاء «مسير الإدارة العليا» ثم إدراج E-106 هنا وإعادة الحساب | ينجح؛ الأعضاء 6؛ `recalculation_count = 1` |
| T-07 | تعديل الفترة إلى 24/07–23/08 والمسير «محسوب» | مرفوض حتى إلغاء الاحتساب والعودة لمسودة |
| T-08 | تعديل قيمة عضو والمسير «معتمد» | مرفوض بـ `PAYRUN-STATE-002` |
| T-09 | تغيير الأساس إلى `actual_period_days` وإعادة الحساب | الإجمالي يصبح 30,887.10؛ إصدار لقطة 2؛ حدث تدقيق بالسبب |
| T-10 | اعتماد المسير بواسطة نفس من احتسبه | مرفوض بـ `PAYRUN-STATE-003` |
| T-11 | موظف التحق 22/08/2026 (آخر يوم) | `coverage_days = 1` ، `factor = 1/30 = 0.033333` |
| T-12 | موظف التحق 05/08/2026 وانتهت خدمته 09/08/2026 | `coverage_days = 5` ، `factor = 5/30 = 0.166667` |
| T-13 | إنشاء المسير التالي بدورة 24→23 بعد دورة 23→22 | تحذير فجوة: يوم 23/08/2026 غير مغطّى |

---

## ٣) الساعة المرنة وشرائح خصم التأخير

هذا القسم يُعرِّف آليتين مترابطتين ولكن **منفصلتين حسابيًا**: (أ) **الساعة المرنة (Flexible Start Window)** التي تحدّد *متى* يُعدّ الموظف متأخرًا وبكم دقيقة، و(ب) **شرائح خصم التأخير (Lateness Deduction Tiers)** التي تحدّد *كم* يُخصم مقابل تلك الدقائق. المخرَج المشترك بينهما هو كميتان رقميتان لكل يوم عمل: **دقائق التأخير الفعلية** و**دقائق نقص ساعات العمل**، وهما مدخلان لمحرّك حساب المسير.

جميع المبالغ في الأمثلة بـ«وحدة نقد» محايدة (ر.س/ج.م)، وجميع المدد بالدقائق الصحيحة.

---

### أ) الساعة المرنة (Flexible Start Window)

#### FX-01 — تفعيل الساعة المرنة على مستوى الوردية الواحدة

**الوصف**
يجب أن يوفّر النظام خاصية «الساعة المرنة» كخيار يُفعَّل أو يُعطَّل **على مستوى تعريف كل وردية (Shift) على حدة**، لا على مستوى الشركة ولا على مستوى الفرع ولا كإعداد عام واحد.

**قواعد العمل**
1. لكل وردية علم منطقي مستقل `flexible_start_enabled` (مُفعَّل/غير مُفعَّل)، قيمته الافتراضية «غير مُفعَّل».
2. يجوز أن تتعايش في المنشأة نفسها، وفي اليوم نفسه، ورديات مرنة وورديات غير مرنة، ويُقيَّم كل موظف وفق إعداد ورديته المُسنَدة له في ذلك اليوم.
3. تفعيل الساعة المرنة يغيّر **طريقة اشتقاق دقائق التأخير الخام فقط**؛ ولا يُلغي تطبيق شرائح الخصم. شرائح الخصم تنطبق على الورديات المرنة وغير المرنة على السواء متى نتج تأخير موجب.
4. في الوردية **غير المرنة**: `دقائق التأخير الخام = MAX(0, وقت الحضور − بداية الدوام الرسمية)`.
5. تغيير قيمة العلم لا يجوز أن يُعيد احتساب أيام سبق إقفالها ماليًا (انظر FX-09).

**معايير القبول**
- AC1: عند إنشاء وردية جديدة، تكون الساعة المرنة معطّلة افتراضيًا.
- AC2: تفعيل الساعة المرنة على وردية «أ» لا يُحدث أي تغيير في نتائج تقييم موظفي وردية «ب».
- AC3: تعطيل الساعة المرنة على وردية مرنة يجعل التأخير يُحسب من بداية الدوام الرسمية مباشرة اعتبارًا من تاريخ سريان التغيير فقط.
- AC4: لا يوجد في النظام أي مفتاح واحد يفعّل/يعطّل الساعة المرنة لكل الورديات دفعة واحدة دون المرور على تعريف كل وردية.

---

#### FX-02 — إعدادات الوردية المرنة

**الوصف**
يجب أن يوفّر النظام مجموعة الحقول التالية لتعريف سلوك الوردية المرنة.

| الحقل (اسم مقترح) | النوع | إلزامي | الوصف | مثال |
|---|---|---|---|---|
| `official_start_time` | وقت | نعم | **بداية الدوام الرسمية** — المرجع الوحيد لقياس التأخير | 09:00 |
| `official_end_time` | وقت | نعم | نهاية الدوام المرجعية (تُستخدم للعرض ولحساب الورديات غير المرنة) | 18:00 |
| `flex_window_minutes` | عدد صحيح موجب | نعم عند التفعيل | طول **نافذة الحضور المرنة** بالدقائق ابتداءً من بداية الدوام الرسمية | 60 |
| `flex_window_end` (مشتق) | وقت | — | `official_start_time + flex_window_minutes` | 10:00 |
| `required_work_minutes` | عدد صحيح موجب | نعم | **عدد ساعات العمل المطلوبة** بالدقائق | 540 (٩ ساعات) |
| `unpaid_break_minutes` | عدد صحيح ≥ 0 | نعم | استراحة غير مدفوعة تُطرح من زمن التواجد | 0 |
| `count_early_work_toward_required` | منطقي | نعم | هل يُحتسب العمل قبل بداية الدوام الرسمية ضمن الساعات المطلوبة | لا (افتراضي) |
| `crosses_midnight` | منطقي | نعم | هل تعبر الوردية منتصف الليل | لا |
| `max_session_minutes` | عدد صحيح موجب | نعم | الحد الأقصى لطول جلسة الحضور الواحدة قبل وسمها للمراجعة | 900 |
| `late_out_grace_minutes` | عدد صحيح ≥ 0 | لا | تسامح في الانصراف المتأخر لا يُنتج أوفرتايم تلقائيًا | 10 |

**قواعد العمل**
1. `flex_window_minutes` يجب أن يكون ≥ 1 عند تفعيل الساعة المرنة، وأن يكون < `required_work_minutes`.
2. **نهاية الدوام في الوردية المرنة عائمة (Floating End)**: `نهاية الدوام المتوقعة = مرجع بدء العدّ + required_work_minutes + unpaid_break_minutes`. فمن يحضر 09:45 في وردية ٩ ساعات ينتهي دوامه 18:45.
3. يجب أن تعرض الواجهة النافذة كمدى زمني صريح («من 09:00 إلى 10:00») لا كعدد دقائق مجرّد.
4. يجب رفض أي تعريف تكون فيه نهاية الدوام المتوقعة لأقصى وقت حضور مسموح متجاوزةً حدًّا يوميًّا مُعرَّفًا للمنشأة، مع رسالة تحذير واضحة.
5. تُخزَّن كل الطوابع الزمنية بمنطقة زمنية صريحة، وتُحسب الفروق على التقويم المحلي للمنشأة.

**معايير القبول**
- AC1: محاولة تفعيل الساعة المرنة دون تحديد `flex_window_minutes` تُرفض برسالة خطأ تحدّد الحقل الناقص.
- AC2: إدخال `flex_window_minutes = 600` مع `required_work_minutes = 540` يُرفض.
- AC3: تعديل `required_work_minutes` يُعيد حساب نهاية الدوام المتوقعة المعروضة فورًا دون تعديل بيانات أيام سابقة مُقفلة.
- AC4: يعرض النظام في تعريف الوردية معاينة نصية: «الحضور المسموح: 09:00–10:00 · المطلوب: ٩ ساعات · الانصراف المتوقع: 18:00–19:00».

---

#### FX-03 — القاعدة ①: الحضور داخل النافذة مع إكمال الساعات ⇒ حاضر بلا خصم

**الوصف**
إذا سجّل الموظف حضوره في أي لحظة بين بداية الدوام الرسمية ونهاية النافذة المرنة (شاملةً الطرفين)، **وأكمل** عدد ساعات العمل المطلوبة، فيجب أن يُعدّ **حاضرًا كامل اليوم بلا أي خصم تأخير وبلا أي خصم نقص ساعات**.

**قواعد العمل**
1. الشرط الزمني: `check_in ≤ flex_window_end` ⇒ `raw_lateness_minutes = 0`.
2. الشرط الكمّي: `worked_minutes ≥ required_work_minutes` ⇒ `shortfall_minutes = 0`.
3. عند تحقق الشرطين: `day_status = PRESENT`، ولا يُولَّد أي سطر خصم من مصدر «تأخير» أو «نقص ساعات» لهذا اليوم.
4. لا يجوز أن يُنتج هذا اليوم أي وسم «متأخر» في التقارير أو في كشف الموظف.
5. الدقائق الزائدة عن المطلوب لا تُعدّ عملًا إضافيًا إلا عبر مسار الأوفرتايم المعتمد.

**مثال محسوب**
- المعطيات: بداية رسمية 09:00 · نافذة تنتهي 10:00 · المطلوب 540 دقيقة · استراحة غير مدفوعة 0.
- الحضور 09:45، الانصراف 18:45.
- الحساب: `check_in (09:45) ≤ 10:00` ⇒ التأخير = 0 دقيقة. زمن العمل = 18:45 − 09:45 = 540 دقيقة ⇒ النقص = `MAX(0, 540 − 540)` = 0.
- **النتيجة**: حاضر · خصم التأخير 0.00 · خصم النقص 0.00.

**معايير القبول**
- AC1: حضور 09:00:00 بالضبط ⇒ تأخير 0.
- AC2: حضور 10:00:00 بالضبط (حدّ النافذة) ⇒ تأخير 0.
- AC3: حضور 09:59 مع إكمال 540 دقيقة ⇒ لا يُولَّد أي سطر خصم، ولا يظهر اليوم في تقرير المتأخرين.
- AC4: زمن عمل 545 دقيقة لا يُنتج أوفرتايم تلقائيًا ولا يُقاصّ نقصًا في يوم آخر.

---

#### FX-04 — القاعدة ②: الحضور داخل النافذة دون إكمال الساعات ⇒ خصم على نقص الساعات وحده

**الوصف**
إذا سجّل الموظف حضوره داخل النافذة المرنة **ولم يُكمل** عدد ساعات العمل المطلوبة، فيجب أن يُحسب خصم على **نقص الساعات (Shortfall)** فقط، **ويجب ألا تُطبَّق شرائح خصم التأخير على هذا اليوم**، لأن دقائق التأخير تساوي صفرًا.

**قواعد العمل**
1. `shortfall_minutes = MAX(0, required_work_minutes − worked_minutes − approved_leave_credit_minutes)`.
2. `raw_lateness_minutes = 0` ⇒ لا مطابقة لأي شريحة تأخير ⇒ خصم التأخير = 0.00.
3. يُحسب خصم النقص وفق قاعدة نقص الساعات المُعرَّفة في السياسة المُسنَدة للمسير، ونمطها الافتراضي «بالسعر نفسه (1:1)».
4. يجب أن تدعم قاعدة النقص حقل **تسامح (Tolerance)** بالدقائق: `if shortfall_minutes ≤ shortfall_tolerance_minutes then shortfall = 0`.
5. يجب ألا يتجاوز خصم النقص اليومي سقفًا يوميًا مُعرَّفًا (افتراضيًا: قيمة يوم واحد).
6. `day_status = PRESENT` مع وسم فرعي `has_shortfall = true` (لا يُوسَم اليوم «متأخر»).

**مثال محسوب**
- المعطيات: الراتب الخاضع للخصم 16,200 · أساس الشهر 30 يومًا · اليوم = 540.00 · الساعة = 60.00 · الدقيقة = 1.00 · المطلوب 540 دقيقة.
- الحضور 09:45، الانصراف 18:05.
- الحساب: التأخير = 0 (داخل النافذة). زمن العمل = 18:05 − 09:45 = 500 دقيقة ⇒ النقص = 540 − 500 = **40 دقيقة**.
- خصم النقص (1:1) = 40 × 1.00 = **40.00 وحدة نقد**. خصم التأخير = **0.00**.

**معايير القبول**
- AC1: مع تسامح نقص = 15 دقيقة ونقص فعلي 10 دقائق، يكون الخصم 0.00.
- AC2: مع تسامح 15 ونقص 40، يُخصم 40 دقيقة كاملة (لا 25) ما لم يُضبط التسامح كخصم من المدة صراحةً.
- AC3: لا يظهر لهذا اليوم أي سطر خصم مصدره «تأخير»، ولا تُستدعى مطابقة الشرائح أصلًا.
- AC4: إذا كان اليوم مغطى بإذن/إجازة جزئية معتمدة، تُطرح دقائقها من المطلوب قبل حساب النقص.

---

#### FX-05 — القاعدة ③ (الحالة الحاسمة): الحضور بعد نهاية النافذة ⇒ التأخير يُقاس من بداية الدوام الرسمية، وتُطبَّق الشرائح ولو أكمل ساعاته

**الوصف**
إذا سجّل الموظف حضوره **بعد** نهاية النافذة المرنة ولو بدقيقة واحدة، فيجب أن يُعدّ **متأخرًا**، وأن تُحسب دقائق تأخيره **من بداية الدوام الرسمية لا من نهاية النافذة**، وأن تُطبَّق عليه شرائح خصم التأخير **حتى لو عوّض وأكمل عدد ساعات العمل المطلوبة كاملة**.

**قواعد العمل**
1. الشرط: `check_in > flex_window_end`.
2. `raw_lateness_minutes = check_in − official_start_time` (وليس `check_in − flex_window_end`).
3. تجاوز النافذة يُسقط امتياز المرونة لذلك اليوم بالكامل؛ فالنافذة **امتياز شرطي** لا خصم ثابت من التأخير.
4. إكمال ساعات العمل المطلوبة **لا يُلغي** خصم التأخير ولا يخفّضه ولا ينقل الموظف إلى شريحة أدنى.
5. إذا أكمل الساعات: `shortfall_minutes = 0` ويُطبَّق خصم التأخير وحده.
6. إذا لم يُكمل الساعات: تكون الكميتان موجبتين معًا، وتُحسم المعالجة بسياسة التداخل في FX-07.
7. `day_status = LATE`، ويجب أن يُظهر كشف الموظف دقائق التأخير والشريحة المطابقة.

**مثال محسوب (مثال المواصفة حرفيًا)**
- المعطيات: بداية رسمية **09:00** · النافذة تنتهي **10:00** · المطلوب 540 دقيقة · الدقيقة = 1.00 وحدة نقد.
- الحضور **10:01** · الانصراف **19:01**.
- الحساب: `10:01 > 10:00` ⇒ متأخر. `raw_lateness = 10:01 − 09:00` = **61 دقيقة** (وليس دقيقة واحدة).
- زمن العمل = 19:01 − 10:01 = 540 دقيقة ⇒ النقص = 0.
- الشريحة المطابقة `[60, 120)` بمضاعف 1.5 ⇒ الخصم = 61 × 1.5 × 1.00 = **91.50 وحدة نقد**.
- **النتيجة**: خصم تأخير 91.50 · خصم نقص 0.00 — رغم إكمال التسع ساعات كاملة.

**معايير القبول**
- AC1: حضور 10:00:59 ⇒ التأخير 60 دقيقة (بعد اقتطاع الثواني)، وليس 0.
- AC2: حضور 10:01 ⇒ التأخير **61** دقيقة قطعًا؛ أي ناتج يساوي 1 دقيقة يُعدّ فشلًا صريحًا للاختبار.
- AC3: حضور 10:01 مع انصراف 19:01 (إكمال كامل) ⇒ يُولَّد سطر خصم تأخير بقيمة موجبة.
- AC4: حضور 10:01 مع انصراف 18:00 ⇒ يُولَّد سطر تأخير (61 دقيقة) **و**سطر نقص (بمقدار 540 − 479 = 61 دقيقة)، وتُطبَّق سياسة التداخل قبل الترحيل للمسير.
- AC5: تعديل `flex_window_minutes` من 60 إلى 30 يجعل الحضور 09:45 متأخرًا بـ 45 دقيقة اعتبارًا من تاريخ السريان فقط.

---

#### FX-06 — القاعدة ④: الحضور قبل بداية الدوام الرسمية

**الوصف**
إذا سجّل الموظف حضوره **قبل** بداية الدوام الرسمية، فيجب ألا يُحتسب عليه أي تأخير، وألا يُعدّ الوقت السابق لبداية الدوام عملًا إضافيًا تلقائيًا؛ ولا يُحتسب إضافيًا إلا عبر مسار الأوفرتايم المعتمد.

**قواعد العمل**
1. `raw_lateness_minutes = 0` بلا شرط.
2. `early_arrival_minutes = official_start_time − check_in` تُخزَّن للعرض والتحليل فقط، ولا تُنتج استحقاقًا ماليًا بذاتها.
3. احتساب الوقت المبكر ضمن ساعات العمل المطلوبة محكوم بالحقل `count_early_work_toward_required`:
   - **غير مُفعَّل (افتراضي)**: يبدأ عدّ زمن العمل من بداية الدوام الرسمية ⇒ `work_start_reference = MAX(check_in, official_start_time)`.
   - **مُفعَّل**: يبدأ العدّ من لحظة الحضور الفعلية ⇒ `work_start_reference = check_in`.
4. الفائض عن الساعات المطلوبة لا يُقاصّ نقصًا في يوم آخر ولا يُرحَّل، إلا إذا فُعِّلت سياسة مقاصّة صريحة على مستوى السياسة.
5. يجب ألا يُحوَّل الوقت المبكر إلى أوفرتايم إلا بعد اعتماد صريح عبر مسار الأوفرتايم.

**مثال محسوب — أثر الحقل الرابع**
- المعطيات نفسها (بداية 09:00 · المطلوب 540 دقيقة · الدقيقة 1.00). الحضور **08:30** والانصراف **17:30**.
- الحالة (أ) `count_early_work_toward_required = لا`: زمن العمل = 17:30 − 09:00 = 510 دقيقة ⇒ النقص 30 دقيقة ⇒ خصم **30.00**.
- الحالة (ب) `count_early_work_toward_required = نعم`: زمن العمل = 17:30 − 08:30 = 540 دقيقة ⇒ النقص 0 ⇒ خصم **0.00**.
- الفارق 30.00 وحدة نقد في اليوم الواحد لنفس البصمات — لذا يجب أن يكون هذا الحقل صريحًا ومُوثَّقًا لا ضمنيًا.

**معايير القبول**
- AC1: حضور 08:30 ⇒ `raw_lateness = 0` و`early_arrival_minutes = 30`.
- AC2: لا يُولَّد أي سطر أوفرتايم تلقائي عن الثلاثين دقيقة المبكرة.
- AC3: تبديل قيمة `count_early_work_toward_required` يغيّر `worked_minutes` وفق الجدول أعلاه ويُوثَّق التغيير في سجل التدقيق.

---

#### FX-07 — الفصل الإلزامي بين «نقص الساعات» و«التأخير الزمني»

**الوصف**
يجب أن يعامل النظام **نقص الساعات (Shortfall)** و**التأخير الزمني (Lateness)** بوصفهما **كميتين مختلفتين تمامًا**، تُحسبان وتُخزَّنان في حقلين منفصلين، بمصدرين مختلفين، وبقاعدتي خصم مختلفتين؛ ويجوز أن تكونا موجبتين في اليوم نفسه.

**لماذا الخلط يُنتج خصمًا خاطئًا**
الكميتان تقيسان شيئين مختلفين:
- **التأخير** يقيس **لحظة البدء** مقارنةً ببداية الدوام الرسمية — وهو انضباط زمني، ويُخصم بشرائح تصاعدية عقابية.
- **النقص** يقيس **إجمالي زمن العمل المُنجَز** مقارنةً بالمطلوب — وهو نقص إنتاج، ويُخصم عادةً بنسبة 1:1.

لا توجد بينهما علاقة رياضية ثابتة: يمكن أن يكون التأخير 61 دقيقة والنقص صفرًا (FX-05)، وأن يكون التأخير صفرًا والنقص 40 دقيقة (FX-04). أي تصميم يشتق إحداهما من الأخرى، أو يخزّنهما في حقل واحد، سيُنتج خصمًا خاطئًا في اتجاهين: **تخفيضًا** لمن تأخر ثم عوّض (فيُعفى من عقوبة التأخير بلا وجه حق)، و**تضخيمًا** لمن حضر في الموعد ثم انصرف مبكرًا (فتُطبَّق عليه شرائح تأخير عقابية لم يستحقها).

**مثال رقمي للخلط**
- المعطيات: الدقيقة 1.00 · اليوم 540.00 · المطلوب 540 دقيقة · بداية 09:00 · نافذة حتى 10:00 · شريحة `[60,120)` بمضاعف 1.5.
- اليوم: الحضور **10:10**، الانصراف **17:00**.
- الكميتان الصحيحتان: `raw_lateness = 10:10 − 09:00 = 70 دقيقة` · `worked = 17:00 − 10:10 = 410 دقيقة` ⇒ `shortfall = 540 − 410 = 130 دقيقة`.
- تصميم خاطئ يعتبر «التأخير = النقص»: يحسب 130 دقيقة تأخيرًا ⇒ شريحة `[120,∞)` بمضاعف 2 ⇒ 260.00 وحدة نقد.
- تصميم خاطئ يعتبر «النقص = التأخير»: يحسب 70 دقيقة نقصًا ⇒ 70.00 وحدة نقد.
- الفارق بين التصميمين الخاطئين وحدهما **190.00 وحدة نقد** في يوم واحد لنفس البصمتين.

**سياسة التداخل (Overlap Policy) — إلزامية وقابلة للضبط**
عند اجتماع الكميتين في اليوم نفسه، يجب أن توفّر السياسة الحقل `overlap_with_shortfall_policy` بالقيم الثلاث الآتية، وأن تُوثَّق القيمة المختارة على مستوى السياسة المُسنَدة للمسير:

| القيمة | المعنى | المعادلة |
|---|---|---|
| `CUMULATIVE` | خصم الكميتين معًا مستقلتين | `total = lateness_amount + shortfall_amount` |
| `MAX_OF_BOTH` | خصم الأكبر منهما فقط | `total = MAX(lateness_amount, shortfall_amount)` |
| `NET_OF_LATENESS` | خصم التأخير كاملًا + نقص الدقائق غير المُفسَّرة بالتأخير | `total = lateness_amount + f(MAX(0, shortfall − raw_lateness))` |

**تطبيق الأمثلة على معطيات اليوم أعلاه** (تأخير 70 دقيقة ⇒ 70 × 1.5 × 1.00 = **105.00** · نقص 130 دقيقة 1:1 ⇒ **130.00**):
- `CUMULATIVE` ⇒ 105.00 + 130.00 = **235.00**
- `MAX_OF_BOTH` ⇒ MAX(105.00, 130.00) = **130.00**
- `NET_OF_LATENESS` ⇒ 105.00 + (130 − 70) × 1.00 = 105.00 + 60.00 = **165.00**

**قواعد العمل**
1. تُخزَّن `raw_lateness_minutes` و`shortfall_minutes` في حقلين منفصلين لكل يوم، ولا يُشتق أحدهما من الآخر أبدًا.
2. يُولَّد لكل كمية سطر خصم مستقل يحمل حقل `source ∈ {LATENESS, SHORTFALL}` قبل تطبيق سياسة التداخل.
3. تُطبَّق سياسة التداخل كخطوة لاحقة صريحة، ويُحفظ أثرها (المبلغ قبل وبعد) في تفصيل الحساب.
4. القيمة الافتراضية الموصى بها `NET_OF_LATENESS` لأنها تمنع ازدواج الخصم عن الدقائق نفسها دون إعفاء المتأخر من عقوبة التأخير.
5. يجب ألا يتجاوز مجموع خصمي اليوم السقف اليومي المُعرَّف في LT-04.

**معايير القبول**
- AC1: كشف اليوم يعرض القيمتين منفصلتين مع تسميتيهما، لا قيمة واحدة مدمجة.
- AC2: تشغيل الحالة أعلاه بالقيم الثلاث يُنتج 235.00 / 130.00 / 165.00 على الترتيب.
- AC3: يوم بتأخير 61 دقيقة ونقص 0 يُنتج سطر تأخير واحد فقط بأي قيمة لسياسة التداخل.
- AC4: يوم بتأخير 0 ونقص 40 دقيقة لا يُنتج أي سطر مصدره `LATENESS` بأي قيمة لسياسة التداخل.

---

#### FX-08 — الحالات الحدّية للساعة المرنة

**الوصف**
يجب أن يعالج النظام الحالات الآتية بقواعد صريحة ونتائج محدَّدة لا باجتهاد وقت التشغيل.

**FX-08.1 — وردية ليلية تعبر منتصف الليل**
1. تُربط الوردية بيوم عمل منطقي (Business Day) هو **تاريخ بداية الوردية**، لا تاريخ البصمة.
2. تُحسب الفروق الزمنية على خط زمني متصل يتجاوز منتصف الليل.
3. مثال: وردية 22:00 → 07:00 · نافذة مرنة حتى 23:00 · المطلوب 480 دقيقة. الحضور 23:10 من يوم الأحد ⇒ `raw_lateness = 23:10 − 22:00 = 70 دقيقة` ويُنسب اليوم للأحد لا للاثنين.
4. عند تغيّر التوقيت الصيفي داخل الوردية، تُحسب المدة بالفارق الفعلي بين اللحظتين المطلقتين، وتُوسَم لليوم للمراجعة.

**FX-08.2 — إذن معتمد يغطي جزءًا من التأخير**
1. يُخزَّن التأخير المُبرَّر في حقل مستقل `excused_lateness_minutes` ولا يُعدَّل التأخير الخام أبدًا.
2. `effective_lateness = MAX(0, raw_lateness − excused_lateness)` ثم تُطبَّق مدة السماح وفق LT-05.
3. مثال: تأخير خام 80 دقيقة · إذن معتمد يغطي 60 دقيقة ⇒ الفعلي 20 دقيقة ⇒ لا يطابق الشريحة الأولى `[60,120)` ⇒ خصم التأخير 0.00. ويظل التأخير الخام 80 ظاهرًا في السجل والتقارير السلوكية.
4. الإذن المعتمد بعد إقفال الفترة يُعالَج كتسوية في الفترة التالية لا بإعادة فتح المُقفل.

**FX-08.3 — نصف يوم إجازة**
1. تُقاس النافذة المرنة من **بداية فترة العمل الفعلية** بعد الإجازة الجزئية، لا من بداية الوردية الأصلية.
2. `required_work_minutes` تُخفَّض بنسبة تغطية الإجازة: نصف يوم ⇒ 540 × 0.5 = 270 دقيقة.
3. طول النافذة المرنة يُطبَّق كما هو افتراضيًا، ويُتاح حقل `prorate_flex_window_on_partial_leave` لتخفيضها بالنسبة نفسها.
4. مثال: إجازة نصف يوم صباحية تنتهي 13:30 · نافذة 60 دقيقة ⇒ الحضور حتى 14:30 بلا تأخير · المطلوب 270 دقيقة · الحضور 14:45 ⇒ `raw_lateness = 14:45 − 13:30 = 75 دقيقة`.

**FX-08.4 — لم يُسجَّل انصراف**
1. `worked_minutes` غير قابل للتحديد ⇒ `day_status = INCOMPLETE` ⇒ **لا يُحسب نقص الساعات تلقائيًا**.
2. **يُحسب التأخير ويُطبَّق** لأن بصمة الحضور موجودة وكافية لقياسه.
3. يُحجب اليوم عن إقفال المسير حتى تسويته، ما لم تُفعَّل سياسة بديلة صريحة: `ASSUME_REQUIRED_HOURS` (افتراض إكمال المطلوب) أو `ASSUME_ZERO` (اعتبار زمن العمل صفرًا) أو `MANUAL_ONLY`.
4. يجب أن يُنبّه النظام مسؤول الحضور بقائمة الأيام غير المكتملة قبل إقفال أي فترة.

**FX-08.5 — حضر ولم ينصرف حتى اليوم التالي**
1. إذا تجاوز طول الجلسة `max_session_minutes` تُوسَم للمراجعة ولا تُرحَّل آليًا إلى الحساب.
2. لا يجوز أن تمتد جلسة حضور واحدة لتغطية يومي عمل منطقيين؛ يُقفَل اليوم الأول وفق سياسة الانصراف المفقود ويُفتح اليوم التالي بحضوره الخاص.
3. إذا وقع الانصراف بعد بداية الوردية التالية للموظف نفسه، تُوسَم الحالة تعارضًا ويُمنع الاحتساب المزدوج للفترة المشتركة.

**FX-08.6 — بصمات متعددة في اليوم**
1. تُعتمد **أول بصمة حضور** و**آخر بصمة انصراف** لتحديد إطار اليوم.
2. تُتاح سياسة اختيارية لطرح فجوات الخروج الوسيطة من زمن العمل عند تفعيل احتساب الجلسات المتعددة.
3. البصمات المكررة خلال نافذة تكرار قصيرة (مثلاً 60 ثانية) تُدمج في بصمة واحدة.

**معايير القبول (مجمّعة)**
- AC1: كل حالة من FX-08.1 إلى FX-08.6 لها اختبار آلي بمدخلات صريحة ونتيجة متوقعة صريحة.
- AC2: لا تُنتج أي حالة حدّية استثناءً غير مُعالَج ولا قيمة سالبة في `worked_minutes` أو `raw_lateness_minutes`.
- AC3: كل يوم موسوم للمراجعة يمنع إقفال الفترة أو يظهر في تقرير استثناءات ما قبل الإقفال.

---

#### FX-09 — سريان الإعدادات زمنيًا (Effective-Dated Settings)

**الوصف**
يجب أن تكون إعدادات الوردية وسياسات الخصم **مؤرَّخة السريان ومُصدَّرة بنسخ (Versioned)**، وأن يُحسب كل يوم عمل بالنسخة السارية في **تاريخ ذلك اليوم**، لا بالنسخة الحالية وقت التشغيل.

**قواعد العمل**
1. لكل تعريف وردية وكل طقم شرائح حقول `effective_from` و`effective_to`، وأي تعديل يُنشئ نسخة جديدة ولا يُعدّل النسخة السابقة.
2. يُخزَّن في سجل كل يوم مُقيَّم معرّف نسخة الوردية ونسخة طقم الشرائح المستخدمين.
3. إعادة احتساب فترة سابقة يجب أن تُنتج النتائج نفسها بالضبط ما دامت البصمات لم تتغيّر.
4. الأيام التي رُحِّلت في مسير مُقفَل لا تُعاد حسابها بأثر رجعي؛ أي تصحيح يمرّ كتسوية في فترة لاحقة.

**معايير القبول**
- AC1: تعديل `flex_window_minutes` اليوم لا يغيّر مبلغ خصم محسوب لشهر سابق.
- AC2: إعادة تشغيل حساب فترة مغلقة يُنتج مبالغ مطابقة تمامًا (فرق صفر).
- AC3: كل سطر خصم يعرض عند الاستعلام معرّف نسخة الإعدادات التي أنتجته.

---

#### FX-10 — خوارزمية التقييم اليومي الموحّدة

**الوصف**
يجب أن يُقيَّم كل يوم عمل بخوارزمية واحدة محدَّدة الترتيب، تُنتج كميات اليوم قبل أي حساب مالي.

```
INPUT: check_in, check_out, shift_version, leave_credit_minutes, excused_lateness_minutes

1) window_end = official_start + flex_window_minutes         // إن كانت الوردية مرنة

2) raw_lateness_minutes =
      IF NOT flexible_start_enabled:
            MAX(0, check_in − official_start)
      ELSE IF check_in <= window_end:
            0
      ELSE:
            check_in − official_start                        // من البداية الرسمية لا من نهاية النافذة

3) early_arrival_minutes = MAX(0, official_start − check_in)

4) work_start_reference =
      IF count_early_work_toward_required: check_in
      ELSE:                                MAX(check_in, official_start)

5) worked_minutes = (check_out − work_start_reference) − unpaid_break_minutes
      IF check_out IS NULL: worked_minutes = NULL ; day_status = INCOMPLETE

6) effective_required = required_work_minutes − leave_credit_minutes

7) shortfall_minutes =
      IF worked_minutes IS NULL: NULL          // لا يُحسب نقص بلا انصراف
      ELSE: MAX(0, effective_required − worked_minutes)

8) effective_lateness_minutes = MAX(0, raw_lateness_minutes − excused_lateness_minutes)
      ثم تُطبَّق مدة السماح وفق LT-05

9) day_status = PRESENT | LATE | INCOMPLETE | ABSENT | ON_LEAVE | HOLIDAY | WEEKLY_REST | EXEMPT

OUTPUT: raw_lateness_minutes, effective_lateness_minutes, shortfall_minutes,
        early_arrival_minutes, worked_minutes, day_status
```

**قواعد العمل**
1. الخطوات 1–9 تُنفَّذ بهذا الترتيب حصرًا؛ ولا يجوز أن يؤثر ناتج نقص الساعات في حساب التأخير أو العكس.
2. لا تُنتج الخوارزمية أي مبلغ مالي؛ المبالغ تُحسب في طبقة الشرائح (LT).
3. جميع المخرجات أعداد صحيحة بالدقائق أو `NULL` صريحة.

**معايير القبول**
- AC1: تشغيل الخوارزمية على مجموعة بصمات ثابتة يُنتج المخرجات نفسها في كل مرة (Deterministic).
- AC2: لا يُنتج أي مسار قيمة سالبة.
- AC3: الخطوة (2) تُنتج 61 لمعطيات FX-05، وتُنتج 0 لمعطيات FX-03.

---

#### FX-11 — نموذج البيانات المقترح (الحضور والوردية)

```sql
-- تعريف الوردية (مؤرَّخ السريان)
shift_definition (
  id                                  PK,
  code                                VARCHAR  UNIQUE,
  name                                VARCHAR,
  official_start_time                 TIME     NOT NULL,
  official_end_time                   TIME     NOT NULL,
  crosses_midnight                    BOOLEAN  NOT NULL DEFAULT FALSE,
  required_work_minutes               INT      NOT NULL CHECK (> 0),
  unpaid_break_minutes                INT      NOT NULL DEFAULT 0,
  flexible_start_enabled              BOOLEAN  NOT NULL DEFAULT FALSE,
  flex_window_minutes                 INT      NULL CHECK (> 0),
  count_early_work_toward_required    BOOLEAN  NOT NULL DEFAULT FALSE,
  prorate_flex_window_on_partial_leave BOOLEAN NOT NULL DEFAULT FALSE,
  max_session_minutes                 INT      NOT NULL,
  missing_checkout_policy             ENUM('BLOCK','ASSUME_REQUIRED_HOURS','ASSUME_ZERO','MANUAL_ONLY'),
  effective_from                      DATE     NOT NULL,
  effective_to                        DATE     NULL,
  version_no                          INT      NOT NULL,
  is_active                           BOOLEAN  NOT NULL
)
-- قيد: flexible_start_enabled = TRUE  ⇒  flex_window_minutes IS NOT NULL AND < required_work_minutes

-- تقييم يوم العمل (سجل واحد لكل موظف/يوم)
attendance_day (
  id                          PK,
  employee_id                 FK,
  work_date                   DATE     NOT NULL,     -- يوم العمل المنطقي
  shift_definition_version_id FK       NOT NULL,     -- النسخة السارية وقت العمل
  first_check_in              DATETIME NULL,
  last_check_out              DATETIME NULL,
  planned_start               DATETIME NOT NULL,
  flex_window_end             DATETIME NULL,
  required_minutes            INT      NOT NULL,
  worked_minutes              INT      NULL,
  raw_lateness_minutes        INT      NOT NULL DEFAULT 0,
  excused_lateness_minutes    INT      NOT NULL DEFAULT 0,
  effective_lateness_minutes  INT      NOT NULL DEFAULT 0,
  shortfall_minutes           INT      NULL,
  early_arrival_minutes       INT      NOT NULL DEFAULT 0,
  day_status                  ENUM('PRESENT','LATE','ABSENT','INCOMPLETE','ON_LEAVE',
                                   'HOLIDAY','WEEKLY_REST','EXEMPT'),
  review_flag                 BOOLEAN  NOT NULL DEFAULT FALSE,
  review_reason               VARCHAR  NULL,
  is_locked                   BOOLEAN  NOT NULL DEFAULT FALSE,
  UNIQUE (employee_id, work_date)
)
```

**معايير القبول**
- AC1: لا يمكن حفظ وردية مرنة بلا `flex_window_minutes` (قيد قاعدة بيانات لا تحقق واجهة فقط).
- AC2: كل سجل `attendance_day` يحمل حقلي التأخير والنقص منفصلين ومعرّف نسخة الوردية.
- AC3: لا يمكن إنشاء سجلين لليوم نفسه للموظف نفسه.

---

#### FX-12 — سيناريوهات اختبار القبول للساعة المرنة

المعطيات الثابتة لكل السيناريوهات: بداية رسمية 09:00 · نافذة 60 دقيقة (تنتهي 10:00) · المطلوب 540 دقيقة · استراحة 0 · شريحة `[60,120)` بمضاعف 1.5 و`[120,∞)` بمضاعف 2 · الدقيقة 1.00 وحدة نقد.

| # | الحضور | الانصراف | زمن العمل | التأخير الخام | النقص | خصم التأخير | خصم النقص | الحالة |
|---|---|---|---|---|---|---|---|---|
| T-01 | 09:00 | 18:00 | 540 | 0 | 0 | 0.00 | 0.00 | PRESENT |
| T-02 | 09:45 | 18:45 | 540 | 0 | 0 | 0.00 | 0.00 | PRESENT |
| T-03 | 10:00 | 19:00 | 540 | 0 | 0 | 0.00 | 0.00 | PRESENT |
| T-04 | 09:45 | 18:05 | 500 | 0 | 40 | 0.00 | 40.00 | PRESENT + نقص |
| T-05 | 10:01 | 19:01 | 540 | **61** | 0 | **91.50** | 0.00 | LATE |
| T-06 | 10:01 | 18:00 | 479 | 61 | 61 | 91.50 | حسب سياسة التداخل | LATE + نقص |
| T-07 | 10:10 | 17:00 | 410 | 70 | 130 | 105.00 | حسب سياسة التداخل | LATE + نقص |
| T-08 | 11:30 | 20:30 | 540 | 150 | 0 | 300.00 | 0.00 | LATE |
| T-09 | 08:30 | 17:30 | 510 / 540 | 0 | 30 / 0 | 0.00 | 30.00 / 0.00 | حسب `count_early_work…` |
| T-10 | 10:05 | — | NULL | 65 | NULL | 97.50 | لا يُحسب | INCOMPLETE |

---

### ب) شرائح خصم التأخير (Lateness Deduction Tiers)

#### LT-01 — بنية الشريحة

**الوصف**
يجب أن يوفّر النظام تعريفًا كاملًا لشرائح خصم التأخير من قِبل المستخدم المخوَّل، بحيث تُضاف الشرائح وتُعدَّل وتُعطَّل دون تدخل برمجي.

**حقول الشريحة**

| الحقل | النوع | إلزامي | الوصف |
|---|---|---|---|
| `tier_set_id` | مرجع | نعم | الطقم الذي تنتمي إليه الشريحة |
| `from_minute` | عدد صحيح ≥ 0 | نعم | حدّ البداية **شامل (Inclusive)** |
| `to_minute` | عدد صحيح أو ∞ | نعم | حدّ النهاية **غير شامل (Exclusive)**؛ القيمة الفارغة تعني «فما فوق» |
| `deduction_pattern` | قائمة | نعم | نمط الخصم: `MULTIPLIER` · `DAY_FRACTION` · `RATE_1_1` · `FIXED_AMOUNT` |
| `multiplier` | كسر عشري > 0 | شرطي | مطلوب مع `MULTIPLIER` |
| `day_fraction` | كسر عشري ∈ (0, 1] | شرطي | مطلوب مع `DAY_FRACTION` |
| `fixed_amount` | مبلغ > 0 | شرطي | مطلوب مع `FIXED_AMOUNT` |
| `sort_order` | عدد صحيح | نعم | الترتيب للعرض وللفصل الحتمي عند التعادل |
| `is_active` | منطقي | نعم | التفعيل/التعطيل دون حذف |
| `label` | نص | لا | تسمية تظهر للموظف مثل «تأخير من ساعة إلى ساعتين» |

**قواعد العمل**
1. المدى نصف مفتوح `[from_minute, to_minute)`؛ فشريحة «من 60 إلى 120» تشمل 60 وتشمل 119 ولا تشمل 120 — وهذا يزيل غموض الحدود نهائيًا.
2. `from_minute < to_minute` إلزاميًا، وكلاهما بالدقائق الصحيحة.
3. يجب أن تكون **الشريحة الأخيرة مفتوحة النهاية** (`to_minute = ∞`) لضمان تغطية كل قيم التأخير الكبيرة.
4. تعطيل شريحة يجب أن يُعاد معه التحقق من قواعد السلامة في LT-04 (لأن التعطيل قد يُحدث فجوة).
5. لا يُسمح بحذف شريحة استُخدمت في حساب مُرحَّل؛ يُسمح بالتعطيل فقط حفاظًا على قابلية إعادة الحساب.
6. عدد الشرائح داخل الطقم غير محدود منطقيًا (بحد أعلى تقني معقول).

**معايير القبول**
- AC1: إنشاء شريحة `from=60, to=120` بمضاعف 1.5 ينجح ويظهر مداها في الواجهة كـ «60 ≤ التأخير < 120».
- AC2: إدخال `from=120, to=60` يُرفض.
- AC3: اختيار نمط `MULTIPLIER` دون تعبئة `multiplier` يُرفض، وكذلك عكسه لكل نمط.
- AC4: تعطيل الشريحة الأخيرة المفتوحة يُرفض ما لم تُعيَّن شريحة أخرى مفتوحة النهاية.

---

#### LT-02 — أنماط الخصم الثلاثة المطلوبة ومعادلاتها

**الوصف**
يجب أن يوفّر النظام — كحدّ أدنى إلزامي — ثلاثة أنماط لخصم التأخير، يُختار النمط **لكل شريحة على حدة** داخل الطقم نفسه، بحيث يمكن أن تكون الشريحة الأولى بمضاعف والثانية بكسر يوم في الطقم ذاته.

**أسعار الأساس (تُحسب مرة واحدة لكل موظف/فترة)**
```
deduction_base   = مجموع مكوّنات الأجر المُعرَّفة كأساس للخصم في السياسة
day_rate         = deduction_base / period_days_basis        // 30 | أيام الشهر الفعلية | أيام العمل
hour_rate        = day_rate / contracted_daily_hours
minute_rate      = hour_rate / 60
```

**النمط ①: مضاعف (MULTIPLIER)** — «الساعة تُخصم بساعة ونصف»
```
amount = effective_lateness_minutes × multiplier × minute_rate
```
- المضاعف 1.5 يعني أن كل دقيقة تأخير تُخصم بقيمة دقيقة ونصف.
- الخصم **متناسب** مع دقائق التأخير داخل الشريحة.

**النمط ②: كسر يوم (DAY_FRACTION)** — «ربع يوم / نصف يوم / يوم كامل»
```
amount = day_fraction × day_rate
```
- الخصم **ثابت** داخل الشريحة ولا يتأثر بعدد الدقائق: من تأخر 61 دقيقة ومن تأخر 119 دقيقة يدفعان المبلغ نفسه.
- القيم الشائعة: 0.25 (ربع يوم) · 0.5 (نصف يوم) · 1.0 (يوم كامل).

**النمط ③: بالسعر نفسه (RATE_1_1)** — «دقيقة بدقيقة»
```
amount = effective_lateness_minutes × minute_rate
```
- يعادل رياضيًا المضاعف 1.0، ويُحتفظ به نمطًا مستقلًا لوضوح النية في التقارير وكشوف الموظفين.

**نمط اختياري: مبلغ مقطوع (FIXED_AMOUNT)**
```
amount = fixed_amount
```

**قواعد العمل**
1. يُختار النمط لكل شريحة استقلالًا؛ ويجوز خلط الأنماط داخل الطقم الواحد شريطة اجتياز فحص الرتابة في LT-04.
2. `period_days_basis` و`contracted_daily_hours` و`deduction_base` تُعرَّف على مستوى السياسة المُسنَدة للمسير، ويجب أن تُخزَّن مع كل سطر خصم كقيم مستخدمة فعليًا.
3. تُحفظ الأسعار الوسيطة بدقة لا تقل عن ستة أرقام عشرية، ولا يُقرَّب إلا المبلغ النهائي (انظر LT-09).
4. لا يجوز أن يتجاوز `day_fraction` القيمة 1.0.

**معايير القبول**
- AC1: شريحة بمضاعف 1.5 مع تأخير 90 دقيقة وسعر دقيقة 1.00 تُنتج 135.00 بالضبط.
- AC2: شريحة بكسر يوم 0.25 وسعر يوم 540.00 تُنتج 135.00 لأي تأخير داخل مداها.
- AC3: شريحة 1:1 مع تأخير 90 دقيقة تُنتج 90.00.
- AC4: يعرض سطر الخصم اسم النمط المطبَّق ومعادلته النصية.

---

#### LT-03 — المثال الرقمي المرجعي (مثال المواصفة)

**الوصف**
يجب أن يُنتج النظام النتائج التالية بالضبط لطقم الشرائح الموصوف، وتُعتمد هذه الأرقام كاختبار قبول مرجعي.

**المعطيات**
```
الأجر الأساسي الخاضع للخصم = 16,200 وحدة نقد / شهر
أساس أيام الشهر            = 30 يومًا
ساعات العمل التعاقدية      = 9 ساعات / يوم

سعر اليوم    = 16,200 ÷ 30 = 540.00 وحدة نقد
سعر الساعة   = 540.00 ÷ 9  =  60.00 وحدة نقد
سعر الدقيقة  =  60.00 ÷ 60 =   1.00 وحدة نقد
```

**طقم الشرائح (نمط المضاعف)**
```
الشريحة A : [60, 120)  ⇒ MULTIPLIER = 1.5
الشريحة B : [120, ∞)   ⇒ MULTIPLIER = 2.0
```

**الحساب — الشريحة A (تأخير 90 دقيقة)**
```
90 دقيقة تقع في [60, 120)  ⇒ الشريحة A
amount = 90 × 1.5 × 1.00 = 135.00 وحدة نقد
مكافئ اليوم = 135.00 ÷ 540.00 = 0.25 يوم  (ربع يوم)
```

**الحساب — الشريحة B (تأخير 150 دقيقة)**
```
150 دقيقة تقع في [120, ∞)  ⇒ الشريحة B
amount = 150 × 2.0 × 1.00 = 300.00 وحدة نقد
مكافئ اليوم = 300.00 ÷ 540.00 = 0.5556 يوم
```

**البديل بكسر اليوم للمقارنة**
```
الشريحة A : [60, 120)  ⇒ DAY_FRACTION = 0.25 ⇒ 0.25 × 540.00 = 135.00
الشريحة B : [120, ∞)   ⇒ DAY_FRACTION = 0.50 ⇒ 0.50 × 540.00 = 270.00
```

**جدول المقارنة الكامل (المبالغ بوحدة النقد)**

| دقائق التأخير | الشريحة | مضاعف (1.5 / 2.0) | كسر يوم (0.25 / 0.50) | بالسعر نفسه (1:1) |
|---|---|---|---|---|
| 59 | لا مطابقة | 0.00 | 0.00 | 0.00 |
| 60 | A | 90.00 | 135.00 | 60.00 |
| 61 | A | 91.50 | 135.00 | 61.00 |
| 90 | A | 135.00 | 135.00 | 90.00 |
| 119 | A | 178.50 | 135.00 | 119.00 |
| 120 | B | 240.00 | 270.00 | 120.00 |
| 150 | B | 300.00 | 270.00 | 150.00 |
| 240 | B | 480.00 | 270.00 | 240.00 |

**تحليل الفرق بين النمطين**
1. **المضاعف تصاعدي مستمر**: يزيد الخصم مع كل دقيقة، فيميّز بين من تأخر 61 دقيقة ومن تأخر 119 دقيقة.
2. **كسر اليوم ثابت داخل الشريحة**: عادل في البساطة والوضوح، لكنه يساوي بين تأخيرين متباعدين داخل الشريحة نفسها، وقد يصبح **أخفّ** من المضاعف عند التأخيرات الكبيرة (240 دقيقة: 270.00 مقابل 480.00).
3. **القفزة عند حدّ الشريحة (Cliff)**: بالمضاعف يقفز الخصم من 178.50 عند 119 دقيقة إلى 240.00 عند 120 دقيقة (زيادة 61.50 مقابل دقيقة واحدة)؛ وبكسر اليوم يقفز من 135.00 إلى 270.00 (زيادة 135.00). هذه القفزة **قرار عمل مقصود** في النمط العقابي، ومن أراد إلغاءها فليستخدم نمط التطبيق التصاعدي في LT-08.

**معايير القبول**
- AC1: كل خلية في جدول المقارنة أعلاه تُعدّ حالة اختبار آلية بنتيجة متوقعة صريحة، وفارق التقريب المسموح 0.00.
- AC2: تأخير 59 دقيقة لا يطابق أي شريحة ويُنتج 0.00 (بسلوك «عدم المطابقة» الافتراضي في LT-04).
- AC3: تغيير `deduction_base` إلى قيمة أخرى يُعيد اشتقاق الأسعار الثلاثة ويُغيّر كل المبالغ تناسبيًا.
- AC4: يعرض تفصيل الحساب للموظف السطر: «تأخير 90 دقيقة · الشريحة 60–120 · مضاعف 1.5 × سعر الدقيقة 1.00 = 135.00».

---

#### LT-04 — قواعد السلامة والتحقق من طقم الشرائح

**الوصف**
يجب أن يمنع النظام حفظ أي طقم شرائح غير سليم منطقيًا، وأن يُجري التحقق عند الحفظ وعند التفعيل وعند تعطيل أي شريحة، لا وقت حساب المسير.

**قواعد العمل**

**1) منع التداخل (No Overlap)**
- لأي شريحتين مُفعَّلتين في الطقم نفسه، يجب ألا يتقاطع مداهما: `NOT (A.from < B.to AND B.from < A.to)`.
- مثال مرفوض: `[60,120)` و`[100,180)` — تتقاطعان في 100..119.
- رسالة الخطأ يجب أن تحدّد الشريحتين والمدى المتقاطع.

**2) منع الفجوات (No Gaps)**
- يجب أن يغطي اتحاد الشرائح المُفعَّلة المدى `[first_from, ∞)` تغطية متصلة: نهاية كل شريحة تساوي بداية التالية بالضبط.
- مثال مرفوض: `[60,120)` ثم `[150,∞)` — الدقائق 120..149 غير مغطاة.
- المدى `[0, first_from)` ليس فجوة بل **منطقة معافاة مقصودة** يجب إظهارها للمستخدم صراحةً: «التأخير أقل من 60 دقيقة: بلا خصم».

**3) وجوب شريحة مفتوحة النهاية**
- يجب أن توجد شريحة مُفعَّلة واحدة بالضبط بـ `to_minute = ∞`.

**4) سلوك عدم مطابقة أي شريحة (`no_match_behavior`)**
| القيمة | السلوك |
|---|---|
| `NO_DEDUCTION` (افتراضي) | خصم 0.00 مع تسجيل ملاحظة «لا شريحة مطابقة» في تفصيل الحساب |
| `FALLBACK_1_1` | تطبيق 1:1 على دقائق التأخير |
| `BLOCK` | إيقاف حساب المسير ورفع استثناء يُلزم بمعالجة الطقم قبل المتابعة |

**5) الرتابة عبر الحدود (Monotonicity)**
- يجب أن تكون دالة الخصم **غير متناقصة** بالنسبة لدقائق التأخير.
- يفحص النظام عند الحفظ قيمة الخصم عند `to_minute − 1` للشريحة السابقة مقابل قيمته عند `from_minute` للشريحة التالية، ويرفض أو يحذّر تحذيرًا صريحًا عند وجود تناقص.
- **مثال على تعريف يجب رفضه**: `[60,120)` بمضاعف 1.5 (⇒ عند 119 دقيقة = 178.50) ثم `[120,∞)` بكسر يوم 0.25 (⇒ عند 120 دقيقة = 135.00). الانتقال إلى شريحة أعلى **يخفّض** الخصم، وهو خلل منطقي يكافئ زيادة التأخير.

**6) السقف اليومي (`max_daily_deduction_day_fraction`)**
- سقف يومي يُعبَّر عنه ككسر يوم (افتراضيًا 1.0) لا يتجاوزه مجموع خصمي التأخير والنقص لليوم الواحد.
- مثال: تأخير 400 دقيقة بمضاعف 2 ⇒ 800.00 وحدة نقد، والسقف 1.0 يوم = 540.00 ⇒ **المبلغ النهائي 540.00**، ويُسجَّل في التفصيل: «المحسوب 800.00 · السقف 540.00 · المطبَّق 540.00».

**7) السقف الدوري (`max_period_deduction_day_fraction`)**
- سقف اختياري لإجمالي خصومات التأخير خلال فترة المسير (مثلاً 5 أيام)، يُطبَّق بعد جمع خصومات الفترة.

**8) قيود إضافية**
- لا يجوز أن يكون المضاعف صفرًا أو سالبًا، ولا كسر اليوم صفرًا أو أكبر من 1.
- لا يجوز أن ينتج خصم سالب في أي مسار.
- تعديل طقم مستخدم في مسير قيد الإعداد يجب أن ينبّه المستخدم بأن إعادة الحساب مطلوبة.

**معايير القبول**
- AC1: محاولة حفظ `[60,120)` و`[100,180)` تُرفض برسالة تسمّي المدى المتقاطع 100–119.
- AC2: محاولة حفظ `[60,120)` و`[150,∞)` تُرفض برسالة تسمّي الفجوة 120–149.
- AC3: محاولة حفظ طقم بلا شريحة مفتوحة النهاية تُرفض.
- AC4: طقم يتناقص خصمه عند حدّ شريحة يُرفض أو يُحفظ فقط بعد إقرار صريح موثَّق في سجل التدقيق.
- AC5: تأخير 400 دقيقة مع سقف يومي 1.0 يوم يُنتج 540.00 لا 800.00، والتفصيل يُظهر القيمتين.
- AC6: تأخير 45 دقيقة مع `no_match_behavior = NO_DEDUCTION` يُنتج 0.00 وملاحظة صريحة.

---

#### LT-05 — مدة السماح (Grace) وعلاقتها بالشرائح

**الوصف**
يجب أن يوفّر النظام **مدة سماح** قابلة للضبط، مع تحديد صريح لطريقة تفاعلها مع دقائق التأخير: **هل تُطرح من التأخير أم تُلغيه بالكامل**؛ والخياران مطلوبان معًا وقابلان للاختيار.

**الأنماط**

| النمط | المعنى | المعادلة |
|---|---|---|
| `NONE` | لا سماح | `effective = raw` |
| `SUBTRACT` | تُطرح مدة السماح من التأخير ثم تُطبَّق الشرائح على الباقي | `effective = MAX(0, raw − grace_minutes)` |
| `WAIVE_ALL_OR_NOTHING` | إن كان التأخير داخل السماح فلا خصم إطلاقًا، وإن تجاوزه احتُسب التأخير **كاملًا** | `effective = (raw ≤ grace) ? 0 : raw` |

**مثال رقمي يوضّح الفارق الجوهري**
- المعطيات: `grace_minutes = 15` · التأخير الخام 70 دقيقة · سعر الدقيقة 1.00 · الشريحة `[60,120)` بمضاعف 1.5.
- `SUBTRACT`: الفعلي = 70 − 15 = **55 دقيقة** ⇒ لا يطابق أي شريحة (< 60) ⇒ الخصم **0.00**.
- `WAIVE_ALL_OR_NOTHING`: 70 > 15 ⇒ الفعلي = **70 دقيقة** ⇒ الشريحة A ⇒ 70 × 1.5 × 1.00 = **105.00**.
- الفارق **105.00 وحدة نقد** لنفس البصمة — فاختيار النمط قرار عمل لا تفصيلًا تقنيًا.
- حالة ثانية: التأخير الخام 12 دقيقة ⇒ النمطان يُنتجان 0.00 (لأن 12 ≤ 15).

**قواعد العمل**
1. مدة السماح تُعرَّف على مستوى **طقم الشرائح/السياسة**، ويجوز تجاوزها بقيمة على مستوى الوردية إذا فُعِّل ذلك صراحةً.
2. **تعطيل السماح افتراضيًا في الورديات المرنة**: عند `flexible_start_enabled = TRUE` تكون قيمة `allow_grace_on_flexible_shift = FALSE` افتراضيًا، لأن النافذة المرنة هي بذاتها مدة التسامح، وتطبيق سماح إضافي فوقها يُنتج **ازدواج تسامح** يُفرغ القاعدة FX-05 من مضمونها.
   - مثال على الازدواج المرفوض: نافذة 60 دقيقة + سماح 15 دقيقة ⇒ من يحضر 10:15 يصبح تأخيره الفعلي 75 − 15 = 60 دقيقة بدل 75، ومن يحضر 10:10 يصبح تأخيره 55 دقيقة ⇒ بلا خصم، أي أن النافذة صارت عمليًا 75 دقيقة لا 60.
3. `grace_max_uses_per_period`: حدّ أقصى لعدد مرات استفادة الموظف من السماح خلال فترة المسير؛ بعد استنفاده يُحسب التأخير الخام كاملًا. مثال: السماح 15 دقيقة بحدّ 3 مرات شهريًا؛ في المرة الرابعة يُحتسب التأخير كاملًا.
4. ترتيب التطبيق الإلزامي: `raw` ← طرح **دقائق الإذن المعتمد** ← تطبيق **مدة السماح** ← مطابقة الشريحة.
5. يجب أن يُظهر تفصيل الحساب مدة السماح المطبَّقة ونمطها وعدد مرات الاستفادة المتبقية.

**معايير القبول**
- AC1: بالمعطيات أعلاه، النمط `SUBTRACT` يُنتج 0.00 والنمط `WAIVE_ALL_OR_NOTHING` يُنتج 105.00.
- AC2: تأخير 15 دقيقة بالضبط مع سماح 15 يُنتج 0.00 في النمطين (الحدّ شامل).
- AC3: على وردية مرنة، لا تُطبَّق مدة السماح ما لم يُفعَّل `allow_grace_on_flexible_shift` صراحةً، ويُسجَّل التفعيل في سجل التدقيق.
- AC4: بعد استنفاد `grace_max_uses_per_period` يُحسب التأخير الخام كاملًا ويُظهر التفصيل سبب عدم تطبيق السماح.

---

#### LT-06 — أساس تطبيق الشرائح: يومي أم تراكم دوري

**الوصف**
يجب أن يوفّر النظام حقلًا صريحًا `application_basis` يحدّد ما إذا كانت الشرائح تُطبَّق **على كل يوم على حدة** أم على **مجموع دقائق التأخير خلال فترة المسير** أم على **عدد مرات التأخير**؛ لأن الاختيار يغيّر الناتج المالي تغييرًا جوهريًا.

**القيم**

| القيمة | المعنى | المعادلة |
|---|---|---|
| `PER_DAY` (افتراضي) | تُطابق شريحة لكل يوم مستقلًا ثم تُجمع المبالغ | `Σ (tier(L_d) applied to L_d)` |
| `PERIOD_ACCUMULATED` | تُجمع دقائق التأخير في الفترة ثم تُطابق شريحة واحدة على المجموع | `tier(Σ L_d) applied to Σ L_d` |
| `OCCURRENCE_COUNT` | تُطابق الشريحة على **عدد مرات** التأخير في الفترة لا على دقائقه | `tier(count(L_d > 0))` |

**مثال رقمي ① — التراكم يُنشئ خصمًا من العدم**
- الشرائح: `[0,60)` بلا خصم · `[60,120)` مضاعف 1.5 · `[120,∞)` مضاعف 2 · سعر الدقيقة 1.00.
- خمسة أيام تأخير: 20 · 25 · 30 · 15 · 40 دقيقة — **المجموع 130 دقيقة**.
- `PER_DAY`: كل يوم أقل من 60 دقيقة ⇒ لا مطابقة ⇒ **الخصم الكلي 0.00**.
- `PERIOD_ACCUMULATED`: 130 دقيقة ⇒ الشريحة `[120,∞)` ⇒ 130 × 2 × 1.00 = **260.00**.
- الفارق: **0.00 مقابل 260.00** لنفس السلوك الشهري تمامًا.

**مثال رقمي ② — التراكم يُشدّد العقوبة**
- يومان تأخير 90 دقيقة لكل منهما — المجموع 180 دقيقة.
- `PER_DAY`: (90 × 1.5) + (90 × 1.5) = 135.00 + 135.00 = **270.00**.
- `PERIOD_ACCUMULATED`: 180 ⇒ الشريحة `[120,∞)` ⇒ 180 × 2 × 1.00 = **360.00**.

**مثال رقمي ③ — التراكم يُخفّف مع نمط كسر اليوم**
- الشرائح بكسر اليوم: `[60,120)` = 0.25 يوم · `[120,∞)` = 0.50 يوم · سعر اليوم 540.00.
- ثلاثة أيام تأخير 90 دقيقة لكل منها — المجموع 270 دقيقة.
- `PER_DAY`: 3 × (0.25 × 540.00) = 3 × 135.00 = **405.00**.
- `PERIOD_ACCUMULATED`: 270 دقيقة ⇒ الشريحة `[120,∞)` ⇒ 0.50 × 540.00 = **270.00** للشهر كله.
- **ملاحظة تصميمية إلزامية**: نمط كسر اليوم مع التراكم الدوري يُطبَّق **مرة واحدة على الفترة بأكملها**، فيصبح أخفّ كثيرًا كلما زادت أيام التأخير. لذا يجب أن يحذّر النظام صراحةً عند دمج `DAY_FRACTION` مع `PERIOD_ACCUMULATED`.

**قواعد العمل**
1. `application_basis` حقل إلزامي في طقم الشرائح، وقيمته الافتراضية `PER_DAY`.
2. عند `PERIOD_ACCUMULATED`، تُجمع **دقائق التأخير الفعلية** (بعد الإذن والسماح) لأيام الفترة المشمولة بالمسير فقط.
3. عند `PERIOD_ACCUMULATED`، لا يُطبَّق السقف اليومي (إذ لا يوجد خصم يومي)، ويُطبَّق السقف الدوري وحده.
4. عند `OCCURRENCE_COUNT`، تُفسَّر حدود الشريحة على أنها **عدد مرات** لا دقائق، ويجب أن تُظهر الواجهة الوحدة المستخدمة بوضوح لمنع الالتباس.
5. لا يجوز خلط الأسس داخل الطقم الواحد؛ الأساس خاصية للطقم كله.
6. يجب أن يُظهر تفصيل الحساب الأساس المطبَّق والمدخل المستخدم (مجموع الدقائق أو دقائق اليوم أو عدد المرات).

**معايير القبول**
- AC1: معطيات المثال ① تُنتج 0.00 بأساس `PER_DAY` و260.00 بأساس `PERIOD_ACCUMULATED`.
- AC2: معطيات المثال ② تُنتج 270.00 و360.00 على الترتيب.
- AC3: معطيات المثال ③ تُنتج 405.00 و270.00 على الترتيب، مع ظهور تحذير عند حفظ التركيبة.
- AC4: تغيير الأساس على طقم مرتبط بمسير قيد الإعداد يُلزم بإعادة الحساب قبل الاعتماد.
- AC5: بأساس `PERIOD_ACCUMULATED` يظهر في كشف الموظف سطر واحد لخصم التأخير عن الفترة مع تفصيل الأيام المكوِّنة للمجموع.

---

#### LT-07 — ربط طقم الشرائح بسياسة تُسنَد لمسير معيّن

**الوصف**
يجب ألا يُربط طقم الشرائح بالشركة كلها ربطًا وحيدًا، بل يُغلَّف داخل **سياسة خصم (Deduction Policy)** تُسنَد إلى **مسير رواتب معيّن**، بحيث يمكن أن تعمل مسيرات مختلفة في المنشأة نفسها بأطقم شرائح مختلفة في الفترة نفسها. (تفاصيل بنية السياسة وأولوياتها ترد في قسم معادلات الرواتب.)

**قواعد العمل**
1. تُنشأ أطقم شرائح متعددة ومسمّاة، ويُعاد استخدام الطقم الواحد في أكثر من سياسة.
2. تجمع السياسة: طقم شرائح التأخير · قاعدة نقص الساعات · إعدادات السماح · سياسة التداخل · أسس الأسعار (`deduction_base`, `period_days_basis`, `contracted_daily_hours`) · السقوف.
3. يُسنَد للمسير الواحد سياسة واحدة نافذة في وقت الحساب، وتُخزَّن **نسخة السياسة** في سجل المسير عند الاعتماد لتضمن قابلية إعادة الإنتاج.
4. الموظف الذي يظهر في أكثر من مسير خلال الفترة نفسها يجب أن يُمنع بقاعدة عدم التداخل في العضوية (تُعالَج في قسم المسيرات)، ولا يُترك حلّه لطبقة الخصومات.
5. تغيير السياسة المُسنَدة لمسير **مُقفَل** ممنوع؛ وعلى مسير قيد الإعداد يُلزم بإعادة حساب كاملة مع إظهار فروق قبل/بعد.
6. يجب أن تُتيح الواجهة معاينة أثر السياسة على عيّنة موظفين قبل الاعتماد.

**معايير القبول**
- AC1: إنشاء ثلاثة أطقم شرائح مختلفة وإسنادها لثلاثة مسيرات يُنتج ثلاثة مبالغ خصم مختلفة لنفس نمط الحضور.
- AC2: نقل موظف من مسير إلى آخر يغيّر طقم الشرائح المطبَّق عليه اعتبارًا من الفترة التالية دون المساس بفترات مُقفَلة.
- AC3: كل سطر خصم يحمل معرّف السياسة ومعرّف نسخة طقم الشرائح المستخدمين.
- AC4: محاولة تعديل سياسة مسير مُقفَل تُرفض برسالة صريحة.

---

#### LT-08 — نمط تطبيق الشريحة: كامل المبلغ أم تصاعدي شرائحي

**الوصف**
يجب أن يوفّر النظام الحقل `tier_application_mode` لتحديد كيفية تطبيق الشرائح على دقائق التأخير، لأن الاختيار يغيّر الناتج ويحسم مسألة القفزة عند حدود الشرائح.

**القيم**

| القيمة | المعنى |
|---|---|
| `WHOLE` (افتراضي) | تُطابق شريحة واحدة على إجمالي دقائق التأخير، ويُطبَّق نمطها على **كل** الدقائق |
| `MARGINAL` | تُوزَّع دقائق التأخير على الشرائح، وتُحسب كل حصة بنمط شريحتها ثم تُجمع (على غرار الشرائح الضريبية) |

**معادلة النمط التصاعدي**
```
portion(T) = MAX(0, MIN(L, T.to_minute) − T.from_minute)
amount     = Σ over all tiers T:  apply_pattern(T, portion(T))
```

**مثال محسوب** (الشرائح `[60,120)` مضاعف 1.5 و`[120,∞)` مضاعف 2 · سعر الدقيقة 1.00)

| التأخير | `WHOLE` | `MARGINAL` (التفصيل) | `MARGINAL` (الناتج) |
|---|---|---|---|
| 90 | 90 × 1.5 = **135.00** | (90−60)=30 دقيقة × 1.5 | **45.00** |
| 119 | 119 × 1.5 = **178.50** | 59 × 1.5 | **88.50** |
| 120 | 120 × 2.0 = **240.00** | (120−60)=60 × 1.5 + 0 × 2 | **90.00** |
| 150 | 150 × 2.0 = **300.00** | 60 × 1.5 + 30 × 2 = 90 + 60 | **150.00** |

**قواعد العمل**
1. `WHOLE` أشدّ وأبسط في الشرح للموظف، وفيه قفزة مقصودة عند حدود الشرائح (178.50 ← 240.00).
2. `MARGINAL` يُلغي القفزة تمامًا (88.50 ← 90.00) وينتج منحنى متصلًا، وهو الأنسب حين يُشتكى من عدم عدالة الحدود.
3. نمط `DAY_FRACTION` و`FIXED_AMOUNT` غير قابلين للتوزيع التصاعدي؛ عند اختيار `MARGINAL` يُطبَّقان كاملين للشريحة التي وصلها التأخير، ويجب أن ينبّه النظام لذلك عند الحفظ.
4. النمط خاصية للطقم كله لا للشريحة.

**معايير القبول**
- AC1: كل خلية في جدول المثال أعلاه تُعدّ حالة اختبار بنتيجة متوقعة صريحة.
- AC2: في نمط `MARGINAL` يكون الفرق بين الخصم عند 119 و120 دقيقة ≤ قيمة دقيقتين من الأجر.
- AC3: يُظهر تفصيل الحساب في نمط `MARGINAL` حصة كل شريحة على حدة.

---

#### LT-09 — وحدات القياس والتقريب

**الوصف**
يجب توحيد وحدة القياس وقواعد التقريب لمنع فروق الكسور المتراكمة في المبالغ.

**قواعد العمل**
1. كل المدد تُخزَّن **بالدقائق الصحيحة**؛ والثواني تُعالَج بالحقل `seconds_rounding_mode ∈ {FLOOR, CEIL, NEAREST}` بقيمة افتراضية `FLOOR` (لمصلحة الموظف في حساب التأخير).
2. `rounding_unit_minutes ∈ {1, 5, 10, 15}` لتقريب دقائق التأخير إلى وحدة أكبر عند الحاجة (افتراضيًا 1).
3. **الأسعار الوسيطة** (`day_rate`, `hour_rate`, `minute_rate`) تُحفظ بدقة لا تقل عن **ستة أرقام عشرية**، ولا تُقرَّب مطلقًا قبل الضرب.
4. **التقريب النقدي يقع مرة واحدة فقط** على المبلغ النهائي لسطر الخصم، بمنزلتين عشريتين وبنمط `HALF_UP`.
5. مجموع أسطر الخصم يجب أن يساوي المبلغ الإجمالي المعروض تمامًا (لا فرق تقريب في الجمع).

**مثال على خطأ التقريب المبكّر**
```
الأجر = 9,000 · أساس الشهر 30 · الساعات التعاقدية 9
سعر اليوم    = 300.000000
سعر الساعة   =  33.333333
سعر الدقيقة  =   0.555556

تأخير 90 دقيقة · مضاعف 1.5:
الحساب الصحيح   : 90 × 1.5 × 0.555556 = 75.00 وحدة نقد
الحساب الخاطئ   : سعر الدقيقة مُقرَّب إلى 0.56 ⇒ 135 × 0.56 = 75.60 وحدة نقد
الفرق = 0.60 في اليوم الواحد ⇒ يتراكم إلى نحو 12.00 وحدة نقد شهريًا لموظف واحد
```

**معايير القبول**
- AC1: بالمعطيات أعلاه يُنتج النظام 75.00 لا 75.60.
- AC2: تأخير 90 ثانية بنمط `FLOOR` يُسجَّل دقيقة واحدة.
- AC3: `rounding_unit_minutes = 15` يحوّل تأخير 61 دقيقة إلى 60 أو 75 وفق النمط المختار، ويُوثَّق ذلك في التفصيل.
- AC4: مجموع أسطر خصم فترة كاملة يطابق الإجمالي المعروض بفرق 0.00.

---

#### LT-10 — الشفافية وأثر التدقيق

**الوصف**
يجب أن يكون كل مبلغ خصم تأخير **قابلًا للتفسير الكامل** من بياناته المخزَّنة دون إعادة تشغيل الحساب، وأن يظهر للموظف بصيغة مفهومة.

**قواعد العمل**
1. يحمل كل سطر خصم: التاريخ · وقت الحضور · التأخير الخام · الدقائق المُبرَّرة بإذن · مدة السماح المطبَّقة · التأخير الفعلي · معرّف الشريحة ومداها · النمط المطبَّق · المضاعف/الكسر · الأسعار المستخدمة · المبلغ المحسوب · السقف المطبَّق · المبلغ النهائي · معرّف نسخة السياسة.
2. تُخزَّن **صيغة نصية للمعادلة** كما نُفِّذت، مثل: `61 دقيقة × 1.5 × 1.000000 = 91.50`.
3. يظهر الخصم للموظف على النظام مع تفصيله، ومع رابط إلى يوم الحضور المصدر.
4. يجب أن يوفّر النظام تقريرًا يربط كل سطر خصم بسجل الحضور الذي أنتجه (تتبّع من المبلغ إلى البصمة).
5. أي إعفاء أو تعديل يدوي على خصم التأخير يُسجَّل بمُنفِّذه وسببه ووقته، ولا يُحذف السطر الأصلي بل يُوسَم «مُعفى» مع سطر مقابل.

**معايير القبول**
- AC1: فتح أي سطر خصم يعرض الحقول المذكورة كاملة دون إعادة حساب.
- AC2: إعادة تشغيل الحساب على البيانات نفسها يُنتج المبلغ نفسه بفرق 0.00.
- AC3: كشف الموظف يعرض عبارة مفهومة: «تأخير 61 دقيقة بتاريخ كذا · شريحة 60–120 دقيقة · خصم 91.50».
- AC4: إعفاء موظف من خصم تأخير يترك أثرًا كاملًا في سجل التدقيق ولا يمحو السطر الأصلي.

---

#### LT-11 — نموذج البيانات المقترح (الشرائح والسياسة)

```sql
-- طقم شرائح التأخير (مؤرَّخ السريان)
lateness_tier_set (
  id                                  PK,
  name                                VARCHAR NOT NULL,
  description                         TEXT,
  application_basis                   ENUM('PER_DAY','PERIOD_ACCUMULATED','OCCURRENCE_COUNT')
                                                NOT NULL DEFAULT 'PER_DAY',
  tier_application_mode               ENUM('WHOLE','MARGINAL') NOT NULL DEFAULT 'WHOLE',
  grace_minutes                       INT     NOT NULL DEFAULT 0,
  grace_mode                          ENUM('NONE','SUBTRACT','WAIVE_ALL_OR_NOTHING')
                                                NOT NULL DEFAULT 'NONE',
  grace_max_uses_per_period           INT     NULL,
  allow_grace_on_flexible_shift       BOOLEAN NOT NULL DEFAULT FALSE,
  no_match_behavior                   ENUM('NO_DEDUCTION','FALLBACK_1_1','BLOCK')
                                                NOT NULL DEFAULT 'NO_DEDUCTION',
  max_daily_deduction_day_fraction    DECIMAL(5,4) NULL DEFAULT 1.0000,
  max_period_deduction_day_fraction   DECIMAL(7,4) NULL,
  seconds_rounding_mode               ENUM('FLOOR','CEIL','NEAREST') NOT NULL DEFAULT 'FLOOR',
  rounding_unit_minutes               INT     NOT NULL DEFAULT 1,
  effective_from                      DATE    NOT NULL,
  effective_to                        DATE    NULL,
  version_no                          INT     NOT NULL,
  is_active                           BOOLEAN NOT NULL
)

-- شريحة واحدة داخل الطقم
lateness_tier (
  id                 PK,
  tier_set_id        FK NOT NULL,
  from_minute        INT NOT NULL CHECK (>= 0),          -- شامل
  to_minute          INT NULL     CHECK (> from_minute), -- غير شامل ، NULL = ∞
  deduction_pattern  ENUM('MULTIPLIER','DAY_FRACTION','RATE_1_1','FIXED_AMOUNT') NOT NULL,
  multiplier         DECIMAL(6,3)  NULL CHECK (> 0),
  day_fraction       DECIMAL(5,4)  NULL CHECK (> 0 AND <= 1),
  fixed_amount       DECIMAL(18,2) NULL CHECK (> 0),
  label              VARCHAR NULL,
  sort_order         INT NOT NULL,
  is_active          BOOLEAN NOT NULL DEFAULT TRUE
)

-- قاعدة خصم نقص ساعات العمل (مستقلة تمامًا عن شرائح التأخير)
shortfall_rule (
  id                          PK,
  policy_id                   FK NOT NULL,
  pattern                     ENUM('RATE_1_1','MULTIPLIER','DAY_FRACTION','TIERED') NOT NULL,
  multiplier                  DECIMAL(6,3) NULL,
  day_fraction                DECIMAL(5,4) NULL,
  tolerance_minutes           INT NOT NULL DEFAULT 0,
  max_daily_day_fraction      DECIMAL(5,4) NULL
)

-- السياسة التي تُسنَد للمسير (تفصيلها في قسم المعادلات)
deduction_policy (
  id                                PK,
  name                              VARCHAR NOT NULL,
  lateness_tier_set_id              FK NOT NULL,
  shortfall_rule_id                 FK NOT NULL,
  overlap_with_shortfall_policy     ENUM('CUMULATIVE','MAX_OF_BOTH','NET_OF_LATENESS')
                                              NOT NULL DEFAULT 'NET_OF_LATENESS',
  deduction_base_components         JSON    NOT NULL,   -- مكوّنات الأجر الداخلة في أساس الخصم
  period_days_basis                 ENUM('FIXED_30','ACTUAL_CALENDAR_DAYS','WORKING_DAYS') NOT NULL,
  contracted_daily_hours            DECIMAL(4,2) NOT NULL,
  effective_from                    DATE NOT NULL,
  effective_to                      DATE NULL,
  version_no                        INT NOT NULL
)

-- سطر خصم ناتج (قابل للتفسير الكامل)
attendance_deduction_line (
  id                      PK,
  employee_id             FK NOT NULL,
  attendance_day_id       FK NULL,        -- فارغ في أساس التراكم الدوري
  payroll_run_id          FK NOT NULL,
  work_date               DATE NULL,
  source                  ENUM('LATENESS','SHORTFALL') NOT NULL,
  tier_id                 FK NULL,
  tier_range_text         VARCHAR NULL,   -- "60–120 دقيقة"
  pattern_applied         VARCHAR NOT NULL,
  input_minutes           INT NOT NULL,
  raw_minutes             INT NOT NULL,
  excused_minutes         INT NOT NULL DEFAULT 0,
  grace_applied_minutes   INT NOT NULL DEFAULT 0,
  day_rate_used           DECIMAL(18,6) NOT NULL,
  minute_rate_used        DECIMAL(18,6) NOT NULL,
  multiplier_used         DECIMAL(6,3)  NULL,
  day_fraction_used       DECIMAL(5,4)  NULL,
  computed_amount         DECIMAL(18,2) NOT NULL,
  cap_applied_amount      DECIMAL(18,2) NULL,
  final_amount            DECIMAL(18,2) NOT NULL,
  formula_text            VARCHAR NOT NULL,   -- "61 × 1.5 × 1.000000 = 91.50"
  policy_version_id       FK NOT NULL,
  tier_set_version_id     FK NOT NULL,
  is_waived               BOOLEAN NOT NULL DEFAULT FALSE,
  waived_by, waived_at, waive_reason
)
```

**معايير القبول**
- AC1: قيود قاعدة البيانات تمنع حفظ شريحة بنمط `MULTIPLIER` بلا مضاعف، وبنمط `DAY_FRACTION` بلا كسر يوم.
- AC2: كل سطر خصم يُخزّن `formula_text` غير فارغ.
- AC3: لا يمكن حذف طقم شرائح مرجعي في أسطر خصم مُرحَّلة.

---

#### LT-12 — سيناريوهات اختبار القبول للشرائح

**المعطيات الثابتة**: الأجر 16,200 · أساس 30 يومًا · 9 ساعات · سعر اليوم 540.00 · سعر الدقيقة 1.00 · الشرائح `[60,120)` مضاعف 1.5 و`[120,∞)` مضاعف 2 · `PER_DAY` · `WHOLE` · بلا سماح.

| # | السيناريو | المدخل | الناتج المتوقع |
|---|---|---|---|
| L-01 | مطابقة الشريحة الأولى | 90 دقيقة | 135.00 |
| L-02 | مطابقة الشريحة الثانية | 150 دقيقة | 300.00 |
| L-03 | حدّ الشريحة الأدنى شامل | 60 دقيقة | 90.00 |
| L-04 | حدّ الشريحة الأعلى غير شامل | 119 دقيقة | 178.50 |
| L-05 | أول دقيقة في الشريحة الثانية | 120 دقيقة | 240.00 |
| L-06 | تحت أدنى شريحة | 59 دقيقة | 0.00 + ملاحظة «لا شريحة مطابقة» |
| L-07 | بديل كسر اليوم | 150 دقيقة (كسر 0.50) | 270.00 |
| L-08 | نمط 1:1 | 90 دقيقة | 90.00 |
| L-09 | السقف اليومي | 400 دقيقة، سقف 1.0 يوم | 540.00 (المحسوب 800.00) |
| L-10 | سماح بالطرح | خام 70، سماح 15، `SUBTRACT` | 0.00 |
| L-11 | سماح إلغائي | خام 70، سماح 15، `WAIVE_ALL_OR_NOTHING` | 105.00 |
| L-12 | إذن معتمد جزئي | خام 80، إذن 60 | 0.00 (الفعلي 20) |
| L-13 | تراكم دوري | 20+25+30+15+40 دقيقة | `PER_DAY` = 0.00 · `PERIOD_ACCUMULATED` = 260.00 |
| L-14 | تصاعدي شرائحي | 150 دقيقة، `MARGINAL` | 150.00 |
| L-15 | رفض التداخل | `[60,120)` + `[100,180)` | رفض الحفظ برسالة تحدّد 100–119 |
| L-16 | رفض الفجوة | `[60,120)` + `[150,∞)` | رفض الحفظ برسالة تحدّد 120–149 |
| L-17 | رفض التناقص | `[60,120)` مضاعف 1.5 + `[120,∞)` كسر 0.25 | رفض/تحذير صريح (178.50 ← 135.00) |
| L-18 | تكامل مع FX-05 | حضور 10:01 · إكمال 540 دقيقة | 91.50 وخصم نقص 0.00 |
| L-19 | تكامل مع FX-04 | حضور 09:45 · عمل 500 دقيقة | خصم تأخير 0.00 وخصم نقص 40.00 |
| L-20 | ثبات إعادة الحساب | إعادة تشغيل فترة مُقفَلة | فروق 0.00 في كل الأسطر |

---

## ٤) محرّك سياسات ومعادلات الرواتب

يعرّف هذا القسم متطلبات المكوّن المسؤول عن تعريف **سياسات الرواتب (Payroll Policies)** وبنودها ومعادلاتها، وربط كل مسير رواتب بسياسة واحدة، وتنفيذ الاحتساب بصورة قابلة للتفسير والتكرار والمقارنة. جميع المعرّفات في هذا القسم تحمل البادئة `PL`.

---

### PL-01 — كيان السياسة (Payroll Policy)

**الوصف**
يجب أن يوفّر النظام كيانًا مستقلًا اسمه «سياسة رواتب» يُعرَّف مرة واحدة ويُعاد استخدامه على أكثر من مسير. السياسة هي الوعاء الذي يحمل بنود الاستحقاق والخصم وشرائحها ومعاملاتها وقواعد التقريب.

**حقول الكيان**

| الحقل | النوع | إلزامي | الوصف |
|---|---|---|---|
| `code` | نص قصير فريد | نعم | كود ثابت لا يتغيّر (مثل `STRICT-BRANCH`) يُستخدم في التكامل والتقارير |
| `name` | نص | نعم | الاسم المعروض («سياسة الفروع المتساهلة») |
| `description` | نص طويل | لا | وصف الغرض وحالات الاستخدام |
| `status` | تعداد | نعم | `draft` (مسوّدة) · `active` (نشطة) · `archived` (مؤرشفة) |
| `version` | عدد صحيح | نعم | رقم النسخة، يبدأ من 1 ويزيد عند كل نشر جديد |
| `parent_policy_id` | مرجع | لا | يربط النسخ المتعاقبة في سلسلة واحدة |
| `effective_from` / `effective_to` | تاريخ | `from` نعم | نطاق سريان النسخة |
| `default_period_type` | تعداد | نعم | `calendar_month` · `custom_day_range` · `semi_monthly` |
| `default_period_start_day` / `default_period_end_day` | عدد (1–31) | عند `custom_day_range` | فترة الاحتساب الافتراضية «من يوم كذا إلى يوم كذا» |
| `default_scope_type` | تعداد | نعم | `company` · `branch` · `department` · `team` · `cost_center` · `custom` |
| `default_scope_ref` | مرجع | لا | الجهة الافتراضية المقترحة عند إنشاء مسير بهذه السياسة |
| `base_days_basis` | تعداد | نعم | أساس قسمة الأيام: `fixed_30` · `actual_period_days` · `working_days` |
| `standard_day_hours` | رقم عشري | نعم | ساعات اليوم القياسية المستخدمة في اشتقاق سعر الساعة |
| `rounding_mode` / `rounding_decimals` | تعداد / عدد | نعم | `half_up` (افتراضي) · `half_even` · `floor` · `ceil` ، وعدد المنازل العشرية للناتج النهائي |
| `division_by_zero_mode` | تعداد | نعم | `zero_with_warning` (افتراضي) · `fail_row` |
| `max_deduction_pct_of_gross` | نسبة | لا | سقف إجمالي الخصومات كنسبة من الإجمالي |
| `min_net_guarantee` | مبلغ | لا | حد أدنى مضمون للصافي |
| `currency` | تعداد | نعم | عملة الاحتساب |
| `created_by` / `created_at` / `published_by` / `published_at` | تدقيق | نعم | بيانات الإنشاء والنشر |

**قواعد العمل**

1. `code` فريد على مستوى المنشأة؛ و(`code`, `version`) فريد بالمجمل.
2. النسخة المنشورة **غير قابلة للتعديل**. أي تعديل على سياسة نشطة يُنشئ تلقائيًا نسخة جديدة بحالة `draft` تحمل `version + 1` وتنسخ كل البنود والشرائح والمعاملات (نسخ عند الكتابة — copy-on-write).
3. لا يجوز تفعيل سياسة إلا إذا احتوت على بند واحد على الأقل، ونجحت **كل** معادلاتها في التحقق (PL-05)، وخلا رسمها البياني من الحلقات (PL-06).
4. الانتقالات المسموحة للحالة: `draft → active`، `active → archived`. لا يُسمح بالرجوع من `archived` إلى `active`؛ بل تُستنسخ نسخة جديدة.
5. السياسة المؤرشفة لا يمكن إسنادها إلى مسير جديد، لكنها تبقى مقروءة ومرجعية لكل المسيرات التاريخية التي استخدمتها.
6. لا يُسمح بحذف سياسة استُخدمت في أي مسير محسوب؛ الأرشفة هي البديل الوحيد.

**معايير القبول**

- عند تعديل سياسة نشطة وحفظها، يُنشأ سجل جديد بـ `version` أعلى بواحد، وتبقى النسخة السابقة كما هي بالضبط (اختبار مقارنة حقل بحقل).
- محاولة تفعيل سياسة بلا بنود تُرفض برسالة: «لا يمكن تفعيل سياسة بلا بنود».
- محاولة حذف سياسة مستخدمة تُرفض مع بيان عدد المسيرات المرتبطة.
- إنشاء سياستين بنفس `code` ونفس `version` يُرفض بقيد فريد على مستوى قاعدة البيانات لا بالتحقق التطبيقي وحده.

---

### PL-02 — إسناد السياسة إلى المسير

**الوصف**
يجب أن يشير كل مسير رواتب إلى **سياسة واحدة بعينها ونسخة بعينها**، ويجب أن يسمح النظام بأن تحمل مسيرات مختلفة في نفس الشهر سياسات مختلفة.

**قواعد العمل**

1. عند إنشاء مسير يُخزَّن (`policy_id`, `policy_version`) معًا؛ لا يكفي تخزين معرّف السياسة وحده.
2. يجوز تغيير السياسة المسندة **ما دام المسير في حالة مسوّدة ولم يُحتسب**. بعد الاحتساب يتطلب التغيير إعادة أخذ لقطة صريحة (PL-07) وإعادة حساب كاملة. بعد الاعتماد يُمنع التغيير نهائيًا.
3. يجب أن تكون السياسة `active` عند لحظة الإسناد، وأن يقع نطاق فترة المسير داخل نطاق سريان السياسة (`effective_from` … `effective_to`)؛ خلاف ذلك يُرفض الإسناد برسالة توضّح التعارض الزمني.
4. عند اختيار نطاق للمسير (فرع/قسم/فريق/مجموعة مخصّصة) يقترح النظام السياسة الافتراضية المرتبطة بذلك النطاق، مع إمكانية تجاوز الاقتراح يدويًا.
5. يجب أن يضمن النظام أن الموظف الواحد **لا يظهر في أكثر من مسير يغطي نفس الفترة**، بحيث تنطبق عليه سياسة واحدة فقط في تلك الفترة، مع رسالة تعارض تُسمّي المسير الآخر.
6. تُسجَّل كل عملية إسناد أو تغيير سياسة في سجل التدقيق: مَن، ومتى، والسياسة السابقة، والسياسة الجديدة، وسبب التغيير (حقل إلزامي عند التغيير بعد الاحتساب).

**مثال توضيحي**
في فترة واحدة (من 26 إلى 25) تُنشأ ثلاثة مسيرات: «مسير الفرع الأول» مرتبط بسياسة متساهلة، «مسير الفرع الثاني» مرتبط بسياسة صارمة، و«مسير مجموعة مخصّصة» مرتبط بسياسة ثالثة بلا خصومات حضور. كل مسير يُحتسب بمعادلاته دون أي تأثير متبادل.

**معايير القبول**

- إنشاء مسيرين في نفس الفترة بسياستين مختلفتين ينجح، وتختلف نواتج الموظفين المتماثلين بينهما وفق فروق السياستين.
- محاولة تغيير السياسة على مسير معتمد تُرفض برسالة: «لا يمكن تغيير السياسة بعد اعتماد المسير».
- إسناد سياسة لا يغطي نطاق سريانها فترة المسير يُرفض ويعرض النطاقين.
- إضافة موظف إلى مسير ثانٍ يغطي نفس الفترة تُرفض وتُسمّي المسير المتعارض.

---

### PL-03 — بنود السياسة (Policy Components)

**الوصف**
يجب أن تتكوّن السياسة من قائمة **بنود** مرتّبة، كل بند يُنتج قيمة رقمية واحدة للموظف في الفترة، ويُصنَّف كاستحقاق أو خصم أو بند معلوماتي (لا يدخل الصافي لكنه يظهر في التفصيل).

**حقول البند**

| الحقل | الوصف |
|---|---|
| `code` | كود فريد داخل السياسة (مثل `LATE_DED`)، يُستخدم للإشارة إليه من معادلات أخرى |
| `label` | التسمية المعروضة في القسيمة والتقارير |
| `component_type` | `earning` (استحقاق) · `deduction` (خصم) · `info` (معلوماتي) |
| `value_source` | مصدر القيمة (انظر الجدول أدناه) |
| `stage` | مرحلة التنفيذ المنطقية (انظر PL-06) |
| `sequence` | ترتيب التنفيذ داخل المرحلة |
| `condition_formula` | معادلة منطقية اختيارية؛ إذا كانت نتيجتها «خطأ» يُتجاوَز البند وتُسجَّل قيمته صفرًا مع سبب التجاوز |
| `unit` | وحدة الناتج: `currency` · `days` · `hours` · `minutes` (تُحوَّل إلى عملة عبر الأسعار المشتقّة قبل الجمع) |
| `proration_mode` | `none` · `by_covered_days` · `by_covered_working_days` — للالتحاق أو الانفكاك في منتصف الفترة |
| `min_amount` / `max_amount` / `cap_pct_of_base` | حدود دنيا وعليا وسقف نسبي على ناتج البند |
| `rounding_override` | تجاوز قاعدة التقريب العامة لهذا البند |
| `visible_on_payslip` | إظهار البند في قسيمة الموظف |
| `is_active` | تعطيل بند دون حذفه |

**مصادر القيمة المطلوبة (`value_source`)**

| المصدر | الوصف | الإعدادات المطلوبة |
|---|---|---|
| `fixed_amount` | مبلغ ثابت لكل موظف يشمله البند | `amount` |
| `percent_of_base` | نسبة مئوية من أساس محدَّد | `percent`, `base_ref` (الراتب الأساسي أو الإجمالي أو ناتج بند سابق) |
| `employee_field` | قيمة مأخوذة من حقل معرَّف في ملف الموظف أو عقده (بدل سكن دائم مثلًا) | `field_ref`, سلوك عند غياب القيمة (`zero` أو `skip`) |
| `system_variable` | متغيّر محسوب من كتالوج المتغيرات مباشرة | `variable_name`, `multiplier` اختياري |
| `bracket_table` | جدول شرائح مرتّب، يُطابَق المدخل مع شريحة وتُطبَّق طريقة خصمها | مصدر المدخل (متغيّر أو معادلة)، وقائمة الشرائح |
| `formula` | نص معادلة يُقيَّم بالمُقيّم الآمن (PL-05) | `formula_text` |
| `ledger_entry` | قيد مستحق في هذه الفترة من دفتر المستحقات والمديونيات (قسط سلفة، مديونية مرحّلة، مستحق سابق) | `ledger_category`, سياسة السداد الجزئي عند عدم كفاية الصافي |

**حقول الشريحة (عند `bracket_table`)**

| الحقل | الوصف |
|---|---|
| `seq` | ترتيب الشريحة |
| `from_value` / `to_value` | حدود الشريحة؛ `from` شامل و`to` غير شامل، والشريحة الأخيرة مفتوحة النهاية |
| `unit` | وحدة المدخل: `minutes` · `hours` · `days` · `currency` |
| `method` | `no_deduction` · `multiplier_of_unit_rate` (مضاعف سعر الوحدة) · `fraction_of_day` (كسر من اليوم: ربع/نصف/يوم كامل) · `fixed_amount` · `formula` |
| `method_param` | المعامل المرافق للطريقة (1.5 · 0.25 · مبلغ · نص معادلة) |
| `application_mode` (على مستوى البند) | `matched_bracket_only` (تُطبَّق شريحة المطابقة على كامل المقدار) · `progressive` (تراكمي شريحة بشريحة) |
| `aggregation_scope` (على مستوى البند) | `per_incident` (لكل واقعة/يوم على حدة) · `per_period` (على مجموع الفترة) |

**قواعد العمل**

1. `code` فريد داخل السياسة الواحدة، وثابت لا يُعدَّل بعد النشر.
2. بند من نوع `deduction` لا يُنتج قيمة سالبة؛ الناتج السالب يُصفَّر ويُسجَّل تحذير في التفصيل.
3. جدول الشرائح يجب أن يكون **متصلًا وغير متداخل**: لا فجوات بين نهاية شريحة وبداية التي تليها، ولا تقاطع بينها، والشريحة الأخيرة مفتوحة النهاية.
4. البند بوحدة غير العملة يُحوَّل قبل الجمع باستخدام سعر اليوم/الساعة/الدقيقة المشتقّ في PL-04.
5. عند `proration_mode = by_covered_days` يُضرب ناتج البند في (`COVERED_DAYS ÷ PERIOD_DAYS`).
6. تُطبَّق الحدود بالترتيب: الناتج الخام ← الحدّ الأدنى/الأعلى ← السقف النسبي ← التقريب.
7. تعطيل بند (`is_active = false`) لا يحذف أثره من المسيرات المحسوبة سابقًا.

**معايير القبول**

- حفظ جدول شرائح فيه فجوة (تنتهي شريحة عند 60 وتبدأ التالية من 62) يُرفض برسالة تحدّد الفجوة.
- حفظ جدول شرائح متداخل (30–60 و50–90) يُرفض ويحدّد الشريحتين المتقاطعتين.
- حفظ جدول شرائح بلا شريحة أخيرة مفتوحة يُرفض.
- بند بوحدة `days` وناتج 0.25 يظهر في التفصيل كـ 0.25 يوم ويُحوَّل إلى مبلغ بضربه في سعر اليوم.
- بند خصم أنتجت معادلته −40 يُسجَّل ناتجه 0 مع تحذير «ناتج سالب لبند خصم».

---

### PL-04 — كتالوج المتغيرات المتاحة داخل المعادلات

**الوصف**
يجب أن يوفّر النظام كتالوجًا معلنًا ومغلقًا للمتغيرات التي يجوز استخدامها داخل المعادلات والشروط. لا يجوز استخدام أي معرّف خارج هذا الكتالوج. الكتالوج نفسه يحمل رقم إصدار (`catalog_version`) يُلتقط ضمن لقطة السياسة.

**الكتالوج المطلوب**

| المتغيّر | الوحدة | التعريف | كيفية الاشتقاق |
|---|---|---|---|
| `BASE_SALARY` | عملة | الراتب الأساسي التعاقدي السائد في الفترة | من العقد الساري في تاريخ نهاية الفترة؛ عند تغيّره داخل الفترة يُحسب متوسطًا موزونًا بأيام كل قيمة |
| `ALLOWANCES_TOTAL` | عملة | إجمالي البدلات الدائمة المستحقة في الفترة | مجموع البدلات الدائمة المفعّلة للموظف بعد تناسبها بأيام التغطية |
| `GROSS_SALARY` | عملة | الإجمالي الثابت | `BASE_SALARY + ALLOWANCES_TOTAL` (لا يشمل المتغيرات كالأوفرتايم والمكافآت) |
| `BASE_DAYS_BASIS` | يوم | أساس قسمة الأيام | من إعداد السياسة: 30 ثابتًا، أو عدد أيام الفترة الفعلية، أو أيام العمل الفعلية |
| `PERIOD_DAYS` | يوم | عدد أيام فترة المسير | `period_end − period_start + 1` |
| `COVERED_DAYS` | يوم | أيام تغطية الموظف داخل الفترة | من `max(period_start, hire_date)` إلى `min(period_end, termination_date)` شاملًا الطرفين؛ للموظف كامل الفترة تساوي `PERIOD_DAYS` |
| `DAY_RATE` | عملة/يوم | سعر اليوم | `(BASE_SALARY أو GROSS_SALARY حسب إعداد السياسة) ÷ BASE_DAYS_BASIS` |
| `STANDARD_DAY_HOURS` | ساعة | ساعات اليوم القياسية | من إعداد السياسة، أو من تعريف وردية الموظف عند تفعيل الاشتقاق من الوردية |
| `HOUR_RATE` | عملة/ساعة | سعر الساعة | `DAY_RATE ÷ STANDARD_DAY_HOURS` |
| `MINUTE_RATE` | عملة/دقيقة | سعر الدقيقة | `HOUR_RATE ÷ 60` |
| `LATE_MINUTES` | دقيقة | إجمالي دقائق التأخير المحتسبة في الفترة | مجموع دقائق التأخير لكل يوم بعد تطبيق قواعد الوردية ونوافذ المرونة؛ تُقاس من بداية الوردية لا من نهاية نافذة السماح |
| `LATE_INCIDENTS` | عدد | عدد الأيام التي سُجّل فيها تأخير محتسب | عدّ الأيام التي `LATE_MINUTES_OF_DAY > 0` |
| `SHORT_MINUTES` | دقيقة | دقائق نقص زمن العمل | مجموع `max(0, الزمن المطلوب − الزمن المنفَّذ)` لكل يوم حضور، بلا احتساب القيم السالبة |
| `ABSENCE_DAYS` | يوم | أيام الغياب غير المصرّح به | عدّ أيام العمل المجدولة بلا حضور وبلا إجازة معتمدة |
| `EXCUSED_ABSENCE_DAYS` | يوم | أيام الغياب المصرّح به | أيام غياب مقترنة بإذن معتمد |
| `UNPAID_LEAVE_DAYS` | يوم | أيام الإجازة بلا أجر | من سجل الإجازات المعتمدة بنوع «بلا أجر»، ضمن حدود الفترة |
| `PAID_LEAVE_DAYS` | يوم | أيام الإجازة مدفوعة الأجر | من سجل الإجازات المعتمدة مدفوعة الأجر |
| `PRESENT_DAYS` | يوم | أيام الحضور الفعلي | عدّ الأيام ذات بصمة حضور محتسبة |
| `WORKED_MINUTES` | دقيقة | إجمالي دقائق العمل المحتسبة | مجموع الزمن المنفَّذ في كل أيام الفترة |
| `REQUIRED_MINUTES` | دقيقة | إجمالي الزمن المطلوب | مجموع الزمن المقرَّر لكل أيام العمل المجدولة داخل التغطية |
| `OT_HOURS_REGULAR` | ساعة | ساعات الإضافي المعتمدة في أيام العمل العادية | مجموع ساعات الإضافي المعتمدة من دورة الاعتماد بنوع «عادي» |
| `OT_HOURS_RESTDAY` | ساعة | ساعات الإضافي المعتمدة في أيام الراحة | كسابقتها بنوع «يوم راحة» |
| `OT_HOURS_HOLIDAY` | ساعة | ساعات الإضافي المعتمدة في العطل الرسمية | كسابقتها بنوع «عطلة» |
| `OT_HOURS_NIGHT` | ساعة | ساعات الإضافي المعتمدة ضمن النافذة الليلية | كسابقتها بنوع «ليلي» |
| `OT_HOURS_TOTAL` | ساعة | مجموع كل أنواع الإضافي المعتمدة | مجموع المتغيرات الأربعة أعلاه |
| `BONUS_TOTAL` | عملة | إجمالي المكافآت الفردية المعتمدة في الفترة | مجموع المكافآت المعتمدة المسندة للفترة |
| `TYPED_DEDUCTION[<code>]` | عملة | إجمالي الخصومات المعتمدة من نوع محدَّد (جودة/التزام/إداري/إنتاجية…) | مجموع الخصومات المصنّفة التي اكتملت دورة اعتمادها وأُسندت لهذه الفترة، بعد استبعاد ما شمله إعفاء فردي |
| `TYPED_DEDUCTIONS_TOTAL` | عملة | إجمالي كل الخصومات المصنّفة المعتمدة | مجموع كل أنواع `TYPED_DEDUCTION` |
| `ADVANCE_BALANCE` | عملة | رصيد السلف المستحق القائم | الرصيد المفتوح في دفتر المستحقات والمديونيات عند بداية الفترة |
| `ADVANCE_DUE_THIS_PERIOD` | عملة | قسط السلف المستحق في هذه الفترة | مجموع الأقساط المجدولة التي يقع تاريخ استحقاقها داخل الفترة |
| `DEBT_DUE_THIS_PERIOD` | عملة | المديونيات الأخرى المستحقة في الفترة | من الدفتر، بخلاف السلف |
| `IS_ATTENDANCE_EXEMPT` | راية 0/1 | هل الموظف مستثنى من احتساب الحضور والغياب | من تصنيف الموظف؛ عند القيمة 1 تُصفَّر كل متغيرات الحضور |
| `SENIORITY_MONTHS` | شهر | أقدمية الخدمة بالأشهر حتى نهاية الفترة | الفرق بين تاريخ التعيين ونهاية الفترة |
| `COMP[<code>]` | عملة/وحدة البند | ناتج بند سابق داخل نفس السياسة | يُقرأ من ناتج البند المشار إليه بشرط أن يسبقه في ترتيب التنفيذ |
| `PARAM[<name>]` | حسب التعريف | معامل رقمي معرَّف على مستوى السياسة | من جدول معاملات السياسة، يُلتقط ضمن اللقطة |

**قواعد العمل**

1. أسماء المتغيرات بصيغة `UPPER_SNAKE_CASE`، وهي محجوزة ولا يجوز أن يحمل بند أو معامل اسمًا مطابقًا لها.
2. كل متغيّر رقمي **غير قابل للقيمة الفارغة**؛ غياب البيانات يُترجم إلى صفر، ويُسجَّل ذلك ضمن مدخلات التفصيل.
3. `IS_ATTENDANCE_EXEMPT = 1` يفرض تصفير `LATE_MINUTES`, `SHORT_MINUTES`, `ABSENCE_DAYS`, `LATE_INCIDENTS` قبل تقييم أي معادلة، بحيث ينزل الراتب ثابتًا دون الحاجة إلى تعديل السياسة.
4. الكتالوج قابل للتوسعة بإضافة متغيرات جديدة برقم إصدار أعلى؛ ولا يجوز حذف متغيّر أو تغيير تعريفه ضمن نفس الإصدار.
5. يجب أن تعرض واجهة تحرير المعادلة الكتالوج كاملًا مع الوحدة والتعريف، وأن تدعم الإكمال التلقائي من داخله فقط.

**معايير القبول**

- استخدام معرّف غير موجود في الكتالوج داخل معادلة يمنع الحفظ ويعرض: «متغيّر غير معرّف: `<name>`» مع موضعه في النص.
- موظف مصنّف مستثنى من الحضور مع وجود 3 أيام غياب في بيانات الحضور يظهر لديه `ABSENCE_DAYS = 0` وخصم غياب صفر.
- عند تغيّر الراتب الأساسي داخل الفترة (من قيمة إلى أخرى) يُحتسب `BASE_SALARY` كمتوسط موزون بأيام كل قيمة، ويظهر الوزنان ضمن مدخلات التفصيل.
- موظف التحق في منتصف الفترة تظهر لديه `COVERED_DAYS` أقل من `PERIOD_DAYS`، وتُطبَّق التناسبية على البنود المعلَّمة بذلك.

---

### PL-05 — مُقيّم المعادلات الآمن (Safe Formula Evaluator)

**الوصف**
يجب أن يُقيَّم نص المعادلة عبر مُحلِّل ومُقيّم مخصَّصين، وأن يكون التقييم **دالة نقيّة** في قيم المتغيرات فقط.

**قواعد العمل — الأمان**

1. **يُمنع منعًا باتًا** تمرير نص المعادلة إلى أي آلية تنفيذ شيفرة في لغة المنصة (تفسير ديناميكي، بناء دوال في زمن التشغيل، استدعاء صدفة نظام). يجب تحليل النص إلى شجرة تعبير (AST) عبر قواعد نحوية صارمة، ثم تقييم الشجرة بمُقيّم يعرف عُقَدًا محدودة فقط.
2. لا يملك المُقيّم أي منفذ إلى نظام الملفات أو الشبكة أو قاعدة البيانات أو متغيرات البيئة أو الزمن الحالي.
3. لا توجد إسنادات، ولا حلقات تكرار، ولا استدعاءات دوال معرَّفة من المستخدم، ولا وصول إلى خصائص كائنات.

**العمليات والدوال المسموحة**

```
العمليات الحسابية:      +   −   ×(*)   ÷(/)
النسبة المئوية:          10%  ≡ 0.10        (لاحقة على العدد فقط؛ عامل باقي القسمة غير مدعوم)
الأقواس:                ( )                 لتحديد الأولوية
عمليات المقارنة:        =  <>  <  <=  >  >=   (في معادلات الشرط فقط)
العمليات المنطقية:      AND   OR   NOT        (في معادلات الشرط فقط)
الدوال:                 MIN(a, b, …)          أصغر القيم
                        MAX(a, b, …)          أكبر القيم
                        ABS(x)                القيمة المطلقة
                        ROUND(x, n)           تقريب إلى n منزلة
                        FLOOR(x)              التقريب لأسفل
                        CEIL(x)               التقريب لأعلى
                        CLAMP(x, lo, hi)      حصر القيمة بين حدّين
                        IF(cond, a, b)        اختيار مشروط (تقييم كسول للفرع غير المختار)
```

**قواعد العمل — التحقق قبل الحفظ**

4. لا تُحفظ معادلة إلا بعد اجتياز التحقق الكامل: سلامة نحوية، وتوازن أقواس، ومعرّفات موجودة في الكتالوج، ودوال ضمن القائمة البيضاء، وعدد وسائط مطابق لكل دالة، وعدم وجود مراجع أمامية أو دائرية (PL-06).
5. **القسمة على ثابت صفر** (مثل `X / 0`) تُرفض عند الحفظ بوصفها خطأً ثابتًا.
6. **القسمة على مقام متغيّر** مسموحة عند الحفظ؛ وفي زمن التنفيذ إذا كان المقام صفرًا يُطبَّق `division_by_zero_mode` المعرَّف في السياسة: إما إرجاع صفر مع تحذير مُسجَّل في تفصيل الموظف، أو تعليم صف الموظف بحالة خطأ ومنع اعتماد المسير حتى تُعالَج.
7. حدود الحماية: طول النص ≤ 500 محرف، عمق التعشيش ≤ 10، عدد الرموز ≤ 200، ومهلة تنفيذ قصوى لكل تقييم. تجاوز أي حد يُرفض عند الحفظ.
8. الحساب الداخلي بأرقام عشرية دقيقة (نوع عشري ثابت الفاصلة، لا عائم ثنائي) بدقة 6 منازل، ويُقرَّب الناتج النهائي للبند حسب قاعدة التقريب.
9. يجب أن توفّر واجهة التحرير زر «اختبار المعادلة» يقبل قيمًا تجريبية للمتغيرات ويعرض الناتج فورًا دون حفظ.
10. رسائل الخطأ يجب أن تحدّد **موضع الخطأ** (رقم المحرف) ونوعه ونصًا مفهومًا لغير التقنيين.

**أمثلة**

```
معادلة صحيحة:
  MIN(LATE_MINUTES * MINUTE_RATE * 1.5, DAY_RATE * 0.5)

معادلة صحيحة بشرط:
  IF(ABSENCE_DAYS > 0, ABSENCE_DAYS * DAY_RATE * PARAM[ABSENCE_FACTOR], 0)

مرفوضة — متغيّر غير معرّف:
  LATE_MINUTES * SALARY_X          → «متغيّر غير معرّف: SALARY_X (الموضع 18)»

مرفوضة — دالة خارج القائمة البيضاء:
  SQRT(BASE_SALARY)                → «دالة غير مسموحة: SQRT»

مرفوضة — قسمة على صفر ثابت:
  BASE_SALARY / 0                  → «قسمة على صفر»

مرفوضة — عدد وسائط خاطئ:
  CLAMP(BASE_SALARY, 100)          → «CLAMP تتطلب 3 وسائط، ورد 2»
```

**معايير القبول**

- حفظ معادلة تحتوي على أي معرّف خارج الكتالوج يُرفض دائمًا (اختبار على 10 أسماء عشوائية).
- تنفيذ معادلة قسمتها على متغيّر قيمته صفر مع `zero_with_warning` يُنتج 0 ويُظهر تحذيرًا في تفصيل الموظف مع نص المعادلة.
- تنفيذ نفس المعادلة مع `fail_row` يعلّم صف الموظف كخطأ ويمنع اعتماد المسير حتى تُصحَّح.
- معادلة بعمق تعشيش 11 تُرفض عند الحفظ.
- اختبار أمني: نص يحاول استدعاء دالة نظام أو الوصول إلى خاصية كائن يُرفض عند التحليل النحوي ولا يصل إلى المُقيّم إطلاقًا.
- تكرار تقييم نفس المعادلة على نفس المدخلات ألف مرة يعطي نفس الناتج بالضبط (حتمية).

---

### PL-06 — ترتيب التنفيذ والاعتماد المتبادل

**الوصف**
يجب أن يكون ترتيب تنفيذ البنود صريحًا ومحدَّدًا، وأن يُسمح لبند بالإشارة إلى ناتج بند سابق، مع منع الحلقات منعًا قاطعًا.

**مراحل التنفيذ القياسية**

| المرحلة | المحتوى |
|---|---|
| 1 — الأساس والبدلات | الراتب الأساسي والبدلات الدائمة ← يتكوّن `GROSS_SALARY` |
| 2 — الاستحقاقات المتغيرة | الأوفرتايم بأنواعه، المكافآت الفردية، الاستحقاقات الاستثنائية |
| 3 — خصومات الحضور | التأخير، نقص الساعات، الغياب، الإجازة بلا أجر |
| 4 — الخصومات المصنّفة | الخصومات المعتمدة بأنواعها بعد استبعاد ما شمله إعفاء فردي |
| 5 — أقساط الدفتر | أقساط السلف والمديونيات المستحقة في الفترة |
| 6 — الصافي والسقوف | تطبيق سقف إجمالي الخصومات والحد الأدنى المضمون للصافي، ثم احتساب الصافي |

**قواعد العمل**

1. لكل بند `sequence` صحيح فريد داخل المرحلة؛ التنفيذ يسير بالمرحلة ثم بالتسلسل.
2. الإشارة إلى ناتج بند سابق تكون عبر `COMP[<code>]` فقط، ولا تصحّ إلا إذا كان البند المُشار إليه **يسبق** البند المُشير في ترتيب التنفيذ الفعلي؛ المرجع الأمامي يُرفض عند الحفظ برسالة تسمّي البندين.
3. يبني النظام رسمًا بيانيًا موجَّهًا للاعتماديات مستخرَجًا من شجرة كل معادلة (بنود ← بنود)، ويجري ترتيبًا طوبولوجيًا. أي حلقة تُرفض مع **عرض مسار الحلقة كاملًا** بصيغة `A → B → C → A`.
4. يجب توفير وضع «ترتيب تلقائي» يعيد ترتيب البنود طوبولوجيًا ويقترح تسلسلًا صالحًا، مع تحذير إذا خالف الترتيب اليدوي الاعتماديات.
5. الصافي بند نظامي محسوب: `مجموع الاستحقاقات − مجموع الخصومات`، ولا يجوز حذفه أو تجاوز موضعه في المرحلة السادسة.
6. عند تفعيل `max_deduction_pct_of_gross` أو `min_net_guarantee`، يُقلَّص الفائض من الخصومات بترتيب أولوية معرَّف على مستوى البند (أولوية التقليص)، ويُرحَّل المبلغ المُقلَّص إلى دفتر المستحقات والمديونيات إن كان البند قابلًا للترحيل، وإلا يسقط مع تسجيل السبب في التفصيل.
7. حذف أو تعطيل بند يُشير إليه بند آخر يُرفض حتى تُزال الإشارة، مع عرض قائمة البنود المعتمدة عليه.

**معايير القبول**

- إنشاء بندين يشير كل منهما إلى الآخر يُرفض عند الحفظ برسالة تحتوي مسار الحلقة.
- إنشاء ثلاثة بنود بحلقة غير مباشرة (A→B→C→A) يُرفض كذلك مع المسار الكامل.
- بند تسلسله 3 يشير إلى بند تسلسله 7 يُرفض برسالة «مرجع أمامي».
- تغيير تسلسل بند بحيث يسبق البند الذي يعتمد عليه يُرفض أو يُصحَّح تلقائيًا في وضع الترتيب التلقائي.
- تفعيل سقف خصومات 30% على موظف بلغت خصومه 45% من الإجمالي يُنتج خصومات مساوية للسقف تمامًا، ويظهر المبلغ المُقلَّص وسببه وجهة ترحيله في التفصيل.

---

### PL-07 — لقطة السياسة على المسير (Policy Snapshot)

**الوصف**
يجب أن يعمل الاحتساب على **نسخة مجمَّدة** من السياسة مرفقة بالمسير، لا على السياسة الحيّة، بحيث لا يغيّر أي تعديل لاحق على السياسة نتيجة مسير محسوب أو معتمد.

**قواعد العمل**

1. عند أول تشغيل احتساب لمسير، تُنسخ نسخًا عميقًا إلى اللقطة: ترويسة السياسة وإعداداتها، وكل البنود، وكل الشرائح، وكل معاملات السياسة، ونصوص المعادلات كما هي، مع `catalog_version` و`engine_version`.
2. تُخزَّن اللقطة بصيغة قابلة للتوثيق (JSON قانوني مرتَّب المفاتيح)، ويُحسب لها **بصمة تجزئة (hash)** تُخزَّن مع المسير وتُعرض في التقارير.
3. كل قراءة أثناء الاحتساب تتم من اللقطة حصرًا؛ يُمنع الرجوع إلى جداول السياسة الحيّة في مسار الاحتساب.
4. إعادة الحساب والمسير في حالة «مسوّدة» تستخدم **نفس اللقطة افتراضيًا**. تحديث اللقطة من النسخة النشطة الحالية يتطلب إجراءً صريحًا «تحديث السياسة على المسير» يعرض **فرقًا مفصَّلًا** بين اللقطتين قبل التأكيد، ويطلب سببًا نصيًا، ويُسجَّل في التدقيق.
5. بعد اعتماد/إقفال المسير تصبح اللقطة غير قابلة للتحديث أو الحذف نهائيًا، وتُمنع إعادة الحساب إلا بعد إجراء «إعادة فتح» موثَّق يُنشئ إصدارًا جديدًا للمسير مع الاحتفاظ بالإصدار السابق كاملًا.
6. أرشفة السياسة أو إنشاء نسخة جديدة منها لا يؤثر إطلاقًا على أي لقطة قائمة.
7. يجب أن تعرض شاشة المسير: اسم السياسة، ورقمها، ورقم نسختها، وبصمة اللقطة، وتاريخ أخذها، ومَن أخذها.

**معايير القبول**

- احتساب مسير ثم تعديل السياسة الأصلية جذريًا ثم فتح المسير: النتائج والتفصيل والبصمة كما هي دون أي تغيير.
- إعادة حساب مسير مسوّدة بعد تعديل السياسة تُنتج نفس الأرقام السابقة، ما لم يُنفَّذ إجراء تحديث اللقطة صراحةً.
- إجراء تحديث اللقطة يعرض قائمة الفروق (بنود مضافة/محذوفة/معدَّلة، شرائح متغيّرة، معاملات متغيّرة) قبل التأكيد.
- محاولة تحديث لقطة مسير معتمد تُرفض برسالة صريحة.
- بصمة اللقطة تتغيّر عند أي تعديل في محتواها، وتبقى ثابتة عند إعادة حساب بلا تغيير.

---

### PL-08 — سجلّ التفصيل (Calculation Breakdown)

**الوصف**
يجب أن يُحفَظ لكل موظف في كل مسير سجلّ تفصيلي لكل بند، يكفي وحده لتفسير أي رقم في القسيمة دون الرجوع إلى بيانات حيّة قد تكون تغيّرت.

**حقول سجل التفصيل**

| الحقل | الوصف |
|---|---|
| `run_id`, `employee_id`, `component_code`, `sequence`, `component_type` | التعريف والموضع |
| `inputs_json` | خريطة بكل متغيّر استُخدم فعلًا وقيمته وقت الاحتساب |
| `formula_text` | نص المعادلة أو وصف الشريحة المطبَّقة كما وردت **في اللقطة** |
| `matched_bracket` | الشريحة المطابقة وطريقتها ومعاملها (عند بنود الشرائح) |
| `substituted_expr` | نص المعادلة بعد استبدال المتغيرات بقيمها الرقمية |
| `result_raw` | الناتج قبل الحدود والتقريب |
| `caps_applied` | ما طُبِّق من حد أدنى/أعلى/سقف نسبي وقيمة الفارق |
| `rounding_applied` | قاعدة التقريب وقيمة فرق التقريب |
| `result_final` | الناتج النهائي الداخل في القسيمة |
| `source_refs_json` | مراجع المصادر: أيام الحضور، أو طلبات الإضافي المعتمدة، أو الخصومات المصنّفة، أو قيود الدفتر التي تكوّن منها الرقم |
| `warnings_json` | تحذيرات (قسمة على صفر، ناتج سالب مصفَّر، بيانات مفقودة عُوملت كصفر، بند تجاوزه شرط) |
| `skipped_reason` | سبب تجاوز البند إن لم يُنفَّذ |

**قواعد العمل**

1. يُكتب سجل تفصيل لكل بند لكل موظف **حتى لو كان الناتج صفرًا**، لأن الصفر نفسه يحتاج تفسيرًا.
2. كل سطر في قسيمة الموظف يجب أن يقابله سجل تفصيل واحد بالضبط، بحيث لا يوجد رقم في القسيمة بلا مصدر.
3. يجب أن توفّر الواجهة إجراء «اشرح هذا الرقم» يعرض سلسلة: المدخلات ← المعادلة/الشريحة ← الاستبدال ← الناتج الخام ← الحدود ← التقريب ← الناتج النهائي.
4. سجل التفصيل يُحفظ ما دام المسير محفوظًا، ويُحذف فقط بحذف المسير نفسه، ولا يُعدَّل يدويًا إطلاقًا.
5. إعادة الحساب تستبدل سجلات التفصيل بالكامل للمسير المسوّدة، مع الاحتفاظ بسجل من نفّذ إعادة الحساب ومتى.
6. يجب أن يكون التفصيل قابلًا للتصدير لكل موظف ولكل المسير.

**مثال على المخرَج المطلوب**

```
البند: خصم تأخير (LATE_DED)
المدخلات:   LATE_MINUTES = 75 ، MINUTE_RATE = 0.555556 ، الشريحة المطابقة: 61–120 دقيقة (مضاعف 1.5)
المعادلة:   LATE_MINUTES × MINUTE_RATE × 1.5
الاستبدال:  75 × 0.555556 × 1.5
الناتج الخام: 62.500050        الحدود: لا شيء        التقريب: half_up إلى منزلتين
الناتج النهائي: 62.50
المصادر: أيام الحضور المرجعية [يوم 11]
```

**معايير القبول**

- لكل رقم في قسيمة أي موظف يمكن الوصول إلى سجل تفصيل واحد يفسّره (اختبار تغطية 100% على مسير كامل).
- بند نتيجته صفر بسبب شرط غير محقَّق يظهر في التفصيل مع `skipped_reason` واضح.
- التفصيل يعرض نص المعادلة من اللقطة، ويظل كما هو بعد تعديل السياسة الحيّة.
- مجموع `result_final` للبنود من نوع استحقاق مطروحًا منه مجموع بنود الخصم يساوي الصافي المخزَّن للموظف بفارق صفر.

---

### PL-09 — محاكاة قبل الاعتماد (Simulation)

**الوصف**
يجب أن يوفّر النظام تشغيلًا تجريبيًا للسياسة على أعضاء المسير يعرض النتائج الكاملة دون أي حفظ نهائي أو أثر جانبي.

**قواعد العمل**

1. المحاكاة تُنتج نفس مخرجات الاحتساب الحقيقي (نتائج + تفصيل + مجاميع)، لكنها تُخزَّن في سجل مؤقت معلَّم `is_simulation = true`.
2. المحاكاة **لا** تُنشئ قسائم، ولا تُغيّر حالة المسير، ولا تستهلك أقساط سلف، ولا تُرحّل قيودًا إلى دفتر المستحقات والمديونيات، ولا تُعلّم الخصومات المصنّفة كمصروفة.
3. يجب أن تسمح المحاكاة باختيار **سياسة مختلفة عن المسندة** للمسير (تحليل «ماذا لو»)، وبتجاوز مؤقّت لقيم معاملات السياسة (`PARAM[…]`) دون تعديل السياسة نفسها.
4. مخرجات المحاكاة: جدول بالموظفين (إجمالي الاستحقاقات، إجمالي الخصومات، الصافي)، وتفصيل لكل موظف، ومجاميع المسير، وقائمة تحذيرات وأخطاء الصفوف.
5. سجل المحاكاة له تاريخ انتهاء صلاحية قابل للإعداد، ويُنظَّف تلقائيًا، ويمكن تصديره قبل ذلك.
6. المحاكاة إجراء محكوم بصلاحية، ويُسجَّل في التدقيق: مَن، ومتى، وأي سياسة، وأي تجاوزات معاملات.
7. **الحتمية**: تشغيل المحاكاة ثم الاحتساب الحقيقي بنفس السياسة وبنفس البيانات يجب أن يعطي أرقامًا متطابقة تمامًا.

**معايير القبول**

- بعد تشغيل محاكاة، حالة المسير وأرصدة السلف والخصومات المصنّفة بلا أي تغيير (مقارنة قبل/بعد على مستوى قاعدة البيانات).
- محاكاة بسياسة بديلة تعرض النتائج ولا تغيّر السياسة المسندة للمسير.
- الفرق بين نتائج المحاكاة والاحتساب الحقيقي بنفس المدخلات = صفر لكل بند ولكل موظف.
- المحاكاة قابلة للتصدير بصيغة جدولية تتضمن التفصيل.

---

### PL-10 — مقارنة السياسات (Policy Comparison)

**الوصف**
يجب أن يوفّر النظام أداة تشغّل سياستين على **نفس مجموعة الموظفين ونفس الفترة ونفس البيانات**، وتعرض الفروق بندًا بندًا لدعم قرار اختيار السياسة.

**قواعد العمل**

1. المدخلات: مجموعة الموظفين (مسير قائم أو نطاق مختار)، والفترة، والسياسة «أ»، والسياسة «ب».
2. التشغيل يتم في وضع المحاكاة لكلتا السياستين على **نفس لقطة المدخلات** (نفس بيانات الحضور والاعتمادات وقيود الدفتر) لضمان أن الفرق ناتج عن السياسة وحدها.
3. المخرَج: مصفوفة «موظف × بند» تعرض قيمة «أ»، وقيمة «ب»، والفرق المطلق، والفرق النسبي؛ وسطر مجاميع لكل بند؛ وسطر إجمالي للمسير.
4. يجب دعم الترتيب والتصفية حسب حجم الانحراف، وإبراز الصفوف التي تتجاوز عتبة يحدّدها المستخدم، وإظهار عدد الموظفين المتأثرين لكل بند.
5. البنود الموجودة في سياسة دون الأخرى تظهر بقيمة صفر في الجانب الغائب مع تعليم «غير معرَّف في هذه السياسة».
6. تُحفظ نتيجة المقارنة كمُخرَج مؤرَّخ يتضمّن بصمتَي لقطتَي السياستين ومَن نفّذها ومتى، وتكون قابلة للتصدير.
7. المقارنة لا تُعدّل أي بيانات إنتاجية إطلاقًا.

**معايير القبول**

- تشغيل مقارنة بين سياستين متطابقتين تمامًا يُنتج فروقًا صفرية لكل موظف ولكل بند.
- تشغيل المقارنة مرتين متتاليتين بنفس المدخلات يُنتج نفس النتيجة بالضبط.
- بند موجود في «ب» فقط يظهر بقيمة صفر في عمود «أ» مع وسم «غير معرَّف في هذه السياسة».
- ملف التصدير يحتوي بصمتَي اللقطتين بحيث يمكن إثبات أي نسختين قورنتا.

---

### PL-11 — التحقق من التكافؤ (Equivalence / Regression Verification)

**الوصف**
عند تغيير سياسة أو ترقية محرّك الاحتساب، يجب أن يمكن مقارنة ناتج مسير مرجعي **قبل وبعد** بندًا بندًا وإظهار أي انحراف مع تصنيف سببه.

**قواعد العمل**

1. **تجميد خط الأساس (Baseline)**: لأي مسير محسوب يمكن تجميد حزمة مرجعية تتضمّن: قائمة الأعضاء، و**قيم كل متغيرات المدخلات لكل موظف** (لا مجرد مراجع إلى بيانات حيّة)، ولقطة السياسة وبصمتها، و`engine_version`، و`catalog_version`، وكل سجلات التفصيل والنتائج النهائية.
2. **التشغيل المقابل**: يُعاد التنفيذ على **نفس المدخلات المجمَّدة** بالسياسة الجديدة و/أو المحرّك الجديد، في وضع محاكاة لا يمس أي بيانات إنتاجية.
3. **المقارنة**: بندًا بندًا لكل موظف، مع عتبة تسامح قابلة للإعداد (افتراضيًا 0.01 وحدة نقد) لاستبعاد ضجيج التقريب.
4. **تصنيف سبب الانحراف** إلزامي لكل فرق: تغيّر تعريف بند · تغيّر حدود شريحة أو طريقتها · تغيّر معامل سياسة · تغيّر ترتيب تنفيذ · تغيّر سقف/حد · تغيّر إصدار المحرّك · تغيّر إصدار كتالوج المتغيرات · **غير مفسَّر**.
5. **تقرير التكافؤ** يعرض: عدد الموظفين المتطابقين، وعدد المنحرفين، وأكبر انحراف مطلق ونسبي، وتوزيع الانحرافات حسب البند وحسب السبب، وقائمة الانحرافات غير المفسَّرة.
6. **بوابة إصدار**: لا تُعتمد ترقية محرّك الاحتساب في بيئة الإنتاج إلا بعد تشغيل تحقّق تكافؤ على مجموعة مسيرات مرجعية معتمدة بنتيجة **صفر انحرافات غير مفسَّرة**.
7. يُحفظ تقرير التكافؤ مؤرَّخًا وقابلًا للتصدير، ويُشار إليه في سجل التغيير.

**معايير القبول**

- إعادة التشغيل بنفس السياسة ونفس المحرّك على مدخلات مجمَّدة يُنتج 0 انحراف (اختبار الحياد).
- تعديل معامل واحد في السياسة ثم التشغيل يُظهر انحرافًا في البنود المتأثرة فقط، مصنَّفًا «تغيّر معامل سياسة»، وبلا انحراف في البنود غير المرتبطة.
- انحراف لا يمكن ربطه بأي تغيير معلوم يُصنَّف «غير مفسَّر» ويظهر بارزًا في أعلى التقرير.
- محاولة اعتماد ترقية محرّك مع وجود انحراف غير مفسَّر واحد تُرفض.
- تغيير بيانات الحضور الحيّة بعد التجميد لا يؤثر على نتيجة التحقق (إثبات أن المقارنة تتم على مدخلات مجمَّدة).

---

### نموذج البيانات المقترح (Proposed Data Model)

> أسماء إنجليزية قياسية عامة، مقدَّمة **كاقتراح تصميم** قابل للتكييف.

```
payroll_policy(
  id, code, name, description, status, version, parent_policy_id,
  effective_from, effective_to,
  default_period_type, default_period_start_day, default_period_end_day,
  default_scope_type, default_scope_ref,
  base_days_basis, day_rate_numerator, standard_day_hours,
  rounding_mode, rounding_decimals, division_by_zero_mode,
  max_deduction_pct_of_gross, min_net_guarantee, currency,
  created_by, created_at, published_by, published_at
)

payroll_policy_component(
  id, policy_id, code, label, component_type, value_source,
  stage, sequence, condition_formula, unit, proration_mode,
  config_json, formula_text,
  min_amount, max_amount, cap_pct_of_base, reduction_priority,
  rounding_override, carry_over_on_cap, visible_on_payslip, is_active
)

payroll_policy_bracket(
  id, component_id, seq, from_value, to_value, unit,
  method, method_param, formula_text
)

payroll_policy_parameter(
  id, policy_id, name, data_type, value, description
)

payroll_variable_catalog(
  name, unit, data_type, definition, derivation_note,
  catalog_version, is_active
)

payroll_run_policy_snapshot(
  id, run_id, policy_id, policy_version,
  snapshot_json, snapshot_hash, engine_version, catalog_version,
  taken_at, taken_by, refresh_reason
)

payroll_line_breakdown(
  id, run_id, employee_id, component_code, sequence, component_type,
  inputs_json, formula_text, matched_bracket_json, substituted_expr,
  result_raw, caps_applied_json, rounding_applied, result_final,
  source_refs_json, warnings_json, skipped_reason
)

payroll_simulation(
  id, scope_type, scope_ref, run_id, period_start, period_end,
  policy_id, policy_version, param_overrides_json,
  result_json, created_by, created_at, expires_at
)

payroll_policy_comparison(
  id, scope_ref, period_start, period_end,
  policy_a_id, policy_a_version, policy_a_hash,
  policy_b_id, policy_b_version, policy_b_hash,
  result_json, created_by, created_at
)

payroll_equivalence_baseline(
  id, run_id, snapshot_hash, engine_version, catalog_version,
  frozen_inputs_json, frozen_results_json, created_by, created_at
)

payroll_equivalence_report(
  id, baseline_id, target_policy_id, target_policy_version, target_engine_version,
  tolerance, matched_count, deviated_count, max_abs_deviation,
  deviations_json, created_by, created_at
)
```

**فهارس وقيود مقترحة**

- فريد: (`payroll_policy.code`, `version`) · (`payroll_policy_component.policy_id`, `code`) · (`payroll_policy_component.policy_id`, `stage`, `sequence`).
- فريد: (`payroll_line_breakdown.run_id`, `employee_id`, `component_code`).
- قيد: `payroll_policy_bracket.from_value < to_value`، والشريحة الأخيرة بـ `to_value` غير محدَّد.
- قيد مرجعي: لا يُحذف صف في `payroll_policy` مرتبط بأي `payroll_run_policy_snapshot`.

---

### الحالات الحدّية (Edge Cases)

| # | الحالة | السلوك المطلوب |
|---|---|---|
| 1 | موظف مستثنى من الحضور | تُصفَّر متغيرات الحضور قبل التقييم؛ ينزل الراتب ثابتًا؛ تظهر البنود المتجاوَزة بسبب واضح |
| 2 | التحاق أو انفكاك في منتصف الفترة | `COVERED_DAYS < PERIOD_DAYS`؛ تُطبَّق التناسبية على البنود المعلَّمة فقط |
| 3 | `BASE_DAYS_BASIS` يساوي صفرًا (فترة بلا أيام عمل) | يُطبَّق `division_by_zero_mode`؛ وفي وضع `fail_row` يُمنع اعتماد المسير |
| 4 | فجوة أو تداخل في جدول الشرائح | يُرفض الحفظ مع تحديد الفجوة/التقاطع |
| 5 | قيمة مدخل خارج كل الشرائح (لعدم وجود شريحة مفتوحة النهاية) | يُرفض حفظ الجدول أصلًا؛ ولا يمكن أن تنشأ الحالة في زمن التنفيذ |
| 6 | إجمالي الخصومات يتجاوز الصافي | يُطبَّق سقف السياسة وأولوية التقليص؛ ويُرحَّل الفائض القابل للترحيل إلى الدفتر مع تسجيل السبب |
| 7 | تعديل السياسة بعد اعتماد المسير | لا أثر على المسير المعتمد إطلاقًا (يعمل من اللقطة) |
| 8 | إضافة متغيّر جديد إلى الكتالوج | لا أثر على أي لقطة قائمة؛ متاح فقط للسياسات المحرَّرة بعد رفع `catalog_version` |
| 9 | حذف بند مستخدم داخل معادلة بند آخر | يُرفض مع قائمة البنود المعتمدة عليه |
| 10 | معادلة خصم أنتجت قيمة سالبة | تُصفَّر مع تحذير مسجَّل في التفصيل |
| 11 | تعديل السياسة أثناء احتساب جارٍ | يُمنع بقفل تفاؤلي على رقم النسخة؛ ويُطلب إعادة المحاولة |
| 12 | تغيّر الراتب الأساسي داخل الفترة | متوسط موزون بالأيام مع إظهار الوزنين في التفصيل |
| 13 | خصم مصنّف اعتُمد بعد احتساب المسير | لا يدخل تلقائيًا؛ يتطلب إعادة حساب صريحة للمسير المسوّدة، أو يُرحَّل للفترة التالية وفق إعداد السياسة |
| 14 | إعفاء فردي يلغي كل خصومات موظف | تُنفَّذ البنود وتُسجَّل بقيمة صفر مع `skipped_reason = «إعفاء فردي»`، حفاظًا على قابلية التفسير |
| 15 | تراكم أخطاء التقريب عبر بنود كثيرة | الحساب الداخلي بست منازل والتقريب عند مخرَج البند فقط؛ فرق التقريب مسجَّل لكل بند |

---

### سيناريوهات الاختبار (Test Scenarios)

1. **تعدّد السياسات في نفس الفترة**: إنشاء مسيرين لنفس الشهر بسياستين مختلفتين، وموظف مماثل في كل منهما ← النتائج تختلف بمقدار فروق السياستين فقط.
2. **حصانة اللقطة**: احتساب مسير ← تعديل السياسة جذريًا ← فتح المسير ← الأرقام والبصمة والتفصيل بلا تغيير.
3. **رفض الحلقات**: إنشاء اعتماد دائري ثلاثي ← رفض عند الحفظ مع عرض المسار الكامل.
4. **أمان المُقيّم**: إدخال 20 نصًا خبيثًا/غير صالح ← رفض 20/20 عند التحليل النحوي، وصفر تنفيذ.
5. **القسمة على صفر**: بيانات تجعل المقام صفرًا ← السلوك مطابق لإعداد السياسة في الوضعين.
6. **صحة الشرائح**: 6 جداول شرائح (فجوة، تداخل، بلا شريحة مفتوحة، مقلوبة الحدود، صحيحة تصاعدية، صحيحة بشريحة مطابقة) ← الرفض/القبول كما هو محدَّد.
7. **قابلية التفسير**: اختيار 30 رقمًا عشوائيًا من قسائم مسير ← لكل رقم سجل تفصيل واحد يفسّره بالكامل.
8. **حياد المحاكاة**: لقطة قاعدة بيانات قبل/بعد محاكاة ← لا فرق في أي جدول إنتاجي.
9. **حتمية المحاكاة**: محاكاة ثم احتساب حقيقي بنفس السياسة ← فرق صفري لكل بند ولكل موظف.
10. **مقارنة سياستين متطابقتين**: كل الفروق = صفر.
11. **تكافؤ محايد**: إعادة تشغيل خط أساس بنفس السياسة والمحرّك ← 0 انحراف.
12. **تكافؤ موجَّه**: تغيير معامل واحد ← انحراف في البنود المتأثرة فقط، مصنَّف بالسبب الصحيح.
13. **سقف الخصومات**: موظف خصومه 45% وسقف السياسة 30% ← الخصومات = 30% بالضبط، والفائض مرحَّل ومسجَّل.
14. **الاستثناء من الحضور**: موظف مستثنى ببيانات غياب وتأخير ← صافي ثابت وخصومات حضور صفر.
15. **التناسبية**: موظف بأيام تغطية 12 من 30 ← البنود المعلَّمة تُضرب في 0.4 وتظهر النسبة في التفصيل.

---

### مثال ختامي محسوب: موظف واحد بسياستين مختلفتين

**المعطيات المشتركة (مدخلات مجمَّدة واحدة)**

| العنصر | القيمة |
|---|---|
| الراتب الأساسي | 9,000 وحدة نقد (ر.س/ج.م) |
| بدل سكن دائم | 1,000 |
| بدل مواصلات دائم | 500 |
| الإجمالي `GROSS_SALARY` | 10,500 |
| أساس أيام القسمة | 30 يومًا (ثابت) |
| ساعات اليوم القياسية | 9 ساعات |
| فترة المسير | 30 يومًا، أيام التغطية 30 |
| وقائع التأخير | يوم (3): 40 دقيقة · يوم (11): 75 دقيقة · يوم (18): 130 دقيقة |
| دقائق نقص الساعات في الفترة | 60 دقيقة |
| أيام الغياب غير المصرّح به | يوم واحد |
| ساعات الإضافي العادي المعتمدة | 6 ساعات |
| خصم جودة معتمد (خصم مصنّف) | 200 |
| قسط سلفة مستحق في الفترة | 500 |

**الأسعار المشتقّة (متطابقة في السياستين لأن الأساس واحد)**

```
DAY_RATE    = 9,000 ÷ 30            = 300.000000
HOUR_RATE   = 300 ÷ 9               = 33.333333
MINUTE_RATE = 33.333333 ÷ 60        = 0.555556
```

**سياسة «أ» — متساهلة**

| البند | القاعدة |
|---|---|
| خصم التأخير | شرائح لكل واقعة (شريحة المطابقة): 1–30 دقيقة بلا خصم · 31–60 مضاعف 1.0 من سعر الدقيقة · 61–120 مضاعف 1.5 · 121 فأكثر ربع يوم مقطوع |
| نقص الساعات | لا خصم إذا كان مجموع الفترة ≤ 60 دقيقة |
| الغياب | 1.0 × سعر اليوم لكل يوم |
| الإضافي العادي | 1.25 × سعر الساعة |
| سقف الخصومات | 25% من الإجمالي |

```
تأخير يوم 3   : 40 دقيقة  → شريحة 31–60   → 40 × 0.555556 × 1.0 = 22.22
تأخير يوم 11  : 75 دقيقة  → شريحة 61–120  → 75 × 0.555556 × 1.5 = 62.50
تأخير يوم 18  : 130 دقيقة → شريحة 121+    → 0.25 × 300          = 75.00
إجمالي خصم التأخير                                              = 159.72

نقص الساعات   : 60 ≤ 60 (سماح)                                  = 0.00
الغياب        : 1 × 300 × 1.0                                    = 300.00
الإضافي       : 6 × 33.333333 × 1.25                             = 250.00

الاستحقاقات : 9,000 + 1,000 + 500 + 250.00                        = 10,750.00
الخصومات    : 159.72 + 0.00 + 300.00 + 200.00 + 500.00            = 1,159.72
فحص السقف   : 25% × 10,500 = 2,625.00  ← 1,159.72 دون السقف، لا تقليص
الصافي      : 10,750.00 − 1,159.72                                = 9,590.28
```

**سياسة «ب» — صارمة**

| البند | القاعدة |
|---|---|
| خصم التأخير | شرائح لكل واقعة (شريحة المطابقة): 1–15 دقيقة بلا خصم · 16–60 مضاعف 1.5 من سعر الدقيقة · 61–120 نصف يوم مقطوع · 121 فأكثر يوم كامل |
| نقص الساعات | كل دقيقة × سعر الدقيقة × 1.0 بلا سماح |
| الغياب | 1.5 × سعر اليوم لكل يوم |
| الإضافي العادي | 1.0 × سعر الساعة |
| سقف الخصومات | 40% من الإجمالي |

```
تأخير يوم 3   : 40 دقيقة  → شريحة 16–60   → 40 × 0.555556 × 1.5 = 33.33
تأخير يوم 11  : 75 دقيقة  → شريحة 61–120  → 0.5 × 300           = 150.00
تأخير يوم 18  : 130 دقيقة → شريحة 121+    → 1.0 × 300           = 300.00
إجمالي خصم التأخير                                              = 483.33

نقص الساعات   : 60 × 0.555556 × 1.0                              = 33.33
الغياب        : 1 × 300 × 1.5                                    = 450.00
الإضافي       : 6 × 33.333333 × 1.0                              = 200.00

الاستحقاقات : 9,000 + 1,000 + 500 + 200.00                        = 10,700.00
الخصومات    : 483.33 + 33.33 + 450.00 + 200.00 + 500.00           = 1,666.66
فحص السقف   : 40% × 10,500 = 4,200.00  ← 1,666.66 دون السقف، لا تقليص
الصافي      : 10,700.00 − 1,666.66                                = 9,033.34
```

**جدول المقارنة النهائي (مخرَج PL-10 لموظف واحد)**

| البند | النوع | سياسة «أ» | سياسة «ب» | الفرق (ب − أ) |
|---|---|---:|---:|---:|
| الراتب الأساسي | استحقاق | 9,000.00 | 9,000.00 | 0.00 |
| بدل سكن | استحقاق | 1,000.00 | 1,000.00 | 0.00 |
| بدل مواصلات | استحقاق | 500.00 | 500.00 | 0.00 |
| الإضافي العادي | استحقاق | 250.00 | 200.00 | −50.00 |
| **إجمالي الاستحقاقات** | | **10,750.00** | **10,700.00** | **−50.00** |
| خصم التأخير | خصم | 159.72 | 483.33 | +323.61 |
| خصم نقص الساعات | خصم | 0.00 | 33.33 | +33.33 |
| خصم الغياب | خصم | 300.00 | 450.00 | +150.00 |
| خصم جودة (مصنّف معتمد) | خصم | 200.00 | 200.00 | 0.00 |
| قسط سلفة (من الدفتر) | خصم | 500.00 | 500.00 | 0.00 |
| **إجمالي الخصومات** | | **1,159.72** | **1,666.66** | **+506.94** |
| **الصافي** | | **9,590.28** | **9,033.34** | **−556.94** |

**قراءة النتيجة**: نفس الموظف ونفس بيانات الحضور والاعتمادات وقيود الدفتر، ومع ذلك يختلف الصافي بمقدار **556.94 وحدة نقد** لأن المسير الذي يضمّه مرتبط بسياسة مختلفة. البنود المشتركة المصدر (الخصم المصنّف المعتمد وقسط السلفة) لم تتأثر لأن مصدرها خارج السياسة، بينما تأثرت كل البنود المشتقّة من الشرائح والمعاملات. كل رقم في العمودين له سجل تفصيل مستقل (PL-08) يعرض المدخلات والشريحة المطابقة والمعادلة المستبدَلة والناتج، ويظل ثابتًا بعد أي تعديل لاحق على أي من السياستين بفضل لقطة السياسة (PL-07).

---

### ٥-٦ مصفوفة تتبّع المتطلبات (Requirements Traceability)

| المعرّف | العنوان المختصر | الاعتماديات |
|---|---|---|
| AL-01 | كتالوج أنواع البدلات | — |
| AL-02 | طبيعة القيمة: ثابت/نسبة | AL-01 |
| AL-03 | الخضوع للتأمينات والضريبة | AL-01 |
| AL-04 | دخول البدل في سعر اليوم | AL-01 · AL-02 |
| AL-05 | إسناد بدل دائم | AL-01 · AL-02 |
| AL-06 | بدل لمرة واحدة مقابل متكرر | AL-05 · DD-09 (آلية الاستهلاك) |
| AL-07 | التناسب بأيام التغطية | AL-05 · AL-04 |
| AL-08 | ظهور البدلات في ملف الموظف | AL-05 |
| AL-09 | تفصيل البدلات في القسيمة | AL-05 · AL-11 |
| AL-10 | سقوف وتحقق القيم | AL-01 |
| AL-11 | لقطة البدلات في المسير | AL-05 |
| AL-12 | الأثر الرجعي للبدلات | AL-11 · DD-11 (للاسترداد) |
| DD-01 | كتالوج أنواع الخصومات | — |
| DD-02 | طرق الاحتساب والحدود | DD-01 · AL-04 (سعر اليوم) |
| DD-03 | صلاحية إنزال الخصم | DD-01 |
| DD-04 | النطاق التنظيمي | DD-03 |
| DD-05 | إنشاء الخصم كطلب | DD-02 · DD-03 · DD-04 |
| DD-06 | سلسلة اعتماد قابلة للتعريف | DD-05 |
| DD-07 | أهلية دخول المسير وتاريخ القطع | DD-06 · DD-09 |
| DD-08 | ظهور الخصم للموظف بحالته | DD-05 · DD-06 · DD-09 |
| DD-09 | الاستهلاك مرة واحدة | DD-06 |
| DD-10 | تقسيط الخصم | DD-09 |
| DD-11 | سقف حماية الصافي | DD-09 · DD-10 · EX-01 |
| DD-12 | التعديل والإلغاء والعكس | DD-09 |
| DD-13 | تقارير الخصومات | DD-05 … DD-12 |
| EX-01 | إعفاء من أي خصم في مسير محدّد | DD-09 · DD-11 |
| EX-02 | نطاق الإعفاء | EX-01 |
| EX-03 | الحقول الإلزامية وصلاحية المنح | EX-01 · DD-03 |
| EX-04 | أثر الإعفاء في القسيمة | EX-01 · EX-02 |
| EX-05 | المكافأة الفردية | DD-06 · DD-09 |
| EX-06 | تقرير حوكمة الإعفاءات | EX-01 … EX-05 |
| EX-07 | ضوابط الإعفاء وحدوده | EX-03 |
| EX-09 | علَم الاستثناء من الحضور | — |
| EX-10 | أثر الاستثناء على خصومات الحضور | EX-09 |
| EX-11 | قرار الأوفرتايم للمستثنى | EX-09 |
| EX-12 | قرار الإجازة بلا أجر للمستثنى | EX-09 · AL-04 |
| EX-13 | سريان الاستثناء بتاريخ والفترة الجزئية | EX-09 · EX-10 |
| EX-14 | ظهور المستثنى في كشوف الحضور | EX-09 · EX-13 |
| EX-15 | جدول قرارات التفاعل | EX-10 … EX-14 |
| EX-16 | حوكمة الاستثناء | EX-09 · EX-13 |

---

### ٥-٧ قرارات تصميم محسومة في هذا القسم (Design Decisions Register)

| # | القرار | المُقرَّر | المبرر |
|---|---|---|---|
| D-1 | هل يدخل البدل في سعر اليوم؟ | قرار **إلزامي وصريح لكل نوع بدل** (`affects_day_rate`)، بلا افتراضي ضمني | سعر اليوم مُضاعِف كل الخصومات الزمنية؛ إخفاؤه في إعداد يجعل كلفة يوم الغياب غير مفسَّرة |
| D-2 | هل يتناسب سعر اليوم مع أيام التغطية؟ | **لا** — يُحسب من القيمة الشهرية الكاملة | سعر اليوم معدّل لا مبلغ؛ تناسبه يُنتج خصمًا مزدوجًا على الملتحق حديثًا |
| D-3 | هل التناسب خيار عام أم لكل نوع بدل؟ | **لكل نوع بدل**، مع تجاوز على مستوى الإسناد | بدلات المقابل الزمني تتناسب، وبدلات المقابل الثابت (مواصلات/هاتف) لا تتناسب |
| D-4 | وحدة الاستهلاك في «مرة واحدة» | **القسط** (`deduction_installment`) حتى لو قسطًا واحدًا | يوحّد المنطق ويختزل الضمان إلى قيد فريد واحد في طبقة التخزين |
| D-5 | أين يُفرض ضمان «مرة واحدة»؟ | **في قاعدة البيانات** (فهرس فريد) لا في التطبيق وحده | يمنع الازدواج عند الاحتساب المتوازي أو إعادة المحاولة |
| D-6 | مصير الفائض عن سقف الحماية | **ترحيل للفترة التالية** افتراضيًا، مع سياستَي إسقاط وتعليق قابلتين للاختيار | الترحيل يحفظ حق المنشأة دون كسر حماية الصافي |
| D-7 | هل يُحتسب المبلغ المُعفى ضمن سقف الحماية؟ | **لا** | لم يُقتطع فعلًا؛ احتسابه يُضيّق السعة بلا مقابل ويؤجّل خصومات مستحقة |
| D-8 | البند المُعفى: يختفي أم يظهر بصفر؟ | **يظهر بصفر مع وسم «مُعفى»** — الإخفاء ممنوع | اختفاء البند يُفقد الموظف والمدقّق أثر إسقاط ضابط رقابي |
| D-9 | مصير الخصم بعد الإعفاء | **إسقاط نهائي** افتراضيًا، مع خيار «تأجيل لهذا الشهر فقط» | يمنع عودة الخصم مفاجئًا دون قرار جديد |
| D-10 | من يملك الإعفاء؟ | صلاحية **مستقلة وأعلى** من صلاحية الإنزال، ومنع مُنزِّل الخصم من إعفائه | فصل مهام يمنع دورة إنزال/إعفاء مغلقة بيد شخص واحد |
| D-11 | الاستثناء من الحضور: منع التوليد أم إعفاء لاحق؟ | **منع التوليد من المصدر** | الاستثناء إلغاء للقاعدة لا استثناء لحالة؛ توليد ثم إعفاء يُغرق التقرير الرقابي |
| D-12 | الأوفرتايم للمستثنى | **غير مستحق افتراضيًا**، مع تجاوز فردي عبر مسار الطلب المعتمد فقط (لا اكتشاف آلي) | من لا يخضع لقياس زمني لا يُكافأ بقياس زمني؛ والتجاوز يبقى ممكنًا بقرار موثَّق |
| D-13 | الإجازة بلا أجر للمستثنى | **تُخصم افتراضيًا**، مع تجاوز فردي | الاستثناء يُلغي الانضباط الزمني لا مبدأ الأجر مقابل مدة الخدمة |
| D-14 | ظهور المستثنى في كشوف الحضور | **يظهر بحالة «مستثنى»**، ويُستبعد من مقامات مؤشرات الالتزام | حذفه يُعمي المدير؛ وإدراجه في المؤشرات يزوّرها |
| D-15 | سريان الاستثناء | **نافذة زمنية بتقييم يوميّ**، لا علَم دائم | يمنع أثرًا رجعيًا غير مقصود ويعالج الفترات الجزئية بدقة |
| D-16 | تعديل التعريفات بعد الترحيل | **لقطة داخل المسير** — المُرحَّل لا يُمسّ | يضمن أن قسيمة قديمة تُطبع بعد سنوات بنفس الأرقام والأسماء |
| D-17 | تصحيح الخطأ بعد الترحيل | **قيد عكسي موثَّق** لا حذف ولا تعديل | يحفظ سلسلة التدقيق كاملة |
| D-18 | ما لا يقبل الإعفاء إطلاقًا | الاستقطاعات النظامية والأحكام القضائية | التزام قانوني خارج سلطة الإعفاء الإداري |

---

## ٥) البدلات والخصومات والإعفاءات والاستثناءات

يحدّد هذا القسم متطلبات أربع منظومات تُغذّي المسير من خارج معادلات السياسة: **البدلات (Allowances)** الدائمة والمؤقتة المُسنَدة للموظف، و**الخصومات المصنّفة (Typed Deductions)** التي تُنزِلها الإدارات المختلفة بدورة اعتماد بدل الورق، و**الإعفاءات والمكافآت الفردية (Exemptions & Bonuses)** التي تُسقِط خصمًا أو تضيف مستحقًا لموظف بعينه في مسير بعينه، و**الاستثناء من الحضور (Attendance Exemption)** لمن يتقاضى راتبًا ثابتًا لا يخضع لقياس زمني. المعرّفات: `AL-xx` للبدلات، `DD-xx` للخصومات المصنّفة، `EX-xx` للإعفاءات والاستثناءات. كل قيمة رقمية موصوفة بـ«افتراضية» في هذا القسم هي **قيمة أولية قابلة للضبط من واجهة الإعدادات بصلاحية**، لا ثابتًا في الشيفرة.

---

### أ) البدلات (Allowances)

### AL-01 — كتالوج أنواع البدلات

**الوصف**
يجب أن يوفّر النظام كتالوجًا مركزيًا لأنواع البدلات (Allowance Type Catalog) تُعرَّف فيه كل صفة تؤثر في احتساب البدل قبل إسناده لأي موظف، بحيث لا يُسنَد بدل إلا من نوع معرَّف في الكتالوج.

**الحقول المقترحة لنوع البدل (`allowance_type`)**

| الحقل | النوع | الوصف |
|---|---|---|
| `code` | نصّي فريد | كود ثابت (مثل `HOUSING`, `TRANSPORT`) لا يُعدَّل بعد أول استخدام |
| `name_ar` / `name_en` | نصّي | التسمية المعروضة في القسيمة والتقارير |
| `value_mode` | قائمة | `fixed_amount` · `percent_of_base` (انظر AL-02) |
| `affects_day_rate` | منطقي إلزامي | هل يدخل البدل في اشتقاق سعر اليوم (AL-04)؛ **بلا قيمة افتراضية** |
| `is_insurable` / `is_taxable` | منطقي | الخضوع للتأمينات والضريبة (AL-03) |
| `proration_mode` | قائمة | `none` · `by_covered_days` · `by_covered_working_days` (AL-07) |
| `recurrence` | قائمة | `recurring` (متكرر) · `one_time` (لمرة واحدة) · `both` (يُحدَّد عند الإسناد) |
| `max_pct_of_base` | نسبة | سقف النوع الواحد كنسبة من الأساسي (AL-10) |
| `display_order` | عدد | ترتيب الظهور في القسيمة |
| `is_active` | منطقي | تعطيل النوع دون حذفه |

**قواعد العمل**

1. حفظ نوع بدل بلا قيمة صريحة لـ`affects_day_rate` يجب أن يُرفض؛ لا يُسمح بافتراضي ضمني لهذا الحقل.
2. تعطيل نوع (`is_active = false`) يمنع إسنادات جديدة ولا يمسّ الإسنادات القائمة ولا المسيرات المحسوبة.
3. حذف نوع مستخدم في أي إسناد أو مسير ممنوع؛ البديل هو التعطيل.
4. يجب أن يُخزَّن رقم إصدار للنوع (`version`) يرتفع مع كل تعديل مؤثر ماليًا (طبيعة القيمة، الخضوع، دخول سعر اليوم، السقف)، ويُلتقط الإصدار في لقطة المسير (AL-11).
5. إدارة الكتالوج صلاحية مستقلة (`allowance_catalog.manage`) تُمنح لدور إدارة الرواتب فقط افتراضيًا.

**معايير القبول**

- محاولة حفظ نوع بدل دون تحديد `affects_day_rate` تُرفض برسالة تسمّي الحقل.
- تعطيل نوع «بدل هاتف» يمنع اختياره في شاشة إسناد بدل جديد، بينما تبقى إسنادات الموظفين الحالية سارية وتظهر في المسير التالي.
- محاولة حذف نوع مُسنَد لموظف واحد على الأقل تُرفض بكود خطأ مخصّص يذكر عدد الإسنادات المرتبطة.
- تعديل سقف النوع يرفع `version` ويظهر الإصدار الجديد في أي مسير يُحسب بعده، بينما تحتفظ المسيرات السابقة بالإصدار القديم.

---

### AL-02 — طبيعة القيمة: ثابت/نسبة

**الوصف**
يجب أن يدعم النظام طبيعتين لقيمة البدل: **مبلغ ثابت (Fixed Amount)** بوحدة النقد، أو **نسبة من الراتب الأساسي (Percent of Base)** تُحتسب في كل مسير من الأساسي الساري في الفترة.

**قواعد العمل**

1. عند `value_mode = fixed_amount` يُخزَّن المبلغ في الإسناد (`amount`) ويُحتسب كما هو قبل التناسب.
2. عند `value_mode = percent_of_base` تُخزَّن النسبة (`percent`) ويُحتسب المبلغ في كل مسير من `BASE_SALARY` الملتقط في لقطة الموظف لتلك الفترة؛ تغيّر الأساسي داخل الفترة يُعامل بالمتوسط الموزون نفسه المستخدم في `BASE_SALARY`.
3. النسبة تُعبَّر عنها كعدد عشري بدقة منزلتين من 0.01 إلى 100.00 (بالمئة).
4. يُسمح بتجاوز `value_mode` على مستوى الإسناد فقط إذا كان النوع معلَّمًا بـ`recurrence = both` أو صُرّح بذلك في النوع (`allow_mode_override = true`).

```
مثال:
  BASE_SALARY = 9,000.00
  بدل سكن   : percent_of_base 25%  → 9,000.00 × 0.25 = 2,250.00
  بدل مواصلات: fixed_amount         → 500.00
  عند رفع الأساسي إلى 10,000 من منتصف فترة (30 يومًا، 15 يومًا لكل قيمة):
  BASE_SALARY = (9,000 × 15 + 10,000 × 15) / 30 = 9,500.00
  بدل سكن = 9,500.00 × 0.25 = 2,375.00
```

**معايير القبول**

- بدل بنسبة 25% على أساسي 9,000 يظهر في القسيمة بقيمة 2,250.00، وعند تغيّر الأساسي إلى 10,000 كامل الفترة يظهر 2,500.00 دون أي تدخّل يدوي.
- إدخال نسبة 0 أو أكبر من 100 أو بأكثر من منزلتين يُرفض عند الحفظ.
- سجل التفصيل لبدل بالنسبة يعرض: الأساسي المستخدم، النسبة، الناتج قبل التناسب وبعده.

---

### AL-03 — الخضوع للتأمينات والضريبة

**الوصف**
يجب أن يحمل كل نوع بدل علَمَين مستقلّين لخضوعه لاشتراكات التأمينات الاجتماعية (Insurable) وللضريبة (Taxable)، وأن يُنتج النظام مجاميع منفصلة للأجر الخاضع لكل منهما تُستهلك في بنود الاستقطاعات النظامية.

**قواعد العمل**

1. يُحسب `INSURABLE_ALLOWANCES_TOTAL` و`TAXABLE_ALLOWANCES_TOTAL` كمجموع البدلات المستحقة في الفترة (بعد التناسب) التي يحمل نوعها العلَم المقابل، ويُتاحان كمتغيّرات في كتالوج المعادلات.
2. تغيير علَم الخضوع على النوع يسري من المسير التالي فقط ويرفع `version`؛ المسيرات المحسوبة تحتفظ بالقيمة الملتقطة.
3. يجب أن تُعرض في القسيمة إشارة (رمز أو عمود) تبيّن للموظف أي بدلاته خاضعة.
4. يُفضَّل دعم سقف اختياري للأجر الخاضع (`insurable_wage_cap`) على مستوى الجهة يُطبَّق بعد الجمع.

```
مثال:
  الأساسي 9,000 · سكن 2,250 (خاضع تأمين + ضريبة) · مواصلات 500 (غير خاضع تأمين، خاضع ضريبة) · طبيعة عمل 300 (غير خاضع للاثنين)
  INSURABLE_WAGE = 9,000 + 2,250            = 11,250.00
  TAXABLE_WAGE   = 9,000 + 2,250 + 500      = 11,750.00
```

**معايير القبول**

- الموظف في المثال أعلاه يظهر لديه أجر خاضع للتأمين 11,250.00 وأجر خاضع للضريبة 11,750.00 في سجل التفصيل.
- تغيير «بدل مواصلات» إلى خاضع للتأمين يرفع الأجر الخاضع إلى 11,750.00 في المسير التالي فقط، ويبقى المسير المعتمد السابق عند 11,250.00.
- القسيمة تميّز بصريًا البدلات الخاضعة عن غير الخاضعة.

---

### AL-04 — دخول البدل في سعر اليوم

**الوصف**
يجب أن يحدّد كل نوع بدل صراحةً ما إذا كان يدخل في **أساس سعر اليوم (Day-Rate Base)** الذي تُشتقّ منه خصومات الغياب والتأخير ونقص الساعات، لأن سعر اليوم مُضاعِف كل خصم زمني، وإخفاء هذا القرار في إعداد عام يجعل كلفة يوم الغياب غير مفسَّرة.

**قواعد العمل**

1. تُعرَّف قاعدة اشتقاق سعر اليوم كما يلي، وتُستخدم نفسها في كل بند زمني بالمسير:

```
DAY_RATE_BASE = BASE_SALARY + Σ (البدلات المستحقة التي affects_day_rate = true)
DAY_RATE      = DAY_RATE_BASE ÷ BASE_DAYS_BASIS
```

2. سعر اليوم **لا يتناسب** بأيام التغطية؛ يُحسب من القيمة الشهرية الكاملة للأساسي والبدلات الداخلة، لأنه معدّل لا مبلغ، وتناسبه يُنتج خصمًا مزدوجًا على الملتحق حديثًا.
3. البدلات لمرة واحدة (AL-06) لا تدخل في سعر اليوم مطلقًا حتى لو كان نوعها معلَّمًا بالدخول.
4. يجب أن يعرض سجل التفصيل مكوّنات `DAY_RATE_BASE` بندًا بندًا مع علَم كل بند.

```
مثال (أساس 30 يومًا):
  الأساسي 9,000 · سكن 2,250 (يدخل) · مواصلات 500 (لا يدخل)
  DAY_RATE_BASE = 9,000 + 2,250 = 11,250.00
  DAY_RATE      = 11,250.00 ÷ 30 = 375.00
  خصم يومَي غياب = 2 × 375.00 = 750.00
  لو كان السكن لا يدخل: DAY_RATE = 300.00 والخصم = 600.00
```

**حالات حدّية**
- موظف التحق في اليوم 16 من فترة 30 يومًا: بدل السكن المستحق يتناسب إلى 1,125.00 لكن `DAY_RATE_BASE` يبقى 11,250.00 (القيمة الشهرية الكاملة).

**معايير القبول**

- تبديل علَم `affects_day_rate` لبدل السكن من «لا» إلى «نعم» يغيّر سعر اليوم من 300.00 إلى 375.00 في المسير التالي ويظهر الفارق في التفصيل.
- الملتحق في منتصف الفترة يُحسب له سعر يوم مطابق لزميله كامل الفترة بنفس الأساسي والبدلات.
- بدل لمرة واحدة بقيمة 1,000 لا يغيّر سعر اليوم مهما كان علَم نوعه.

---

### AL-05 — إسناد بدل دائم

**الوصف**
يجب أن يوفّر النظام إسناد بدل دائم (Recurring Allowance Assignment) لموظف بعينه من نوع معرَّف في الكتالوج، بقيمة وتاريخ سريان وتاريخ انتهاء اختياري، بحيث يدخل في كل مسير تقع فترته داخل نافذة السريان دون إعادة إدخال، ويملك المستخدم المخوَّل منحه لموظف والامتناع عنه لآخر.

**الحقول المقترحة (`employee_allowance`)**

| الحقل | الوصف |
|---|---|
| `employee_id` · `allowance_type_id` | الموظف والنوع |
| `value_mode` · `amount` / `percent` | تجاوز اختياري للنوع، وقيمة الإسناد |
| `effective_from` · `effective_to` | نافذة السريان (النهاية اختيارية = مفتوح) |
| `proration_override` | تجاوز اختياري لوضع التناسب المعرَّف على النوع |
| `status` | `active` · `ended` · `cancelled` |
| `created_by` · `approved_by` · `reason` | من أنشأ، من اعتمد، ولماذا |

**قواعد العمل**

1. لا يجوز أن يكون للموظف إسنادان **نشطان متداخلا السريان** من نفس النوع؛ التعديل يكون بإنهاء الإسناد القائم بتاريخ وإنشاء إسناد جديد يبدأ بعده (لا تعديل مباشر للقيمة يمحو التاريخ).
2. يدخل الإسناد في مسير ما إذا تقاطعت نافذة سريانه مع فترة المسير بيوم واحد على الأقل، ويُحتسب على الأيام المتقاطعة فقط وفق AL-07.
3. الإسناد يمرّ بدورة اعتماد قابلة للتعريف (خطوة واحدة على الأقل: إدارة الرواتب) قبل أن يصبح `active`؛ يُفضَّل السماح بتعطيل الدورة لدور محدّد.
4. الصلاحية: `allowance.assign` بنطاق تنظيمي (شركة/فرع/قسم)؛ لا يمنح المستخدم بدلًا لموظف خارج نطاقه، ولا لنفسه.
5. إلغاء إسناد (`cancelled`) لا يحذف أثره من مسيرات حُسبت قبل الإلغاء.

**معايير القبول**

- إسناد بدل سكن 2,000 بسريان من 2026-01-01 مفتوح النهاية يظهر تلقائيًا في كل مسير لاحق دون إعادة إدخال.
- محاولة إنشاء إسناد ثانٍ من نفس النوع بسريان يتداخل مع القائم تُرفض بذكر الإسناد المتعارض.
- مستخدم بنطاق «فرع الرياض» لا يستطيع إسناد بدل لموظف في «فرع جدة»، ولا لنفسه.
- إنهاء إسناد بتاريخ 2026-03-15 يجعل مسير مارس يحتسبه على الأيام المتقاطعة فقط، ولا يظهر في مسير أبريل.

---

### AL-06 — بدل لمرة واحدة مقابل متكرر

**الوصف**
يجب أن يدعم النظام، إلى جانب البدل المتكرر، **بدلًا لمرة واحدة (One-Time Allowance)** يُسنَد لفترة مسير محدّدة ويُستهلك مرة واحدة فقط بنفس آلية الاستهلاك المعرَّفة للخصومات (DD-09)، ولا يتكرر ولا يدخل في سعر اليوم.

**قواعد العمل**

1. الحقل `recurrence` على الإسناد: `recurring` (نافذة سريان) أو `one_time` (يحمل `target_period_id` أو تاريخ استحقاق يقع داخل فترة واحدة).
2. البدل لمرة واحدة يُعامل كقيد في دفتر المستحقات (`ledger_entry` بفئة `one_time_allowance`) يُعلَّم `consumed` بمرجع المسير الذي استهلكه، ويُمنع دخوله في مسير ثانٍ بقيد فريد في طبقة التخزين.
3. البدل لمرة واحدة **لا يتناسب** بأيام التغطية ولا يدخل في `DAY_RATE_BASE` ولا في `ALLOWANCES_TOTAL`؛ يُجمع في متغيّر مستقل `ONE_TIME_ALLOWANCES_TOTAL`.
4. إذا لم يُحسب مسير للفترة المستهدفة (موظف بلا مسير)، يبقى القيد `pending` ويُدرج في تقرير «قيود بلا مسير» ويُستهلك في أول مسير لاحق يشمل الموظف، مع تعليم ذلك في التفصيل.
5. إلغاء ترحيل مسير استهلك القيد يعيده إلى `pending` (DD-12).

```
مثال:
  بدل سكن متكرر 2,000 (يدخل سعر اليوم) + بدل انتقال لمرة واحدة 1,500 في فترة يونيو
  ALLOWANCES_TOTAL          = 2,000.00
  ONE_TIME_ALLOWANCES_TOTAL = 1,500.00
  DAY_RATE_BASE             = 9,000 + 2,000 = 11,000.00  (الـ1,500 خارجه)
  إجمالي الاستحقاق = 9,000 + 2,000 + 1,500 = 12,500.00
```

**معايير القبول**

- بدل لمرة واحدة بقيمة 1,500 مستهدف لفترة يونيو يظهر في مسير يونيو فقط ولا يظهر في يوليو.
- إعادة حساب مسير يونيو لا يُنتج القيد مرتين، ومحاولة إدراجه في مسير آخر متزامن تُرفض على مستوى قاعدة البيانات.
- الموظف الملتحق في منتصف الفترة يستلم البدل لمرة واحدة كاملًا (1,500.00) لا متناسبًا.
- إلغاء ترحيل مسير يونيو يعيد القيد إلى `pending` ويظهر في مسير يونيو المُعاد حسابه.

---
### AL-07 — التناسب بأيام التغطية

**الوصف**
يجب أن يحتسب النظام البدل المتكرر للموظف الذي التحق أو انفكّ أو بدأ/انتهى إسناده داخل الفترة **بنسبة الأيام المتقاطعة** بين نافذة سريان الإسناد وأيام تغطية الموظف داخل فترة المسير، وفق وضع التناسب المعرَّف على نوع البدل مع إمكان تجاوزه على مستوى الإسناد.

**قواعد العمل**

1. أوضاع التناسب: `none` (المبلغ كاملًا متى تقاطعت النافذة بيوم واحد) · `by_covered_days` (أيام تقويمية) · `by_covered_working_days` (أيام عمل مجدولة).
2. وضع التناسب يُعرَّف **لكل نوع بدل** (بدلات المقابل الزمني كالسكن تتناسب؛ بدلات المقابل الثابت كالهاتف قد لا تتناسب)، ويجوز تجاوزه في الإسناد بصلاحية.
3. المعادلة الملزمة:

```
allowance_from = MAX(period_start, hire_date, assignment.effective_from)
allowance_to   = MIN(period_end, COALESCE(termination_date, period_end), COALESCE(assignment.effective_to, period_end))
eligible_days  = (allowance_to − allowance_from) + 1          … إذا كانت allowance_to ≥ allowance_from وإلا 0
by_covered_days:          amount × eligible_days ÷ PERIOD_DAYS
by_covered_working_days:  amount × eligible_working_days ÷ scheduled_working_days_in_period
```

4. التقريب إلى منزلتين يُطبَّق على ناتج كل بدل على حدة بعد التناسب، بقاعدة التقريب العامة للجهة.
5. يجب ألا يُطبَّق التناسب مرتين: بند السياسة الذي يقرأ `ALLOWANCES_TOTAL` يستلم القيمة **بعد** تناسبها ويجب أن يكون `proration_mode = none` عليه، ويُظهر النظام تحذيرًا عند تعارض ذلك.

```
مثال (فترة 1–30 يونيو، 30 يومًا):
  موظف التحق في 16 يونيو، بدل سكن 2,000 (by_covered_days)، بدل هاتف 150 (none)
  سكن  = 2,000 × 15 ÷ 30 = 1,000.00
  هاتف = 150.00 (كاملًا)
  بدل بدأ إسناده 21 يونيو بقيمة 900 لموظف كامل الفترة = 900 × 10 ÷ 30 = 300.00
```

**حالات حدّية**
- انتهاء الخدمة في يوم بدء الإسناد نفسه: `eligible_days = 1`.
- إسناد ينتهي قبل بداية الفترة بيوم: لا يدخل المسير إطلاقًا.

**معايير القبول**

- الملتحق في 16 يونيو يستلم بدل سكن 1,000.00 وبدل هاتف 150.00 كما في المثال.
- تجاوز وضع التناسب على إسناد بعينه إلى `none` يجعل السكن 2,000.00 لنفس الموظف ويُسجَّل التجاوز في أثر التدقيق.
- سجل التفصيل يعرض `allowance_from` و`allowance_to` و`eligible_days` والمقام المستخدم لكل بدل متناسب.

---

### AL-08 — ظهور البدلات في ملف الموظف

**الوصف**
يجب أن يعرض النظام للموظف، في ملفه الذاتي (Self-Service)، قائمة بدلاته الدائمة والمؤقتة بحالتها الحالية، وأن يعرض لمن يملك الصلاحية في ملف الموظف الإداري سجلّ البدلات كاملًا بتاريخه.

**قواعد العمل**

1. يرى الموظف: اسم البدل، طبيعة القيمة، القيمة، تاريخ السريان والانتهاء، الحالة، وهل يخضع للتأمين/الضريبة؛ ولا يرى: من أنشأ الإسناد، ملاحظات داخلية، أو سقوف الكتالوج.
2. الإسنادات المعلّقة في دورة الاعتماد تظهر للموظف بحالة «قيد الاعتماد» دون قيمة إن اختارت الجهة ذلك (إعداد `show_pending_allowance_amount`، افتراضي: إظهار).
3. يرى المدير في نطاقه بدلات موظفيه ذاتها بصيغة القراءة فقط ما لم يملك صلاحية الإسناد.
4. أي تغيير في حالة إسناد (اعتماد، إنهاء، إلغاء) يُنتج إشعارًا للموظف.

**معايير القبول**

- موظف له ثلاثة إسنادات (نشط، منتهٍ، معلّق) يرى الثلاثة بحالاتها مع تمييز بصري، ولا يرى إسنادات زملائه.
- اعتماد إسناد جديد يُظهره في ملف الموظف خلال نفس الجلسة (بدون انتظار مسير) ويرسل إشعارًا.
- مدير قسم يرى بدلات موظفي قسمه فقط وبصيغة قراءة.

---

### AL-09 — تفصيل البدلات في القسيمة

**الوصف**
يجب أن تعرض قسيمة الراتب (Payslip) كل بدل مستحق كسطر مستقل باسمه وقيمته الشهرية وقيمته بعد التناسب، مأخوذًا من لقطة البدلات في المسير (AL-11) لا من الإسناد الحيّ، بحيث تُطبع القسيمة بعد سنوات بنفس الأرقام والأسماء.

**قواعد العمل**

1. لكل سطر بدل في القسيمة: الاسم كما كان وقت الحساب، طبيعة القيمة، القيمة الأساسية، أيام الأهلية والمقام إن تناسب، القيمة النهائية، علامتا الخضوع.
2. البدلات المتكررة تُجمع تحت «إجمالي البدلات الدائمة» والبدلات لمرة واحدة تحت «بدلات غير متكررة»، ويُعرض المجموعان منفصلين.
3. لا يُخفى بدل بقيمة صفر ناتج عن تناسب (مثل أهلية صفر يوم) بل يُعرض بصفر مع سبب مختصر.
4. الترتيب وفق `display_order` الملتقط في اللقطة.

**معايير القبول**

- قسيمة الموظف في مثال AL-07 تعرض سطرين: «بدل سكن 2,000.00 × 15/30 = 1,000.00» و«بدل هاتف 150.00»، وإجمالي بدلات دائمة 1,150.00.
- إعادة تسمية «بدل سكن» في الكتالوج إلى «بدل إسكان» لا تغيّر نص القسيمة المعتمدة سابقًا.
- قسيمة بها بدل متكرر وبدل لمرة واحدة تعرض المجموعين منفصلين ويُطابق مجموعهما إجمالي الاستحقاقات من البدلات.

---

### AL-10 — سقوف وتحقق القيم

**الوصف**
يجب أن يتحقّق النظام من قيم البدلات عند الإسناد وعند الاحتساب وفق سقوف قابلة للضبط، ويرفض أو يحذّر بحسب سياسة الجهة، منعًا للأخطاء الإدخالية (رقم زائد أو تاريخ مقلوب) ومنعًا لتجاوز نطاق التعويض المسموح.

**السقوف والقيم الافتراضية (قابلة للضبط)**

| الضابط | الافتراضي | السلوك عند التجاوز |
|---|---|---|
| سقف البدل الواحد كنسبة من الأساسي (`max_pct_of_base` على النوع) | **100%** من `BASE_SALARY` | رفض الحفظ (`hard`) أو تحذير مع تبرير إلزامي (`soft`)؛ الافتراضي `hard` |
| سقف إجمالي البدلات المتكررة للموظف (`max_total_allowances_pct`) | **200%** من `BASE_SALARY` | رفض الإسناد الذي يجعل المجموع يتجاوز السقف |
| الحدّ الأدنى للمبلغ | 0.01 | رفض الصفر والسالب |
| دقة المبلغ | منزلتان عشريتان | رفض أكثر من منزلتين |
| النسبة | 0.01 إلى 100.00 | رفض ما خرج عن المدى |
| التواريخ | `effective_from ≤ effective_to` | رفض التاريخ المقلوب |
| السريان الرجعي | لا يسبق `effective_from` أول يوم في الفترة المفتوحة الحالية إلا بصلاحية AL-12 | رفض أو تحويل إلى تسوية |

**قواعد العمل**

1. سقف البدل الواحد بالنسبة يُطبَّق على النوع الثابت كما على النسبي: بدل ثابت 9,500 على أساسي 9,000 يتجاوز 100%.
2. يُحسب سقف الإجمالي على البدلات المتكررة النشطة في تاريخ سريان الإسناد الجديد؛ البدلات لمرة واحدة خارج السقف ولها سقف مستقل (`max_one_time_pct`، افتراضي **100%** من الأساسي).
3. يُعاد التحقق من السقوف عند الاحتساب أيضًا (لأن الأساسي قد ينخفض بعد الإسناد)؛ التجاوز عند الاحتساب يُنتج **تحذيرًا لا رفضًا** يظهر في معاينة المسير.
4. كل سقف قابل للتعريف على مستوى الجهة مع تجاوز اختياري على مستوى النوع.

```
مثال:
  الأساسي 9,000 · السقف الواحد 100% = 9,000 · سقف الإجمالي 200% = 18,000
  بدلات نشطة: سكن 2,250 + مواصلات 500 + طبيعة عمل 300 = 3,050
  إسناد جديد «بدل مسؤولية» 15,000 → المجموع 18,050 > 18,000 → يُرفض
  إسناد «بدل مسؤولية» 9,500 → 9,500 > 9,000 سقف الواحد → يُرفض وإن كان المجموع 12,550 ضمن الإجمالي
```

**معايير القبول**

- إسناد بدل ثابت 9,500 على أساسي 9,000 يُرفض بذكر السقف 100% والقيمة الحدّية 9,000.00.
- إسناد يجعل الإجمالي 18,050 على سقف 18,000 يُرفض بذكر المجموع الحالي والمتبقّي المسموح (14,950.00).
- إدخال مبلغ 1,250.005 أو −100 أو تاريخ انتهاء يسبق البداية يُرفض قبل الحفظ بثلاث رسائل مستقلة.
- خفض الأساسي إلى 5,000 بعد الإسناد يُظهر تحذير «تجاوز سقف» في معاينة المسير التالي دون منع الاحتساب.

---

### AL-11 — لقطة البدلات في المسير

**الوصف**
يجب أن يلتقط النظام عند احتساب المسير **لقطة (Snapshot)** لكل بدل دخل حساب كل موظف: تعريف النوع بإصداره، قيمة الإسناد، مدخلات التناسب، والناتج؛ بحيث لا يغيّر أي تعديل لاحق على الكتالوج أو الإسناد مسيرًا محسوبًا أو معتمدًا.

**الحقول المقترحة (`payroll_allowance_line`)**

| الحقل | الوصف |
|---|---|
| `payroll_run_id` · `employee_id` · `employee_allowance_id` | المراجع |
| `allowance_type_code_snapshot` · `allowance_name_snapshot` · `type_version` | تعريف النوع وقت الحساب |
| `value_mode_snapshot` · `base_amount_or_percent` · `base_salary_used` | مدخلات القيمة |
| `affects_day_rate_snapshot` · `is_insurable_snapshot` · `is_taxable_snapshot` | الأعلام الملتقطة |
| `proration_mode_snapshot` · `eligible_days` · `denominator_days` | مدخلات التناسب |
| `computed_amount` | الناتج النهائي بعد التقريب |

**قواعد العمل**

1. تُنشأ اللقطة عند أول انتقال إلى «محسوب» وتُستبدل بالكامل عند كل إعادة حساب مسموح بها، مع حفظ الإصدار السابق.
2. بعد «معتمد» تصبح اللقطة غير قابلة للتغيير.
3. القسيمة والتقارير والمتغيّر `ALLOWANCES_TOTAL` تُقرأ من اللقطة حصرًا.
4. يجب أن يعرض النظام «فرق اللقطة» للبدلات عند إعادة الحساب: بدل أُضيف، بدل زال، قيمة تغيّرت.

**معايير القبول**

- تعديل قيمة إسناد من 2,000 إلى 2,500 بعد اعتماد المسير لا يغيّر `computed_amount` في المسير المعتمد ويظهر 2,500 في المسير التالي فقط.
- تعطيل نوع بدل بعد الاحتساب لا يمسّ سطور اللقطة.
- إعادة حساب مسير بعد إضافة إسناد جديد يعرض في فرق اللقطة سطرًا «أُضيف: بدل مسؤولية 1,000.00».

---

### AL-12 — الأثر الرجعي للبدلات

**الوصف**
يجب أن يحدّد النظام سلوكًا صريحًا لإسناد بدل بتاريخ سريان **يسبق** الفترة المفتوحة الحالية (Retroactive Allowance)، بحيث يُسترد الفارق المستحق دون المساس بمسيرات معتمدة أو مرحّلة.

**القيم الافتراضية (قابلة للضبط)**

| الضابط | الافتراضي |
|---|---|
| مدى الاسترداد التلقائي (`retro_auto_scope`) | **الفترة الحالية المفتوحة فقط**؛ الأيام السابقة لها لا تُحتسب تلقائيًا |
| آلية ما قبل الفترة الحالية | **قيد تسوية (Adjustment Entry)** موثّق يُنشأ في الفترة التالية للمسير المرحّل، بفئة `retro_allowance`، ويمرّ بدورة اعتماد |
| أقصى عمق للأثر الرجعي (`retro_max_months`) | **3 أشهر** قبل الفترة الحالية؛ ما تجاوز ذلك يتطلّب صلاحية `allowance.retro_extended` |
| صلاحية الإسناد الرجعي | `allowance.retro_assign` (إدارة الرواتب افتراضيًا) |

**قواعد العمل**

1. إذا وقع `effective_from` داخل الفترة المفتوحة الحالية أو بعدها: يُحتسب عاديًا وفق AL-07.
2. إذا سبق `effective_from` الفترة المفتوحة الحالية: يُحتسب الجزء الواقع في الفترة الحالية عاديًا، ويُنتج النظام **تلقائيًا** قيد تسوية لكل فترة سابقة مغطاة بقيمة الفارق (ما كان يجب استحقاقه − ما استُحق فعلًا = الفارق)، بحالة `pending_approval`، مرتبطًا بالإسناد وبالمسيرات السابقة مرجعيًا.
3. قيد التسوية يُستهلك في أول مسير تالٍ يشمل الموظف بنفس آلية DD-09، ويظهر في القسيمة تحت «تسويات فترات سابقة» بذكر الفترة الأصلية.
4. قيد التسوية **لا يعدّل** لقطة المسير السابق ولا سعر يومه ولا أجره الخاضع؛ الأثر التأميني/الضريبي للتسوية يُحتسب في فترة الصرف.
5. إذا كان أثر التسوية سالبًا (إنهاء بدل بأثر رجعي)، يُنشأ قيد استرداد بفئة `retro_allowance_clawback` يخضع لسقف حماية الصافي (DD-11) وترتيب أولويته «باقي الأنواع».

```
مثال:
  اليوم داخل فترة يوليو (مفتوحة). إسناد بدل سكن 2,000 بسريان 2026-05-16، مسيرا مايو ويونيو مرحّلان.
  يوليو : يُحتسب 2,000.00 عاديًا
  يونيو : الفارق = 2,000 − 0 = 2,000.00           → قيد تسوية
  مايو  : الفارق = 2,000 × 16/31 − 0 = 1,032.26   → قيد تسوية
  إجمالي التسويات 3,032.26 يُستهلك في مسير يوليو تحت «تسويات فترات سابقة»
```

**معايير القبول**

- إسناد بسريان داخل الفترة الحالية لا يُنتج أي قيد تسوية.
- الإسناد في المثال يُنتج قيدَي تسوية بقيمتي 2,000.00 و1,032.26 بحالة `pending_approval`، ولا يتغيّر صافي مسيرَي مايو ويونيو المرحّلين.
- إسناد بسريان يسبق الفترة الحالية بأربعة أشهر يُرفض لمستخدم بلا صلاحية `allowance.retro_extended`.
- إنهاء بدل بأثر رجعي يُنتج قيد استرداد يخضع لسقف الحماية ويظهر في تقرير الاسترداد.

---
### ب) الخصومات المصنّفة (Typed Deductions)

### DD-01 — كتالوج أنواع الخصومات

**الوصف**
يجب أن يوفّر النظام كتالوجًا لأنواع الخصومات (Deduction Type Catalog) يُعرَّف فيه كل نوع تُنزِله الإدارات (جودة · التزام · إداري · إنتاجية · …) مع خصائصه المالية وجهته المالكة، بحيث تُصنَّف كل الخصومات غير الآلية من مصدر واحد ويُمنع الإدخال الحرّ.

**الحقول المقترحة (`deduction_type`)**

| الحقل | الوصف |
|---|---|
| `code` · `name_ar` · `name_en` | كود ثابت وتسمية (مثل `QUALITY`, `COMPLIANCE`, `ADMIN`, `PRODUCTIVITY`) |
| `category` | `disciplinary` · `performance` · `administrative` · `statutory` · `court_order` |
| `owner_org_unit_id` | الجهة المالكة (إدارة الجودة تملك «خصم جودة») |
| `calc_method` · `default_value` | طريقة الاحتساب وقيمتها الافتراضية (DD-02) |
| `min_amount` · `max_amount` · `max_pct_of_gross` | حدود النوع (DD-02) |
| `is_exemptable` | هل يقبل الإعفاء (EX-01)؛ `statutory` و`court_order` = لا دائمًا |
| `installment_allowed` · `max_installments` | هل يُقسَّط وأقصى عدد أقساط (DD-10) |
| `carry_forward_priority` | ترتيب الترحيل عند تجاوز سقف الحماية (DD-11) |
| `requires_attachment` · `requires_employee_ack` | متطلبات التوثيق |
| `approval_chain_id` | سلسلة الاعتماد الافتراضية للنوع (DD-06) |
| `version` · `is_active` | الإصدار والتفعيل |

**قواعد العمل**

1. الأنواع الأربعة (جودة، التزام، إداري، إنتاجية) تُنشأ كبذرة أولية قابلة للتعديل؛ الجهة تضيف ما تشاء.
2. النوع من فئة `statutory` أو `court_order` يُحفظ بـ`is_exemptable = false` قسرًا ولا يقبل التعديل.
3. تعطيل نوع يمنع طلبات جديدة ولا يمسّ الطلبات في الدورة ولا القيود المستحقة ولا المسيرات.
4. كل تعديل مؤثر ماليًا يرفع `version` ويُلتقط الإصدار في القيد عند اعتماده.

**معايير القبول**

- محاولة إنشاء خصم بنوع غير معرَّف في الكتالوج تُرفض من واجهة الإدخال ومن واجهة البرمجة معًا.
- تعديل `is_exemptable` لنوع من فئة `court_order` يُرفض.
- تعطيل «خصم إنتاجية» يخفيه من قائمة الإنزال ويبقي الطلبات القائمة بحالتها.

---

### DD-02 — طرق الاحتساب والحدود

**الوصف**
يجب أن يدعم كل نوع خصم طريقة احتساب معرَّفة تُنتج مبلغًا بالعملة عند الاعتماد، مع حدود دنيا وعليا مطلقة ونسبية تُفحص عند الإنشاء وعند الاعتماد.

**طرق الاحتساب المطلوبة (`calc_method`)**

| الطريقة | المدخل | الناتج |
|---|---|---|
| `fixed_amount` | مبلغ | كما هو |
| `days_of_salary` | عدد أيام (يقبل كسورًا: 0.25 · 0.5 · 1 · 1.5) | `days × DAY_RATE` بسعر يوم المسير المستهلِك (AL-04) |
| `percent_of_base` | نسبة | `percent × BASE_SALARY` للفترة المستهلِكة |
| `percent_of_gross` | نسبة | `percent × GROSS_SALARY` |
| `hours_of_salary` | عدد ساعات | `hours × HOUR_RATE` |

**قواعد العمل**

1. الطرق المعتمدة على سعر اليوم/الساعة تُخزَّن بوحدتها (أيام/ساعات) وتُحوَّل إلى عملة **لحظة الاستهلاك** في المسير، لأن سعر اليوم تابع للمسير؛ ويُخزَّن المبلغ المحوَّل والسعر المستخدم في القيد بعد الاستهلاك.
2. الحدود الافتراضية القابلة للضبط لكل نوع: `min_amount` = 1.00 · `max_amount` = بلا (فارغ) · `max_pct_of_gross` = **25%** من `GROSS_SALARY` للخصم الواحد.
3. تجاوز `max_pct_of_gross` عند الإنشاء يُرفض؛ تجاوزه عند الاستهلاك (بسبب انخفاض الإجمالي) يُقلَّص المبلغ إلى الحد ويُرحَّل الباقي وفق DD-11 مع تحذير.
4. `days_of_salary` تقبل كسور اليوم بمضاعفات 0.25 فقط افتراضيًا (`day_fraction_step`، قابل للضبط).
5. ناتج أي طريقة لا يكون سالبًا؛ الناتج صفر يُرفض عند الإنشاء.

```
مثال (أساسي 9,000 · بدل سكن 2,250 يدخل سعر اليوم · أساس 30 يومًا):
  DAY_RATE = 11,250 ÷ 30 = 375.00
  خصم جودة  1.5 يوم        = 1.5 × 375.00 = 562.50
  خصم إداري 5% من الأساسي = 0.05 × 9,000 = 450.00
  سقف الواحد 25% × GROSS (11,750) = 2,937.50 → كلاهما ضمن الحد
  خصم إداري بقيمة 3,000 → يتجاوز 2,937.50 → يُرفض عند الإنشاء
```

**معايير القبول**

- خصم جودة 1.5 يوم على الموظف أعلاه يظهر في المسير بقيمة 562.50 مع عرض سعر اليوم المستخدم.
- خصم بقيمة 3,000 على إجمالي 11,750 وسقف 25% يُرفض بذكر الحد 2,937.50.
- إدخال 0.3 يوم يُرفض عند خطوة 0.25، وإدخال 0.75 يُقبل.
- نفس القيد بالأيام يُنتج مبالغ مختلفة في مسيرين بأساسي أيام مختلف، ويُعرض السعر المستخدم في كل منهما.

---

### DD-03 — صلاحية إنزال الخصم

**الوصف**
يجب أن يربط النظام صلاحية إنشاء خصم من نوع معيّن بـ**دور** و**نوع** معًا، بحيث يستطيع مدير الجودة إنزال «خصم جودة» فقط، ومدير الإدارة إنزال «خصم إداري» فقط، ولا يستطيع أي منهما إنزال نوع لا يملكه.

**قواعد العمل**

1. مصفوفة صلاحيات (`deduction_type_permission`) بحقول: `deduction_type_id` · `role_id` · `can_create` · `can_edit_own` · `can_cancel_own` · `max_amount_without_escalation`.
2. الأدوار الافتراضية المقترحة: **مدير مباشر** (`line_manager`)، **مدير فريق** (`team_lead`)، **مدير قسم** (`department_manager`)، **مدير إدارة/جهة مالكة** (`function_owner`)، **الموارد البشرية** (`hr_officer`)؛ الجهة تُعدّل المصفوفة بحرية.
3. النوع المملوك لجهة (`owner_org_unit_id`) لا يُنزله إلا دور ينتمي إلى تلك الجهة أو دور HR، حتى لو كان الموظف ضمن نطاق المُنزِّل تنظيميًا.
4. لا يستطيع مستخدم إنزال خصم على نفسه، ولا على من يعلوه في التسلسل الإداري.
5. `max_amount_without_escalation` (افتراضي: **يوم واحد** من الراتب أو ما يعادله) يفرض خطوة اعتماد إضافية عند تجاوزه (DD-06).

**معايير القبول**

- مدير الجودة يرى في قائمة الأنواع «خصم جودة» فقط عند إنشاء خصم؛ ومحاولة تمرير نوع «إداري» عبر واجهة البرمجة تُرفض بكود 403 مخصّص.
- مدير قسم يحاول إنزال خصم على مدير الإدارة الأعلى منه يُرفض.
- خصم بقيمة 2 يوم من مدير مباشر (السقف يوم واحد) يُضاف تلقائيًا إلى مساره خطوة «اعتماد مدير القسم».

---

### DD-04 — النطاق التنظيمي

**الوصف**
يجب أن يقيّد النظام إنزال الخصم بـ**النطاق التنظيمي (Organizational Scope)** للمُنزِّل: المدير المباشر على مرؤوسيه المباشرين، مدير الفريق على فريقه، مدير القسم على قسمه، مدير الجهة المالكة على كل من يقع ضمن نطاق مسؤوليته الوظيفية المعرَّف صراحةً.

**قواعد العمل**

1. النطاق يُشتقّ من الهيكل التنظيمي الساري في تاريخ الإنشاء (شجرة الفروع/الأقسام/الفرق وعلاقة المدير المباشر)، ويُلتقط في الطلب (`scope_basis_snapshot`) لأغراض التدقيق.
2. للجهات الوظيفية العابرة للأقسام (كالجودة) يُعرَّف **نطاق وظيفي (Functional Scope)** صريح: قائمة أقسام/فرق/موظفين تخضع لرقابتها، يُدار بصلاحية HR.
3. الموظف خارج النطاق لا يظهر في قائمة الاختيار أصلًا، والتحقق يُعاد عند الحفظ وعند كل خطوة اعتماد (تحسّبًا لنقل الموظف أثناء الدورة).
4. نقل الموظف خارج نطاق المُنزِّل بعد الإنشاء لا يُلغي الطلب لكنه يُعلَّم «خرج عن النطاق» ويُوجَّه للاعتماد إلى HR.

**معايير القبول**

- مدير جودة نطاقه الوظيفي «قسم الإنتاج» لا يستطيع إنزال خصم جودة على موظف في «قسم المبيعات» ولا يراه في القائمة.
- مدير مباشر ينزل خصمًا على مرؤوس مباشر يُقبل، وعلى مرؤوس مرؤوسه (غير مباشر) يُرفض ما لم يكن مدير قسم.
- نقل الموظف إلى قسم آخر بعد إنشاء الطلب يُظهر وسم «خرج عن النطاق» ويحوّل الاعتماد إلى HR.

---

### DD-05 — إنشاء الخصم كطلب

**الوصف**
يجب أن يُنشأ كل خصم مصنّف كـ**طلب (Deduction Request)** له حالة ودورة حياة، لا كقيد مباشر في المسير، بحيث يُوثَّق السبب ويُخطر الموظف ويمرّ بالاعتماد قبل أن يصبح قيدًا مستحقًا.

**الحقول المقترحة (`deduction_request`)**

| الحقل | الوصف |
|---|---|
| `employee_id` · `deduction_type_id` · `type_version` | الموظف والنوع وإصداره |
| `calc_method` · `input_value` · `estimated_amount` | الطريقة والمدخل والمبلغ التقديري وقت الإنشاء |
| `incident_date` · `reason` (≥ 20 حرفًا) · `attachment_refs` | الواقعة وسببها وأدلّتها |
| `target_period_policy` | `next_open_period` (افتراضي) · `specific_period` مع `target_period_id` |
| `installments_requested` | عدد الأقساط المطلوب (DD-10) |
| `status` | `draft` · `submitted` · `in_approval` · `approved` · `rejected` · `cancelled` · `withdrawn` |
| `created_by` · `created_at` · `scope_basis_snapshot` | المُنزِّل وأساس نطاقه |

**قواعد العمل**

1. الحقول الإلزامية: الموظف، النوع، الطريقة والمدخل، تاريخ الواقعة، السبب (≥ 20 حرفًا)؛ والمرفق إلزامي إذا كان النوع `requires_attachment = true`.
2. تاريخ الواقعة لا يكون مستقبليًا ولا أقدم من **90 يومًا** افتراضيًا (`max_incident_age_days`، قابل للضبط)؛ ما تجاوز ذلك يتطلّب صلاحية HR.
3. عند الإرسال (`submitted`) يُنشأ الطلب في سلسلة الاعتماد (DD-06) ويُخطر الموظف فورًا بوجود خصم مقترح بحالته (DD-08).
4. المُنزِّل يستطيع سحب الطلب (`withdrawn`) ما لم يكتمل اعتماده؛ بعد الاعتماد يُدار بـDD-12.
5. لا يجوز إنشاء طلبين متطابقين (نفس الموظف والنوع وتاريخ الواقعة والمدخل) خلال 24 ساعة إلا بتأكيد صريح «هذا ليس تكرارًا».

**معايير القبول**

- طلب بسبب من 12 حرفًا يُرفض بذكر الحد الأدنى 20؛ وبسبب من 20 حرفًا يُقبل.
- إرسال طلب يُنتج إشعارًا للموظف خلال دقيقة واحدة يعرض النوع والمبلغ التقديري والحالة «قيد الاعتماد».
- طلب بتاريخ واقعة قبل 100 يوم يُرفض لمدير قسم ويُقبل لمستخدم HR مع تسجيل التجاوز.
- محاولة إنشاء طلب مطابق لآخر أُنشئ قبل ساعة تُظهر تحذير تكرار يتطلّب تأكيدًا.

---

### DD-06 — سلسلة اعتماد قابلة للتعريف

**الوصف**
يجب أن يوفّر النظام محرّك سلاسل اعتماد (Configurable Approval Chain) لطلبات الخصم، تُعرَّف خطواته من واجهة الإعدادات لكل نوع خصم مع شروط تصعيد بحسب المبلغ، بحيث تنتهي كل سلسلة بخطوة الموارد البشرية افتراضيًا ثم يتحوّل الطلب إلى قيد مستحق جاهز للمسير.

**قواعد العمل**

1. السلسلة (`approval_chain`) قائمة خطوات مرتّبة، لكل خطوة: الدور المعتمِد (`approver_role`)، نطاق الاعتماد (نفس نطاق الموظف/أي)، مهلة (`sla_hours`، افتراضي **48**)، سلوك انتهاء المهلة (`escalate` افتراضيًا · `auto_reject` · `wait`)، وشرط تفعيل اختياري (مثل `amount > 1 × DAY_RATE`).
2. السلسلة الافتراضية المبذورة لكل الأنواع: **(1)** مدير القسم — إن لم يكن هو المُنزِّل — **(2)** الموارد البشرية. الجهة تُعدّلها بحرية، وخطوة HR الأخيرة لا تُحذف إلا بصلاحية `approval_chain.remove_hr_step`.
3. المعتمِد لا يكون هو المُنزِّل نفسه (يُتخطّى دوره تلقائيًا إلى الخطوة التالية مع تسجيل ذلك)، ولا يكون الموظف المستهدف.
4. كل خطوة تقبل: `approve` · `reject` (بسبب إلزامي) · `return_for_edit` (بسبب) · `adjust_amount` (بصلاحية، يُسجَّل الأصل والمعدَّل).
5. اكتمال آخر خطوة يحوّل الطلب إلى `approved` ويُنشئ قيدًا مستحقًا في دفتر المستحقات (DD-09) بأقساطه (DD-10)، ويُخطر الموظف والمُنزِّل.
6. تغيير تعريف السلسلة يسري على الطلبات الجديدة فقط؛ الطلبات في الدورة تُكمل على السلسلة الملتقطة فيها (`chain_snapshot`).

**معايير القبول**

- طلب من مدير القسم نفسه يتخطّى خطوة «مدير القسم» ويذهب مباشرة إلى HR مع تسجيل «تُخطّي: المعتمِد هو المُنزِّل».
- طلب لم يُبتّ فيه خلال 48 ساعة يُصعَّد إلى الخطوة التالية ويُسجَّل التصعيد.
- رفض HR بسبب «لا يوجد مرفق» يعيد الحالة إلى `rejected` ويُخطر المُنزِّل والموظف، ولا يُنشأ أي قيد.
- تعديل السلسلة بإضافة خطوة «المدير المالي» لا يؤثر على طلب كان في الخطوة الثانية وقت التعديل.

---

### DD-07 — أهلية دخول المسير وتاريخ القطع

**الوصف**
يجب أن يحدّد النظام بدقة **أي القيود** تدخل مسيرًا بعينه: القيود المعتمدة (`approved`) قبل **تاريخ القطع (Cut-off)** للمسير والمستحقة لفترته أو المرحّلة إليه؛ وما اعتُمد بعد القطع يُرحَّل تلقائيًا للمسير التالي دون تدخّل.

**قواعد العمل**

1. لكل مسير `cutoff_at` (تاريخ ووقت) يُحدَّد عند الإنشاء؛ الافتراضي: **نهاية يوم انتهاء الفترة** (`period_end 23:59`)، قابل للتعديل قبل الاحتساب.
2. يدخل المسير القيد الذي يستوفي معًا: `status = approved` · `approved_at ≤ cutoff_at` · القسط مستحق لفترة المسير أو لفترة أقدم غير مستهلَكة (ترحيل) · الموظف عضو في لقطة المسير · القيد غير مستهلَك.
3. القيد المعتمد بعد القطع يُعلَّم `deferred_to_next` ويظهر في معاينة المسير ضمن قائمة «مؤجَّل للمسير التالي» ليعرف المُنزِّل والموظف مصيره.
4. إعادة حساب المسير بعد القطع **لا تسحب** قيودًا اعتُمدت بعده إلا إذا حرّك مستخدم مخوَّل `cutoff_at` صراحةً بتوثيق.
5. القيد المستهدف لفترة محدّدة (`specific_period`) لم يُحسب لها مسير يشمل الموظف يبقى `pending` ويُلتقط في أول مسير لاحق.

**حالات حدّية**
- اعتماد قيد في نفس دقيقة القطع: يدخل إذا كان `approved_at ≤ cutoff_at` (مقارنة شاملة).
- موظف انتهت خدمته وقيد معتمد بعد آخر مسير له: يُوجَّه إلى التسوية النهائية ويظهر في تقرير «قيود بلا مسير».

**معايير القبول**

- قيد اعتُمد الساعة 10:00 يوم القطع يدخل المسير؛ وقيد اعتُمد 00:30 من اليوم التالي لا يدخل ويُعلَّم مؤجَّلًا.
- تحريك `cutoff_at` يومًا للأمام بصلاحية يُدخل القيد المؤجَّل بعد إعادة الحساب ويُسجَّل التحريك في أثر التدقيق.
- معاينة المسير تعرض قائمة القيود المؤجَّلة بعددها ومجموعها.

---
### DD-08 — ظهور الخصم للموظف بحالته

**الوصف**
يجب أن يرى الموظف كل خصم مصنّف يخصّه **فور إنشائه** وبحالته الحالية في كل مرحلة (مقترح، قيد الاعتماد، معتمد، مؤجَّل، مستهلَك في مسير، مُعفى، ملغى)، مع النوع والسبب والمبلغ والجهة المُنزِلة، لأن الهدف من المنظومة إلغاء الورق وإتاحة الشفافية للموظف قبل نزول المسير.

**قواعد العمل**

1. يظهر للموظف: النوع، تاريخ الواقعة، السبب، المبلغ التقديري أو النهائي، الحالة، الجهة المُنزِلة (بالدور والاسم وفق إعداد `show_issuer_name`، افتراضي: إظهار)، رقم القسط إن قُسِّط، والمسير الذي استهلكه.
2. لا يرى الموظف: المناقشات الداخلية بين المعتمِدين، ولا المرفقات المعلَّمة `internal`.
3. يُتاح للموظف إبداء **اعتراض (Objection)** خلال مهلة (`objection_window_days`، افتراضي **5 أيام**) من الإخطار؛ الاعتراض لا يوقف الدورة تلقائيًا لكنه يظهر لكل معتمِد ويُلزمه بالردّ قبل الاعتماد (إعداد `objection_blocks_approval`، افتراضي: نعم).
4. الإخطار عند كل تغيير حالة: إنشاء، اعتماد، رفض، تأجيل، استهلاك، إعفاء، إلغاء.
5. ملف الموظف الذاتي يعرض ملخّصًا: إجمالي الخصومات المعتمدة غير المستهلَكة، والمتوقّع في المسير القادم.

**معايير القبول**

- خلال دقيقة من إرسال طلب خصم يظهر في ملف الموظف بحالة «قيد الاعتماد» مع المبلغ التقديري.
- اعتراض الموظف خلال المهلة يظهر لمعتمِد HR ويمنع زر «اعتماد» حتى يُسجَّل ردّ.
- بعد الاستهلاك في مسير يونيو يعرض الخصم «مستهلَك — مسير يونيو 2026» ورقم القسيمة.
- الموظف لا يستطيع رؤية خصومات زميل بأي مسار.

---

### DD-09 — الاستهلاك مرة واحدة

**الوصف**
يجب أن يضمن النظام أن كل قسط من قيد خصم معتمد يدخل **مسيرًا واحدًا فقط** ويُعلَّم مستهلَكًا بمرجعه، وأن يُفرض هذا الضمان في **طبقة التخزين** (قيد فريد) لا في منطق التطبيق وحده، حمايةً من الازدواج عند إعادة الحساب أو الاحتساب المتوازي أو إعادة المحاولة بعد خطأ.

**الحقول المقترحة (`deduction_installment`)**

| الحقل | الوصف |
|---|---|
| `deduction_request_id` · `seq` | القيد الأصل ورقم القسط |
| `input_value` · `planned_amount` | المدخل والمبلغ المخطّط (بالعملة إن كانت الطريقة ثابتة) |
| `due_period_id` | الفترة المستحقة |
| `status` | `pending` · `reserved` · `consumed` · `carried_forward` · `exempted` · `cancelled` · `reversed` |
| `payroll_run_id` · `consumed_amount` · `rate_used` · `consumed_at` | مرجع الاستهلاك ومبلغه والسعر المستخدم |
| `carried_from_installment_id` | عند الترحيل (DD-11) |

**قواعد العمل**

1. وحدة الاستهلاك هي **القسط** حتى لو كان القيد قسطًا واحدًا؛ لا يُستهلك القيد ككل.
2. قيد فريد على (`deduction_installment_id`) في جدول سطور المسير المستهلِكة (`payroll_deduction_line`) مع حالة سطر فعّالة، بحيث تفشل أي محاولة إدراج ثانية على مستوى قاعدة البيانات.
3. عند الاحتساب: الأقساط المؤهلة (DD-07) تُحجز (`reserved`) باسم المسير، ثم تصبح `consumed` عند الاعتماد؛ إعادة الحساب تُحرّر الحجز وتُعيده في نفس المعاملة.
4. إلغاء المسير أو إلغاء ترحيله يعيد الأقساط إلى `pending` عبر **قيد عكسي** لا بتعديل الحالة الصامت (DD-12).
5. القسط الذي حاول مسيران حجزه في الوقت نفسه: أحدهما فقط ينجح والآخر يستلم كود خطأ «قسط محجوز لمسير آخر» ويُستبعد القسط من معاينته مع بيان المسير الحاجز.

**معايير القبول**

- تشغيل احتساب مسيرين متوازيين يضمّان نفس الموظف بالخطأ لنفس الفترة: القسط يظهر في أحدهما فقط، والآخر يعرض سبب الاستبعاد.
- إعادة حساب المسير خمس مرات تُنتج سطرًا واحدًا للقسط في اللقطة النهائية ومبلغًا واحدًا في القسيمة.
- إدراج سطر ثانٍ لنفس القسط عبر استعلام مباشر يفشل بخرق قيد فريد.
- إلغاء ترحيل المسير يُنتج قيدًا عكسيًا ويعيد القسط إلى `pending` مع بقاء السطر الأصلي مرئيًا في التاريخ.

---

### DD-10 — تقسيط الخصم

**الوصف**
يجب أن يدعم النظام تقسيط الخصم المعتمد على عدد فترات متتالية (Installments) بمبالغ متساوية مع ترحيل فرق التقريب إلى القسط الأخير، ضمن حدّ أقصى للأقساط معرَّف على النوع، بحيث يُوزَّع الخصم الكبير على الراتب دون كسر سقف الحماية.

**قواعد العمل**

1. عدد الأقساط يُطلب عند الإنشاء أو يُعدَّل عند الاعتماد بصلاحية؛ الحدّ الأقصى الافتراضي `max_installments = 6` لكل نوع، قابل للضبط.
2. قسط الخصم بالوحدات الزمنية (`days_of_salary`) يُقسَّم على الوحدة (الأيام) لا على المبلغ، ويُحوَّل كل قسط بسعر يوم مسيره.
3. المعادلة الملزمة للمبلغ الثابت:

```
base = FLOOR(total × 100 ÷ n) ÷ 100
installments[1 … n−1] = base
installments[n]       = total − base × (n − 1)
مثال: 1,000 على 3 = 333.33 · 333.33 · 333.34 (المجموع 1,000.00)
```

4. الأقساط تستحق لفترات متتالية بدءًا من الفترة المستهدفة؛ القسط الذي يُرحَّل بسبب سقف الحماية (DD-11) **لا يُزيح** الأقساط التالية بل يُضاف إليها.
5. إعادة جدولة الأقساط غير المستهلَكة ممكنة بصلاحية HR وبسبب موثَّق؛ المستهلَك لا يُمسّ.
6. عند انتهاء الخدمة تُستحق كل الأقساط المتبقية في التسوية النهائية دفعة واحدة، خاضعة لسقف الحماية إن كانت السياسة تطبّقه على التسوية (`apply_cap_on_final_settlement`، افتراضي: لا).

**معايير القبول**

- خصم 1,000 على 3 أقساط يُنتج 333.33 / 333.33 / 333.34 والمجموع 1,000.00 بالضبط.
- طلب 8 أقساط على نوع حدّه 6 يُرفض بذكر الحد.
- خصم 3 أيام على 2 قسط يُنتج 1.5 يوم لكل قسط، وقد يختلف مبلغ القسطين إن اختلف سعر اليوم بين المسيرين مع بيان السعر.
- إنهاء خدمة موظف عليه قسطان متبقيان يُظهرهما مجتمعين في التسوية النهائية.

---

### DD-11 — سقف حماية الصافي

**الوصف**
يجب أن يفرض النظام **سقفًا لإجمالي الخصومات** في المسير الواحد كنسبة من **الأجر بعد البدلات وقبل الخصومات** (Net Protection Cap)، بحيث لا ينزل صافي الموظف عن حدّ معيشي، ويُرحَّل الفائض إلى الفترة التالية بترتيب أولوية معرَّف، مع سياستَي إسقاط وتعليق بديلتين قابلتين للاختيار.

**القيم الافتراضية (قابلة للضبط على مستوى الجهة مع تجاوز على مستوى السياسة)**

| الضابط | الافتراضي |
|---|---|
| وعاء السقف (`cap_base`) | `BASE_SALARY + ALLOWANCES_TOTAL` (بعد التناسب، قبل أي خصم، بلا مكافآت ولا أوفرتايم) |
| النسبة القصوى (`max_total_deductions_pct`) | **50%** من الوعاء |
| الخصومات الخاضعة للسقف | الخصومات المصنّفة، أقساط السلف، خصومات الحضور (تأخير/غياب/نقص ساعات)، الاستردادات |
| المستثنى من السقف | الاستقطاعات النظامية والأحكام القضائية (تُطبَّق قبل السقف وتُخصم من السعة) والإجازة بلا أجر (اعتُبرت عدم استحقاق لا خصمًا) |
| مصير الفائض (`overflow_policy`) | **`carry_forward`** (ترحيل) · `drop` (إسقاط بصلاحية) · `suspend` (تعليق لقرار يدوي) |
| ترتيب الترحيل (`carry_forward_priority`) | ما يُخصم أولًا ضمن السعة: **(1)** خصومات الحضور **(2)** الاستقطاعات المستثناة سبقتها؛ وما يُرحَّل أولًا عند العجز: **(1)** أقساط السلف **(2)** الخصومات الإدارية **(3)** باقي الأنواع المصنّفة **(4)** الاستردادات |
| أقصى عدد ترحيلات للقسط الواحد (`max_carry_forward_count`) | **3**؛ بعدها يُعلَّق ويُحال إلى HR |

**قواعد العمل**

1. الخوارزمية الملزمة:

```
cap_amount        = cap_base × max_total_deductions_pct
statutory_total   = Σ الاستقطاعات النظامية + الأحكام القضائية
capacity          = MAX(0, cap_amount − statutory_total)
attendance_total  = Σ خصومات الحضور                       → تُخصم أولًا (لا تُرحَّل؛ الفائض منها يُسقط بتحذير)
capacity          = MAX(0, capacity − attendance_total)
ثم تُمرَّر القيود الخاضعة للسقف بترتيب أولوية الخصم (عكس ترتيب الترحيل):
  الاستردادات ← باقي الأنواع المصنّفة ← الخصومات الإدارية ← أقساط السلف
  لكل قيد: consumed = MIN(remaining, capacity) ; capacity −= consumed ; الباقي يُرحَّل (carry_forward)
```

2. المبلغ **المُعفى** (EX-01) لا يُحتسب ضمن السقف لأنه لم يُقتطع فعلًا.
3. القسط المُرحَّل يُنشأ كقسط جديد (`carried_from_installment_id`) مستحق للفترة التالية، ويُعلَّم الأصل `carried_forward`؛ يظهر للموظف بحالته وبسببه.
4. عند `suspend` يُعلَّق القسط وتُنشأ مهمة لـHR بقرار: ترحيل، إسقاط، إعادة جدولة.
5. القسيمة تعرض سطرًا معلوماتيًا: «سقف الحماية: {النسبة} × {الوعاء} = {السقف} · المستخدم {X} · المُرحَّل {Y}».
6. تغيير النسبة يسري على المسيرات غير المحسوبة فقط؛ النسبة الملتقطة تُخزَّن في لقطة السياسة.

```
مثال (أساسي 9,000 · بدلات 2,750 · سقف 50%):
  cap_base   = 11,750.00 → cap_amount = 5,875.00
  استقطاعات نظامية: 1,057.50  → capacity = 4,817.50
  خصومات حضور: 750.00           → capacity = 4,067.50
  قيود خاضعة: استرداد 500 · خصم جودة 562.50 · خصم إداري 450 · قسط سلفة 3,000
  استرداد 500.00     → capacity 3,567.50
  جودة   562.50      → capacity 3,005.00
  إداري  450.00      → capacity 2,555.00
  سلفة   3,000.00    → يُخصم 2,555.00 ويُرحَّل 445.00
  إجمالي الخصومات = 1,057.50 + 750 + 500 + 562.50 + 450 + 2,555 = 5,875.00 (= السقف)
  الصافي = 11,750.00 − 5,875.00 = 5,875.00
```

**حالات حدّية**
- الاستقطاعات النظامية وحدها تتجاوز السقف: السعة صفر، كل القيود تُرحَّل، ويُرفع تنبيه.
- موظف بأيام تغطية قليلة (وعاء صغير): يُطبَّق السقف على الوعاء المتناسب فعلًا.

**معايير القبول**

- الموظف في المثال يظهر لديه قسط سلفة مستهلَك 2,555.00 وقسط مُرحَّل 445.00 مستحق للفترة التالية، وصافي 5,875.00.
- رفع النسبة إلى 60% على نفس المدخلات يُدخل القسط كاملًا ولا يُرحَّل شيء.
- إعفاء خصم الجودة (562.50) يحرّر سعة بقدره لأن المُعفى لا يُحتسب ضمن السقف: تصبح السعة المتبقية قبل السلفة 3,117.50 فيُخصم قسط السلفة 3,000.00 كاملًا ولا يُرحَّل شيء، ويصبح إجمالي الخصومات 5,757.50 والصافي 5,992.50.
- قسط رُحِّل 3 مرات يُعلَّق في المرة الرابعة وتُنشأ مهمة لـHR.

---

### DD-12 — التعديل والإلغاء والعكس

**الوصف**
يجب أن يوفّر النظام تعديل الخصم قبل الاعتماد، وإلغاءه بعد الاعتماد وقبل الاستهلاك، و**عكسه بقيد عكسي موثَّق** بعد الاستهلاك أو الترحيل، دون حذف فعلي لأي سجل ودون تعديل مسير معتمد.

**قواعد العمل**

1. قبل الاعتماد: التعديل متاح للمُنزِّل (إعادة إلى `draft` بسبب) ولمعتمِد يملك `adjust_amount`؛ كل تعديل يحفظ النسخة السابقة.
2. بعد الاعتماد وقبل الاستهلاك: الإلغاء بصلاحية HR وبسبب ≥ 20 حرفًا؛ الأقساط `pending` تُعلَّم `cancelled` ويُخطر الموظف والمُنزِّل.
3. بعد الاستهلاك في مسير **غير مرحّل**: يُلغى القسط ويُعاد حساب المسير (يُحرَّر الحجز)؛ في مسير **مرحّل**: يُنشأ **قيد عكسي** (`reversal`) بفئة `deduction_reversal` بمبلغ موجب للموظف يُستهلك في المسير التالي، ويبقى السطر الأصلي كما هو مع إشارة إلى العكس.
4. العكس الجزئي مسموح (مبلغ أقل من المستهلَك) مع تبرير.
5. إلغاء ترحيل مسير كامل يعكس كل أقساطه إلى `pending` بقيود عكسية آلية مرتبطة بحدث الإلغاء.
6. كل ما سبق يُسجَّل في أثر التدقيق: من، متى، الحالة قبل وبعد، السبب.

**معايير القبول**

- إلغاء خصم معتمد غير مستهلَك يغيّر أقساطه إلى `cancelled` ويُبقي الطلب والأقساط مرئية بحالتها، ولا يظهر في المسير التالي.
- عكس خصم 562.50 مستهلَك في مسير مرحّل يُنشئ قيدًا موجبًا 562.50 يظهر في المسير التالي تحت «عكس خصم — مسير يونيو» ولا يتغيّر مسير يونيو.
- محاولة حذف طلب خصم فعليًا من واجهة البرمجة تُرفض؛ الحذف غير معرَّف كإجراء.
- عكس جزئي 300 من 562.50 يُنتج قيدًا موجبًا 300.00 ويعرض في تفاصيل القسط «مستهلَك 562.50 — مُعكس 300.00».

---

### DD-13 — تقارير الخصومات

**الوصف**
يجب أن يوفّر النظام مجموعة تقارير للخصومات المصنّفة تخدم المُنزِّل والمعتمِد والموارد البشرية والإدارة العليا، مع تصفية بالفترة والنوع والجهة والنطاق والحالة، وتصدير إلى ملفات جدولية.

**التقارير المطلوبة**

| التقرير | المحتوى | المستفيد |
|---|---|---|
| سجل الطلبات | كل الطلبات بحالتها ومراحل اعتمادها وزمن كل مرحلة | HR، المُنزِّل |
| الخصومات بحسب النوع والجهة | عدد ومجموع الخصومات لكل نوع ولكل جهة مُنزِلة لكل فترة | الإدارة العليا |
| الخصومات بحسب الموظف | تكرار الخصومات على الموظف الواحد (عدد، مجموع، أنواع) خلال 3/6/12 شهرًا | HR، المدير |
| المؤجَّل والمُرحَّل والمعلَّق | أقساط بعد القطع، ومُرحَّلة بالسقف، ومعلَّقة تنتظر قرارًا | HR، الرواتب |
| القيود بلا مسير | أقساط `pending` لموظفين لم يدخلوا مسيرًا | الرواتب |
| زمن دورة الاعتماد | متوسط وأقصى زمن لكل خطوة ولكل معتمِد، ونسبة التصعيد | الإدارة العليا |
| الاعتراضات | اعتراضات الموظفين ونتائجها | HR |
| المطابقة | مجموع الأقساط المستهلَكة في الفترة = مجموع سطور الخصم في المسيرات المعتمدة | الرواتب، المراجعة |

**قواعد العمل**

1. كل تقرير يحترم نطاق المستخدم: المُنزِّل يرى خصوماته ونطاقه فقط؛ HR ترى الكل.
2. تقرير المطابقة يجب أن يُنتج **صفر فرق** في كل فترة؛ أي فرق يُعرض كاستثناء بتفاصيله.
3. التقارير تُقرأ من اللقطات والدفتر لا من الطلبات الحيّة، بحيث لا تتغيّر أرقام فترة مقفلة.
4. يُفضَّل مؤشر تنبيهي: موظف تجاوز **3 خصومات** في 90 يومًا (`repeat_deduction_threshold`، قابل للضبط) يُعلَّم في تقرير الموظف.

**معايير القبول**

- تقرير الخصومات بحسب النوع لفترة يونيو يطابق مجموع سطور الخصم المصنّفة في كل مسيرات يونيو المعتمدة.
- مدير الجودة يشغّل تقرير الطلبات فيرى طلبات «خصم جودة» في نطاقه الوظيفي فقط.
- تقرير المطابقة يُظهر فرقًا صفريًا بعد سيناريو عكس واحد وترحيل واحد.
- التصدير الجدولي يحتوي على نفس الأعمدة والمجاميع المعروضة على الشاشة.

---
### ج) الإعفاءات والمكافآت الفردية (Exemptions & Bonuses)

### EX-01 — إعفاء من أي خصم في مسير محدّد

**الوصف**
يجب أن يوفّر النظام إجراء **إعفاء (Exemption)** يُسقط عن موظف بعينه، في مسير بعينه، خصمًا محدّدًا أو نوع خصم أو كل الخصومات — سواء كانت آلية (تأخير، غياب، نقص ساعات) أو مصنّفة معتمدة (جودة، التزام، إداري، إنتاجية، …) — دون تعديل السياسة ودون المساس بالقيد الأصلي، بحيث يبقى الإعفاء قرارًا موثَّقًا يظهر أثره في القسيمة والتقارير.

**الحقول المقترحة (`payroll_exemption`)**

| الحقل | الوصف |
|---|---|
| `employee_id` · `payroll_run_id` | الموظف والمسير المستهدف (مسير واحد فقط) |
| `scope_kind` | `all_deductions` · `deduction_type` · `single_entry` (EX-02) |
| `deduction_type_id` / `target_ref` | النوع أو القسط/سطر الحضور المستهدف بحسب النطاق |
| `reason` (≥ 20 حرفًا) · `attachment_refs` | السبب والمستندات |
| `disposition` | `drop` (إسقاط نهائي، افتراضي) · `defer_one_period` (تأجيل لهذا المسير فقط) (EX-08) |
| `granted_by` · `granted_at` · `approved_by` | المانح وتاريخ المنح والمعتمِد إن وُجد |
| `status` | `pending_approval` · `active` · `applied` · `revoked` · `expired` |
| `exempted_amount_snapshot` | المبلغ الذي أُسقط فعلًا وقت الاحتساب |

**قواعد العمل**

1. الإعفاء **إجراء مستقل** له كيانه؛ لا يُنفَّذ بتعديل مبلغ القيد ولا بحذف سطر الحضور ولا بتعديل السياسة.
2. يُطبَّق الإعفاء عند الاحتساب: البند المشمول يُنتج **صفرًا** مع وسم «مُعفى» ومرجع الإعفاء، ويُخزَّن المبلغ الذي كان سيُقتطع في `exempted_amount_snapshot`.
3. الإعفاء يستهدف مسيرًا واحدًا محدّدًا بحالة تسمح بالتعديل (قبل الاعتماد)؛ لا يوجد إعفاء دائم أو مفتوح المدة (الاستثناء من الحضور له مسار مستقل: EX-09).
4. الإعفاء بعد الاحتساب وقبل الاعتماد يستلزم إعادة حساب؛ وبعد الاعتماد يُرفض، والبديل قيد عكسي (DD-12).
5. الأنواع غير القابلة للإعفاء (`is_exemptable = false`: الاستقطاعات النظامية والأحكام القضائية) تُستبعد من أي إعفاء حتى في نطاق `all_deductions`، ويُبيَّن ذلك للمانح.
6. الإعفاء **لا يحرّر** الخصم المصنّف فحسب بل يحدّد مصيره وفق `disposition` (EX-08).

```
مثال (مسير يونيو):
  خصومات الموظف: تأخير 159.72 · غياب 750.00 · جودة 562.50 · قسط سلفة 1,000.00 · استقطاع نظامي 1,057.50
  إعفاء scope = all_deductions
  → تأخير 0 (مُعفى) · غياب 0 (مُعفى) · جودة 0 (مُعفى) · سلفة 0 (مُعفى — مؤجَّل قسرًا، انظر EX-02)
  → الاستقطاع النظامي 1,057.50 يبقى (غير قابل للإعفاء)
  exempted_amount_snapshot = 159.72 + 750.00 + 562.50 + 1,000.00 = 2,472.22
```

**معايير القبول**

- إعفاء `all_deductions` على الموظف أعلاه يُنتج قسيمة فيها أربعة بنود بصفر موسومة «مُعفى» وبند نظامي 1,057.50 كما هو.
- محاولة إعفاء على مسير معتمد تُرفض بكود خطأ يوجّه إلى القيد العكسي.
- سطر الحضور الأصلي (يوما الغياب) وقيد الخصم المصنّف يبقيان في مصدريهما دون تغيير بعد الإعفاء.
- تقرير الإعفاءات يعرض المبلغ المُسقط 2,472.22 لهذا الإعفاء.

---

### EX-02 — نطاق الإعفاء

**الوصف**
يجب أن يدعم النظام ثلاثة نطاقات للإعفاء داخل المسير الواحد: **كل الخصومات** للموظف، أو **نوع خصم** واحد (كل التأخير، أو كل خصومات الجودة)، أو **قيد واحد** بعينه (قسط محدّد أو يوم غياب محدّد)، مع قواعد واضحة لتفاعل الإعفاءات المتداخلة.

**قواعد العمل**

1. `all_deductions`: يشمل خصومات الحضور الآلية بأنواعها وكل الخصومات المصنّفة القابلة للإعفاء وأقساط السلف؛ ولا يشمل الاستقطاعات النظامية والأحكام القضائية والإجازة بلا أجر (عدم استحقاق لا خصم).
2. `deduction_type`: يستهدف نوعًا من كتالوج الخصومات المصنّفة، أو أحد الأنواع الآلية المحجوزة: `LATENESS` · `ABSENCE` · `SHORTFALL` · `ADVANCE_INSTALLMENT`.
3. `single_entry`: يستهدف قسط خصم مصنّف واحدًا، أو قسط سلفة واحدًا، أو يوم حضور محدّدًا (تاريخ + نوع الواقعة).
4. أقساط السلف المشمولة بالإعفاء **لا تُسقط** (السلفة دين) بل تُؤجَّل قسرًا للفترة التالية بـ`disposition = defer_one_period` مهما كان خيار الإعفاء، ويُبيَّن ذلك للمانح قبل الحفظ.
5. تداخل الإعفاءات: عند وجود إعفاء `all_deductions` نشط، تُرفض إعفاءات أضيق على نفس المسير باعتبارها محتواة؛ وعند وجود إعفاء أضيق ثم طلب `all_deductions`، يُقبل الأوسع ويُعلَّم الأضيق `superseded`.
6. لا يجوز أن يستهدف إعفاءان نشطان القيدَ نفسه.

**معايير القبول**

- إعفاء بنوع `LATENESS` يُصفّر خصم التأخير 159.72 ويُبقي الغياب 750.00 والجودة 562.50.
- إعفاء `single_entry` ليوم غياب 12 يونيو يُصفّر يومًا واحدًا (375.00) ويُبقي اليوم الآخر (375.00).
- إعفاء يشمل قسط سلفة يُظهر تنبيهًا «سيُؤجَّل القسط لا يُسقط» ويُنشئ قسطًا مرحَّلًا للفترة التالية.
- محاولة إعفاء نوع `QUALITY` على مسير عليه إعفاء `all_deductions` نشط تُرفض بذكر الإعفاء المحتوي.

---

### EX-03 — الحقول الإلزامية وصلاحية المنح

**الوصف**
يجب أن يفرض النظام على كل إعفاء مجموعة حقول إلزامية وأن يقصر منحه على دور معرَّف ضمن نطاق تنظيمي معرَّف، مع فصل صريح بين صلاحية إنزال الخصم وصلاحية إعفائه.

**الحقول الإلزامية**

| الحقل | القيد |
|---|---|
| الموظف | عضو في لقطة المسير المستهدف |
| المسير | مسير واحد بحالة تسبق الاعتماد |
| نوع الخصم أو `all` | من الكتالوج أو الأنواع الآلية المحجوزة أو `all_deductions` |
| السبب | نص ≥ **20 حرفًا** (`exemption_reason_min_length`، قابل للضبط) |
| المانح | يُلتقط آليًا من الجلسة، لا يُدخل يدويًا |
| تاريخ المنح | يُلتقط آليًا (طابع زمني للخادم) |
| المرفق | إلزامي إذا تجاوز المبلغ المُعفى المقدَّر سقف EX-07 أو كان النوع `requires_attachment` |

**صلاحية المنح (الافتراضي، قابل للضبط)**

| الدور | نطاق المنح | نطاق الخصم المسموح إعفاؤه |
|---|---|---|
| `hr_manager` (مدير الموارد البشرية) | الشركة كلها | الكل (عدا غير القابل للإعفاء) |
| `payroll_manager` (مدير الرواتب) | الشركة كلها | الكل (عدا غير القابل للإعفاء) |
| `department_manager` (مدير القسم) | قسمه فقط | خصومات الحضور الآلية فقط، بمبلغ لا يتجاوز سقف EX-07، ويمرّ بخطوة اعتماد HR |
| `function_owner` (مدير الجهة المالكة) | نطاقه الوظيفي | النوع المملوك لجهته فقط، بشرط ألا يكون هو مُنزِّل الخصم نفسه |

**قواعد العمل**

1. صلاحية الإعفاء (`deduction.exempt`) **مستقلة وأعلى** من صلاحية الإنزال (`deduction.create`)؛ امتلاك الثانية لا يمنح الأولى.
2. **فصل المهام**: لا يستطيع المستخدم إعفاء خصم أنزله هو، ولا إعفاء نفسه، ولا إعفاء من يعلوه إداريًا.
3. الإعفاء من دور دون `hr_manager`/`payroll_manager` يمرّ بدورة اعتماد (خطوة HR واحدة على الأقل) قبل أن يصبح `active`.
4. السبب لا يقبل تكرار حرف واحد أو مسافات فقط لبلوغ الحد (تحقق بسيط: ≥ 3 كلمات مميزة).
5. كل إعفاء يُخطر: الموظف، مُنزِّل الخصم الأصلي (إن وُجد)، ومدير الرواتب.

**معايير القبول**

- مدير قسم يحاول إعفاء «خصم جودة» يُرفض (خارج نطاقه المسموح)، ويستطيع إعفاء خصم تأخير على موظف في قسمه مع تحويله إلى اعتماد HR.
- مدير الجودة الذي أنزل خصمًا يحاول إعفاءه يُرفض بكود «فصل مهام»؛ مدير الجودة الآخر في نفس الجهة يستطيع.
- إعفاء بسبب «موافقة موافقة موافقة موافقة» (تكرار) يُرفض رغم بلوغ 20 حرفًا.
- حقلا المانح وتاريخ المنح لا يظهران كحقول إدخال ويُملآن من الخادم.

---

### EX-04 — أثر الإعفاء في القسيمة

**الوصف**
يجب أن يُظهر النظام البند المُعفى في قسيمة الراتب **بقيمة صفر ووسم «مُعفى»** مع المبلغ الأصلي الذي كان سيُقتطع ورقم الإعفاء، ويُمنع إخفاء البند، بحيث يعرف الموظف والمدقّق أن ضابطًا رقابيًا أُسقط بقرار موثَّق.

**قواعد العمل**

1. صيغة السطر: «{اسم البند} — {المبلغ الأصلي} — مُعفى (إعفاء #{رقم}) — 0.00».
2. تُضاف فقرة تذييل في القسيمة: «إجمالي المبالغ المُعفاة في هذا المسير: {X}» عند وجود إعفاء واحد على الأقل.
3. المبلغ المُعفى **لا يدخل** في إجمالي الخصومات ولا في وعاء سقف الحماية ولا في `TYPED_DEDUCTIONS_TOTAL`.
4. سبب الإعفاء **لا يُطبع** في القسيمة افتراضيًا (`print_exemption_reason`، افتراضي: لا)؛ اسم المانح يُطبع بالدور لا بالاسم.
5. الترتيب: البند المُعفى في موضعه الطبيعي بين الخصومات، لا في قسم منفصل.

**معايير القبول**

- قسيمة موظف أُعفي من خصم غياب 750.00 تعرض سطر «خصم غياب — 750.00 — مُعفى (إعفاء #A-118) — 0.00» وإجمالي خصومات لا يشمل 750.00.
- إعادة طباعة القسيمة بعد سنة تعرض نفس السطر ونفس رقم الإعفاء.
- لا يوجد إعداد يسمح بإخفاء البند المُعفى؛ واجهة تصميم القسيمة تعرض الخيار معطّلًا.

---

### EX-05 — المكافأة الفردية

**الوصف**
يجب أن يوفّر النظام منح **مكافأة (Bonus)** لموظف بعينه بمبلغ محدّد لفترة مسير محدّدة، تمرّ بدورة اعتماد قابلة للتعريف وتُستهلك مرة واحدة كقيد مستحق موجب، وتظهر للموظف بحالتها من لحظة الاقتراح.

**الحقول المقترحة (`bonus_request`)**

| الحقل | الوصف |
|---|---|
| `employee_id` · `bonus_type_id` | الموظف ونوع المكافأة من كتالوج (`PERFORMANCE`, `SPOT`, `PROJECT`, …) |
| `calc_method` · `input_value` | `fixed_amount` · `days_of_salary` · `percent_of_base` |
| `reason` (≥ 20 حرفًا) · `attachment_refs` | التبرير |
| `target_period_policy` · `target_period_id` | الفترة المستهدفة (افتراضي: الفترة المفتوحة التالية) |
| `is_insurable` · `is_taxable` | الخضوع (يُورَث من النوع ويُتجاوز بصلاحية) |
| `status` · `created_by` · `approved_by` | الحالة والمُنشئ والمعتمِد |

**قواعد العمل**

1. صلاحية الاقتراح `bonus.propose` بنطاق تنظيمي (المدير على موظفيه)، وصلاحية الاعتماد النهائي `bonus.approve` لـHR/الرواتب افتراضيًا، مع سقف تصعيد: مكافأة تتجاوز **يومًا واحدًا** من الراتب (`bonus_escalation_threshold`) تضيف خطوة «المدير الأعلى» (افتراضي قابل للضبط).
2. سقف المكافأة الواحدة: **100%** من `BASE_SALARY` افتراضيًا (`max_bonus_pct_of_base`)؛ ما تجاوزه يتطلّب صلاحية `bonus.exceed_cap`.
3. المكافأة المعتمدة قيد مستحق موجب في الدفتر (`ledger_entry` بفئة `bonus`) يُستهلك مرة واحدة بآلية DD-09 ويُجمع في `BONUS_TOTAL`؛ لا تدخل في سعر اليوم ولا في وعاء سقف الحماية ولا تتناسب.
4. لا يستطيع المستخدم اقتراح مكافأة لنفسه أو لمن يعلوه.
5. الإلغاء والعكس بنفس قواعد DD-12.

```
مثال:
  مكافأة أداء 2 يوم على DAY_RATE 375.00 = 750.00 (تُحوَّل بسعر يوم المسير المستهلِك)
  مكافأة ثابتة 1,500.00 خاضعة للضريبة غير خاضعة للتأمين
  BONUS_TOTAL = 2,250.00 · TAXABLE_WAGE يزيد 2,250.00 · INSURABLE_WAGE يزيد 0
```

**معايير القبول**

- اقتراح مكافأة 1,500 من مدير قسم يظهر للموظف بحالة «قيد الاعتماد» ويصل إلى HR للاعتماد.
- مكافأة 2 يوم تظهر في مسير يونيو بقيمة 750.00 مع بيان سعر اليوم، ولا تظهر في يوليو.
- مكافأة 9,500 على أساسي 9,000 تُرفض لمن لا يملك `bonus.exceed_cap`.
- إعادة حساب المسير لا تكرّر المكافأة، وإلغاء ترحيله يعيدها إلى `pending`.

---

### EX-06 — تقرير حوكمة الإعفاءات

**الوصف**
يجب أن يوفّر النظام تقريرًا رقابيًا للإعفاءات والمكافآت يُظهر من أعفى من ومن ماذا وبكم ولماذا، مع مؤشرات تركّز تكشف الأنماط غير الطبيعية (مانح يُعفي كثيرًا، موظف يُعفى دومًا، نوع خصم يُفرَّغ من أثره).

**محتوى التقرير**

| المحور | المؤشرات |
|---|---|
| بحسب المانح | عدد الإعفاءات، إجمالي المبلغ المُعفى، نسبته من إجمالي الخصومات في نطاقه، نسبة الإعفاءات المعتمدة/المرفوضة |
| بحسب الموظف | عدد الإعفاءات في 3/6/12 شهرًا، إجمالي المبلغ، الأنواع المُعفاة، نسبة المُعفى من خصوماته |
| بحسب نوع الخصم | نسبة المُعفى من إجمالي النوع (معدل التفريغ) |
| بحسب المسير | إجمالي المُعفى والمكافآت وأثرهما في إجمالي الرواتب |
| المكافآت | بحسب المانح والموظف والنوع، مع نسبة المكافآت إلى الأساسي |
| الاستثناءات | إعفاءات بلا مرفق فوق السقف، إعفاءات متكرّرة على القيد نفسه بعد عكس، إعفاءات من دور مدير قسم أُعيدت |

**قواعد العمل**

1. مؤشرات التنبيه الافتراضية (قابلة للضبط): موظف أُعفي **3 مرات** في 6 أشهر؛ مانح تجاوزت نسبة إعفاءاته **20%** من خصومات نطاقه في الفترة؛ نوع خصم تجاوز معدل تفريغه **30%**.
2. التقرير يُقرأ من اللقطات (`exempted_amount_snapshot`) لا من القيود الحيّة.
3. صلاحية التقرير: `hr_manager` · `payroll_manager` · `internal_audit`؛ ويُفضَّل إرسال ملخّص شهري آلي لهم.
4. كل سطر يربط إلى الإعفاء نفسه وسببه ومرفقه وأثر تدقيقه.

**معايير القبول**

- بعد سيناريو مانح واحد أعفى 25% من خصومات نطاقه يظهر المانح موسومًا في قسم التنبيهات.
- موظف أُعفي 3 مرات خلال 6 أشهر يظهر في قائمة التكرار مع الأنواع الثلاثة.
- مجموع المبالغ المُعفاة في التقرير لفترة ما يساوي مجموع `exempted_amount_snapshot` لكل مسيرات الفترة المعتمدة.

---

### EX-07 — ضوابط الإعفاء وحدوده

**الوصف**
يجب أن يفرض النظام حدودًا كمّية ونوعية على الإعفاءات تمنع تحوّلها إلى قناة لتفريغ الانضباط أو للمحاباة، مع بقاء كل حدّ قابلًا للضبط وكل تجاوز محتاجًا إلى صلاحية أعلى وتوثيق.

**الحدود الافتراضية (قابلة للضبط)**

| الضابط | الافتراضي | عند التجاوز |
|---|---|---|
| سقف المبلغ المُعفى للإعفاء الواحد بلا مرفق (`exemption_attachment_threshold`) | **1 × DAY_RATE** | المرفق إلزامي |
| سقف الإعفاءات للموظف الواحد في 12 شهرًا (`max_exemptions_per_employee_year`) | **4** | يتطلّب `hr_manager` وتبريرًا إضافيًا |
| سقف نسبة المُعفى للمانح من خصومات نطاقه في الفترة (`max_exemption_pct_per_grantor`) | **20%** | الإعفاء يُحوَّل لاعتماد `hr_manager` |
| إعفاء `all_deductions` | لـ`hr_manager`/`payroll_manager` فقط | مرفوض للأدوار الأخرى |
| ما لا يقبل الإعفاء إطلاقًا | الاستقطاعات النظامية، الأحكام القضائية، الإجازة بلا أجر | مرفوض للجميع بلا استثناء |
| الإعفاء بأثر رجعي على مسير معتمد | ممنوع | البديل قيد عكسي (DD-12) |
| إعادة إعفاء قيد عاد بعد عكس إعفاء (EX-08) | يتطلّب `hr_manager` | — |

**قواعد العمل**

1. الحدود تُفحص عند الحفظ وعند الاعتماد وعند الاحتساب (لأن المبلغ الفعلي قد يختلف عن المقدَّر).
2. فصل المهام (EX-03) لا يُتجاوز بأي صلاحية.
3. كل تجاوز مسموح يُسجَّل في أثر التدقيق بوسم `limit_override` مع الحد المتجاوَز وقيمته.
4. يُفضَّل «فترة تهدئة»: لا يُقبل إعفاء على قيد خلال **24 ساعة** من اعتماده إلا من `hr_manager` (`exemption_cooldown_hours`).

**معايير القبول**

- إعفاء خصم 750.00 على سعر يوم 375.00 بلا مرفق يُرفض بذكر الحد 375.00؛ ومع مرفق يُقبل.
- الإعفاء الخامس لنفس الموظف خلال 12 شهرًا يُرفض لمدير الرواتب ويُقبل لمدير HR مع تسجيل `limit_override`.
- محاولة إعفاء حكم قضائي تُرفض لمدير HR نفسه.
- إعفاء `all_deductions` من مدير قسم يُرفض قبل الوصول إلى دورة الاعتماد.

---

### EX-08 — دورة حياة الإعفاء ومصير الخصم المُعفى

**الوصف**
يجب أن يحدّد النظام دورة حياة الإعفاء من الاقتراح حتى التطبيق أو الإلغاء، ومصير الخصم المشمول به: **إسقاط نهائي** افتراضيًا، أو **تأجيل لهذا المسير فقط** بحيث يعود القيد إلى الفترة التالية بقرار موثَّق لا مفاجئًا.

**دورة الحياة**

```
pending_approval → active → applied (عند اعتماد المسير)
                 ↘ rejected
active → revoked (قبل اعتماد المسير) → إعادة حساب
active → expired (اعتُمد المسير دون أن يشمل الموظف، أو أُلغي المسير)
```

**قواعد العمل**

1. `disposition = drop` (الافتراضي): القسط أو سطر الحضور المشمول يُعلَّم `exempted` نهائيًا ولا يعود في أي مسير لاحق؛ المبلغ يُثبَّت في `exempted_amount_snapshot`.
2. `disposition = defer_one_period`: القسط يُعلَّم `deferred_by_exemption` ويُنشأ قسط جديد مستحق للفترة التالية بمرجع الإعفاء؛ خصومات الحضور الآلية لا تقبل التأجيل (إما إسقاط وإما لا إعفاء) لأنها تُشتق من فترة بعينها؛ أقساط السلف تُؤجَّل دائمًا (EX-02).
3. `revoked`: إلغاء إعفاء نشط قبل اعتماد المسير يعيد القيد إلى حالته السابقة ويستلزم إعادة حساب؛ يتطلّب سببًا ≥ 20 حرفًا وصلاحية المانح أو أعلى.
4. بعد `applied` لا يُلغى الإعفاء؛ تصحيح الخطأ يكون بإنشاء **خصم جديد** بدورة اعتماد كاملة يذكر الإعفاء المُصحَّح، لا بإعادة القيد القديم.
5. `expired`: إذا اعتُمد المسير ولم يكن الموظف عضوًا فيه، أو أُلغي المسير، ينتهي الإعفاء ولا ينتقل تلقائيًا لمسير آخر؛ يُنشأ إعفاء جديد إن لزم.
6. سجل الإعفاءات (`exemption_log`) يحفظ كل انتقال حالة مع الفاعل والوقت والسبب، ويُربط بالقيد وبالمسير.

```
مثال:
  خصم جودة 562.50 معتمد لمسير يونيو
  إعفاء drop            → يونيو: 0 (مُعفى) · يوليو: لا شيء · القسط exempted
  إعفاء defer_one_period → يونيو: 0 (مُعفى — مؤجَّل) · يوليو: 562.50 (قسط جديد بمرجع الإعفاء) · القسط الأصلي deferred_by_exemption
```

**معايير القبول**

- إعفاء بإسقاط على خصم جودة يجعل القسط `exempted` ولا يظهر في يوليو ولا في أي مسير لاحق.
- إعفاء بتأجيل على نفس الخصم يُنتج قسطًا جديدًا في يوليو بقيمة 562.50 يحمل مرجع الإعفاء، ويظهر للموظف «مؤجَّل من يونيو بقرار إعفاء».
- إلغاء إعفاء نشط قبل اعتماد المسير يعيد الخصم 562.50 بعد إعادة الحساب ويسجّل الإلغاء وسببه.
- محاولة إلغاء إعفاء `applied` تُرفض وتوجّه إلى إنشاء خصم جديد.

---
### د) المستثنون من الحضور (Attendance-Exempt Employees)

### EX-09 — علَم الاستثناء من الحضور

**الوصف**
يجب أن يوفّر النظام تعريف الموظف **المستثنى من الحضور والانصراف (Attendance-Exempt)** الذي لا يخضع لقياس الحضور ولا التأخير ولا الغياب ويتقاضى راتبًا ثابتًا شهريًا، عبر كيان مستقل له نافذة سريان وسبب وصلاحية، لا عبر علَم دائم مبهم في ملف الموظف.

**الحقول المقترحة (`attendance_exemption`)**

| الحقل | الوصف |
|---|---|
| `employee_id` | الموظف |
| `effective_from` · `effective_to` | نافذة السريان (النهاية اختيارية = مفتوح) |
| `reason_code` | `executive` (قيادي) · `field_role` (ميداني) · `remote` (عن بُعد) · `contractual` (بنص العقد) · `medical` (طبي مؤقت) · `other` مع نص |
| `overtime_eligible_override` | `null` (يرث الافتراضي: غير مستحق) · `true` · `false` (EX-11) |
| `unpaid_leave_deductible_override` | `null` (يرث الافتراضي: يُخصم) · `true` · `false` (EX-12) |
| `requires_checkin_for_presence` | هل يُطلب منه تسجيل حضور معلوماتي بلا أثر مالي (افتراضي: لا) |
| `status` · `created_by` · `approved_by` | الحالة والمُنشئ والمعتمِد |

**قواعد العمل**

1. الاستثناء **نافذة زمنية تُقيَّم يوميًا**: الموظف مستثنى في يوم ما إذا وقع اليوم داخل نافذة استثناء نشطة (EX-13)؛ لا يوجد علَم دائم منفصل عن النافذة.
2. لا تتداخل نافذتان نشطتان لموظف واحد؛ التعديل بإنهاء النافذة القائمة وفتح أخرى.
3. الإنشاء والتعديل بصلاحية `attendance_exemption.manage` (HR افتراضيًا) ويمرّ بخطوة اعتماد واحدة على الأقل (مدير HR) قبل السريان.
4. يُتاح تعريف قالب على مستوى **الوظيفة/الدرجة** (`job_grade_default_exempt`) يقترح الاستثناء تلقائيًا عند التعيين، لكن السريان يبقى بنافذة فردية معتمدة.
5. الموظف يرى في ملفه حالته «مستثنى من الحضور من {تاريخ} إلى {تاريخ/مفتوح}» والسبب المختصر.

**معايير القبول**

- إنشاء استثناء مفتوح النهاية لمدير تنفيذي بسريان 2026-01-01 يجعل `IS_ATTENDANCE_EXEMPT = 1` في كل مسير لاحق يشمله.
- محاولة إنشاء نافذة ثانية تتداخل مع القائمة تُرفض بذكر النافذة المتعارضة.
- مستخدم HR دون صلاحية `attendance_exemption.manage` لا يرى زر الإنشاء، والطلب عبر واجهة البرمجة يُرفض.
- الموظف المستثنى يرى في ملفه الذاتي حالة الاستثناء ونافذته.

---

### EX-10 — أثر الاستثناء على خصومات الحضور

**الوصف**
يجب أن يمنع النظام **توليد** أي خصم حضور (تأخير، غياب، نقص ساعات، انصراف مبكر) للموظف المستثنى في الأيام المشمولة بالاستثناء **من المصدر**، لا بتوليد الخصم ثم إعفائه، بحيث ينزل راتبه ثابتًا دون أن يُغرق تقرير الإعفاءات بحالات ليست إعفاءً.

**قواعد العمل**

1. عند احتساب متغيرات الحضور للمسير، تُصفَّر `LATE_MINUTES` · `LATE_INCIDENTS` · `SHORT_MINUTES` · `ABSENCE_DAYS` · `EARLY_LEAVE_MINUTES` للأيام المستثناة قبل تقييم أي معادلة أو شريحة؛ و`IS_ATTENDANCE_EXEMPT = 1` للموظف المستثنى كامل الفترة.
2. سجلات البصمة (إن وُجدت) تُحفظ كما هي لأغراض معلوماتية ولا تُنتج وقائع تأخير أو غياب؛ يُعلَّم اليوم في سجل الحضور بحالة `exempt`.
3. لا يُنشأ أي «إعفاء» (EX-01) آليًا للمستثنى؛ وتقرير الإعفاءات لا يعرض المستثنين إلا في فقرة إحصائية مستقلة «راتب ثابت بالاستثناء».
4. الخصومات **المصنّفة** (جودة، التزام، إداري، إنتاجية) والسلف والاستقطاعات النظامية **تسري** على المستثنى كأي موظف؛ الاستثناء يخصّ الانضباط الزمني فقط.
5. الإجازات مدفوعة الأجر تُسجَّل للمستثنى لأغراض الرصيد دون أثر مالي؛ الإجازة بلا أجر وفق EX-12.
6. سجل التفصيل يعرض للمستثنى سطرًا معلوماتيًا: «خصومات الحضور: غير مولَّدة — استثناء #{رقم} من {تاريخ}».

```
مثال (أساسي 12,000 · بدل سكن 3,000 · أساس 30):
  موظف مستثنى كامل الفترة، بيانات البصمة تُظهر 3 أيام بلا حضور و95 دقيقة تأخير
  ABSENCE_DAYS = 0 · LATE_MINUTES = 0
  خصم غياب = 0 · خصم تأخير = 0
  خصم جودة معتمد 1 يوم = 1 × (15,000 ÷ 30) = 500.00  ← يسري
  الصافي قبل الاستقطاعات = 15,000 − 500 = 14,500.00
```

**معايير القبول**

- المستثنى في المثال يظهر لديه خصم غياب 0 وخصم تأخير 0 وخصم جودة 500.00.
- تقرير الإعفاءات لفترة المثال لا يحتوي على أي إعفاء لهذا الموظف.
- سجل الحضور اليومي للمستثنى يعرض الأيام الثلاثة بحالة `exempt` لا `absent`، مع الاحتفاظ ببصمات الأيام الأخرى معلوماتيًا.
- تطبيق سياسة بشرائح تأخير صارمة على مسير يضمّ مستثنى لا يُنتج له أي خصم تأخير.

---

### EX-11 — قرار الأوفرتايم للمستثنى

**الوصف**
يجب أن يحدّد النظام صراحةً أن المستثنى من الحضور **غير مستحق للعمل الإضافي افتراضيًا** — لأن من لا يخضع لقياس زمني لا يُكافأ بقياس زمني — مع إتاحة **تجاوز فردي** عبر مسار طلب معتمد فقط، وبلا اكتشاف آلي للإضافي من البصمة.

**قواعد العمل**

1. الافتراضي على مستوى الجهة `exempt_overtime_default = not_eligible`، قابل للضبط؛ ويُتجاوز على مستوى نافذة الاستثناء بـ`overtime_eligible_override`.
2. عند عدم الاستحقاق: تُصفَّر `OT_HOURS_*` للأيام المستثناة، ويُرفض أي طلب إضافي للموظف برسالة تذكر الاستثناء، ولا تُولَّد اقتراحات إضافي من البصمة.
3. عند التجاوز الفردي (`true`): يُستحق الإضافي **عبر طلب مسبق أو لاحق معتمد** بعدد ساعات صريح يعتمده المدير وHR؛ لا يُحتسب من الفارق بين البصمة ونهاية الوردية.
4. سعر الساعة للمستثنى المستحق يُشتق من `DAY_RATE ÷ STANDARD_DAY_HOURS` بنفس قواعد الجميع؛ و`STANDARD_DAY_HOURS` تؤخذ من إعداد السياسة لا من وردية (قد لا تكون له وردية).
5. القرار (افتراضي أو تجاوز) يُلتقط في لقطة عضوية المسير (`overtime_eligible_snapshot`).

```
مثال:
  مستثنى بتجاوز true · DAY_RATE 500.00 · STANDARD_DAY_HOURS 8 → HOUR_RATE 62.50
  طلب إضافي معتمد 6 ساعات عادية بمعامل 1.5 = 6 × 62.50 × 1.5 = 562.50
  مستثنى بلا تجاوز: نفس البصمة (بقاء 6 ساعات بعد الدوام) → 0.00 ولا اقتراح
```

**معايير القبول**

- مستثنى بلا تجاوز يقدّم طلب إضافي فيُرفض عند الإرسال بذكر رقم الاستثناء.
- مستثنى بتجاوز `true` وطلب معتمد 6 ساعات يستلم 562.50 كما في المثال، ولا يظهر في تقرير «إضافي مكتشف من البصمة».
- تغيير الافتراضي على مستوى الجهة لا يغيّر لقطة مسير محسوب.

---

### EX-12 — قرار الإجازة بلا أجر للمستثنى

**الوصف**
يجب أن يحدّد النظام أن الإجازة بلا أجر المعتمدة **تُخصم من المستثنى افتراضيًا** — لأن الاستثناء يُلغي الانضباط الزمني لا مبدأ الأجر مقابل مدة الخدمة — مع تجاوز فردي معرَّف على نافذة الاستثناء.

**قواعد العمل**

1. الافتراضي `exempt_unpaid_leave_default = deductible`، قابل للضبط؛ التجاوز الفردي بـ`unpaid_leave_deductible_override`.
2. الخصم يُحتسب: `UNPAID_LEAVE_DAYS × DAY_RATE` بسعر يوم المسير (AL-04) وبدخول البدلات وفق علَم `affects_day_rate`؛ ويُعامل كعدم استحقاق (يُخفّض الوعاء) لا كخصم خاضع لسقف الحماية.
3. تُحتسب أيام الإجازة بلا أجر من سجل الإجازات المعتمدة فقط، لا من غياب البصمة (الذي لا يُولِّد شيئًا للمستثنى).
4. عند التجاوز إلى `false`: الإجازة بلا أجر تُسجَّل للرصيد ولا تُخصم، ويُبيَّن ذلك في التفصيل بسطر معلوماتي.
5. الإجازة بلا أجر التي تمتد عبر فترتين تُوزَّع بأيامها على كل فترة.

```
مثال (أساسي 12,000 · بدل سكن 3,000 يدخل سعر اليوم · أساس 30):
  DAY_RATE = 15,000 ÷ 30 = 500.00
  إجازة بلا أجر معتمدة 4 أيام → 4 × 500.00 = 2,000.00
  الاستحقاق = 15,000 − 2,000 = 13,000.00
  بتجاوز false → 15,000.00 مع سطر «إجازة بلا أجر 4 أيام — غير مخصومة بقرار الاستثناء #{رقم}»
```

**معايير القبول**

- مستثنى بإجازة بلا أجر 4 أيام معتمدة يُخصم منه 2,000.00 كما في المثال.
- تعديل التجاوز إلى `false` يُلغي الخصم في المسير التالي ويظهر السطر المعلوماتي.
- غياب المستثنى 4 أيام بلا طلب إجازة لا يُنتج أي خصم (EX-10)، بينما الإجازة بلا أجر المعتمدة تُنتجه.

---

### EX-13 — سريان الاستثناء بتاريخ والفترة الجزئية

**الوصف**
يجب أن يُقيَّم الاستثناء **يومًا بيوم** داخل فترة المسير، بحيث يُعامل الموظف الذي بدأ استثناؤه أو انتهى في منتصف الفترة معاملة مختلطة: أيام خاضعة للحضور بقواعده الكاملة، وأيام مستثناة بلا توليد خصومات، دون أثر رجعي غير مقصود على أيام سابقة.

**قواعد العمل**

1. لكل يوم في الفترة: `is_exempt_day = (يقع اليوم داخل نافذة استثناء نشطة معتمدة)`.
2. متغيرات الحضور تُحتسب على الأيام غير المستثناة فقط؛ `IS_ATTENDANCE_EXEMPT` على مستوى الموظف = 1 فقط إذا كانت **كل** أيام تغطيته في الفترة مستثناة، وإلا 0 مع متغيّر إضافي `EXEMPT_DAYS` (عدد الأيام المستثناة).
3. الشرائح ذات النطاق `per_period` (مثل شرائح تأخير تراكمية) تُطبَّق على مجموع الأيام غير المستثناة فقط.
4. لا يجوز أن يسبق `effective_from` تاريخ اعتماد النافذة بأكثر من **الفترة المفتوحة الحالية** (`exemption_retro_scope`، افتراضي: الفترة الحالية فقط)؛ الأثر الرجعي على فترة مرحّلة يكون بقيد عكسي موثَّق لخصومات الحضور المولَّدة، بصلاحية `hr_manager`.
5. إنهاء النافذة (`effective_to`) في منتصف الفترة يعيد الأيام التالية إلى القياس الكامل من اليوم التالي للانتهاء.
6. لقطة عضوية المسير تحفظ `exempt_from_snapshot` · `exempt_to_snapshot` · `exempt_days` لكل موظف.

```
مثال (فترة 1–30 يونيو · أساسي 9,000 · أساس 30 · DAY_RATE 300.00):
  استثناء يبدأ 16 يونيو (مفتوح)
  1–15 يونيو: خاضع — غياب يوم 8 يونيو → 1 × 300.00 = 300.00 · تأخير 40 دقيقة يوم 3 يونيو → حسب الشرائح
  16–30 يونيو: مستثنى — غياب يومي 20 و27 يونيو → 0.00
  EXEMPT_DAYS = 15 · ABSENCE_DAYS = 1 · IS_ATTENDANCE_EXEMPT = 0
```

**حالات حدّية**
- نافذة تبدأ وتنتهي في اليوم نفسه: يوم مستثنى واحد.
- استثناء ينتهي في آخر يوم من الفترة وموظف ينتهي عقده في اليوم ذاته: تُحتسب التغطية والاستثناء كلاهما شاملين للطرفين.

**معايير القبول**

- الموظف في المثال يظهر لديه خصم غياب 300.00 عن يوم 8 يونيو فقط، و`EXEMPT_DAYS = 15`.
- إنشاء نافذة بسريان يسبق الفترة المفتوحة الحالية يُرفض لمستخدم بلا `hr_manager`؛ ومع الصلاحية يُنشأ قيد عكسي لخصومات الحضور في الفترة المرحّلة ويظهر في المسير التالي.
- شريحة تأخير تراكمية `per_period` تُطبَّق على دقائق الأيام غير المستثناة فقط.

---

### EX-14 — ظهور المستثنى في كشوف الحضور

**الوصف**
يجب أن يظهر الموظف المستثنى في كشوف الحضور اليومية والشهرية **بحالة «مستثنى»** واضحة لا كغائب ولا كحاضر، وأن يُستبعد من **مقامات** مؤشرات الالتزام (نسبة الحضور، متوسط التأخير، معدل الغياب) حتى لا يُعمى المدير عنه ولا تُزوَّر المؤشرات به.

**قواعد العمل**

1. في الكشف اليومي: الأيام المستثناة تُعرض بحالة `exempt` بلون/رمز مميّز، مع إظهار البصمات إن وُجدت كمعلومة (`requires_checkin_for_presence`).
2. في الكشف الشهري: سطر الموظف يُعرض مع عدّاد `exempt_days` وتُترك خانات التأخير/الغياب فارغة (لا صفر) للأيام المستثناة.
3. مؤشرات الالتزام: البسط والمقام يُحسبان على الأيام غير المستثناة فقط؛ الموظف المستثنى كامل الفترة يُستبعد من مقام «عدد الموظفين» في مؤشرات القسم، ويُعرض عدد المستثنين إلى جانب المؤشر.
4. تصفية الكشوف تدعم «إظهار/إخفاء المستثنين» و«المستثنون فقط»، والافتراضي **إظهار**.
5. تقرير «المستثنون من الحضور» يعرض القائمة الحالية بنوافذها وأسبابها ومن اعتمدها ومتى تنتهي.

**معايير القبول**

- قسم من 10 موظفين أحدهم مستثنى كامل الفترة و9 حضروا كاملًا: نسبة الحضور 100% (مقام 9) مع بيان «مستثنى: 1».
- الموظف في مثال EX-13 يظهر في الكشف الشهري بـ15 يوم `exempt` ويوم غياب واحد، ومؤشر غيابه يُحسب على 15 يومًا لا 30.
- إخفاء المستثنين من الكشف اليومي لا يغيّر قيم المؤشرات.

---

### EX-15 — جدول قرارات التفاعل

**الوصف**
يجب أن يُنفَّذ تفاعل الاستثناء مع كل بند من بنود الاستحقاق والخصم وفق **جدول قرارات (Decision Table)** معلن وقابل للضبط على مستوى الجهة، بحيث لا يُترك أي بند لسلوك ضمني، ويُلتقط الجدول في لقطة السياسة.

**جدول القرارات الافتراضي (قابل للضبط)**

| البند | المستثنى (افتراضي) | التجاوز الفردي | المرجع |
|---|---|---|---|
| خصم التأخير | لا يُولَّد | — | EX-10 |
| خصم الغياب غير المصرّح | لا يُولَّد | — | EX-10 |
| خصم نقص الساعات / الانصراف المبكر | لا يُولَّد | — | EX-10 |
| العمل الإضافي | غير مستحق | نعم، عبر طلب معتمد | EX-11 |
| الإجازة بلا أجر | تُخصم | نعم | EX-12 |
| الإجازة مدفوعة الأجر | تُسجَّل للرصيد بلا أثر مالي | — | EX-10 |
| الخصومات المصنّفة (جودة/التزام/إداري/إنتاجية) | تسري | — | DD |
| أقساط السلف والمديونيات | تسري | — | AD |
| الاستقطاعات النظامية والأحكام القضائية | تسري | — | — |
| البدلات الدائمة والتناسب | تسري بقواعدها | — | AL |
| المكافآت | تسري | — | EX-05 |
| بدل الوردية/العمل الليلي (إن وُجد) | غير مستحق | نعم | — |
| مؤشرات الالتزام | مستبعد من المقام | — | EX-14 |
| سقف حماية الصافي | يسري | — | DD-11 |

**قواعد العمل**

1. الجدول يُدار من واجهة إعدادات بصلاحية `payroll_manager`، ويحمل رقم إصدار يُلتقط في لقطة السياسة.
2. إضافة بند جديد للسياسة يُلزم بتحديد سلوكه للمستثنى قبل نشر السياسة (لا افتراضي ضمني).
3. التجاوز الفردي لا يُتاح إلا للبنود المعلَّمة «نعم» في عمود التجاوز.
4. سجل التفصيل للمستثنى يذكر لكل بند القرار المطبَّق ومصدره (افتراضي/تجاوز).

**معايير القبول**

- نشر سياسة تحتوي بندًا جديدًا بلا قرار للمستثنى يُرفض بذكر البند.
- تغيير قرار «الإجازة بلا أجر» إلى «لا تُخصم» على مستوى الجهة يسري على المسيرات غير المحسوبة فقط.
- سجل تفصيل مستثنى يعرض 14 سطر قرار مطابقة للجدول المطبَّق بإصداره.

---

### EX-16 — حوكمة الاستثناء

**الوصف**
يجب أن يخضع الاستثناء من الحضور لحوكمة صريحة — لأنه يُلغي ضابطًا رقابيًا كاملًا — تشمل الاعتماد والمراجعة الدورية وحدود العدد والمدة والتقارير وأثر التدقيق.

**الضوابط الافتراضية (قابلة للضبط)**

| الضابط | الافتراضي |
|---|---|
| الاعتماد | خطوة `hr_manager` إلزامية؛ وللسبب `executive` خطوة إضافية «الرئيس التنفيذي/المفوَّض» |
| المراجعة الدورية للنوافذ المفتوحة (`exemption_review_months`) | كل **12 شهرًا**: تنبيه قبل 30 يومًا، ويُعلَّق الاستثناء إن لم يُجدَّد خلال 30 يومًا بعد الموعد (`suspend_on_review_lapse`، افتراضي: نعم) |
| الحدّ الأقصى لمدة السبب `medical` | **6 أشهر** لكل نافذة، ثم تجديد بمستند |
| سقف نسبة المستثنين في القسم (`max_exempt_pct_per_unit`) | **20%** من موظفي الوحدة؛ التجاوز يتطلّب `hr_manager` وتبريرًا |
| الأثر الرجعي | الفترة المفتوحة الحالية فقط (EX-13) |
| الإلغاء | بسبب ≥ 20 حرفًا، يسري من تاريخ محدّد لا يسبق الفترة المفتوحة |

**قواعد العمل**

1. كل إنشاء/تعديل/إنهاء/تعليق/تجديد يُسجَّل في أثر التدقيق مع الفاعل والوقت والقيم قبل وبعد والسبب.
2. تقرير حوكمة الاستثناءات: القائمة الحالية، النوافذ التي تنتهي خلال 60 يومًا، النوافذ التي تجاوزت موعد المراجعة، نسبة المستثنين لكل وحدة، والاستثناءات المُنشأة بأثر رجعي.
3. الموظف والمدير المباشر يُخطران عند الاعتماد والتعليق والإنهاء؛ HR تُخطر بموعد المراجعة.
4. تعليق الاستثناء (`suspended`) يعيد الموظف إلى القياس الكامل من يوم التعليق دون حذف النافذة؛ التجديد يُنشئ نافذة جديدة.
5. الاستثناء لا يُنقل تلقائيًا مع نقل الموظف بين الأقسام؛ يُعلَّم «بحاجة إلى مراجعة» ويبقى ساريًا حتى قرار خلال 30 يومًا.

**معايير القبول**

- استثناء بسبب `executive` لا يصبح ساريًا قبل خطوتي الاعتماد، ويظهر بحالة «قيد الاعتماد» حتى اكتمالهما.
- نافذة مفتوحة أُنشئت قبل 12 شهرًا تُنتج تنبيه مراجعة قبل 30 يومًا، وتُعلَّق تلقائيًا بعد 30 يومًا من الموعد إن لم تُجدَّد، ويظهر التعليق في أثر التدقيق.
- محاولة إنشاء استثناء يرفع نسبة المستثنين في قسم من 10 موظفين إلى 30% تُرفض لمن دون `hr_manager`.
- تقرير الحوكمة يعرض الاستثناءات المُنشأة بأثر رجعي مع القيود العكسية المرتبطة بها.

---

## ٦) السلف والعمل الإضافي والتقارير

يحدّد هذا القسم متطلبات ثلاث منظومات مترابطة: **سقوف السلف (Advance Limits)**، و**السلف الاستثنائية المقسّطة (Exceptional Installment Advances)**، و**العمل الإضافي (Overtime)**، ثم **منظومة التقارير والتحليلات (Reporting & Analytics)** التي تغطّي دورة الرواتب بالكامل. المعرّفات: `AD-xx` للسلف، `OT-xx` للعمل الإضافي، `RP-xx` للتقارير.

جميع المبالغ في هذا القسم تُعرض بـ**وحدة نقد** محايدة (ر.س/ج.م) ويجب أن يدعم النظام تعريف العملة ودقّة الكسور العشرية على مستوى الجهة (Tenant).

---

### أ) سقوف السلف (Advance Limits)

#### AD-01 — تعريف سياسة سقوف السلف

**الوصف**
يجب أن يوفّر النظام كيانًا لتعريف **سياسة سلف (Advance Policy)** قابلة للضبط دون تعديل برمجي، تحدّد الحدود القصوى المسموح بها لطلبات السلف. ويجب أن تكون السياسة قابلة للتخصيص بحسب **نطاق (Scope)**: الشركة كاملة · فرع · قسم · درجة وظيفية · فئة تعاقدية · مجموعة موظفين مخصّصة.

**قواعد العمل**

| الحقل | النوع | الوصف |
|---|---|---|
| `scope_type` | تعداد | `COMPANY` / `BRANCH` / `DEPARTMENT` / `JOB_GRADE` / `EMPLOYMENT_TYPE` / `CUSTOM_GROUP` |
| `scope_ref` | معرّف | مرجع الكيان المستهدف (فارغ عند `COMPANY`) |
| `percent_of_salary` | نسبة | السقف كنسبة مئوية من قاعدة الأجر |
| `salary_base` | تعداد | قاعدة احتساب النسبة: `BASIC` (الأساسي) / `GROSS` (الإجمالي) / `NET_LAST_PERIOD` (صافي آخر فترة) |
| `flat_cap_amount` | مبلغ | سقف مقطوع لا يتجاوزه أي طلب |
| `max_requests_per_month` | عدد | أقصى عدد طلبات مقبولة في الشهر الواحد |
| `max_amount_per_month` | مبلغ | أقصى إجمالي قيمة سلف في الشهر الواحد |
| `max_outstanding_balance` | مبلغ | أقصى رصيد مديونية قائمة (AD-06) |
| `effective_from` / `effective_to` | تاريخ | نافذة سريان السياسة |
| `priority` | عدد | أولوية الترجيح عند تطابق أكثر من سياسة |
| `is_active` | منطقي | تفعيل/تعطيل |

- يجب أن تكون كل حقول السقوف **اختيارية**؛ الحقل الفارغ يعني «لا سقف من هذا النوع» ولا يُشارك في المقارنة.
- يجب أن يحتفظ النظام بـ**تأريخ (Versioning)** لكل سياسة، فلا يُعدَّل سجل سارٍ في مكانه بل تُنشأ نسخة جديدة بتاريخ سريان.
- عند انطباق أكثر من سياسة على الموظف نفسه، يُختار المرشّح الأخصّ نطاقًا، وعند التساوي تُرجّح `priority` الأعلى.

**معايير القبول**
- [ ] يمكن إنشاء سياسة على مستوى الشركة وأخرى على مستوى فرع، ويظهر أثر سياسة الفرع على موظفي ذلك الفرع فقط.
- [ ] تعديل سياسة سارية يُنتج نسخة جديدة ويحتفظ بالنسخة السابقة قابلة للاستعلام بتاريخ.
- [ ] سياسة مُعطّلة أو خارج نافذة السريان لا تُشارك في أي تحقق.
- [ ] ترك جميع حقول السقوف فارغة يُنتج رسالة تحقق تمنع الحفظ.

---

#### AD-02 — السقف كنسبة من الراتب

**الوصف**
يجب أن يحتسب النظام سقفًا لكل موظف على حدة بضرب `percent_of_salary` في قاعدة الأجر المحدّدة في السياسة.

**قواعد العمل**

```
percent_cap = round( salary_base_value × percent_of_salary / 100 , currency_precision )
```

- يجب أن تُقرأ `salary_base_value` من **بنية الأجر السارية بتاريخ الطلب**، لا من قيمة مستقبلية أو منتهية.
- إذا كانت القاعدة `NET_LAST_PERIOD` ولم توجد فترة رواتب سابقة للموظف (موظف جديد)، يجب أن يتراجع النظام إلى `BASIC` وأن يسجّل سبب التراجع في أثر الاحتساب.
- يجب أن تكون سياسة التقريب قابلة للضبط: لأقرب وحدة نقد صحيحة، أو لأقرب كسر عشري، أو للأسفل دائمًا.

**مثال محسوب**

```
المعطيات: الراتب الأساسي = 8,000 وحدة نقد ، النسبة = 10% ، التقريب = لأقرب وحدة صحيحة
الحساب  : 8,000 × 10 ÷ 100 = 800
النتيجة : السقف بالنسبة = 800 وحدة نقد
```

**معايير القبول**
- [ ] تغيير قاعدة الاحتساب من `BASIC` إلى `GROSS` يغيّر السقف المعروض للموظف فورًا.
- [ ] موظف بلا فترة رواتب سابقة وسياسة `NET_LAST_PERIOD` يحصل على سقف محتسب من الأساسي مع بيان السبب.
- [ ] السقف المحتسب يظهر للموظف قبل تقديم الطلب.

---

#### AD-03 — السقف بمبلغ مقطوع على مستوى الشركة

**الوصف**
يجب أن يوفّر النظام سقفًا بمبلغ ثابت لا يتجاوزه أي طلب سلفة بصرف النظر عن راتب مقدّم الطلب.

**قواعد العمل**
- السقف المقطوع يُطبَّق على **قيمة الطلب الواحد**.
- يجب أن يكون قابلًا للتخصيص بنفس آلية النطاقات في AD-01، فيجوز أن يكون للشركة سقف مقطوع عام ولفرع معيّن سقف مقطوع أدنى.
- يجب رفض أي محاولة لحفظ سياسة يكون فيها السقف المقطوع صفرًا أو سالبًا.

**معايير القبول**
- [ ] موظف براتب مرتفع لا يستطيع طلب مبلغ يتجاوز السقف المقطوع.
- [ ] السقف المقطوع للفرع يَجُبّ السقف المقطوع للشركة عندما يكون أدنى.

---

#### AD-04 — سقف عدد وقيمة السلف في الشهر الواحد

**الوصف**
يجب أن يوفّر النظام حدّين شهريين مستقلّين: **عدد الطلبات** المسموح بها في الشهر، و**إجمالي قيمة** السلف في الشهر.

**قواعد العمل**
- يجب تعريف «الشهر» بوضوح وقابلية الضبط: شهر تقويمي، أو **فترة المسير (Payroll Period)** التي يقع فيها تاريخ الطلب. القيمة الافتراضية: فترة المسير.
- يجب أن تُحتسب ضمن العدّاد الشهري الطلبات ذات الحالات: `APPROVED` و`DISBURSED` و`PENDING_APPROVAL`. ولا تُحتسب `REJECTED` و`CANCELLED`.
- الطلب المعلّق يحجز حصة من العدّاد؛ فإذا رُفض تُحرَّر الحصة تلقائيًا.
- يجب أن يعرض النظام للموظف المتبقي من كلا الحدّين قبل تقديم الطلب (مثال: «متبقٍ لك طلب واحد بقيمة 300 وحدة نقد هذا الشهر»).

**معايير القبول**
- [ ] عند بلوغ حد العدد الشهري يُمنع تقديم طلب جديد مع رسالة تبيّن تاريخ إعادة تعيين العدّاد.
- [ ] رفض طلب معلّق يُعيد الحصة المحجوزة للعدّاد فورًا.
- [ ] تغيير تعريف الشهر من تقويمي إلى فترة مسير يعيد احتساب العدّادات المعروضة دون بيانات معطوبة.

---

#### AD-05 — قاعدة الأقل عند تعدّد السقوف

**الوصف**
عند انطباق أكثر من نوع سقف على الطلب نفسه، يجب أن يُطبِّق النظام **أدنى القيم** بوصفها المبلغ الأقصى المسموح به.

**قواعد العمل**

```
effective_cap = MIN(
    percent_cap,                                  ← AD-02 (إن وُجد)
    flat_cap_amount,                              ← AD-03 (إن وُجد)
    max_amount_per_month - used_amount_this_month,← AD-04 (إن وُجد)
    max_outstanding_balance - current_outstanding ← AD-06 (إن وُجد)
)
إذا كانت effective_cap ≤ 0 → يُمنع تقديم الطلب.
```

- يجب أن يعرض النظام **السقف الفعّال ومصدره** (أي القيد الذي حكم)، لأن معرفة القيد الحاكم شرط لتبرير الرفض أمام الموظف.
- يجب تخزين **لقطة (Snapshot)** من قيم السقوف المطبَّقة لحظة التقديم داخل سجل الطلب لأغراض التدقيق.

**مثال محسوب**

```
المعطيات:
  الراتب الأساسي            = 8,000     → سقف النسبة (10%)        = 800
  السقف المقطوع للشركة                                              = 1,500
  سقف قيمة السلف الشهرية    = 1,000 ، المستهلك هذا الشهر = 0        = 1,000
  سقف المديونية القائمة     = 3,000 ، الرصيد القائم       = 2,500   = 500
الحساب : MIN(800 , 1500 , 1000 , 500)
النتيجة: السقف الفعّال = 500 وحدة نقد ، والقيد الحاكم = «سقف المديونية القائمة»
```

**معايير القبول**
- [ ] الطلب بمبلغ 501 في المثال أعلاه يُرفض مع بيان القيد الحاكم نصًّا.
- [ ] الطلب بمبلغ 500 يُقبل.
- [ ] لقطة السقوف محفوظة في سجل الطلب وتظهر في مسار التدقيق حتى بعد تعديل السياسة لاحقًا.

---

#### AD-06 — سقف إجمالي المديونية القائمة

**الوصف**
يجب أن يوفّر النظام سقفًا لإجمالي **الرصيد القائم (Outstanding Balance)** للموظف، بحيث لا يتجاوز مجموع ما لم يُسدَّد بعدُ من جميع سلفه حدًّا معيّنًا.

**قواعد العمل**

```
current_outstanding = Σ (advance.disbursed_amount) − Σ (advance.repaid_amount)
                      لكل السلف ذات الحالة ACTIVE أو PARTIALLY_REPAID
available_headroom  = max_outstanding_balance − current_outstanding
```

- يجب أن تدخل في الحساب **جميع أنواع السلف** (العادية والاستثنائية) وأي مديونية أخرى مُعرَّفة كقابلة للاسترداد من الراتب، ما لم تُستثنَ صراحة بعلامة `excluded_from_outstanding_cap`.
- يجب تحديث الرصيد القائم عند كل عملية صرف وكل عملية خصم قسط وكل سداد مبكر، بمعاملة ذرّية (Atomic) تمنع تداخل عمليتين متزامنتين على الموظف نفسه.
- يجب أن يعرض النظام للموظف رصيده القائم والمساحة المتاحة قبل الطلب.

**حالات حدّية**
- سلفة مصروفة جزئيًا: يدخل المصروف فعلًا في الرصيد لا المعتمد.
- سلفة معتمدة وغير مصروفة: يجب أن يكون قابلًا للضبط هل تُحجز ضمن الرصيد القائم (الافتراضي: تُحجز، منعًا لتجاوز السقف بين الاعتماد والصرف).
- رصيد قائم يتجاوز السقف بعد تخفيض السقف إداريًا: تُمنع الطلبات الجديدة ولا تُلغى الالتزامات القائمة.

**معايير القبول**
- [ ] موظف رصيده القائم يساوي السقف لا يستطيع تقديم أي طلب جديد.
- [ ] بعد خصم قسط، تزداد المساحة المتاحة بقيمة القسط المخصوم فورًا.
- [ ] طلبان متزامنان لموظف واحد لا ينتج عنهما تجاوز للسقف.

---

#### AD-07 — التحقق المزدوج: وقت التقديم ووقت الاعتماد

**الوصف**
يجب أن يُجري النظام تحقق السقوف **مرّتين على الأقل**: عند تقديم الطلب، وعند كل خطوة اعتماد قبل تثبيتها؛ لأن معطيات الموظف قد تتغيّر بين اللحظتين.

**قواعد العمل**
- عند التقديم: يُحتسب `effective_cap` ويُخزَّن في لقطة الطلب مع تاريخ الاحتساب.
- عند الاعتماد: يُعاد احتساب `effective_cap` بمعطيات **لحظة الاعتماد**. فإذا صار المبلغ المطلوب أكبر من السقف الجديد، يجب أن يعرض النظام على المعتمد ثلاثة خيارات صريحة:
  1. **الرفض** مع بيان السبب.
  2. **التخفيض** إلى السقف الجديد (تُسجَّل قيمة معتمدة `approved_amount` أقل من المطلوبة).
  3. **التصعيد** إلى مسار السلفة الاستثنائية (AD-08) إذا كانت صلاحية المعتمد تسمح.
- يجب ألّا يُنفَّذ الاعتماد بصمت مع تجاوز السقف؛ وأي تجاوز مقصود يجب أن يُسجَّل بوصفه استثناءً موثّقًا يظهر في تقرير الحوكمة (RP-11).

**معايير القبول**
- [ ] تغيير سياسة السقوف بين التقديم والاعتماد يظهر للمعتمد كتنبيه صريح.
- [ ] لا يمكن إتمام اعتماد يتجاوز السقف دون تسجيل استثناء موثّق بمعرّف مستخدم وسبب.
- [ ] الطلب المخفَّض يحتفظ بقيمتين متمايزتين: المطلوبة والمعتمدة.

---

#### AD-08 — تغيّر الراتب بعد الاعتماد وقبل الصرف

**الوصف**
يجب أن يحدّد النظام سلوكًا صريحًا عند تغيّر أجر الموظف (زيادة أو تخفيض أو تغيير بنية الأجر) في الفترة الواقعة بين اعتماد السلفة وصرفها.

**قواعد العمل**
- يجب أن يوفّر النظام إعدادًا على مستوى السياسة باسم `on_salary_change_before_disbursement` بالقيم:

| القيمة | السلوك |
|---|---|
| `KEEP_APPROVED` | تُصرف القيمة المعتمدة كما هي (اللقطة تحكم) |
| `REVALIDATE_AND_CAP` | يُعاد احتساب السقف؛ فإن صار أدنى تُخفَّض القيمة المصروفة إليه تلقائيًا مع إشعار |
| `REVALIDATE_AND_HOLD` | يُعاد احتساب السقف؛ فإن صار أدنى تُعلَّق السلفة وتُعاد لخطوة اعتماد إضافية |

- القيمة الافتراضية الموصى بها: `REVALIDATE_AND_HOLD` للتخفيضات و`KEEP_APPROVED` للزيادات — أي أن **الزيادة لا تُعيد فتح الطلب** بينما التخفيض يُعيده.
- في كل الأحوال يجب إشعار الموظف والمعتمد بالتغيير وسببه، وتسجيل الحدث في مسار التدقيق.
- إذا كان تغيّر الأجر بأثر رجعي يشمل تاريخ الاعتماد، يُعتمد الأجر **النافذ فعليًا بتاريخ الصرف**.

**مثال محسوب**

```
المعطيات: الراتب لحظة الاعتماد = 8,000 ، النسبة 10% → السقف = 800 ، المعتمد = 800
          قبل الصرف عُدِّل الراتب إلى 6,000  →  السقف الجديد = 600
السلوك REVALIDATE_AND_CAP : يُصرف 600 ، ويُخطر الموظف بالتخفيض وسببه
السلوك REVALIDATE_AND_HOLD: تُعلَّق السلفة وتعود لاعتماد جديد على مبلغ 600
```

**حالات حدّية**
- إنهاء خدمة الموظف بين الاعتماد والصرف → تُلغى السلفة تلقائيًا ولا تُصرف.
- تحوّل الموظف إلى فرع/قسم تحكمه سياسة سقوف مختلفة → تُطبَّق سياسة الوحدة الجديدة عند إعادة التحقق.

**معايير القبول**
- [ ] تخفيض الراتب قبل الصرف يُنتج السلوك المعرَّف في السياسة، لا سلوكًا ضمنيًا.
- [ ] كل تغيير في القيمة المصروفة مقابل المعتمدة مسجَّل بسبب ومرجع.
- [ ] إنهاء الخدمة قبل الصرف يمنع الصرف.

---

#### AD-09 — إنشاء السلفة الاستثنائية

**الوصف**
يجب أن يوفّر النظام مسارًا مستقلًا تنشئ من خلاله **الموارد البشرية** سلفة لموظف بعينه بمبلغ **يتجاوز السقف العادي**، مع سبب موثّق ومستند داعم.

**قواعد العمل**
- الصلاحية: يجب حصر إنشاء السلفة الاستثنائية في أدوار محدّدة (مثل `HR_MANAGER` / `PAYROLL_ADMIN`)، مع إمكانية اشتراط اعتماد إضافي من الإدارة المالية فوق مبلغ معيّن (`exceptional_second_approval_threshold`).
- الحقول الإلزامية: الموظف · المبلغ · **سبب نصّي إلزامي** لا يقل عن حد أدنى من المحارف · تصنيف السبب (تعداد قابل للضبط: حالة طبية، ظرف عائلي، التزام دراسي، أخرى) · **مرفق داعم اختياري أو إلزامي بحسب الإعداد** · عدد أشهر التقسيط · فترة المسير التي يبدأ منها الخصم.
- يجب أن يعرض النظام صراحة **مقدار التجاوز** عن السقف العادي (`requested_amount − effective_cap`) قبل الحفظ.
- يجب وسم السجل بـ`is_exceptional = true` ليظهر تلقائيًا في تقرير الحوكمة (RP-11).
- يجب أن يخضع سقف المديونية القائمة (AD-06) للضبط: هل تُعفى السلفة الاستثنائية منه أم لا (`bypass_outstanding_cap`)، وأي إعفاء يُسجَّل بوصفه استثناءً.

**معايير القبول**
- [ ] لا يمكن حفظ سلفة استثنائية بدون سبب مكتوب وتصنيف.
- [ ] مستخدم بدور غير مخوَّل لا يرى إجراء الإنشاء أصلًا، ومحاولة النفاذ المباشر تُرفض.
- [ ] السلفة الاستثنائية تظهر في تقرير الاستثناءات خلال الفترة نفسها التي أُنشئت فيها.
- [ ] مبلغ يتجاوز عتبة الاعتماد الثاني لا يصبح نافذًا قبل الاعتماد الثاني.

---

#### AD-10 — تحديد أشهر التقسيط وتوليد جدول الأقساط

**الوصف**
يجب أن تحدّد الموارد البشرية **عدد أشهر التقسيط** (1، 2، 3، … حتى حد أقصى قابل للضبط)، وأن يولّد النظام **جدول أقساط (Installment Schedule)** تلقائيًا يُخصم قسط منه في كل مسير رواتب.

**قواعد العمل**

```
base_installment = FLOOR( principal_amount / months_count , currency_precision )
last_installment = principal_amount − ( base_installment × ( months_count − 1 ) )
```

- يجب أن يستوعب **القسط الأخير** فرق التقريب كاملًا، بحيث يتحقّق دائمًا:
  `Σ (planned_amount) = principal_amount` بلا فرق ولا كسر معلّق.
- يجب أن يكون تاريخ بدء الخصم قابلًا للتحديد: **المسير الحالي** أو **المسير التالي** أو فترة محدّدة يدويًا.
- يجب أن يُنشَأ لكل قسط سجل مستقل بحقول: الترتيب · فترة الاستحقاق · المبلغ المخطّط · المبلغ المخصوم فعلًا · الحالة (`PENDING` / `DEDUCTED` / `PARTIAL` / `SKIPPED` / `WAIVED` / `SETTLED_EARLY`) · مرجع المسير الذي خُصم فيه.
- يجب أن يكون جدول الأقساط قابلًا لإعادة الجدولة بقرار موثّق (تغيير عدد الأشهر أو تأجيل قسط)، على أن يُعاد توليد الأقساط **غير المخصومة فقط** وتبقى المخصومة كما هي.
- الحد الأقصى لعدد الأشهر يجب أن يكون قابلًا للضبط، ويجب منع جدولة أقساط تمتد إلى ما بعد تاريخ انتهاء عقد محدّد المدة دون تنبيه صريح.

**مثال محسوب — 5,000 على 3 أشهر**

```
المعطيات : أصل المبلغ = 5,000 وحدة نقد ، عدد الأشهر = 3 ، دقة العملة = وحدة صحيحة
الحساب   : base = FLOOR(5,000 ÷ 3) = 1,666
           القسط 1 = 1,666
           القسط 2 = 1,666
           القسط 3 = 5,000 − (1,666 × 2) = 5,000 − 3,332 = 1,668
التحقق   : 1,666 + 1,666 + 1,668 = 5,000 ✔
النتيجة  : لا كسور معلّقة ، والفرق (2 وحدة نقد) استوعبه القسط الأخير
```

**مثال ثانٍ بدقّة عشرية**

```
المعطيات : أصل المبلغ = 1,000.00 ، عدد الأشهر = 3 ، الدقة = منزلتان
الحساب   : base = FLOOR(333.3333, 2) = 333.33
           القسط 1 = 333.33 ، القسط 2 = 333.33
           القسط 3 = 1,000.00 − 666.66 = 333.34
التحقق   : 333.33 + 333.33 + 333.34 = 1,000.00 ✔
```

**معايير القبول**
- [ ] مجموع الأقساط المولَّدة يساوي أصل المبلغ بالضبط في كل حالات التقريب المختبرة.
- [ ] القسط الأخير — لا الأول — هو الذي يحمل فرق التقريب.
- [ ] تقسيط على شهر واحد يُنتج قسطًا واحدًا بقيمة أصل المبلغ.
- [ ] إعادة الجدولة بعد خصم قسطين تُبقي القسطين المخصومين وتعيد توزيع المتبقي فقط.

---

#### AD-11 — الخصم التلقائي للأقساط داخل المسير

**الوصف**
يجب أن يلتقط كل مسير رواتب — عند احتسابه — الأقساط المستحقة داخل فترة المسير للموظفين المشمولين، ويخصمها تلقائيًا بندًا مستقلًا في قسيمة الراتب.

**قواعد العمل**
- يُعتبر القسط مستحقًا داخل المسير إذا وقعت فترة استحقاقه ضمن فترة المسير أو كان متأخّرًا من فترة سابقة ولم يُخصم بعد.
- يجب أن يظهر الخصم في قسيمة الراتب باسم واضح ورقم مرجع السلفة وترتيب القسط (مثال: «قسط سلفة رقم 3 من 3»).
- عند إعادة احتساب المسير قبل ترحيله، يجب ألّا تتضاعف الأقساط: تُلغى القيود المؤقتة ويُعاد بناؤها.
- عند **ترحيل/إقفال المسير**، تنتقل حالة القسط إلى `DEDUCTED` وتُقيَّد حركة سداد في دفتر السلفة (AD-15) بشكل ذرّي.
- يجب ألّا يُخصم القسط من موظف غير مشمول في المسير؛ ويُبلَّغ عن هذه الحالة في تقرير «موظفون بلا مسير في الفترة» (RP-12).

**معايير القبول**
- [ ] احتساب المسير مرتين قبل الترحيل لا ينتج عنه خصم مزدوج.
- [ ] إلغاء ترحيل مسير يعيد الأقساط إلى `PENDING` ويعكس حركات الدفتر المقابلة.
- [ ] القسط المتأخّر من فترة سابقة يُلتقط في المسير التالي تلقائيًا.

---

#### AD-12 — تعذّر الخصم لعدم كفاية الصافي

**الوصف**
يجب أن يحدّد النظام سلوكًا صريحًا عندما لا يكفي صافي راتب الموظف لخصم القسط المستحق كاملًا.

**قواعد العمل**
- يجب توفير إعداد `minimum_net_pay_floor` — حد أدنى للصافي لا يجوز النزول تحته بفعل خصومات السلف (مبلغ ثابت أو نسبة من الأجر).
- عند تعذّر الخصم الكامل، يُطبَّق أحد سلوكين قابلين للضبط على مستوى السياسة:

| السلوك | الوصف |
|---|---|
| `PARTIAL_THEN_CARRY` | يُخصم أكبر مبلغ ممكن دون كسر الحد الأدنى، وتُرحَّل البقية إلى القسط التالي؛ حالة القسط `PARTIAL` |
| `SKIP_AND_EXTEND` | يُتخطّى القسط كاملًا، وتُمدَّد الجدولة بشهر إضافي؛ حالة القسط `SKIPPED` |

- ترتيب الأولوية عند تزاحم أكثر من التزام على الصافي (أقساط سلف، خصومات مقتطعة، التزامات أخرى) يجب أن يكون **قابلًا للتعريف** بقائمة أولويات مرتّبة، والافتراضي: الالتزامات النظامية ثم أقساط السلف الاستثنائية ثم العادية.
- يجب أن يُنتج النظام **استثناءً مرئيًا** في كشف المسير وفي تقرير السلف (RP-08) لكل قسط لم يُخصم كاملًا.

**مثال محسوب**

```
المعطيات: الصافي قبل خصم القسط = 900 ، القسط المستحق = 1,666 ، الحد الأدنى للصافي = 500
المتاح للخصم = 900 − 500 = 400
سلوك PARTIAL_THEN_CARRY : يُخصم 400 ، يُرحَّل 1,266 إلى القسط التالي ، حالة القسط = PARTIAL
سلوك SKIP_AND_EXTEND    : لا يُخصم شيء ، يُزاد عدد الأقساط بواحد ، حالة القسط = SKIPPED
```

**معايير القبول**
- [ ] لا ينتج عن خصم الأقساط صافٍ سالب في أي حال.
- [ ] لا ينزل الصافي تحت الحد الأدنى المعرَّف.
- [ ] كل قسط جزئي أو متخطّى يظهر في تقرير مخصّص ولا يمرّ صامتًا.
- [ ] المبلغ المرحَّل يظهر مضافًا إلى القسط التالي بقيمة صحيحة، ومجموع المسدَّد النهائي = أصل المبلغ.

---

#### AD-13 — ترك الخدمة والرصيد قائم

**الوصف**
عند إنهاء خدمة موظف عليه رصيد سلف قائم، يجب أن يُرحَّل الرصيد كاملًا إلى تسوية **مستحقات نهاية الخدمة (Final Settlement)**.

**قواعد العمل**
- عند تسجيل تاريخ آخر يوم عمل، يجب أن يُوقف النظام توليد الأقساط المستقبلية وأن يجمع الرصيد المتبقي في التزام واحد `FINAL_SETTLEMENT_RECOVERY`.
- يُقاصّ الرصيد القائم مع مستحقات نهاية الخدمة بترتيب مقاصّة قابل للتعريف.
- إذا لم تكفِ المستحقات لتغطية الرصيد، يجب أن يُنتج النظام **رصيدًا متبقّيًا مدينًا** بحالة `PENDING_RECOVERY` مع بيان قيمته، وأن يُدرَج في تقرير الأرصدة القائمة، دون أن يختفي أو يُشطب تلقائيًا.
- الشطب (Write-off) يجب أن يكون إجراءً صريحًا بصلاحية مستقلة وسبب موثّق، ويظهر في تقرير الحوكمة (RP-11).

**مثال محسوب**

```
المعطيات: الرصيد القائم = 3,340 ، مستحقات نهاية الخدمة = 2,100
المقاصّة : 2,100 ← تُقاصّ بالكامل
النتيجة  : مستحقات مدفوعة = 0 ، رصيد مدين متبقٍّ = 1,240 بحالة PENDING_RECOVERY
```

**معايير القبول**
- [ ] لا يُنشأ أي قسط بتاريخ استحقاق بعد آخر يوم عمل.
- [ ] الرصيد المتبقي بعد المقاصّة لا يُشطب تلقائيًا ويظهر في التقارير.
- [ ] عملية الشطب تتطلب صلاحية مستقلة وسببًا، وتُسجَّل في مسار التدقيق.

---

#### AD-14 — السداد المبكر

**الوصف**
يجب أن يدعم النظام **السداد المبكر (Early Settlement)** لسلفة قائمة، كليًّا أو جزئيًا، خارج آلية الخصم من المسير.

**قواعد العمل**
- يجب دعم مصدرين للسداد: **سداد نقدي/تحويل** يسجّله المخوَّل، و**خصم استثنائي دفعة واحدة** في مسير محدّد.
- عند السداد الجزئي، يجب أن يوفّر النظام خيارين قابلين للاختيار عند التنفيذ:
  1. **تقصير المدة**: يبقى مبلغ القسط كما هو وينقص عدد الأقساط.
  2. **تخفيض القسط**: يبقى عدد الأقساط ويُعاد توزيع المتبقي عليها.
- عند السداد الكلي، تنتقل السلفة إلى حالة `CLOSED` وتُلغى الأقساط `PENDING` المتبقية مع بيان سبب الإلغاء `EARLY_SETTLEMENT`.
- يجب أن يمنع النظام قبول سداد يتجاوز الرصيد المتبقي، وأن يقترح قيمة الإقفال الدقيقة.

**مثال محسوب**

```
المعطيات: أصل = 5,000 ، أقساط = 1,666 / 1,666 / 1,668 ، خُصم القسط الأول فقط
الرصيد المتبقي = 3,334 ، سداد مبكر جزئي = 1,334
خيار «تقصير المدة»  : يُلغى قسط ويبقى 2,000 على قسط واحد
خيار «تخفيض القسط» : يبقى قسطان ، كل قسط = FLOOR(2,000 ÷ 2) = 1,000 ، والأخير 1,000
التحقق: المسدَّد الكلي = 1,666 + 1,334 + 1,000 + 1,000 = 5,000 ✔
```

**معايير القبول**
- [ ] السداد الكلي يُقفل السلفة ويُلغي الأقساط المعلّقة فقط.
- [ ] مجموع كل الحركات (خصومات + سدادات مبكرة) يساوي أصل المبلغ عند الإقفال.
- [ ] لا يمكن تسجيل سداد يتجاوز الرصيد المتبقي.

---

#### AD-15 — سجل السلفة ودفتر الأرصدة

**الوصف**
يجب أن يوفّر النظام **سجلًا واضحًا (Advance Ledger)** لكل سلفة يعرض في أي لحظة: **أصل المبلغ · المسدَّد · المتبقي · حالة كل قسط**، مع أثر كامل لكل حركة.

**قواعد العمل**
- يجب أن يكون الدفتر **مضافًا فقط (Append-Only)**: لا تُعدَّل الحركات، وأي تصحيح يتم بحركة عكسية مرجعية.
- كل حركة تحمل: التاريخ · النوع (`DISBURSEMENT` / `INSTALLMENT_DEDUCTION` / `PARTIAL_DEDUCTION` / `EARLY_SETTLEMENT` / `REVERSAL` / `WRITE_OFF` / `FINAL_SETTLEMENT_OFFSET`) · المبلغ · **الرصيد بعد الحركة** · مرجع المصدر (المسير/المستند) · المستخدم المنفّذ.
- يجب أن تتحقق المعادلة في كل لحظة:

```
outstanding = disbursed_amount − Σ(repayments) − Σ(write_offs) + Σ(reversals)
```

- يجب أن يرى الموظف نسخة مبسّطة من سجله الخاص فقط، وأن ترى الموارد البشرية والمالية السجل الكامل.

**نموذج بيانات مقترح**

```sql
advance_policy(
  id, scope_type, scope_ref, salary_base, percent_of_salary, flat_cap_amount,
  max_requests_per_month, max_amount_per_month, max_outstanding_balance,
  month_definition, on_salary_change_before_disbursement, minimum_net_pay_floor,
  shortfall_behavior, max_installment_months, effective_from, effective_to,
  priority, is_active
)

advance(
  id, employee_id, request_date, requested_amount, approved_amount,
  disbursed_amount, disbursed_at, currency, is_exceptional, reason_category,
  reason_text, attachment_ref, policy_snapshot_json, effective_cap_at_request,
  effective_cap_at_approval, binding_constraint_code, status,
  created_by, approved_by, closed_at, close_reason
)

advance_installment_plan(
  id, advance_id, months_count, first_due_period_id, rounding_mode,
  regenerated_from_seq, created_at, created_by, change_reason
)

advance_installment(
  id, plan_id, seq, due_period_id, planned_amount, deducted_amount,
  carried_forward_amount, status, payroll_run_ref, note
)

advance_ledger_entry(
  id, advance_id, entry_date, entry_type, amount, running_balance,
  source_type, source_ref, performed_by, reversal_of_entry_id
)
```

**معايير القبول**
- [ ] شاشة السلفة تعرض أصل المبلغ والمسدَّد والمتبقي وحالة كل قسط في مكان واحد.
- [ ] لا توجد أي عملية تعديل مباشر على حركات الدفتر؛ التصحيح بحركة عكسية فقط.
- [ ] الرصيد المحسوب من الدفتر يطابق الرصيد المخزَّن في كل الاختبارات (اختبار مطابقة دوري).

**سيناريوهات اختبار مقترحة (السلف)**

| # | السيناريو | النتيجة المتوقعة |
|---|---|---|
| AD-T1 | طلب يساوي السقف الفعّال بالضبط | يُقبل |
| AD-T2 | طلب يزيد وحدة نقد واحدة عن السقف | يُرفض مع بيان القيد الحاكم |
| AD-T3 | طلبان متزامنان يستنفدان السقف معًا | أحدهما فقط ينجح |
| AD-T4 | تخفيض الراتب بين الاعتماد والصرف | يُطبَّق سلوك السياسة المعرَّف |
| AD-T5 | 5,000 على 3 أشهر | 1,666 / 1,666 / 1,668 والمجموع 5,000 |
| AD-T6 | صافٍ لا يكفي للقسط | خصم جزئي أو تخطٍّ بحسب الإعداد، ولا صافي سالب |
| AD-T7 | إنهاء خدمة برصيد قائم | ترحيل للتسوية النهائية ورصيد مدين إن لم تكفِ |
| AD-T8 | سداد مبكر كلي بعد قسطين | إقفال السلفة وإلغاء المعلّق ومجموع مطابق للأصل |
| AD-T9 | إلغاء ترحيل مسير بعد خصم قسط | عكس الحركة وإعادة القسط إلى `PENDING` |

---

### ب) العمل الإضافي (Overtime)

يجب أن يتعامل النظام مع العمل الإضافي عبر **ثلاثة مسارات متمايزة**: ضبط النوافذ الزمنية، والاكتشاف التلقائي من أجهزة البصمة، وطلب الموظف بنفسه — على أن تنتهي جميعها إلى **دورة اعتماد** موحّدة المبدأ قبل أي صرف.

#### OT-01 — نوافذ فتح وإغلاق العمل الإضافي بالتاريخ

**الوصف**
يجب أن يوفّر النظام كيان **نافذة عمل إضافي (Overtime Window)** يحدّد الفترات التي **يُسمح** فيها باحتساب الإضافي والفترات التي **يُمنع** فيها ذلك.

**قواعد العمل**

| الحقل | الوصف |
|---|---|
| `window_type` | `OPEN` (مفتوحة) أو `CLOSED` (مغلقة) |
| `date_from` / `date_to` | نطاق التواريخ (شامل الطرفين) |
| `scope_type` / `scope_ref` | الشركة · فرع · قسم · فريق · مركز تكلفة · مجموعة مخصّصة · موظف بعينه |
| `applies_to_day_types` | اختياري: تخصيص النافذة لأنواع أيام محدّدة (عمل/راحة/عطلة رسمية) |
| `note` | سبب/ملاحظة إدارية |
| `created_by` / `created_at` | أثر التدقيق |

- الحالة **الافتراضية** خارج أي نافذة معرَّفة يجب أن تكون قابلة للضبط على مستوى الجهة (`default_overtime_state`: مفتوح أو مغلق)، والقيمة الموصى بها: **مغلق**.
- يجب أن تدعم النوافذ التخصيص بالنطاق، فيجوز أن يكون الإضافي مفتوحًا لفرع ومغلقًا لآخر في التواريخ نفسها.
- يجب أن يعرض النظام **تقويمًا مرئيًا** لحالة الإضافي (مفتوح/مغلق) لكل نطاق، ليُعرف مسبقًا وضع أي يوم.

**معايير القبول**
- [ ] يوم يقع داخل نافذة `OPEN` يسمح بالاكتشاف التلقائي؛ ويوم خارجها لا يسمح.
- [ ] نافذة معرَّفة على فرع لا تؤثر على موظفي فرع آخر.
- [ ] تعديل نافذة لا يؤثر بأثر رجعي على سجلات إضافي **معتمدة** سابقًا.

---

#### OT-02 — ترجيح الإغلاق عند تداخل النوافذ

**الوصف**
عند تداخل أكثر من نافذة على اليوم والموظف نفسيهما، يجب أن **يغلب الإغلاق** على الفتح.

**قواعد العمل**

```
resolve_window(employee, date):
    matched = كل النوافذ السارية على (employee, date) أيًّا كان نطاقها
    إذا وُجدت نافذة واحدة على الأقل بنوع CLOSED  → الحالة = مغلق
    وإلا إذا وُجدت نافذة بنوع OPEN                → الحالة = مفتوح
    وإلا                                          → default_overtime_state
```

- قاعدة الترجيح هذه **مطلقة** ولا تتأثر بأخصّية النطاق: نافذة إغلاق على مستوى الشركة تغلب نافذة فتح على مستوى فرع، والعكس صحيح كذلك.
- يجب أن يعرض النظام عند الاستعلام عن يوم معيّن **النافذة الحاكمة ومصدرها**، حتى يُعرف سبب الإغلاق.

**مثال**

```
المعطيات: نافذة فتح على «فرع الشمال» من 01 إلى 30 من الشهر
          نافذة إغلاق على مستوى الشركة من 25 إلى 28 (جرد سنوي)
النتيجة  : الأيام 01–24 و29–30 مفتوحة ، والأيام 25–28 مغلقة لجميع الموظفين
```

**معايير القبول**
- [ ] تداخل نافذتي فتح وإغلاق يُنتج «مغلق» في كل الحالات المختبرة.
- [ ] واجهة الاستعلام تُظهر معرّف النافذة الحاكمة وسببها.

---

#### OT-03 — الاكتشاف التلقائي للعمل الإضافي من أجهزة البصمة

**الوصف**
داخل الفترة **المفتوحة**، يجب أن يكتشف النظام العمل الإضافي تلقائيًا من سجلات الحضور والانصراف (البصمات) دون تدخل يدوي، وأن ينشئ سجل إضافي بحالة «مكتشف — بانتظار الاعتماد».

**قواعد العمل**

```
scheduled_end   = وقت نهاية وردية الموظف في ذلك اليوم
actual_out      = آخر بصمة انصراف معتبرة في اليوم
raw_ot_minutes  = MAX( 0 , actual_out − scheduled_end )

إذا raw_ot_minutes < min_threshold_minutes → لا يُنشأ سجل إضافي
otherwise:
  counted_minutes = ROUND_DOWN( raw_ot_minutes , rounding_unit_minutes )
  counted_minutes = MIN( counted_minutes , max_daily_ot_minutes )
```

- **العتبة الدنيا (`min_threshold_minutes`)**: قابلة للضبط (مثال 30 دقيقة)، وتُطبَّق قبل التقريب؛ ما دونها لا يُحتسب إطلاقًا.
- **وحدة التقريب (`rounding_unit_minutes`)**: قابلة للضبط (مثال 15 دقيقة) مع اتجاه تقريب قابل للضبط (للأسفل افتراضيًا).
- يجب أن يوفّر النظام حدودًا سقفية: `max_daily_ot_minutes` و`max_monthly_ot_minutes`، وأن يُنبّه عند تجاوزها بدل الاحتساب الصامت.
- يجب أن تُستبعد من الاحتساب: البصمات المكرّرة خلال نافذة زمنية قصيرة، والبصمات غير المكتملة (دخول بلا خروج) — وتُدرَج هذه الحالات في قائمة استثناءات تحتاج معالجة يدوية.
- يجب أن يعمل الاكتشاف كـ**عملية دورية مجدولة** وكـ**عملية يدوية عند الطلب** لفترة محدّدة.

**مثال محسوب**

```
المعطيات: الوردية 09:00–18:00 ، آخر بصمة انصراف 20:35
          العتبة الدنيا = 30 دقيقة ، وحدة التقريب = 15 دقيقة للأسفل
الحساب  : الخام = 20:35 − 18:00 = 155 دقيقة (2 س 35 د)
          155 ≥ 30 ✔ → التقريب: FLOOR(155 ÷ 15) × 15 = 10 × 15 = 150 دقيقة
النتيجة : ساعات إضافي محتسبة = 150 دقيقة = 2.5 ساعة (بانتظار الاعتماد)
```

**حالات حدّية**
- انصراف قبل نهاية الوردية → لا إضافي (قيمة سالبة تُصفَّر).
- وردية ليلية تعبر منتصف الليل → يُنسب الإضافي إلى **يوم بدء الوردية**، ويجب أن تكون قاعدة النسبة موثّقة وقابلة للضبط.
- خروج ودخول متعدّد في اليوم → يُعتمد آخر انصراف معتبر، وتُخصم فترات الخروج المؤقت إن كانت السياسة تقتضي ذلك.
- بصمة انصراف بعد منتصف الليل بساعات غير معقولة تتجاوز حدًّا (`implausible_out_threshold`) → تُعلَّم للمراجعة ولا تُحتسب تلقائيًا.

**معايير القبول**
- [ ] 25 دقيقة إضافي مع عتبة 30 لا تُنشئ سجلًا.
- [ ] 155 دقيقة مع تقريب 15 تُنتج 150 دقيقة بالضبط.
- [ ] الاكتشاف داخل نافذة مغلقة لا يُنشئ أي سجل.
- [ ] السجل المُنشأ يحمل حالة «بانتظار الاعتماد» ولا يدخل أي حساب مالي بعد.

---

#### OT-04 — دورة اعتماد العمل الإضافي القابلة للتعريف

**الوصف**
يجب أن يمرّ كل سجل عمل إضافي — مكتشفًا كان أو مطلوبًا من الموظف — في **دورة اعتماد (Approval Flow) قابلة للتعريف** بعدد خطوات وأدوار يحدّدها المستخدم المخوَّل، مثل: المدير المباشر ← رئيس القسم ← الموارد البشرية.

**قواعد العمل**
- يجب أن يوفّر النظام محرّرًا لتعريف مسارات الاعتماد بالحقول: ترتيب الخطوة · الدور أو المستخدم المعتمد · شرط تفعيل الخطوة (اختياري، مثل: تُفعَّل إذا تجاوزت الساعات حدًّا) · مهلة الاستجابة · سلوك انتهاء المهلة (تصعيد / اعتماد ضمني / بقاء معلّقًا — والافتراضي: **البقاء معلّقًا**).
- يجب دعم **البديل عند الغياب (Delegate)** لكل معتمد.
- يجب أن يكون المسار قابلًا للتخصيص بالنطاق (فرع/قسم/نوع مسار الإضافي)، بحيث يمكن أن يختلف مسار اعتماد الإضافي المكتشف عن مسار طلب الموظف.
- **الرفض في أي خطوة يوقف السجل** ويعيده بحالة `REJECTED` مع سبب إلزامي، ولا يستأنف تلقائيًا.
- يجب دعم **التعديل عند الاعتماد**: للمعتمد المخوَّل تخفيض عدد الساعات المعتمدة عن المكتشفة، مع سبب؛ ويُحفَظ الرقمان معًا (`detected_minutes` و`approved_minutes`) لأن الفارق بينهما مادة تقرير (RP-07).
- يجب أن يُسجَّل لكل خطوة: المعتمد الفعلي · التاريخ والوقت · القرار · الملاحظة.

**معايير القبول**
- [ ] تعريف مسار من ثلاث خطوات يُنتج ثلاث خطوات اعتماد متسلسلة فعليًا.
- [ ] رفض في الخطوة الثانية يوقف السجل ولا يصل للخطوة الثالثة.
- [ ] تخفيض الساعات عند الاعتماد يحفظ القيمتين المكتشفة والمعتمدة.
- [ ] غياب المعتمد وتفعيل البديل يسمح باستمرار الدورة.

---

#### OT-05 — منع صرف أي عمل إضافي بلا اعتماد

**الوصف**
يجب ألّا يدخل أي عمل إضافي في احتساب الرواتب ما لم يكن قد **اكتمل اعتماده** بجميع خطوات مساره.

**قواعد العمل**
- الحالات المؤهلة للترحيل إلى المسير: `APPROVED` فقط. أما `DETECTED` و`PENDING_APPROVAL` و`REJECTED` و`CANCELLED` و`EXPIRED` فلا تُرحَّل بأي حال.
- يجب أن يعرض النظام قبل إقفال المسير قائمة **«إضافي معلّق داخل الفترة»** لتنبيه المسؤول قبل الترحيل.
- سجل الإضافي الذي يكتمل اعتماده **بعد إقفال المسير** يجب أن يُرحَّل إلى المسير التالي بوصفه بندًا بأثر رجعي (`retroactive_overtime`) موسومًا بفترة استحقاقه الأصلية، لا أن يُهمل.
- يجب تجميد سجل الإضافي المعتمد المرحَّل إلى مسير مُقفل ضد التعديل؛ وأي تصحيح يتم ببند تسوية في مسير لاحق.

**معايير القبول**
- [ ] احتساب مسير يتجاهل كل سجلات الإضافي غير المعتمدة.
- [ ] قائمة «إضافي معلّق» تظهر قبل الإقفال وتُحصي السجلات بدقة.
- [ ] الاعتماد بعد الإقفال ينتج بندًا بأثر رجعي في المسير التالي مع بيان الفترة الأصلية.

---

#### OT-06 — طلب الموظف احتساب عمل إضافي بنفسه

**الوصف**
يجب أن يوفّر النظام للموظف إمكانية **تقديم طلب احتساب عمل إضافي ليوم محدّد** — وبخاصة داخل الفترة **المغلقة** التي لا يعمل فيها الاكتشاف التلقائي — وأن **يرجع النظام إلى بصمات ذلك اليوم ويحسب الساعات الفعلية التي تجاوزت نهاية الدوام ويعرضها داخل الطلب**، ثم يمرّ الطلب في دورة الاعتماد.

**قواعد العمل**
- عند اختيار الموظف تاريخًا، يجب أن يُظهر النظام فورًا وقبل الإرسال:
  1. **وردية ذلك اليوم** ووقت بدايتها ونهايتها.
  2. **بصمات ذلك اليوم** (الدخول والخروج المعتبرة).
  3. **الساعات المحتسبة تلقائيًا** وفق معادلة OT-03 نفسها (العتبة والتقريب والسقف اليومي).
  4. **نوع اليوم** والمضاعف المتوقّع (OT-08).
- الرقم المعروض هو **رقم النظام لا رقم الموظف**: يجوز أن يذكر الموظف عدد ساعات مطلوبًا، لكن الاحتساب المرجعي يبقى المستخرَج من البصمات، ويُعرض الرقمان للمعتمد.
- يجب أن يكون **سبب الطلب** حقلًا إلزاميًا داخل الفترة المغلقة.
- الطلب يمرّ في دورة الاعتماد المعرَّفة (OT-04)، ولا يُصرف بغير اعتماد (OT-05).
- يجب أن يمنع النظام تقديم طلب على يوم يوجد له سجل إضافي غير مرفوض (OT-10).

**مثال محسوب**

```
المعطيات: التاريخ المطلوب داخل فترة مغلقة ، الوردية 08:00–17:00
          بصمات اليوم: دخول 07:52 ، خروج 19:20
          العتبة = 30 دقيقة ، التقريب = 15 دقيقة للأسفل
الحساب  : الخام = 19:20 − 17:00 = 140 دقيقة
          140 ≥ 30 ✔ → FLOOR(140 ÷ 15) × 15 = 9 × 15 = 135 دقيقة
النتيجة : يعرض الطلب «2 ساعة و15 دقيقة» محسوبة من البصمات ، ويُرسل للاعتماد
ملاحظة  : الحضور المبكر (08:00 − 07:52) لا يُحتسب إضافيًا ما لم تُفعَّل سياسة
          احتساب ما قبل بداية الوردية صراحة
```

**حالات حدّية — يجب تعريف سلوك صريح لكل منها**

| الحالة | السلوك المطلوب |
|---|---|
| **لا توجد بصمات في ذلك اليوم** | يُعرض «لا توجد بصمات» والساعات المحتسبة = صفر. يجب أن يكون قابلًا للضبط: **منع الإرسال** (الافتراضي)، أو السماح بإرساله بحالة `NO_PUNCH_EVIDENCE` تتطلب **اعتمادًا استثنائيًا بصلاحية أعلى** ويُوسم في تقرير الحوكمة |
| **بصمة دخول بلا خروج** | لا تُحتسب ساعات تلقائيًا؛ يُعلَّم الطلب `INCOMPLETE_PUNCH` ويتطلب تسوية سجل الحضور أولًا |
| **اليوم إجازة أسبوعية أو عطلة رسمية** | لا توجد «نهاية دوام» يُقاس عليها؛ تُحتسب **كل الدقائق المعمولة الموثّقة بالبصمة** إضافيًا (مع تطبيق العتبة والتقريب)، ويُطبَّق مضاعف نوع اليوم المقابل (OT-08) |
| **اليوم إجازة معتمدة للموظف (مدفوعة أو غير مدفوعة)** | يجب أن يكون السلوك قابلًا للضبط: منع الطلب، أو السماح مع وسم تعارض `LEAVE_CONFLICT` يظهر للمعتمد؛ ولا يجوز الجمع بين أجر الإجازة وأجر الإضافي عن اليوم نفسه دون قرار صريح موثّق |
| **اليوم مسجَّل غيابًا** | يُمنع الطلب حتى تُسوّى حالة الغياب؛ ووجود بصمات مع تسجيل غياب يُعامَل بوصفه تعارض بيانات يُعرض للمعتمد |
| **يوم يقع في مسير مُقفل** | يُقبل الطلب إن كان ضمن حد الأثر الرجعي، ويُرحَّل — عند اعتماده — بندًا بأثر رجعي في المسير التالي (OT-05) |
| **موظف مستثنى من الحضور** | يُمنع مسار الإضافي المستند إلى البصمة له، مع رسالة مفسِّرة |

- **الحد الأقصى للتقديم بأثر رجعي (`backdate_limit_days`)**: يجب أن يكون قابلًا للضبط (مثال 30 يومًا)، ويُحتسب من تاريخ اليوم المطلوب إلى تاريخ التقديم. ويجب دعم قيد إضافي مستقل: **منع التقديم على فترات مُقفلة ماليًا** تجاوزت عددًا محدّدًا من فترات المسير. وعند التعارض بين القيدين يُطبَّق **الأشد**.
- يجب أن يوفّر النظام إمكانية تجاوز حد الأثر الرجعي بصلاحية أعلى وسبب موثّق، ويظهر التجاوز في تقرير الحوكمة (RP-11).

**معايير القبول**
- [ ] اختيار تاريخ يعرض بصمات اليوم والساعات المحتسبة قبل الإرسال دون إدخال يدوي من الموظف.
- [ ] يوم بلا بصمات يُمنع إرساله في الوضع الافتراضي.
- [ ] يوم عطلة رسمية يُحتسب فيه كامل الوقت المعمول لا الفائض عن نهاية وردية.
- [ ] طلب على يوم يتجاوز حد الأثر الرجعي يُرفض تلقائيًا مع بيان الحد.
- [ ] الطلب يعرض للمعتمد رقم النظام ورقم الموظف المطلوب معًا عند اختلافهما.

---

#### OT-07 — تصنيف نوع اليوم ومصدر المعرفة

**الوصف**
يجب أن يحدّد النظام **نوع اليوم (Day Type)** لكل تاريخ ولكل موظف، لأنه أساس اختيار مضاعف الأجر.

**قواعد العمل**
- أنواع اليوم الأساسية: `REGULAR_WORKDAY` (يوم عمل عادي) · `WEEKLY_REST` (نهاية أسبوع/راحة أسبوعية) · `PUBLIC_HOLIDAY` (عطلة رسمية) — مع إمكانية إضافة أنواع مخصّصة.
- **ترتيب تحديد النوع (Resolution Order)** — يجب أن يكون بهذا التسلسل، والأعلى يَجُبّ ما دونه:

```
1) تقويم العطل الرسمية (Public Holiday Calendar) المخصّص بالنطاق (دولة/فرع)
2) جدول الراحة الأسبوعية للموظف المستمد من جدول ورديته/دوامه
3) وجود وردية مجدولة لليوم  → يوم عمل عادي
4) الافتراضي المعرَّف على مستوى الجهة
```

- يجب أن يكون **تقويم العطل الرسمية** كيانًا قابلًا للإدارة (إضافة/تعديل/استيراد سنوي) ومخصّصًا بالنطاق، لأن العطل تختلف بين الفروع والدول.
- يجب أن يدعم النظام حالة **العطلة الممتدة عبر أيام** وحالة **يوم عمل تعويضي** يُعامَل يوم عمل عادي رغم وقوعه في نهاية أسبوع.
- يجب أن يُخزَّن نوع اليوم المطبَّق **داخل سجل الإضافي نفسه (لقطة)** لا أن يُحسب لحظيًا عند العرض، حتى لا تتغيّر نتائج سجلات ماضية بتعديل التقويم لاحقًا.

**معايير القبول**
- [ ] إضافة عطلة رسمية لفرع لا تغيّر نوع اليوم لموظفي فرع آخر.
- [ ] يوم يقع في الراحة الأسبوعية ومعرَّف كيوم عمل تعويضي يُصنَّف يوم عمل عادي.
- [ ] تعديل تقويم العطل لا يغيّر نوع اليوم في سجلات إضافي معتمدة سابقًا.

---

#### OT-08 — مضاعفات أجر العمل الإضافي

**الوصف**
يجب أن يوفّر النظام **مضاعفات أجر (Overtime Multipliers)** قابلة للضبط بحسب نوع اليوم، تُطبَّق على أجر الساعة لاحتساب قيمة الإضافي.

**قواعد العمل**

| نوع اليوم | المفتاح | قيمة افتراضية مقترحة (قابلة للضبط) |
|---|---|---|
| يوم عمل عادي | `multiplier_regular_day` | 1.50 |
| نهاية أسبوع / راحة أسبوعية | `multiplier_weekly_rest` | 1.75 |
| عطلة رسمية | `multiplier_public_holiday` | 2.00 |

- يجب أن تكون المضاعفات قابلة للتخصيص بالنطاق (شركة/فرع/فئة تعاقدية) وبتاريخ سريان.
- يجب أن يكون **مصدر أجر الساعة** قابلًا للضبط بمعادلة معلنة:

```
hourly_rate = wage_base_for_overtime / ( days_per_month × standard_daily_hours )

حيث:
  wage_base_for_overtime : قابل للضبط — الأساسي فقط ، أو الأساسي + بدلات محدّدة
  days_per_month         : قابل للضبط (مثال 30)
  standard_daily_hours   : ساعات الدوام اليومية المعيارية (مثال 8)

overtime_amount = ROUND( hourly_rate × multiplier × approved_hours , currency_precision )
```

- يجب أن تُخزَّن **لقطة** من `hourly_rate` والمضاعف المطبَّق داخل سجل الإضافي عند الاعتماد، حتى لا تتغيّر قيمة معتمدة بتغيّر الراتب لاحقًا.
- يجب دعم تعدّد المضاعفات داخل اليوم الواحد إن اقتضت السياسة (مثل مضاعف أعلى للساعات الليلية) بوصفه امتدادًا اختياريًا، على أن يُقسَّم السجل إلى شرائح لكل منها مضاعفها.

**مثال محسوب**

```
المعطيات: الأجر المعتمد للإضافي = 9,000 وحدة نقد ، أيام الشهر = 30 ، الساعات اليومية = 8
الحساب  : أجر الساعة = 9,000 ÷ 30 ÷ 8 = 37.50 وحدة نقد

الحالة (أ) — يوم عمل عادي ، 2.5 ساعة معتمدة ، مضاعف 1.50
    37.50 × 1.50 = 56.25 لكل ساعة
    56.25 × 2.5   = 140.63 وحدة نقد

الحالة (ب) — راحة أسبوعية ، 6 ساعات معتمدة ، مضاعف 1.75
    37.50 × 1.75 = 65.625 لكل ساعة
    65.625 × 6    = 393.75 وحدة نقد

الحالة (ج) — عطلة رسمية ، 8 ساعات معتمدة ، مضاعف 2.00
    37.50 × 2.00 = 75.00 لكل ساعة
    75.00 × 8     = 600.00 وحدة نقد

الإجمالي في المسير = 140.63 + 393.75 + 600.00 = 1,134.38 وحدة نقد
```

**معايير القبول**
- [ ] تغيير المضاعف يؤثر على السجلات الجديدة فقط ولا يغيّر المعتمدة سابقًا.
- [ ] تغيير راتب الموظف بعد اعتماد الإضافي لا يغيّر قيمة الإضافي المعتمد.
- [ ] معادلة أجر الساعة معروضة للمستخدم المخوَّل وقابلة للضبط دون تعديل برمجي.
- [ ] قسيمة الراتب تعرض عدد الساعات والمضاعف والقيمة لكل نوع يوم على حدة.

---

#### OT-09 — سقوف العمل الإضافي والتنبيهات

**الوصف**
يجب أن يوفّر النظام سقوفًا للعمل الإضافي وآلية تنبيه عند الاقتراب منها أو تجاوزها.

**قواعد العمل**
- سقوف قابلة للضبط: يومي · أسبوعي · شهري · لكل موظف أو نطاق.
- عند بلوغ نسبة تنبيه (مثال 80%) يُرسل إشعار للمدير المعني.
- عند التجاوز، يجب أن يكون السلوك قابلًا للضبط: **منع الاعتماد** أو **السماح مع تسجيل استثناء موثّق** يظهر في RP-11.
- يجب أن يتيح النظام تقريرًا لأعلى النطاقات استهلاكًا للإضافي بغرض ضبط التكلفة.

**معايير القبول**
- [ ] تجاوز السقف الشهري يُنتج السلوك المعرَّف لا سلوكًا ضمنيًا.
- [ ] الاستثناء المسجَّل يظهر في تقرير الحوكمة بالمبرّر والمستخدم.

---

#### OT-10 — منع الازدواج بين المسارين

**الوصف**
يجب أن يضمن النظام **ألّا يُحتسب اليوم الواحد للموظف الواحد مرتين** عبر مسار الاكتشاف التلقائي ومسار طلب الموظف.

**قواعد العمل**
- يجب فرض قيد تفرّد منطقي: **لا يزيد سجل إضافي فعّال واحد لكل (موظف، تاريخ عمل)**، حيث «الفعّال» = أي حالة عدا `REJECTED` و`CANCELLED`.
- عند محاولة إنشاء سجل ثانٍ لليوم نفسه، يُطبَّق التالي:

| الحالة القائمة | السلوك المطلوب |
|---|---|
| سجل مكتشف بحالة `DETECTED` / `PENDING_APPROVAL` | يُمنع إنشاء طلب موظف؛ ويُوجَّه الموظف إلى السجل القائم لمتابعته |
| سجل بحالة `APPROVED` | يُمنع الإنشاء منعًا باتًا؛ وأي زيادة تتم بطلب **تعديل** على السجل القائم بمسار اعتماد |
| سجل بحالة `REJECTED` | يُسمح بإنشاء طلب موظف جديد، مع إظهار سبب الرفض السابق للمعتمد |
| طلب موظف قائم ثم فُتحت النافذة وعمل الاكتشاف | يجب أن **يتخطّى الاكتشاف** الأيام التي لها سجل فعّال، لا أن ينشئ سجلًا موازيًا |

- يجب أن يفحص الاكتشاف التلقائي وجود سجل فعّال **قبل** الإنشاء، وأن يكون هذا الفحص جزءًا من المعاملة الذرّية لمنع سباق التزامن.
- عند ترحيل الإضافي إلى المسير، يجب فحص ثانٍ يمنع ترحيل أكثر من سجل لليوم نفسه، ويُنتج خطأ احتساب مرئيًا بدل التمرير الصامت.

**معايير القبول**
- [ ] تشغيل الاكتشاف مرتين على الفترة نفسها لا يُنتج سجلات مكرّرة.
- [ ] طلب موظف على يوم له سجل مكتشف يُرفض مع إحالة إلى السجل القائم.
- [ ] المسير لا يحتوي أبدًا على بندي إضافي لليوم نفسه للموظف نفسه.

---

#### OT-11 — نموذج بيانات العمل الإضافي المقترح

```sql
overtime_window(
  id, window_type, scope_type, scope_ref, date_from, date_to,
  applies_to_day_types, note, created_by, created_at, is_active
)

overtime_policy(
  id, scope_type, scope_ref, min_threshold_minutes, rounding_unit_minutes,
  rounding_direction, max_daily_ot_minutes, max_weekly_ot_minutes,
  max_monthly_ot_minutes, backdate_limit_days, wage_base_for_overtime,
  days_per_month, standard_daily_hours, multiplier_regular_day,
  multiplier_weekly_rest, multiplier_public_holiday, on_cap_exceeded,
  allow_request_without_punch, effective_from, effective_to
)

day_type_calendar(
  id, scope_type, scope_ref, calendar_date, day_type, label, is_active
)

overtime_record(
  id, employee_id, work_date, origin,              -- DETECTED | EMPLOYEE_REQUEST
  window_ref, day_type_snapshot, first_in_punch, last_out_punch,
  scheduled_start, scheduled_end, raw_minutes, detected_minutes,
  claimed_minutes, approved_minutes, multiplier_applied,
  hourly_rate_snapshot, computed_amount, status, reason_text,
  exception_flags, payroll_run_ref, original_period_ref,
  created_at, created_by, closed_at
)

approval_flow_definition(
  id, flow_code, scope_type, scope_ref, applies_to_origin, is_active
)

approval_flow_step(
  id, flow_id, seq, approver_role, approver_ref, activation_condition,
  sla_hours, on_timeout_behavior, allow_amount_edit
)

overtime_approval_step(
  id, overtime_record_id, seq, approver_role, actual_approver_id,
  decision, decided_at, note, delegated_from
)
```

**سيناريوهات اختبار مقترحة (الإضافي)**

| # | السيناريو | النتيجة المتوقعة |
|---|---|---|
| OT-T1 | إضافي 25 دقيقة والعتبة 30 | لا يُنشأ سجل |
| OT-T2 | إضافي 155 دقيقة وتقريب 15 | 150 دقيقة |
| OT-T3 | نافذة فتح للفرع ونافذة إغلاق للشركة على اليوم نفسه | مغلق |
| OT-T4 | طلب موظف داخل فترة مغلقة على يوم له بصمات | يُعرض الاحتساب من البصمات ويمرّ للاعتماد |
| OT-T5 | طلب موظف على يوم بلا بصمات | يُمنع افتراضيًا |
| OT-T6 | طلب على عطلة رسمية بعمل 8 ساعات | يُحتسب كامل الوقت بمضاعف العطلة |
| OT-T7 | طلب بأثر رجعي يتجاوز الحد | يُرفض مع بيان الحد |
| OT-T8 | تشغيل الاكتشاف على يوم له طلب موظف قائم | لا يُنشأ سجل ثانٍ |
| OT-T9 | رفض في الخطوة الثانية من ثلاث | يتوقف السجل ولا يُصرف |
| OT-T10 | اعتماد بعد إقفال المسير | بند بأثر رجعي في المسير التالي |
| OT-T11 | تغيير الراتب بعد اعتماد الإضافي | قيمة الإضافي المعتمد لا تتغيّر |

---

### ج) التقارير والتحليلات (Reporting & Analytics)

#### RP-00 — المبادئ العامة لمنظومة التقارير

**الوصف**
يجب أن تقوم جميع التقارير على مبادئ موحّدة تضمن الاتساق والحوكمة.

**قواعد العمل**
- **مرشّحات قياسية مشتركة** في كل التقارير: الفترة/المسير · الفرع · القسم · الفريق · مركز التكلفة · حالة الموظف (على رأس العمل/منتهي) · نوع التعاقد.
- **نطاق البيانات محكوم بالصلاحية (Row-Level Scoping)**: لا يرى المستخدم إلا الصفوف الداخلة في نطاقه التنظيمي، بصرف النظر عن التقرير المطلوب.
- **مصدر واحد للحقيقة**: تُبنى التقارير على بيانات المسير المُرحَّل ولقطاتها، فلا تتغيّر أرقام تقرير عن فترة مُقفلة بتغيير الإعدادات لاحقًا.
- **قابلية التتبّع (Drill-down)**: كل رقم مجمّع يجب أن يكون قابلًا للنزول منه إلى مكوّناته حتى مستوى الموظف والبند والمستند المصدر.
- **إخفاء الحساسية**: التقارير التي تكشف مبالغ أجور فردية يجب أن تُقيَّد بأدوار محدّدة، وأن تدعم وضع «تجميعي بلا تفاصيل فردية» للأدوار الأدنى.
- كل تشغيل تقرير يُسجَّل في **أثر تدقيق** (من شغّله، متى، بأي مرشّحات، كم صفًّا).

**مستويات الصلاحية المقترحة**

| الدور | النطاق |
|---|---|
| `PAYROLL_ADMIN` | كل التقارير، كل الشركة، تفاصيل مالية كاملة |
| `HR_MANAGER` | كل التقارير في نطاقه، تفاصيل كاملة |
| `FINANCE` | التقارير المالية والتكلفة والتحليل، دون بيانات شخصية غير ضرورية |
| `BRANCH_MANAGER` | تقارير فرعه، تفاصيل مالية بحسب التخويل |
| `DEPT_MANAGER` | تقارير قسمه، بيانات حضور وخصومات وإضافي؛ مبالغ الأجور بحسب التخويل |
| `TEAM_LEAD` | فريقه، بيانات حضور وإضافي فقط، بلا مبالغ أجور |
| `AUDITOR` | قراءة فقط لكل التقارير مع تقارير الحوكمة والاستثناءات |
| `EMPLOYEE` | قسيمته الشخصية وسجل سلفه وطلبات إضافيه فقط |

**معايير القبول**
- [ ] مستخدم بدور `TEAM_LEAD` لا يرى مبالغ أجور في أي تقرير.
- [ ] تقرير عن فترة مُقفلة يُنتج الأرقام نفسها بعد تغيير الإعدادات.
- [ ] كل رقم مجمّع قابل للنزول إلى تفاصيله.

---

#### RP-01 — كشف رواتب المسير (تفصيلي)

| البند | المحتوى |
|---|---|
| **الغرض** | عرض احتساب الراتب لكل موظف في المسير بندًا بندًا |
| **السؤال الإداري** | «كيف تكوّن صافي راتب كل موظف في هذا المسير بالضبط؟» |
| **الأعمدة** | رقم الموظف · الاسم · الفرع · القسم · مركز التكلفة · الأجر الأساسي · البدلات (عمود لكل بدل أو عمود مجمّع قابل للفرد) · إجمالي الاستحقاق · خصم التأخير · خصم نقص الساعات · خصم الغياب · الخصومات حسب النوع (جودة/التزام/إداري/إنتاجية…) · قسط السلفة · المكافآت · قيمة العمل الإضافي · إجمالي الخصومات · **الصافي** · حالة الاحتساب · وجود إعفاء |
| **المرشّحات** | المسير · الفترة · الفرع/القسم/مركز التكلفة · نطاق الصافي (من–إلى) · «من عليه خصومات فقط» · «من له إضافي فقط» · «من عليه إعفاء فقط» |
| **الصلاحية** | `PAYROLL_ADMIN` · `HR_MANAGER` · `FINANCE` · `BRANCH_MANAGER` (نطاقه) |

**متطلبات إضافية**
- يجب أن يعرض الكشف **صفّ إجماليات** في نهايته، وأن تتطابق إجمالياته مع الكشف الملخّص (RP-02) تطابقًا تامًا.
- يجب أن يتيح كل مبلغ خصم أو إضافي النزول إلى **مصدره** (يوم الحضور، سجل الإضافي، مستند الخصم، قسط السلفة).

**معايير القبول**
- [ ] إجمالي الكشف التفصيلي = إجمالي الكشف الملخّص للمسير نفسه.
- [ ] النقر على أي قيمة خصم يفتح مصدرها.
- [ ] الكشف يعمل على مسير غير مرحَّل (وضع معاينة) وعلى مسير مرحَّل (وضع نهائي) مع تمييز واضح بينهما.

---

#### RP-02 — كشف رواتب المسير (ملخّص)

| البند | المحتوى |
|---|---|
| **الغرض** | صورة تجميعية للمسير للاعتماد والصرف |
| **السؤال الإداري** | «كم إجمالي ما سيُصرف في هذا المسير، وكيف يتوزّع؟» |
| **الأعمدة** | مستوى التجميع (فرع/قسم/مركز تكلفة) · عدد الموظفين · إجمالي الاستحقاقات · إجمالي البدلات · إجمالي الإضافي · إجمالي الخصومات حسب النوع · إجمالي أقساط السلف · **إجمالي الصافي** · نسبة الخصومات من الاستحقاق |
| **المرشّحات** | المسير · مستوى التجميع · الفرع/القسم |
| **الصلاحية** | `PAYROLL_ADMIN` · `HR_MANAGER` · `FINANCE` · `BRANCH_MANAGER` |

**معايير القبول**
- [ ] تغيير مستوى التجميع لا يغيّر الإجمالي الكلي.
- [ ] الكشف يعرض عدد الموظفين المحتسبين وعدد المستبعدين مع سبب الاستبعاد.

---

#### RP-03 — قسيمة الراتب الفردية (Payslip)

| البند | المحتوى |
|---|---|
| **الغرض** | مستند فردي يبيّن للموظف تفاصيل راتبه عن الفترة |
| **السؤال الإداري** | «لماذا صافي راتبي هذا الشهر بهذه القيمة؟» |
| **الأعمدة/الأقسام** | بيانات الموظف والفترة · الاستحقاقات (أساسي + بدلات مفصّلة) · الخصومات مفصّلة **بالنوع والسبب والتاريخ ومن أنزلها** · تفصيل الإضافي (التاريخ · نوع اليوم · الساعات · المضاعف · القيمة) · قسط السلفة مع «قسط س من ص» والرصيد المتبقي · المكافآت · الإعفاءات الممنوحة · الصافي · طريقة الصرف |
| **المرشّحات** | الموظف · الفترة/المسير |
| **الصلاحية** | `EMPLOYEE` (قسيمته فقط) · `HR_MANAGER` · `PAYROLL_ADMIN` · مديره المباشر بحسب التخويل |

**متطلبات إضافية**
- يجب أن تكون القسيمة **قابلة للتصدير PDF** بتنسيق ثابت، وأن تحمل مرجعًا فريدًا وتاريخ إصدار.
- يجب ألّا تُتاح قسيمة عن مسير غير مرحَّل إلا للأدوار الإدارية وبعلامة «مسودّة» ظاهرة.
- يجب أن تدعم القسيمة اللغتين العربية والإنجليزية بحسب تفضيل المستلم.

**معايير القبول**
- [ ] الموظف يرى قسيمته فقط ولا ينفذ إلى قسيمة غيره بأي وسيلة.
- [ ] مجموع بنود القسيمة = الصافي المعروض = صف الموظف في RP-01.
- [ ] قسيمة مسير غير مرحَّل تحمل وسم «مسودّة».

---

#### RP-04 — تقرير الخصومات حسب النوع والقسم والمدير المُنزِّل

| البند | المحتوى |
|---|---|
| **الغرض** | تحليل الخصومات المُنزَّلة يدويًا بأنواعها ومصادرها |
| **السؤال الإداري** | «من أنزل أي خصم، على من، بأي نوع وقيمة، وهل اعتُمد؟ وأي الأقسام أكثر إنزالًا للخصومات؟» |
| **الأعمدة** | نوع الخصم · القسم المُنزِّل · **المدير المُنزِّل** · الموظف المستهدف · قسم الموظف · القيمة · التاريخ · السبب · حالة الاعتماد · خطوات الاعتماد والقرارات · هل نزل في المسير · مرجع المسير |
| **المرشّحات** | الفترة · نوع الخصم · القسم المُنزِّل · المدير المُنزِّل · حالة الاعتماد · نطاق القيمة |
| **الصلاحية** | `HR_MANAGER` · `PAYROLL_ADMIN` · `AUDITOR` · `DEPT_MANAGER` (ما أنزله قسمه) |

**متطلبات إضافية**
- يجب أن يوفّر التقرير عرضًا محوريًا (Pivot) بمحورَي «نوع الخصم × القسم» وبمحورَي «المدير المُنزِّل × الشهر».
- يجب أن يُبرِز التقرير **الخصومات المرفوضة والملغاة** لا المعتمدة فقط، لأن نسبة الرفض مؤشّر إداري.

**معايير القبول**
- [ ] مجموع الخصومات المعتمدة في التقرير = مجموع الخصومات المقابلة في RP-01 للفترة نفسها.
- [ ] كل خصم يمكن تتبّعه إلى مستنده وخطوات اعتماده.

---

#### RP-05 — تقرير التأخير ونقص الساعات مقابل الخصم المطبَّق

| البند | المحتوى |
|---|---|
| **الغرض** | الربط بين واقعة الحضور والخصم الناتج عنها للتحقق من صحة تطبيق سياسة الخصم |
| **السؤال الإداري** | «هل الخصومات المطبَّقة تطابق سياسة الشرائح المعرَّفة؟ ومن أكثر الموظفين تأخّرًا؟» |
| **الأعمدة** | الموظف · القسم · التاريخ · الوردية · وقت الحضور المتوقّع · وقت الحضور الفعلي · **دقائق التأخير** · الساعات المطلوبة · الساعات المنجزة · **نقص الساعات** · هل طُبِّقت الساعة المرنة · الشريحة المطبَّقة · طريقة الخصم للشريحة · **قيمة الخصم** · هل مُنح إعفاء · مرجع المسير |
| **المرشّحات** | الفترة · الفرع/القسم/الموظف · حد أدنى لدقائق التأخير · الشريحة · «المُعفَون فقط» · «من طُبِّقت عليهم مرونة» |
| **الصلاحية** | `HR_MANAGER` · `PAYROLL_ADMIN` · `DEPT_MANAGER` (نطاقه) · `AUDITOR` |

**متطلبات إضافية**
- يجب أن يعرض التقرير **الشريحة الحاكمة** لكل واقعة صراحة، لأن ذلك أساس الاعتراض والمراجعة.
- يجب توفير ملخّص شهري لكل موظف: عدد مرات التأخير · إجمالي الدقائق · إجمالي الخصم.

**معايير القبول**
- [ ] لكل صف: قيمة الخصم قابلة لإعادة الاحتساب يدويًا من الشريحة والطريقة المعروضتين.
- [ ] الصفوف المُعفاة تظهر بقيمة خصم صفر مع بيان مرجع الإعفاء.

---

#### RP-06 — تقرير الغياب

| البند | المحتوى |
|---|---|
| **الغرض** | حصر أيام الغياب وتصنيفها وأثرها المالي |
| **السؤال الإداري** | «كم يوم غياب في الفترة، وكيف يتوزّع بين مبرّر وغير مبرّر، وكم كلّف؟» |
| **الأعمدة** | الموظف · القسم · التاريخ · تصنيف الغياب (بإذن/بدون إذن/إجازة غير مدفوعة/بلا بصمة) · هل يوجد مستند مبرّر · معامل الخصم المطبَّق · **قيمة الخصم** · هل مُنح إعفاء · مرجع المسير |
| **المرشّحات** | الفترة · التصنيف · الفرع/القسم/الموظف · «غير المبرّر فقط» · حد أدنى لعدد الأيام |
| **الصلاحية** | `HR_MANAGER` · `PAYROLL_ADMIN` · `DEPT_MANAGER` (نطاقه) · `AUDITOR` |

**متطلبات إضافية**
- يجب توفير عرض «تكرار الغياب» الذي يُبرز الموظفين المتجاوزين عتبة معيّنة في نافذة زمنية متحرّكة.

**معايير القبول**
- [ ] إجمالي خصومات الغياب في التقرير = إجمالي عمود خصم الغياب في RP-01.
- [ ] تغيير تصنيف يوم غياب ينعكس على القيمة عند إعادة احتساب المسير غير المرحَّل فقط.

---

#### RP-07 — تقرير العمل الإضافي (مكتشف مقابل مطلوب، معتمد مقابل مرفوض)

| البند | المحتوى |
|---|---|
| **الغرض** | متابعة دورة الإضافي بالكامل وتكلفته وسلوك الاعتماد |
| **السؤال الإداري** | «كم إضافيًا نشأ، من أي مسار، وكم اعتُمد وكم رُفض، وكم كلّفنا، ومن يعطّل الاعتماد؟» |
| **الأعمدة** | الموظف · القسم · تاريخ العمل · **المصدر** (مكتشف/طلب موظف) · نوع اليوم · حالة النافذة وقتها · الدقائق الخام · **الدقائق المكتشفة** · الدقائق المطلوبة من الموظف · **الدقائق المعتمدة** · الفرق (مكتشف − معتمد) · المضاعف · أجر الساعة المطبَّق · **القيمة** · الحالة · خطوات الاعتماد ومن اعتمد ومتى · مدة البقاء في كل خطوة · مرجع المسير |
| **المرشّحات** | الفترة · المصدر · الحالة · نوع اليوم · الفرع/القسم/الموظف · المعتمد · «المعلّق أكثر من N يومًا» · «المرفوض فقط» |
| **الصلاحية** | `HR_MANAGER` · `PAYROLL_ADMIN` · `FINANCE` · `DEPT_MANAGER` (نطاقه) · `TEAM_LEAD` (ساعات بلا مبالغ) · `AUDITOR` |

**متطلبات إضافية**
- يجب أن يوفّر التقرير **مؤشّرات تجميعية**: نسبة المعتمد إلى المكتشف · نسبة الرفض · متوسط زمن الاعتماد لكل خطوة · توزيع التكلفة حسب نوع اليوم.
- يجب أن يُبرِز السجلات ذات وسوم الاستثناء (`NO_PUNCH_EVIDENCE` / `INCOMPLETE_PUNCH` / `LEAVE_CONFLICT` / تجاوز السقف / تجاوز حد الأثر الرجعي).

**معايير القبول**
- [ ] إجمالي قيمة الإضافي المعتمد في التقرير = إجمالي عمود الإضافي في RP-01 للفترة نفسها.
- [ ] السجلات المرفوضة تظهر بقيمة صفر ولا تدخل الإجمالي المالي.
- [ ] مؤشّر متوسط زمن الاعتماد قابل للتفصيل لكل خطوة ومعتمد.

---

#### RP-08 — تقرير السلف والأرصدة القائمة وجدول الأقساط

| البند | المحتوى |
|---|---|
| **الغرض** | متابعة محفظة السلف والالتزامات المستحقة على الموظفين |
| **السؤال الإداري** | «كم إجمالي المديونية القائمة على الموظفين، وما جدول تحصيلها، وأي الأقساط تعثّرت؟» |
| **الأعمدة** | الموظف · القسم · رقم السلفة · نوعها (عادية/استثنائية) · تاريخ الصرف · **أصل المبلغ** · عدد الأقساط · **المسدَّد** · **المتبقي** · القسط الحالي (س من ص) · تاريخ استحقاق القسط التالي · **حالة كل قسط** · الأقساط المتعثّرة/الجزئية/المتخطّاة · تاريخ الإقفال المتوقّع · حالة السلفة |
| **المرشّحات** | الفترة · نوع السلفة · حالة السلفة · «ذوو رصيد قائم فقط» · «الأقساط المتعثّرة فقط» · نطاق الرصيد · الفرع/القسم |
| **الصلاحية** | `HR_MANAGER` · `PAYROLL_ADMIN` · `FINANCE` · `AUDITOR` · `EMPLOYEE` (سلفه فقط) |

**متطلبات إضافية**
- يجب توفير **عرض تقادُم (Aging)** للأرصدة القائمة بشرائح زمنية.
- يجب توفير **جدول تحصيل مستقبلي (Collection Forecast)** يبيّن المتوقّع خصمه في الفترات القادمة.
- يجب إفراد قسم لـ**الأرصدة المدينة بعد ترك الخدمة** (AD-13).

**معايير القبول**
- [ ] مجموع «المسدَّد + المتبقي» = أصل المبلغ لكل سلفة في كل الحالات.
- [ ] مجموع أقساط الفترة في التقرير = مجموع عمود قسط السلفة في RP-01.
- [ ] الأقساط الجزئية والمتخطّاة ظاهرة ومميّزة بصريًا.

---

#### RP-09 — تقرير البدلات السارية

| البند | المحتوى |
|---|---|
| **الغرض** | حصر البدلات الممنوحة وتكلفتها الدورية |
| **السؤال الإداري** | «من يستحق أي بدل، بأي قيمة، ومنذ متى، وكم يكلّفنا شهريًا؟» |
| **الأعمدة** | الموظف · القسم · نوع البدل · طريقة الاحتساب (مبلغ ثابت/نسبة) · القيمة · دورية الصرف · تاريخ بدء السريان · تاريخ الانتهاء إن وُجد · من منحه ومتى · مرجع القرار · هل صُرف في الفترة |
| **المرشّحات** | الفترة · نوع البدل · الفرع/القسم · «السارية فقط» · «المنتهية خلال الفترة» · «الممنوحة حديثًا» |
| **الصلاحية** | `HR_MANAGER` · `PAYROLL_ADMIN` · `FINANCE` · `BRANCH_MANAGER` (نطاقه) |

**متطلبات إضافية**
- يجب توفير ملخّص تكلفة شهرية لكل نوع بدل ولكل وحدة تنظيمية.
- يجب تنبيه إداري بالبدلات التي تنتهي خلال فترة قادمة قابلة للضبط.

**معايير القبول**
- [ ] إجمالي البدلات في التقرير = إجمالي أعمدة البدلات في RP-01 للفترة نفسها.
- [ ] بدل انتهى سريانه لا يظهر في قائمة «السارية».

---

#### RP-10 — تقرير المقارنة والانحراف (Variance) بين المسيرات والشهور

| البند | المحتوى |
|---|---|
| **الغرض** | كشف القفزات والانحرافات غير المتوقّعة بين فترة وأخرى قبل الصرف |
| **السؤال الإداري** | «ما الذي تغيّر عن الشهر الماضي، ومن الموظفون الذين قفزت أو هبطت رواتبهم، ولماذا؟» |
| **الأعمدة** | الموظف/الوحدة · قيمة الفترة الحالية · قيمة الفترة المقارَنة · **الفرق المطلق** · **الفرق النسبي %** · تصنيف سبب الفرق (بدل جديد/انتهاء بدل/إضافي/خصم جديد/قسط سلفة/تغيير أجر/انضمام/إنهاء خدمة/تغيير أيام العمل) · اتجاه الحركة |
| **المرشّحات** | المسير الحالي · المسير المقارَن · مستوى المقارنة (موظف/قسم/فرع/بند) · **عتبة الانحراف** (مثل: أظهر ما تجاوز 10% أو 500 وحدة نقد) · اتجاه الفرق (زيادة/نقص) · البند محل المقارنة |
| **الصلاحية** | `PAYROLL_ADMIN` · `HR_MANAGER` · `FINANCE` · `AUDITOR` |

**متطلبات إضافية**
- يجب أن يدعم التقرير المقارنة على مستوى **البند** لا الصافي فقط، لأن صافيًا ثابتًا قد يخفي تغييرين متعاكسين.
- يجب أن يحاول النظام **إسناد سبب** لكل انحراف تلقائيًا من الأحداث الواقعة في الفترة، ويعرض «غير مفسَّر» عند العجز — وهذه الفئة تحديدًا هي مادة المراجعة.
- يجب إتاحته **قبل ترحيل المسير** بوصفه أداة ضبط استباقية.

**مثال محسوب**

```
المعطيات: صافي الموظف في الفترة السابقة = 7,800 ، وفي الحالية = 9,150
الفرق المطلق = 9,150 − 7,800 = 1,350
الفرق النسبي = 1,350 ÷ 7,800 × 100 = 17.31%
العتبة المضبوطة = 10%  →  17.31% > 10%  →  يظهر الصف في التقرير
الإسناد التلقائي: + إضافي معتمد 1,134.38 ، + مكافأة 500 ، − قسط سلفة جديد 284.38
التحقق: 1,134.38 + 500 − 284.38 = 1,350.00 ✔  → الانحراف مفسَّر بالكامل
```

**معايير القبول**
- [ ] رفع العتبة يقلّل عدد الصفوف الظاهرة دون تغيير قيم الفروق.
- [ ] الانحراف المفسَّر بالكامل يُظهر مكوّنات تساوي الفرق بالضبط.
- [ ] الانحرافات «غير المفسَّرة» مفرزة في قائمة مستقلة.

---

#### RP-11 — تقرير الإعفاءات والاستثناءات (حوكمة)

| البند | المحتوى |
|---|---|
| **الغرض** | رقابة على كل خروج عن القاعدة داخل دورة الرواتب |
| **السؤال الإداري** | «من استُثني من القواعد، ومن أعفاه، ولماذا، وكم كلّفت الاستثناءات؟» |
| **الأعمدة** | نوع الاستثناء · الموظف · القسم · الفترة · القيمة المالية للأثر · **من نفّذه** · **السبب الموثّق** · المستند المرفق · تاريخ التنفيذ · هل مرّ باعتماد إضافي · مرجع المسير |
| **أنواع الاستثناءات المشمولة إلزامًا** | إعفاء من خصم التأخير · إعفاء من خصم الغياب · إعفاء من خصم بنوعه (جودة/التزام/إداري/إنتاجية) · إعفاء شامل لموظف · منح مكافأة فردية · استثناء من الحضور والانصراف · سلفة استثنائية تتجاوز السقف · تجاوز سقف المديونية القائمة · شطب رصيد سلفة · إعادة جدولة أقساط · اعتماد إضافي بلا بصمات · تجاوز حد الأثر الرجعي · تجاوز سقف الإضافي · تعديل يدوي على قيمة محتسبة · إعادة فتح مسير مُقفل |
| **المرشّحات** | الفترة · نوع الاستثناء · المنفّذ · الفرع/القسم · نطاق القيمة · «بلا سبب موثّق» |
| **الصلاحية** | `AUDITOR` · `PAYROLL_ADMIN` · `HR_MANAGER` (قراءة) — ولا يُتاح لمن يملك صلاحية منح الاستثناء أن يُخفي صفوفه منه |

**متطلبات إضافية**
- يجب أن يكون **إلزاميًا** تسجيل سبب نصّي لكل استثناء، وأن يُبرِز التقرير أي استثناء بلا سبب كافٍ.
- يجب توفير مؤشّر «تركّز الاستثناءات»: أكثر المنفّذين، وأكثر الموظفين استفادة، خلال نافذة زمنية.
- يجب أن يكون التقرير **غير قابل للحذف أو التعديل**، ويُبنى على مسار تدقيق مضاف فقط.

**معايير القبول**
- [ ] كل إعفاء أو تجاوز مذكور في القائمة يظهر في التقرير خلال الفترة نفسها دون استثناء.
- [ ] لا يمكن لأي دور حذف صف من التقرير أو تعديله.
- [ ] التقرير يحسب الأثر المالي التقديري لكل استثناء (قيمة الخصم المُعفى أو المبلغ المتجاوز).

---

#### RP-12 — تقرير «موظفون بلا مسير في الفترة»

| البند | المحتوى |
|---|---|
| **الغرض** | ضمان عدم سقوط أي موظف من التغطية بين المسيرات |
| **السؤال الإداري** | «هل هناك موظف على رأس العمل لم يُحتسب له راتب هذه الفترة؟ ولماذا؟» |
| **الأعمدة** | الموظف · القسم/الفرع · حالة التوظيف · تاريخ التعيين · تاريخ آخر يوم عمل إن وُجد · آخر مسير احتُسب له · **سبب الاستبعاد المرجَّح** (غير مشمول في نطاق أي مسير · مضاف بعد لقطة الأعضاء · بلا بنية أجر سارية · بيانات ناقصة · موقوف · فترته خارج نطاق المسير) · هل له أرصدة معلّقة (أقساط/إضافي معتمد) |
| **المرشّحات** | الفترة · الفرع/القسم · حالة التوظيف · نوع السبب |
| **الصلاحية** | `PAYROLL_ADMIN` · `HR_MANAGER` · `AUDITOR` |

**متطلبات إضافية**
- يجب أن يكون هذا التقرير **إلزامي العرض قبل إقفال أي مسير**، وأن يُطالب المسؤول بإقرار صريح إن كانت القائمة غير فارغة.
- يجب أن يُظهر التقرير كذلك **الموظفين المشمولين في أكثر من مسير** في الفترة نفسها (ازدواج تغطية) — وهو خطأ يجب منعه لا الإبلاغ عنه فقط.

**معايير القبول**
- [ ] موظف على رأس العمل غير مشمول في أي مسير للفترة يظهر في التقرير مع سبب.
- [ ] لا يمكن إقفال مسير والقائمة غير فارغة دون إقرار موثّق باسم المستخدم.
- [ ] ازدواج التغطية يُكتشف ويُمنع قبل الترحيل.

---

#### RP-13 — تقرير تكلفة الرواتب حسب الفرع/القسم/مركز التكلفة

| البند | المحتوى |
|---|---|
| **الغرض** | إسناد تكلفة الأجور إلى الوحدات التنظيمية والمالية لأغراض الموازنة والمحاسبة |
| **السؤال الإداري** | «كم تكلّف الرواتب في كل فرع/قسم/مركز تكلفة، وكيف تتوزّع مكوّناتها، وكيف تتحرك عبر الشهور؟» |
| **الأعمدة** | الوحدة (فرع/قسم/مركز تكلفة) · عدد الموظفين · **التكلفة الأساسية** · تكلفة البدلات · **تكلفة العمل الإضافي** · المكافآت · إجمالي الخصومات (مخفِّضة للتكلفة) · **صافي التكلفة** · التكلفة لكل موظف (متوسط) · نسبة تكلفة الإضافي من الإجمالي · مقارنة بالفترة السابقة |
| **المرشّحات** | الفترة أو مدى فترات · مستوى التجميع · الفرع/القسم/مركز التكلفة · نوع التعاقد · «إظهار الاتجاه عبر N فترة» |
| **الصلاحية** | `FINANCE` · `PAYROLL_ADMIN` · `HR_MANAGER` · `BRANCH_MANAGER` (نطاقه) |

**متطلبات إضافية**
- يجب أن يدعم التقرير **التوزيع على أكثر من مركز تكلفة** للموظف الواحد بنِسَب معرَّفة، وأن يكون مجموع النِسَب 100% دائمًا.
- يجب أن يوفّر عرضًا زمنيًا (اتجاه) عبر عدة فترات لا فترة واحدة.
- يجب أن يوفّر مخرجًا مناسبًا للترحيل المحاسبي (تجميع بحساب/مركز تكلفة).

**مثال محسوب**

```
المعطيات (قسم واحد ، فترة واحدة):
  الأساسي = 120,000 · البدلات = 18,000 · الإضافي = 9,400 · المكافآت = 3,000
  الخصومات (تأخير + غياب + خصومات بأنواع) = 4,600
الحساب : إجمالي التكلفة قبل الخصومات = 120,000 + 18,000 + 9,400 + 3,000 = 150,400
         صافي التكلفة = 150,400 − 4,600 = 145,800
         نسبة تكلفة الإضافي = 9,400 ÷ 150,400 × 100 = 6.25%
النتيجة: صافي تكلفة القسم = 145,800 وحدة نقد ، ووزن الإضافي 6.25%
```

**معايير القبول**
- [ ] مجموع تكاليف كل الوحدات = إجمالي المسير في RP-02.
- [ ] موظف موزّع على مركزي تكلفة بنسبة 60/40 تنعكس تكلفته بالنسبتين تمامًا.
- [ ] العرض الزمني عبر 6 فترات يُنتج 6 نقاط بيانات صحيحة.

---

#### RP-14 — تصدير التقارير (Excel / PDF)

**الوصف**
يجب أن يوفّر النظام تصديرًا لكل تقرير بصيغتي **Excel** و**PDF** على الأقل، مع الحفاظ على المرشّحات المطبَّقة.

**قواعد العمل**
- **Excel**: يجب أن يُصدَّر بصفوف بيانات خام قابلة للفرز والتصفية، وبأنواع بيانات صحيحة (الأرقام أرقامًا والتواريخ تواريخ لا نصوصًا)، مع صفحة أولى تحمل **بيانات التشغيل**: اسم التقرير · الفترة · المرشّحات المطبَّقة · تاريخ ووقت الإصدار · المستخدم · عدد الصفوف.
- **PDF**: يجب أن يُصدَّر بتنسيق مطبعي بترويسة وتذييل وترقيم صفحات ودعم كامل للاتجاه من اليمين لليسار في المحتوى العربي.
- يجب أن يعكس المُصدَّر **ما يراه المستخدم بصلاحيته** لا أكثر؛ فلا يجوز أن يكشف التصدير أعمدة أو صفوفًا محجوبة في العرض.
- التقارير كبيرة الحجم يجب أن تُصدَّر **بشكل غير متزامن** مع إشعار عند الجهوزية ورابط تنزيل محدود الصلاحية زمنيًا.
- يجب تسجيل كل عملية تصدير في أثر التدقيق (من، ماذا، متى، كم صفًّا) لأن التصدير هو نقطة تسرّب البيانات الأرجح.

**معايير القبول**
- [ ] ملف Excel المصدَّر يمكن فرزه وتصفيته وحساب مجاميعه دون تنظيف يدوي.
- [ ] ملف PDF بالعربية يظهر بمحاذاة واتجاه صحيحين.
- [ ] تصدير مستخدم محدود الصلاحية لا يحتوي بيانات خارج نطاقه.
- [ ] تقرير بعشرات آلاف الصفوف يُصدَّر دون انقطاع الجلسة.

---

#### RP-15 — جدولة التقارير وإرسالها دوريًا

**الوصف**
يجب أن يوفّر النظام إمكانية **جدولة تشغيل أي تقرير وإرساله تلقائيًا** إلى مستلمين محدّدين بدورية معرَّفة.

**قواعد العمل**

| الحقل | الوصف |
|---|---|
| `report_code` | التقرير المجدوَل |
| `filters_snapshot` | المرشّحات المحفوظة، مع دعم مرشّحات نسبية (الفترة الحالية · الفترة السابقة · آخر 3 فترات) |
| `frequency` | يومي · أسبوعي · شهري · عند إقفال كل مسير · تاريخ محدّد |
| `run_time` / `time_zone` | وقت التشغيل والمنطقة الزمنية |
| `recipients` | مستخدمون أو أدوار أو مجموعات بريدية |
| `format` | Excel · PDF · كليهما |
| `delivery_channel` | بريد إلكتروني · إشعار داخل النظام · إيداع في مجلد مؤمَّن |
| `is_active` | تفعيل/تعطيل |

- يجب أن يُطبَّق **نطاق صلاحية كل مستلم على حدة** على النسخة المرسَلة إليه؛ فلا يستقبل مستلم بيانات خارج نطاقه حتى لو كان مُنشئ الجدولة أوسع صلاحية.
- يجب دعم **مرشّحات نسبية** حتى لا تحتاج الجدولة تعديلًا شهريًا.
- يجب أن يُسجَّل كل تشغيل مجدوَل: وقت التنفيذ · النجاح/الفشل · سبب الفشل · المستلمون الفعليون، وأن يُعاد المحاولة تلقائيًا عند الفشل العابر بعدد محاولات قابل للضبط.
- يجب دعم **جدولة الإرسال المشروط** (مثال: أرسل تقرير الانحراف فقط إذا تجاوز عدد الصفوف صفرًا) لتقليل الضجيج.
- يجب أن يوقف النظام الجدولة تلقائيًا ويُخطر المالك إذا فقد المستلم صلاحيته أو تعطّل حسابه.

**معايير القبول**
- [ ] جدولة شهرية بمرشّح «الفترة السابقة» تُنتج التقرير الصحيح تلقائيًا دون تعديل يدوي.
- [ ] مستلمان بصلاحيتين مختلفتين يستلمان نسختين مختلفتي النطاق من الجدولة نفسها.
- [ ] فشل الإرسال يُسجَّل ويُعاد المحاولة ثم يُخطر مالك الجدولة.
- [ ] تعطيل حساب مستلم يوقف إرسال البيانات إليه فورًا.

---

#### RP-16 — نموذج بيانات التقارير المقترح

```sql
report_definition(
  id, report_code, name_ar, name_en, category, required_permission,
  supports_drilldown, default_filters_json, is_active
)

report_schedule(
  id, report_code, owner_user_id, filters_snapshot_json, relative_period_mode,
  frequency, cron_expression, run_time, time_zone, format, delivery_channel,
  send_condition, is_active, next_run_at, last_run_at, last_run_status
)

report_schedule_recipient(
  id, schedule_id, recipient_type, recipient_ref, applied_scope_snapshot
)

report_execution_log(
  id, report_code, schedule_id, executed_by, executed_at, filters_json,
  row_count, duration_ms, output_format, output_ref, status, error_message
)
```

**سيناريوهات اختبار مقترحة (التقارير)**

| # | السيناريو | النتيجة المتوقعة |
|---|---|---|
| RP-T1 | مطابقة إجماليات RP-01 مع RP-02 | تطابق تام |
| RP-T2 | مطابقة إجمالي الإضافي في RP-07 مع عمود الإضافي في RP-01 | تطابق تام |
| RP-T3 | مطابقة أقساط الفترة في RP-08 مع RP-01 | تطابق تام |
| RP-T4 | مستخدم بنطاق قسم يشغّل RP-13 | يرى قسمه فقط في العرض والتصدير |
| RP-T5 | تشغيل تقرير عن فترة مُقفلة بعد تغيير سياسة الخصومات | الأرقام لم تتغيّر |
| RP-T6 | إعفاء ممنوح لموظف | يظهر في RP-11 وفي RP-05/RP-06 كقيمة صفر بمرجع |
| RP-T7 | موظف على رأس العمل خارج كل المسيرات | يظهر في RP-12 ويمنع الإقفال بلا إقرار |
| RP-T8 | انحراف 17.31% مع عتبة 10% | يظهر في RP-10 مفسَّرًا بمكوّناته |
| RP-T9 | جدولة شهرية بمرشّح نسبي | تعمل تلقائيًا لفترتين متتاليتين دون تعديل |
| RP-T10 | تصدير 50,000 صف | ينجح غير متزامن مع إشعار ورابط محدود الصلاحية |